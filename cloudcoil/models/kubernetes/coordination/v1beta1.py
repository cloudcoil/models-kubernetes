# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    MicroTime,
    ObjectMeta,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class LeaseCandidateSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LeaseCandidateSpec"]:
            return LeaseCandidateSpec

        def build(self) -> "LeaseCandidateSpec":
            return LeaseCandidateSpec(**self._attrs)

        def binary_version(self, value: str, /) -> Self:
            "BinaryVersion is the binary version. It must be in a semver format without leading `v`. This field is required."
            return self._set("binary_version", value)

        def emulation_version(self, value: str | None, /) -> Self:
            'EmulationVersion is the emulation version. It must be in a semver format without leading `v`. EmulationVersion must be less than or equal to BinaryVersion. This field is required when strategy is "OldestEmulationVersion"'
            return self._set("emulation_version", value)

        def lease_name(self, value: str, /) -> Self:
            "LeaseName is the name of the lease for which this candidate is contending. The limits on this field are the same as on Lease.name. Multiple lease candidates may reference the same Lease.name. This field is immutable."
            return self._set("lease_name", value)

        @overload
        def ping_time(
            self, value_or_callback: MicroTime | None, /
        ) -> "LeaseCandidateSpec.Builder": ...

        @overload
        def ping_time(
            self,
            value_or_callback: Callable[[MicroTime.Builder], MicroTime.Builder | MicroTime],
            /,
        ) -> "LeaseCandidateSpec.Builder": ...

        @overload
        def ping_time(self, value_or_callback: Never = ...) -> "MicroTime.BuilderContext": ...

        def ping_time(self, value_or_callback=UNSET, /):
            "PingTime is the last time that the server has requested the LeaseCandidate to renew. It is only done during leader election to check if any LeaseCandidates have become ineligible. When PingTime is updated, the LeaseCandidate will respond by updating RenewTime."
            if self._in_context and value_or_callback is UNSET:
                context = MicroTime.BuilderContext()
                context._parent_builder = self
                context._field_name = "ping_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(MicroTime.builder())
                if isinstance(output, MicroTime.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("ping_time", value)

        @overload
        def renew_time(
            self, value_or_callback: MicroTime | None, /
        ) -> "LeaseCandidateSpec.Builder": ...

        @overload
        def renew_time(
            self,
            value_or_callback: Callable[[MicroTime.Builder], MicroTime.Builder | MicroTime],
            /,
        ) -> "LeaseCandidateSpec.Builder": ...

        @overload
        def renew_time(self, value_or_callback: Never = ...) -> "MicroTime.BuilderContext": ...

        def renew_time(self, value_or_callback=UNSET, /):
            "RenewTime is the time that the LeaseCandidate was last updated. Any time a Lease needs to do leader election, the PingTime field is updated to signal to the LeaseCandidate that they should update the RenewTime. Old LeaseCandidate objects are also garbage collected if it has been hours since the last renew. The PingTime field is updated regularly to prevent garbage collection for still active LeaseCandidates."
            if self._in_context and value_or_callback is UNSET:
                context = MicroTime.BuilderContext()
                context._parent_builder = self
                context._field_name = "renew_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(MicroTime.builder())
                if isinstance(output, MicroTime.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("renew_time", value)

        def strategy(self, value: str, /) -> Self:
            "Strategy is the strategy that coordinated leader election will use for picking the leader. If multiple candidates for the same Lease return different strategies, the strategy provided by the candidate with the latest BinaryVersion will be used. If there is still conflict, this is a user error and coordinated leader election will not operate the Lease until resolved."
            return self._set("strategy", value)

    class BuilderContext(BuilderContextBase["LeaseCandidateSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LeaseCandidateSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LeaseCandidateSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LeaseCandidateSpec", Builder]:
        return GenericListBuilder[LeaseCandidateSpec, LeaseCandidateSpec.Builder]()

    binary_version: Annotated[str, Field(alias="binaryVersion")]
    "BinaryVersion is the binary version. It must be in a semver format without leading `v`. This field is required."
    emulation_version: Annotated[str | None, Field(alias="emulationVersion")] = None
    'EmulationVersion is the emulation version. It must be in a semver format without leading `v`. EmulationVersion must be less than or equal to BinaryVersion. This field is required when strategy is "OldestEmulationVersion"'
    lease_name: Annotated[str, Field(alias="leaseName")]
    "LeaseName is the name of the lease for which this candidate is contending. The limits on this field are the same as on Lease.name. Multiple lease candidates may reference the same Lease.name. This field is immutable."
    ping_time: Annotated[MicroTime | None, Field(alias="pingTime")] = None
    "PingTime is the last time that the server has requested the LeaseCandidate to renew. It is only done during leader election to check if any LeaseCandidates have become ineligible. When PingTime is updated, the LeaseCandidate will respond by updating RenewTime."
    renew_time: Annotated[MicroTime | None, Field(alias="renewTime")] = None
    "RenewTime is the time that the LeaseCandidate was last updated. Any time a Lease needs to do leader election, the PingTime field is updated to signal to the LeaseCandidate that they should update the RenewTime. Old LeaseCandidate objects are also garbage collected if it has been hours since the last renew. The PingTime field is updated regularly to prevent garbage collection for still active LeaseCandidates."
    strategy: str
    "Strategy is the strategy that coordinated leader election will use for picking the leader. If multiple candidates for the same Lease return different strategies, the strategy provided by the candidate with the latest BinaryVersion will be used. If there is still conflict, this is a user error and coordinated leader election will not operate the Lease until resolved."


class LeaseCandidate(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "leasecandidates",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LeaseCandidate"]:
            return LeaseCandidate

        def build(self) -> "LeaseCandidate":
            return LeaseCandidate(**self._attrs)

        def api_version(self, value: Literal["coordination.k8s.io/v1beta1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["LeaseCandidate"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "LeaseCandidate.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "LeaseCandidate.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: LeaseCandidateSpec, /) -> "LeaseCandidate.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [LeaseCandidateSpec.Builder],
                LeaseCandidateSpec.Builder | LeaseCandidateSpec,
            ],
            /,
        ) -> "LeaseCandidate.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "LeaseCandidateSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec contains the specification of the Lease. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = LeaseCandidateSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LeaseCandidateSpec.builder())
                if isinstance(output, LeaseCandidateSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["LeaseCandidate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LeaseCandidate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LeaseCandidate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LeaseCandidate", Builder]:
        return GenericListBuilder[LeaseCandidate, LeaseCandidate.Builder]()

    api_version: Annotated[Literal["coordination.k8s.io/v1beta1"], Field(alias="apiVersion")] = (
        "coordination.k8s.io/v1beta1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["LeaseCandidate"] = "LeaseCandidate"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: LeaseCandidateSpec
    "spec contains the specification of the Lease. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


LeaseCandidateList = ResourceList[LeaseCandidate]
