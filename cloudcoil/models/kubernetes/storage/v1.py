# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    LabelSelector,
    ObjectMeta,
    Quantity,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field

from ..core import v1


class CSIStorageCapacity(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "csistoragecapacities",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSIStorageCapacity"]:
            return CSIStorageCapacity

        def build(self) -> "CSIStorageCapacity":
            return CSIStorageCapacity(**self._attrs)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        @overload
        def capacity(
            self, value_or_callback: Quantity | None, /
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def capacity(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def capacity(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def capacity(self, value_or_callback=UNSET, /):
            "capacity is the value reported by the CSI driver in its GetCapacityResponse for a GetCapacityRequest with topology and parameters that match the previous fields.\n\nThe semantic is currently (CSI spec 1.2) defined as: The available capacity, in bytes, of the storage that can be used to provision volumes. If not set, that information is currently unavailable."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "capacity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("capacity", value)

        def kind(self, value: Literal["CSIStorageCapacity"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def maximum_volume_size(
            self, value_or_callback: Quantity | None, /
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def maximum_volume_size(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def maximum_volume_size(
            self, value_or_callback: Never = ...
        ) -> "Quantity.BuilderContext": ...

        def maximum_volume_size(self, value_or_callback=UNSET, /):
            "maximumVolumeSize is the value reported by the CSI driver in its GetCapacityResponse for a GetCapacityRequest with topology and parameters that match the previous fields.\n\nThis is defined since CSI spec 1.4.0 as the largest size that may be used in a CreateVolumeRequest.capacity_range.required_bytes field to create a volume with the same parameters as those in GetCapacityRequest. The corresponding value in the Kubernetes API is ResourceRequirements.Requests in a volume claim."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "maximum_volume_size"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("maximum_volume_size", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. The name has no particular meaning. It must be a DNS subdomain (dots allowed, 253 characters). To ensure that there are no conflicts with other CSI drivers on the cluster, the recommendation is to use csisc-\u003cuuid\u003e, a generated name, or a reverse-domain name which ends with the unique CSI driver name.\n\nObjects are namespaced.\n\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def node_topology(
            self, value_or_callback: LabelSelector | None, /
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def node_topology(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "CSIStorageCapacity.Builder": ...

        @overload
        def node_topology(
            self, value_or_callback: Never = ...
        ) -> "LabelSelector.BuilderContext": ...

        def node_topology(self, value_or_callback=UNSET, /):
            "nodeTopology defines which nodes have access to the storage for which capacity was reported. If not set, the storage is not accessible from any node in the cluster. If empty, the storage is accessible from all nodes. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_topology"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_topology", value)

        def storage_class_name(self, value: str, /) -> Self:
            "storageClassName represents the name of the StorageClass that the reported capacity applies to. It must meet the same requirements as the name of a StorageClass object (non-empty, DNS subdomain). If that object no longer exists, the CSIStorageCapacity object is obsolete and should be removed by its creator. This field is immutable."
            return self._set("storage_class_name", value)

    class BuilderContext(BuilderContextBase["CSIStorageCapacity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSIStorageCapacity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSIStorageCapacity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSIStorageCapacity", Builder]:
        return GenericListBuilder[CSIStorageCapacity, CSIStorageCapacity.Builder]()

    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    capacity: Quantity | None = None
    "capacity is the value reported by the CSI driver in its GetCapacityResponse for a GetCapacityRequest with topology and parameters that match the previous fields.\n\nThe semantic is currently (CSI spec 1.2) defined as: The available capacity, in bytes, of the storage that can be used to provision volumes. If not set, that information is currently unavailable."
    kind: Literal["CSIStorageCapacity"] = "CSIStorageCapacity"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    maximum_volume_size: Annotated[Quantity | None, Field(alias="maximumVolumeSize")] = None
    "maximumVolumeSize is the value reported by the CSI driver in its GetCapacityResponse for a GetCapacityRequest with topology and parameters that match the previous fields.\n\nThis is defined since CSI spec 1.4.0 as the largest size that may be used in a CreateVolumeRequest.capacity_range.required_bytes field to create a volume with the same parameters as those in GetCapacityRequest. The corresponding value in the Kubernetes API is ResourceRequirements.Requests in a volume claim."
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. The name has no particular meaning. It must be a DNS subdomain (dots allowed, 253 characters). To ensure that there are no conflicts with other CSI drivers on the cluster, the recommendation is to use csisc-\u003cuuid\u003e, a generated name, or a reverse-domain name which ends with the unique CSI driver name.\n\nObjects are namespaced.\n\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    node_topology: Annotated[LabelSelector | None, Field(alias="nodeTopology")] = None
    "nodeTopology defines which nodes have access to the storage for which capacity was reported. If not set, the storage is not accessible from any node in the cluster. If empty, the storage is accessible from all nodes. This field is immutable."
    storage_class_name: Annotated[str, Field(alias="storageClassName")]
    "storageClassName represents the name of the StorageClass that the reported capacity applies to. It must meet the same requirements as the name of a StorageClass object (non-empty, DNS subdomain). If that object no longer exists, the CSIStorageCapacity object is obsolete and should be removed by its creator. This field is immutable."


CSIStorageCapacityList = ResourceList[CSIStorageCapacity]


class StorageClass(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "storageclasses",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageClass"]:
            return StorageClass

        def build(self) -> "StorageClass":
            return StorageClass(**self._attrs)

        def allow_volume_expansion(self, value: bool | None, /) -> Self:
            "allowVolumeExpansion shows whether the storage class allow volume expand."
            return self._set("allow_volume_expansion", value)

        @overload
        def allowed_topologies(
            self, value_or_callback: list[v1.TopologySelectorTerm] | None, /
        ) -> "StorageClass.Builder": ...

        @overload
        def allowed_topologies(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[v1.TopologySelectorTerm, v1.TopologySelectorTerm.Builder]],
                GenericListBuilder[v1.TopologySelectorTerm, v1.TopologySelectorTerm.Builder]
                | list[v1.TopologySelectorTerm],
            ],
            /,
        ) -> "StorageClass.Builder": ...

        @overload
        def allowed_topologies(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[v1.TopologySelectorTerm.Builder]: ...

        def allowed_topologies(self, value_or_callback=UNSET, /):
            "allowedTopologies restrict the node topologies where volumes can be dynamically provisioned. Each volume plugin defines its own supported topology specifications. An empty TopologySelectorTerm list means there is no topology restriction. This field is only honored by servers that enable the VolumeScheduling feature."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[v1.TopologySelectorTerm.Builder]()
                context._parent_builder = self
                context._field_name = "allowed_topologies"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.TopologySelectorTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("allowed_topologies", value)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["StorageClass"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "StorageClass.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "StorageClass.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        def mount_options(self, value: list[str] | None, /) -> Self:
            'mountOptions controls the mountOptions for dynamically provisioned PersistentVolumes of this storage class. e.g. ["ro", "soft"]. Not validated - mount of the PVs will simply fail if one is invalid.'
            return self._set("mount_options", value)

        def parameters(self, value: dict[str, str] | None, /) -> Self:
            "parameters holds the parameters for the provisioner that should create volumes of this storage class."
            return self._set("parameters", value)

        def provisioner(self, value: str, /) -> Self:
            "provisioner indicates the type of the provisioner."
            return self._set("provisioner", value)

        def reclaim_policy(self, value: str | None, /) -> Self:
            "reclaimPolicy controls the reclaimPolicy for dynamically provisioned PersistentVolumes of this storage class. Defaults to Delete."
            return self._set("reclaim_policy", value)

        def volume_binding_mode(self, value: str | None, /) -> Self:
            "volumeBindingMode indicates how PersistentVolumeClaims should be provisioned and bound.  When unset, VolumeBindingImmediate is used. This field is only honored by servers that enable the VolumeScheduling feature."
            return self._set("volume_binding_mode", value)

    class BuilderContext(BuilderContextBase["StorageClass.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageClass.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageClass."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageClass", Builder]:
        return GenericListBuilder[StorageClass, StorageClass.Builder]()

    allow_volume_expansion: Annotated[bool | None, Field(alias="allowVolumeExpansion")] = None
    "allowVolumeExpansion shows whether the storage class allow volume expand."
    allowed_topologies: Annotated[
        list[v1.TopologySelectorTerm] | None, Field(alias="allowedTopologies")
    ] = None
    "allowedTopologies restrict the node topologies where volumes can be dynamically provisioned. Each volume plugin defines its own supported topology specifications. An empty TopologySelectorTerm list means there is no topology restriction. This field is only honored by servers that enable the VolumeScheduling feature."
    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["StorageClass"] = "StorageClass"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    mount_options: Annotated[list[str] | None, Field(alias="mountOptions")] = None
    'mountOptions controls the mountOptions for dynamically provisioned PersistentVolumes of this storage class. e.g. ["ro", "soft"]. Not validated - mount of the PVs will simply fail if one is invalid.'
    parameters: dict[str, str] | None = None
    "parameters holds the parameters for the provisioner that should create volumes of this storage class."
    provisioner: str
    "provisioner indicates the type of the provisioner."
    reclaim_policy: Annotated[str | None, Field(alias="reclaimPolicy")] = None
    "reclaimPolicy controls the reclaimPolicy for dynamically provisioned PersistentVolumes of this storage class. Defaults to Delete."
    volume_binding_mode: Annotated[str | None, Field(alias="volumeBindingMode")] = None
    "volumeBindingMode indicates how PersistentVolumeClaims should be provisioned and bound.  When unset, VolumeBindingImmediate is used. This field is only honored by servers that enable the VolumeScheduling feature."


StorageClassList = ResourceList[StorageClass]


class StorageHealthCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageHealthCondition"]:
            return StorageHealthCondition

        def build(self) -> "StorageHealthCondition":
            return StorageHealthCondition(**self._attrs)

        def access_mode(self, value: str | None, /) -> Self:
            "accessMode is the access mode affected. Nil means all access modes are affected."
            return self._set("access_mode", value)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "StorageHealthCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "StorageHealthCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "lastTransitionTime is when this condition first appeared at its current state."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "message is a human-readable description. Maximum permitted length of a message is 1024 characters."
            return self._set("message", value)

        def reason(self, value: str, /) -> Self:
            "reason is a brief CamelCase machine-parseable reason. Maximum permitted length of a reason is 256 characters."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            'status is the health status category. One of "StorageUnreachable", "StorageDegraded".'
            return self._set("status", value)

        def volume_mode(self, value: str | None, /) -> Self:
            "volumeMode is the volume mode affected. Nil means both are affected."
            return self._set("volume_mode", value)

    class BuilderContext(BuilderContextBase["StorageHealthCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageHealthCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageHealthCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageHealthCondition", Builder]:
        return GenericListBuilder[StorageHealthCondition, StorageHealthCondition.Builder]()

    access_mode: Annotated[str | None, Field(alias="accessMode")] = None
    "accessMode is the access mode affected. Nil means all access modes are affected."
    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "lastTransitionTime is when this condition first appeared at its current state."
    message: str | None = None
    "message is a human-readable description. Maximum permitted length of a message is 1024 characters."
    reason: str
    "reason is a brief CamelCase machine-parseable reason. Maximum permitted length of a reason is 256 characters."
    status: str
    'status is the health status category. One of "StorageUnreachable", "StorageDegraded".'
    volume_mode: Annotated[str | None, Field(alias="volumeMode")] = None
    "volumeMode is the volume mode affected. Nil means both are affected."


class TokenRequest(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TokenRequest"]:
            return TokenRequest

        def build(self) -> "TokenRequest":
            return TokenRequest(**self._attrs)

        def audience(self, value: str, /) -> Self:
            'audience is the intended audience of the token in "TokenRequestSpec". It will default to the audiences of kube apiserver.'
            return self._set("audience", value)

        def expiration_seconds(self, value: int | None, /) -> Self:
            'expirationSeconds is the duration of validity of the token in "TokenRequestSpec". It has the same default value of "ExpirationSeconds" in "TokenRequestSpec".'
            return self._set("expiration_seconds", value)

    class BuilderContext(BuilderContextBase["TokenRequest.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TokenRequest.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TokenRequest."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TokenRequest", Builder]:
        return GenericListBuilder[TokenRequest, TokenRequest.Builder]()

    audience: str
    'audience is the intended audience of the token in "TokenRequestSpec". It will default to the audiences of kube apiserver.'
    expiration_seconds: Annotated[int | None, Field(alias="expirationSeconds")] = None
    'expirationSeconds is the duration of validity of the token in "TokenRequestSpec". It has the same default value of "ExpirationSeconds" in "TokenRequestSpec".'


class VolumeAttributesClass(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "volumeattributesclasses",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeAttributesClass"]:
            return VolumeAttributesClass

        def build(self) -> "VolumeAttributesClass":
            return VolumeAttributesClass(**self._attrs)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def driver_name(self, value: str, /) -> Self:
            "driverName is the name of the CSI driver This field is immutable."
            return self._set("driver_name", value)

        def kind(self, value: Literal["VolumeAttributesClass"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "VolumeAttributesClass.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "VolumeAttributesClass.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        def parameters(self, value: dict[str, str] | None, /) -> Self:
            'parameters hold volume attributes defined by the CSI driver. These values are opaque to the Kubernetes and are passed directly to the CSI driver. The underlying storage provider supports changing these attributes on an existing volume, however the parameters field itself is immutable. To invoke a volume update, a new VolumeAttributesClass should be created with new parameters, and the PersistentVolumeClaim should be updated to reference the new VolumeAttributesClass.\n\nThis field is required and must contain at least one key/value pair. The keys cannot be empty, and the maximum number of parameters is 512, with a cumulative max size of 256K. If the CSI driver rejects invalid parameters, the target PersistentVolumeClaim will be set to an "Infeasible" state in the modifyVolumeStatus field.'
            return self._set("parameters", value)

    class BuilderContext(BuilderContextBase["VolumeAttributesClass.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeAttributesClass.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeAttributesClass."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeAttributesClass", Builder]:
        return GenericListBuilder[VolumeAttributesClass, VolumeAttributesClass.Builder]()

    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    driver_name: Annotated[str, Field(alias="driverName")]
    "driverName is the name of the CSI driver This field is immutable."
    kind: Literal["VolumeAttributesClass"] = "VolumeAttributesClass"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    parameters: dict[str, str] | None = None
    'parameters hold volume attributes defined by the CSI driver. These values are opaque to the Kubernetes and are passed directly to the CSI driver. The underlying storage provider supports changing these attributes on an existing volume, however the parameters field itself is immutable. To invoke a volume update, a new VolumeAttributesClass should be created with new parameters, and the PersistentVolumeClaim should be updated to reference the new VolumeAttributesClass.\n\nThis field is required and must contain at least one key/value pair. The keys cannot be empty, and the maximum number of parameters is 512, with a cumulative max size of 256K. If the CSI driver rejects invalid parameters, the target PersistentVolumeClaim will be set to an "Infeasible" state in the modifyVolumeStatus field.'


VolumeAttributesClassList = ResourceList[VolumeAttributesClass]


class VolumeError(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeError"]:
            return VolumeError

        def build(self) -> "VolumeError":
            return VolumeError(**self._attrs)

        def error_code(self, value: int | None, /) -> Self:
            "errorCode is a numeric gRPC code representing the error encountered during Attach or Detach operations.\n\nThis field requires the MutableCSINodeAllocatableCount feature gate being enabled to be set."
            return self._set("error_code", value)

        def message(self, value: str | None, /) -> Self:
            "message represents the error encountered during Attach or Detach operation. This string may be logged, so it should not contain sensitive information."
            return self._set("message", value)

        @overload
        def time(self, value_or_callback: Time | None, /) -> "VolumeError.Builder": ...

        @overload
        def time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "VolumeError.Builder": ...

        @overload
        def time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def time(self, value_or_callback=UNSET, /):
            "time represents the time the error was encountered."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("time", value)

    class BuilderContext(BuilderContextBase["VolumeError.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeError.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeError."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeError", Builder]:
        return GenericListBuilder[VolumeError, VolumeError.Builder]()

    error_code: Annotated[int | None, Field(alias="errorCode")] = None
    "errorCode is a numeric gRPC code representing the error encountered during Attach or Detach operations.\n\nThis field requires the MutableCSINodeAllocatableCount feature gate being enabled to be set."
    message: str | None = None
    "message represents the error encountered during Attach or Detach operation. This string may be logged, so it should not contain sensitive information."
    time: Time | None = None
    "time represents the time the error was encountered."


class VolumeNodeResources(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeNodeResources"]:
            return VolumeNodeResources

        def build(self) -> "VolumeNodeResources":
            return VolumeNodeResources(**self._attrs)

        def count(self, value: int | None, /) -> Self:
            "count indicates the maximum number of unique volumes managed by the CSI driver that can be used on a node. A volume that is both attached and mounted on a node is considered to be used once, not twice. The same rule applies for a unique volume that is shared among multiple pods on the same node. If this field is not specified, then the supported number of volumes on this node is unbounded."
            return self._set("count", value)

    class BuilderContext(BuilderContextBase["VolumeNodeResources.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeNodeResources.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeNodeResources."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeNodeResources", Builder]:
        return GenericListBuilder[VolumeNodeResources, VolumeNodeResources.Builder]()

    count: int | None = None
    "count indicates the maximum number of unique volumes managed by the CSI driver that can be used on a node. A volume that is both attached and mounted on a node is considered to be used once, not twice. The same rule applies for a unique volume that is shared among multiple pods on the same node. If this field is not specified, then the supported number of volumes on this node is unbounded."


class CSIDriverSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSIDriverSpec"]:
            return CSIDriverSpec

        def build(self) -> "CSIDriverSpec":
            return CSIDriverSpec(**self._attrs)

        def attach_required(self, value: bool | None, /) -> Self:
            "attachRequired indicates this CSI volume driver requires an attach operation (because it implements the CSI ControllerPublishVolume() method), and that the Kubernetes attach detach controller should call the attach volume interface which checks the volumeattachment status and waits until the volume is attached before proceeding to mounting. The CSI external-attacher coordinates with CSI volume driver and updates the volumeattachment status when the attach operation is complete. If the value is specified to false, the attach operation will be skipped. Otherwise the attach operation will be called.\n\nThis field is immutable."
            return self._set("attach_required", value)

        def fs_group_policy(self, value: str | None, /) -> Self:
            "fsGroupPolicy defines if the underlying volume supports changing ownership and permission of the volume before being mounted. Refer to the specific FSGroupPolicy values for additional details.\n\nThis field was immutable in Kubernetes \u003c 1.29 and now is mutable.\n\nDefaults to ReadWriteOnceWithFSType, which will examine each volume to determine if Kubernetes should modify ownership and permissions of the volume. With the default policy the defined fsGroup will only be applied if a fstype is defined and the volume\u0027s access mode contains ReadWriteOnce."
            return self._set("fs_group_policy", value)

        def node_allocatable_update_period_seconds(self, value: int | None, /) -> Self:
            "nodeAllocatableUpdatePeriodSeconds specifies the interval between periodic updates of the CSINode allocatable capacity for this driver. When set, both periodic updates and updates triggered by capacity-related failures are enabled. If not set, no updates occur (neither periodic nor upon detecting capacity-related failures), and the allocatable.count remains static. The minimum allowed value for this field is 10 seconds.\n\nThis feature requires the MutableCSINodeAllocatableCount feature gate to be enabled.\n\nThis field is mutable."
            return self._set("node_allocatable_update_period_seconds", value)

        def pod_info_on_mount(self, value: bool | None, /) -> Self:
            'podInfoOnMount indicates this CSI volume driver requires additional pod information (like podName, podUID, etc.) during mount operations, if set to true. If set to false, pod information will not be passed on mount. Default is false.\n\nThe CSI driver specifies podInfoOnMount as part of driver deployment. If true, Kubelet will pass pod information as VolumeContext in the CSI NodePublishVolume() calls. The CSI driver is responsible for parsing and validating the information passed in as VolumeContext.\n\nThe following VolumeContext will be passed if podInfoOnMount is set to true. This list might grow, but the prefix will be used. "csi.storage.k8s.io/pod.name": pod.Name "csi.storage.k8s.io/pod.namespace": pod.Namespace "csi.storage.k8s.io/pod.uid": string(pod.UID) "csi.storage.k8s.io/ephemeral": "true" if the volume is an ephemeral inline volume\n                                defined by a CSIVolumeSource, otherwise "false"\n\n"csi.storage.k8s.io/ephemeral" is a new feature in Kubernetes 1.16. It is only required for drivers which support both the "Persistent" and "Ephemeral" VolumeLifecycleMode. Other drivers can leave pod info disabled and/or ignore this field. As Kubernetes 1.15 doesn\u0027t support this field, drivers can only support one mode when deployed on such a cluster and the deployment determines which mode that is, for example via a command line parameter of the driver.\n\nThis field was immutable in Kubernetes \u003c 1.29 and now is mutable.'
            return self._set("pod_info_on_mount", value)

        def prevent_pod_scheduling_if_missing(self, value: bool | None, /) -> Self:
            'preventPodSchedulingIfMissing indicates that the CSI driver wants to prevent pod scheduling if the CSI driver on the node is missing.\n\nEnabling this option will prevent the scheduler (or any other component which embeds default scheduler such as cluster-autoscaler) from scheduling pods to nodes where CSI driver is not installed.\n\nFor components(such as cluster-autoscaler) that embed the scheduler and run pod placement simulations using scheduler plugins, they MUST be aware of CSI driver registration information via CSINode object. They must create simulated CSINode objects in addition to Node objects during scheduling simulation, otherwise if PreventPodSchedulingIfMissing is enabled globally for CSIDriver object, any newly created node may be rejected by the scheduler because of missing CSI driver information from the node.\n\nThis is a beta feature and requires the VolumeLimitScaling feature gate to be enabled. Default is "false".'
            return self._set("prevent_pod_scheduling_if_missing", value)

        def requires_republish(self, value: bool | None, /) -> Self:
            "requiresRepublish indicates the CSI driver wants `NodePublishVolume` being periodically called to reflect any possible change in the mounted volume. This field defaults to false.\n\nNote: After a successful initial NodePublishVolume call, subsequent calls to NodePublishVolume should only update the contents of the volume. New mount points will not be seen by a running container."
            return self._set("requires_republish", value)

        def se_linux_mount(self, value: bool | None, /) -> Self:
            'seLinuxMount specifies if the CSI driver supports "-o context" mount option.\n\nWhen "true", the CSI driver must ensure that all volumes provided by this CSI driver can be mounted separately with different `-o context` options. This is typical for storage backends that provide volumes as filesystems on block devices or as independent shared volumes. Kubernetes will call NodeStage / NodePublish with "-o context=xyz" mount option when mounting a ReadWriteOncePod volume used in Pod that has explicitly set SELinux context. In the future, it may be expanded to other volume AccessModes. In any case, Kubernetes will ensure that the volume is mounted only with a single SELinux context.\n\nWhen "false", Kubernetes won\u0027t pass any special SELinux mount options to the driver. This is typical for volumes that represent subdirectories of a bigger shared filesystem.\n\nDefault is "false".'
            return self._set("se_linux_mount", value)

        def service_account_token_in_secrets(self, value: bool | None, /) -> Self:
            'serviceAccountTokenInSecrets is an opt-in for CSI drivers to indicate that service account tokens should be passed via the Secrets field in NodePublishVolumeRequest instead of the VolumeContext field. The CSI specification provides a dedicated Secrets field for sensitive information like tokens, which is the appropriate mechanism for handling credentials. This addresses security concerns where sensitive tokens were being logged as part of volume context.\n\nWhen "true", kubelet will pass the tokens only in the Secrets field with the key "csi.storage.k8s.io/serviceAccount.tokens". The CSI driver must be updated to read tokens from the Secrets field instead of VolumeContext.\n\nWhen "false" or not set, kubelet will pass the tokens in VolumeContext with the key "csi.storage.k8s.io/serviceAccount.tokens" (existing behavior). This maintains backward compatibility with existing CSI drivers.\n\nThis field can only be set when TokenRequests is configured. The API server will reject CSIDriver specs that set this field without TokenRequests.\n\nDefault behavior if unset is to pass tokens in the VolumeContext field.'
            return self._set("service_account_token_in_secrets", value)

        def storage_capacity(self, value: bool | None, /) -> Self:
            "storageCapacity indicates that the CSI volume driver wants pod scheduling to consider the storage capacity that the driver deployment will report by creating CSIStorageCapacity objects with capacity information, if set to true.\n\nThe check can be enabled immediately when deploying a driver. In that case, provisioning new volumes with late binding will pause until the driver deployment has published some suitable CSIStorageCapacity object.\n\nAlternatively, the driver can be deployed with the field unset or false and it can be flipped later when storage capacity information has been published.\n\nThis field was immutable in Kubernetes \u003c= 1.22 and now is mutable."
            return self._set("storage_capacity", value)

        @overload
        def token_requests(
            self, value_or_callback: list[TokenRequest] | None, /
        ) -> "CSIDriverSpec.Builder": ...

        @overload
        def token_requests(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TokenRequest, TokenRequest.Builder]],
                GenericListBuilder[TokenRequest, TokenRequest.Builder] | list[TokenRequest],
            ],
            /,
        ) -> "CSIDriverSpec.Builder": ...

        @overload
        def token_requests(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TokenRequest.Builder]: ...

        def token_requests(self, value_or_callback=UNSET, /):
            'tokenRequests indicates the CSI driver needs pods\u0027 service account tokens it is mounting volume for to do necessary authentication. Kubelet will pass the tokens in VolumeContext in the CSI NodePublishVolume calls. The CSI driver should parse and validate the following VolumeContext: "csi.storage.k8s.io/serviceAccount.tokens": {\n  "\u003caudience\u003e": {\n    "token": \u003ctoken\u003e,\n    "expirationTimestamp": \u003cexpiration timestamp in RFC3339\u003e,\n  },\n  ...\n}\n\nNote: Audience in each TokenRequest should be different and at most one token is empty string. To receive a new token after expiry, RequiresRepublish can be used to trigger NodePublishVolume periodically.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TokenRequest.Builder]()
                context._parent_builder = self
                context._field_name = "token_requests"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TokenRequest.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("token_requests", value)

        def volume_lifecycle_modes(self, value: list[str] | None, /) -> Self:
            'volumeLifecycleModes defines what kind of volumes this CSI volume driver supports. The default if the list is empty is "Persistent", which is the usage defined by the CSI specification and implemented in Kubernetes via the usual PV/PVC mechanism.\n\nThe other mode is "Ephemeral". In this mode, volumes are defined inline inside the pod spec with CSIVolumeSource and their lifecycle is tied to the lifecycle of that pod. A driver has to be aware of this because it is only going to get a NodePublishVolume call for such a volume.\n\nFor more information about implementing this mode, see https://kubernetes-csi.github.io/docs/ephemeral-local-volumes.html A driver can support one or more of these modes and more modes may be added in the future.\n\nThis field is beta. This field is immutable.'
            return self._set("volume_lifecycle_modes", value)

    class BuilderContext(BuilderContextBase["CSIDriverSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSIDriverSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSIDriverSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSIDriverSpec", Builder]:
        return GenericListBuilder[CSIDriverSpec, CSIDriverSpec.Builder]()

    attach_required: Annotated[bool | None, Field(alias="attachRequired")] = None
    "attachRequired indicates this CSI volume driver requires an attach operation (because it implements the CSI ControllerPublishVolume() method), and that the Kubernetes attach detach controller should call the attach volume interface which checks the volumeattachment status and waits until the volume is attached before proceeding to mounting. The CSI external-attacher coordinates with CSI volume driver and updates the volumeattachment status when the attach operation is complete. If the value is specified to false, the attach operation will be skipped. Otherwise the attach operation will be called.\n\nThis field is immutable."
    fs_group_policy: Annotated[str | None, Field(alias="fsGroupPolicy")] = None
    "fsGroupPolicy defines if the underlying volume supports changing ownership and permission of the volume before being mounted. Refer to the specific FSGroupPolicy values for additional details.\n\nThis field was immutable in Kubernetes \u003c 1.29 and now is mutable.\n\nDefaults to ReadWriteOnceWithFSType, which will examine each volume to determine if Kubernetes should modify ownership and permissions of the volume. With the default policy the defined fsGroup will only be applied if a fstype is defined and the volume\u0027s access mode contains ReadWriteOnce."
    node_allocatable_update_period_seconds: Annotated[
        int | None, Field(alias="nodeAllocatableUpdatePeriodSeconds")
    ] = None
    "nodeAllocatableUpdatePeriodSeconds specifies the interval between periodic updates of the CSINode allocatable capacity for this driver. When set, both periodic updates and updates triggered by capacity-related failures are enabled. If not set, no updates occur (neither periodic nor upon detecting capacity-related failures), and the allocatable.count remains static. The minimum allowed value for this field is 10 seconds.\n\nThis feature requires the MutableCSINodeAllocatableCount feature gate to be enabled.\n\nThis field is mutable."
    pod_info_on_mount: Annotated[bool | None, Field(alias="podInfoOnMount")] = None
    'podInfoOnMount indicates this CSI volume driver requires additional pod information (like podName, podUID, etc.) during mount operations, if set to true. If set to false, pod information will not be passed on mount. Default is false.\n\nThe CSI driver specifies podInfoOnMount as part of driver deployment. If true, Kubelet will pass pod information as VolumeContext in the CSI NodePublishVolume() calls. The CSI driver is responsible for parsing and validating the information passed in as VolumeContext.\n\nThe following VolumeContext will be passed if podInfoOnMount is set to true. This list might grow, but the prefix will be used. "csi.storage.k8s.io/pod.name": pod.Name "csi.storage.k8s.io/pod.namespace": pod.Namespace "csi.storage.k8s.io/pod.uid": string(pod.UID) "csi.storage.k8s.io/ephemeral": "true" if the volume is an ephemeral inline volume\n                                defined by a CSIVolumeSource, otherwise "false"\n\n"csi.storage.k8s.io/ephemeral" is a new feature in Kubernetes 1.16. It is only required for drivers which support both the "Persistent" and "Ephemeral" VolumeLifecycleMode. Other drivers can leave pod info disabled and/or ignore this field. As Kubernetes 1.15 doesn\u0027t support this field, drivers can only support one mode when deployed on such a cluster and the deployment determines which mode that is, for example via a command line parameter of the driver.\n\nThis field was immutable in Kubernetes \u003c 1.29 and now is mutable.'
    prevent_pod_scheduling_if_missing: Annotated[
        bool | None, Field(alias="preventPodSchedulingIfMissing")
    ] = None
    'preventPodSchedulingIfMissing indicates that the CSI driver wants to prevent pod scheduling if the CSI driver on the node is missing.\n\nEnabling this option will prevent the scheduler (or any other component which embeds default scheduler such as cluster-autoscaler) from scheduling pods to nodes where CSI driver is not installed.\n\nFor components(such as cluster-autoscaler) that embed the scheduler and run pod placement simulations using scheduler plugins, they MUST be aware of CSI driver registration information via CSINode object. They must create simulated CSINode objects in addition to Node objects during scheduling simulation, otherwise if PreventPodSchedulingIfMissing is enabled globally for CSIDriver object, any newly created node may be rejected by the scheduler because of missing CSI driver information from the node.\n\nThis is a beta feature and requires the VolumeLimitScaling feature gate to be enabled. Default is "false".'
    requires_republish: Annotated[bool | None, Field(alias="requiresRepublish")] = None
    "requiresRepublish indicates the CSI driver wants `NodePublishVolume` being periodically called to reflect any possible change in the mounted volume. This field defaults to false.\n\nNote: After a successful initial NodePublishVolume call, subsequent calls to NodePublishVolume should only update the contents of the volume. New mount points will not be seen by a running container."
    se_linux_mount: Annotated[bool | None, Field(alias="seLinuxMount")] = None
    'seLinuxMount specifies if the CSI driver supports "-o context" mount option.\n\nWhen "true", the CSI driver must ensure that all volumes provided by this CSI driver can be mounted separately with different `-o context` options. This is typical for storage backends that provide volumes as filesystems on block devices or as independent shared volumes. Kubernetes will call NodeStage / NodePublish with "-o context=xyz" mount option when mounting a ReadWriteOncePod volume used in Pod that has explicitly set SELinux context. In the future, it may be expanded to other volume AccessModes. In any case, Kubernetes will ensure that the volume is mounted only with a single SELinux context.\n\nWhen "false", Kubernetes won\u0027t pass any special SELinux mount options to the driver. This is typical for volumes that represent subdirectories of a bigger shared filesystem.\n\nDefault is "false".'
    service_account_token_in_secrets: Annotated[
        bool | None, Field(alias="serviceAccountTokenInSecrets")
    ] = None
    'serviceAccountTokenInSecrets is an opt-in for CSI drivers to indicate that service account tokens should be passed via the Secrets field in NodePublishVolumeRequest instead of the VolumeContext field. The CSI specification provides a dedicated Secrets field for sensitive information like tokens, which is the appropriate mechanism for handling credentials. This addresses security concerns where sensitive tokens were being logged as part of volume context.\n\nWhen "true", kubelet will pass the tokens only in the Secrets field with the key "csi.storage.k8s.io/serviceAccount.tokens". The CSI driver must be updated to read tokens from the Secrets field instead of VolumeContext.\n\nWhen "false" or not set, kubelet will pass the tokens in VolumeContext with the key "csi.storage.k8s.io/serviceAccount.tokens" (existing behavior). This maintains backward compatibility with existing CSI drivers.\n\nThis field can only be set when TokenRequests is configured. The API server will reject CSIDriver specs that set this field without TokenRequests.\n\nDefault behavior if unset is to pass tokens in the VolumeContext field.'
    storage_capacity: Annotated[bool | None, Field(alias="storageCapacity")] = None
    "storageCapacity indicates that the CSI volume driver wants pod scheduling to consider the storage capacity that the driver deployment will report by creating CSIStorageCapacity objects with capacity information, if set to true.\n\nThe check can be enabled immediately when deploying a driver. In that case, provisioning new volumes with late binding will pause until the driver deployment has published some suitable CSIStorageCapacity object.\n\nAlternatively, the driver can be deployed with the field unset or false and it can be flipped later when storage capacity information has been published.\n\nThis field was immutable in Kubernetes \u003c= 1.22 and now is mutable."
    token_requests: Annotated[list[TokenRequest] | None, Field(alias="tokenRequests")] = None
    'tokenRequests indicates the CSI driver needs pods\u0027 service account tokens it is mounting volume for to do necessary authentication. Kubelet will pass the tokens in VolumeContext in the CSI NodePublishVolume calls. The CSI driver should parse and validate the following VolumeContext: "csi.storage.k8s.io/serviceAccount.tokens": {\n  "\u003caudience\u003e": {\n    "token": \u003ctoken\u003e,\n    "expirationTimestamp": \u003cexpiration timestamp in RFC3339\u003e,\n  },\n  ...\n}\n\nNote: Audience in each TokenRequest should be different and at most one token is empty string. To receive a new token after expiry, RequiresRepublish can be used to trigger NodePublishVolume periodically.'
    volume_lifecycle_modes: Annotated[list[str] | None, Field(alias="volumeLifecycleModes")] = None
    'volumeLifecycleModes defines what kind of volumes this CSI volume driver supports. The default if the list is empty is "Persistent", which is the usage defined by the CSI specification and implemented in Kubernetes via the usual PV/PVC mechanism.\n\nThe other mode is "Ephemeral". In this mode, volumes are defined inline inside the pod spec with CSIVolumeSource and their lifecycle is tied to the lifecycle of that pod. A driver has to be aware of this because it is only going to get a NodePublishVolume call for such a volume.\n\nFor more information about implementing this mode, see https://kubernetes-csi.github.io/docs/ephemeral-local-volumes.html A driver can support one or more of these modes and more modes may be added in the future.\n\nThis field is beta. This field is immutable.'


class CSINodeDriver(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSINodeDriver"]:
            return CSINodeDriver

        def build(self) -> "CSINodeDriver":
            return CSINodeDriver(**self._attrs)

        @overload
        def allocatable(
            self, value_or_callback: VolumeNodeResources | None, /
        ) -> "CSINodeDriver.Builder": ...

        @overload
        def allocatable(
            self,
            value_or_callback: Callable[
                [VolumeNodeResources.Builder],
                VolumeNodeResources.Builder | VolumeNodeResources,
            ],
            /,
        ) -> "CSINodeDriver.Builder": ...

        @overload
        def allocatable(
            self, value_or_callback: Never = ...
        ) -> "VolumeNodeResources.BuilderContext": ...

        def allocatable(self, value_or_callback=UNSET, /):
            "allocatable represents the volume resources of a node that are available for scheduling. This field is beta."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeNodeResources.BuilderContext()
                context._parent_builder = self
                context._field_name = "allocatable"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeNodeResources.builder())
                if isinstance(output, VolumeNodeResources.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("allocatable", value)

        def name(self, value: str, /) -> Self:
            "name represents the name of the CSI driver that this object refers to. This MUST be the same name returned by the CSI GetPluginName() call for that driver."
            return self._set("name", value)

        def node_id(self, value: str, /) -> Self:
            'nodeID of the node from the driver point of view. This field enables Kubernetes to communicate with storage systems that do not share the same nomenclature for nodes. For example, Kubernetes may refer to a given node as "node1", but the storage system may refer to the same node as "nodeA". When Kubernetes issues a command to the storage system to attach a volume to a specific node, it can use this field to refer to the node name using the ID that the storage system will understand, e.g. "nodeA" instead of "node1". This field is required.'
            return self._set("node_id", value)

        def topology_keys(self, value: list[str] | None, /) -> Self:
            'topologyKeys is the list of keys supported by the driver. When a driver is initialized on a cluster, it provides a set of topology keys that it understands (e.g. "company.com/zone", "company.com/region"). When a driver is initialized on a node, it provides the same topology keys along with values. Kubelet will expose these topology keys as labels on its own node object. When Kubernetes does topology aware provisioning, it can use this list to determine which labels it should retrieve from the node object and pass back to the driver. It is possible for different nodes to use different topology keys. This can be empty if driver does not support topology.'
            return self._set("topology_keys", value)

    class BuilderContext(BuilderContextBase["CSINodeDriver.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSINodeDriver.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSINodeDriver."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSINodeDriver", Builder]:
        return GenericListBuilder[CSINodeDriver, CSINodeDriver.Builder]()

    allocatable: VolumeNodeResources | None = None
    "allocatable represents the volume resources of a node that are available for scheduling. This field is beta."
    name: str
    "name represents the name of the CSI driver that this object refers to. This MUST be the same name returned by the CSI GetPluginName() call for that driver."
    node_id: Annotated[str, Field(alias="nodeID")]
    'nodeID of the node from the driver point of view. This field enables Kubernetes to communicate with storage systems that do not share the same nomenclature for nodes. For example, Kubernetes may refer to a given node as "node1", but the storage system may refer to the same node as "nodeA". When Kubernetes issues a command to the storage system to attach a volume to a specific node, it can use this field to refer to the node name using the ID that the storage system will understand, e.g. "nodeA" instead of "node1". This field is required.'
    topology_keys: Annotated[list[str] | None, Field(alias="topologyKeys")] = None
    'topologyKeys is the list of keys supported by the driver. When a driver is initialized on a cluster, it provides a set of topology keys that it understands (e.g. "company.com/zone", "company.com/region"). When a driver is initialized on a node, it provides the same topology keys along with values. Kubelet will expose these topology keys as labels on its own node object. When Kubernetes does topology aware provisioning, it can use this list to determine which labels it should retrieve from the node object and pass back to the driver. It is possible for different nodes to use different topology keys. This can be empty if driver does not support topology.'


class CSINodeSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSINodeSpec"]:
            return CSINodeSpec

        def build(self) -> "CSINodeSpec":
            return CSINodeSpec(**self._attrs)

        @overload
        def drivers(self, value_or_callback: list[CSINodeDriver], /) -> "CSINodeSpec.Builder": ...

        @overload
        def drivers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[CSINodeDriver, CSINodeDriver.Builder]],
                GenericListBuilder[CSINodeDriver, CSINodeDriver.Builder] | list[CSINodeDriver],
            ],
            /,
        ) -> "CSINodeSpec.Builder": ...

        @overload
        def drivers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CSINodeDriver.Builder]: ...

        def drivers(self, value_or_callback=UNSET, /):
            "drivers is a list of information of all CSI Drivers existing on a node. If all drivers in the list are uninstalled, this can become empty."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CSINodeDriver.Builder]()
                context._parent_builder = self
                context._field_name = "drivers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSINodeDriver.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("drivers", value)

    class BuilderContext(BuilderContextBase["CSINodeSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSINodeSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSINodeSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSINodeSpec", Builder]:
        return GenericListBuilder[CSINodeSpec, CSINodeSpec.Builder]()

    drivers: list[CSINodeDriver]
    "drivers is a list of information of all CSI Drivers existing on a node. If all drivers in the list are uninstalled, this can become empty."


class StorageHealth(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageHealth"]:
            return StorageHealth

        def build(self) -> "StorageHealth":
            return StorageHealth(**self._attrs)

        @overload
        def health_conditions(
            self, value_or_callback: list[StorageHealthCondition] | None, /
        ) -> "StorageHealth.Builder": ...

        @overload
        def health_conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[StorageHealthCondition, StorageHealthCondition.Builder]],
                GenericListBuilder[StorageHealthCondition, StorageHealthCondition.Builder]
                | list[StorageHealthCondition],
            ],
            /,
        ) -> "StorageHealth.Builder": ...

        @overload
        def health_conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[StorageHealthCondition.Builder]: ...

        def health_conditions(self, value_or_callback=UNSET, /):
            "healthConditions are the adverse storage backend conditions reported by the CSI driver. At most 16 conditions may be reported."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[StorageHealthCondition.Builder]()
                context._parent_builder = self
                context._field_name = "health_conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageHealthCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("health_conditions", value)

        def name(self, value: str, /) -> Self:
            "name is the CSI driver name, matching CSINodeDriver.name."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["StorageHealth.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageHealth.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageHealth."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageHealth", Builder]:
        return GenericListBuilder[StorageHealth, StorageHealth.Builder]()

    health_conditions: Annotated[
        list[StorageHealthCondition] | None, Field(alias="healthConditions")
    ] = None
    "healthConditions are the adverse storage backend conditions reported by the CSI driver. At most 16 conditions may be reported."
    name: str
    "name is the CSI driver name, matching CSINodeDriver.name."


class VolumeAttachmentStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeAttachmentStatus"]:
            return VolumeAttachmentStatus

        def build(self) -> "VolumeAttachmentStatus":
            return VolumeAttachmentStatus(**self._attrs)

        @overload
        def attach_error(
            self, value_or_callback: VolumeError | None, /
        ) -> "VolumeAttachmentStatus.Builder": ...

        @overload
        def attach_error(
            self,
            value_or_callback: Callable[[VolumeError.Builder], VolumeError.Builder | VolumeError],
            /,
        ) -> "VolumeAttachmentStatus.Builder": ...

        @overload
        def attach_error(self, value_or_callback: Never = ...) -> "VolumeError.BuilderContext": ...

        def attach_error(self, value_or_callback=UNSET, /):
            "attachError represents the last error encountered during attach operation, if any. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeError.BuilderContext()
                context._parent_builder = self
                context._field_name = "attach_error"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeError.builder())
                if isinstance(output, VolumeError.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("attach_error", value)

        def attached(self, value: bool, /) -> Self:
            "attached indicates the volume is successfully attached. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
            return self._set("attached", value)

        def attachment_metadata(self, value: dict[str, str] | None, /) -> Self:
            "attachmentMetadata is populated with any information returned by the attach operation, upon successful attach, that must be passed into subsequent WaitForAttach or Mount calls. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
            return self._set("attachment_metadata", value)

        @overload
        def detach_error(
            self, value_or_callback: VolumeError | None, /
        ) -> "VolumeAttachmentStatus.Builder": ...

        @overload
        def detach_error(
            self,
            value_or_callback: Callable[[VolumeError.Builder], VolumeError.Builder | VolumeError],
            /,
        ) -> "VolumeAttachmentStatus.Builder": ...

        @overload
        def detach_error(self, value_or_callback: Never = ...) -> "VolumeError.BuilderContext": ...

        def detach_error(self, value_or_callback=UNSET, /):
            "detachError represents the last error encountered during detach operation, if any. This field must only be set by the entity completing the detach operation, i.e. the external-attacher."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeError.BuilderContext()
                context._parent_builder = self
                context._field_name = "detach_error"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeError.builder())
                if isinstance(output, VolumeError.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("detach_error", value)

    class BuilderContext(BuilderContextBase["VolumeAttachmentStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeAttachmentStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeAttachmentStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeAttachmentStatus", Builder]:
        return GenericListBuilder[VolumeAttachmentStatus, VolumeAttachmentStatus.Builder]()

    attach_error: Annotated[VolumeError | None, Field(alias="attachError")] = None
    "attachError represents the last error encountered during attach operation, if any. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
    attached: bool
    "attached indicates the volume is successfully attached. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
    attachment_metadata: Annotated[dict[str, str] | None, Field(alias="attachmentMetadata")] = None
    "attachmentMetadata is populated with any information returned by the attach operation, upon successful attach, that must be passed into subsequent WaitForAttach or Mount calls. This field must only be set by the entity completing the attach operation, i.e. the external-attacher."
    detach_error: Annotated[VolumeError | None, Field(alias="detachError")] = None
    "detachError represents the last error encountered during detach operation, if any. This field must only be set by the entity completing the detach operation, i.e. the external-attacher."


class CSIDriver(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "csidrivers",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSIDriver"]:
            return CSIDriver

        def build(self) -> "CSIDriver":
            return CSIDriver(**self._attrs)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["CSIDriver"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "CSIDriver.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "CSIDriver.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. metadata.Name indicates the name of the CSI driver that this object refers to; it MUST be the same name returned by the CSI GetPluginName() call for that driver. The driver name must be 63 characters or less, beginning and ending with an alphanumeric character ([a-z0-9A-Z]) with dashes (-), dots (.), and alphanumerics between. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: CSIDriverSpec, /) -> "CSIDriver.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [CSIDriverSpec.Builder], CSIDriverSpec.Builder | CSIDriverSpec
            ],
            /,
        ) -> "CSIDriver.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "CSIDriverSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec represents the specification of the CSI Driver."
            if self._in_context and value_or_callback is UNSET:
                context = CSIDriverSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSIDriverSpec.builder())
                if isinstance(output, CSIDriverSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["CSIDriver.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSIDriver.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSIDriver."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSIDriver", Builder]:
        return GenericListBuilder[CSIDriver, CSIDriver.Builder]()

    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["CSIDriver"] = "CSIDriver"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. metadata.Name indicates the name of the CSI driver that this object refers to; it MUST be the same name returned by the CSI GetPluginName() call for that driver. The driver name must be 63 characters or less, beginning and ending with an alphanumeric character ([a-z0-9A-Z]) with dashes (-), dots (.), and alphanumerics between. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: CSIDriverSpec
    "spec represents the specification of the CSI Driver."


CSIDriverList = ResourceList[CSIDriver]


class CSINodeStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSINodeStatus"]:
            return CSINodeStatus

        def build(self) -> "CSINodeStatus":
            return CSINodeStatus(**self._attrs)

        @overload
        def storage_health(
            self, value_or_callback: list[StorageHealth] | None, /
        ) -> "CSINodeStatus.Builder": ...

        @overload
        def storage_health(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[StorageHealth, StorageHealth.Builder]],
                GenericListBuilder[StorageHealth, StorageHealth.Builder] | list[StorageHealth],
            ],
            /,
        ) -> "CSINodeStatus.Builder": ...

        @overload
        def storage_health(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[StorageHealth.Builder]: ...

        def storage_health(self, value_or_callback=UNSET, /):
            "storageHealth contains backend health reports for CSI drivers registered on the node."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[StorageHealth.Builder]()
                context._parent_builder = self
                context._field_name = "storage_health"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageHealth.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("storage_health", value)

    class BuilderContext(BuilderContextBase["CSINodeStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSINodeStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSINodeStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSINodeStatus", Builder]:
        return GenericListBuilder[CSINodeStatus, CSINodeStatus.Builder]()

    storage_health: Annotated[list[StorageHealth] | None, Field(alias="storageHealth")] = None
    "storageHealth contains backend health reports for CSI drivers registered on the node."


class VolumeAttachmentSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeAttachmentSource"]:
            return VolumeAttachmentSource

        def build(self) -> "VolumeAttachmentSource":
            return VolumeAttachmentSource(**self._attrs)

        @overload
        def inline_volume_spec(
            self, value_or_callback: v1.PersistentVolumeSpec | None, /
        ) -> "VolumeAttachmentSource.Builder": ...

        @overload
        def inline_volume_spec(
            self,
            value_or_callback: Callable[
                [v1.PersistentVolumeSpec.Builder],
                v1.PersistentVolumeSpec.Builder | v1.PersistentVolumeSpec,
            ],
            /,
        ) -> "VolumeAttachmentSource.Builder": ...

        @overload
        def inline_volume_spec(
            self, value_or_callback: Never = ...
        ) -> "v1.PersistentVolumeSpec.BuilderContext": ...

        def inline_volume_spec(self, value_or_callback=UNSET, /):
            "inlineVolumeSpec contains all the information necessary to attach a persistent volume defined by a pod\u0027s inline VolumeSource. This field is populated only for the CSIMigration feature. It contains translated fields from a pod\u0027s inline VolumeSource to a PersistentVolumeSpec. This field is beta-level and is only honored by servers that enabled the CSIMigration feature."
            if self._in_context and value_or_callback is UNSET:
                context = v1.PersistentVolumeSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "inline_volume_spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.PersistentVolumeSpec.builder())
                if isinstance(output, v1.PersistentVolumeSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("inline_volume_spec", value)

        def persistent_volume_name(self, value: str | None, /) -> Self:
            "persistentVolumeName represents the name of the persistent volume to attach."
            return self._set("persistent_volume_name", value)

    class BuilderContext(BuilderContextBase["VolumeAttachmentSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeAttachmentSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeAttachmentSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeAttachmentSource", Builder]:
        return GenericListBuilder[VolumeAttachmentSource, VolumeAttachmentSource.Builder]()

    inline_volume_spec: Annotated[
        v1.PersistentVolumeSpec | None, Field(alias="inlineVolumeSpec")
    ] = None
    "inlineVolumeSpec contains all the information necessary to attach a persistent volume defined by a pod\u0027s inline VolumeSource. This field is populated only for the CSIMigration feature. It contains translated fields from a pod\u0027s inline VolumeSource to a PersistentVolumeSpec. This field is beta-level and is only honored by servers that enabled the CSIMigration feature."
    persistent_volume_name: Annotated[str | None, Field(alias="persistentVolumeName")] = None
    "persistentVolumeName represents the name of the persistent volume to attach."


class VolumeAttachmentSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeAttachmentSpec"]:
            return VolumeAttachmentSpec

        def build(self) -> "VolumeAttachmentSpec":
            return VolumeAttachmentSpec(**self._attrs)

        def attacher(self, value: str, /) -> Self:
            "attacher indicates the name of the volume driver that MUST handle this request. This is the name returned by GetPluginName()."
            return self._set("attacher", value)

        def node_name(self, value: str, /) -> Self:
            "nodeName represents the node that the volume should be attached to."
            return self._set("node_name", value)

        @overload
        def source(
            self, value_or_callback: VolumeAttachmentSource, /
        ) -> "VolumeAttachmentSpec.Builder": ...

        @overload
        def source(
            self,
            value_or_callback: Callable[
                [VolumeAttachmentSource.Builder],
                VolumeAttachmentSource.Builder | VolumeAttachmentSource,
            ],
            /,
        ) -> "VolumeAttachmentSpec.Builder": ...

        @overload
        def source(
            self, value_or_callback: Never = ...
        ) -> "VolumeAttachmentSource.BuilderContext": ...

        def source(self, value_or_callback=UNSET, /):
            "source represents the volume that should be attached."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeAttachmentSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "source"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeAttachmentSource.builder())
                if isinstance(output, VolumeAttachmentSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("source", value)

    class BuilderContext(BuilderContextBase["VolumeAttachmentSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeAttachmentSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeAttachmentSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeAttachmentSpec", Builder]:
        return GenericListBuilder[VolumeAttachmentSpec, VolumeAttachmentSpec.Builder]()

    attacher: str
    "attacher indicates the name of the volume driver that MUST handle this request. This is the name returned by GetPluginName()."
    node_name: Annotated[str, Field(alias="nodeName")]
    "nodeName represents the node that the volume should be attached to."
    source: VolumeAttachmentSource
    "source represents the volume that should be attached."


class CSINode(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "csinodes",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSINode"]:
            return CSINode

        def build(self) -> "CSINode":
            return CSINode(**self._attrs)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["CSINode"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "CSINode.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "CSINode.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. metadata.name must be the Kubernetes node name."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: CSINodeSpec, /) -> "CSINode.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[CSINodeSpec.Builder], CSINodeSpec.Builder | CSINodeSpec],
            /,
        ) -> "CSINode.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "CSINodeSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec is the specification of CSINode"
            if self._in_context and value_or_callback is UNSET:
                context = CSINodeSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSINodeSpec.builder())
                if isinstance(output, CSINodeSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: CSINodeStatus | None, /) -> "CSINode.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [CSINodeStatus.Builder], CSINodeStatus.Builder | CSINodeStatus
            ],
            /,
        ) -> "CSINode.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "CSINodeStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status contains health and status information for the node\u0027s storage."
            if self._in_context and value_or_callback is UNSET:
                context = CSINodeStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSINodeStatus.builder())
                if isinstance(output, CSINodeStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["CSINode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSINode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSINode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSINode", Builder]:
        return GenericListBuilder[CSINode, CSINode.Builder]()

    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["CSINode"] = "CSINode"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. metadata.name must be the Kubernetes node name."
    spec: CSINodeSpec
    "spec is the specification of CSINode"
    status: CSINodeStatus | None = None
    "status contains health and status information for the node\u0027s storage."


CSINodeList = ResourceList[CSINode]


class VolumeAttachment(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "volumeattachments",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeAttachment"]:
            return VolumeAttachment

        def build(self) -> "VolumeAttachment":
            return VolumeAttachment(**self._attrs)

        def api_version(self, value: Literal["storage.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["VolumeAttachment"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: VolumeAttachmentSpec, /
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [VolumeAttachmentSpec.Builder],
                VolumeAttachmentSpec.Builder | VolumeAttachmentSpec,
            ],
            /,
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "VolumeAttachmentSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec represents specification of the desired attach/detach volume behavior. Populated by the Kubernetes system."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeAttachmentSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeAttachmentSpec.builder())
                if isinstance(output, VolumeAttachmentSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: VolumeAttachmentStatus | None, /
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [VolumeAttachmentStatus.Builder],
                VolumeAttachmentStatus.Builder | VolumeAttachmentStatus,
            ],
            /,
        ) -> "VolumeAttachment.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "VolumeAttachmentStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents status of the VolumeAttachment request. Populated by the entity completing the attach or detach operation, i.e. the external-attacher."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeAttachmentStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeAttachmentStatus.builder())
                if isinstance(output, VolumeAttachmentStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["VolumeAttachment.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeAttachment.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeAttachment."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeAttachment", Builder]:
        return GenericListBuilder[VolumeAttachment, VolumeAttachment.Builder]()

    api_version: Annotated[Literal["storage.k8s.io/v1"], Field(alias="apiVersion")] = (
        "storage.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["VolumeAttachment"] = "VolumeAttachment"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: VolumeAttachmentSpec
    "spec represents specification of the desired attach/detach volume behavior. Populated by the Kubernetes system."
    status: VolumeAttachmentStatus | None = None
    "status represents status of the VolumeAttachment request. Populated by the entity completing the attach or detach operation, i.e. the external-attacher."


VolumeAttachmentList = ResourceList[VolumeAttachment]
