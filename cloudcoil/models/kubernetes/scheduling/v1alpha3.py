# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    Condition,
    ObjectMeta,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class AllCompositeDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AllCompositeDisruptionMode"]:
            return AllCompositeDisruptionMode

        def build(self) -> "AllCompositeDisruptionMode":
            return AllCompositeDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["AllCompositeDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AllCompositeDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AllCompositeDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AllCompositeDisruptionMode", Builder]:
        return GenericListBuilder[AllCompositeDisruptionMode, AllCompositeDisruptionMode.Builder]()

    pass


class AllDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AllDisruptionMode"]:
            return AllDisruptionMode

        def build(self) -> "AllDisruptionMode":
            return AllDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["AllDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AllDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AllDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AllDisruptionMode", Builder]:
        return GenericListBuilder[AllDisruptionMode, AllDisruptionMode.Builder]()

    pass


class BasicSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BasicSchedulingPolicy"]:
            return BasicSchedulingPolicy

        def build(self) -> "BasicSchedulingPolicy":
            return BasicSchedulingPolicy(**self._attrs)

    class BuilderContext(BuilderContextBase["BasicSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BasicSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BasicSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BasicSchedulingPolicy", Builder]:
        return GenericListBuilder[BasicSchedulingPolicy, BasicSchedulingPolicy.Builder]()

    pass


class CompositeBasicSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositeBasicSchedulingPolicy"]:
            return CompositeBasicSchedulingPolicy

        def build(self) -> "CompositeBasicSchedulingPolicy":
            return CompositeBasicSchedulingPolicy(**self._attrs)

    class BuilderContext(BuilderContextBase["CompositeBasicSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositeBasicSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositeBasicSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CompositeBasicSchedulingPolicy", Builder]:
        return GenericListBuilder[
            CompositeBasicSchedulingPolicy, CompositeBasicSchedulingPolicy.Builder
        ]()

    pass


class CompositeGangSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositeGangSchedulingPolicy"]:
            return CompositeGangSchedulingPolicy

        def build(self) -> "CompositeGangSchedulingPolicy":
            return CompositeGangSchedulingPolicy(**self._attrs)

        def min_group_count(self, value: int, /) -> Self:
            "minGroupCount is the minimum number of child groups that must be schedulable or scheduled at the same time for the scheduler to admit the entire group. It must be a positive integer."
            return self._set("min_group_count", value)

    class BuilderContext(BuilderContextBase["CompositeGangSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositeGangSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositeGangSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CompositeGangSchedulingPolicy", Builder]:
        return GenericListBuilder[
            CompositeGangSchedulingPolicy, CompositeGangSchedulingPolicy.Builder
        ]()

    min_group_count: Annotated[int, Field(alias="minGroupCount")]
    "minGroupCount is the minimum number of child groups that must be schedulable or scheduled at the same time for the scheduler to admit the entire group. It must be a positive integer."


class CompositePodGroupSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroupSchedulingPolicy"]:
            return CompositePodGroupSchedulingPolicy

        def build(self) -> "CompositePodGroupSchedulingPolicy":
            return CompositePodGroupSchedulingPolicy(**self._attrs)

        @overload
        def basic(
            self, value_or_callback: CompositeBasicSchedulingPolicy | None, /
        ) -> "CompositePodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self,
            value_or_callback: Callable[
                [CompositeBasicSchedulingPolicy.Builder],
                CompositeBasicSchedulingPolicy.Builder | CompositeBasicSchedulingPolicy,
            ],
            /,
        ) -> "CompositePodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self, value_or_callback: Never = ...
        ) -> "CompositeBasicSchedulingPolicy.BuilderContext": ...

        def basic(self, value_or_callback=UNSET, /):
            "basic specifies that the groups of this composite group should be scheduled independently. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositeBasicSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "basic"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositeBasicSchedulingPolicy.builder())
                if isinstance(output, CompositeBasicSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("basic", value)

        @overload
        def gang(
            self, value_or_callback: CompositeGangSchedulingPolicy | None, /
        ) -> "CompositePodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(
            self,
            value_or_callback: Callable[
                [CompositeGangSchedulingPolicy.Builder],
                CompositeGangSchedulingPolicy.Builder | CompositeGangSchedulingPolicy,
            ],
            /,
        ) -> "CompositePodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(
            self, value_or_callback: Never = ...
        ) -> "CompositeGangSchedulingPolicy.BuilderContext": ...

        def gang(self, value_or_callback=UNSET, /):
            "gang specifies that the groups of this composite group should be scheduled using all-or-nothing semantics."
            if self._in_context and value_or_callback is UNSET:
                context = CompositeGangSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "gang"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositeGangSchedulingPolicy.builder())
                if isinstance(output, CompositeGangSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("gang", value)

    class BuilderContext(BuilderContextBase["CompositePodGroupSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroupSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroupSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CompositePodGroupSchedulingPolicy", Builder]:
        return GenericListBuilder[
            CompositePodGroupSchedulingPolicy, CompositePodGroupSchedulingPolicy.Builder
        ]()

    basic: CompositeBasicSchedulingPolicy | None = None
    "basic specifies that the groups of this composite group should be scheduled independently. This field is immutable."
    gang: CompositeGangSchedulingPolicy | None = None
    "gang specifies that the groups of this composite group should be scheduled using all-or-nothing semantics."


class CompositePodGroupStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroupStatus"]:
            return CompositePodGroupStatus

        def build(self) -> "CompositePodGroupStatus":
            return CompositePodGroupStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "CompositePodGroupStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "CompositePodGroupStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            'conditions represent the latest observations of the CompositePodGroup\u0027s state.\n\nKnown condition types: - "CompositePodGroupInitiallyScheduled": Indicates whether the overall scheduling requirement\n  for the subtree under this CompositePodGroup has been satisfied. Once this condition\n  transitions to True, it serves as a terminal state and will never revert to False,\n  even if pods are subsequently deleted and group constraints are no longer met.\n- "DisruptionTarget": Indicates whether the CompositePodGroup is about to be terminated\n  due to disruption such as preemption.\n\nKnown reasons for the CompositePodGroupInitiallyScheduled condition: - "Unschedulable": The CompositePodGroup\u0027s subtree could not be placed due to resource constraints,\n  affinity/anti-affinity, or topological constraints.\n- "SchedulerError": The CompositePodGroup cannot be scheduled due to some internal error\n  that occurred during scheduling.\n- "Invalid": Set to True when kube-scheduler detects an invalid group layout during\n  runtime validation. The `message` field details the specific layout violation (such as\n  a detected cycle, exceeding the maximum depth of 4, or referencing multiple distinct Workloads).\n\nKnown reasons for the DisruptionTarget condition: - "PreemptionByScheduler": The CompositePodGroup was targeted by the scheduler\u0027s preemption loop\n  to free up capacity for higher-priority preemptors.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

    class BuilderContext(BuilderContextBase["CompositePodGroupStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroupStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroupStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CompositePodGroupStatus", Builder]:
        return GenericListBuilder[CompositePodGroupStatus, CompositePodGroupStatus.Builder]()

    conditions: list[Condition] | None = None
    'conditions represent the latest observations of the CompositePodGroup\u0027s state.\n\nKnown condition types: - "CompositePodGroupInitiallyScheduled": Indicates whether the overall scheduling requirement\n  for the subtree under this CompositePodGroup has been satisfied. Once this condition\n  transitions to True, it serves as a terminal state and will never revert to False,\n  even if pods are subsequently deleted and group constraints are no longer met.\n- "DisruptionTarget": Indicates whether the CompositePodGroup is about to be terminated\n  due to disruption such as preemption.\n\nKnown reasons for the CompositePodGroupInitiallyScheduled condition: - "Unschedulable": The CompositePodGroup\u0027s subtree could not be placed due to resource constraints,\n  affinity/anti-affinity, or topological constraints.\n- "SchedulerError": The CompositePodGroup cannot be scheduled due to some internal error\n  that occurred during scheduling.\n- "Invalid": Set to True when kube-scheduler detects an invalid group layout during\n  runtime validation. The `message` field details the specific layout violation (such as\n  a detected cycle, exceeding the maximum depth of 4, or referencing multiple distinct Workloads).\n\nKnown reasons for the DisruptionTarget condition: - "PreemptionByScheduler": The CompositePodGroup was targeted by the scheduler\u0027s preemption loop\n  to free up capacity for higher-priority preemptors.'


class GangSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GangSchedulingPolicy"]:
            return GangSchedulingPolicy

        def build(self) -> "GangSchedulingPolicy":
            return GangSchedulingPolicy(**self._attrs)

        def min_count(self, value: int, /) -> Self:
            "minCount is the minimum number of pods that must be schedulable or scheduled at the same time for the scheduler to admit the entire group. It must be a positive integer. This field is mutable to support workload scaling.\n\nNote that the scheduler operates on an eventually consistent model. Updates to minCount may not be immediately reflected in scheduling decisions due to propagation delays. If minCount is updated while a scheduling cycle is in progress for that group, the new value may not take effect until the next cycle. Moreover, minCount is only enforced during scheduling, meaning that modifications to this field do not affect already-scheduled pods, applying only to those evaluated in future cycles."
            return self._set("min_count", value)

    class BuilderContext(BuilderContextBase["GangSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GangSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GangSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["GangSchedulingPolicy", Builder]:
        return GenericListBuilder[GangSchedulingPolicy, GangSchedulingPolicy.Builder]()

    min_count: Annotated[int, Field(alias="minCount")]
    "minCount is the minimum number of pods that must be schedulable or scheduled at the same time for the scheduler to admit the entire group. It must be a positive integer. This field is mutable to support workload scaling.\n\nNote that the scheduler operates on an eventually consistent model. Updates to minCount may not be immediately reflected in scheduling decisions due to propagation delays. If minCount is updated while a scheduling cycle is in progress for that group, the new value may not take effect until the next cycle. Moreover, minCount is only enforced during scheduling, meaning that modifications to this field do not affect already-scheduled pods, applying only to those evaluated in future cycles."


class PodGroupResourceClaim(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupResourceClaim"]:
            return PodGroupResourceClaim

        def build(self) -> "PodGroupResourceClaim":
            return PodGroupResourceClaim(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name uniquely identifies this resource claim inside the PodGroup. This must be a DNS_LABEL."
            return self._set("name", value)

        def resource_claim_name(self, value: str | None, /) -> Self:
            "resourceClaimName is the name of a ResourceClaim object in the same namespace as this PodGroup. The ResourceClaim will be reserved for the PodGroup instead of its individual pods.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
            return self._set("resource_claim_name", value)

        def resource_claim_template_name(self, value: str | None, /) -> Self:
            "resourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace as this PodGroup.\n\nThe template will be used to create a new ResourceClaim, which will be bound to this PodGroup. When this PodGroup is deleted, the ResourceClaim will also be deleted. The PodGroup name and resource name, along with a generated component, will be used to form a unique name for the ResourceClaim, which will be recorded in podgroup.status.resourceClaimStatuses.\n\nThis field is immutable and no changes will be made to the corresponding ResourceClaim by the control plane after creating the ResourceClaim.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
            return self._set("resource_claim_template_name", value)

    class BuilderContext(BuilderContextBase["PodGroupResourceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupResourceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupResourceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupResourceClaim", Builder]:
        return GenericListBuilder[PodGroupResourceClaim, PodGroupResourceClaim.Builder]()

    name: str
    "name uniquely identifies this resource claim inside the PodGroup. This must be a DNS_LABEL."
    resource_claim_name: Annotated[str | None, Field(alias="resourceClaimName")] = None
    "resourceClaimName is the name of a ResourceClaim object in the same namespace as this PodGroup. The ResourceClaim will be reserved for the PodGroup instead of its individual pods.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
    resource_claim_template_name: Annotated[
        str | None, Field(alias="resourceClaimTemplateName")
    ] = None
    "resourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace as this PodGroup.\n\nThe template will be used to create a new ResourceClaim, which will be bound to this PodGroup. When this PodGroup is deleted, the ResourceClaim will also be deleted. The PodGroup name and resource name, along with a generated component, will be used to form a unique name for the ResourceClaim, which will be recorded in podgroup.status.resourceClaimStatuses.\n\nThis field is immutable and no changes will be made to the corresponding ResourceClaim by the control plane after creating the ResourceClaim.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."


class PodGroupResourceClaimStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupResourceClaimStatus"]:
            return PodGroupResourceClaimStatus

        def build(self) -> "PodGroupResourceClaimStatus":
            return PodGroupResourceClaimStatus(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name uniquely identifies this resource claim inside the PodGroup. This must match the name of an entry in podgroup.spec.resourceClaims, which implies that the string must be a DNS_LABEL."
            return self._set("name", value)

        def resource_claim_name(self, value: str | None, /) -> Self:
            "resourceClaimName is the name of the ResourceClaim that was generated for the PodGroup in the namespace of the PodGroup. If this is unset, then generating a ResourceClaim was not necessary. The podgroup.spec.resourceClaims entry can be ignored in this case."
            return self._set("resource_claim_name", value)

    class BuilderContext(BuilderContextBase["PodGroupResourceClaimStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupResourceClaimStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupResourceClaimStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupResourceClaimStatus", Builder]:
        return GenericListBuilder[
            PodGroupResourceClaimStatus, PodGroupResourceClaimStatus.Builder
        ]()

    name: str
    "name uniquely identifies this resource claim inside the PodGroup. This must match the name of an entry in podgroup.spec.resourceClaims, which implies that the string must be a DNS_LABEL."
    resource_claim_name: Annotated[str | None, Field(alias="resourceClaimName")] = None
    "resourceClaimName is the name of the ResourceClaim that was generated for the PodGroup in the namespace of the PodGroup. If this is unset, then generating a ResourceClaim was not necessary. The podgroup.spec.resourceClaims entry can be ignored in this case."


class PodGroupSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupSchedulingPolicy"]:
            return PodGroupSchedulingPolicy

        def build(self) -> "PodGroupSchedulingPolicy":
            return PodGroupSchedulingPolicy(**self._attrs)

        @overload
        def basic(
            self, value_or_callback: BasicSchedulingPolicy | None, /
        ) -> "PodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self,
            value_or_callback: Callable[
                [BasicSchedulingPolicy.Builder],
                BasicSchedulingPolicy.Builder | BasicSchedulingPolicy,
            ],
            /,
        ) -> "PodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self, value_or_callback: Never = ...
        ) -> "BasicSchedulingPolicy.BuilderContext": ...

        def basic(self, value_or_callback=UNSET, /):
            "basic specifies that the pods in this group should be scheduled using standard Kubernetes scheduling behavior. Setting this field at group creation time opts this group to basic scheduling; this field cannot be changed afterward."
            if self._in_context and value_or_callback is UNSET:
                context = BasicSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "basic"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BasicSchedulingPolicy.builder())
                if isinstance(output, BasicSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("basic", value)

        @overload
        def gang(
            self, value_or_callback: GangSchedulingPolicy | None, /
        ) -> "PodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(
            self,
            value_or_callback: Callable[
                [GangSchedulingPolicy.Builder],
                GangSchedulingPolicy.Builder | GangSchedulingPolicy,
            ],
            /,
        ) -> "PodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(self, value_or_callback: Never = ...) -> "GangSchedulingPolicy.BuilderContext": ...

        def gang(self, value_or_callback=UNSET, /):
            "gang specifies that the pods in this group should be scheduled using all-or-nothing semantics. Setting this field at group creation time opts this group to gang scheduling; this field cannot be set or unset afterward. The minCount field within Gang scheduling policy remains mutable after group creation."
            if self._in_context and value_or_callback is UNSET:
                context = GangSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "gang"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GangSchedulingPolicy.builder())
                if isinstance(output, GangSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("gang", value)

    class BuilderContext(BuilderContextBase["PodGroupSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupSchedulingPolicy", Builder]:
        return GenericListBuilder[PodGroupSchedulingPolicy, PodGroupSchedulingPolicy.Builder]()

    basic: BasicSchedulingPolicy | None = None
    "basic specifies that the pods in this group should be scheduled using standard Kubernetes scheduling behavior. Setting this field at group creation time opts this group to basic scheduling; this field cannot be changed afterward."
    gang: GangSchedulingPolicy | None = None
    "gang specifies that the pods in this group should be scheduled using all-or-nothing semantics. Setting this field at group creation time opts this group to gang scheduling; this field cannot be set or unset afterward. The minCount field within Gang scheduling policy remains mutable after group creation."


class PodGroupStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupStatus"]:
            return PodGroupStatus

        def build(self) -> "PodGroupStatus":
            return PodGroupStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "PodGroupStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "PodGroupStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            'conditions represent the latest observations of the PodGroup\u0027s state.\n\nKnown condition types: - "PodGroupInitiallyScheduled": Indicates whether the scheduling requirement has been satisfied. Once this condition transitions to True, it serves as a terminal state and will never revert to False, even if pods are subsequently evicted and group constraints are no longer met. - "DisruptionTarget": Indicates whether the PodGroup is about to be terminated\n  due to disruption such as preemption.\n\nKnown reasons for the PodGroupInitiallyScheduled condition: - "Unschedulable": The PodGroup cannot be scheduled due to resource constraints,\n  affinity/anti-affinity rules, or insufficient capacity for the gang.\n- "SchedulerError": The PodGroup cannot be scheduled due to some internal error\n  that happened during scheduling, for example due to nodeAffinity parsing errors.\n\nKnown reasons for the DisruptionTarget condition: - "PreemptionByScheduler": The PodGroup was preempted by the scheduler to make room for\n  higher-priority PodGroups or Pods.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def resource_claim_statuses(
            self, value_or_callback: list[PodGroupResourceClaimStatus] | None, /
        ) -> "PodGroupStatus.Builder": ...

        @overload
        def resource_claim_statuses(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        PodGroupResourceClaimStatus, PodGroupResourceClaimStatus.Builder
                    ]
                ],
                GenericListBuilder[PodGroupResourceClaimStatus, PodGroupResourceClaimStatus.Builder]
                | list[PodGroupResourceClaimStatus],
            ],
            /,
        ) -> "PodGroupStatus.Builder": ...

        @overload
        def resource_claim_statuses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodGroupResourceClaimStatus.Builder]: ...

        def resource_claim_statuses(self, value_or_callback=UNSET, /):
            "resourceClaimStatuses is status of resource claims."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodGroupResourceClaimStatus.Builder]()
                context._parent_builder = self
                context._field_name = "resource_claim_statuses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupResourceClaimStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_claim_statuses", value)

    class BuilderContext(BuilderContextBase["PodGroupStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupStatus", Builder]:
        return GenericListBuilder[PodGroupStatus, PodGroupStatus.Builder]()

    conditions: list[Condition] | None = None
    'conditions represent the latest observations of the PodGroup\u0027s state.\n\nKnown condition types: - "PodGroupInitiallyScheduled": Indicates whether the scheduling requirement has been satisfied. Once this condition transitions to True, it serves as a terminal state and will never revert to False, even if pods are subsequently evicted and group constraints are no longer met. - "DisruptionTarget": Indicates whether the PodGroup is about to be terminated\n  due to disruption such as preemption.\n\nKnown reasons for the PodGroupInitiallyScheduled condition: - "Unschedulable": The PodGroup cannot be scheduled due to resource constraints,\n  affinity/anti-affinity rules, or insufficient capacity for the gang.\n- "SchedulerError": The PodGroup cannot be scheduled due to some internal error\n  that happened during scheduling, for example due to nodeAffinity parsing errors.\n\nKnown reasons for the DisruptionTarget condition: - "PreemptionByScheduler": The PodGroup was preempted by the scheduler to make room for\n  higher-priority PodGroups or Pods.'
    resource_claim_statuses: Annotated[
        list[PodGroupResourceClaimStatus] | None, Field(alias="resourceClaimStatuses")
    ] = None
    "resourceClaimStatuses is status of resource claims."


class SingleCompositeDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SingleCompositeDisruptionMode"]:
            return SingleCompositeDisruptionMode

        def build(self) -> "SingleCompositeDisruptionMode":
            return SingleCompositeDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["SingleCompositeDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SingleCompositeDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SingleCompositeDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["SingleCompositeDisruptionMode", Builder]:
        return GenericListBuilder[
            SingleCompositeDisruptionMode, SingleCompositeDisruptionMode.Builder
        ]()

    pass


class SingleDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SingleDisruptionMode"]:
            return SingleDisruptionMode

        def build(self) -> "SingleDisruptionMode":
            return SingleDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["SingleDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SingleDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SingleDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SingleDisruptionMode", Builder]:
        return GenericListBuilder[SingleDisruptionMode, SingleDisruptionMode.Builder]()

    pass


class TopologyConstraint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TopologyConstraint"]:
            return TopologyConstraint

        def build(self) -> "TopologyConstraint":
            return TopologyConstraint(**self._attrs)

        def key(self, value: str, /) -> Self:
            'key specifies the key of the node label representing the topology domain. All pods within the PodGroup must be colocated within the same domain instance. Different PodGroups can land on different domain instances even if they derive from the same PodGroupTemplate. Examples: "topology.kubernetes.io/rack"'
            return self._set("key", value)

    class BuilderContext(BuilderContextBase["TopologyConstraint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TopologyConstraint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TopologyConstraint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TopologyConstraint", Builder]:
        return GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]()

    key: str
    'key specifies the key of the node label representing the topology domain. All pods within the PodGroup must be colocated within the same domain instance. Different PodGroups can land on different domain instances even if they derive from the same PodGroupTemplate. Examples: "topology.kubernetes.io/rack"'


class TypedLocalObjectReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TypedLocalObjectReference"]:
            return TypedLocalObjectReference

        def build(self) -> "TypedLocalObjectReference":
            return TypedLocalObjectReference(**self._attrs)

        def api_group(self, value: str | None, /) -> Self:
            "apiGroup is the group for the resource being referenced. If APIGroup is empty, the specified Kind must be in the core API group. For any other third-party types, setting APIGroup is required. It must be a DNS subdomain."
            return self._set("api_group", value)

        def kind(self, value: str, /) -> Self:
            "kind is the type of resource being referenced. It must be a path segment name."
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "name is the name of resource being referenced. It must be a path segment name."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["TypedLocalObjectReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TypedLocalObjectReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TypedLocalObjectReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TypedLocalObjectReference", Builder]:
        return GenericListBuilder[TypedLocalObjectReference, TypedLocalObjectReference.Builder]()

    api_group: Annotated[str | None, Field(alias="apiGroup")] = None
    "apiGroup is the group for the resource being referenced. If APIGroup is empty, the specified Kind must be in the core API group. For any other third-party types, setting APIGroup is required. It must be a DNS subdomain."
    kind: str
    "kind is the type of resource being referenced. It must be a path segment name."
    name: str
    "name is the name of resource being referenced. It must be a path segment name."


class WorkloadPodGroupAllDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupAllDisruptionMode"]:
            return WorkloadPodGroupAllDisruptionMode

        def build(self) -> "WorkloadPodGroupAllDisruptionMode":
            return WorkloadPodGroupAllDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupAllDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupAllDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupAllDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupAllDisruptionMode", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupAllDisruptionMode, WorkloadPodGroupAllDisruptionMode.Builder
        ]()

    pass


class WorkloadPodGroupBasicSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupBasicSchedulingPolicy"]:
            return WorkloadPodGroupBasicSchedulingPolicy

        def build(self) -> "WorkloadPodGroupBasicSchedulingPolicy":
            return WorkloadPodGroupBasicSchedulingPolicy(**self._attrs)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupBasicSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupBasicSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupBasicSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupBasicSchedulingPolicy", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupBasicSchedulingPolicy,
            WorkloadPodGroupBasicSchedulingPolicy.Builder,
        ]()

    pass


class WorkloadPodGroupGangSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupGangSchedulingPolicy"]:
            return WorkloadPodGroupGangSchedulingPolicy

        def build(self) -> "WorkloadPodGroupGangSchedulingPolicy":
            return WorkloadPodGroupGangSchedulingPolicy(**self._attrs)

        def min_count(self, value: int | None, /) -> Self:
            "minCount is the minimum number of pods that must be scheduled at the same time for the scheduler to admit the entire group. This field is optional. If it is not specified, the controller should inject a context-specific sane default (e.g., parallelism for a Job). If set, it must be a positive integer."
            return self._set("min_count", value)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupGangSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupGangSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupGangSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupGangSchedulingPolicy", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupGangSchedulingPolicy,
            WorkloadPodGroupGangSchedulingPolicy.Builder,
        ]()

    min_count: Annotated[int | None, Field(alias="minCount")] = None
    "minCount is the minimum number of pods that must be scheduled at the same time for the scheduler to admit the entire group. This field is optional. If it is not specified, the controller should inject a context-specific sane default (e.g., parallelism for a Job). If set, it must be a positive integer."


class WorkloadPodGroupResourceClaim(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupResourceClaim"]:
            return WorkloadPodGroupResourceClaim

        def build(self) -> "WorkloadPodGroupResourceClaim":
            return WorkloadPodGroupResourceClaim(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name uniquely identifies this resource claim inside the group. This field is required. It must be a DNS_LABEL."
            return self._set("name", value)

        def resource_claim_name(self, value: str | None, /) -> Self:
            "resourceClaimName is the name of a ResourceClaim object in the same namespace. This field is optional. If it is not specified, no resource claim is used. If set, it must be a DNS subdomain."
            return self._set("resource_claim_name", value)

        def resource_claim_template_name(self, value: str | None, /) -> Self:
            "resourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace. This field is optional. If it is not specified, no resource claim template is used. If set, it must be a DNS subdomain."
            return self._set("resource_claim_template_name", value)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupResourceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupResourceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupResourceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupResourceClaim", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupResourceClaim, WorkloadPodGroupResourceClaim.Builder
        ]()

    name: str
    "name uniquely identifies this resource claim inside the group. This field is required. It must be a DNS_LABEL."
    resource_claim_name: Annotated[str | None, Field(alias="resourceClaimName")] = None
    "resourceClaimName is the name of a ResourceClaim object in the same namespace. This field is optional. If it is not specified, no resource claim is used. If set, it must be a DNS subdomain."
    resource_claim_template_name: Annotated[
        str | None, Field(alias="resourceClaimTemplateName")
    ] = None
    "resourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace. This field is optional. If it is not specified, no resource claim template is used. If set, it must be a DNS subdomain."


class WorkloadPodGroupSchedulingConstraints(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupSchedulingConstraints"]:
            return WorkloadPodGroupSchedulingConstraints

        def build(self) -> "WorkloadPodGroupSchedulingConstraints":
            return WorkloadPodGroupSchedulingConstraints(**self._attrs)

        @overload
        def topology(
            self, value_or_callback: list[TopologyConstraint] | None, /
        ) -> "WorkloadPodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]],
                GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]
                | list[TopologyConstraint],
            ],
            /,
        ) -> "WorkloadPodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TopologyConstraint.Builder]: ...

        def topology(self, value_or_callback=UNSET, /):
            "topology specifies desired topological placements for all pods within the pod group. If unset, no topology placement is requested."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TopologyConstraint.Builder]()
                context._parent_builder = self
                context._field_name = "topology"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TopologyConstraint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("topology", value)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupSchedulingConstraints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupSchedulingConstraints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupSchedulingConstraints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupSchedulingConstraints", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupSchedulingConstraints,
            WorkloadPodGroupSchedulingConstraints.Builder,
        ]()

    topology: list[TopologyConstraint] | None = None
    "topology specifies desired topological placements for all pods within the pod group. If unset, no topology placement is requested."


class WorkloadPodGroupSchedulingPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupSchedulingPolicy"]:
            return WorkloadPodGroupSchedulingPolicy

        def build(self) -> "WorkloadPodGroupSchedulingPolicy":
            return WorkloadPodGroupSchedulingPolicy(**self._attrs)

        @overload
        def basic(
            self, value_or_callback: WorkloadPodGroupBasicSchedulingPolicy | None, /
        ) -> "WorkloadPodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self,
            value_or_callback: Callable[
                [WorkloadPodGroupBasicSchedulingPolicy.Builder],
                WorkloadPodGroupBasicSchedulingPolicy.Builder
                | WorkloadPodGroupBasicSchedulingPolicy,
            ],
            /,
        ) -> "WorkloadPodGroupSchedulingPolicy.Builder": ...

        @overload
        def basic(
            self, value_or_callback: Never = ...
        ) -> "WorkloadPodGroupBasicSchedulingPolicy.BuilderContext": ...

        def basic(self, value_or_callback=UNSET, /):
            "basic specifies that standard, pod-by-pod Kubernetes scheduling behavior should be used."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadPodGroupBasicSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "basic"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadPodGroupBasicSchedulingPolicy.builder())
                if isinstance(output, WorkloadPodGroupBasicSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("basic", value)

        @overload
        def gang(
            self, value_or_callback: WorkloadPodGroupGangSchedulingPolicy | None, /
        ) -> "WorkloadPodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(
            self,
            value_or_callback: Callable[
                [WorkloadPodGroupGangSchedulingPolicy.Builder],
                WorkloadPodGroupGangSchedulingPolicy.Builder | WorkloadPodGroupGangSchedulingPolicy,
            ],
            /,
        ) -> "WorkloadPodGroupSchedulingPolicy.Builder": ...

        @overload
        def gang(
            self, value_or_callback: Never = ...
        ) -> "WorkloadPodGroupGangSchedulingPolicy.BuilderContext": ...

        def gang(self, value_or_callback=UNSET, /):
            "gang specifies all-or-nothing scheduling semantics."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadPodGroupGangSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "gang"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadPodGroupGangSchedulingPolicy.builder())
                if isinstance(output, WorkloadPodGroupGangSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("gang", value)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupSchedulingPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupSchedulingPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupSchedulingPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupSchedulingPolicy", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupSchedulingPolicy, WorkloadPodGroupSchedulingPolicy.Builder
        ]()

    basic: WorkloadPodGroupBasicSchedulingPolicy | None = None
    "basic specifies that standard, pod-by-pod Kubernetes scheduling behavior should be used."
    gang: WorkloadPodGroupGangSchedulingPolicy | None = None
    "gang specifies all-or-nothing scheduling semantics."


class WorkloadPodGroupSingleDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupSingleDisruptionMode"]:
            return WorkloadPodGroupSingleDisruptionMode

        def build(self) -> "WorkloadPodGroupSingleDisruptionMode":
            return WorkloadPodGroupSingleDisruptionMode(**self._attrs)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupSingleDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupSingleDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupSingleDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupSingleDisruptionMode", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupSingleDisruptionMode,
            WorkloadPodGroupSingleDisruptionMode.Builder,
        ]()

    pass


class WorkloadReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadReference"]:
            return WorkloadReference

        def build(self) -> "WorkloadReference":
            return WorkloadReference(**self._attrs)

        def template_name(self, value: str, /) -> Self:
            "templateName is the name of a template within the Workload object that was used to create a pod group. It must be a DNS label. This field is required."
            return self._set("template_name", value)

        def workload_name(self, value: str, /) -> Self:
            "workloadName is the name of the Workload object that contains a template that was used when creating a pod group. It must be a DNS name. This field is required."
            return self._set("workload_name", value)

    class BuilderContext(BuilderContextBase["WorkloadReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["WorkloadReference", Builder]:
        return GenericListBuilder[WorkloadReference, WorkloadReference.Builder]()

    template_name: Annotated[str, Field(alias="templateName")]
    "templateName is the name of a template within the Workload object that was used to create a pod group. It must be a DNS label. This field is required."
    workload_name: Annotated[str, Field(alias="workloadName")]
    "workloadName is the name of the Workload object that contains a template that was used when creating a pod group. It must be a DNS name. This field is required."


class CompositeDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositeDisruptionMode"]:
            return CompositeDisruptionMode

        def build(self) -> "CompositeDisruptionMode":
            return CompositeDisruptionMode(**self._attrs)

        @overload
        def all(
            self, value_or_callback: AllCompositeDisruptionMode | None, /
        ) -> "CompositeDisruptionMode.Builder": ...

        @overload
        def all(
            self,
            value_or_callback: Callable[
                [AllCompositeDisruptionMode.Builder],
                AllCompositeDisruptionMode.Builder | AllCompositeDisruptionMode,
            ],
            /,
        ) -> "CompositeDisruptionMode.Builder": ...

        @overload
        def all(
            self, value_or_callback: Never = ...
        ) -> "AllCompositeDisruptionMode.BuilderContext": ...

        def all(self, value_or_callback=UNSET, /):
            "all specifies that all children groups can only be disrupted together."
            if self._in_context and value_or_callback is UNSET:
                context = AllCompositeDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "all"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AllCompositeDisruptionMode.builder())
                if isinstance(output, AllCompositeDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("all", value)

        @overload
        def single(
            self, value_or_callback: SingleCompositeDisruptionMode | None, /
        ) -> "CompositeDisruptionMode.Builder": ...

        @overload
        def single(
            self,
            value_or_callback: Callable[
                [SingleCompositeDisruptionMode.Builder],
                SingleCompositeDisruptionMode.Builder | SingleCompositeDisruptionMode,
            ],
            /,
        ) -> "CompositeDisruptionMode.Builder": ...

        @overload
        def single(
            self, value_or_callback: Never = ...
        ) -> "SingleCompositeDisruptionMode.BuilderContext": ...

        def single(self, value_or_callback=UNSET, /):
            "single specifies that children groups can be disrupted independently from each other."
            if self._in_context and value_or_callback is UNSET:
                context = SingleCompositeDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "single"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SingleCompositeDisruptionMode.builder())
                if isinstance(output, SingleCompositeDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("single", value)

    class BuilderContext(BuilderContextBase["CompositeDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositeDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositeDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CompositeDisruptionMode", Builder]:
        return GenericListBuilder[CompositeDisruptionMode, CompositeDisruptionMode.Builder]()

    all: AllCompositeDisruptionMode | None = None
    "all specifies that all children groups can only be disrupted together."
    single: SingleCompositeDisruptionMode | None = None
    "single specifies that children groups can be disrupted independently from each other."


class CompositePodGroupSchedulingConstraints(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroupSchedulingConstraints"]:
            return CompositePodGroupSchedulingConstraints

        def build(self) -> "CompositePodGroupSchedulingConstraints":
            return CompositePodGroupSchedulingConstraints(**self._attrs)

        @overload
        def topology(
            self, value_or_callback: list[TopologyConstraint] | None, /
        ) -> "CompositePodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]],
                GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]
                | list[TopologyConstraint],
            ],
            /,
        ) -> "CompositePodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TopologyConstraint.Builder]: ...

        def topology(self, value_or_callback=UNSET, /):
            "topology defines the topology constraints for the composite pod group. Currently only a single topology constraint can be specified. This may change in the future."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TopologyConstraint.Builder]()
                context._parent_builder = self
                context._field_name = "topology"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TopologyConstraint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("topology", value)

    class BuilderContext(BuilderContextBase["CompositePodGroupSchedulingConstraints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroupSchedulingConstraints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroupSchedulingConstraints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CompositePodGroupSchedulingConstraints", Builder]:
        return GenericListBuilder[
            CompositePodGroupSchedulingConstraints,
            CompositePodGroupSchedulingConstraints.Builder,
        ]()

    topology: list[TopologyConstraint] | None = None
    "topology defines the topology constraints for the composite pod group. Currently only a single topology constraint can be specified. This may change in the future."


class CompositePodGroupSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroupSpec"]:
            return CompositePodGroupSpec

        def build(self) -> "CompositePodGroupSpec":
            return CompositePodGroupSpec(**self._attrs)

        @overload
        def disruption_mode(
            self, value_or_callback: CompositeDisruptionMode | None, /
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def disruption_mode(
            self,
            value_or_callback: Callable[
                [CompositeDisruptionMode.Builder],
                CompositeDisruptionMode.Builder | CompositeDisruptionMode,
            ],
            /,
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def disruption_mode(
            self, value_or_callback: Never = ...
        ) -> "CompositeDisruptionMode.BuilderContext": ...

        def disruption_mode(self, value_or_callback=UNSET, /):
            "disruptionMode defines the mode in which a given CompositePodGroup can be disrupted. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. One of Single, All. Defaults to Single if unset. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositeDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "disruption_mode"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositeDisruptionMode.builder())
                if isinstance(output, CompositeDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("disruption_mode", value)

        def parent_composite_pod_group_name(self, value: str | None, /) -> Self:
            "parentCompositePodGroupName contains the name of the parent composite pod group within the same namespace as this composite pod group. It must be a DNS name. If it\u0027s nil, then this composite pod group is a root of a workload\u0027s hierarchy. This field is immutable."
            return self._set("parent_composite_pod_group_name", value)

        def preemption_policy(self, value: str | None, /) -> Self:
            "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset. When Priority Admission Controller is enabled, it populates this field from PriorityClassName, and defaults to PreemptLowerPriority if value is unset in PriorityClass. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
            return self._set("preemption_policy", value)

        def priority(self, value: int | None, /) -> Self:
            "priority is the value of priority of this composite pod group. Various system components use this field to find the priority of the composite pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
            return self._set("priority", value)

        def priority_class_name(self, value: str | None, /) -> Self:
            "priorityClassName defines the priority that should be considered when scheduling this CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. If left unspecified, it is validated and resolved similarly to the PriorityClassName field in Pods (i.e. if no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, the composite pod group\u0027s priority will be zero). This field is immutable."
            return self._set("priority_class_name", value)

        @overload
        def scheduling_constraints(
            self, value_or_callback: CompositePodGroupSchedulingConstraints | None, /
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def scheduling_constraints(
            self,
            value_or_callback: Callable[
                [CompositePodGroupSchedulingConstraints.Builder],
                CompositePodGroupSchedulingConstraints.Builder
                | CompositePodGroupSchedulingConstraints,
            ],
            /,
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def scheduling_constraints(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupSchedulingConstraints.BuilderContext": ...

        def scheduling_constraints(self, value_or_callback=UNSET, /):
            "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupSchedulingConstraints.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupSchedulingConstraints.builder())
                if isinstance(output, CompositePodGroupSchedulingConstraints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_constraints", value)

        @overload
        def scheduling_policy(
            self, value_or_callback: CompositePodGroupSchedulingPolicy, /
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def scheduling_policy(
            self,
            value_or_callback: Callable[
                [CompositePodGroupSchedulingPolicy.Builder],
                CompositePodGroupSchedulingPolicy.Builder | CompositePodGroupSchedulingPolicy,
            ],
            /,
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def scheduling_policy(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupSchedulingPolicy.BuilderContext": ...

        def scheduling_policy(self, value_or_callback=UNSET, /):
            "schedulingPolicy defines the scheduling policy for this instance of the CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupSchedulingPolicy.builder())
                if isinstance(output, CompositePodGroupSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_policy", value)

        @overload
        def workload_ref(
            self, value_or_callback: WorkloadReference, /
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def workload_ref(
            self,
            value_or_callback: Callable[
                [WorkloadReference.Builder],
                WorkloadReference.Builder | WorkloadReference,
            ],
            /,
        ) -> "CompositePodGroupSpec.Builder": ...

        @overload
        def workload_ref(
            self, value_or_callback: Never = ...
        ) -> "WorkloadReference.BuilderContext": ...

        def workload_ref(self, value_or_callback=UNSET, /):
            "workloadRef references an optional CompositePodGroup template within the Workload object that was used to create the CompositePodGroup. This field is required. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "workload_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadReference.builder())
                if isinstance(output, WorkloadReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("workload_ref", value)

    class BuilderContext(BuilderContextBase["CompositePodGroupSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroupSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroupSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CompositePodGroupSpec", Builder]:
        return GenericListBuilder[CompositePodGroupSpec, CompositePodGroupSpec.Builder]()

    disruption_mode: Annotated[CompositeDisruptionMode | None, Field(alias="disruptionMode")] = None
    "disruptionMode defines the mode in which a given CompositePodGroup can be disrupted. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. One of Single, All. Defaults to Single if unset. This field is immutable."
    parent_composite_pod_group_name: Annotated[
        str | None, Field(alias="parentCompositePodGroupName")
    ] = None
    "parentCompositePodGroupName contains the name of the parent composite pod group within the same namespace as this composite pod group. It must be a DNS name. If it\u0027s nil, then this composite pod group is a root of a workload\u0027s hierarchy. This field is immutable."
    preemption_policy: Annotated[str | None, Field(alias="preemptionPolicy")] = None
    "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset. When Priority Admission Controller is enabled, it populates this field from PriorityClassName, and defaults to PreemptLowerPriority if value is unset in PriorityClass. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
    priority: int | None = None
    "priority is the value of priority of this composite pod group. Various system components use this field to find the priority of the composite pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
    priority_class_name: Annotated[str | None, Field(alias="priorityClassName")] = None
    "priorityClassName defines the priority that should be considered when scheduling this CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. If left unspecified, it is validated and resolved similarly to the PriorityClassName field in Pods (i.e. if no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, the composite pod group\u0027s priority will be zero). This field is immutable."
    scheduling_constraints: Annotated[
        CompositePodGroupSchedulingConstraints | None,
        Field(alias="schedulingConstraints"),
    ] = None
    "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. This field is immutable."
    scheduling_policy: Annotated[CompositePodGroupSchedulingPolicy, Field(alias="schedulingPolicy")]
    "schedulingPolicy defines the scheduling policy for this instance of the CompositePodGroup. Controllers are expected to fill this field by copying it from a CompositePodGroupTemplate. This field is immutable."
    workload_ref: Annotated[WorkloadReference, Field(alias="workloadRef")]
    "workloadRef references an optional CompositePodGroup template within the Workload object that was used to create the CompositePodGroup. This field is required. This field is immutable."


class DisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DisruptionMode"]:
            return DisruptionMode

        def build(self) -> "DisruptionMode":
            return DisruptionMode(**self._attrs)

        @overload
        def all(
            self, value_or_callback: AllDisruptionMode | None, /
        ) -> "DisruptionMode.Builder": ...

        @overload
        def all(
            self,
            value_or_callback: Callable[
                [AllDisruptionMode.Builder],
                AllDisruptionMode.Builder | AllDisruptionMode,
            ],
            /,
        ) -> "DisruptionMode.Builder": ...

        @overload
        def all(self, value_or_callback: Never = ...) -> "AllDisruptionMode.BuilderContext": ...

        def all(self, value_or_callback=UNSET, /):
            "all specifies that all children can only be disrupted together."
            if self._in_context and value_or_callback is UNSET:
                context = AllDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "all"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AllDisruptionMode.builder())
                if isinstance(output, AllDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("all", value)

        @overload
        def single(
            self, value_or_callback: SingleDisruptionMode | None, /
        ) -> "DisruptionMode.Builder": ...

        @overload
        def single(
            self,
            value_or_callback: Callable[
                [SingleDisruptionMode.Builder],
                SingleDisruptionMode.Builder | SingleDisruptionMode,
            ],
            /,
        ) -> "DisruptionMode.Builder": ...

        @overload
        def single(
            self, value_or_callback: Never = ...
        ) -> "SingleDisruptionMode.BuilderContext": ...

        def single(self, value_or_callback=UNSET, /):
            "single specifies that children can be disrupted independently from each other."
            if self._in_context and value_or_callback is UNSET:
                context = SingleDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "single"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SingleDisruptionMode.builder())
                if isinstance(output, SingleDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("single", value)

    class BuilderContext(BuilderContextBase["DisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DisruptionMode", Builder]:
        return GenericListBuilder[DisruptionMode, DisruptionMode.Builder]()

    all: AllDisruptionMode | None = None
    "all specifies that all children can only be disrupted together."
    single: SingleDisruptionMode | None = None
    "single specifies that children can be disrupted independently from each other."


class PodGroupSchedulingConstraints(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupSchedulingConstraints"]:
            return PodGroupSchedulingConstraints

        def build(self) -> "PodGroupSchedulingConstraints":
            return PodGroupSchedulingConstraints(**self._attrs)

        @overload
        def topology(
            self, value_or_callback: list[TopologyConstraint] | None, /
        ) -> "PodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]],
                GenericListBuilder[TopologyConstraint, TopologyConstraint.Builder]
                | list[TopologyConstraint],
            ],
            /,
        ) -> "PodGroupSchedulingConstraints.Builder": ...

        @overload
        def topology(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TopologyConstraint.Builder]: ...

        def topology(self, value_or_callback=UNSET, /):
            "topology defines the topology constraints for the pod group. Currently only a single topology constraint can be specified. This may change in the future."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TopologyConstraint.Builder]()
                context._parent_builder = self
                context._field_name = "topology"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TopologyConstraint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("topology", value)

    class BuilderContext(BuilderContextBase["PodGroupSchedulingConstraints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupSchedulingConstraints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupSchedulingConstraints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PodGroupSchedulingConstraints", Builder]:
        return GenericListBuilder[
            PodGroupSchedulingConstraints, PodGroupSchedulingConstraints.Builder
        ]()

    topology: list[TopologyConstraint] | None = None
    "topology defines the topology constraints for the pod group. Currently only a single topology constraint can be specified. This may change in the future."


class PodGroupSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupSpec"]:
            return PodGroupSpec

        def build(self) -> "PodGroupSpec":
            return PodGroupSpec(**self._attrs)

        @overload
        def disruption_mode(
            self, value_or_callback: DisruptionMode | None, /
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def disruption_mode(
            self,
            value_or_callback: Callable[
                [DisruptionMode.Builder], DisruptionMode.Builder | DisruptionMode
            ],
            /,
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def disruption_mode(
            self, value_or_callback: Never = ...
        ) -> "DisruptionMode.BuilderContext": ...

        def disruption_mode(self, value_or_callback=UNSET, /):
            "disruptionMode defines the mode in which a given PodGroup can be disrupted. Controllers are expected to fill this field by copying it from a PodGroupTemplate. One of Single, All. Defaults to Single if unset. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = DisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "disruption_mode"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DisruptionMode.builder())
                if isinstance(output, DisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("disruption_mode", value)

        def parent_composite_pod_group_name(self, value: str | None, /) -> Self:
            "parentCompositePodGroupName contains the name of the parent composite pod group within the same namespace as this pod group. If it\u0027s nil, then this pod group is a root of a workload\u0027s hierarchy. This field is used only when the CompositePodGroup feature gate is enabled. This field is immutable."
            return self._set("parent_composite_pod_group_name", value)

        def preemption_policy(self, value: str | None, /) -> Self:
            "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset. When Priority Admission Controller is enabled, it populates this field from PriorityClassName, and defaults to PreemptLowerPriority if value is unset in PriorityClass. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
            return self._set("preemption_policy", value)

        def priority(self, value: int | None, /) -> Self:
            "priority is the value of priority of this pod group. Various system components use this field to find the priority of the pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
            return self._set("priority", value)

        def priority_class_name(self, value: str | None, /) -> Self:
            "priorityClassName defines the priority that should be considered when scheduling this pod group. Controllers are expected to fill this field by copying it from a PodGroupTemplate. Otherwise, it is validated and resolved similarly to the PriorityClassName on PodGroupTemplate (i.e. if no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, the pod group\u0027s priority will be zero). This field is immutable."
            return self._set("priority_class_name", value)

        @overload
        def resource_claims(
            self, value_or_callback: list[PodGroupResourceClaim] | None, /
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def resource_claims(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodGroupResourceClaim, PodGroupResourceClaim.Builder]],
                GenericListBuilder[PodGroupResourceClaim, PodGroupResourceClaim.Builder]
                | list[PodGroupResourceClaim],
            ],
            /,
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def resource_claims(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodGroupResourceClaim.Builder]: ...

        def resource_claims(self, value_or_callback=UNSET, /):
            "resourceClaims defines which ResourceClaims may be shared among Pods in the group. Pods consume the devices allocated to a PodGroup\u0027s claim by defining a claim in its own Spec.ResourceClaims that matches the PodGroup\u0027s claim exactly. The claim must have the same name and refer to the same ResourceClaim or ResourceClaimTemplate.\n\nThis is a beta-level field and requires that the DRAWorkloadResourceClaims feature gate is enabled.\n\nThis field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodGroupResourceClaim.Builder]()
                context._parent_builder = self
                context._field_name = "resource_claims"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupResourceClaim.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_claims", value)

        @overload
        def scheduling_constraints(
            self, value_or_callback: PodGroupSchedulingConstraints | None, /
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def scheduling_constraints(
            self,
            value_or_callback: Callable[
                [PodGroupSchedulingConstraints.Builder],
                PodGroupSchedulingConstraints.Builder | PodGroupSchedulingConstraints,
            ],
            /,
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def scheduling_constraints(
            self, value_or_callback: Never = ...
        ) -> "PodGroupSchedulingConstraints.BuilderContext": ...

        def scheduling_constraints(self, value_or_callback=UNSET, /):
            "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this PodGroup. Controllers are expected to fill this field by copying it from a PodGroupTemplate. This field is immutable. This field is only available when the TopologyAwareWorkloadScheduling feature gate is enabled."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupSchedulingConstraints.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupSchedulingConstraints.builder())
                if isinstance(output, PodGroupSchedulingConstraints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_constraints", value)

        @overload
        def scheduling_policy(
            self, value_or_callback: PodGroupSchedulingPolicy, /
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def scheduling_policy(
            self,
            value_or_callback: Callable[
                [PodGroupSchedulingPolicy.Builder],
                PodGroupSchedulingPolicy.Builder | PodGroupSchedulingPolicy,
            ],
            /,
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def scheduling_policy(
            self, value_or_callback: Never = ...
        ) -> "PodGroupSchedulingPolicy.BuilderContext": ...

        def scheduling_policy(self, value_or_callback=UNSET, /):
            "schedulingPolicy defines the scheduling policy for this instance of the PodGroup. Controllers are expected to fill this field by copying it from a PodGroupTemplate."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupSchedulingPolicy.builder())
                if isinstance(output, PodGroupSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_policy", value)

        @overload
        def workload_ref(
            self, value_or_callback: WorkloadReference | None, /
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def workload_ref(
            self,
            value_or_callback: Callable[
                [WorkloadReference.Builder],
                WorkloadReference.Builder | WorkloadReference,
            ],
            /,
        ) -> "PodGroupSpec.Builder": ...

        @overload
        def workload_ref(
            self, value_or_callback: Never = ...
        ) -> "WorkloadReference.BuilderContext": ...

        def workload_ref(self, value_or_callback=UNSET, /):
            "workloadRef references an optional PodGroup template within the Workload object that was used to create the PodGroup. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "workload_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadReference.builder())
                if isinstance(output, WorkloadReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("workload_ref", value)

    class BuilderContext(BuilderContextBase["PodGroupSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupSpec", Builder]:
        return GenericListBuilder[PodGroupSpec, PodGroupSpec.Builder]()

    disruption_mode: Annotated[DisruptionMode | None, Field(alias="disruptionMode")] = None
    "disruptionMode defines the mode in which a given PodGroup can be disrupted. Controllers are expected to fill this field by copying it from a PodGroupTemplate. One of Single, All. Defaults to Single if unset. This field is immutable."
    parent_composite_pod_group_name: Annotated[
        str | None, Field(alias="parentCompositePodGroupName")
    ] = None
    "parentCompositePodGroupName contains the name of the parent composite pod group within the same namespace as this pod group. If it\u0027s nil, then this pod group is a root of a workload\u0027s hierarchy. This field is used only when the CompositePodGroup feature gate is enabled. This field is immutable."
    preemption_policy: Annotated[str | None, Field(alias="preemptionPolicy")] = None
    "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset. When Priority Admission Controller is enabled, it populates this field from PriorityClassName, and defaults to PreemptLowerPriority if value is unset in PriorityClass. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
    priority: int | None = None
    "priority is the value of priority of this pod group. Various system components use this field to find the priority of the pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
    priority_class_name: Annotated[str | None, Field(alias="priorityClassName")] = None
    "priorityClassName defines the priority that should be considered when scheduling this pod group. Controllers are expected to fill this field by copying it from a PodGroupTemplate. Otherwise, it is validated and resolved similarly to the PriorityClassName on PodGroupTemplate (i.e. if no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, the pod group\u0027s priority will be zero). This field is immutable."
    resource_claims: Annotated[
        list[PodGroupResourceClaim] | None, Field(alias="resourceClaims")
    ] = None
    "resourceClaims defines which ResourceClaims may be shared among Pods in the group. Pods consume the devices allocated to a PodGroup\u0027s claim by defining a claim in its own Spec.ResourceClaims that matches the PodGroup\u0027s claim exactly. The claim must have the same name and refer to the same ResourceClaim or ResourceClaimTemplate.\n\nThis is a beta-level field and requires that the DRAWorkloadResourceClaims feature gate is enabled.\n\nThis field is immutable."
    scheduling_constraints: Annotated[
        PodGroupSchedulingConstraints | None, Field(alias="schedulingConstraints")
    ] = None
    "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this PodGroup. Controllers are expected to fill this field by copying it from a PodGroupTemplate. This field is immutable. This field is only available when the TopologyAwareWorkloadScheduling feature gate is enabled."
    scheduling_policy: Annotated[PodGroupSchedulingPolicy, Field(alias="schedulingPolicy")]
    "schedulingPolicy defines the scheduling policy for this instance of the PodGroup. Controllers are expected to fill this field by copying it from a PodGroupTemplate."
    workload_ref: Annotated[WorkloadReference | None, Field(alias="workloadRef")] = None
    "workloadRef references an optional PodGroup template within the Workload object that was used to create the PodGroup. This field is immutable."


class PodGroupTemplate(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroupTemplate"]:
            return PodGroupTemplate

        def build(self) -> "PodGroupTemplate":
            return PodGroupTemplate(**self._attrs)

        @overload
        def disruption_mode(
            self, value_or_callback: DisruptionMode | None, /
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def disruption_mode(
            self,
            value_or_callback: Callable[
                [DisruptionMode.Builder], DisruptionMode.Builder | DisruptionMode
            ],
            /,
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def disruption_mode(
            self, value_or_callback: Never = ...
        ) -> "DisruptionMode.BuilderContext": ...

        def disruption_mode(self, value_or_callback=UNSET, /):
            "disruptionMode defines the mode in which a given PodGroup can be disrupted. One of Single, All. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = DisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "disruption_mode"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DisruptionMode.builder())
                if isinstance(output, DisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("disruption_mode", value)

        def name(self, value: str, /) -> Self:
            "name is a unique identifier for the PodGroupTemplate within the Workload. It must be a DNS label. This field is immutable."
            return self._set("name", value)

        def preemption_policy(self, value: str | None, /) -> Self:
            "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
            return self._set("preemption_policy", value)

        def priority(self, value: int | None, /) -> Self:
            "priority is the value of priority of pod groups created from this template. Various system components use this field to find the priority of the pod group. The higher the value, the higher the priority. This field is immutable."
            return self._set("priority", value)

        def priority_class_name(self, value: str | None, /) -> Self:
            "priorityClassName indicates the priority that should be considered when scheduling a pod group created from this template. This field is immutable."
            return self._set("priority_class_name", value)

        @overload
        def resource_claims(
            self, value_or_callback: list[PodGroupResourceClaim] | None, /
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def resource_claims(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodGroupResourceClaim, PodGroupResourceClaim.Builder]],
                GenericListBuilder[PodGroupResourceClaim, PodGroupResourceClaim.Builder]
                | list[PodGroupResourceClaim],
            ],
            /,
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def resource_claims(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodGroupResourceClaim.Builder]: ...

        def resource_claims(self, value_or_callback=UNSET, /):
            "resourceClaims defines which ResourceClaims may be shared among Pods in the group. Pods consume the devices allocated to a PodGroup\u0027s claim by defining a claim in its own Spec.ResourceClaims that matches the PodGroup\u0027s claim exactly. The claim must have the same name and refer to the same ResourceClaim or ResourceClaimTemplate.\n\nThis is a beta-level field and requires that the DRAWorkloadResourceClaims feature gate is enabled.\n\nThis field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodGroupResourceClaim.Builder]()
                context._parent_builder = self
                context._field_name = "resource_claims"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupResourceClaim.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_claims", value)

        @overload
        def scheduling_constraints(
            self, value_or_callback: PodGroupSchedulingConstraints | None, /
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def scheduling_constraints(
            self,
            value_or_callback: Callable[
                [PodGroupSchedulingConstraints.Builder],
                PodGroupSchedulingConstraints.Builder | PodGroupSchedulingConstraints,
            ],
            /,
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def scheduling_constraints(
            self, value_or_callback: Never = ...
        ) -> "PodGroupSchedulingConstraints.BuilderContext": ...

        def scheduling_constraints(self, value_or_callback=UNSET, /):
            "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this PodGroupTemplate. This field is only available when the TopologyAwareWorkloadScheduling feature gate is enabled. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupSchedulingConstraints.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupSchedulingConstraints.builder())
                if isinstance(output, PodGroupSchedulingConstraints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_constraints", value)

        @overload
        def scheduling_policy(
            self, value_or_callback: PodGroupSchedulingPolicy, /
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def scheduling_policy(
            self,
            value_or_callback: Callable[
                [PodGroupSchedulingPolicy.Builder],
                PodGroupSchedulingPolicy.Builder | PodGroupSchedulingPolicy,
            ],
            /,
        ) -> "PodGroupTemplate.Builder": ...

        @overload
        def scheduling_policy(
            self, value_or_callback: Never = ...
        ) -> "PodGroupSchedulingPolicy.BuilderContext": ...

        def scheduling_policy(self, value_or_callback=UNSET, /):
            "schedulingPolicy defines the scheduling policy for this PodGroupTemplate."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupSchedulingPolicy.builder())
                if isinstance(output, PodGroupSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_policy", value)

    class BuilderContext(BuilderContextBase["PodGroupTemplate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroupTemplate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroupTemplate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroupTemplate", Builder]:
        return GenericListBuilder[PodGroupTemplate, PodGroupTemplate.Builder]()

    disruption_mode: Annotated[DisruptionMode | None, Field(alias="disruptionMode")] = None
    "disruptionMode defines the mode in which a given PodGroup can be disrupted. One of Single, All. This field is immutable."
    name: str
    "name is a unique identifier for the PodGroupTemplate within the Workload. It must be a DNS label. This field is immutable."
    preemption_policy: Annotated[str | None, Field(alias="preemptionPolicy")] = None
    "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
    priority: int | None = None
    "priority is the value of priority of pod groups created from this template. Various system components use this field to find the priority of the pod group. The higher the value, the higher the priority. This field is immutable."
    priority_class_name: Annotated[str | None, Field(alias="priorityClassName")] = None
    "priorityClassName indicates the priority that should be considered when scheduling a pod group created from this template. This field is immutable."
    resource_claims: Annotated[
        list[PodGroupResourceClaim] | None, Field(alias="resourceClaims")
    ] = None
    "resourceClaims defines which ResourceClaims may be shared among Pods in the group. Pods consume the devices allocated to a PodGroup\u0027s claim by defining a claim in its own Spec.ResourceClaims that matches the PodGroup\u0027s claim exactly. The claim must have the same name and refer to the same ResourceClaim or ResourceClaimTemplate.\n\nThis is a beta-level field and requires that the DRAWorkloadResourceClaims feature gate is enabled.\n\nThis field is immutable."
    scheduling_constraints: Annotated[
        PodGroupSchedulingConstraints | None, Field(alias="schedulingConstraints")
    ] = None
    "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this PodGroupTemplate. This field is only available when the TopologyAwareWorkloadScheduling feature gate is enabled. This field is immutable."
    scheduling_policy: Annotated[PodGroupSchedulingPolicy, Field(alias="schedulingPolicy")]
    "schedulingPolicy defines the scheduling policy for this PodGroupTemplate."


class WorkloadPodGroupDisruptionMode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadPodGroupDisruptionMode"]:
            return WorkloadPodGroupDisruptionMode

        def build(self) -> "WorkloadPodGroupDisruptionMode":
            return WorkloadPodGroupDisruptionMode(**self._attrs)

        @overload
        def all(
            self, value_or_callback: WorkloadPodGroupAllDisruptionMode | None, /
        ) -> "WorkloadPodGroupDisruptionMode.Builder": ...

        @overload
        def all(
            self,
            value_or_callback: Callable[
                [WorkloadPodGroupAllDisruptionMode.Builder],
                WorkloadPodGroupAllDisruptionMode.Builder | WorkloadPodGroupAllDisruptionMode,
            ],
            /,
        ) -> "WorkloadPodGroupDisruptionMode.Builder": ...

        @overload
        def all(
            self, value_or_callback: Never = ...
        ) -> "WorkloadPodGroupAllDisruptionMode.BuilderContext": ...

        def all(self, value_or_callback=UNSET, /):
            "all specifies that all pods in the group must be disrupted together."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadPodGroupAllDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "all"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadPodGroupAllDisruptionMode.builder())
                if isinstance(output, WorkloadPodGroupAllDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("all", value)

        @overload
        def single(
            self, value_or_callback: WorkloadPodGroupSingleDisruptionMode | None, /
        ) -> "WorkloadPodGroupDisruptionMode.Builder": ...

        @overload
        def single(
            self,
            value_or_callback: Callable[
                [WorkloadPodGroupSingleDisruptionMode.Builder],
                WorkloadPodGroupSingleDisruptionMode.Builder | WorkloadPodGroupSingleDisruptionMode,
            ],
            /,
        ) -> "WorkloadPodGroupDisruptionMode.Builder": ...

        @overload
        def single(
            self, value_or_callback: Never = ...
        ) -> "WorkloadPodGroupSingleDisruptionMode.BuilderContext": ...

        def single(self, value_or_callback=UNSET, /):
            "single specifies that pods can be disrupted independently from each other."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadPodGroupSingleDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "single"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadPodGroupSingleDisruptionMode.builder())
                if isinstance(output, WorkloadPodGroupSingleDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("single", value)

    class BuilderContext(BuilderContextBase["WorkloadPodGroupDisruptionMode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadPodGroupDisruptionMode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadPodGroupDisruptionMode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WorkloadPodGroupDisruptionMode", Builder]:
        return GenericListBuilder[
            WorkloadPodGroupDisruptionMode, WorkloadPodGroupDisruptionMode.Builder
        ]()

    all: WorkloadPodGroupAllDisruptionMode | None = None
    "all specifies that all pods in the group must be disrupted together."
    single: WorkloadPodGroupSingleDisruptionMode | None = None
    "single specifies that pods can be disrupted independently from each other."


class CompositePodGroup(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "compositepodgroups",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroup"]:
            return CompositePodGroup

        def build(self) -> "CompositePodGroup":
            return CompositePodGroup(**self._attrs)

        def api_version(self, value: Literal["scheduling.k8s.io/v1alpha3"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["CompositePodGroup"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: CompositePodGroupSpec, /
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [CompositePodGroupSpec.Builder],
                CompositePodGroupSpec.Builder | CompositePodGroupSpec,
            ],
            /,
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the desired state of the CompositePodGroup."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupSpec.builder())
                if isinstance(output, CompositePodGroupSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: CompositePodGroupStatus | None, /
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [CompositePodGroupStatus.Builder],
                CompositePodGroupStatus.Builder | CompositePodGroupStatus,
            ],
            /,
        ) -> "CompositePodGroup.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the current observed state of the CompositePodGroup."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupStatus.builder())
                if isinstance(output, CompositePodGroupStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["CompositePodGroup.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroup.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroup."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CompositePodGroup", Builder]:
        return GenericListBuilder[CompositePodGroup, CompositePodGroup.Builder]()

    api_version: Annotated[Literal["scheduling.k8s.io/v1alpha3"], Field(alias="apiVersion")] = (
        "scheduling.k8s.io/v1alpha3"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["CompositePodGroup"] = "CompositePodGroup"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: CompositePodGroupSpec
    "spec defines the desired state of the CompositePodGroup."
    status: CompositePodGroupStatus | None = None
    "status represents the current observed state of the CompositePodGroup."


CompositePodGroupList = ResourceList[CompositePodGroup]


class CompositePodGroupTemplate(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CompositePodGroupTemplate"]:
            return CompositePodGroupTemplate

        def build(self) -> "CompositePodGroupTemplate":
            return CompositePodGroupTemplate(**self._attrs)

        @overload
        def composite_pod_group_templates(
            self, value_or_callback: list[CompositePodGroupTemplate] | None, /
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def composite_pod_group_templates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[CompositePodGroupTemplate, CompositePodGroupTemplate.Builder]],
                GenericListBuilder[CompositePodGroupTemplate, CompositePodGroupTemplate.Builder]
                | list[CompositePodGroupTemplate],
            ],
            /,
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def composite_pod_group_templates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CompositePodGroupTemplate.Builder]: ...

        def composite_pod_group_templates(self, value_or_callback=UNSET, /):
            "compositePodGroupTemplates is the list of templates for children CompositePodGroups. The maximum number of templates is 8. At least one entry in CompositePodGroupTemplates or PodGroupTemplates must be set."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CompositePodGroupTemplate.Builder]()
                context._parent_builder = self
                context._field_name = "composite_pod_group_templates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupTemplate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("composite_pod_group_templates", value)

        @overload
        def disruption_mode(
            self, value_or_callback: CompositeDisruptionMode | None, /
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def disruption_mode(
            self,
            value_or_callback: Callable[
                [CompositeDisruptionMode.Builder],
                CompositeDisruptionMode.Builder | CompositeDisruptionMode,
            ],
            /,
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def disruption_mode(
            self, value_or_callback: Never = ...
        ) -> "CompositeDisruptionMode.BuilderContext": ...

        def disruption_mode(self, value_or_callback=UNSET, /):
            "disruptionMode defines the mode in which a given CompositePodGroup can be disrupted. One of Single, All. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositeDisruptionMode.BuilderContext()
                context._parent_builder = self
                context._field_name = "disruption_mode"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositeDisruptionMode.builder())
                if isinstance(output, CompositeDisruptionMode.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("disruption_mode", value)

        def name(self, value: str, /) -> Self:
            "name is a unique identifier for the CompositePodGroupTemplate within the Workload. It must be a DNS label. This field is required."
            return self._set("name", value)

        @overload
        def pod_group_templates(
            self, value_or_callback: list[PodGroupTemplate] | None, /
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def pod_group_templates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodGroupTemplate, PodGroupTemplate.Builder]],
                GenericListBuilder[PodGroupTemplate, PodGroupTemplate.Builder]
                | list[PodGroupTemplate],
            ],
            /,
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def pod_group_templates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodGroupTemplate.Builder]: ...

        def pod_group_templates(self, value_or_callback=UNSET, /):
            "podGroupTemplates is the list of templates for children PodGroups. The maximum number of templates is 8. At least one entry in CompositePodGroupTemplates or PodGroupTemplates must be set."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodGroupTemplate.Builder]()
                context._parent_builder = self
                context._field_name = "pod_group_templates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupTemplate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_group_templates", value)

        def preemption_policy(self, value: str | None, /) -> Self:
            "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
            return self._set("preemption_policy", value)

        def priority(self, value: int | None, /) -> Self:
            "priority is the value of priority of composite pod groups created from this template. Various system components use this field to find the priority of the composite pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
            return self._set("priority", value)

        def priority_class_name(self, value: str | None, /) -> Self:
            "priorityClassName indicates the priority that should be considered when scheduling a composite pod group created from this template. If no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, composite pod groups created from this template will have the priority set to zero. This field is immutable."
            return self._set("priority_class_name", value)

        @overload
        def scheduling_constraints(
            self, value_or_callback: CompositePodGroupSchedulingConstraints | None, /
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def scheduling_constraints(
            self,
            value_or_callback: Callable[
                [CompositePodGroupSchedulingConstraints.Builder],
                CompositePodGroupSchedulingConstraints.Builder
                | CompositePodGroupSchedulingConstraints,
            ],
            /,
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def scheduling_constraints(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupSchedulingConstraints.BuilderContext": ...

        def scheduling_constraints(self, value_or_callback=UNSET, /):
            "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this CompositePodGroupTemplate. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupSchedulingConstraints.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupSchedulingConstraints.builder())
                if isinstance(output, CompositePodGroupSchedulingConstraints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_constraints", value)

        @overload
        def scheduling_policy(
            self, value_or_callback: CompositePodGroupSchedulingPolicy, /
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def scheduling_policy(
            self,
            value_or_callback: Callable[
                [CompositePodGroupSchedulingPolicy.Builder],
                CompositePodGroupSchedulingPolicy.Builder | CompositePodGroupSchedulingPolicy,
            ],
            /,
        ) -> "CompositePodGroupTemplate.Builder": ...

        @overload
        def scheduling_policy(
            self, value_or_callback: Never = ...
        ) -> "CompositePodGroupSchedulingPolicy.BuilderContext": ...

        def scheduling_policy(self, value_or_callback=UNSET, /):
            "schedulingPolicy defines the scheduling policy for this template."
            if self._in_context and value_or_callback is UNSET:
                context = CompositePodGroupSchedulingPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupSchedulingPolicy.builder())
                if isinstance(output, CompositePodGroupSchedulingPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_policy", value)

    class BuilderContext(BuilderContextBase["CompositePodGroupTemplate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CompositePodGroupTemplate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CompositePodGroupTemplate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CompositePodGroupTemplate", Builder]:
        return GenericListBuilder[CompositePodGroupTemplate, CompositePodGroupTemplate.Builder]()

    composite_pod_group_templates: Annotated[
        list[CompositePodGroupTemplate] | None,
        Field(alias="compositePodGroupTemplates"),
    ] = None
    "compositePodGroupTemplates is the list of templates for children CompositePodGroups. The maximum number of templates is 8. At least one entry in CompositePodGroupTemplates or PodGroupTemplates must be set."
    disruption_mode: Annotated[CompositeDisruptionMode | None, Field(alias="disruptionMode")] = None
    "disruptionMode defines the mode in which a given CompositePodGroup can be disrupted. One of Single, All. This field is immutable."
    name: str
    "name is a unique identifier for the CompositePodGroupTemplate within the Workload. It must be a DNS label. This field is required."
    pod_group_templates: Annotated[
        list[PodGroupTemplate] | None, Field(alias="podGroupTemplates")
    ] = None
    "podGroupTemplates is the list of templates for children PodGroups. The maximum number of templates is 8. At least one entry in CompositePodGroupTemplates or PodGroupTemplates must be set."
    preemption_policy: Annotated[str | None, Field(alias="preemptionPolicy")] = None
    "preemptionPolicy is the Policy for preempting pods/podgroups with lower priority. One of Never, PreemptLowerPriority. This field is immutable. This field is available only when the PodGroupPreemptionPolicy feature gate is enabled."
    priority: int | None = None
    "priority is the value of priority of composite pod groups created from this template. Various system components use this field to find the priority of the composite pod group. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority. This field is immutable."
    priority_class_name: Annotated[str | None, Field(alias="priorityClassName")] = None
    "priorityClassName indicates the priority that should be considered when scheduling a composite pod group created from this template. If no priority class is specified, admission control can set this to the global default priority class if it exists. Otherwise, composite pod groups created from this template will have the priority set to zero. This field is immutable."
    scheduling_constraints: Annotated[
        CompositePodGroupSchedulingConstraints | None,
        Field(alias="schedulingConstraints"),
    ] = None
    "schedulingConstraints defines optional scheduling constraints (e.g. topology) for this CompositePodGroupTemplate. This field is immutable."
    scheduling_policy: Annotated[CompositePodGroupSchedulingPolicy, Field(alias="schedulingPolicy")]
    "schedulingPolicy defines the scheduling policy for this template."


class PodGroup(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "podgroups",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodGroup"]:
            return PodGroup

        def build(self) -> "PodGroup":
            return PodGroup(**self._attrs)

        def api_version(self, value: Literal["scheduling.k8s.io/v1alpha3"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["PodGroup"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "PodGroup.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PodGroup.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: PodGroupSpec, /) -> "PodGroup.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [PodGroupSpec.Builder], PodGroupSpec.Builder | PodGroupSpec
            ],
            /,
        ) -> "PodGroup.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "PodGroupSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the desired state of the PodGroup."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupSpec.builder())
                if isinstance(output, PodGroupSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: PodGroupStatus | None, /) -> "PodGroup.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [PodGroupStatus.Builder], PodGroupStatus.Builder | PodGroupStatus
            ],
            /,
        ) -> "PodGroup.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "PodGroupStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the current observed state of the PodGroup."
            if self._in_context and value_or_callback is UNSET:
                context = PodGroupStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupStatus.builder())
                if isinstance(output, PodGroupStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["PodGroup.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodGroup.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodGroup."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodGroup", Builder]:
        return GenericListBuilder[PodGroup, PodGroup.Builder]()

    api_version: Annotated[Literal["scheduling.k8s.io/v1alpha3"], Field(alias="apiVersion")] = (
        "scheduling.k8s.io/v1alpha3"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["PodGroup"] = "PodGroup"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: PodGroupSpec
    "spec defines the desired state of the PodGroup."
    status: PodGroupStatus | None = None
    "status represents the current observed state of the PodGroup."


PodGroupList = ResourceList[PodGroup]


class WorkloadSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WorkloadSpec"]:
            return WorkloadSpec

        def build(self) -> "WorkloadSpec":
            return WorkloadSpec(**self._attrs)

        @overload
        def composite_pod_group_templates(
            self, value_or_callback: list[CompositePodGroupTemplate] | None, /
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def composite_pod_group_templates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[CompositePodGroupTemplate, CompositePodGroupTemplate.Builder]],
                GenericListBuilder[CompositePodGroupTemplate, CompositePodGroupTemplate.Builder]
                | list[CompositePodGroupTemplate],
            ],
            /,
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def composite_pod_group_templates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CompositePodGroupTemplate.Builder]: ...

        def composite_pod_group_templates(self, value_or_callback=UNSET, /):
            "compositePodGroupTemplates is the list of CompositePodGroup templates that make up the Workload. The maximum number of templates is 8. This field is immutable. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set.\n\nThis field is used only when the CompositePodGroup feature gate is enabled."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CompositePodGroupTemplate.Builder]()
                context._parent_builder = self
                context._field_name = "composite_pod_group_templates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CompositePodGroupTemplate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("composite_pod_group_templates", value)

        @overload
        def controller_ref(
            self, value_or_callback: TypedLocalObjectReference | None, /
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def controller_ref(
            self,
            value_or_callback: Callable[
                [TypedLocalObjectReference.Builder],
                TypedLocalObjectReference.Builder | TypedLocalObjectReference,
            ],
            /,
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def controller_ref(
            self, value_or_callback: Never = ...
        ) -> "TypedLocalObjectReference.BuilderContext": ...

        def controller_ref(self, value_or_callback=UNSET, /):
            "controllerRef is an optional reference to the controlling object, such as a Deployment or Job. This field is intended for use by tools like CLIs to provide a link back to the original workload definition. This field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = TypedLocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "controller_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TypedLocalObjectReference.builder())
                if isinstance(output, TypedLocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("controller_ref", value)

        @overload
        def pod_group_templates(
            self, value_or_callback: list[PodGroupTemplate] | None, /
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def pod_group_templates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodGroupTemplate, PodGroupTemplate.Builder]],
                GenericListBuilder[PodGroupTemplate, PodGroupTemplate.Builder]
                | list[PodGroupTemplate],
            ],
            /,
        ) -> "WorkloadSpec.Builder": ...

        @overload
        def pod_group_templates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodGroupTemplate.Builder]: ...

        def pod_group_templates(self, value_or_callback=UNSET, /):
            "podGroupTemplates is the list of templates that make up the Workload. The maximum number of templates is 8. Templates cannot be added or removed after the workload is created. Existing templates may still be updated where their individual fields allow it. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodGroupTemplate.Builder]()
                context._parent_builder = self
                context._field_name = "pod_group_templates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodGroupTemplate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_group_templates", value)

    class BuilderContext(BuilderContextBase["WorkloadSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WorkloadSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WorkloadSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["WorkloadSpec", Builder]:
        return GenericListBuilder[WorkloadSpec, WorkloadSpec.Builder]()

    composite_pod_group_templates: Annotated[
        list[CompositePodGroupTemplate] | None,
        Field(alias="compositePodGroupTemplates"),
    ] = None
    "compositePodGroupTemplates is the list of CompositePodGroup templates that make up the Workload. The maximum number of templates is 8. This field is immutable. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set.\n\nThis field is used only when the CompositePodGroup feature gate is enabled."
    controller_ref: Annotated[TypedLocalObjectReference | None, Field(alias="controllerRef")] = None
    "controllerRef is an optional reference to the controlling object, such as a Deployment or Job. This field is intended for use by tools like CLIs to provide a link back to the original workload definition. This field is immutable."
    pod_group_templates: Annotated[
        list[PodGroupTemplate] | None, Field(alias="podGroupTemplates")
    ] = None
    "podGroupTemplates is the list of templates that make up the Workload. The maximum number of templates is 8. Templates cannot be added or removed after the workload is created. Existing templates may still be updated where their individual fields allow it. Exactly one of CompositePodGroupTemplates and PodGroupTemplates must be set."


class Workload(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "workloads",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Workload"]:
            return Workload

        def build(self) -> "Workload":
            return Workload(**self._attrs)

        def api_version(self, value: Literal["scheduling.k8s.io/v1alpha3"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Workload"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Workload.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Workload.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: WorkloadSpec, /) -> "Workload.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [WorkloadSpec.Builder], WorkloadSpec.Builder | WorkloadSpec
            ],
            /,
        ) -> "Workload.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "WorkloadSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the desired behavior of a Workload."
            if self._in_context and value_or_callback is UNSET:
                context = WorkloadSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WorkloadSpec.builder())
                if isinstance(output, WorkloadSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["Workload.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Workload.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Workload."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Workload", Builder]:
        return GenericListBuilder[Workload, Workload.Builder]()

    api_version: Annotated[Literal["scheduling.k8s.io/v1alpha3"], Field(alias="apiVersion")] = (
        "scheduling.k8s.io/v1alpha3"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Workload"] = "Workload"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: WorkloadSpec
    "spec defines the desired behavior of a Workload."


WorkloadList = ResourceList[Workload]


CompositePodGroupTemplate.model_rebuild()
