# Generated by cloudcoil-model-codegen v0.7.0
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    ObjectMeta,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field

from ..core import v1


class EndpointConditions(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointConditions"]:
            return EndpointConditions

        def build(self) -> "EndpointConditions":
            return EndpointConditions(**self._attrs)

        def ready(self, value: bool | None, /) -> Self:
            'ready indicates that this endpoint is ready to receive traffic, according to whatever system is managing the endpoint. A nil value should be interpreted as "true". In general, an endpoint should be marked ready if it is serving and not terminating, though this can be overridden in some cases, such as when the associated Service has set the publishNotReadyAddresses flag.'
            return self._set("ready", value)

        def serving(self, value: bool | None, /) -> Self:
            'serving indicates that this endpoint is able to receive traffic, according to whatever system is managing the endpoint. For endpoints backed by pods, the EndpointSlice controller will mark the endpoint as serving if the pod\u0027s Ready condition is True. A nil value should be interpreted as "true".'
            return self._set("serving", value)

        def terminating(self, value: bool | None, /) -> Self:
            'terminating indicates that this endpoint is terminating. A nil value should be interpreted as "false".'
            return self._set("terminating", value)

    class BuilderContext(BuilderContextBase["EndpointConditions.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointConditions.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointConditions."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointConditions", Builder]:
        return GenericListBuilder[EndpointConditions, EndpointConditions.Builder]()

    ready: bool | None = None
    'ready indicates that this endpoint is ready to receive traffic, according to whatever system is managing the endpoint. A nil value should be interpreted as "true". In general, an endpoint should be marked ready if it is serving and not terminating, though this can be overridden in some cases, such as when the associated Service has set the publishNotReadyAddresses flag.'
    serving: bool | None = None
    'serving indicates that this endpoint is able to receive traffic, according to whatever system is managing the endpoint. For endpoints backed by pods, the EndpointSlice controller will mark the endpoint as serving if the pod\u0027s Ready condition is True. A nil value should be interpreted as "true".'
    terminating: bool | None = None
    'terminating indicates that this endpoint is terminating. A nil value should be interpreted as "false".'


class EndpointPort(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointPort"]:
            return EndpointPort

        def build(self) -> "EndpointPort":
            return EndpointPort(**self._attrs)

        def app_protocol(self, value: str | None, /) -> Self:
            "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
            return self._set("app_protocol", value)

        def name(self, value: str | None, /) -> Self:
            "name represents the name of this port. All ports in an EndpointSlice must have a unique name. If the EndpointSlice is derived from a Kubernetes service, this corresponds to the Service.ports[].name. Name must either be an empty string or pass DNS_LABEL validation: * must be no more than 63 characters long. * must consist of lower case alphanumeric characters or \u0027-\u0027. * must start and end with an alphanumeric character. Default is empty string."
            return self._set("name", value)

        def port(self, value: int | None, /) -> Self:
            "port represents the port number of the endpoint. If the EndpointSlice is derived from a Kubernetes service, this must be set to the service\u0027s target port. EndpointSlices used for other purposes may have a nil port."
            return self._set("port", value)

        def protocol(self, value: str | None, /) -> Self:
            "protocol represents the IP protocol for this port. Must be UDP, TCP, or SCTP. Default is TCP."
            return self._set("protocol", value)

    class BuilderContext(BuilderContextBase["EndpointPort.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointPort.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointPort."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointPort", Builder]:
        return GenericListBuilder[EndpointPort, EndpointPort.Builder]()

    app_protocol: Annotated[str | None, Field(alias="appProtocol")] = None
    "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
    name: str | None = None
    "name represents the name of this port. All ports in an EndpointSlice must have a unique name. If the EndpointSlice is derived from a Kubernetes service, this corresponds to the Service.ports[].name. Name must either be an empty string or pass DNS_LABEL validation: * must be no more than 63 characters long. * must consist of lower case alphanumeric characters or \u0027-\u0027. * must start and end with an alphanumeric character. Default is empty string."
    port: int | None = None
    "port represents the port number of the endpoint. If the EndpointSlice is derived from a Kubernetes service, this must be set to the service\u0027s target port. EndpointSlices used for other purposes may have a nil port."
    protocol: str | None = None
    "protocol represents the IP protocol for this port. Must be UDP, TCP, or SCTP. Default is TCP."


class ForNode(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ForNode"]:
            return ForNode

        def build(self) -> "ForNode":
            return ForNode(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name represents the name of the node."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["ForNode.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ForNode.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ForNode."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ForNode", Builder]:
        return GenericListBuilder[ForNode, ForNode.Builder]()

    name: str
    "name represents the name of the node."


class ForZone(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ForZone"]:
            return ForZone

        def build(self) -> "ForZone":
            return ForZone(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name represents the name of the zone."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["ForZone.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ForZone.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ForZone."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ForZone", Builder]:
        return GenericListBuilder[ForZone, ForZone.Builder]()

    name: str
    "name represents the name of the zone."


class EndpointHints(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointHints"]:
            return EndpointHints

        def build(self) -> "EndpointHints":
            return EndpointHints(**self._attrs)

        @overload
        def for_nodes(
            self, value_or_callback: list[ForNode] | None, /
        ) -> "EndpointHints.Builder": ...

        @overload
        def for_nodes(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ForNode, ForNode.Builder]],
                GenericListBuilder[ForNode, ForNode.Builder] | list[ForNode],
            ],
            /,
        ) -> "EndpointHints.Builder": ...

        @overload
        def for_nodes(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ForNode.Builder]: ...

        def for_nodes(self, value_or_callback=UNSET, /):
            "forNodes indicates the node(s) this endpoint should be consumed by when using topology aware routing. May contain a maximum of 8 entries. This is an Alpha feature and is only used when the PreferSameTrafficDistribution feature gate is enabled."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ForNode.Builder]()
                context._parent_builder = self
                context._field_name = "for_nodes"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ForNode.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("for_nodes", value)

        @overload
        def for_zones(
            self, value_or_callback: list[ForZone] | None, /
        ) -> "EndpointHints.Builder": ...

        @overload
        def for_zones(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ForZone, ForZone.Builder]],
                GenericListBuilder[ForZone, ForZone.Builder] | list[ForZone],
            ],
            /,
        ) -> "EndpointHints.Builder": ...

        @overload
        def for_zones(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ForZone.Builder]: ...

        def for_zones(self, value_or_callback=UNSET, /):
            "forZones indicates the zone(s) this endpoint should be consumed by when using topology aware routing. May contain a maximum of 8 entries."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ForZone.Builder]()
                context._parent_builder = self
                context._field_name = "for_zones"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ForZone.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("for_zones", value)

    class BuilderContext(BuilderContextBase["EndpointHints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointHints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointHints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointHints", Builder]:
        return GenericListBuilder[EndpointHints, EndpointHints.Builder]()

    for_nodes: Annotated[list[ForNode] | None, Field(alias="forNodes")] = None
    "forNodes indicates the node(s) this endpoint should be consumed by when using topology aware routing. May contain a maximum of 8 entries. This is an Alpha feature and is only used when the PreferSameTrafficDistribution feature gate is enabled."
    for_zones: Annotated[list[ForZone] | None, Field(alias="forZones")] = None
    "forZones indicates the zone(s) this endpoint should be consumed by when using topology aware routing. May contain a maximum of 8 entries."


class Endpoint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Endpoint"]:
            return Endpoint

        def build(self) -> "Endpoint":
            return Endpoint(**self._attrs)

        def addresses(self, value: list[str], /) -> Self:
            'addresses of this endpoint. For EndpointSlices of addressType "IPv4" or "IPv6", the values are IP addresses in canonical form. The syntax and semantics of other addressType values are not defined. This must contain at least one address but no more than 100. EndpointSlices generated by the EndpointSlice controller will always have exactly 1 address. No semantics are defined for additional addresses beyond the first, and kube-proxy does not look at them.'
            return self._set("addresses", value)

        @overload
        def conditions(
            self, value_or_callback: EndpointConditions | None, /
        ) -> "Endpoint.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [EndpointConditions.Builder],
                EndpointConditions.Builder | EndpointConditions,
            ],
            /,
        ) -> "Endpoint.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> "EndpointConditions.BuilderContext": ...

        def conditions(self, value_or_callback=UNSET, /):
            "conditions contains information about the current status of the endpoint."
            if self._in_context and value_or_callback is UNSET:
                context = EndpointConditions.BuilderContext()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointConditions.builder())
                if isinstance(output, EndpointConditions.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def deprecated_topology(self, value: dict[str, str] | None, /) -> Self:
            "deprecatedTopology contains topology information part of the v1beta1 API. This field is deprecated, and will be removed when the v1beta1 API is removed (no sooner than kubernetes v1.24).  While this field can hold values, it is not writable through the v1 API, and any attempts to write to it will be silently ignored. Topology information can be found in the zone and nodeName fields instead."
            return self._set("deprecated_topology", value)

        @overload
        def hints(self, value_or_callback: EndpointHints | None, /) -> "Endpoint.Builder": ...

        @overload
        def hints(
            self,
            value_or_callback: Callable[
                [EndpointHints.Builder], EndpointHints.Builder | EndpointHints
            ],
            /,
        ) -> "Endpoint.Builder": ...

        @overload
        def hints(self, value_or_callback: Never = ...) -> "EndpointHints.BuilderContext": ...

        def hints(self, value_or_callback=UNSET, /):
            "hints contains information associated with how an endpoint should be consumed."
            if self._in_context and value_or_callback is UNSET:
                context = EndpointHints.BuilderContext()
                context._parent_builder = self
                context._field_name = "hints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointHints.builder())
                if isinstance(output, EndpointHints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("hints", value)

        def hostname(self, value: str | None, /) -> Self:
            "hostname of this endpoint. This field may be used by consumers of endpoints to distinguish endpoints from each other (e.g. in DNS names). Multiple endpoints which use the same hostname should be considered fungible (e.g. multiple A values in DNS). Must be lowercase and pass DNS Label (RFC 1123) validation."
            return self._set("hostname", value)

        def node_name(self, value: str | None, /) -> Self:
            "nodeName represents the name of the Node hosting this endpoint. This can be used to determine endpoints local to a Node."
            return self._set("node_name", value)

        @overload
        def target_ref(
            self, value_or_callback: v1.ObjectReference | None, /
        ) -> "Endpoint.Builder": ...

        @overload
        def target_ref(
            self,
            value_or_callback: Callable[
                [v1.ObjectReference.Builder],
                v1.ObjectReference.Builder | v1.ObjectReference,
            ],
            /,
        ) -> "Endpoint.Builder": ...

        @overload
        def target_ref(
            self, value_or_callback: Never = ...
        ) -> "v1.ObjectReference.BuilderContext": ...

        def target_ref(self, value_or_callback=UNSET, /):
            "targetRef is a reference to a Kubernetes object that represents this endpoint."
            if self._in_context and value_or_callback is UNSET:
                context = v1.ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "target_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.ObjectReference.builder())
                if isinstance(output, v1.ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target_ref", value)

        def zone(self, value: str | None, /) -> Self:
            "zone is the name of the Zone this endpoint exists in."
            return self._set("zone", value)

    class BuilderContext(BuilderContextBase["Endpoint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Endpoint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Endpoint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Endpoint", Builder]:
        return GenericListBuilder[Endpoint, Endpoint.Builder]()

    addresses: list[str]
    'addresses of this endpoint. For EndpointSlices of addressType "IPv4" or "IPv6", the values are IP addresses in canonical form. The syntax and semantics of other addressType values are not defined. This must contain at least one address but no more than 100. EndpointSlices generated by the EndpointSlice controller will always have exactly 1 address. No semantics are defined for additional addresses beyond the first, and kube-proxy does not look at them.'
    conditions: EndpointConditions | None = None
    "conditions contains information about the current status of the endpoint."
    deprecated_topology: Annotated[dict[str, str] | None, Field(alias="deprecatedTopology")] = None
    "deprecatedTopology contains topology information part of the v1beta1 API. This field is deprecated, and will be removed when the v1beta1 API is removed (no sooner than kubernetes v1.24).  While this field can hold values, it is not writable through the v1 API, and any attempts to write to it will be silently ignored. Topology information can be found in the zone and nodeName fields instead."
    hints: EndpointHints | None = None
    "hints contains information associated with how an endpoint should be consumed."
    hostname: str | None = None
    "hostname of this endpoint. This field may be used by consumers of endpoints to distinguish endpoints from each other (e.g. in DNS names). Multiple endpoints which use the same hostname should be considered fungible (e.g. multiple A values in DNS). Must be lowercase and pass DNS Label (RFC 1123) validation."
    node_name: Annotated[str | None, Field(alias="nodeName")] = None
    "nodeName represents the name of the Node hosting this endpoint. This can be used to determine endpoints local to a Node."
    target_ref: Annotated[v1.ObjectReference | None, Field(alias="targetRef")] = None
    "targetRef is a reference to a Kubernetes object that represents this endpoint."
    zone: str | None = None
    "zone is the name of the Zone this endpoint exists in."


class EndpointSlice(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "endpointslices",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointSlice"]:
            return EndpointSlice

        def build(self) -> "EndpointSlice":
            return EndpointSlice(**self._attrs)

        def address_type(self, value: str, /) -> Self:
            'addressType specifies the type of address carried by this EndpointSlice. All addresses in this slice must be the same type. This field is immutable after creation. The following address types are currently supported: * IPv4: Represents an IPv4 Address. * IPv6: Represents an IPv6 Address. * FQDN: Represents a Fully Qualified Domain Name. (Deprecated) The EndpointSlice controller only generates, and kube-proxy only processes, slices of addressType "IPv4" and "IPv6". No semantics are defined for the "FQDN" type.'
            return self._set("address_type", value)

        def api_version(self, value: Literal["discovery.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        @overload
        def endpoints(self, value_or_callback: list[Endpoint], /) -> "EndpointSlice.Builder": ...

        @overload
        def endpoints(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Endpoint, Endpoint.Builder]],
                GenericListBuilder[Endpoint, Endpoint.Builder] | list[Endpoint],
            ],
            /,
        ) -> "EndpointSlice.Builder": ...

        @overload
        def endpoints(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Endpoint.Builder]: ...

        def endpoints(self, value_or_callback=UNSET, /):
            "endpoints is a list of unique endpoints in this slice. Each slice may include a maximum of 1000 endpoints."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Endpoint.Builder]()
                context._parent_builder = self
                context._field_name = "endpoints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Endpoint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("endpoints", value)

        def kind(self, value: Literal["EndpointSlice"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "EndpointSlice.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "EndpointSlice.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def ports(
            self, value_or_callback: list[EndpointPort] | None, /
        ) -> "EndpointSlice.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EndpointPort, EndpointPort.Builder]],
                GenericListBuilder[EndpointPort, EndpointPort.Builder] | list[EndpointPort],
            ],
            /,
        ) -> "EndpointSlice.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EndpointPort.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            "ports specifies the list of network ports exposed by each endpoint in this slice. Each port must have a unique name. Each slice may include a maximum of 100 ports. Services always have at least 1 port, so EndpointSlices generated by the EndpointSlice controller will likewise always have at least 1 port. EndpointSlices used for other purposes may have an empty ports list."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EndpointPort.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointPort.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

    class BuilderContext(BuilderContextBase["EndpointSlice.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointSlice.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointSlice."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointSlice", Builder]:
        return GenericListBuilder[EndpointSlice, EndpointSlice.Builder]()

    address_type: Annotated[str, Field(alias="addressType")]
    'addressType specifies the type of address carried by this EndpointSlice. All addresses in this slice must be the same type. This field is immutable after creation. The following address types are currently supported: * IPv4: Represents an IPv4 Address. * IPv6: Represents an IPv6 Address. * FQDN: Represents a Fully Qualified Domain Name. (Deprecated) The EndpointSlice controller only generates, and kube-proxy only processes, slices of addressType "IPv4" and "IPv6". No semantics are defined for the "FQDN" type.'
    api_version: Annotated[Literal["discovery.k8s.io/v1"], Field(alias="apiVersion")] = (
        "discovery.k8s.io/v1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    endpoints: list[Endpoint]
    "endpoints is a list of unique endpoints in this slice. Each slice may include a maximum of 1000 endpoints."
    kind: Literal["EndpointSlice"] = "EndpointSlice"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata."
    ports: list[EndpointPort] | None = None
    "ports specifies the list of network ports exposed by each endpoint in this slice. Each port must have a unique name. Each slice may include a maximum of 100 ports. Services always have at least 1 port, so EndpointSlices generated by the EndpointSlice controller will likewise always have at least 1 port. EndpointSlices used for other purposes may have an empty ports list."


EndpointSliceList = ResourceList[EndpointSlice]
