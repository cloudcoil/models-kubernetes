# Generated by cloudcoil-model-codegen v0.7.0
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    ObjectMeta,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class ServerStorageVersion(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerStorageVersion"]:
            return ServerStorageVersion

        def build(self) -> "ServerStorageVersion":
            return ServerStorageVersion(**self._attrs)

        def api_server_id(self, value: str, /) -> Self:
            "apiServerID is the ID of the reporting API server."
            return self._set("api_server_id", value)

        def decodable_versions(self, value: list[str], /) -> Self:
            "decodableVersions are the encoding versions the API server can handle to decode. The API server can decode objects encoded in these versions. The encodingVersion must be included in the decodableVersions."
            return self._set("decodable_versions", value)

        def encoding_version(self, value: str, /) -> Self:
            "encodingVersion the API server encodes the object to when persisting it in the backend (e.g., etcd)."
            return self._set("encoding_version", value)

        def served_versions(self, value: list[str] | None, /) -> Self:
            "servedVersions lists all versions the API server can serve. DecodableVersions must include all ServedVersions."
            return self._set("served_versions", value)

    class BuilderContext(BuilderContextBase["ServerStorageVersion.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerStorageVersion.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerStorageVersion."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServerStorageVersion", Builder]:
        return GenericListBuilder[ServerStorageVersion, ServerStorageVersion.Builder]()

    api_server_id: Annotated[str, Field(alias="apiServerID")]
    "apiServerID is the ID of the reporting API server."
    decodable_versions: Annotated[list[str], Field(alias="decodableVersions")]
    "decodableVersions are the encoding versions the API server can handle to decode. The API server can decode objects encoded in these versions. The encodingVersion must be included in the decodableVersions."
    encoding_version: Annotated[str, Field(alias="encodingVersion")]
    "encodingVersion the API server encodes the object to when persisting it in the backend (e.g., etcd)."
    served_versions: Annotated[list[str] | None, Field(alias="servedVersions")] = None
    "servedVersions lists all versions the API server can serve. DecodableVersions must include all ServedVersions."


class StorageVersionCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageVersionCondition"]:
            return StorageVersionCondition

        def build(self) -> "StorageVersionCondition":
            return StorageVersionCondition(**self._attrs)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "StorageVersionCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "StorageVersionCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "lastTransitionTime is the last time the condition transitioned from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str, /) -> Self:
            "message is a human readable string indicating details about the transition."
            return self._set("message", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "observedGeneration represents the .metadata.generation that the condition was set based upon, if field is set."
            return self._set("observed_generation", value)

        def reason(self, value: str, /) -> Self:
            "reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "type of the condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["StorageVersionCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageVersionCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageVersionCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageVersionCondition", Builder]:
        return GenericListBuilder[StorageVersionCondition, StorageVersionCondition.Builder]()

    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "lastTransitionTime is the last time the condition transitioned from one status to another."
    message: str
    "message is a human readable string indicating details about the transition."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "observedGeneration represents the .metadata.generation that the condition was set based upon, if field is set."
    reason: str
    "reason for the condition\u0027s last transition."
    status: str
    "status of the condition, one of True, False, Unknown."
    type: str
    "type of the condition."


class StorageVersionSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageVersionSpec"]:
            return StorageVersionSpec

        def build(self) -> "StorageVersionSpec":
            return StorageVersionSpec(**self._attrs)

    class BuilderContext(BuilderContextBase["StorageVersionSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageVersionSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageVersionSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageVersionSpec", Builder]:
        return GenericListBuilder[StorageVersionSpec, StorageVersionSpec.Builder]()

    pass


class StorageVersionStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageVersionStatus"]:
            return StorageVersionStatus

        def build(self) -> "StorageVersionStatus":
            return StorageVersionStatus(**self._attrs)

        def common_encoding_version(self, value: str | None, /) -> Self:
            "commonEncodingVersion is set to an encoding storage version if all API server instances share that same version. If they don\u0027t share one storage version, this field is left empty. API servers should finish updating its storageVersionStatus entry before serving write operations, so that this field will be in sync with the reality."
            return self._set("common_encoding_version", value)

        @overload
        def conditions(
            self, value_or_callback: list[StorageVersionCondition] | None, /
        ) -> "StorageVersionStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[StorageVersionCondition, StorageVersionCondition.Builder]],
                GenericListBuilder[StorageVersionCondition, StorageVersionCondition.Builder]
                | list[StorageVersionCondition],
            ],
            /,
        ) -> "StorageVersionStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[StorageVersionCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "conditions lists the latest available observations of the storageVersion\u0027s state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[StorageVersionCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageVersionCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def storage_versions(
            self, value_or_callback: list[ServerStorageVersion] | None, /
        ) -> "StorageVersionStatus.Builder": ...

        @overload
        def storage_versions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ServerStorageVersion, ServerStorageVersion.Builder]],
                GenericListBuilder[ServerStorageVersion, ServerStorageVersion.Builder]
                | list[ServerStorageVersion],
            ],
            /,
        ) -> "StorageVersionStatus.Builder": ...

        @overload
        def storage_versions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ServerStorageVersion.Builder]: ...

        def storage_versions(self, value_or_callback=UNSET, /):
            "storageVersions lists the reported versions per API server instance."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ServerStorageVersion.Builder]()
                context._parent_builder = self
                context._field_name = "storage_versions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServerStorageVersion.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("storage_versions", value)

    class BuilderContext(BuilderContextBase["StorageVersionStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageVersionStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageVersionStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageVersionStatus", Builder]:
        return GenericListBuilder[StorageVersionStatus, StorageVersionStatus.Builder]()

    common_encoding_version: Annotated[str | None, Field(alias="commonEncodingVersion")] = None
    "commonEncodingVersion is set to an encoding storage version if all API server instances share that same version. If they don\u0027t share one storage version, this field is left empty. API servers should finish updating its storageVersionStatus entry before serving write operations, so that this field will be in sync with the reality."
    conditions: list[StorageVersionCondition] | None = None
    "conditions lists the latest available observations of the storageVersion\u0027s state."
    storage_versions: Annotated[
        list[ServerStorageVersion] | None, Field(alias="storageVersions")
    ] = None
    "storageVersions lists the reported versions per API server instance."


class StorageVersion(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "storageversions",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageVersion"]:
            return StorageVersion

        def build(self) -> "StorageVersion":
            return StorageVersion(**self._attrs)

        def api_version(self, value: Literal["internal.apiserver.k8s.io/v1alpha1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["StorageVersion"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "StorageVersion.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "StorageVersion.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata. The name is \u003cgroup\u003e.\u003cresource\u003e."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: StorageVersionSpec | None, /
        ) -> "StorageVersion.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [StorageVersionSpec.Builder],
                StorageVersionSpec.Builder | StorageVersionSpec,
            ],
            /,
        ) -> "StorageVersion.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "StorageVersionSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec is an empty spec. It is here to comply with Kubernetes API style."
            if self._in_context and value_or_callback is UNSET:
                context = StorageVersionSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageVersionSpec.builder())
                if isinstance(output, StorageVersionSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: StorageVersionStatus | None, /
        ) -> "StorageVersion.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [StorageVersionStatus.Builder],
                StorageVersionStatus.Builder | StorageVersionStatus,
            ],
            /,
        ) -> "StorageVersion.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "StorageVersionStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status on the version the API server instance can decode from and encode objects to when persisting objects in the backend."
            if self._in_context and value_or_callback is UNSET:
                context = StorageVersionStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageVersionStatus.builder())
                if isinstance(output, StorageVersionStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["StorageVersion.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageVersion.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageVersion."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageVersion", Builder]:
        return GenericListBuilder[StorageVersion, StorageVersion.Builder]()

    api_version: Annotated[
        Literal["internal.apiserver.k8s.io/v1alpha1"], Field(alias="apiVersion")
    ] = "internal.apiserver.k8s.io/v1alpha1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["StorageVersion"] = "StorageVersion"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata. The name is \u003cgroup\u003e.\u003cresource\u003e."
    spec: StorageVersionSpec | None = None
    "spec is an empty spec. It is here to comply with Kubernetes API style."
    status: StorageVersionStatus | None = None
    "status on the version the API server instance can decode from and encode objects to when persisting objects in the backend."


StorageVersionList = ResourceList[StorageVersion]
