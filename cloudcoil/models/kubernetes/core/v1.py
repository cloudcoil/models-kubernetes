# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    Condition,
    IntOrString,
    LabelSelector,
    MicroTime,
    ObjectMeta,
    Quantity,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class AWSElasticBlockStoreVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AWSElasticBlockStoreVolumeSource"]:
            return AWSElasticBlockStoreVolumeSource

        def build(self) -> "AWSElasticBlockStoreVolumeSource":
            return AWSElasticBlockStoreVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore'
            return self._set("fs_type", value)

        def partition(self, value: int | None, /) -> Self:
            'partition is the partition in the volume that you want to mount. If omitted, the default is to mount by volume name. Examples: For volume /dev/sda1, you specify the partition as "1". Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).'
            return self._set("partition", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly value true will force the readOnly setting in VolumeMounts. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
            return self._set("read_only", value)

        def volume_id(self, value: str, /) -> Self:
            "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume). More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
            return self._set("volume_id", value)

    class BuilderContext(BuilderContextBase["AWSElasticBlockStoreVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AWSElasticBlockStoreVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AWSElasticBlockStoreVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["AWSElasticBlockStoreVolumeSource", Builder]:
        return GenericListBuilder[
            AWSElasticBlockStoreVolumeSource, AWSElasticBlockStoreVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore'
    partition: int | None = None
    'partition is the partition in the volume that you want to mount. If omitted, the default is to mount by volume name. Examples: For volume /dev/sda1, you specify the partition as "1". Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly value true will force the readOnly setting in VolumeMounts. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
    volume_id: Annotated[str, Field(alias="volumeID")]
    "volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume). More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"


class AppArmorProfile(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AppArmorProfile"]:
            return AppArmorProfile

        def build(self) -> "AppArmorProfile":
            return AppArmorProfile(**self._attrs)

        def localhost_profile(self, value: str | None, /) -> Self:
            'localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost".'
            return self._set("localhost_profile", value)

        def type(self, value: str, /) -> Self:
            "type indicates which kind of AppArmor profile will be applied. Valid options are:\n  Localhost - a profile pre-loaded on the node.\n  RuntimeDefault - the container runtime\u0027s default profile.\n  Unconfined - no AppArmor enforcement."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["AppArmorProfile.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AppArmorProfile.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AppArmorProfile."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AppArmorProfile", Builder]:
        return GenericListBuilder[AppArmorProfile, AppArmorProfile.Builder]()

    localhost_profile: Annotated[str | None, Field(alias="localhostProfile")] = None
    'localhostProfile indicates a profile loaded on the node that should be used. The profile must be preconfigured on the node to work. Must match the loaded name of the profile. Must be set if and only if type is "Localhost".'
    type: str
    "type indicates which kind of AppArmor profile will be applied. Valid options are:\n  Localhost - a profile pre-loaded on the node.\n  RuntimeDefault - the container runtime\u0027s default profile.\n  Unconfined - no AppArmor enforcement."


class AttachedVolume(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AttachedVolume"]:
            return AttachedVolume

        def build(self) -> "AttachedVolume":
            return AttachedVolume(**self._attrs)

        def device_path(self, value: str, /) -> Self:
            "DevicePath represents the device path where the volume should be available"
            return self._set("device_path", value)

        def name(self, value: str, /) -> Self:
            "Name of the attached volume"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["AttachedVolume.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AttachedVolume.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AttachedVolume."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AttachedVolume", Builder]:
        return GenericListBuilder[AttachedVolume, AttachedVolume.Builder]()

    device_path: Annotated[str, Field(alias="devicePath")]
    "DevicePath represents the device path where the volume should be available"
    name: str
    "Name of the attached volume"


class AzureDiskVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AzureDiskVolumeSource"]:
            return AzureDiskVolumeSource

        def build(self) -> "AzureDiskVolumeSource":
            return AzureDiskVolumeSource(**self._attrs)

        def caching_mode(self, value: str | None, /) -> Self:
            "cachingMode is the Host Caching mode: None, Read Only, Read Write."
            return self._set("caching_mode", value)

        def disk_name(self, value: str, /) -> Self:
            "diskName is the Name of the data disk in the blob storage"
            return self._set("disk_name", value)

        def disk_uri(self, value: str, /) -> Self:
            "diskURI is the URI of data disk in the blob storage"
            return self._set("disk_uri", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def kind(self, value: str | None, /) -> Self:
            "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared"
            return self._set("kind", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

    class BuilderContext(BuilderContextBase["AzureDiskVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AzureDiskVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AzureDiskVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AzureDiskVolumeSource", Builder]:
        return GenericListBuilder[AzureDiskVolumeSource, AzureDiskVolumeSource.Builder]()

    caching_mode: Annotated[str | None, Field(alias="cachingMode")] = None
    "cachingMode is the Host Caching mode: None, Read Only, Read Write."
    disk_name: Annotated[str, Field(alias="diskName")]
    "diskName is the Name of the data disk in the blob storage"
    disk_uri: Annotated[str, Field(alias="diskURI")]
    "diskURI is the URI of data disk in the blob storage"
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    kind: str | None = None
    "kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."


class AzureFilePersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AzureFilePersistentVolumeSource"]:
            return AzureFilePersistentVolumeSource

        def build(self) -> "AzureFilePersistentVolumeSource":
            return AzureFilePersistentVolumeSource(**self._attrs)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        def secret_name(self, value: str, /) -> Self:
            "secretName is the name of secret that contains Azure Storage Account Name and Key"
            return self._set("secret_name", value)

        def secret_namespace(self, value: str | None, /) -> Self:
            "secretNamespace is the namespace of the secret that contains Azure Storage Account Name and Key default is the same as the Pod"
            return self._set("secret_namespace", value)

        def share_name(self, value: str, /) -> Self:
            "shareName is the azure Share Name"
            return self._set("share_name", value)

    class BuilderContext(BuilderContextBase["AzureFilePersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AzureFilePersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AzureFilePersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["AzureFilePersistentVolumeSource", Builder]:
        return GenericListBuilder[
            AzureFilePersistentVolumeSource, AzureFilePersistentVolumeSource.Builder
        ]()

    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_name: Annotated[str, Field(alias="secretName")]
    "secretName is the name of secret that contains Azure Storage Account Name and Key"
    secret_namespace: Annotated[str | None, Field(alias="secretNamespace")] = None
    "secretNamespace is the namespace of the secret that contains Azure Storage Account Name and Key default is the same as the Pod"
    share_name: Annotated[str, Field(alias="shareName")]
    "shareName is the azure Share Name"


class AzureFileVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AzureFileVolumeSource"]:
            return AzureFileVolumeSource

        def build(self) -> "AzureFileVolumeSource":
            return AzureFileVolumeSource(**self._attrs)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        def secret_name(self, value: str, /) -> Self:
            "secretName is the  name of secret that contains Azure Storage Account Name and Key"
            return self._set("secret_name", value)

        def share_name(self, value: str, /) -> Self:
            "shareName is the azure share Name"
            return self._set("share_name", value)

    class BuilderContext(BuilderContextBase["AzureFileVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AzureFileVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AzureFileVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AzureFileVolumeSource", Builder]:
        return GenericListBuilder[AzureFileVolumeSource, AzureFileVolumeSource.Builder]()

    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_name: Annotated[str, Field(alias="secretName")]
    "secretName is the  name of secret that contains Azure Storage Account Name and Key"
    share_name: Annotated[str, Field(alias="shareName")]
    "shareName is the azure share Name"


class Capabilities(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Capabilities"]:
            return Capabilities

        def build(self) -> "Capabilities":
            return Capabilities(**self._attrs)

        def add(self, value: list[str] | None, /) -> Self:
            "Added capabilities"
            return self._set("add", value)

        def drop(self, value: list[str] | None, /) -> Self:
            "Removed capabilities"
            return self._set("drop", value)

    class BuilderContext(BuilderContextBase["Capabilities.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Capabilities.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Capabilities."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Capabilities", Builder]:
        return GenericListBuilder[Capabilities, Capabilities.Builder]()

    add: list[str] | None = None
    "Added capabilities"
    drop: list[str] | None = None
    "Removed capabilities"


class ClientIPConfig(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ClientIPConfig"]:
            return ClientIPConfig

        def build(self) -> "ClientIPConfig":
            return ClientIPConfig(**self._attrs)

        def timeout_seconds(self, value: int | None, /) -> Self:
            'timeoutSeconds specifies the seconds of ClientIP type session sticky time. The value must be \u003e0 \u0026\u0026 \u003c=86400(for 1 day) if ServiceAffinity == "ClientIP". Default value is 10800(for 3 hours).'
            return self._set("timeout_seconds", value)

    class BuilderContext(BuilderContextBase["ClientIPConfig.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ClientIPConfig.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ClientIPConfig."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ClientIPConfig", Builder]:
        return GenericListBuilder[ClientIPConfig, ClientIPConfig.Builder]()

    timeout_seconds: Annotated[int | None, Field(alias="timeoutSeconds")] = None
    'timeoutSeconds specifies the seconds of ClientIP type session sticky time. The value must be \u003e0 \u0026\u0026 \u003c=86400(for 1 day) if ServiceAffinity == "ClientIP". Default value is 10800(for 3 hours).'


class ClusterTrustBundleProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ClusterTrustBundleProjection"]:
            return ClusterTrustBundleProjection

        def build(self) -> "ClusterTrustBundleProjection":
            return ClusterTrustBundleProjection(**self._attrs)

        @overload
        def label_selector(
            self, value_or_callback: LabelSelector | None, /
        ) -> "ClusterTrustBundleProjection.Builder": ...

        @overload
        def label_selector(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "ClusterTrustBundleProjection.Builder": ...

        @overload
        def label_selector(
            self, value_or_callback: Never = ...
        ) -> "LabelSelector.BuilderContext": ...

        def label_selector(self, value_or_callback=UNSET, /):
            'Select all ClusterTrustBundles that match this label selector.  Only has effect if signerName is set.  Mutually-exclusive with name.  If unset, interpreted as "match nothing".  If set but empty, interpreted as "match everything".'
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "label_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("label_selector", value)

        def name(self, value: str | None, /) -> Self:
            "Select a single ClusterTrustBundle by object name.  Mutually-exclusive with signerName and labelSelector."
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "If true, don\u0027t block pod startup if the referenced ClusterTrustBundle(s) aren\u0027t available.  If using name, then the named ClusterTrustBundle is allowed not to exist.  If using signerName, then the combination of signerName and labelSelector is allowed to match zero ClusterTrustBundles."
            return self._set("optional", value)

        def path(self, value: str, /) -> Self:
            "Relative path from the volume root to write the bundle."
            return self._set("path", value)

        def signer_name(self, value: str | None, /) -> Self:
            "Select all ClusterTrustBundles that match this signer name. Mutually-exclusive with name.  The contents of all selected ClusterTrustBundles will be unified and deduplicated."
            return self._set("signer_name", value)

    class BuilderContext(BuilderContextBase["ClusterTrustBundleProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ClusterTrustBundleProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ClusterTrustBundleProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ClusterTrustBundleProjection", Builder]:
        return GenericListBuilder[
            ClusterTrustBundleProjection, ClusterTrustBundleProjection.Builder
        ]()

    label_selector: Annotated[LabelSelector | None, Field(alias="labelSelector")] = None
    'Select all ClusterTrustBundles that match this label selector.  Only has effect if signerName is set.  Mutually-exclusive with name.  If unset, interpreted as "match nothing".  If set but empty, interpreted as "match everything".'
    name: str | None = None
    "Select a single ClusterTrustBundle by object name.  Mutually-exclusive with signerName and labelSelector."
    optional: bool | None = None
    "If true, don\u0027t block pod startup if the referenced ClusterTrustBundle(s) aren\u0027t available.  If using name, then the named ClusterTrustBundle is allowed not to exist.  If using signerName, then the combination of signerName and labelSelector is allowed to match zero ClusterTrustBundles."
    path: str
    "Relative path from the volume root to write the bundle."
    signer_name: Annotated[str | None, Field(alias="signerName")] = None
    "Select all ClusterTrustBundles that match this signer name. Mutually-exclusive with name.  The contents of all selected ClusterTrustBundles will be unified and deduplicated."


class ComponentCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ComponentCondition"]:
            return ComponentCondition

        def build(self) -> "ComponentCondition":
            return ComponentCondition(**self._attrs)

        def error(self, value: str | None, /) -> Self:
            "Condition error code for a component. For example, a health check error code."
            return self._set("error", value)

        def message(self, value: str | None, /) -> Self:
            "Message about the condition for a component. For example, information about a health check."
            return self._set("message", value)

        def status(self, value: str, /) -> Self:
            'Status of the condition for a component. Valid values for "Healthy": "True", "False", or "Unknown".'
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            'Type of condition for a component. Valid value: "Healthy"'
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["ComponentCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ComponentCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ComponentCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ComponentCondition", Builder]:
        return GenericListBuilder[ComponentCondition, ComponentCondition.Builder]()

    error: str | None = None
    "Condition error code for a component. For example, a health check error code."
    message: str | None = None
    "Message about the condition for a component. For example, information about a health check."
    status: str
    'Status of the condition for a component. Valid values for "Healthy": "True", "False", or "Unknown".'
    type: str
    'Type of condition for a component. Valid value: "Healthy"'


class ComponentStatus(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "componentstatuses",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ComponentStatus"]:
            return ComponentStatus

        def build(self) -> "ComponentStatus":
            return ComponentStatus(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        @overload
        def conditions(
            self, value_or_callback: list[ComponentCondition] | None, /
        ) -> "ComponentStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ComponentCondition, ComponentCondition.Builder]],
                GenericListBuilder[ComponentCondition, ComponentCondition.Builder]
                | list[ComponentCondition],
            ],
            /,
        ) -> "ComponentStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ComponentCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "List of component conditions observed"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ComponentCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ComponentCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def kind(self, value: Literal["ComponentStatus"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ComponentStatus.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ComponentStatus.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

    class BuilderContext(BuilderContextBase["ComponentStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ComponentStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ComponentStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ComponentStatus", Builder]:
        return GenericListBuilder[ComponentStatus, ComponentStatus.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    conditions: list[ComponentCondition] | None = None
    "List of component conditions observed"
    kind: Literal["ComponentStatus"] = "ComponentStatus"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"


ComponentStatusList = ResourceList[ComponentStatus]


class ConfigMap(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "configmaps",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMap"]:
            return ConfigMap

        def build(self) -> "ConfigMap":
            return ConfigMap(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def binary_data(self, value: dict[str, str] | None, /) -> Self:
            "BinaryData contains the binary data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. BinaryData can contain byte sequences that are not in the UTF-8 range. The keys stored in BinaryData must not overlap with the ones in the Data field, this is enforced during validation process. Using this field will require 1.10+ apiserver and kubelet."
            return self._set("binary_data", value)

        def data(self, value: dict[str, str] | None, /) -> Self:
            "Data contains the configuration data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. Values with non-UTF-8 byte sequences must use the BinaryData field. The keys stored in Data must not overlap with the keys in the BinaryData field, this is enforced during validation process."
            return self._set("data", value)

        def immutable(self, value: bool | None, /) -> Self:
            "Immutable, if set to true, ensures that data stored in the ConfigMap cannot be updated (only object metadata can be modified). If not set to true, the field can be modified at any time. Defaulted to nil."
            return self._set("immutable", value)

        def kind(self, value: Literal["ConfigMap"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "ConfigMap.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ConfigMap.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

    class BuilderContext(BuilderContextBase["ConfigMap.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMap.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMap."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMap", Builder]:
        return GenericListBuilder[ConfigMap, ConfigMap.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    binary_data: Annotated[dict[str, str] | None, Field(alias="binaryData")] = None
    "BinaryData contains the binary data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. BinaryData can contain byte sequences that are not in the UTF-8 range. The keys stored in BinaryData must not overlap with the ones in the Data field, this is enforced during validation process. Using this field will require 1.10+ apiserver and kubelet."
    data: dict[str, str] | None = None
    "Data contains the configuration data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. Values with non-UTF-8 byte sequences must use the BinaryData field. The keys stored in Data must not overlap with the keys in the BinaryData field, this is enforced during validation process."
    immutable: bool | None = None
    "Immutable, if set to true, ensures that data stored in the ConfigMap cannot be updated (only object metadata can be modified). If not set to true, the field can be modified at any time. Defaulted to nil."
    kind: Literal["ConfigMap"] = "ConfigMap"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"


class ConfigMapEnvSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMapEnvSource"]:
            return ConfigMapEnvSource

        def build(self) -> "ConfigMapEnvSource":
            return ConfigMapEnvSource(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "Specify whether the ConfigMap must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["ConfigMapEnvSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMapEnvSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMapEnvSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMapEnvSource", Builder]:
        return GenericListBuilder[ConfigMapEnvSource, ConfigMapEnvSource.Builder]()

    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "Specify whether the ConfigMap must be defined"


class ConfigMapKeySelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMapKeySelector"]:
            return ConfigMapKeySelector

        def build(self) -> "ConfigMapKeySelector":
            return ConfigMapKeySelector(**self._attrs)

        def key(self, value: str, /) -> Self:
            "The key to select."
            return self._set("key", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "Specify whether the ConfigMap or its key must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["ConfigMapKeySelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMapKeySelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMapKeySelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMapKeySelector", Builder]:
        return GenericListBuilder[ConfigMapKeySelector, ConfigMapKeySelector.Builder]()

    key: str
    "The key to select."
    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "Specify whether the ConfigMap or its key must be defined"


ConfigMapList = ResourceList[ConfigMap]


class ConfigMapNodeConfigSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMapNodeConfigSource"]:
            return ConfigMapNodeConfigSource

        def build(self) -> "ConfigMapNodeConfigSource":
            return ConfigMapNodeConfigSource(**self._attrs)

        def kubelet_config_key(self, value: str, /) -> Self:
            "KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases."
            return self._set("kubelet_config_key", value)

        def name(self, value: str, /) -> Self:
            "Name is the metadata.name of the referenced ConfigMap. This field is required in all cases."
            return self._set("name", value)

        def namespace(self, value: str, /) -> Self:
            "Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases."
            return self._set("namespace", value)

        def resource_version(self, value: str | None, /) -> Self:
            "ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status."
            return self._set("resource_version", value)

        def uid(self, value: str | None, /) -> Self:
            "UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status."
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["ConfigMapNodeConfigSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMapNodeConfigSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMapNodeConfigSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMapNodeConfigSource", Builder]:
        return GenericListBuilder[ConfigMapNodeConfigSource, ConfigMapNodeConfigSource.Builder]()

    kubelet_config_key: Annotated[str, Field(alias="kubeletConfigKey")]
    "KubeletConfigKey declares which key of the referenced ConfigMap corresponds to the KubeletConfiguration structure This field is required in all cases."
    name: str
    "Name is the metadata.name of the referenced ConfigMap. This field is required in all cases."
    namespace: str
    "Namespace is the metadata.namespace of the referenced ConfigMap. This field is required in all cases."
    resource_version: Annotated[str | None, Field(alias="resourceVersion")] = None
    "ResourceVersion is the metadata.ResourceVersion of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status."
    uid: str | None = None
    "UID is the metadata.UID of the referenced ConfigMap. This field is forbidden in Node.Spec, and required in Node.Status."


class ContainerExtendedResourceRequest(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerExtendedResourceRequest"]:
            return ContainerExtendedResourceRequest

        def build(self) -> "ContainerExtendedResourceRequest":
            return ContainerExtendedResourceRequest(**self._attrs)

        def container_name(self, value: str, /) -> Self:
            "The name of the container requesting resources."
            return self._set("container_name", value)

        def request_name(self, value: str, /) -> Self:
            "The name of the request in the special ResourceClaim which corresponds to the extended resource."
            return self._set("request_name", value)

        def resource_name(self, value: str, /) -> Self:
            "The name of the extended resource in that container which gets backed by DRA."
            return self._set("resource_name", value)

    class BuilderContext(BuilderContextBase["ContainerExtendedResourceRequest.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerExtendedResourceRequest.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerExtendedResourceRequest."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ContainerExtendedResourceRequest", Builder]:
        return GenericListBuilder[
            ContainerExtendedResourceRequest, ContainerExtendedResourceRequest.Builder
        ]()

    container_name: Annotated[str, Field(alias="containerName")]
    "The name of the container requesting resources."
    request_name: Annotated[str, Field(alias="requestName")]
    "The name of the request in the special ResourceClaim which corresponds to the extended resource."
    resource_name: Annotated[str, Field(alias="resourceName")]
    "The name of the extended resource in that container which gets backed by DRA."


class ContainerImage(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerImage"]:
            return ContainerImage

        def build(self) -> "ContainerImage":
            return ContainerImage(**self._attrs)

        def names(self, value: list[str] | None, /) -> Self:
            'Names by which this image is known. e.g. ["kubernetes.example/hyperkube:v1.0.7", "cloud-vendor.registry.example/cloud-vendor/hyperkube:v1.0.7"]'
            return self._set("names", value)

        def size_bytes(self, value: int | None, /) -> Self:
            "The size of the image in bytes."
            return self._set("size_bytes", value)

    class BuilderContext(BuilderContextBase["ContainerImage.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerImage.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerImage."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerImage", Builder]:
        return GenericListBuilder[ContainerImage, ContainerImage.Builder]()

    names: list[str] | None = None
    'Names by which this image is known. e.g. ["kubernetes.example/hyperkube:v1.0.7", "cloud-vendor.registry.example/cloud-vendor/hyperkube:v1.0.7"]'
    size_bytes: Annotated[int | None, Field(alias="sizeBytes")] = None
    "The size of the image in bytes."


class ContainerPort(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerPort"]:
            return ContainerPort

        def build(self) -> "ContainerPort":
            return ContainerPort(**self._attrs)

        def container_port(self, value: int, /) -> Self:
            "Number of port to expose on the pod\u0027s IP address. This must be a valid port number, 0 \u003c x \u003c 65536."
            return self._set("container_port", value)

        def host_ip(self, value: str | None, /) -> Self:
            "What host IP to bind the external port to."
            return self._set("host_ip", value)

        def host_port(self, value: int | None, /) -> Self:
            "Number of port to expose on the host. If specified, this must be a valid port number, 0 \u003c x \u003c 65536. If HostNetwork is specified, this must match ContainerPort. Most containers do not need this."
            return self._set("host_port", value)

        def name(self, value: str | None, /) -> Self:
            "If specified, this must be an IANA_SVC_NAME and unique within the pod. Each named port in a pod must have a unique name. Name for the port that can be referred to by services."
            return self._set("name", value)

        def protocol(self, value: str | None, /) -> Self:
            'Protocol for port. Must be UDP, TCP, or SCTP. Defaults to "TCP".'
            return self._set("protocol", value)

    class BuilderContext(BuilderContextBase["ContainerPort.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerPort.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerPort."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerPort", Builder]:
        return GenericListBuilder[ContainerPort, ContainerPort.Builder]()

    container_port: Annotated[int, Field(alias="containerPort")]
    "Number of port to expose on the pod\u0027s IP address. This must be a valid port number, 0 \u003c x \u003c 65536."
    host_ip: Annotated[str | None, Field(alias="hostIP")] = None
    "What host IP to bind the external port to."
    host_port: Annotated[int | None, Field(alias="hostPort")] = None
    "Number of port to expose on the host. If specified, this must be a valid port number, 0 \u003c x \u003c 65536. If HostNetwork is specified, this must match ContainerPort. Most containers do not need this."
    name: str | None = None
    "If specified, this must be an IANA_SVC_NAME and unique within the pod. Each named port in a pod must have a unique name. Name for the port that can be referred to by services."
    protocol: str | None = None
    'Protocol for port. Must be UDP, TCP, or SCTP. Defaults to "TCP".'


class ContainerResizePolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerResizePolicy"]:
            return ContainerResizePolicy

        def build(self) -> "ContainerResizePolicy":
            return ContainerResizePolicy(**self._attrs)

        def resource_name(self, value: str, /) -> Self:
            "Name of the resource to which this resource resize policy applies. Supported values: cpu, memory."
            return self._set("resource_name", value)

        def restart_policy(self, value: str, /) -> Self:
            "Restart policy to apply when specified resource is resized. If not specified, it defaults to NotRequired."
            return self._set("restart_policy", value)

    class BuilderContext(BuilderContextBase["ContainerResizePolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerResizePolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerResizePolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerResizePolicy", Builder]:
        return GenericListBuilder[ContainerResizePolicy, ContainerResizePolicy.Builder]()

    resource_name: Annotated[str, Field(alias="resourceName")]
    "Name of the resource to which this resource resize policy applies. Supported values: cpu, memory."
    restart_policy: Annotated[str, Field(alias="restartPolicy")]
    "Restart policy to apply when specified resource is resized. If not specified, it defaults to NotRequired."


class ContainerRestartRuleOnExitCodes(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerRestartRuleOnExitCodes"]:
            return ContainerRestartRuleOnExitCodes

        def build(self) -> "ContainerRestartRuleOnExitCodes":
            return ContainerRestartRuleOnExitCodes(**self._attrs)

        def operator(self, value: str, /) -> Self:
            "Represents the relationship between the container exit code(s) and the specified values. Possible values are: - In: the requirement is satisfied if the container exit code is in the\n  set of specified values.\n- NotIn: the requirement is satisfied if the container exit code is\n  not in the set of specified values."
            return self._set("operator", value)

        def values(self, value: list[int] | None, /) -> Self:
            "Specifies the set of values to check for container exit codes. At most 255 elements are allowed."
            return self._set("values", value)

    class BuilderContext(BuilderContextBase["ContainerRestartRuleOnExitCodes.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerRestartRuleOnExitCodes.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerRestartRuleOnExitCodes."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ContainerRestartRuleOnExitCodes", Builder]:
        return GenericListBuilder[
            ContainerRestartRuleOnExitCodes, ContainerRestartRuleOnExitCodes.Builder
        ]()

    operator: str
    "Represents the relationship between the container exit code(s) and the specified values. Possible values are: - In: the requirement is satisfied if the container exit code is in the\n  set of specified values.\n- NotIn: the requirement is satisfied if the container exit code is\n  not in the set of specified values."
    values: list[int] | None = None
    "Specifies the set of values to check for container exit codes. At most 255 elements are allowed."


class ContainerStateRunning(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerStateRunning"]:
            return ContainerStateRunning

        def build(self) -> "ContainerStateRunning":
            return ContainerStateRunning(**self._attrs)

        @overload
        def started_at(
            self, value_or_callback: Time | None, /
        ) -> "ContainerStateRunning.Builder": ...

        @overload
        def started_at(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ContainerStateRunning.Builder": ...

        @overload
        def started_at(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def started_at(self, value_or_callback=UNSET, /):
            "Time at which the container was last (re-)started"
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "started_at"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("started_at", value)

    class BuilderContext(BuilderContextBase["ContainerStateRunning.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerStateRunning.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerStateRunning."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerStateRunning", Builder]:
        return GenericListBuilder[ContainerStateRunning, ContainerStateRunning.Builder]()

    started_at: Annotated[Time | None, Field(alias="startedAt")] = None
    "Time at which the container was last (re-)started"


class ContainerStateTerminated(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerStateTerminated"]:
            return ContainerStateTerminated

        def build(self) -> "ContainerStateTerminated":
            return ContainerStateTerminated(**self._attrs)

        def container_id(self, value: str | None, /) -> Self:
            "Container\u0027s ID in the format \u0027\u003ctype\u003e://\u003ccontainer_id\u003e\u0027"
            return self._set("container_id", value)

        def exit_code(self, value: int, /) -> Self:
            "Exit status from the last termination of the container"
            return self._set("exit_code", value)

        @overload
        def finished_at(
            self, value_or_callback: Time | None, /
        ) -> "ContainerStateTerminated.Builder": ...

        @overload
        def finished_at(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ContainerStateTerminated.Builder": ...

        @overload
        def finished_at(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def finished_at(self, value_or_callback=UNSET, /):
            "Time at which the container last terminated"
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "finished_at"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("finished_at", value)

        def message(self, value: str | None, /) -> Self:
            "Message regarding the last termination of the container"
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "(brief) reason from the last termination of the container"
            return self._set("reason", value)

        def signal(self, value: int | None, /) -> Self:
            "Signal from the last termination of the container"
            return self._set("signal", value)

        @overload
        def started_at(
            self, value_or_callback: Time | None, /
        ) -> "ContainerStateTerminated.Builder": ...

        @overload
        def started_at(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ContainerStateTerminated.Builder": ...

        @overload
        def started_at(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def started_at(self, value_or_callback=UNSET, /):
            "Time at which previous execution of the container started"
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "started_at"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("started_at", value)

    class BuilderContext(BuilderContextBase["ContainerStateTerminated.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerStateTerminated.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerStateTerminated."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerStateTerminated", Builder]:
        return GenericListBuilder[ContainerStateTerminated, ContainerStateTerminated.Builder]()

    container_id: Annotated[str | None, Field(alias="containerID")] = None
    "Container\u0027s ID in the format \u0027\u003ctype\u003e://\u003ccontainer_id\u003e\u0027"
    exit_code: Annotated[int, Field(alias="exitCode")]
    "Exit status from the last termination of the container"
    finished_at: Annotated[Time | None, Field(alias="finishedAt")] = None
    "Time at which the container last terminated"
    message: str | None = None
    "Message regarding the last termination of the container"
    reason: str | None = None
    "(brief) reason from the last termination of the container"
    signal: int | None = None
    "Signal from the last termination of the container"
    started_at: Annotated[Time | None, Field(alias="startedAt")] = None
    "Time at which previous execution of the container started"


class ContainerStateWaiting(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerStateWaiting"]:
            return ContainerStateWaiting

        def build(self) -> "ContainerStateWaiting":
            return ContainerStateWaiting(**self._attrs)

        def message(self, value: str | None, /) -> Self:
            "Message regarding why the container is not yet running."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "(brief) reason the container is not yet running."
            return self._set("reason", value)

    class BuilderContext(BuilderContextBase["ContainerStateWaiting.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerStateWaiting.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerStateWaiting."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerStateWaiting", Builder]:
        return GenericListBuilder[ContainerStateWaiting, ContainerStateWaiting.Builder]()

    message: str | None = None
    "Message regarding why the container is not yet running."
    reason: str | None = None
    "(brief) reason the container is not yet running."


class DaemonEndpoint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DaemonEndpoint"]:
            return DaemonEndpoint

        def build(self) -> "DaemonEndpoint":
            return DaemonEndpoint(**self._attrs)

        def port(self, value: int, /) -> Self:
            "Port number of the given endpoint."
            return self._set("port", value)

    class BuilderContext(BuilderContextBase["DaemonEndpoint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DaemonEndpoint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DaemonEndpoint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DaemonEndpoint", Builder]:
        return GenericListBuilder[DaemonEndpoint, DaemonEndpoint.Builder]()

    port: Annotated[int, Field(alias="Port")]
    "Port number of the given endpoint."


class EmptyDirVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EmptyDirVolumeSource"]:
            return EmptyDirVolumeSource

        def build(self) -> "EmptyDirVolumeSource":
            return EmptyDirVolumeSource(**self._attrs)

        def medium(self, value: str | None, /) -> Self:
            'medium represents what type of storage medium should back this directory. The default is "" which means to use the node\u0027s default medium. Must be an empty string (default) or Memory. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir'
            return self._set("medium", value)

        @overload
        def size_limit(
            self, value_or_callback: Quantity | None, /
        ) -> "EmptyDirVolumeSource.Builder": ...

        @overload
        def size_limit(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "EmptyDirVolumeSource.Builder": ...

        @overload
        def size_limit(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def size_limit(self, value_or_callback=UNSET, /):
            "sizeLimit is the total amount of local storage required for this EmptyDir volume. The size limit is also applicable for memory medium. The maximum usage on memory medium EmptyDir would be the minimum value between the SizeLimit specified here and the sum of memory limits of all containers in a pod. The default is nil which means that the limit is undefined. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir"
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "size_limit"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("size_limit", value)

    class BuilderContext(BuilderContextBase["EmptyDirVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EmptyDirVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EmptyDirVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EmptyDirVolumeSource", Builder]:
        return GenericListBuilder[EmptyDirVolumeSource, EmptyDirVolumeSource.Builder]()

    medium: str | None = None
    'medium represents what type of storage medium should back this directory. The default is "" which means to use the node\u0027s default medium. Must be an empty string (default) or Memory. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir'
    size_limit: Annotated[Quantity | None, Field(alias="sizeLimit")] = None
    "sizeLimit is the total amount of local storage required for this EmptyDir volume. The size limit is also applicable for memory medium. The maximum usage on memory medium EmptyDir would be the minimum value between the SizeLimit specified here and the sum of memory limits of all containers in a pod. The default is nil which means that the limit is undefined. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir"


class EndpointPort(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointPort"]:
            return EndpointPort

        def build(self) -> "EndpointPort":
            return EndpointPort(**self._attrs)

        def app_protocol(self, value: str | None, /) -> Self:
            "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
            return self._set("app_protocol", value)

        def name(self, value: str | None, /) -> Self:
            "The name of this port.  This must match the \u0027name\u0027 field in the corresponding ServicePort. Must be a DNS_LABEL. Optional only if one port is defined."
            return self._set("name", value)

        def port(self, value: int, /) -> Self:
            "The port number of the endpoint."
            return self._set("port", value)

        def protocol(self, value: str | None, /) -> Self:
            "The IP protocol for this port. Must be UDP, TCP, or SCTP. Default is TCP."
            return self._set("protocol", value)

    class BuilderContext(BuilderContextBase["EndpointPort.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointPort.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointPort."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointPort", Builder]:
        return GenericListBuilder[EndpointPort, EndpointPort.Builder]()

    app_protocol: Annotated[str | None, Field(alias="appProtocol")] = None
    "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
    name: str | None = None
    "The name of this port.  This must match the \u0027name\u0027 field in the corresponding ServicePort. Must be a DNS_LABEL. Optional only if one port is defined."
    port: int
    "The port number of the endpoint."
    protocol: str | None = None
    "The IP protocol for this port. Must be UDP, TCP, or SCTP. Default is TCP."


class EventSeries(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EventSeries"]:
            return EventSeries

        def build(self) -> "EventSeries":
            return EventSeries(**self._attrs)

        def count(self, value: int | None, /) -> Self:
            "Number of occurrences in this series up to the last heartbeat time"
            return self._set("count", value)

        @overload
        def last_observed_time(
            self, value_or_callback: MicroTime | None, /
        ) -> "EventSeries.Builder": ...

        @overload
        def last_observed_time(
            self,
            value_or_callback: Callable[[MicroTime.Builder], MicroTime.Builder | MicroTime],
            /,
        ) -> "EventSeries.Builder": ...

        @overload
        def last_observed_time(
            self, value_or_callback: Never = ...
        ) -> "MicroTime.BuilderContext": ...

        def last_observed_time(self, value_or_callback=UNSET, /):
            "Time of the last occurrence observed"
            if self._in_context and value_or_callback is UNSET:
                context = MicroTime.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_observed_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(MicroTime.builder())
                if isinstance(output, MicroTime.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_observed_time", value)

    class BuilderContext(BuilderContextBase["EventSeries.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EventSeries.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EventSeries."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EventSeries", Builder]:
        return GenericListBuilder[EventSeries, EventSeries.Builder]()

    count: int | None = None
    "Number of occurrences in this series up to the last heartbeat time"
    last_observed_time: Annotated[MicroTime | None, Field(alias="lastObservedTime")] = None
    "Time of the last occurrence observed"


class EventSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EventSource"]:
            return EventSource

        def build(self) -> "EventSource":
            return EventSource(**self._attrs)

        def component(self, value: str | None, /) -> Self:
            "Component from which the event is generated."
            return self._set("component", value)

        def host(self, value: str | None, /) -> Self:
            "Node name on which the event is generated."
            return self._set("host", value)

    class BuilderContext(BuilderContextBase["EventSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EventSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EventSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EventSource", Builder]:
        return GenericListBuilder[EventSource, EventSource.Builder]()

    component: str | None = None
    "Component from which the event is generated."
    host: str | None = None
    "Node name on which the event is generated."


class ExecAction(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ExecAction"]:
            return ExecAction

        def build(self) -> "ExecAction":
            return ExecAction(**self._attrs)

        def command(self, value: list[str] | None, /) -> Self:
            "Command is the command line to execute inside the container, the working directory for the command  is root (\u0027/\u0027) in the container\u0027s filesystem. The command is simply exec\u0027d, it is not run inside a shell, so traditional shell instructions (\u0027|\u0027, etc) won\u0027t work. To use a shell, you need to explicitly call out to that shell. Exit status of 0 is treated as live/healthy and non-zero is unhealthy."
            return self._set("command", value)

    class BuilderContext(BuilderContextBase["ExecAction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ExecAction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ExecAction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ExecAction", Builder]:
        return GenericListBuilder[ExecAction, ExecAction.Builder]()

    command: list[str] | None = None
    "Command is the command line to execute inside the container, the working directory for the command  is root (\u0027/\u0027) in the container\u0027s filesystem. The command is simply exec\u0027d, it is not run inside a shell, so traditional shell instructions (\u0027|\u0027, etc) won\u0027t work. To use a shell, you need to explicitly call out to that shell. Exit status of 0 is treated as live/healthy and non-zero is unhealthy."


class FCVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["FCVolumeSource"]:
            return FCVolumeSource

        def build(self) -> "FCVolumeSource":
            return FCVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def lun(self, value: int | None, /) -> Self:
            "lun is Optional: FC target lun number"
            return self._set("lun", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        def target_ww_ns(self, value: list[str] | None, /) -> Self:
            "targetWWNs is Optional: FC target worldwide names (WWNs)"
            return self._set("target_ww_ns", value)

        def wwids(self, value: list[str] | None, /) -> Self:
            "wwids Optional: FC volume world wide identifiers (wwids) Either wwids or combination of targetWWNs and lun must be set, but not both simultaneously."
            return self._set("wwids", value)

    class BuilderContext(BuilderContextBase["FCVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = FCVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for FCVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["FCVolumeSource", Builder]:
        return GenericListBuilder[FCVolumeSource, FCVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    lun: int | None = None
    "lun is Optional: FC target lun number"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    target_ww_ns: Annotated[list[str] | None, Field(alias="targetWWNs")] = None
    "targetWWNs is Optional: FC target worldwide names (WWNs)"
    wwids: list[str] | None = None
    "wwids Optional: FC volume world wide identifiers (wwids) Either wwids or combination of targetWWNs and lun must be set, but not both simultaneously."


class FileKeySelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["FileKeySelector"]:
            return FileKeySelector

        def build(self) -> "FileKeySelector":
            return FileKeySelector(**self._attrs)

        def key(self, value: str, /) -> Self:
            "The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters."
            return self._set("key", value)

        def optional(self, value: bool | None, /) -> Self:
            "Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod\u0027s containers.\n\nIf optional is set to false and the specified key does not exist, an error will be returned during Pod creation."
            return self._set("optional", value)

        def path(self, value: str, /) -> Self:
            "The path within the volume from which to select the file. Must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
            return self._set("path", value)

        def volume_name(self, value: str, /) -> Self:
            "The name of the volume mount containing the env file."
            return self._set("volume_name", value)

    class BuilderContext(BuilderContextBase["FileKeySelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = FileKeySelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for FileKeySelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["FileKeySelector", Builder]:
        return GenericListBuilder[FileKeySelector, FileKeySelector.Builder]()

    key: str
    "The key within the env file. An invalid key will prevent the pod from starting. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters."
    optional: bool | None = None
    "Specify whether the file or its key must be defined. If the file or key does not exist, then the env var is not published. If optional is set to true and the specified key does not exist, the environment variable will not be set in the Pod\u0027s containers.\n\nIf optional is set to false and the specified key does not exist, an error will be returned during Pod creation."
    path: str
    "The path within the volume from which to select the file. Must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
    volume_name: Annotated[str, Field(alias="volumeName")]
    "The name of the volume mount containing the env file."


class FlockerVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["FlockerVolumeSource"]:
            return FlockerVolumeSource

        def build(self) -> "FlockerVolumeSource":
            return FlockerVolumeSource(**self._attrs)

        def dataset_name(self, value: str | None, /) -> Self:
            "datasetName is Name of the dataset stored as metadata -\u003e name on the dataset for Flocker should be considered as deprecated"
            return self._set("dataset_name", value)

        def dataset_uuid(self, value: str | None, /) -> Self:
            "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset"
            return self._set("dataset_uuid", value)

    class BuilderContext(BuilderContextBase["FlockerVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = FlockerVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for FlockerVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["FlockerVolumeSource", Builder]:
        return GenericListBuilder[FlockerVolumeSource, FlockerVolumeSource.Builder]()

    dataset_name: Annotated[str | None, Field(alias="datasetName")] = None
    "datasetName is Name of the dataset stored as metadata -\u003e name on the dataset for Flocker should be considered as deprecated"
    dataset_uuid: Annotated[str | None, Field(alias="datasetUUID")] = None
    "datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset"


class GCEPersistentDiskVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GCEPersistentDiskVolumeSource"]:
            return GCEPersistentDiskVolumeSource

        def build(self) -> "GCEPersistentDiskVolumeSource":
            return GCEPersistentDiskVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk'
            return self._set("fs_type", value)

        def partition(self, value: int | None, /) -> Self:
            'partition is the partition in the volume that you want to mount. If omitted, the default is to mount by volume name. Examples: For volume /dev/sda1, you specify the partition as "1". Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty). More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk'
            return self._set("partition", value)

        def pd_name(self, value: str, /) -> Self:
            "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
            return self._set("pd_name", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
            return self._set("read_only", value)

    class BuilderContext(BuilderContextBase["GCEPersistentDiskVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GCEPersistentDiskVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GCEPersistentDiskVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["GCEPersistentDiskVolumeSource", Builder]:
        return GenericListBuilder[
            GCEPersistentDiskVolumeSource, GCEPersistentDiskVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk'
    partition: int | None = None
    'partition is the partition in the volume that you want to mount. If omitted, the default is to mount by volume name. Examples: For volume /dev/sda1, you specify the partition as "1". Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty). More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk'
    pd_name: Annotated[str, Field(alias="pdName")]
    "pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"


class GRPCAction(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GRPCAction"]:
            return GRPCAction

        def build(self) -> "GRPCAction":
            return GRPCAction(**self._attrs)

        def port(self, value: int, /) -> Self:
            "Port number of the gRPC service. Number must be in the range 1 to 65535."
            return self._set("port", value)

        def service(self, value: str | None, /) -> Self:
            "Service is the name of the service to place in the gRPC HealthCheckRequest (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC."
            return self._set("service", value)

    class BuilderContext(BuilderContextBase["GRPCAction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GRPCAction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GRPCAction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["GRPCAction", Builder]:
        return GenericListBuilder[GRPCAction, GRPCAction.Builder]()

    port: int
    "Port number of the gRPC service. Number must be in the range 1 to 65535."
    service: str | None = None
    "Service is the name of the service to place in the gRPC HealthCheckRequest (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).\n\nIf this is not specified, the default behavior is defined by gRPC."


class GitRepoVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GitRepoVolumeSource"]:
            return GitRepoVolumeSource

        def build(self) -> "GitRepoVolumeSource":
            return GitRepoVolumeSource(**self._attrs)

        def directory(self, value: str | None, /) -> Self:
            "directory is the target directory name. Must not contain or start with \u0027..\u0027.  If \u0027.\u0027 is supplied, the volume directory will be the git repository.  Otherwise, if specified, the volume will contain the git repository in the subdirectory with the given name."
            return self._set("directory", value)

        def repository(self, value: str, /) -> Self:
            "repository is the URL"
            return self._set("repository", value)

        def revision(self, value: str | None, /) -> Self:
            "revision is the commit hash for the specified revision."
            return self._set("revision", value)

    class BuilderContext(BuilderContextBase["GitRepoVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GitRepoVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GitRepoVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["GitRepoVolumeSource", Builder]:
        return GenericListBuilder[GitRepoVolumeSource, GitRepoVolumeSource.Builder]()

    directory: str | None = None
    "directory is the target directory name. Must not contain or start with \u0027..\u0027.  If \u0027.\u0027 is supplied, the volume directory will be the git repository.  Otherwise, if specified, the volume will contain the git repository in the subdirectory with the given name."
    repository: str
    "repository is the URL"
    revision: str | None = None
    "revision is the commit hash for the specified revision."


class GlusterfsPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GlusterfsPersistentVolumeSource"]:
            return GlusterfsPersistentVolumeSource

        def build(self) -> "GlusterfsPersistentVolumeSource":
            return GlusterfsPersistentVolumeSource(**self._attrs)

        def endpoints(self, value: str, /) -> Self:
            "endpoints is the endpoint name that details Glusterfs topology. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("endpoints", value)

        def endpoints_namespace(self, value: str | None, /) -> Self:
            "endpointsNamespace is the namespace that contains Glusterfs endpoint. If this field is empty, the EndpointNamespace defaults to the same namespace as the bound PVC. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("endpoints_namespace", value)

        def path(self, value: str, /) -> Self:
            "path is the Glusterfs volume path. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("path", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the Glusterfs volume to be mounted with read-only permissions. Defaults to false. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("read_only", value)

    class BuilderContext(BuilderContextBase["GlusterfsPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GlusterfsPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GlusterfsPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["GlusterfsPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            GlusterfsPersistentVolumeSource, GlusterfsPersistentVolumeSource.Builder
        ]()

    endpoints: str
    "endpoints is the endpoint name that details Glusterfs topology. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
    endpoints_namespace: Annotated[str | None, Field(alias="endpointsNamespace")] = None
    "endpointsNamespace is the namespace that contains Glusterfs endpoint. If this field is empty, the EndpointNamespace defaults to the same namespace as the bound PVC. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
    path: str
    "path is the Glusterfs volume path. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the Glusterfs volume to be mounted with read-only permissions. Defaults to false. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"


class GlusterfsVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["GlusterfsVolumeSource"]:
            return GlusterfsVolumeSource

        def build(self) -> "GlusterfsVolumeSource":
            return GlusterfsVolumeSource(**self._attrs)

        def endpoints(self, value: str, /) -> Self:
            "endpoints is the endpoint name that details Glusterfs topology."
            return self._set("endpoints", value)

        def path(self, value: str, /) -> Self:
            "path is the Glusterfs volume path. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("path", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the Glusterfs volume to be mounted with read-only permissions. Defaults to false. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
            return self._set("read_only", value)

    class BuilderContext(BuilderContextBase["GlusterfsVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = GlusterfsVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for GlusterfsVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["GlusterfsVolumeSource", Builder]:
        return GenericListBuilder[GlusterfsVolumeSource, GlusterfsVolumeSource.Builder]()

    endpoints: str
    "endpoints is the endpoint name that details Glusterfs topology."
    path: str
    "path is the Glusterfs volume path. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the Glusterfs volume to be mounted with read-only permissions. Defaults to false. More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod"


class HTTPHeader(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["HTTPHeader"]:
            return HTTPHeader

        def build(self) -> "HTTPHeader":
            return HTTPHeader(**self._attrs)

        def name(self, value: str, /) -> Self:
            "The header field name. This will be canonicalized upon output, so case-variant names will be understood as the same header."
            return self._set("name", value)

        def value(self, value: str, /) -> Self:
            "The header field value"
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["HTTPHeader.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = HTTPHeader.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for HTTPHeader."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["HTTPHeader", Builder]:
        return GenericListBuilder[HTTPHeader, HTTPHeader.Builder]()

    name: str
    "The header field name. This will be canonicalized upon output, so case-variant names will be understood as the same header."
    value: str
    "The header field value"


class HostAlias(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["HostAlias"]:
            return HostAlias

        def build(self) -> "HostAlias":
            return HostAlias(**self._attrs)

        def hostnames(self, value: list[str] | None, /) -> Self:
            "Hostnames for the above IP address."
            return self._set("hostnames", value)

        def ip(self, value: str, /) -> Self:
            "IP address of the host file entry."
            return self._set("ip", value)

    class BuilderContext(BuilderContextBase["HostAlias.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = HostAlias.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for HostAlias."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["HostAlias", Builder]:
        return GenericListBuilder[HostAlias, HostAlias.Builder]()

    hostnames: list[str] | None = None
    "Hostnames for the above IP address."
    ip: str
    "IP address of the host file entry."


class HostIP(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["HostIP"]:
            return HostIP

        def build(self) -> "HostIP":
            return HostIP(**self._attrs)

        def ip(self, value: str, /) -> Self:
            "IP is the IP address assigned to the host"
            return self._set("ip", value)

    class BuilderContext(BuilderContextBase["HostIP.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = HostIP.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for HostIP."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["HostIP", Builder]:
        return GenericListBuilder[HostIP, HostIP.Builder]()

    ip: str
    "IP is the IP address assigned to the host"


class HostPathVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["HostPathVolumeSource"]:
            return HostPathVolumeSource

        def build(self) -> "HostPathVolumeSource":
            return HostPathVolumeSource(**self._attrs)

        def path(self, value: str, /) -> Self:
            "path of the directory on the host. If the path is a symlink, it will follow the link to the real path. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
            return self._set("path", value)

        def type(self, value: str | None, /) -> Self:
            'type for HostPath Volume Defaults to "" More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath'
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["HostPathVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = HostPathVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for HostPathVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["HostPathVolumeSource", Builder]:
        return GenericListBuilder[HostPathVolumeSource, HostPathVolumeSource.Builder]()

    path: str
    "path of the directory on the host. If the path is a symlink, it will follow the link to the real path. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
    type: str | None = None
    'type for HostPath Volume Defaults to "" More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath'


class ImageVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ImageVolumeSource"]:
            return ImageVolumeSource

        def build(self) -> "ImageVolumeSource":
            return ImageVolumeSource(**self._attrs)

        def pull_policy(self, value: str | None, /) -> Self:
            "Policy for pulling OCI objects. Possible values are: Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails. Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn\u0027t present. IfNotPresent: the kubelet pulls if the reference isn\u0027t already present on disk. Container creation will fail if the reference isn\u0027t present and the pull fails. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise."
            return self._set("pull_policy", value)

        def reference(self, value: str | None, /) -> Self:
            "Required: Image or artifact reference to be used. Behaves in the same way as pod.spec.containers[*].image. Pull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets. More info: https://kubernetes.io/docs/concepts/containers/images This field is optional to allow higher level config management to default or override container images in workload controllers like Deployments and StatefulSets."
            return self._set("reference", value)

    class BuilderContext(BuilderContextBase["ImageVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ImageVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ImageVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ImageVolumeSource", Builder]:
        return GenericListBuilder[ImageVolumeSource, ImageVolumeSource.Builder]()

    pull_policy: Annotated[str | None, Field(alias="pullPolicy")] = None
    "Policy for pulling OCI objects. Possible values are: Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails. Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn\u0027t present. IfNotPresent: the kubelet pulls if the reference isn\u0027t already present on disk. Container creation will fail if the reference isn\u0027t present and the pull fails. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise."
    reference: str | None = None
    "Required: Image or artifact reference to be used. Behaves in the same way as pod.spec.containers[*].image. Pull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets. More info: https://kubernetes.io/docs/concepts/containers/images This field is optional to allow higher level config management to default or override container images in workload controllers like Deployments and StatefulSets."


class KeyToPath(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["KeyToPath"]:
            return KeyToPath

        def build(self) -> "KeyToPath":
            return KeyToPath(**self._attrs)

        def key(self, value: str, /) -> Self:
            "key is the key to project."
            return self._set("key", value)

        def mode(self, value: int | None, /) -> Self:
            "mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("mode", value)

        def path(self, value: str, /) -> Self:
            "path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element \u0027..\u0027. May not start with the string \u0027..\u0027."
            return self._set("path", value)

    class BuilderContext(BuilderContextBase["KeyToPath.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = KeyToPath.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for KeyToPath."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["KeyToPath", Builder]:
        return GenericListBuilder[KeyToPath, KeyToPath.Builder]()

    key: str
    "key is the key to project."
    mode: int | None = None
    "mode is Optional: mode bits used to set permissions on this file. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    path: str
    "path is the relative path of the file to map the key to. May not be an absolute path. May not contain the path element \u0027..\u0027. May not start with the string \u0027..\u0027."


class LimitRangeItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LimitRangeItem"]:
            return LimitRangeItem

        def build(self) -> "LimitRangeItem":
            return LimitRangeItem(**self._attrs)

        def default(self, value: dict[str, Quantity] | None, /) -> Self:
            "Default resource requirement limit value by resource name if resource limit is omitted."
            return self._set("default", value)

        def default_request(self, value: dict[str, Quantity] | None, /) -> Self:
            "DefaultRequest is the default resource requirement request value by resource name if resource request is omitted."
            return self._set("default_request", value)

        def max(self, value: dict[str, Quantity] | None, /) -> Self:
            "Max usage constraints on this kind by resource name."
            return self._set("max", value)

        def max_limit_request_ratio(self, value: dict[str, Quantity] | None, /) -> Self:
            "MaxLimitRequestRatio if specified, the named resource must have a request and limit that are both non-zero where limit divided by request is less than or equal to the enumerated value; this represents the max burst for the named resource."
            return self._set("max_limit_request_ratio", value)

        def min(self, value: dict[str, Quantity] | None, /) -> Self:
            "Min usage constraints on this kind by resource name."
            return self._set("min", value)

        def type(self, value: str, /) -> Self:
            "Type of resource that this limit applies to."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["LimitRangeItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LimitRangeItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LimitRangeItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LimitRangeItem", Builder]:
        return GenericListBuilder[LimitRangeItem, LimitRangeItem.Builder]()

    default: dict[str, Quantity] | None = None
    "Default resource requirement limit value by resource name if resource limit is omitted."
    default_request: Annotated[dict[str, Quantity] | None, Field(alias="defaultRequest")] = None
    "DefaultRequest is the default resource requirement request value by resource name if resource request is omitted."
    max: dict[str, Quantity] | None = None
    "Max usage constraints on this kind by resource name."
    max_limit_request_ratio: Annotated[
        dict[str, Quantity] | None, Field(alias="maxLimitRequestRatio")
    ] = None
    "MaxLimitRequestRatio if specified, the named resource must have a request and limit that are both non-zero where limit divided by request is less than or equal to the enumerated value; this represents the max burst for the named resource."
    min: dict[str, Quantity] | None = None
    "Min usage constraints on this kind by resource name."
    type: str
    "Type of resource that this limit applies to."


class LimitRangeSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LimitRangeSpec"]:
            return LimitRangeSpec

        def build(self) -> "LimitRangeSpec":
            return LimitRangeSpec(**self._attrs)

        @overload
        def limits(
            self, value_or_callback: list[LimitRangeItem], /
        ) -> "LimitRangeSpec.Builder": ...

        @overload
        def limits(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[LimitRangeItem, LimitRangeItem.Builder]],
                GenericListBuilder[LimitRangeItem, LimitRangeItem.Builder] | list[LimitRangeItem],
            ],
            /,
        ) -> "LimitRangeSpec.Builder": ...

        @overload
        def limits(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[LimitRangeItem.Builder]: ...

        def limits(self, value_or_callback=UNSET, /):
            "Limits is the list of LimitRangeItem objects that are enforced."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[LimitRangeItem.Builder]()
                context._parent_builder = self
                context._field_name = "limits"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LimitRangeItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("limits", value)

    class BuilderContext(BuilderContextBase["LimitRangeSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LimitRangeSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LimitRangeSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LimitRangeSpec", Builder]:
        return GenericListBuilder[LimitRangeSpec, LimitRangeSpec.Builder]()

    limits: list[LimitRangeItem]
    "Limits is the list of LimitRangeItem objects that are enforced."


class LinuxContainerUser(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LinuxContainerUser"]:
            return LinuxContainerUser

        def build(self) -> "LinuxContainerUser":
            return LinuxContainerUser(**self._attrs)

        def gid(self, value: int, /) -> Self:
            "GID is the primary gid initially attached to the first process in the container"
            return self._set("gid", value)

        def supplemental_groups(self, value: list[int] | None, /) -> Self:
            "SupplementalGroups are the supplemental groups initially attached to the first process in the container"
            return self._set("supplemental_groups", value)

        def uid(self, value: int, /) -> Self:
            "UID is the primary uid initially attached to the first process in the container"
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["LinuxContainerUser.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LinuxContainerUser.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LinuxContainerUser."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LinuxContainerUser", Builder]:
        return GenericListBuilder[LinuxContainerUser, LinuxContainerUser.Builder]()

    gid: int
    "GID is the primary gid initially attached to the first process in the container"
    supplemental_groups: Annotated[list[int] | None, Field(alias="supplementalGroups")] = None
    "SupplementalGroups are the supplemental groups initially attached to the first process in the container"
    uid: int
    "UID is the primary uid initially attached to the first process in the container"


class LocalObjectReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LocalObjectReference"]:
            return LocalObjectReference

        def build(self) -> "LocalObjectReference":
            return LocalObjectReference(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["LocalObjectReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LocalObjectReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LocalObjectReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LocalObjectReference", Builder]:
        return GenericListBuilder[LocalObjectReference, LocalObjectReference.Builder]()

    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"


class LocalVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LocalVolumeSource"]:
            return LocalVolumeSource

        def build(self) -> "LocalVolumeSource":
            return LocalVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. It applies only when the Path is a block device. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default value is to auto-select a filesystem if unspecified.'
            return self._set("fs_type", value)

        def path(self, value: str, /) -> Self:
            "path of the full path to the volume on the node. It can be either a directory or block device (disk, partition, ...)."
            return self._set("path", value)

    class BuilderContext(BuilderContextBase["LocalVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LocalVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LocalVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LocalVolumeSource", Builder]:
        return GenericListBuilder[LocalVolumeSource, LocalVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. It applies only when the Path is a block device. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default value is to auto-select a filesystem if unspecified.'
    path: str
    "path of the full path to the volume on the node. It can be either a directory or block device (disk, partition, ...)."


class ModifyVolumeStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ModifyVolumeStatus"]:
            return ModifyVolumeStatus

        def build(self) -> "ModifyVolumeStatus":
            return ModifyVolumeStatus(**self._attrs)

        def status(self, value: str, /) -> Self:
            "status is the status of the ControllerModifyVolume operation. It can be in any of following states:\n - Pending\n   Pending indicates that the PersistentVolumeClaim cannot be modified due to unmet requirements, such as\n   the specified VolumeAttributesClass not existing.\n - InProgress\n   InProgress indicates that the volume is being modified.\n - Infeasible\n  Infeasible indicates that the request has been rejected as invalid by the CSI driver. To\n\t  resolve the error, a valid VolumeAttributesClass needs to be specified.\nNote: New statuses can be added in the future. Consumers should check for unknown statuses and fail appropriately."
            return self._set("status", value)

        def target_volume_attributes_class_name(self, value: str | None, /) -> Self:
            "targetVolumeAttributesClassName is the name of the VolumeAttributesClass the PVC currently being reconciled"
            return self._set("target_volume_attributes_class_name", value)

    class BuilderContext(BuilderContextBase["ModifyVolumeStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ModifyVolumeStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ModifyVolumeStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ModifyVolumeStatus", Builder]:
        return GenericListBuilder[ModifyVolumeStatus, ModifyVolumeStatus.Builder]()

    status: str
    "status is the status of the ControllerModifyVolume operation. It can be in any of following states:\n - Pending\n   Pending indicates that the PersistentVolumeClaim cannot be modified due to unmet requirements, such as\n   the specified VolumeAttributesClass not existing.\n - InProgress\n   InProgress indicates that the volume is being modified.\n - Infeasible\n  Infeasible indicates that the request has been rejected as invalid by the CSI driver. To\n\t  resolve the error, a valid VolumeAttributesClass needs to be specified.\nNote: New statuses can be added in the future. Consumers should check for unknown statuses and fail appropriately."
    target_volume_attributes_class_name: Annotated[
        str | None, Field(alias="targetVolumeAttributesClassName")
    ] = None
    "targetVolumeAttributesClassName is the name of the VolumeAttributesClass the PVC currently being reconciled"


class NFSVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NFSVolumeSource"]:
            return NFSVolumeSource

        def build(self) -> "NFSVolumeSource":
            return NFSVolumeSource(**self._attrs)

        def path(self, value: str, /) -> Self:
            "path that is exported by the NFS server. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
            return self._set("path", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the NFS export to be mounted with read-only permissions. Defaults to false. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
            return self._set("read_only", value)

        def server(self, value: str, /) -> Self:
            "server is the hostname or IP address of the NFS server. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
            return self._set("server", value)

    class BuilderContext(BuilderContextBase["NFSVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NFSVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NFSVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NFSVolumeSource", Builder]:
        return GenericListBuilder[NFSVolumeSource, NFSVolumeSource.Builder]()

    path: str
    "path that is exported by the NFS server. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the NFS export to be mounted with read-only permissions. Defaults to false. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
    server: str
    "server is the hostname or IP address of the NFS server. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"


class NamespaceCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NamespaceCondition"]:
            return NamespaceCondition

        def build(self) -> "NamespaceCondition":
            return NamespaceCondition(**self._attrs)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "NamespaceCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "NamespaceCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "Last time the condition transitioned from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "Human-readable message indicating details about last transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "Unique, one-word, CamelCase reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of namespace controller condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["NamespaceCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NamespaceCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NamespaceCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NamespaceCondition", Builder]:
        return GenericListBuilder[NamespaceCondition, NamespaceCondition.Builder]()

    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "Last time the condition transitioned from one status to another."
    message: str | None = None
    "Human-readable message indicating details about last transition."
    reason: str | None = None
    "Unique, one-word, CamelCase reason for the condition\u0027s last transition."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of namespace controller condition."


class NamespaceSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NamespaceSpec"]:
            return NamespaceSpec

        def build(self) -> "NamespaceSpec":
            return NamespaceSpec(**self._attrs)

        def finalizers(self, value: list[str] | None, /) -> Self:
            "Finalizers is an opaque list of values that must be empty to permanently remove object from storage. More info: https://kubernetes.io/docs/tasks/administer-cluster/namespaces/"
            return self._set("finalizers", value)

    class BuilderContext(BuilderContextBase["NamespaceSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NamespaceSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NamespaceSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NamespaceSpec", Builder]:
        return GenericListBuilder[NamespaceSpec, NamespaceSpec.Builder]()

    finalizers: list[str] | None = None
    "Finalizers is an opaque list of values that must be empty to permanently remove object from storage. More info: https://kubernetes.io/docs/tasks/administer-cluster/namespaces/"


class NamespaceStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NamespaceStatus"]:
            return NamespaceStatus

        def build(self) -> "NamespaceStatus":
            return NamespaceStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[NamespaceCondition] | None, /
        ) -> "NamespaceStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NamespaceCondition, NamespaceCondition.Builder]],
                GenericListBuilder[NamespaceCondition, NamespaceCondition.Builder]
                | list[NamespaceCondition],
            ],
            /,
        ) -> "NamespaceStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NamespaceCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Represents the latest available observations of a namespace\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NamespaceCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NamespaceCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def phase(self, value: str | None, /) -> Self:
            "Phase is the current lifecycle phase of the namespace. More info: https://kubernetes.io/docs/tasks/administer-cluster/namespaces/"
            return self._set("phase", value)

    class BuilderContext(BuilderContextBase["NamespaceStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NamespaceStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NamespaceStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NamespaceStatus", Builder]:
        return GenericListBuilder[NamespaceStatus, NamespaceStatus.Builder]()

    conditions: list[NamespaceCondition] | None = None
    "Represents the latest available observations of a namespace\u0027s current state."
    phase: str | None = None
    "Phase is the current lifecycle phase of the namespace. More info: https://kubernetes.io/docs/tasks/administer-cluster/namespaces/"


class NodeAddress(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeAddress"]:
            return NodeAddress

        def build(self) -> "NodeAddress":
            return NodeAddress(**self._attrs)

        def address(self, value: str, /) -> Self:
            "The node address."
            return self._set("address", value)

        def type(self, value: str, /) -> Self:
            "Node address type, one of Hostname, ExternalIP or InternalIP."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["NodeAddress.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeAddress.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeAddress."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeAddress", Builder]:
        return GenericListBuilder[NodeAddress, NodeAddress.Builder]()

    address: str
    "The node address."
    type: str
    "Node address type, one of Hostname, ExternalIP or InternalIP."


class NodeCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeCondition"]:
            return NodeCondition

        def build(self) -> "NodeCondition":
            return NodeCondition(**self._attrs)

        @overload
        def last_heartbeat_time(
            self, value_or_callback: Time | None, /
        ) -> "NodeCondition.Builder": ...

        @overload
        def last_heartbeat_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "NodeCondition.Builder": ...

        @overload
        def last_heartbeat_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_heartbeat_time(self, value_or_callback=UNSET, /):
            "Last time we got an update on a given condition."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_heartbeat_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_heartbeat_time", value)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "NodeCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "NodeCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "Last time the condition transit from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "Human readable message indicating details about last transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "(brief) reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of node condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["NodeCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeCondition", Builder]:
        return GenericListBuilder[NodeCondition, NodeCondition.Builder]()

    last_heartbeat_time: Annotated[Time | None, Field(alias="lastHeartbeatTime")] = None
    "Last time we got an update on a given condition."
    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "Last time the condition transit from one status to another."
    message: str | None = None
    "Human readable message indicating details about last transition."
    reason: str | None = None
    "(brief) reason for the condition\u0027s last transition."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of node condition."


class NodeConfigSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeConfigSource"]:
            return NodeConfigSource

        def build(self) -> "NodeConfigSource":
            return NodeConfigSource(**self._attrs)

        @overload
        def config_map(
            self, value_or_callback: ConfigMapNodeConfigSource | None, /
        ) -> "NodeConfigSource.Builder": ...

        @overload
        def config_map(
            self,
            value_or_callback: Callable[
                [ConfigMapNodeConfigSource.Builder],
                ConfigMapNodeConfigSource.Builder | ConfigMapNodeConfigSource,
            ],
            /,
        ) -> "NodeConfigSource.Builder": ...

        @overload
        def config_map(
            self, value_or_callback: Never = ...
        ) -> "ConfigMapNodeConfigSource.BuilderContext": ...

        def config_map(self, value_or_callback=UNSET, /):
            "ConfigMap is a reference to a Node\u0027s ConfigMap"
            if self._in_context and value_or_callback is UNSET:
                context = ConfigMapNodeConfigSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_map"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ConfigMapNodeConfigSource.builder())
                if isinstance(output, ConfigMapNodeConfigSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_map", value)

    class BuilderContext(BuilderContextBase["NodeConfigSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeConfigSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeConfigSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeConfigSource", Builder]:
        return GenericListBuilder[NodeConfigSource, NodeConfigSource.Builder]()

    config_map: Annotated[ConfigMapNodeConfigSource | None, Field(alias="configMap")] = None
    "ConfigMap is a reference to a Node\u0027s ConfigMap"


class NodeConfigStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeConfigStatus"]:
            return NodeConfigStatus

        def build(self) -> "NodeConfigStatus":
            return NodeConfigStatus(**self._attrs)

        @overload
        def active(
            self, value_or_callback: NodeConfigSource | None, /
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def active(
            self,
            value_or_callback: Callable[
                [NodeConfigSource.Builder], NodeConfigSource.Builder | NodeConfigSource
            ],
            /,
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def active(self, value_or_callback: Never = ...) -> "NodeConfigSource.BuilderContext": ...

        def active(self, value_or_callback=UNSET, /):
            "Active reports the checkpointed config the node is actively using. Active will represent either the current version of the Assigned config, or the current LastKnownGood config, depending on whether attempting to use the Assigned config results in an error."
            if self._in_context and value_or_callback is UNSET:
                context = NodeConfigSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "active"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeConfigSource.builder())
                if isinstance(output, NodeConfigSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("active", value)

        @overload
        def assigned(
            self, value_or_callback: NodeConfigSource | None, /
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def assigned(
            self,
            value_or_callback: Callable[
                [NodeConfigSource.Builder], NodeConfigSource.Builder | NodeConfigSource
            ],
            /,
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def assigned(self, value_or_callback: Never = ...) -> "NodeConfigSource.BuilderContext": ...

        def assigned(self, value_or_callback=UNSET, /):
            "Assigned reports the checkpointed config the node will try to use. When Node.Spec.ConfigSource is updated, the node checkpoints the associated config payload to local disk, along with a record indicating intended config. The node refers to this record to choose its config checkpoint, and reports this record in Assigned. Assigned only updates in the status after the record has been checkpointed to disk. When the Kubelet is restarted, it tries to make the Assigned config the Active config by loading and validating the checkpointed payload identified by Assigned."
            if self._in_context and value_or_callback is UNSET:
                context = NodeConfigSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "assigned"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeConfigSource.builder())
                if isinstance(output, NodeConfigSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("assigned", value)

        def error(self, value: str | None, /) -> Self:
            "Error describes any problems reconciling the Spec.ConfigSource to the Active config. Errors may occur, for example, attempting to checkpoint Spec.ConfigSource to the local Assigned record, attempting to checkpoint the payload associated with Spec.ConfigSource, attempting to load or validate the Assigned config, etc. Errors may occur at different points while syncing config. Earlier errors (e.g. download or checkpointing errors) will not result in a rollback to LastKnownGood, and may resolve across Kubelet retries. Later errors (e.g. loading or validating a checkpointed config) will result in a rollback to LastKnownGood. In the latter case, it is usually possible to resolve the error by fixing the config assigned in Spec.ConfigSource. You can find additional information for debugging by searching the error message in the Kubelet log. Error is a human-readable description of the error state; machines can check whether or not Error is empty, but should not rely on the stability of the Error text across Kubelet versions."
            return self._set("error", value)

        @overload
        def last_known_good(
            self, value_or_callback: NodeConfigSource | None, /
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def last_known_good(
            self,
            value_or_callback: Callable[
                [NodeConfigSource.Builder], NodeConfigSource.Builder | NodeConfigSource
            ],
            /,
        ) -> "NodeConfigStatus.Builder": ...

        @overload
        def last_known_good(
            self, value_or_callback: Never = ...
        ) -> "NodeConfigSource.BuilderContext": ...

        def last_known_good(self, value_or_callback=UNSET, /):
            "LastKnownGood reports the checkpointed config the node will fall back to when it encounters an error attempting to use the Assigned config. The Assigned config becomes the LastKnownGood config when the node determines that the Assigned config is stable and correct. This is currently implemented as a 10-minute soak period starting when the local record of Assigned config is updated. If the Assigned config is Active at the end of this period, it becomes the LastKnownGood. Note that if Spec.ConfigSource is reset to nil (use local defaults), the LastKnownGood is also immediately reset to nil, because the local default config is always assumed good. You should not make assumptions about the node\u0027s method of determining config stability and correctness, as this may change or become configurable in the future."
            if self._in_context and value_or_callback is UNSET:
                context = NodeConfigSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_known_good"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeConfigSource.builder())
                if isinstance(output, NodeConfigSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_known_good", value)

    class BuilderContext(BuilderContextBase["NodeConfigStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeConfigStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeConfigStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeConfigStatus", Builder]:
        return GenericListBuilder[NodeConfigStatus, NodeConfigStatus.Builder]()

    active: NodeConfigSource | None = None
    "Active reports the checkpointed config the node is actively using. Active will represent either the current version of the Assigned config, or the current LastKnownGood config, depending on whether attempting to use the Assigned config results in an error."
    assigned: NodeConfigSource | None = None
    "Assigned reports the checkpointed config the node will try to use. When Node.Spec.ConfigSource is updated, the node checkpoints the associated config payload to local disk, along with a record indicating intended config. The node refers to this record to choose its config checkpoint, and reports this record in Assigned. Assigned only updates in the status after the record has been checkpointed to disk. When the Kubelet is restarted, it tries to make the Assigned config the Active config by loading and validating the checkpointed payload identified by Assigned."
    error: str | None = None
    "Error describes any problems reconciling the Spec.ConfigSource to the Active config. Errors may occur, for example, attempting to checkpoint Spec.ConfigSource to the local Assigned record, attempting to checkpoint the payload associated with Spec.ConfigSource, attempting to load or validate the Assigned config, etc. Errors may occur at different points while syncing config. Earlier errors (e.g. download or checkpointing errors) will not result in a rollback to LastKnownGood, and may resolve across Kubelet retries. Later errors (e.g. loading or validating a checkpointed config) will result in a rollback to LastKnownGood. In the latter case, it is usually possible to resolve the error by fixing the config assigned in Spec.ConfigSource. You can find additional information for debugging by searching the error message in the Kubelet log. Error is a human-readable description of the error state; machines can check whether or not Error is empty, but should not rely on the stability of the Error text across Kubelet versions."
    last_known_good: Annotated[NodeConfigSource | None, Field(alias="lastKnownGood")] = None
    "LastKnownGood reports the checkpointed config the node will fall back to when it encounters an error attempting to use the Assigned config. The Assigned config becomes the LastKnownGood config when the node determines that the Assigned config is stable and correct. This is currently implemented as a 10-minute soak period starting when the local record of Assigned config is updated. If the Assigned config is Active at the end of this period, it becomes the LastKnownGood. Note that if Spec.ConfigSource is reset to nil (use local defaults), the LastKnownGood is also immediately reset to nil, because the local default config is always assumed good. You should not make assumptions about the node\u0027s method of determining config stability and correctness, as this may change or become configurable in the future."


class NodeDaemonEndpoints(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeDaemonEndpoints"]:
            return NodeDaemonEndpoints

        def build(self) -> "NodeDaemonEndpoints":
            return NodeDaemonEndpoints(**self._attrs)

        @overload
        def kubelet_endpoint(
            self, value_or_callback: DaemonEndpoint | None, /
        ) -> "NodeDaemonEndpoints.Builder": ...

        @overload
        def kubelet_endpoint(
            self,
            value_or_callback: Callable[
                [DaemonEndpoint.Builder], DaemonEndpoint.Builder | DaemonEndpoint
            ],
            /,
        ) -> "NodeDaemonEndpoints.Builder": ...

        @overload
        def kubelet_endpoint(
            self, value_or_callback: Never = ...
        ) -> "DaemonEndpoint.BuilderContext": ...

        def kubelet_endpoint(self, value_or_callback=UNSET, /):
            "Endpoint on which Kubelet is listening."
            if self._in_context and value_or_callback is UNSET:
                context = DaemonEndpoint.BuilderContext()
                context._parent_builder = self
                context._field_name = "kubelet_endpoint"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DaemonEndpoint.builder())
                if isinstance(output, DaemonEndpoint.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("kubelet_endpoint", value)

    class BuilderContext(BuilderContextBase["NodeDaemonEndpoints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeDaemonEndpoints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeDaemonEndpoints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeDaemonEndpoints", Builder]:
        return GenericListBuilder[NodeDaemonEndpoints, NodeDaemonEndpoints.Builder]()

    kubelet_endpoint: Annotated[DaemonEndpoint | None, Field(alias="kubeletEndpoint")] = None
    "Endpoint on which Kubelet is listening."


class NodeFeatures(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeFeatures"]:
            return NodeFeatures

        def build(self) -> "NodeFeatures":
            return NodeFeatures(**self._attrs)

        def supplemental_groups_policy(self, value: bool | None, /) -> Self:
            "SupplementalGroupsPolicy is set to true if the runtime supports SupplementalGroupsPolicy and ContainerUser."
            return self._set("supplemental_groups_policy", value)

    class BuilderContext(BuilderContextBase["NodeFeatures.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeFeatures.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeFeatures."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeFeatures", Builder]:
        return GenericListBuilder[NodeFeatures, NodeFeatures.Builder]()

    supplemental_groups_policy: Annotated[bool | None, Field(alias="supplementalGroupsPolicy")] = (
        None
    )
    "SupplementalGroupsPolicy is set to true if the runtime supports SupplementalGroupsPolicy and ContainerUser."


class NodeRuntimeHandlerFeatures(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeRuntimeHandlerFeatures"]:
            return NodeRuntimeHandlerFeatures

        def build(self) -> "NodeRuntimeHandlerFeatures":
            return NodeRuntimeHandlerFeatures(**self._attrs)

        def recursive_read_only_mounts(self, value: bool | None, /) -> Self:
            "RecursiveReadOnlyMounts is set to true if the runtime handler supports RecursiveReadOnlyMounts."
            return self._set("recursive_read_only_mounts", value)

        def user_namespaces(self, value: bool | None, /) -> Self:
            "UserNamespaces is set to true if the runtime handler supports UserNamespaces, including for volumes."
            return self._set("user_namespaces", value)

    class BuilderContext(BuilderContextBase["NodeRuntimeHandlerFeatures.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeRuntimeHandlerFeatures.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeRuntimeHandlerFeatures."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeRuntimeHandlerFeatures", Builder]:
        return GenericListBuilder[NodeRuntimeHandlerFeatures, NodeRuntimeHandlerFeatures.Builder]()

    recursive_read_only_mounts: Annotated[bool | None, Field(alias="recursiveReadOnlyMounts")] = (
        None
    )
    "RecursiveReadOnlyMounts is set to true if the runtime handler supports RecursiveReadOnlyMounts."
    user_namespaces: Annotated[bool | None, Field(alias="userNamespaces")] = None
    "UserNamespaces is set to true if the runtime handler supports UserNamespaces, including for volumes."


class NodeSelectorRequirement(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSelectorRequirement"]:
            return NodeSelectorRequirement

        def build(self) -> "NodeSelectorRequirement":
            return NodeSelectorRequirement(**self._attrs)

        def key(self, value: str, /) -> Self:
            "The label key that the selector applies to."
            return self._set("key", value)

        def operator(self, value: str, /) -> Self:
            "Represents a key\u0027s relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt."
            return self._set("operator", value)

        def values(self, value: list[str] | None, /) -> Self:
            "An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch."
            return self._set("values", value)

    class BuilderContext(BuilderContextBase["NodeSelectorRequirement.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSelectorRequirement.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSelectorRequirement."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSelectorRequirement", Builder]:
        return GenericListBuilder[NodeSelectorRequirement, NodeSelectorRequirement.Builder]()

    key: str
    "The label key that the selector applies to."
    operator: str
    "Represents a key\u0027s relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt."
    values: list[str] | None = None
    "An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch."


class NodeSelectorTerm(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSelectorTerm"]:
            return NodeSelectorTerm

        def build(self) -> "NodeSelectorTerm":
            return NodeSelectorTerm(**self._attrs)

        @overload
        def match_expressions(
            self, value_or_callback: list[NodeSelectorRequirement] | None, /
        ) -> "NodeSelectorTerm.Builder": ...

        @overload
        def match_expressions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeSelectorRequirement, NodeSelectorRequirement.Builder]],
                GenericListBuilder[NodeSelectorRequirement, NodeSelectorRequirement.Builder]
                | list[NodeSelectorRequirement],
            ],
            /,
        ) -> "NodeSelectorTerm.Builder": ...

        @overload
        def match_expressions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeSelectorRequirement.Builder]: ...

        def match_expressions(self, value_or_callback=UNSET, /):
            "A list of node selector requirements by node\u0027s labels."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeSelectorRequirement.Builder]()
                context._parent_builder = self
                context._field_name = "match_expressions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelectorRequirement.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("match_expressions", value)

        @overload
        def match_fields(
            self, value_or_callback: list[NodeSelectorRequirement] | None, /
        ) -> "NodeSelectorTerm.Builder": ...

        @overload
        def match_fields(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeSelectorRequirement, NodeSelectorRequirement.Builder]],
                GenericListBuilder[NodeSelectorRequirement, NodeSelectorRequirement.Builder]
                | list[NodeSelectorRequirement],
            ],
            /,
        ) -> "NodeSelectorTerm.Builder": ...

        @overload
        def match_fields(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeSelectorRequirement.Builder]: ...

        def match_fields(self, value_or_callback=UNSET, /):
            "A list of node selector requirements by node\u0027s fields."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeSelectorRequirement.Builder]()
                context._parent_builder = self
                context._field_name = "match_fields"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelectorRequirement.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("match_fields", value)

    class BuilderContext(BuilderContextBase["NodeSelectorTerm.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSelectorTerm.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSelectorTerm."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSelectorTerm", Builder]:
        return GenericListBuilder[NodeSelectorTerm, NodeSelectorTerm.Builder]()

    match_expressions: Annotated[
        list[NodeSelectorRequirement] | None, Field(alias="matchExpressions")
    ] = None
    "A list of node selector requirements by node\u0027s labels."
    match_fields: Annotated[list[NodeSelectorRequirement] | None, Field(alias="matchFields")] = None
    "A list of node selector requirements by node\u0027s fields."


class NodeSwapStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSwapStatus"]:
            return NodeSwapStatus

        def build(self) -> "NodeSwapStatus":
            return NodeSwapStatus(**self._attrs)

        def capacity(self, value: int | None, /) -> Self:
            "Total amount of swap memory in bytes."
            return self._set("capacity", value)

    class BuilderContext(BuilderContextBase["NodeSwapStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSwapStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSwapStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSwapStatus", Builder]:
        return GenericListBuilder[NodeSwapStatus, NodeSwapStatus.Builder]()

    capacity: int | None = None
    "Total amount of swap memory in bytes."


class NodeSystemInfo(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSystemInfo"]:
            return NodeSystemInfo

        def build(self) -> "NodeSystemInfo":
            return NodeSystemInfo(**self._attrs)

        def architecture(self, value: str, /) -> Self:
            "The Architecture reported by the node"
            return self._set("architecture", value)

        def boot_id(self, value: str, /) -> Self:
            "Boot ID reported by the node."
            return self._set("boot_id", value)

        def container_runtime_version(self, value: str, /) -> Self:
            "ContainerRuntime Version reported by the node through runtime remote API (e.g. containerd://1.4.2)."
            return self._set("container_runtime_version", value)

        def kernel_version(self, value: str, /) -> Self:
            "Kernel Version reported by the node from \u0027uname -r\u0027 (e.g. 3.16.0-0.bpo.4-amd64)."
            return self._set("kernel_version", value)

        def kube_proxy_version(self, value: str, /) -> Self:
            "Deprecated: KubeProxy Version reported by the node."
            return self._set("kube_proxy_version", value)

        def kubelet_version(self, value: str, /) -> Self:
            "Kubelet Version reported by the node."
            return self._set("kubelet_version", value)

        def machine_id(self, value: str, /) -> Self:
            "MachineID reported by the node. For unique machine identification in the cluster this field is preferred. Learn more from man(5) machine-id: http://man7.org/linux/man-pages/man5/machine-id.5.html"
            return self._set("machine_id", value)

        def operating_system(self, value: str, /) -> Self:
            "The Operating System reported by the node"
            return self._set("operating_system", value)

        def os_image(self, value: str, /) -> Self:
            "OS Image reported by the node from /etc/os-release (e.g. Debian GNU/Linux 7 (wheezy))."
            return self._set("os_image", value)

        @overload
        def swap(self, value_or_callback: NodeSwapStatus | None, /) -> "NodeSystemInfo.Builder": ...

        @overload
        def swap(
            self,
            value_or_callback: Callable[
                [NodeSwapStatus.Builder], NodeSwapStatus.Builder | NodeSwapStatus
            ],
            /,
        ) -> "NodeSystemInfo.Builder": ...

        @overload
        def swap(self, value_or_callback: Never = ...) -> "NodeSwapStatus.BuilderContext": ...

        def swap(self, value_or_callback=UNSET, /):
            "Swap Info reported by the node."
            if self._in_context and value_or_callback is UNSET:
                context = NodeSwapStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "swap"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSwapStatus.builder())
                if isinstance(output, NodeSwapStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("swap", value)

        def system_uuid(self, value: str, /) -> Self:
            "SystemUUID reported by the node. For unique machine identification MachineID is preferred. This field is specific to Red Hat hosts https://access.redhat.com/documentation/en-us/red_hat_subscription_management/1/html/rhsm/uuid"
            return self._set("system_uuid", value)

    class BuilderContext(BuilderContextBase["NodeSystemInfo.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSystemInfo.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSystemInfo."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSystemInfo", Builder]:
        return GenericListBuilder[NodeSystemInfo, NodeSystemInfo.Builder]()

    architecture: str
    "The Architecture reported by the node"
    boot_id: Annotated[str, Field(alias="bootID")]
    "Boot ID reported by the node."
    container_runtime_version: Annotated[str, Field(alias="containerRuntimeVersion")]
    "ContainerRuntime Version reported by the node through runtime remote API (e.g. containerd://1.4.2)."
    kernel_version: Annotated[str, Field(alias="kernelVersion")]
    "Kernel Version reported by the node from \u0027uname -r\u0027 (e.g. 3.16.0-0.bpo.4-amd64)."
    kube_proxy_version: Annotated[str, Field(alias="kubeProxyVersion")]
    "Deprecated: KubeProxy Version reported by the node."
    kubelet_version: Annotated[str, Field(alias="kubeletVersion")]
    "Kubelet Version reported by the node."
    machine_id: Annotated[str, Field(alias="machineID")]
    "MachineID reported by the node. For unique machine identification in the cluster this field is preferred. Learn more from man(5) machine-id: http://man7.org/linux/man-pages/man5/machine-id.5.html"
    operating_system: Annotated[str, Field(alias="operatingSystem")]
    "The Operating System reported by the node"
    os_image: Annotated[str, Field(alias="osImage")]
    "OS Image reported by the node from /etc/os-release (e.g. Debian GNU/Linux 7 (wheezy))."
    swap: NodeSwapStatus | None = None
    "Swap Info reported by the node."
    system_uuid: Annotated[str, Field(alias="systemUUID")]
    "SystemUUID reported by the node. For unique machine identification MachineID is preferred. This field is specific to Red Hat hosts https://access.redhat.com/documentation/en-us/red_hat_subscription_management/1/html/rhsm/uuid"


class ObjectFieldSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ObjectFieldSelector"]:
            return ObjectFieldSelector

        def build(self) -> "ObjectFieldSelector":
            return ObjectFieldSelector(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            'Version of the schema the FieldPath is written in terms of, defaults to "v1".'
            return self._set("api_version", value)

        def field_path(self, value: str, /) -> Self:
            "Path of the field to select in the specified API version."
            return self._set("field_path", value)

    class BuilderContext(BuilderContextBase["ObjectFieldSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ObjectFieldSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ObjectFieldSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ObjectFieldSelector", Builder]:
        return GenericListBuilder[ObjectFieldSelector, ObjectFieldSelector.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    'Version of the schema the FieldPath is written in terms of, defaults to "v1".'
    field_path: Annotated[str, Field(alias="fieldPath")]
    "Path of the field to select in the specified API version."


class ObjectReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ObjectReference"]:
            return ObjectReference

        def build(self) -> "ObjectReference":
            return ObjectReference(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def field_path(self, value: str | None, /) -> Self:
            'If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.'
            return self._set("field_path", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"
            return self._set("namespace", value)

        def resource_version(self, value: str | None, /) -> Self:
            "Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency"
            return self._set("resource_version", value)

        def uid(self, value: str | None, /) -> Self:
            "UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids"
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["ObjectReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ObjectReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ObjectReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ObjectReference", Builder]:
        return GenericListBuilder[ObjectReference, ObjectReference.Builder]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    field_path: Annotated[str | None, Field(alias="fieldPath")] = None
    'If referring to a piece of an object instead of an entire object, this string should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2]. For example, if the object reference is to a container within a pod, this would take on a value like: "spec.containers{name}" (where "name" refers to the name of the container that triggered the event) or if no container name is specified "spec.containers[2]" (container with index 2 in this pod). This syntax is chosen only to have some well-defined way of referencing a part of an object.'
    kind: str | None = None
    "Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"
    resource_version: Annotated[str | None, Field(alias="resourceVersion")] = None
    "Specific resourceVersion to which this reference is made, if any. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency"
    uid: str | None = None
    "UID of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids"


class PersistentVolumeClaimCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaimCondition"]:
            return PersistentVolumeClaimCondition

        def build(self) -> "PersistentVolumeClaimCondition":
            return PersistentVolumeClaimCondition(**self._attrs)

        @overload
        def last_probe_time(
            self, value_or_callback: Time | None, /
        ) -> "PersistentVolumeClaimCondition.Builder": ...

        @overload
        def last_probe_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PersistentVolumeClaimCondition.Builder": ...

        @overload
        def last_probe_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_probe_time(self, value_or_callback=UNSET, /):
            "lastProbeTime is the time we probed the condition."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_probe_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_probe_time", value)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "PersistentVolumeClaimCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PersistentVolumeClaimCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "lastTransitionTime is the time the condition transitioned from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "message is the human-readable message indicating details about last transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            'reason is a unique, this should be a short, machine understandable string that gives the reason for condition\u0027s last transition. If it reports "Resizing" that means the underlying persistent volume is being resized.'
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "Status is the status of the condition. Can be True, False, Unknown. More info: https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/persistent-volume-claim-v1/#:~:text=state%20of%20pvc-,conditions.status,-(string)%2C%20required"
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type is the type of the condition. More info: https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/persistent-volume-claim-v1/#:~:text=set%20to%20%27ResizeStarted%27.-,PersistentVolumeClaimCondition,-contains%20details%20about"
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaimCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaimCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaimCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PersistentVolumeClaimCondition", Builder]:
        return GenericListBuilder[
            PersistentVolumeClaimCondition, PersistentVolumeClaimCondition.Builder
        ]()

    last_probe_time: Annotated[Time | None, Field(alias="lastProbeTime")] = None
    "lastProbeTime is the time we probed the condition."
    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "lastTransitionTime is the time the condition transitioned from one status to another."
    message: str | None = None
    "message is the human-readable message indicating details about last transition."
    reason: str | None = None
    'reason is a unique, this should be a short, machine understandable string that gives the reason for condition\u0027s last transition. If it reports "Resizing" that means the underlying persistent volume is being resized.'
    status: str
    "Status is the status of the condition. Can be True, False, Unknown. More info: https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/persistent-volume-claim-v1/#:~:text=state%20of%20pvc-,conditions.status,-(string)%2C%20required"
    type: str
    "Type is the type of the condition. More info: https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/persistent-volume-claim-v1/#:~:text=set%20to%20%27ResizeStarted%27.-,PersistentVolumeClaimCondition,-contains%20details%20about"


class PersistentVolumeClaimStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaimStatus"]:
            return PersistentVolumeClaimStatus

        def build(self) -> "PersistentVolumeClaimStatus":
            return PersistentVolumeClaimStatus(**self._attrs)

        def access_modes(self, value: list[str] | None, /) -> Self:
            "accessModes contains the actual access modes the volume backing the PVC has. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1"
            return self._set("access_modes", value)

        def allocated_resource_statuses(self, value: dict[str, str] | None, /) -> Self:
            'allocatedResourceStatuses stores status of resource being resized for the given PVC. Key names follow standard Kubernetes label syntax. Valid values are either:\n\t* Un-prefixed keys:\n\t\t- storage - the capacity of the volume.\n\t* Custom resources must use implementation-defined prefixed names such as "example.com/my-custom-resource"\nApart from above values - keys that are unprefixed or have kubernetes.io prefix are considered reserved and hence may not be used.\n\nClaimResourceStatus can be in any of following states:\n\t- ControllerResizeInProgress:\n\t\tState set when resize controller starts resizing the volume in control-plane.\n\t- ControllerResizeFailed:\n\t\tState set when resize has failed in resize controller with a terminal error.\n\t- NodeResizePending:\n\t\tState set when resize controller has finished resizing the volume but further resizing of\n\t\tvolume is needed on the node.\n\t- NodeResizeInProgress:\n\t\tState set when kubelet starts resizing the volume.\n\t- NodeResizeFailed:\n\t\tState set when resizing has failed in kubelet with a terminal error. Transient errors don\u0027t set\n\t\tNodeResizeFailed.\nFor example: if expanding a PVC for more capacity - this field can be one of the following states:\n\t- pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "ControllerResizeInProgress"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "ControllerResizeFailed"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizePending"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizeInProgress"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizeFailed"\nWhen this field is not set, it means that no resize operation is in progress for the given PVC.\n\nA controller that receives PVC update with previously unknown resourceName or ClaimResourceStatus should ignore the update for the purpose it was designed. For example - a controller that only is responsible for resizing capacity of the volume, should ignore PVC updates that change other valid resources associated with PVC.\n\nThis is an alpha field and requires enabling RecoverVolumeExpansionFailure feature.'
            return self._set("allocated_resource_statuses", value)

        def allocated_resources(self, value: dict[str, Quantity] | None, /) -> Self:
            'allocatedResources tracks the resources allocated to a PVC including its capacity. Key names follow standard Kubernetes label syntax. Valid values are either:\n\t* Un-prefixed keys:\n\t\t- storage - the capacity of the volume.\n\t* Custom resources must use implementation-defined prefixed names such as "example.com/my-custom-resource"\nApart from above values - keys that are unprefixed or have kubernetes.io prefix are considered reserved and hence may not be used.\n\nCapacity reported here may be larger than the actual capacity when a volume expansion operation is requested. For storage quota, the larger value from allocatedResources and PVC.spec.resources is used. If allocatedResources is not set, PVC.spec.resources alone is used for quota calculation. If a volume expansion capacity request is lowered, allocatedResources is only lowered if there are no expansion operations in progress and if the actual volume capacity is equal or lower than the requested capacity.\n\nA controller that receives PVC update with previously unknown resourceName should ignore the update for the purpose it was designed. For example - a controller that only is responsible for resizing capacity of the volume, should ignore PVC updates that change other valid resources associated with PVC.\n\nThis is an alpha field and requires enabling RecoverVolumeExpansionFailure feature.'
            return self._set("allocated_resources", value)

        def capacity(self, value: dict[str, Quantity] | None, /) -> Self:
            "capacity represents the actual resources of the underlying volume."
            return self._set("capacity", value)

        @overload
        def conditions(
            self, value_or_callback: list[PersistentVolumeClaimCondition] | None, /
        ) -> "PersistentVolumeClaimStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        PersistentVolumeClaimCondition,
                        PersistentVolumeClaimCondition.Builder,
                    ]
                ],
                GenericListBuilder[
                    PersistentVolumeClaimCondition,
                    PersistentVolumeClaimCondition.Builder,
                ]
                | list[PersistentVolumeClaimCondition],
            ],
            /,
        ) -> "PersistentVolumeClaimStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PersistentVolumeClaimCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "conditions is the current Condition of persistent volume claim. If underlying persistent volume is being resized then the Condition will be set to \u0027Resizing\u0027."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PersistentVolumeClaimCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def current_volume_attributes_class_name(self, value: str | None, /) -> Self:
            "currentVolumeAttributesClassName is the current name of the VolumeAttributesClass the PVC is using. When unset, there is no VolumeAttributeClass applied to this PersistentVolumeClaim"
            return self._set("current_volume_attributes_class_name", value)

        @overload
        def modify_volume_status(
            self, value_or_callback: ModifyVolumeStatus | None, /
        ) -> "PersistentVolumeClaimStatus.Builder": ...

        @overload
        def modify_volume_status(
            self,
            value_or_callback: Callable[
                [ModifyVolumeStatus.Builder],
                ModifyVolumeStatus.Builder | ModifyVolumeStatus,
            ],
            /,
        ) -> "PersistentVolumeClaimStatus.Builder": ...

        @overload
        def modify_volume_status(
            self, value_or_callback: Never = ...
        ) -> "ModifyVolumeStatus.BuilderContext": ...

        def modify_volume_status(self, value_or_callback=UNSET, /):
            "ModifyVolumeStatus represents the status object of ControllerModifyVolume operation. When this is unset, there is no ModifyVolume operation being attempted."
            if self._in_context and value_or_callback is UNSET:
                context = ModifyVolumeStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "modify_volume_status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ModifyVolumeStatus.builder())
                if isinstance(output, ModifyVolumeStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("modify_volume_status", value)

        def phase(self, value: str | None, /) -> Self:
            "phase represents the current phase of PersistentVolumeClaim."
            return self._set("phase", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaimStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaimStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaimStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolumeClaimStatus", Builder]:
        return GenericListBuilder[
            PersistentVolumeClaimStatus, PersistentVolumeClaimStatus.Builder
        ]()

    access_modes: Annotated[list[str] | None, Field(alias="accessModes")] = None
    "accessModes contains the actual access modes the volume backing the PVC has. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1"
    allocated_resource_statuses: Annotated[
        dict[str, str] | None, Field(alias="allocatedResourceStatuses")
    ] = None
    'allocatedResourceStatuses stores status of resource being resized for the given PVC. Key names follow standard Kubernetes label syntax. Valid values are either:\n\t* Un-prefixed keys:\n\t\t- storage - the capacity of the volume.\n\t* Custom resources must use implementation-defined prefixed names such as "example.com/my-custom-resource"\nApart from above values - keys that are unprefixed or have kubernetes.io prefix are considered reserved and hence may not be used.\n\nClaimResourceStatus can be in any of following states:\n\t- ControllerResizeInProgress:\n\t\tState set when resize controller starts resizing the volume in control-plane.\n\t- ControllerResizeFailed:\n\t\tState set when resize has failed in resize controller with a terminal error.\n\t- NodeResizePending:\n\t\tState set when resize controller has finished resizing the volume but further resizing of\n\t\tvolume is needed on the node.\n\t- NodeResizeInProgress:\n\t\tState set when kubelet starts resizing the volume.\n\t- NodeResizeFailed:\n\t\tState set when resizing has failed in kubelet with a terminal error. Transient errors don\u0027t set\n\t\tNodeResizeFailed.\nFor example: if expanding a PVC for more capacity - this field can be one of the following states:\n\t- pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "ControllerResizeInProgress"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "ControllerResizeFailed"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizePending"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizeInProgress"\n     - pvc.status.allocatedResourceStatus[\u0027storage\u0027] = "NodeResizeFailed"\nWhen this field is not set, it means that no resize operation is in progress for the given PVC.\n\nA controller that receives PVC update with previously unknown resourceName or ClaimResourceStatus should ignore the update for the purpose it was designed. For example - a controller that only is responsible for resizing capacity of the volume, should ignore PVC updates that change other valid resources associated with PVC.\n\nThis is an alpha field and requires enabling RecoverVolumeExpansionFailure feature.'
    allocated_resources: Annotated[
        dict[str, Quantity] | None, Field(alias="allocatedResources")
    ] = None
    'allocatedResources tracks the resources allocated to a PVC including its capacity. Key names follow standard Kubernetes label syntax. Valid values are either:\n\t* Un-prefixed keys:\n\t\t- storage - the capacity of the volume.\n\t* Custom resources must use implementation-defined prefixed names such as "example.com/my-custom-resource"\nApart from above values - keys that are unprefixed or have kubernetes.io prefix are considered reserved and hence may not be used.\n\nCapacity reported here may be larger than the actual capacity when a volume expansion operation is requested. For storage quota, the larger value from allocatedResources and PVC.spec.resources is used. If allocatedResources is not set, PVC.spec.resources alone is used for quota calculation. If a volume expansion capacity request is lowered, allocatedResources is only lowered if there are no expansion operations in progress and if the actual volume capacity is equal or lower than the requested capacity.\n\nA controller that receives PVC update with previously unknown resourceName should ignore the update for the purpose it was designed. For example - a controller that only is responsible for resizing capacity of the volume, should ignore PVC updates that change other valid resources associated with PVC.\n\nThis is an alpha field and requires enabling RecoverVolumeExpansionFailure feature.'
    capacity: dict[str, Quantity] | None = None
    "capacity represents the actual resources of the underlying volume."
    conditions: list[PersistentVolumeClaimCondition] | None = None
    "conditions is the current Condition of persistent volume claim. If underlying persistent volume is being resized then the Condition will be set to \u0027Resizing\u0027."
    current_volume_attributes_class_name: Annotated[
        str | None, Field(alias="currentVolumeAttributesClassName")
    ] = None
    "currentVolumeAttributesClassName is the current name of the VolumeAttributesClass the PVC is using. When unset, there is no VolumeAttributeClass applied to this PersistentVolumeClaim"
    modify_volume_status: Annotated[
        ModifyVolumeStatus | None, Field(alias="modifyVolumeStatus")
    ] = None
    "ModifyVolumeStatus represents the status object of ControllerModifyVolume operation. When this is unset, there is no ModifyVolume operation being attempted."
    phase: str | None = None
    "phase represents the current phase of PersistentVolumeClaim."


class PersistentVolumeClaimVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaimVolumeSource"]:
            return PersistentVolumeClaimVolumeSource

        def build(self) -> "PersistentVolumeClaimVolumeSource":
            return PersistentVolumeClaimVolumeSource(**self._attrs)

        def claim_name(self, value: str, /) -> Self:
            "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
            return self._set("claim_name", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly Will force the ReadOnly setting in VolumeMounts. Default false."
            return self._set("read_only", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaimVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaimVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaimVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PersistentVolumeClaimVolumeSource", Builder]:
        return GenericListBuilder[
            PersistentVolumeClaimVolumeSource, PersistentVolumeClaimVolumeSource.Builder
        ]()

    claim_name: Annotated[str, Field(alias="claimName")]
    "claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly Will force the ReadOnly setting in VolumeMounts. Default false."


class PersistentVolumeStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeStatus"]:
            return PersistentVolumeStatus

        def build(self) -> "PersistentVolumeStatus":
            return PersistentVolumeStatus(**self._attrs)

        @overload
        def last_phase_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "PersistentVolumeStatus.Builder": ...

        @overload
        def last_phase_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PersistentVolumeStatus.Builder": ...

        @overload
        def last_phase_transition_time(
            self, value_or_callback: Never = ...
        ) -> "Time.BuilderContext": ...

        def last_phase_transition_time(self, value_or_callback=UNSET, /):
            "lastPhaseTransitionTime is the time the phase transitioned from one to another and automatically resets to current time everytime a volume phase transitions."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_phase_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_phase_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "message is a human-readable message indicating details about why the volume is in this state."
            return self._set("message", value)

        def phase(self, value: str | None, /) -> Self:
            "phase indicates if a volume is available, bound to a claim, or released by a claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#phase"
            return self._set("phase", value)

        def reason(self, value: str | None, /) -> Self:
            "reason is a brief CamelCase string that describes any failure and is meant for machine parsing and tidy display in the CLI."
            return self._set("reason", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolumeStatus", Builder]:
        return GenericListBuilder[PersistentVolumeStatus, PersistentVolumeStatus.Builder]()

    last_phase_transition_time: Annotated[Time | None, Field(alias="lastPhaseTransitionTime")] = (
        None
    )
    "lastPhaseTransitionTime is the time the phase transitioned from one to another and automatically resets to current time everytime a volume phase transitions."
    message: str | None = None
    "message is a human-readable message indicating details about why the volume is in this state."
    phase: str | None = None
    "phase indicates if a volume is available, bound to a claim, or released by a claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#phase"
    reason: str | None = None
    "reason is a brief CamelCase string that describes any failure and is meant for machine parsing and tidy display in the CLI."


class PhotonPersistentDiskVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PhotonPersistentDiskVolumeSource"]:
            return PhotonPersistentDiskVolumeSource

        def build(self) -> "PhotonPersistentDiskVolumeSource":
            return PhotonPersistentDiskVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def pd_id(self, value: str, /) -> Self:
            "pdID is the ID that identifies Photon Controller persistent disk"
            return self._set("pd_id", value)

    class BuilderContext(BuilderContextBase["PhotonPersistentDiskVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PhotonPersistentDiskVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PhotonPersistentDiskVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PhotonPersistentDiskVolumeSource", Builder]:
        return GenericListBuilder[
            PhotonPersistentDiskVolumeSource, PhotonPersistentDiskVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    pd_id: Annotated[str, Field(alias="pdID")]
    "pdID is the ID that identifies Photon Controller persistent disk"


class PodAffinityTerm(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodAffinityTerm"]:
            return PodAffinityTerm

        def build(self) -> "PodAffinityTerm":
            return PodAffinityTerm(**self._attrs)

        @overload
        def label_selector(
            self, value_or_callback: LabelSelector | None, /
        ) -> "PodAffinityTerm.Builder": ...

        @overload
        def label_selector(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "PodAffinityTerm.Builder": ...

        @overload
        def label_selector(
            self, value_or_callback: Never = ...
        ) -> "LabelSelector.BuilderContext": ...

        def label_selector(self, value_or_callback=UNSET, /):
            "A label query over a set of resources, in this case pods. If it\u0027s null, this PodAffinityTerm matches with no Pods."
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "label_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("label_selector", value)

        def match_label_keys(self, value: list[str] | None, /) -> Self:
            "MatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod\u0027s pod (anti) affinity. Keys that don\u0027t exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both matchLabelKeys and labelSelector. Also, matchLabelKeys cannot be set when labelSelector isn\u0027t set."
            return self._set("match_label_keys", value)

        def mismatch_label_keys(self, value: list[str] | None, /) -> Self:
            "MismatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod\u0027s pod (anti) affinity. Keys that don\u0027t exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both mismatchLabelKeys and labelSelector. Also, mismatchLabelKeys cannot be set when labelSelector isn\u0027t set."
            return self._set("mismatch_label_keys", value)

        @overload
        def namespace_selector(
            self, value_or_callback: LabelSelector | None, /
        ) -> "PodAffinityTerm.Builder": ...

        @overload
        def namespace_selector(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "PodAffinityTerm.Builder": ...

        @overload
        def namespace_selector(
            self, value_or_callback: Never = ...
        ) -> "LabelSelector.BuilderContext": ...

        def namespace_selector(self, value_or_callback=UNSET, /):
            'A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod\u0027s namespace". An empty selector ({}) matches all namespaces.'
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "namespace_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("namespace_selector", value)

        def namespaces(self, value: list[str] | None, /) -> Self:
            'namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod\u0027s namespace".'
            return self._set("namespaces", value)

        def topology_key(self, value: str, /) -> Self:
            "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed."
            return self._set("topology_key", value)

    class BuilderContext(BuilderContextBase["PodAffinityTerm.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodAffinityTerm.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodAffinityTerm."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodAffinityTerm", Builder]:
        return GenericListBuilder[PodAffinityTerm, PodAffinityTerm.Builder]()

    label_selector: Annotated[LabelSelector | None, Field(alias="labelSelector")] = None
    "A label query over a set of resources, in this case pods. If it\u0027s null, this PodAffinityTerm matches with no Pods."
    match_label_keys: Annotated[list[str] | None, Field(alias="matchLabelKeys")] = None
    "MatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod\u0027s pod (anti) affinity. Keys that don\u0027t exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both matchLabelKeys and labelSelector. Also, matchLabelKeys cannot be set when labelSelector isn\u0027t set."
    mismatch_label_keys: Annotated[list[str] | None, Field(alias="mismatchLabelKeys")] = None
    "MismatchLabelKeys is a set of pod label keys to select which pods will be taken into consideration. The keys are used to lookup values from the incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)` to select the group of existing pods which pods will be taken into consideration for the incoming pod\u0027s pod (anti) affinity. Keys that don\u0027t exist in the incoming pod labels will be ignored. The default value is empty. The same key is forbidden to exist in both mismatchLabelKeys and labelSelector. Also, mismatchLabelKeys cannot be set when labelSelector isn\u0027t set."
    namespace_selector: Annotated[LabelSelector | None, Field(alias="namespaceSelector")] = None
    'A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod\u0027s namespace". An empty selector ({}) matches all namespaces.'
    namespaces: list[str] | None = None
    'namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod\u0027s namespace".'
    topology_key: Annotated[str, Field(alias="topologyKey")]
    "This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed."


class PodCertificateProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodCertificateProjection"]:
            return PodCertificateProjection

        def build(self) -> "PodCertificateProjection":
            return PodCertificateProjection(**self._attrs)

        def certificate_chain_path(self, value: str | None, /) -> Self:
            "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation."
            return self._set("certificate_chain_path", value)

        def credential_bundle_path(self, value: str | None, /) -> Self:
            "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks. The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private key.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued certificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod\u0027s application code make a single atomic read that retrieves a consistent key and certificate chain.  If you project them to separate files, your application code will need to additionally check that the leaf certificate was issued to the key."
            return self._set("credential_bundle_path", value)

        def key_path(self, value: str | None, /) -> Self:
            "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation."
            return self._set("key_path", value)

        def key_type(self, value: str, /) -> Self:
            'The type of keypair Kubelet will generate for the pod.\n\nValid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384", "ECDSAP521", and "ED25519".'
            return self._set("key_type", value)

        def max_expiration_seconds(self, value: int | None, /) -> Self:
            "maxExpirationSeconds is the maximum lifetime permitted for the certificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it generates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver will reject values shorter than 3600 (1 hour).  The maximum allowable value is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600 seconds (1 hour).  This constraint is enforced by kube-apiserver. `kubernetes.io` signers will never issue certificates with a lifetime longer than 24 hours."
            return self._set("max_expiration_seconds", value)

        def signer_name(self, value: str, /) -> Self:
            "Kubelet\u0027s generated CSRs will be addressed to this signer."
            return self._set("signer_name", value)

    class BuilderContext(BuilderContextBase["PodCertificateProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodCertificateProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodCertificateProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodCertificateProjection", Builder]:
        return GenericListBuilder[PodCertificateProjection, PodCertificateProjection.Builder]()

    certificate_chain_path: Annotated[str | None, Field(alias="certificateChainPath")] = None
    "Write the certificate chain at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation."
    credential_bundle_path: Annotated[str | None, Field(alias="credentialBundlePath")] = None
    "Write the credential bundle at this path in the projected volume.\n\nThe credential bundle is a single file that contains multiple PEM blocks. The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private key.\n\nThe remaining blocks are CERTIFICATE blocks, containing the issued certificate chain from the signer (leaf and any intermediates).\n\nUsing credentialBundlePath lets your Pod\u0027s application code make a single atomic read that retrieves a consistent key and certificate chain.  If you project them to separate files, your application code will need to additionally check that the leaf certificate was issued to the key."
    key_path: Annotated[str | None, Field(alias="keyPath")] = None
    "Write the key at this path in the projected volume.\n\nMost applications should use credentialBundlePath.  When using keyPath and certificateChainPath, your application needs to check that the key and leaf certificate are consistent, because it is possible to read the files mid-rotation."
    key_type: Annotated[str, Field(alias="keyType")]
    'The type of keypair Kubelet will generate for the pod.\n\nValid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384", "ECDSAP521", and "ED25519".'
    max_expiration_seconds: Annotated[int | None, Field(alias="maxExpirationSeconds")] = None
    "maxExpirationSeconds is the maximum lifetime permitted for the certificate.\n\nKubelet copies this value verbatim into the PodCertificateRequests it generates for this projection.\n\nIf omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver will reject values shorter than 3600 (1 hour).  The maximum allowable value is 7862400 (91 days).\n\nThe signer implementation is then free to issue a certificate with any lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600 seconds (1 hour).  This constraint is enforced by kube-apiserver. `kubernetes.io` signers will never issue certificates with a lifetime longer than 24 hours."
    signer_name: Annotated[str, Field(alias="signerName")]
    "Kubelet\u0027s generated CSRs will be addressed to this signer."


class PodCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodCondition"]:
            return PodCondition

        def build(self) -> "PodCondition":
            return PodCondition(**self._attrs)

        @overload
        def last_probe_time(self, value_or_callback: Time | None, /) -> "PodCondition.Builder": ...

        @overload
        def last_probe_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PodCondition.Builder": ...

        @overload
        def last_probe_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_probe_time(self, value_or_callback=UNSET, /):
            "Last time we probed the condition."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_probe_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_probe_time", value)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "PodCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PodCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "Last time the condition transitioned from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "Human-readable message indicating details about last transition."
            return self._set("message", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "If set, this represents the .metadata.generation that the pod condition was set based upon. This is an alpha field. Enable PodObservedGenerationTracking to be able to use this field."
            return self._set("observed_generation", value)

        def reason(self, value: str | None, /) -> Self:
            "Unique, one-word, CamelCase reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "Status is the status of the condition. Can be True, False, Unknown. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type is the type of the condition. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["PodCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodCondition", Builder]:
        return GenericListBuilder[PodCondition, PodCondition.Builder]()

    last_probe_time: Annotated[Time | None, Field(alias="lastProbeTime")] = None
    "Last time we probed the condition."
    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "Last time the condition transitioned from one status to another."
    message: str | None = None
    "Human-readable message indicating details about last transition."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "If set, this represents the .metadata.generation that the pod condition was set based upon. This is an alpha field. Enable PodObservedGenerationTracking to be able to use this field."
    reason: str | None = None
    "Unique, one-word, CamelCase reason for the condition\u0027s last transition."
    status: str
    "Status is the status of the condition. Can be True, False, Unknown. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"
    type: str
    "Type is the type of the condition. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"


class PodDNSConfigOption(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodDNSConfigOption"]:
            return PodDNSConfigOption

        def build(self) -> "PodDNSConfigOption":
            return PodDNSConfigOption(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            "Name is this DNS resolver option\u0027s name. Required."
            return self._set("name", value)

        def value(self, value: str | None, /) -> Self:
            "Value is this DNS resolver option\u0027s value."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["PodDNSConfigOption.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodDNSConfigOption.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodDNSConfigOption."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodDNSConfigOption", Builder]:
        return GenericListBuilder[PodDNSConfigOption, PodDNSConfigOption.Builder]()

    name: str | None = None
    "Name is this DNS resolver option\u0027s name. Required."
    value: str | None = None
    "Value is this DNS resolver option\u0027s value."


class PodExtendedResourceClaimStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodExtendedResourceClaimStatus"]:
            return PodExtendedResourceClaimStatus

        def build(self) -> "PodExtendedResourceClaimStatus":
            return PodExtendedResourceClaimStatus(**self._attrs)

        @overload
        def request_mappings(
            self, value_or_callback: list[ContainerExtendedResourceRequest], /
        ) -> "PodExtendedResourceClaimStatus.Builder": ...

        @overload
        def request_mappings(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        ContainerExtendedResourceRequest,
                        ContainerExtendedResourceRequest.Builder,
                    ]
                ],
                GenericListBuilder[
                    ContainerExtendedResourceRequest,
                    ContainerExtendedResourceRequest.Builder,
                ]
                | list[ContainerExtendedResourceRequest],
            ],
            /,
        ) -> "PodExtendedResourceClaimStatus.Builder": ...

        @overload
        def request_mappings(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerExtendedResourceRequest.Builder]: ...

        def request_mappings(self, value_or_callback=UNSET, /):
            "RequestMappings identifies the mapping of \u003ccontainer, extended resource backed by DRA\u003e to  device request in the generated ResourceClaim."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerExtendedResourceRequest.Builder]()
                context._parent_builder = self
                context._field_name = "request_mappings"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerExtendedResourceRequest.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("request_mappings", value)

        def resource_claim_name(self, value: str, /) -> Self:
            "ResourceClaimName is the name of the ResourceClaim that was generated for the Pod in the namespace of the Pod."
            return self._set("resource_claim_name", value)

    class BuilderContext(BuilderContextBase["PodExtendedResourceClaimStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodExtendedResourceClaimStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodExtendedResourceClaimStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PodExtendedResourceClaimStatus", Builder]:
        return GenericListBuilder[
            PodExtendedResourceClaimStatus, PodExtendedResourceClaimStatus.Builder
        ]()

    request_mappings: Annotated[
        list[ContainerExtendedResourceRequest], Field(alias="requestMappings")
    ]
    "RequestMappings identifies the mapping of \u003ccontainer, extended resource backed by DRA\u003e to  device request in the generated ResourceClaim."
    resource_claim_name: Annotated[str, Field(alias="resourceClaimName")]
    "ResourceClaimName is the name of the ResourceClaim that was generated for the Pod in the namespace of the Pod."


class PodIP(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodIP"]:
            return PodIP

        def build(self) -> "PodIP":
            return PodIP(**self._attrs)

        def ip(self, value: str, /) -> Self:
            "IP is the IP address assigned to the pod"
            return self._set("ip", value)

    class BuilderContext(BuilderContextBase["PodIP.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodIP.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodIP."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodIP", Builder]:
        return GenericListBuilder[PodIP, PodIP.Builder]()

    ip: str
    "IP is the IP address assigned to the pod"


class PodOS(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodOS"]:
            return PodOS

        def build(self) -> "PodOS":
            return PodOS(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name is the name of the operating system. The currently supported values are linux and windows. Additional value may be defined in future and can be one of: https://github.com/opencontainers/runtime-spec/blob/master/config.md#platform-specific-configuration Clients should expect to handle additional values and treat unrecognized values in this field as os: null"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["PodOS.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodOS.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodOS."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodOS", Builder]:
        return GenericListBuilder[PodOS, PodOS.Builder]()

    name: str
    "Name is the name of the operating system. The currently supported values are linux and windows. Additional value may be defined in future and can be one of: https://github.com/opencontainers/runtime-spec/blob/master/config.md#platform-specific-configuration Clients should expect to handle additional values and treat unrecognized values in this field as os: null"


class PodReadinessGate(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodReadinessGate"]:
            return PodReadinessGate

        def build(self) -> "PodReadinessGate":
            return PodReadinessGate(**self._attrs)

        def condition_type(self, value: str, /) -> Self:
            "ConditionType refers to a condition in the pod\u0027s condition list with matching type."
            return self._set("condition_type", value)

    class BuilderContext(BuilderContextBase["PodReadinessGate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodReadinessGate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodReadinessGate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodReadinessGate", Builder]:
        return GenericListBuilder[PodReadinessGate, PodReadinessGate.Builder]()

    condition_type: Annotated[str, Field(alias="conditionType")]
    "ConditionType refers to a condition in the pod\u0027s condition list with matching type."


class PodResourceClaim(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodResourceClaim"]:
            return PodResourceClaim

        def build(self) -> "PodResourceClaim":
            return PodResourceClaim(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name uniquely identifies this resource claim inside the pod. This must be a DNS_LABEL."
            return self._set("name", value)

        def resource_claim_name(self, value: str | None, /) -> Self:
            "ResourceClaimName is the name of a ResourceClaim object in the same namespace as this pod.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
            return self._set("resource_claim_name", value)

        def resource_claim_template_name(self, value: str | None, /) -> Self:
            "ResourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace as this pod.\n\nThe template will be used to create a new ResourceClaim, which will be bound to this pod. When this pod is deleted, the ResourceClaim will also be deleted. The pod name and resource name, along with a generated component, will be used to form a unique name for the ResourceClaim, which will be recorded in pod.status.resourceClaimStatuses.\n\nThis field is immutable and no changes will be made to the corresponding ResourceClaim by the control plane after creating the ResourceClaim.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
            return self._set("resource_claim_template_name", value)

    class BuilderContext(BuilderContextBase["PodResourceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodResourceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodResourceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodResourceClaim", Builder]:
        return GenericListBuilder[PodResourceClaim, PodResourceClaim.Builder]()

    name: str
    "Name uniquely identifies this resource claim inside the pod. This must be a DNS_LABEL."
    resource_claim_name: Annotated[str | None, Field(alias="resourceClaimName")] = None
    "ResourceClaimName is the name of a ResourceClaim object in the same namespace as this pod.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."
    resource_claim_template_name: Annotated[
        str | None, Field(alias="resourceClaimTemplateName")
    ] = None
    "ResourceClaimTemplateName is the name of a ResourceClaimTemplate object in the same namespace as this pod.\n\nThe template will be used to create a new ResourceClaim, which will be bound to this pod. When this pod is deleted, the ResourceClaim will also be deleted. The pod name and resource name, along with a generated component, will be used to form a unique name for the ResourceClaim, which will be recorded in pod.status.resourceClaimStatuses.\n\nThis field is immutable and no changes will be made to the corresponding ResourceClaim by the control plane after creating the ResourceClaim.\n\nExactly one of ResourceClaimName and ResourceClaimTemplateName must be set."


class PodResourceClaimStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodResourceClaimStatus"]:
            return PodResourceClaimStatus

        def build(self) -> "PodResourceClaimStatus":
            return PodResourceClaimStatus(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name uniquely identifies this resource claim inside the pod. This must match the name of an entry in pod.spec.resourceClaims, which implies that the string must be a DNS_LABEL."
            return self._set("name", value)

        def resource_claim_name(self, value: str | None, /) -> Self:
            "ResourceClaimName is the name of the ResourceClaim that was generated for the Pod in the namespace of the Pod. If this is unset, then generating a ResourceClaim was not necessary. The pod.spec.resourceClaims entry can be ignored in this case."
            return self._set("resource_claim_name", value)

    class BuilderContext(BuilderContextBase["PodResourceClaimStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodResourceClaimStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodResourceClaimStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodResourceClaimStatus", Builder]:
        return GenericListBuilder[PodResourceClaimStatus, PodResourceClaimStatus.Builder]()

    name: str
    "Name uniquely identifies this resource claim inside the pod. This must match the name of an entry in pod.spec.resourceClaims, which implies that the string must be a DNS_LABEL."
    resource_claim_name: Annotated[str | None, Field(alias="resourceClaimName")] = None
    "ResourceClaimName is the name of the ResourceClaim that was generated for the Pod in the namespace of the Pod. If this is unset, then generating a ResourceClaim was not necessary. The pod.spec.resourceClaims entry can be ignored in this case."


class PodSchedulingGate(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodSchedulingGate"]:
            return PodSchedulingGate

        def build(self) -> "PodSchedulingGate":
            return PodSchedulingGate(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name of the scheduling gate. Each scheduling gate must have a unique name field."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["PodSchedulingGate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodSchedulingGate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodSchedulingGate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodSchedulingGate", Builder]:
        return GenericListBuilder[PodSchedulingGate, PodSchedulingGate.Builder]()

    name: str
    "Name of the scheduling gate. Each scheduling gate must have a unique name field."


class PortStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PortStatus"]:
            return PortStatus

        def build(self) -> "PortStatus":
            return PortStatus(**self._attrs)

        def error(self, value: str | None, /) -> Self:
            "Error is to record the problem with the service port The format of the error shall comply with the following rules: - built-in error values shall be specified in this file and those shall use\n  CamelCase names\n- cloud provider specific error values must have names that comply with the\n  format foo.example.com/CamelCase."
            return self._set("error", value)

        def port(self, value: int, /) -> Self:
            "Port is the port number of the service port of which status is recorded here"
            return self._set("port", value)

        def protocol(self, value: str, /) -> Self:
            'Protocol is the protocol of the service port of which status is recorded here The supported values are: "TCP", "UDP", "SCTP"'
            return self._set("protocol", value)

    class BuilderContext(BuilderContextBase["PortStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PortStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PortStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PortStatus", Builder]:
        return GenericListBuilder[PortStatus, PortStatus.Builder]()

    error: str | None = None
    "Error is to record the problem with the service port The format of the error shall comply with the following rules: - built-in error values shall be specified in this file and those shall use\n  CamelCase names\n- cloud provider specific error values must have names that comply with the\n  format foo.example.com/CamelCase."
    port: int
    "Port is the port number of the service port of which status is recorded here"
    protocol: str
    'Protocol is the protocol of the service port of which status is recorded here The supported values are: "TCP", "UDP", "SCTP"'


class PortworxVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PortworxVolumeSource"]:
            return PortworxVolumeSource

        def build(self) -> "PortworxVolumeSource":
            return PortworxVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fSType represents the filesystem type to mount Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        def volume_id(self, value: str, /) -> Self:
            "volumeID uniquely identifies a Portworx volume"
            return self._set("volume_id", value)

    class BuilderContext(BuilderContextBase["PortworxVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PortworxVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PortworxVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PortworxVolumeSource", Builder]:
        return GenericListBuilder[PortworxVolumeSource, PortworxVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fSType represents the filesystem type to mount Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs". Implicitly inferred to be "ext4" if unspecified.'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    volume_id: Annotated[str, Field(alias="volumeID")]
    "volumeID uniquely identifies a Portworx volume"


class PreferredSchedulingTerm(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PreferredSchedulingTerm"]:
            return PreferredSchedulingTerm

        def build(self) -> "PreferredSchedulingTerm":
            return PreferredSchedulingTerm(**self._attrs)

        @overload
        def preference(
            self, value_or_callback: NodeSelectorTerm, /
        ) -> "PreferredSchedulingTerm.Builder": ...

        @overload
        def preference(
            self,
            value_or_callback: Callable[
                [NodeSelectorTerm.Builder], NodeSelectorTerm.Builder | NodeSelectorTerm
            ],
            /,
        ) -> "PreferredSchedulingTerm.Builder": ...

        @overload
        def preference(
            self, value_or_callback: Never = ...
        ) -> "NodeSelectorTerm.BuilderContext": ...

        def preference(self, value_or_callback=UNSET, /):
            "A node selector term, associated with the corresponding weight."
            if self._in_context and value_or_callback is UNSET:
                context = NodeSelectorTerm.BuilderContext()
                context._parent_builder = self
                context._field_name = "preference"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelectorTerm.builder())
                if isinstance(output, NodeSelectorTerm.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("preference", value)

        def weight(self, value: int, /) -> Self:
            "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100."
            return self._set("weight", value)

    class BuilderContext(BuilderContextBase["PreferredSchedulingTerm.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PreferredSchedulingTerm.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PreferredSchedulingTerm."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PreferredSchedulingTerm", Builder]:
        return GenericListBuilder[PreferredSchedulingTerm, PreferredSchedulingTerm.Builder]()

    preference: NodeSelectorTerm
    "A node selector term, associated with the corresponding weight."
    weight: int
    "Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100."


class QuobyteVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["QuobyteVolumeSource"]:
            return QuobyteVolumeSource

        def build(self) -> "QuobyteVolumeSource":
            return QuobyteVolumeSource(**self._attrs)

        def group(self, value: str | None, /) -> Self:
            "group to map volume access to Default is no group"
            return self._set("group", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the Quobyte volume to be mounted with read-only permissions. Defaults to false."
            return self._set("read_only", value)

        def registry(self, value: str, /) -> Self:
            "registry represents a single or multiple Quobyte Registry services specified as a string as host:port pair (multiple entries are separated with commas) which acts as the central registry for volumes"
            return self._set("registry", value)

        def tenant(self, value: str | None, /) -> Self:
            "tenant owning the given Quobyte volume in the Backend Used with dynamically provisioned Quobyte volumes, value is set by the plugin"
            return self._set("tenant", value)

        def user(self, value: str | None, /) -> Self:
            "user to map volume access to Defaults to serivceaccount user"
            return self._set("user", value)

        def volume(self, value: str, /) -> Self:
            "volume is a string that references an already created Quobyte volume by name."
            return self._set("volume", value)

    class BuilderContext(BuilderContextBase["QuobyteVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = QuobyteVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for QuobyteVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["QuobyteVolumeSource", Builder]:
        return GenericListBuilder[QuobyteVolumeSource, QuobyteVolumeSource.Builder]()

    group: str | None = None
    "group to map volume access to Default is no group"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the Quobyte volume to be mounted with read-only permissions. Defaults to false."
    registry: str
    "registry represents a single or multiple Quobyte Registry services specified as a string as host:port pair (multiple entries are separated with commas) which acts as the central registry for volumes"
    tenant: str | None = None
    "tenant owning the given Quobyte volume in the Backend Used with dynamically provisioned Quobyte volumes, value is set by the plugin"
    user: str | None = None
    "user to map volume access to Defaults to serivceaccount user"
    volume: str
    "volume is a string that references an already created Quobyte volume by name."


class RBDVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["RBDVolumeSource"]:
            return RBDVolumeSource

        def build(self) -> "RBDVolumeSource":
            return RBDVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd'
            return self._set("fs_type", value)

        def image(self, value: str, /) -> Self:
            "image is the rados image name. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("image", value)

        def keyring(self, value: str | None, /) -> Self:
            "keyring is the path to key ring for RBDUser. Default is /etc/ceph/keyring. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("keyring", value)

        def monitors(self, value: list[str], /) -> Self:
            "monitors is a collection of Ceph monitors. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("monitors", value)

        def pool(self, value: str | None, /) -> Self:
            "pool is the rados pool name. Default is rbd. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("pool", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "RBDVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "RBDVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is name of the authentication secret for RBDUser. If provided overrides keyring. Default is nil. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def user(self, value: str | None, /) -> Self:
            "user is the rados user name. Default is admin. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("user", value)

    class BuilderContext(BuilderContextBase["RBDVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = RBDVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for RBDVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["RBDVolumeSource", Builder]:
        return GenericListBuilder[RBDVolumeSource, RBDVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd'
    image: str
    "image is the rados image name. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    keyring: str | None = None
    "keyring is the path to key ring for RBDUser. Default is /etc/ceph/keyring. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    monitors: list[str]
    "monitors is a collection of Ceph monitors. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    pool: str | None = None
    "pool is the rados pool name. Default is rbd. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef is name of the authentication secret for RBDUser. If provided overrides keyring. Default is nil. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    user: str | None = None
    "user is the rados user name. Default is admin. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"


class ReplicationControllerCondition(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ReplicationControllerCondition"]:
            return ReplicationControllerCondition

        def build(self) -> "ReplicationControllerCondition":
            return ReplicationControllerCondition(**self._attrs)

        @overload
        def last_transition_time(
            self, value_or_callback: Time | None, /
        ) -> "ReplicationControllerCondition.Builder": ...

        @overload
        def last_transition_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ReplicationControllerCondition.Builder": ...

        @overload
        def last_transition_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_transition_time(self, value_or_callback=UNSET, /):
            "The last time the condition transitioned from one status to another."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_transition_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of replication controller condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["ReplicationControllerCondition.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ReplicationControllerCondition.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ReplicationControllerCondition."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ReplicationControllerCondition", Builder]:
        return GenericListBuilder[
            ReplicationControllerCondition, ReplicationControllerCondition.Builder
        ]()

    last_transition_time: Annotated[Time | None, Field(alias="lastTransitionTime")] = None
    "The last time the condition transitioned from one status to another."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of replication controller condition."


class ReplicationControllerStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ReplicationControllerStatus"]:
            return ReplicationControllerStatus

        def build(self) -> "ReplicationControllerStatus":
            return ReplicationControllerStatus(**self._attrs)

        def available_replicas(self, value: int | None, /) -> Self:
            "The number of available replicas (ready for at least minReadySeconds) for this replication controller."
            return self._set("available_replicas", value)

        @overload
        def conditions(
            self, value_or_callback: list[ReplicationControllerCondition] | None, /
        ) -> "ReplicationControllerStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        ReplicationControllerCondition,
                        ReplicationControllerCondition.Builder,
                    ]
                ],
                GenericListBuilder[
                    ReplicationControllerCondition,
                    ReplicationControllerCondition.Builder,
                ]
                | list[ReplicationControllerCondition],
            ],
            /,
        ) -> "ReplicationControllerStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ReplicationControllerCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Represents the latest available observations of a replication controller\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ReplicationControllerCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ReplicationControllerCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def fully_labeled_replicas(self, value: int | None, /) -> Self:
            "The number of pods that have labels matching the labels of the pod template of the replication controller."
            return self._set("fully_labeled_replicas", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration reflects the generation of the most recently observed replication controller."
            return self._set("observed_generation", value)

        def ready_replicas(self, value: int | None, /) -> Self:
            "The number of ready replicas for this replication controller."
            return self._set("ready_replicas", value)

        def replicas(self, value: int, /) -> Self:
            "Replicas is the most recently observed number of replicas. More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#what-is-a-replicationcontroller"
            return self._set("replicas", value)

    class BuilderContext(BuilderContextBase["ReplicationControllerStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ReplicationControllerStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ReplicationControllerStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ReplicationControllerStatus", Builder]:
        return GenericListBuilder[
            ReplicationControllerStatus, ReplicationControllerStatus.Builder
        ]()

    available_replicas: Annotated[int | None, Field(alias="availableReplicas")] = None
    "The number of available replicas (ready for at least minReadySeconds) for this replication controller."
    conditions: list[ReplicationControllerCondition] | None = None
    "Represents the latest available observations of a replication controller\u0027s current state."
    fully_labeled_replicas: Annotated[int | None, Field(alias="fullyLabeledReplicas")] = None
    "The number of pods that have labels matching the labels of the pod template of the replication controller."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration reflects the generation of the most recently observed replication controller."
    ready_replicas: Annotated[int | None, Field(alias="readyReplicas")] = None
    "The number of ready replicas for this replication controller."
    replicas: int
    "Replicas is the most recently observed number of replicas. More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#what-is-a-replicationcontroller"


class ResourceClaim(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaim"]:
            return ResourceClaim

        def build(self) -> "ResourceClaim":
            return ResourceClaim(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container."
            return self._set("name", value)

        def request(self, value: str | None, /) -> Self:
            "Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request."
            return self._set("request", value)

    class BuilderContext(BuilderContextBase["ResourceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaim", Builder]:
        return GenericListBuilder[ResourceClaim, ResourceClaim.Builder]()

    name: str
    "Name must match the name of one entry in pod.spec.resourceClaims of the Pod where this field is used. It makes that resource available inside a container."
    request: str | None = None
    "Request is the name chosen for a request in the referenced claim. If empty, everything from the claim is made available, otherwise only the result of this request."


class ResourceFieldSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceFieldSelector"]:
            return ResourceFieldSelector

        def build(self) -> "ResourceFieldSelector":
            return ResourceFieldSelector(**self._attrs)

        def container_name(self, value: str | None, /) -> Self:
            "Container name: required for volumes, optional for env vars"
            return self._set("container_name", value)

        @overload
        def divisor(
            self, value_or_callback: Quantity | None, /
        ) -> "ResourceFieldSelector.Builder": ...

        @overload
        def divisor(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "ResourceFieldSelector.Builder": ...

        @overload
        def divisor(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def divisor(self, value_or_callback=UNSET, /):
            'Specifies the output format of the exposed resources, defaults to "1"'
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "divisor"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("divisor", value)

        def resource(self, value: str, /) -> Self:
            "Required: resource to select"
            return self._set("resource", value)

    class BuilderContext(BuilderContextBase["ResourceFieldSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceFieldSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceFieldSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceFieldSelector", Builder]:
        return GenericListBuilder[ResourceFieldSelector, ResourceFieldSelector.Builder]()

    container_name: Annotated[str | None, Field(alias="containerName")] = None
    "Container name: required for volumes, optional for env vars"
    divisor: Quantity | None = None
    'Specifies the output format of the exposed resources, defaults to "1"'
    resource: str
    "Required: resource to select"


class ResourceHealth(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceHealth"]:
            return ResourceHealth

        def build(self) -> "ResourceHealth":
            return ResourceHealth(**self._attrs)

        def health(self, value: str | None, /) -> Self:
            "Health of the resource. can be one of:\n - Healthy: operates as normal\n - Unhealthy: reported unhealthy. We consider this a temporary health issue\n              since we do not have a mechanism today to distinguish\n              temporary and permanent issues.\n - Unknown: The status cannot be determined.\n            For example, Device Plugin got unregistered and hasn\u0027t been re-registered since.\n\nIn future we may want to introduce the PermanentlyUnhealthy Status."
            return self._set("health", value)

        def resource_id(self, value: str, /) -> Self:
            "ResourceID is the unique identifier of the resource. See the ResourceID type for more information."
            return self._set("resource_id", value)

    class BuilderContext(BuilderContextBase["ResourceHealth.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceHealth.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceHealth."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceHealth", Builder]:
        return GenericListBuilder[ResourceHealth, ResourceHealth.Builder]()

    health: str | None = None
    "Health of the resource. can be one of:\n - Healthy: operates as normal\n - Unhealthy: reported unhealthy. We consider this a temporary health issue\n              since we do not have a mechanism today to distinguish\n              temporary and permanent issues.\n - Unknown: The status cannot be determined.\n            For example, Device Plugin got unregistered and hasn\u0027t been re-registered since.\n\nIn future we may want to introduce the PermanentlyUnhealthy Status."
    resource_id: Annotated[str, Field(alias="resourceID")]
    "ResourceID is the unique identifier of the resource. See the ResourceID type for more information."


class ResourceQuotaStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceQuotaStatus"]:
            return ResourceQuotaStatus

        def build(self) -> "ResourceQuotaStatus":
            return ResourceQuotaStatus(**self._attrs)

        def hard(self, value: dict[str, Quantity] | None, /) -> Self:
            "Hard is the set of enforced hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/"
            return self._set("hard", value)

        def used(self, value: dict[str, Quantity] | None, /) -> Self:
            "Used is the current observed total usage of the resource in the namespace."
            return self._set("used", value)

    class BuilderContext(BuilderContextBase["ResourceQuotaStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceQuotaStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceQuotaStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceQuotaStatus", Builder]:
        return GenericListBuilder[ResourceQuotaStatus, ResourceQuotaStatus.Builder]()

    hard: dict[str, Quantity] | None = None
    "Hard is the set of enforced hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/"
    used: dict[str, Quantity] | None = None
    "Used is the current observed total usage of the resource in the namespace."


class ResourceRequirements(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceRequirements"]:
            return ResourceRequirements

        def build(self) -> "ResourceRequirements":
            return ResourceRequirements(**self._attrs)

        @overload
        def claims(
            self, value_or_callback: list[ResourceClaim] | None, /
        ) -> "ResourceRequirements.Builder": ...

        @overload
        def claims(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ResourceClaim, ResourceClaim.Builder]],
                GenericListBuilder[ResourceClaim, ResourceClaim.Builder] | list[ResourceClaim],
            ],
            /,
        ) -> "ResourceRequirements.Builder": ...

        @overload
        def claims(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ResourceClaim.Builder]: ...

        def claims(self, value_or_callback=UNSET, /):
            "Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container.\n\nThis field depends on the DynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ResourceClaim.Builder]()
                context._parent_builder = self
                context._field_name = "claims"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaim.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("claims", value)

        def limits(self, value: dict[str, Quantity] | None, /) -> Self:
            "Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
            return self._set("limits", value)

        def requests(self, value: dict[str, Quantity] | None, /) -> Self:
            "Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["ResourceRequirements.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceRequirements.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceRequirements."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceRequirements", Builder]:
        return GenericListBuilder[ResourceRequirements, ResourceRequirements.Builder]()

    claims: list[ResourceClaim] | None = None
    "Claims lists the names of resources, defined in spec.resourceClaims, that are used by this container.\n\nThis field depends on the DynamicResourceAllocation feature gate.\n\nThis field is immutable. It can only be set for containers."
    limits: dict[str, Quantity] | None = None
    "Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
    requests: dict[str, Quantity] | None = None
    "Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"


class ResourceStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceStatus"]:
            return ResourceStatus

        def build(self) -> "ResourceStatus":
            return ResourceStatus(**self._attrs)

        def name(self, value: str, /) -> Self:
            'Name of the resource. Must be unique within the pod and in case of non-DRA resource, match one of the resources from the pod spec. For DRA resources, the value must be "claim:\u003cclaim_name\u003e/\u003crequest\u003e". When this status is reported about a container, the "claim_name" and "request" must match one of the claims of this container.'
            return self._set("name", value)

        @overload
        def resources(
            self, value_or_callback: list[ResourceHealth] | None, /
        ) -> "ResourceStatus.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ResourceHealth, ResourceHealth.Builder]],
                GenericListBuilder[ResourceHealth, ResourceHealth.Builder] | list[ResourceHealth],
            ],
            /,
        ) -> "ResourceStatus.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ResourceHealth.Builder]: ...

        def resources(self, value_or_callback=UNSET, /):
            "List of unique resources health. Each element in the list contains an unique resource ID and its health. At a minimum, for the lifetime of a Pod, resource ID must uniquely identify the resource allocated to the Pod on the Node. If other Pod on the same Node reports the status with the same resource ID, it must be the same resource they share. See ResourceID type definition for a specific format it has in various use cases."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ResourceHealth.Builder]()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceHealth.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

    class BuilderContext(BuilderContextBase["ResourceStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceStatus", Builder]:
        return GenericListBuilder[ResourceStatus, ResourceStatus.Builder]()

    name: str
    'Name of the resource. Must be unique within the pod and in case of non-DRA resource, match one of the resources from the pod spec. For DRA resources, the value must be "claim:\u003cclaim_name\u003e/\u003crequest\u003e". When this status is reported about a container, the "claim_name" and "request" must match one of the claims of this container.'
    resources: list[ResourceHealth] | None = None
    "List of unique resources health. Each element in the list contains an unique resource ID and its health. At a minimum, for the lifetime of a Pod, resource ID must uniquely identify the resource allocated to the Pod on the Node. If other Pod on the same Node reports the status with the same resource ID, it must be the same resource they share. See ResourceID type definition for a specific format it has in various use cases."


class SELinuxOptions(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SELinuxOptions"]:
            return SELinuxOptions

        def build(self) -> "SELinuxOptions":
            return SELinuxOptions(**self._attrs)

        def level(self, value: str | None, /) -> Self:
            "Level is SELinux level label that applies to the container."
            return self._set("level", value)

        def role(self, value: str | None, /) -> Self:
            "Role is a SELinux role label that applies to the container."
            return self._set("role", value)

        def type(self, value: str | None, /) -> Self:
            "Type is a SELinux type label that applies to the container."
            return self._set("type", value)

        def user(self, value: str | None, /) -> Self:
            "User is a SELinux user label that applies to the container."
            return self._set("user", value)

    class BuilderContext(BuilderContextBase["SELinuxOptions.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SELinuxOptions.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SELinuxOptions."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SELinuxOptions", Builder]:
        return GenericListBuilder[SELinuxOptions, SELinuxOptions.Builder]()

    level: str | None = None
    "Level is SELinux level label that applies to the container."
    role: str | None = None
    "Role is a SELinux role label that applies to the container."
    type: str | None = None
    "Type is a SELinux type label that applies to the container."
    user: str | None = None
    "User is a SELinux user label that applies to the container."


class ScaleIOVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ScaleIOVolumeSource"]:
            return ScaleIOVolumeSource

        def build(self) -> "ScaleIOVolumeSource":
            return ScaleIOVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Default is "xfs".'
            return self._set("fs_type", value)

        def gateway(self, value: str, /) -> Self:
            "gateway is the host address of the ScaleIO API Gateway."
            return self._set("gateway", value)

        def protection_domain(self, value: str | None, /) -> Self:
            "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage."
            return self._set("protection_domain", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference, /
        ) -> "ScaleIOVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "ScaleIOVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef references to the secret for ScaleIO user and other sensitive information. If this is not provided, Login operation will fail."
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def ssl_enabled(self, value: bool | None, /) -> Self:
            "sslEnabled Flag enable/disable SSL communication with Gateway, default false"
            return self._set("ssl_enabled", value)

        def storage_mode(self, value: str | None, /) -> Self:
            "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned. Default is ThinProvisioned."
            return self._set("storage_mode", value)

        def storage_pool(self, value: str | None, /) -> Self:
            "storagePool is the ScaleIO Storage Pool associated with the protection domain."
            return self._set("storage_pool", value)

        def system(self, value: str, /) -> Self:
            "system is the name of the storage system as configured in ScaleIO."
            return self._set("system", value)

        def volume_name(self, value: str | None, /) -> Self:
            "volumeName is the name of a volume already created in the ScaleIO system that is associated with this volume source."
            return self._set("volume_name", value)

    class BuilderContext(BuilderContextBase["ScaleIOVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ScaleIOVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ScaleIOVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ScaleIOVolumeSource", Builder]:
        return GenericListBuilder[ScaleIOVolumeSource, ScaleIOVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Default is "xfs".'
    gateway: str
    "gateway is the host address of the ScaleIO API Gateway."
    protection_domain: Annotated[str | None, Field(alias="protectionDomain")] = None
    "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[LocalObjectReference, Field(alias="secretRef")]
    "secretRef references to the secret for ScaleIO user and other sensitive information. If this is not provided, Login operation will fail."
    ssl_enabled: Annotated[bool | None, Field(alias="sslEnabled")] = None
    "sslEnabled Flag enable/disable SSL communication with Gateway, default false"
    storage_mode: Annotated[str | None, Field(alias="storageMode")] = None
    "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned. Default is ThinProvisioned."
    storage_pool: Annotated[str | None, Field(alias="storagePool")] = None
    "storagePool is the ScaleIO Storage Pool associated with the protection domain."
    system: str
    "system is the name of the storage system as configured in ScaleIO."
    volume_name: Annotated[str | None, Field(alias="volumeName")] = None
    "volumeName is the name of a volume already created in the ScaleIO system that is associated with this volume source."


class ScopedResourceSelectorRequirement(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ScopedResourceSelectorRequirement"]:
            return ScopedResourceSelectorRequirement

        def build(self) -> "ScopedResourceSelectorRequirement":
            return ScopedResourceSelectorRequirement(**self._attrs)

        def operator(self, value: str, /) -> Self:
            "Represents a scope\u0027s relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist."
            return self._set("operator", value)

        def scope_name(self, value: str, /) -> Self:
            "The name of the scope that the selector applies to."
            return self._set("scope_name", value)

        def values(self, value: list[str] | None, /) -> Self:
            "An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch."
            return self._set("values", value)

    class BuilderContext(BuilderContextBase["ScopedResourceSelectorRequirement.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ScopedResourceSelectorRequirement.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ScopedResourceSelectorRequirement."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ScopedResourceSelectorRequirement", Builder]:
        return GenericListBuilder[
            ScopedResourceSelectorRequirement, ScopedResourceSelectorRequirement.Builder
        ]()

    operator: str
    "Represents a scope\u0027s relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist."
    scope_name: Annotated[str, Field(alias="scopeName")]
    "The name of the scope that the selector applies to."
    values: list[str] | None = None
    "An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch."


class SeccompProfile(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SeccompProfile"]:
            return SeccompProfile

        def build(self) -> "SeccompProfile":
            return SeccompProfile(**self._attrs)

        def localhost_profile(self, value: str | None, /) -> Self:
            'localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet\u0027s configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type.'
            return self._set("localhost_profile", value)

        def type(self, value: str, /) -> Self:
            "type indicates which kind of seccomp profile will be applied. Valid options are:\n\nLocalhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["SeccompProfile.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SeccompProfile.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SeccompProfile."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SeccompProfile", Builder]:
        return GenericListBuilder[SeccompProfile, SeccompProfile.Builder]()

    localhost_profile: Annotated[str | None, Field(alias="localhostProfile")] = None
    'localhostProfile indicates a profile defined in a file on the node should be used. The profile must be preconfigured on the node to work. Must be a descending path, relative to the kubelet\u0027s configured seccomp profile location. Must be set if type is "Localhost". Must NOT be set for any other type.'
    type: str
    "type indicates which kind of seccomp profile will be applied. Valid options are:\n\nLocalhost - a profile defined in a file on the node should be used. RuntimeDefault - the container runtime default profile should be used. Unconfined - no profile should be applied."


class Secret(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "secrets",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Secret"]:
            return Secret

        def build(self) -> "Secret":
            return Secret(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def data(self, value: dict[str, str] | None, /) -> Self:
            "Data contains the secret data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. The serialized form of the secret data is a base64 encoded string, representing the arbitrary (possibly non-string) data value here. Described in https://tools.ietf.org/html/rfc4648#section-4"
            return self._set("data", value)

        def immutable(self, value: bool | None, /) -> Self:
            "Immutable, if set to true, ensures that data stored in the Secret cannot be updated (only object metadata can be modified). If not set to true, the field can be modified at any time. Defaulted to nil."
            return self._set("immutable", value)

        def kind(self, value: Literal["Secret"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Secret.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Secret.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        def string_data(self, value: dict[str, str] | None, /) -> Self:
            "stringData allows specifying non-binary secret data in string form. It is provided as a write-only input field for convenience. All keys and values are merged into the data field on write, overwriting any existing values. The stringData field is never output when reading from the API."
            return self._set("string_data", value)

        def type(self, value: str | None, /) -> Self:
            "Used to facilitate programmatic handling of secret data. More info: https://kubernetes.io/docs/concepts/configuration/secret/#secret-types"
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["Secret.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Secret.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Secret."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Secret", Builder]:
        return GenericListBuilder[Secret, Secret.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    data: dict[str, str] | None = None
    "Data contains the secret data. Each key must consist of alphanumeric characters, \u0027-\u0027, \u0027_\u0027 or \u0027.\u0027. The serialized form of the secret data is a base64 encoded string, representing the arbitrary (possibly non-string) data value here. Described in https://tools.ietf.org/html/rfc4648#section-4"
    immutable: bool | None = None
    "Immutable, if set to true, ensures that data stored in the Secret cannot be updated (only object metadata can be modified). If not set to true, the field can be modified at any time. Defaulted to nil."
    kind: Literal["Secret"] = "Secret"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    string_data: Annotated[dict[str, str] | None, Field(alias="stringData")] = None
    "stringData allows specifying non-binary secret data in string form. It is provided as a write-only input field for convenience. All keys and values are merged into the data field on write, overwriting any existing values. The stringData field is never output when reading from the API."
    type: str | None = None
    "Used to facilitate programmatic handling of secret data. More info: https://kubernetes.io/docs/concepts/configuration/secret/#secret-types"


class SecretEnvSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecretEnvSource"]:
            return SecretEnvSource

        def build(self) -> "SecretEnvSource":
            return SecretEnvSource(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "Specify whether the Secret must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["SecretEnvSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecretEnvSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecretEnvSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecretEnvSource", Builder]:
        return GenericListBuilder[SecretEnvSource, SecretEnvSource.Builder]()

    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "Specify whether the Secret must be defined"


class SecretKeySelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecretKeySelector"]:
            return SecretKeySelector

        def build(self) -> "SecretKeySelector":
            return SecretKeySelector(**self._attrs)

        def key(self, value: str, /) -> Self:
            "The key of the secret to select from.  Must be a valid secret key."
            return self._set("key", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "Specify whether the Secret or its key must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["SecretKeySelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecretKeySelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecretKeySelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecretKeySelector", Builder]:
        return GenericListBuilder[SecretKeySelector, SecretKeySelector.Builder]()

    key: str
    "The key of the secret to select from.  Must be a valid secret key."
    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "Specify whether the Secret or its key must be defined"


SecretList = ResourceList[Secret]


class SecretProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecretProjection"]:
            return SecretProjection

        def build(self) -> "SecretProjection":
            return SecretProjection(**self._attrs)

        @overload
        def items(
            self, value_or_callback: list[KeyToPath] | None, /
        ) -> "SecretProjection.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[KeyToPath, KeyToPath.Builder]],
                GenericListBuilder[KeyToPath, KeyToPath.Builder] | list[KeyToPath],
            ],
            /,
        ) -> "SecretProjection.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KeyToPath.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "items if unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KeyToPath.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KeyToPath.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "optional field specify whether the Secret or its key must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["SecretProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecretProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecretProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecretProjection", Builder]:
        return GenericListBuilder[SecretProjection, SecretProjection.Builder]()

    items: list[KeyToPath] | None = None
    "items if unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "optional field specify whether the Secret or its key must be defined"


class SecretReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecretReference"]:
            return SecretReference

        def build(self) -> "SecretReference":
            return SecretReference(**self._attrs)

        def name(self, value: str | None, /) -> Self:
            "name is unique within a namespace to reference a secret resource."
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "namespace defines the space within which the secret name must be unique."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["SecretReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecretReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecretReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecretReference", Builder]:
        return GenericListBuilder[SecretReference, SecretReference.Builder]()

    name: str | None = None
    "name is unique within a namespace to reference a secret resource."
    namespace: str | None = None
    "namespace defines the space within which the secret name must be unique."


class SecretVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecretVolumeSource"]:
            return SecretVolumeSource

        def build(self) -> "SecretVolumeSource":
            return SecretVolumeSource(**self._attrs)

        def default_mode(self, value: int | None, /) -> Self:
            "defaultMode is Optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("default_mode", value)

        @overload
        def items(
            self, value_or_callback: list[KeyToPath] | None, /
        ) -> "SecretVolumeSource.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[KeyToPath, KeyToPath.Builder]],
                GenericListBuilder[KeyToPath, KeyToPath.Builder] | list[KeyToPath],
            ],
            /,
        ) -> "SecretVolumeSource.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KeyToPath.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "items If unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KeyToPath.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KeyToPath.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

        def optional(self, value: bool | None, /) -> Self:
            "optional field specify whether the Secret or its keys must be defined"
            return self._set("optional", value)

        def secret_name(self, value: str | None, /) -> Self:
            "secretName is the name of the secret in the pod\u0027s namespace to use. More info: https://kubernetes.io/docs/concepts/storage/volumes#secret"
            return self._set("secret_name", value)

    class BuilderContext(BuilderContextBase["SecretVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecretVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecretVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecretVolumeSource", Builder]:
        return GenericListBuilder[SecretVolumeSource, SecretVolumeSource.Builder]()

    default_mode: Annotated[int | None, Field(alias="defaultMode")] = None
    "defaultMode is Optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    items: list[KeyToPath] | None = None
    "items If unspecified, each key-value pair in the Data field of the referenced Secret will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the Secret, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
    optional: bool | None = None
    "optional field specify whether the Secret or its keys must be defined"
    secret_name: Annotated[str | None, Field(alias="secretName")] = None
    "secretName is the name of the secret in the pod\u0027s namespace to use. More info: https://kubernetes.io/docs/concepts/storage/volumes#secret"


class ServiceAccount(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "serviceaccounts",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServiceAccount"]:
            return ServiceAccount

        def build(self) -> "ServiceAccount":
            return ServiceAccount(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def automount_service_account_token(self, value: bool | None, /) -> Self:
            "AutomountServiceAccountToken indicates whether pods running as this service account should have an API token automatically mounted. Can be overridden at the pod level."
            return self._set("automount_service_account_token", value)

        @overload
        def image_pull_secrets(
            self, value_or_callback: list[LocalObjectReference] | None, /
        ) -> "ServiceAccount.Builder": ...

        @overload
        def image_pull_secrets(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[LocalObjectReference, LocalObjectReference.Builder]],
                GenericListBuilder[LocalObjectReference, LocalObjectReference.Builder]
                | list[LocalObjectReference],
            ],
            /,
        ) -> "ServiceAccount.Builder": ...

        @overload
        def image_pull_secrets(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[LocalObjectReference.Builder]: ...

        def image_pull_secrets(self, value_or_callback=UNSET, /):
            "ImagePullSecrets is a list of references to secrets in the same namespace to use for pulling any images in pods that reference this ServiceAccount. ImagePullSecrets are distinct from Secrets because Secrets can be mounted in the pod, but ImagePullSecrets are only accessed by the kubelet. More info: https://kubernetes.io/docs/concepts/containers/images/#specifying-imagepullsecrets-on-a-pod"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[LocalObjectReference.Builder]()
                context._parent_builder = self
                context._field_name = "image_pull_secrets"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("image_pull_secrets", value)

        def kind(self, value: Literal["ServiceAccount"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "ServiceAccount.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ServiceAccount.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def secrets(
            self, value_or_callback: list[ObjectReference] | None, /
        ) -> "ServiceAccount.Builder": ...

        @overload
        def secrets(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ObjectReference, ObjectReference.Builder]],
                GenericListBuilder[ObjectReference, ObjectReference.Builder]
                | list[ObjectReference],
            ],
            /,
        ) -> "ServiceAccount.Builder": ...

        @overload
        def secrets(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ObjectReference.Builder]: ...

        def secrets(self, value_or_callback=UNSET, /):
            'Secrets is a list of the secrets in the same namespace that pods running using this ServiceAccount are allowed to use. Pods are only limited to this list if this service account has a "kubernetes.io/enforce-mountable-secrets" annotation set to "true". The "kubernetes.io/enforce-mountable-secrets" annotation is deprecated since v1.32. Prefer separate namespaces to isolate access to mounted secrets. This field should not be used to find auto-generated service account token secrets for use outside of pods. Instead, tokens can be requested directly using the TokenRequest API, or service account token secrets can be manually created. More info: https://kubernetes.io/docs/concepts/configuration/secret'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ObjectReference.Builder]()
                context._parent_builder = self
                context._field_name = "secrets"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("secrets", value)

    class BuilderContext(BuilderContextBase["ServiceAccount.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServiceAccount.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServiceAccount."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServiceAccount", Builder]:
        return GenericListBuilder[ServiceAccount, ServiceAccount.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    automount_service_account_token: Annotated[
        bool | None, Field(alias="automountServiceAccountToken")
    ] = None
    "AutomountServiceAccountToken indicates whether pods running as this service account should have an API token automatically mounted. Can be overridden at the pod level."
    image_pull_secrets: Annotated[
        list[LocalObjectReference] | None, Field(alias="imagePullSecrets")
    ] = None
    "ImagePullSecrets is a list of references to secrets in the same namespace to use for pulling any images in pods that reference this ServiceAccount. ImagePullSecrets are distinct from Secrets because Secrets can be mounted in the pod, but ImagePullSecrets are only accessed by the kubelet. More info: https://kubernetes.io/docs/concepts/containers/images/#specifying-imagepullsecrets-on-a-pod"
    kind: Literal["ServiceAccount"] = "ServiceAccount"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    secrets: list[ObjectReference] | None = None
    'Secrets is a list of the secrets in the same namespace that pods running using this ServiceAccount are allowed to use. Pods are only limited to this list if this service account has a "kubernetes.io/enforce-mountable-secrets" annotation set to "true". The "kubernetes.io/enforce-mountable-secrets" annotation is deprecated since v1.32. Prefer separate namespaces to isolate access to mounted secrets. This field should not be used to find auto-generated service account token secrets for use outside of pods. Instead, tokens can be requested directly using the TokenRequest API, or service account token secrets can be manually created. More info: https://kubernetes.io/docs/concepts/configuration/secret'


ServiceAccountList = ResourceList[ServiceAccount]


class ServiceAccountTokenProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServiceAccountTokenProjection"]:
            return ServiceAccountTokenProjection

        def build(self) -> "ServiceAccountTokenProjection":
            return ServiceAccountTokenProjection(**self._attrs)

        def audience(self, value: str | None, /) -> Self:
            "audience is the intended audience of the token. A recipient of a token must identify itself with an identifier specified in the audience of the token, and otherwise should reject the token. The audience defaults to the identifier of the apiserver."
            return self._set("audience", value)

        def expiration_seconds(self, value: int | None, /) -> Self:
            "expirationSeconds is the requested duration of validity of the service account token. As the token approaches expiration, the kubelet volume plugin will proactively rotate the service account token. The kubelet will start trying to rotate the token if the token is older than 80 percent of its time to live or if the token is older than 24 hours.Defaults to 1 hour and must be at least 10 minutes."
            return self._set("expiration_seconds", value)

        def path(self, value: str, /) -> Self:
            "path is the path relative to the mount point of the file to project the token into."
            return self._set("path", value)

    class BuilderContext(BuilderContextBase["ServiceAccountTokenProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServiceAccountTokenProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServiceAccountTokenProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ServiceAccountTokenProjection", Builder]:
        return GenericListBuilder[
            ServiceAccountTokenProjection, ServiceAccountTokenProjection.Builder
        ]()

    audience: str | None = None
    "audience is the intended audience of the token. A recipient of a token must identify itself with an identifier specified in the audience of the token, and otherwise should reject the token. The audience defaults to the identifier of the apiserver."
    expiration_seconds: Annotated[int | None, Field(alias="expirationSeconds")] = None
    "expirationSeconds is the requested duration of validity of the service account token. As the token approaches expiration, the kubelet volume plugin will proactively rotate the service account token. The kubelet will start trying to rotate the token if the token is older than 80 percent of its time to live or if the token is older than 24 hours.Defaults to 1 hour and must be at least 10 minutes."
    path: str
    "path is the path relative to the mount point of the file to project the token into."


class ServicePort(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServicePort"]:
            return ServicePort

        def build(self) -> "ServicePort":
            return ServicePort(**self._attrs)

        def app_protocol(self, value: str | None, /) -> Self:
            "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
            return self._set("app_protocol", value)

        def name(self, value: str | None, /) -> Self:
            "The name of this port within the service. This must be a DNS_LABEL. All ports within a ServiceSpec must have unique names. When considering the endpoints for a Service, this must match the \u0027name\u0027 field in the EndpointPort. Optional if only one ServicePort is defined on this service."
            return self._set("name", value)

        def node_port(self, value: int | None, /) -> Self:
            "The port on each node on which this service is exposed when type is NodePort or LoadBalancer.  Usually assigned by the system. If a value is specified, in-range, and not in use it will be used, otherwise the operation will fail.  If not specified, a port will be allocated if this Service requires one.  If this field is specified when creating a Service which does not need it, creation will fail. This field will be wiped when updating a Service to no longer need it (e.g. changing type from NodePort to ClusterIP). More info: https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport"
            return self._set("node_port", value)

        def port(self, value: int, /) -> Self:
            "The port that will be exposed by this service."
            return self._set("port", value)

        def protocol(self, value: str | None, /) -> Self:
            'The IP protocol for this port. Supports "TCP", "UDP", and "SCTP". Default is TCP.'
            return self._set("protocol", value)

        @overload
        def target_port(
            self, value_or_callback: IntOrString | None, /
        ) -> "ServicePort.Builder": ...

        @overload
        def target_port(
            self,
            value_or_callback: Callable[[IntOrString.Builder], IntOrString.Builder | IntOrString],
            /,
        ) -> "ServicePort.Builder": ...

        @overload
        def target_port(self, value_or_callback: Never = ...) -> "IntOrString.BuilderContext": ...

        def target_port(self, value_or_callback=UNSET, /):
            "Number or name of the port to access on the pods targeted by the service. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME. If this is a string, it will be looked up as a named port in the target Pod\u0027s container ports. If this is not specified, the value of the \u0027port\u0027 field is used (an identity map). This field is ignored for services with clusterIP=None, and should be omitted or set equal to the \u0027port\u0027 field. More info: https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service"
            if self._in_context and value_or_callback is UNSET:
                context = IntOrString.BuilderContext()
                context._parent_builder = self
                context._field_name = "target_port"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IntOrString.builder())
                if isinstance(output, IntOrString.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target_port", value)

    class BuilderContext(BuilderContextBase["ServicePort.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServicePort.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServicePort."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServicePort", Builder]:
        return GenericListBuilder[ServicePort, ServicePort.Builder]()

    app_protocol: Annotated[str | None, Field(alias="appProtocol")] = None
    "The application protocol for this port. This is used as a hint for implementations to offer richer behavior for protocols that they understand. This field follows standard Kubernetes label syntax. Valid values are either:\n\n* Un-prefixed protocol names - reserved for IANA standard service names (as per RFC-6335 and https://www.iana.org/assignments/service-names).\n\n* Kubernetes-defined prefixed names:\n  * \u0027kubernetes.io/h2c\u0027 - HTTP/2 prior knowledge over cleartext as described in https://www.rfc-editor.org/rfc/rfc9113.html#name-starting-http-2-with-prior-\n  * \u0027kubernetes.io/ws\u0027  - WebSocket over cleartext as described in https://www.rfc-editor.org/rfc/rfc6455\n  * \u0027kubernetes.io/wss\u0027 - WebSocket over TLS as described in https://www.rfc-editor.org/rfc/rfc6455\n\n* Other protocols should use implementation-defined prefixed names such as mycompany.com/my-custom-protocol."
    name: str | None = None
    "The name of this port within the service. This must be a DNS_LABEL. All ports within a ServiceSpec must have unique names. When considering the endpoints for a Service, this must match the \u0027name\u0027 field in the EndpointPort. Optional if only one ServicePort is defined on this service."
    node_port: Annotated[int | None, Field(alias="nodePort")] = None
    "The port on each node on which this service is exposed when type is NodePort or LoadBalancer.  Usually assigned by the system. If a value is specified, in-range, and not in use it will be used, otherwise the operation will fail.  If not specified, a port will be allocated if this Service requires one.  If this field is specified when creating a Service which does not need it, creation will fail. This field will be wiped when updating a Service to no longer need it (e.g. changing type from NodePort to ClusterIP). More info: https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport"
    port: int
    "The port that will be exposed by this service."
    protocol: str | None = None
    'The IP protocol for this port. Supports "TCP", "UDP", and "SCTP". Default is TCP.'
    target_port: Annotated[IntOrString | None, Field(alias="targetPort")] = None
    "Number or name of the port to access on the pods targeted by the service. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME. If this is a string, it will be looked up as a named port in the target Pod\u0027s container ports. If this is not specified, the value of the \u0027port\u0027 field is used (an identity map). This field is ignored for services with clusterIP=None, and should be omitted or set equal to the \u0027port\u0027 field. More info: https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service"


class SessionAffinityConfig(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SessionAffinityConfig"]:
            return SessionAffinityConfig

        def build(self) -> "SessionAffinityConfig":
            return SessionAffinityConfig(**self._attrs)

        @overload
        def client_ip(
            self, value_or_callback: ClientIPConfig | None, /
        ) -> "SessionAffinityConfig.Builder": ...

        @overload
        def client_ip(
            self,
            value_or_callback: Callable[
                [ClientIPConfig.Builder], ClientIPConfig.Builder | ClientIPConfig
            ],
            /,
        ) -> "SessionAffinityConfig.Builder": ...

        @overload
        def client_ip(self, value_or_callback: Never = ...) -> "ClientIPConfig.BuilderContext": ...

        def client_ip(self, value_or_callback=UNSET, /):
            "clientIP contains the configurations of Client IP based session affinity."
            if self._in_context and value_or_callback is UNSET:
                context = ClientIPConfig.BuilderContext()
                context._parent_builder = self
                context._field_name = "client_ip"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ClientIPConfig.builder())
                if isinstance(output, ClientIPConfig.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("client_ip", value)

    class BuilderContext(BuilderContextBase["SessionAffinityConfig.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SessionAffinityConfig.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SessionAffinityConfig."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SessionAffinityConfig", Builder]:
        return GenericListBuilder[SessionAffinityConfig, SessionAffinityConfig.Builder]()

    client_ip: Annotated[ClientIPConfig | None, Field(alias="clientIP")] = None
    "clientIP contains the configurations of Client IP based session affinity."


class SleepAction(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SleepAction"]:
            return SleepAction

        def build(self) -> "SleepAction":
            return SleepAction(**self._attrs)

        def seconds(self, value: int, /) -> Self:
            "Seconds is the number of seconds to sleep."
            return self._set("seconds", value)

    class BuilderContext(BuilderContextBase["SleepAction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SleepAction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SleepAction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SleepAction", Builder]:
        return GenericListBuilder[SleepAction, SleepAction.Builder]()

    seconds: int
    "Seconds is the number of seconds to sleep."


class StorageOSPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageOSPersistentVolumeSource"]:
            return StorageOSPersistentVolumeSource

        def build(self) -> "StorageOSPersistentVolumeSource":
            return StorageOSPersistentVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: ObjectReference | None, /
        ) -> "StorageOSPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "StorageOSPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "ObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef specifies the secret to use for obtaining the StorageOS API credentials.  If not specified, default values will be attempted."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def volume_name(self, value: str | None, /) -> Self:
            "volumeName is the human-readable name of the StorageOS volume.  Volume names are only unique within a namespace."
            return self._set("volume_name", value)

        def volume_namespace(self, value: str | None, /) -> Self:
            'volumeNamespace specifies the scope of the volume within StorageOS.  If no namespace is specified then the Pod\u0027s namespace will be used.  This allows the Kubernetes name scoping to be mirrored within StorageOS for tighter integration. Set VolumeName to any name to override the default behaviour. Set to "default" if you are not using namespaces within StorageOS. Namespaces that do not pre-exist within StorageOS will be created.'
            return self._set("volume_namespace", value)

    class BuilderContext(BuilderContextBase["StorageOSPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageOSPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageOSPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["StorageOSPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            StorageOSPersistentVolumeSource, StorageOSPersistentVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[ObjectReference | None, Field(alias="secretRef")] = None
    "secretRef specifies the secret to use for obtaining the StorageOS API credentials.  If not specified, default values will be attempted."
    volume_name: Annotated[str | None, Field(alias="volumeName")] = None
    "volumeName is the human-readable name of the StorageOS volume.  Volume names are only unique within a namespace."
    volume_namespace: Annotated[str | None, Field(alias="volumeNamespace")] = None
    'volumeNamespace specifies the scope of the volume within StorageOS.  If no namespace is specified then the Pod\u0027s namespace will be used.  This allows the Kubernetes name scoping to be mirrored within StorageOS for tighter integration. Set VolumeName to any name to override the default behaviour. Set to "default" if you are not using namespaces within StorageOS. Namespaces that do not pre-exist within StorageOS will be created.'


class StorageOSVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["StorageOSVolumeSource"]:
            return StorageOSVolumeSource

        def build(self) -> "StorageOSVolumeSource":
            return StorageOSVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "StorageOSVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "StorageOSVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef specifies the secret to use for obtaining the StorageOS API credentials.  If not specified, default values will be attempted."
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def volume_name(self, value: str | None, /) -> Self:
            "volumeName is the human-readable name of the StorageOS volume.  Volume names are only unique within a namespace."
            return self._set("volume_name", value)

        def volume_namespace(self, value: str | None, /) -> Self:
            'volumeNamespace specifies the scope of the volume within StorageOS.  If no namespace is specified then the Pod\u0027s namespace will be used.  This allows the Kubernetes name scoping to be mirrored within StorageOS for tighter integration. Set VolumeName to any name to override the default behaviour. Set to "default" if you are not using namespaces within StorageOS. Namespaces that do not pre-exist within StorageOS will be created.'
            return self._set("volume_namespace", value)

    class BuilderContext(BuilderContextBase["StorageOSVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = StorageOSVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for StorageOSVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["StorageOSVolumeSource", Builder]:
        return GenericListBuilder[StorageOSVolumeSource, StorageOSVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef specifies the secret to use for obtaining the StorageOS API credentials.  If not specified, default values will be attempted."
    volume_name: Annotated[str | None, Field(alias="volumeName")] = None
    "volumeName is the human-readable name of the StorageOS volume.  Volume names are only unique within a namespace."
    volume_namespace: Annotated[str | None, Field(alias="volumeNamespace")] = None
    'volumeNamespace specifies the scope of the volume within StorageOS.  If no namespace is specified then the Pod\u0027s namespace will be used.  This allows the Kubernetes name scoping to be mirrored within StorageOS for tighter integration. Set VolumeName to any name to override the default behaviour. Set to "default" if you are not using namespaces within StorageOS. Namespaces that do not pre-exist within StorageOS will be created.'


class Sysctl(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Sysctl"]:
            return Sysctl

        def build(self) -> "Sysctl":
            return Sysctl(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name of a property to set"
            return self._set("name", value)

        def value(self, value: str, /) -> Self:
            "Value of a property to set"
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["Sysctl.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Sysctl.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Sysctl."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Sysctl", Builder]:
        return GenericListBuilder[Sysctl, Sysctl.Builder]()

    name: str
    "Name of a property to set"
    value: str
    "Value of a property to set"


class TCPSocketAction(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TCPSocketAction"]:
            return TCPSocketAction

        def build(self) -> "TCPSocketAction":
            return TCPSocketAction(**self._attrs)

        def host(self, value: str | None, /) -> Self:
            "Optional: Host name to connect to, defaults to the pod IP."
            return self._set("host", value)

        @overload
        def port(self, value_or_callback: IntOrString, /) -> "TCPSocketAction.Builder": ...

        @overload
        def port(
            self,
            value_or_callback: Callable[[IntOrString.Builder], IntOrString.Builder | IntOrString],
            /,
        ) -> "TCPSocketAction.Builder": ...

        @overload
        def port(self, value_or_callback: Never = ...) -> "IntOrString.BuilderContext": ...

        def port(self, value_or_callback=UNSET, /):
            "Number or name of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME."
            if self._in_context and value_or_callback is UNSET:
                context = IntOrString.BuilderContext()
                context._parent_builder = self
                context._field_name = "port"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IntOrString.builder())
                if isinstance(output, IntOrString.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("port", value)

    class BuilderContext(BuilderContextBase["TCPSocketAction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TCPSocketAction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TCPSocketAction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TCPSocketAction", Builder]:
        return GenericListBuilder[TCPSocketAction, TCPSocketAction.Builder]()

    host: str | None = None
    "Optional: Host name to connect to, defaults to the pod IP."
    port: IntOrString
    "Number or name of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME."


class Taint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Taint"]:
            return Taint

        def build(self) -> "Taint":
            return Taint(**self._attrs)

        def effect(self, value: str, /) -> Self:
            "Required. The effect of the taint on pods that do not tolerate the taint. Valid effects are NoSchedule, PreferNoSchedule and NoExecute."
            return self._set("effect", value)

        def key(self, value: str, /) -> Self:
            "Required. The taint key to be applied to a node."
            return self._set("key", value)

        @overload
        def time_added(self, value_or_callback: Time | None, /) -> "Taint.Builder": ...

        @overload
        def time_added(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "Taint.Builder": ...

        @overload
        def time_added(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def time_added(self, value_or_callback=UNSET, /):
            "TimeAdded represents the time at which the taint was added."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "time_added"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("time_added", value)

        def value(self, value: str | None, /) -> Self:
            "The taint value corresponding to the taint key."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["Taint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Taint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Taint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Taint", Builder]:
        return GenericListBuilder[Taint, Taint.Builder]()

    effect: str
    "Required. The effect of the taint on pods that do not tolerate the taint. Valid effects are NoSchedule, PreferNoSchedule and NoExecute."
    key: str
    "Required. The taint key to be applied to a node."
    time_added: Annotated[Time | None, Field(alias="timeAdded")] = None
    "TimeAdded represents the time at which the taint was added."
    value: str | None = None
    "The taint value corresponding to the taint key."


class Toleration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Toleration"]:
            return Toleration

        def build(self) -> "Toleration":
            return Toleration(**self._attrs)

        def effect(self, value: str | None, /) -> Self:
            "Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute."
            return self._set("effect", value)

        def key(self, value: str | None, /) -> Self:
            "Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys."
            return self._set("key", value)

        def operator(self, value: str | None, /) -> Self:
            "Operator represents a key\u0027s relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category."
            return self._set("operator", value)

        def toleration_seconds(self, value: int | None, /) -> Self:
            "TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system."
            return self._set("toleration_seconds", value)

        def value(self, value: str | None, /) -> Self:
            "Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["Toleration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Toleration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Toleration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Toleration", Builder]:
        return GenericListBuilder[Toleration, Toleration.Builder]()

    effect: str | None = None
    "Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute."
    key: str | None = None
    "Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys."
    operator: str | None = None
    "Operator represents a key\u0027s relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category."
    toleration_seconds: Annotated[int | None, Field(alias="tolerationSeconds")] = None
    "TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system."
    value: str | None = None
    "Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string."


class TopologySelectorLabelRequirement(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TopologySelectorLabelRequirement"]:
            return TopologySelectorLabelRequirement

        def build(self) -> "TopologySelectorLabelRequirement":
            return TopologySelectorLabelRequirement(**self._attrs)

        def key(self, value: str, /) -> Self:
            "The label key that the selector applies to."
            return self._set("key", value)

        def values(self, value: list[str], /) -> Self:
            "An array of string values. One value must match the label to be selected. Each entry in Values is ORed."
            return self._set("values", value)

    class BuilderContext(BuilderContextBase["TopologySelectorLabelRequirement.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TopologySelectorLabelRequirement.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TopologySelectorLabelRequirement."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["TopologySelectorLabelRequirement", Builder]:
        return GenericListBuilder[
            TopologySelectorLabelRequirement, TopologySelectorLabelRequirement.Builder
        ]()

    key: str
    "The label key that the selector applies to."
    values: list[str]
    "An array of string values. One value must match the label to be selected. Each entry in Values is ORed."


class TopologySelectorTerm(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TopologySelectorTerm"]:
            return TopologySelectorTerm

        def build(self) -> "TopologySelectorTerm":
            return TopologySelectorTerm(**self._attrs)

        @overload
        def match_label_expressions(
            self, value_or_callback: list[TopologySelectorLabelRequirement] | None, /
        ) -> "TopologySelectorTerm.Builder": ...

        @overload
        def match_label_expressions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        TopologySelectorLabelRequirement,
                        TopologySelectorLabelRequirement.Builder,
                    ]
                ],
                GenericListBuilder[
                    TopologySelectorLabelRequirement,
                    TopologySelectorLabelRequirement.Builder,
                ]
                | list[TopologySelectorLabelRequirement],
            ],
            /,
        ) -> "TopologySelectorTerm.Builder": ...

        @overload
        def match_label_expressions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TopologySelectorLabelRequirement.Builder]: ...

        def match_label_expressions(self, value_or_callback=UNSET, /):
            "A list of topology selector requirements by labels."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TopologySelectorLabelRequirement.Builder]()
                context._parent_builder = self
                context._field_name = "match_label_expressions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TopologySelectorLabelRequirement.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("match_label_expressions", value)

    class BuilderContext(BuilderContextBase["TopologySelectorTerm.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TopologySelectorTerm.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TopologySelectorTerm."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TopologySelectorTerm", Builder]:
        return GenericListBuilder[TopologySelectorTerm, TopologySelectorTerm.Builder]()

    match_label_expressions: Annotated[
        list[TopologySelectorLabelRequirement] | None,
        Field(alias="matchLabelExpressions"),
    ] = None
    "A list of topology selector requirements by labels."


class TopologySpreadConstraint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TopologySpreadConstraint"]:
            return TopologySpreadConstraint

        def build(self) -> "TopologySpreadConstraint":
            return TopologySpreadConstraint(**self._attrs)

        @overload
        def label_selector(
            self, value_or_callback: LabelSelector | None, /
        ) -> "TopologySpreadConstraint.Builder": ...

        @overload
        def label_selector(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "TopologySpreadConstraint.Builder": ...

        @overload
        def label_selector(
            self, value_or_callback: Never = ...
        ) -> "LabelSelector.BuilderContext": ...

        def label_selector(self, value_or_callback=UNSET, /):
            "LabelSelector is used to find matching pods. Pods that match this label selector are counted to determine the number of pods in their corresponding topology domain."
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "label_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("label_selector", value)

        def match_label_keys(self, value: list[str] | None, /) -> Self:
            "MatchLabelKeys is a set of pod label keys to select the pods over which spreading will be calculated. The keys are used to lookup values from the incoming pod labels, those key-value labels are ANDed with labelSelector to select the group of existing pods over which spreading will be calculated for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector. MatchLabelKeys cannot be set when LabelSelector isn\u0027t set. Keys that don\u0027t exist in the incoming pod labels will be ignored. A null or empty list means only match against labelSelector.\n\nThis is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default)."
            return self._set("match_label_keys", value)

        def max_skew(self, value: int, /) -> Self:
            "MaxSkew describes the degree to which pods may be unevenly distributed. When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference between the number of matching pods in the target topology and the global minimum. The global minimum is the minimum number of matching pods in an eligible domain or zero if the number of eligible domains is less than MinDomains. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 2/2/1: In this case, the global minimum is 1. | zone1 | zone2 | zone3 | |  P P  |  P P  |   P   | - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2; scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2) violate MaxSkew(1). - if MaxSkew is 2, incoming pod can be scheduled onto any zone. When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence to topologies that satisfy it. It\u0027s a required field. Default value is 1 and 0 is not allowed."
            return self._set("max_skew", value)

        def min_domains(self, value: int | None, /) -> Self:
            'MinDomains indicates a minimum number of eligible domains. When the number of eligible domains with matching topology keys is less than minDomains, Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed. And when the number of eligible domains with matching topology keys equals or greater than minDomains, this value has no effect on scheduling. As a result, when the number of eligible domains is less than minDomains, scheduler won\u0027t schedule more than maxSkew Pods to those domains. If value is nil, the constraint behaves as if MinDomains is equal to 1. Valid values are integers greater than 0. When value is not nil, WhenUnsatisfiable must be DoNotSchedule.\n\nFor example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same labelSelector spread as 2/2/2: | zone1 | zone2 | zone3 | |  P P  |  P P  |  P P  | The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0. In this situation, new pod with the same labelSelector cannot be scheduled, because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones, it will violate MaxSkew.'
            return self._set("min_domains", value)

        def node_affinity_policy(self, value: str | None, /) -> Self:
            "NodeAffinityPolicy indicates how we will treat Pod\u0027s nodeAffinity/nodeSelector when calculating pod topology spread skew. Options are: - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations. - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations.\n\nIf this value is nil, the behavior is equivalent to the Honor policy."
            return self._set("node_affinity_policy", value)

        def node_taints_policy(self, value: str | None, /) -> Self:
            "NodeTaintsPolicy indicates how we will treat node taints when calculating pod topology spread skew. Options are: - Honor: nodes without taints, along with tainted nodes for which the incoming pod has a toleration, are included. - Ignore: node taints are ignored. All nodes are included.\n\nIf this value is nil, the behavior is equivalent to the Ignore policy."
            return self._set("node_taints_policy", value)

        def topology_key(self, value: str, /) -> Self:
            'TopologyKey is the key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology. We consider each \u003ckey, value\u003e as a "bucket", and try to put balanced number of pods into each bucket. We define a domain as a particular instance of a topology. Also, we define an eligible domain as a domain whose nodes meet the requirements of nodeAffinityPolicy and nodeTaintsPolicy. e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology. And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology. It\u0027s a required field.'
            return self._set("topology_key", value)

        def when_unsatisfiable(self, value: str, /) -> Self:
            'WhenUnsatisfiable indicates how to deal with a pod if it doesn\u0027t satisfy the spread constraint. - DoNotSchedule (default) tells the scheduler not to schedule it. - ScheduleAnyway tells the scheduler to schedule the pod in any location,\n  but giving higher precedence to topologies that would help reduce the\n  skew.\nA constraint is considered "Unsatisfiable" for an incoming pod if and only if every possible node assignment for that pod would violate "MaxSkew" on some topology. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 3/1/1: | zone1 | zone2 | zone3 | | P P P |   P   |   P   | If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler won\u0027t make it *more* imbalanced. It\u0027s a required field.'
            return self._set("when_unsatisfiable", value)

    class BuilderContext(BuilderContextBase["TopologySpreadConstraint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TopologySpreadConstraint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TopologySpreadConstraint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TopologySpreadConstraint", Builder]:
        return GenericListBuilder[TopologySpreadConstraint, TopologySpreadConstraint.Builder]()

    label_selector: Annotated[LabelSelector | None, Field(alias="labelSelector")] = None
    "LabelSelector is used to find matching pods. Pods that match this label selector are counted to determine the number of pods in their corresponding topology domain."
    match_label_keys: Annotated[list[str] | None, Field(alias="matchLabelKeys")] = None
    "MatchLabelKeys is a set of pod label keys to select the pods over which spreading will be calculated. The keys are used to lookup values from the incoming pod labels, those key-value labels are ANDed with labelSelector to select the group of existing pods over which spreading will be calculated for the incoming pod. The same key is forbidden to exist in both MatchLabelKeys and LabelSelector. MatchLabelKeys cannot be set when LabelSelector isn\u0027t set. Keys that don\u0027t exist in the incoming pod labels will be ignored. A null or empty list means only match against labelSelector.\n\nThis is a beta field and requires the MatchLabelKeysInPodTopologySpread feature gate to be enabled (enabled by default)."
    max_skew: Annotated[int, Field(alias="maxSkew")]
    "MaxSkew describes the degree to which pods may be unevenly distributed. When `whenUnsatisfiable=DoNotSchedule`, it is the maximum permitted difference between the number of matching pods in the target topology and the global minimum. The global minimum is the minimum number of matching pods in an eligible domain or zero if the number of eligible domains is less than MinDomains. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 2/2/1: In this case, the global minimum is 1. | zone1 | zone2 | zone3 | |  P P  |  P P  |   P   | - if MaxSkew is 1, incoming pod can only be scheduled to zone3 to become 2/2/2; scheduling it onto zone1(zone2) would make the ActualSkew(3-1) on zone1(zone2) violate MaxSkew(1). - if MaxSkew is 2, incoming pod can be scheduled onto any zone. When `whenUnsatisfiable=ScheduleAnyway`, it is used to give higher precedence to topologies that satisfy it. It\u0027s a required field. Default value is 1 and 0 is not allowed."
    min_domains: Annotated[int | None, Field(alias="minDomains")] = None
    'MinDomains indicates a minimum number of eligible domains. When the number of eligible domains with matching topology keys is less than minDomains, Pod Topology Spread treats "global minimum" as 0, and then the calculation of Skew is performed. And when the number of eligible domains with matching topology keys equals or greater than minDomains, this value has no effect on scheduling. As a result, when the number of eligible domains is less than minDomains, scheduler won\u0027t schedule more than maxSkew Pods to those domains. If value is nil, the constraint behaves as if MinDomains is equal to 1. Valid values are integers greater than 0. When value is not nil, WhenUnsatisfiable must be DoNotSchedule.\n\nFor example, in a 3-zone cluster, MaxSkew is set to 2, MinDomains is set to 5 and pods with the same labelSelector spread as 2/2/2: | zone1 | zone2 | zone3 | |  P P  |  P P  |  P P  | The number of domains is less than 5(MinDomains), so "global minimum" is treated as 0. In this situation, new pod with the same labelSelector cannot be scheduled, because computed skew will be 3(3 - 0) if new Pod is scheduled to any of the three zones, it will violate MaxSkew.'
    node_affinity_policy: Annotated[str | None, Field(alias="nodeAffinityPolicy")] = None
    "NodeAffinityPolicy indicates how we will treat Pod\u0027s nodeAffinity/nodeSelector when calculating pod topology spread skew. Options are: - Honor: only nodes matching nodeAffinity/nodeSelector are included in the calculations. - Ignore: nodeAffinity/nodeSelector are ignored. All nodes are included in the calculations.\n\nIf this value is nil, the behavior is equivalent to the Honor policy."
    node_taints_policy: Annotated[str | None, Field(alias="nodeTaintsPolicy")] = None
    "NodeTaintsPolicy indicates how we will treat node taints when calculating pod topology spread skew. Options are: - Honor: nodes without taints, along with tainted nodes for which the incoming pod has a toleration, are included. - Ignore: node taints are ignored. All nodes are included.\n\nIf this value is nil, the behavior is equivalent to the Ignore policy."
    topology_key: Annotated[str, Field(alias="topologyKey")]
    'TopologyKey is the key of node labels. Nodes that have a label with this key and identical values are considered to be in the same topology. We consider each \u003ckey, value\u003e as a "bucket", and try to put balanced number of pods into each bucket. We define a domain as a particular instance of a topology. Also, we define an eligible domain as a domain whose nodes meet the requirements of nodeAffinityPolicy and nodeTaintsPolicy. e.g. If TopologyKey is "kubernetes.io/hostname", each Node is a domain of that topology. And, if TopologyKey is "topology.kubernetes.io/zone", each zone is a domain of that topology. It\u0027s a required field.'
    when_unsatisfiable: Annotated[str, Field(alias="whenUnsatisfiable")]
    'WhenUnsatisfiable indicates how to deal with a pod if it doesn\u0027t satisfy the spread constraint. - DoNotSchedule (default) tells the scheduler not to schedule it. - ScheduleAnyway tells the scheduler to schedule the pod in any location,\n  but giving higher precedence to topologies that would help reduce the\n  skew.\nA constraint is considered "Unsatisfiable" for an incoming pod if and only if every possible node assignment for that pod would violate "MaxSkew" on some topology. For example, in a 3-zone cluster, MaxSkew is set to 1, and pods with the same labelSelector spread as 3/1/1: | zone1 | zone2 | zone3 | | P P P |   P   |   P   | If WhenUnsatisfiable is set to DoNotSchedule, incoming pod can only be scheduled to zone2(zone3) to become 3/2/1(3/1/2) as ActualSkew(2-1) on zone2(zone3) satisfies MaxSkew(1). In other words, the cluster can still be imbalanced, but scheduler won\u0027t make it *more* imbalanced. It\u0027s a required field.'


class TypedLocalObjectReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TypedLocalObjectReference"]:
            return TypedLocalObjectReference

        def build(self) -> "TypedLocalObjectReference":
            return TypedLocalObjectReference(**self._attrs)

        def api_group(self, value: str | None, /) -> Self:
            "APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required."
            return self._set("api_group", value)

        def kind(self, value: str, /) -> Self:
            "Kind is the type of resource being referenced"
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "Name is the name of resource being referenced"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["TypedLocalObjectReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TypedLocalObjectReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TypedLocalObjectReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TypedLocalObjectReference", Builder]:
        return GenericListBuilder[TypedLocalObjectReference, TypedLocalObjectReference.Builder]()

    api_group: Annotated[str | None, Field(alias="apiGroup")] = None
    "APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required."
    kind: str
    "Kind is the type of resource being referenced"
    name: str
    "Name is the name of resource being referenced"


class TypedObjectReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TypedObjectReference"]:
            return TypedObjectReference

        def build(self) -> "TypedObjectReference":
            return TypedObjectReference(**self._attrs)

        def api_group(self, value: str | None, /) -> Self:
            "APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required."
            return self._set("api_group", value)

        def kind(self, value: str, /) -> Self:
            "Kind is the type of resource being referenced"
            return self._set("kind", value)

        def name(self, value: str, /) -> Self:
            "Name is the name of resource being referenced"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace is the namespace of resource being referenced Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace\u0027s owner to accept the reference. See the ReferenceGrant documentation for details. (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["TypedObjectReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TypedObjectReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TypedObjectReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TypedObjectReference", Builder]:
        return GenericListBuilder[TypedObjectReference, TypedObjectReference.Builder]()

    api_group: Annotated[str | None, Field(alias="apiGroup")] = None
    "APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required."
    kind: str
    "Kind is the type of resource being referenced"
    name: str
    "Name is the name of resource being referenced"
    namespace: str | None = None
    "Namespace is the namespace of resource being referenced Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace\u0027s owner to accept the reference. See the ReferenceGrant documentation for details. (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled."


class VolumeDevice(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeDevice"]:
            return VolumeDevice

        def build(self) -> "VolumeDevice":
            return VolumeDevice(**self._attrs)

        def device_path(self, value: str, /) -> Self:
            "devicePath is the path inside of the container that the device will be mapped to."
            return self._set("device_path", value)

        def name(self, value: str, /) -> Self:
            "name must match the name of a persistentVolumeClaim in the pod"
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["VolumeDevice.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeDevice.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeDevice."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeDevice", Builder]:
        return GenericListBuilder[VolumeDevice, VolumeDevice.Builder]()

    device_path: Annotated[str, Field(alias="devicePath")]
    "devicePath is the path inside of the container that the device will be mapped to."
    name: str
    "name must match the name of a persistentVolumeClaim in the pod"


class VolumeMount(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeMount"]:
            return VolumeMount

        def build(self) -> "VolumeMount":
            return VolumeMount(**self._attrs)

        def mount_path(self, value: str, /) -> Self:
            "Path within the container at which the volume should be mounted.  Must not contain \u0027:\u0027."
            return self._set("mount_path", value)

        def mount_propagation(self, value: str | None, /) -> Self:
            "mountPropagation determines how mounts are propagated from the host to container and the other way around. When not set, MountPropagationNone is used. This field is beta in 1.10. When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified (which defaults to None)."
            return self._set("mount_propagation", value)

        def name(self, value: str, /) -> Self:
            "This must match the Name of a Volume."
            return self._set("name", value)

        def read_only(self, value: bool | None, /) -> Self:
            "Mounted read-only if true, read-write otherwise (false or unspecified). Defaults to false."
            return self._set("read_only", value)

        def recursive_read_only(self, value: str | None, /) -> Self:
            "RecursiveReadOnly specifies whether read-only mounts should be handled recursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made recursively read-only.  If this field is set to IfPossible, the mount is made recursively read-only, if it is supported by the container runtime.  If this field is set to Enabled, the mount is made recursively read-only if it is supported by the container runtime, otherwise the pod will not be started and an error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to None (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled."
            return self._set("recursive_read_only", value)

        def sub_path(self, value: str | None, /) -> Self:
            'Path within the volume from which the container\u0027s volume should be mounted. Defaults to "" (volume\u0027s root).'
            return self._set("sub_path", value)

        def sub_path_expr(self, value: str | None, /) -> Self:
            'Expanded path within the volume from which the container\u0027s volume should be mounted. Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container\u0027s environment. Defaults to "" (volume\u0027s root). SubPathExpr and SubPath are mutually exclusive.'
            return self._set("sub_path_expr", value)

    class BuilderContext(BuilderContextBase["VolumeMount.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeMount.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeMount."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeMount", Builder]:
        return GenericListBuilder[VolumeMount, VolumeMount.Builder]()

    mount_path: Annotated[str, Field(alias="mountPath")]
    "Path within the container at which the volume should be mounted.  Must not contain \u0027:\u0027."
    mount_propagation: Annotated[str | None, Field(alias="mountPropagation")] = None
    "mountPropagation determines how mounts are propagated from the host to container and the other way around. When not set, MountPropagationNone is used. This field is beta in 1.10. When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified (which defaults to None)."
    name: str
    "This must match the Name of a Volume."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "Mounted read-only if true, read-write otherwise (false or unspecified). Defaults to false."
    recursive_read_only: Annotated[str | None, Field(alias="recursiveReadOnly")] = None
    "RecursiveReadOnly specifies whether read-only mounts should be handled recursively.\n\nIf ReadOnly is false, this field has no meaning and must be unspecified.\n\nIf ReadOnly is true, and this field is set to Disabled, the mount is not made recursively read-only.  If this field is set to IfPossible, the mount is made recursively read-only, if it is supported by the container runtime.  If this field is set to Enabled, the mount is made recursively read-only if it is supported by the container runtime, otherwise the pod will not be started and an error will be generated to indicate the reason.\n\nIf this field is set to IfPossible or Enabled, MountPropagation must be set to None (or be unspecified, which defaults to None).\n\nIf this field is not specified, it is treated as an equivalent of Disabled."
    sub_path: Annotated[str | None, Field(alias="subPath")] = None
    'Path within the volume from which the container\u0027s volume should be mounted. Defaults to "" (volume\u0027s root).'
    sub_path_expr: Annotated[str | None, Field(alias="subPathExpr")] = None
    'Expanded path within the volume from which the container\u0027s volume should be mounted. Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container\u0027s environment. Defaults to "" (volume\u0027s root). SubPathExpr and SubPath are mutually exclusive.'


class VolumeMountStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeMountStatus"]:
            return VolumeMountStatus

        def build(self) -> "VolumeMountStatus":
            return VolumeMountStatus(**self._attrs)

        def mount_path(self, value: str, /) -> Self:
            "MountPath corresponds to the original VolumeMount."
            return self._set("mount_path", value)

        def name(self, value: str, /) -> Self:
            "Name corresponds to the name of the original VolumeMount."
            return self._set("name", value)

        def read_only(self, value: bool | None, /) -> Self:
            "ReadOnly corresponds to the original VolumeMount."
            return self._set("read_only", value)

        def recursive_read_only(self, value: str | None, /) -> Self:
            "RecursiveReadOnly must be set to Disabled, Enabled, or unspecified (for non-readonly mounts). An IfPossible value in the original VolumeMount must be translated to Disabled or Enabled, depending on the mount result."
            return self._set("recursive_read_only", value)

    class BuilderContext(BuilderContextBase["VolumeMountStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeMountStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeMountStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeMountStatus", Builder]:
        return GenericListBuilder[VolumeMountStatus, VolumeMountStatus.Builder]()

    mount_path: Annotated[str, Field(alias="mountPath")]
    "MountPath corresponds to the original VolumeMount."
    name: str
    "Name corresponds to the name of the original VolumeMount."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "ReadOnly corresponds to the original VolumeMount."
    recursive_read_only: Annotated[str | None, Field(alias="recursiveReadOnly")] = None
    "RecursiveReadOnly must be set to Disabled, Enabled, or unspecified (for non-readonly mounts). An IfPossible value in the original VolumeMount must be translated to Disabled or Enabled, depending on the mount result."


class VolumeResourceRequirements(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeResourceRequirements"]:
            return VolumeResourceRequirements

        def build(self) -> "VolumeResourceRequirements":
            return VolumeResourceRequirements(**self._attrs)

        def limits(self, value: dict[str, Quantity] | None, /) -> Self:
            "Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
            return self._set("limits", value)

        def requests(self, value: dict[str, Quantity] | None, /) -> Self:
            "Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["VolumeResourceRequirements.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeResourceRequirements.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeResourceRequirements."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeResourceRequirements", Builder]:
        return GenericListBuilder[VolumeResourceRequirements, VolumeResourceRequirements.Builder]()

    limits: dict[str, Quantity] | None = None
    "Limits describes the maximum amount of compute resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
    requests: dict[str, Quantity] | None = None
    "Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. Requests cannot exceed Limits. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"


class VsphereVirtualDiskVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VsphereVirtualDiskVolumeSource"]:
            return VsphereVirtualDiskVolumeSource

        def build(self) -> "VsphereVirtualDiskVolumeSource":
            return VsphereVirtualDiskVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
            return self._set("fs_type", value)

        def storage_policy_id(self, value: str | None, /) -> Self:
            "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName."
            return self._set("storage_policy_id", value)

        def storage_policy_name(self, value: str | None, /) -> Self:
            "storagePolicyName is the storage Policy Based Management (SPBM) profile name."
            return self._set("storage_policy_name", value)

        def volume_path(self, value: str, /) -> Self:
            "volumePath is the path that identifies vSphere volume vmdk"
            return self._set("volume_path", value)

    class BuilderContext(BuilderContextBase["VsphereVirtualDiskVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VsphereVirtualDiskVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VsphereVirtualDiskVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["VsphereVirtualDiskVolumeSource", Builder]:
        return GenericListBuilder[
            VsphereVirtualDiskVolumeSource, VsphereVirtualDiskVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.'
    storage_policy_id: Annotated[str | None, Field(alias="storagePolicyID")] = None
    "storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName."
    storage_policy_name: Annotated[str | None, Field(alias="storagePolicyName")] = None
    "storagePolicyName is the storage Policy Based Management (SPBM) profile name."
    volume_path: Annotated[str, Field(alias="volumePath")]
    "volumePath is the path that identifies vSphere volume vmdk"


class WeightedPodAffinityTerm(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WeightedPodAffinityTerm"]:
            return WeightedPodAffinityTerm

        def build(self) -> "WeightedPodAffinityTerm":
            return WeightedPodAffinityTerm(**self._attrs)

        @overload
        def pod_affinity_term(
            self, value_or_callback: PodAffinityTerm, /
        ) -> "WeightedPodAffinityTerm.Builder": ...

        @overload
        def pod_affinity_term(
            self,
            value_or_callback: Callable[
                [PodAffinityTerm.Builder], PodAffinityTerm.Builder | PodAffinityTerm
            ],
            /,
        ) -> "WeightedPodAffinityTerm.Builder": ...

        @overload
        def pod_affinity_term(
            self, value_or_callback: Never = ...
        ) -> "PodAffinityTerm.BuilderContext": ...

        def pod_affinity_term(self, value_or_callback=UNSET, /):
            "Required. A pod affinity term, associated with the corresponding weight."
            if self._in_context and value_or_callback is UNSET:
                context = PodAffinityTerm.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod_affinity_term"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodAffinityTerm.builder())
                if isinstance(output, PodAffinityTerm.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_affinity_term", value)

        def weight(self, value: int, /) -> Self:
            "weight associated with matching the corresponding podAffinityTerm, in the range 1-100."
            return self._set("weight", value)

    class BuilderContext(BuilderContextBase["WeightedPodAffinityTerm.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WeightedPodAffinityTerm.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WeightedPodAffinityTerm."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["WeightedPodAffinityTerm", Builder]:
        return GenericListBuilder[WeightedPodAffinityTerm, WeightedPodAffinityTerm.Builder]()

    pod_affinity_term: Annotated[PodAffinityTerm, Field(alias="podAffinityTerm")]
    "Required. A pod affinity term, associated with the corresponding weight."
    weight: int
    "weight associated with matching the corresponding podAffinityTerm, in the range 1-100."


class WindowsSecurityContextOptions(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["WindowsSecurityContextOptions"]:
            return WindowsSecurityContextOptions

        def build(self) -> "WindowsSecurityContextOptions":
            return WindowsSecurityContextOptions(**self._attrs)

        def gmsa_credential_spec(self, value: str | None, /) -> Self:
            "GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field."
            return self._set("gmsa_credential_spec", value)

        def gmsa_credential_spec_name(self, value: str | None, /) -> Self:
            "GMSACredentialSpecName is the name of the GMSA credential spec to use."
            return self._set("gmsa_credential_spec_name", value)

        def host_process(self, value: bool | None, /) -> Self:
            "HostProcess determines if a container should be run as a \u0027Host Process\u0027 container. All of a Pod\u0027s containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true."
            return self._set("host_process", value)

        def run_as_user_name(self, value: str | None, /) -> Self:
            "The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."
            return self._set("run_as_user_name", value)

    class BuilderContext(BuilderContextBase["WindowsSecurityContextOptions.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = WindowsSecurityContextOptions.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for WindowsSecurityContextOptions."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["WindowsSecurityContextOptions", Builder]:
        return GenericListBuilder[
            WindowsSecurityContextOptions, WindowsSecurityContextOptions.Builder
        ]()

    gmsa_credential_spec: Annotated[str | None, Field(alias="gmsaCredentialSpec")] = None
    "GMSACredentialSpec is where the GMSA admission webhook (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the GMSA credential spec named by the GMSACredentialSpecName field."
    gmsa_credential_spec_name: Annotated[str | None, Field(alias="gmsaCredentialSpecName")] = None
    "GMSACredentialSpecName is the name of the GMSA credential spec to use."
    host_process: Annotated[bool | None, Field(alias="hostProcess")] = None
    "HostProcess determines if a container should be run as a \u0027Host Process\u0027 container. All of a Pod\u0027s containers must have the same effective HostProcess value (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers). In addition, if HostProcess is true then HostNetwork must also be set to true."
    run_as_user_name: Annotated[str | None, Field(alias="runAsUserName")] = None
    "The UserName in Windows to run the entrypoint of the container process. Defaults to the user specified in image metadata if unspecified. May also be set in PodSecurityContext. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."


class Binding(Resource):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Binding"]:
            return Binding

        def build(self) -> "Binding":
            return Binding(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Binding"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Binding.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Binding.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def target(self, value_or_callback: ObjectReference, /) -> "Binding.Builder": ...

        @overload
        def target(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "Binding.Builder": ...

        @overload
        def target(self, value_or_callback: Never = ...) -> "ObjectReference.BuilderContext": ...

        def target(self, value_or_callback=UNSET, /):
            "The target object that you want to bind to the standard object."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "target"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target", value)

    class BuilderContext(BuilderContextBase["Binding.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Binding.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Binding."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Binding", Builder]:
        return GenericListBuilder[Binding, Binding.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Binding"] = "Binding"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    target: ObjectReference
    "The target object that you want to bind to the standard object."


class CSIPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSIPersistentVolumeSource"]:
            return CSIPersistentVolumeSource

        def build(self) -> "CSIPersistentVolumeSource":
            return CSIPersistentVolumeSource(**self._attrs)

        @overload
        def controller_expand_secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def controller_expand_secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def controller_expand_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def controller_expand_secret_ref(self, value_or_callback=UNSET, /):
            "controllerExpandSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI ControllerExpandVolume call. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "controller_expand_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("controller_expand_secret_ref", value)

        @overload
        def controller_publish_secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def controller_publish_secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def controller_publish_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def controller_publish_secret_ref(self, value_or_callback=UNSET, /):
            "controllerPublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI ControllerPublishVolume and ControllerUnpublishVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "controller_publish_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("controller_publish_secret_ref", value)

        def driver(self, value: str, /) -> Self:
            "driver is the name of the driver to use for this volume. Required."
            return self._set("driver", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs".'
            return self._set("fs_type", value)

        @overload
        def node_expand_secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_expand_secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_expand_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def node_expand_secret_ref(self, value_or_callback=UNSET, /):
            "nodeExpandSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodeExpandVolume call. This field is optional, may be omitted if no secret is required. If the secret object contains more than one secret, all secrets are passed."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_expand_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_expand_secret_ref", value)

        @overload
        def node_publish_secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_publish_secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_publish_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def node_publish_secret_ref(self, value_or_callback=UNSET, /):
            "nodePublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodePublishVolume and NodeUnpublishVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_publish_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_publish_secret_ref", value)

        @overload
        def node_stage_secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_stage_secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CSIPersistentVolumeSource.Builder": ...

        @overload
        def node_stage_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def node_stage_secret_ref(self, value_or_callback=UNSET, /):
            "nodeStageSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodeStageVolume and NodeStageVolume and NodeUnstageVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_stage_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_stage_secret_ref", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly value to pass to ControllerPublishVolumeRequest. Defaults to false (read/write)."
            return self._set("read_only", value)

        def volume_attributes(self, value: dict[str, str] | None, /) -> Self:
            "volumeAttributes of the volume to publish."
            return self._set("volume_attributes", value)

        def volume_handle(self, value: str, /) -> Self:
            "volumeHandle is the unique volume name returned by the CSI volume plugin\u2019s CreateVolume to refer to the volume on all subsequent calls. Required."
            return self._set("volume_handle", value)

    class BuilderContext(BuilderContextBase["CSIPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSIPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSIPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSIPersistentVolumeSource", Builder]:
        return GenericListBuilder[CSIPersistentVolumeSource, CSIPersistentVolumeSource.Builder]()

    controller_expand_secret_ref: Annotated[
        SecretReference | None, Field(alias="controllerExpandSecretRef")
    ] = None
    "controllerExpandSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI ControllerExpandVolume call. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
    controller_publish_secret_ref: Annotated[
        SecretReference | None, Field(alias="controllerPublishSecretRef")
    ] = None
    "controllerPublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI ControllerPublishVolume and ControllerUnpublishVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
    driver: str
    "driver is the name of the driver to use for this volume. Required."
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs".'
    node_expand_secret_ref: Annotated[
        SecretReference | None, Field(alias="nodeExpandSecretRef")
    ] = None
    "nodeExpandSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodeExpandVolume call. This field is optional, may be omitted if no secret is required. If the secret object contains more than one secret, all secrets are passed."
    node_publish_secret_ref: Annotated[
        SecretReference | None, Field(alias="nodePublishSecretRef")
    ] = None
    "nodePublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodePublishVolume and NodeUnpublishVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
    node_stage_secret_ref: Annotated[SecretReference | None, Field(alias="nodeStageSecretRef")] = (
        None
    )
    "nodeStageSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodeStageVolume and NodeStageVolume and NodeUnstageVolume calls. This field is optional, and may be empty if no secret is required. If the secret object contains more than one secret, all secrets are passed."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly value to pass to ControllerPublishVolumeRequest. Defaults to false (read/write)."
    volume_attributes: Annotated[dict[str, str] | None, Field(alias="volumeAttributes")] = None
    "volumeAttributes of the volume to publish."
    volume_handle: Annotated[str, Field(alias="volumeHandle")]
    "volumeHandle is the unique volume name returned by the CSI volume plugin\u2019s CreateVolume to refer to the volume on all subsequent calls. Required."


class CSIVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CSIVolumeSource"]:
            return CSIVolumeSource

        def build(self) -> "CSIVolumeSource":
            return CSIVolumeSource(**self._attrs)

        def driver(self, value: str, /) -> Self:
            "driver is the name of the CSI driver that handles this volume. Consult with your admin for the correct name as registered in the cluster."
            return self._set("driver", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType to mount. Ex. "ext4", "xfs", "ntfs". If not provided, the empty value is passed to the associated CSI driver which will determine the default filesystem to apply.'
            return self._set("fs_type", value)

        @overload
        def node_publish_secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "CSIVolumeSource.Builder": ...

        @overload
        def node_publish_secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "CSIVolumeSource.Builder": ...

        @overload
        def node_publish_secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def node_publish_secret_ref(self, value_or_callback=UNSET, /):
            "nodePublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodePublishVolume and NodeUnpublishVolume calls. This field is optional, and  may be empty if no secret is required. If the secret object contains more than one secret, all secret references are passed."
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_publish_secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_publish_secret_ref", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly specifies a read-only configuration for the volume. Defaults to false (read/write)."
            return self._set("read_only", value)

        def volume_attributes(self, value: dict[str, str] | None, /) -> Self:
            "volumeAttributes stores driver-specific properties that are passed to the CSI driver. Consult your driver\u0027s documentation for supported values."
            return self._set("volume_attributes", value)

    class BuilderContext(BuilderContextBase["CSIVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CSIVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CSIVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CSIVolumeSource", Builder]:
        return GenericListBuilder[CSIVolumeSource, CSIVolumeSource.Builder]()

    driver: str
    "driver is the name of the CSI driver that handles this volume. Consult with your admin for the correct name as registered in the cluster."
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType to mount. Ex. "ext4", "xfs", "ntfs". If not provided, the empty value is passed to the associated CSI driver which will determine the default filesystem to apply.'
    node_publish_secret_ref: Annotated[
        LocalObjectReference | None, Field(alias="nodePublishSecretRef")
    ] = None
    "nodePublishSecretRef is a reference to the secret object containing sensitive information to pass to the CSI driver to complete the CSI NodePublishVolume and NodeUnpublishVolume calls. This field is optional, and  may be empty if no secret is required. If the secret object contains more than one secret, all secret references are passed."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly specifies a read-only configuration for the volume. Defaults to false (read/write)."
    volume_attributes: Annotated[dict[str, str] | None, Field(alias="volumeAttributes")] = None
    "volumeAttributes stores driver-specific properties that are passed to the CSI driver. Consult your driver\u0027s documentation for supported values."


class CephFSPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CephFSPersistentVolumeSource"]:
            return CephFSPersistentVolumeSource

        def build(self) -> "CephFSPersistentVolumeSource":
            return CephFSPersistentVolumeSource(**self._attrs)

        def monitors(self, value: list[str], /) -> Self:
            "monitors is Required: Monitors is a collection of Ceph monitors More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("monitors", value)

        def path(self, value: str | None, /) -> Self:
            "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /"
            return self._set("path", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("read_only", value)

        def secret_file(self, value: str | None, /) -> Self:
            "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("secret_file", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CephFSPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CephFSPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def user(self, value: str | None, /) -> Self:
            "user is Optional: User is the rados user name, default is admin More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("user", value)

    class BuilderContext(BuilderContextBase["CephFSPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CephFSPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CephFSPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CephFSPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            CephFSPersistentVolumeSource, CephFSPersistentVolumeSource.Builder
        ]()

    monitors: list[str]
    "monitors is Required: Monitors is a collection of Ceph monitors More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    path: str | None = None
    "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    secret_file: Annotated[str | None, Field(alias="secretFile")] = None
    "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    secret_ref: Annotated[SecretReference | None, Field(alias="secretRef")] = None
    "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    user: str | None = None
    "user is Optional: User is the rados user name, default is admin More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"


class CephFSVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CephFSVolumeSource"]:
            return CephFSVolumeSource

        def build(self) -> "CephFSVolumeSource":
            return CephFSVolumeSource(**self._attrs)

        def monitors(self, value: list[str], /) -> Self:
            "monitors is Required: Monitors is a collection of Ceph monitors More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("monitors", value)

        def path(self, value: str | None, /) -> Self:
            "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /"
            return self._set("path", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("read_only", value)

        def secret_file(self, value: str | None, /) -> Self:
            "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("secret_file", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "CephFSVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "CephFSVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def user(self, value: str | None, /) -> Self:
            "user is optional: User is the rados user name, default is admin More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
            return self._set("user", value)

    class BuilderContext(BuilderContextBase["CephFSVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CephFSVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CephFSVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CephFSVolumeSource", Builder]:
        return GenericListBuilder[CephFSVolumeSource, CephFSVolumeSource.Builder]()

    monitors: list[str]
    "monitors is Required: Monitors is a collection of Ceph monitors More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    path: str | None = None
    "path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    secret_file: Annotated[str | None, Field(alias="secretFile")] = None
    "secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty. More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"
    user: str | None = None
    "user is optional: User is the rados user name, default is admin More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it"


class CinderPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CinderPersistentVolumeSource"]:
            return CinderPersistentVolumeSource

        def build(self) -> "CinderPersistentVolumeSource":
            return CinderPersistentVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType Filesystem type to mount. Must be a filesystem type supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://examples.k8s.io/mysql-cinder-pd/README.md'
            return self._set("fs_type", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "CinderPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "CinderPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is Optional: points to a secret object containing parameters used to connect to OpenStack."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def volume_id(self, value: str, /) -> Self:
            "volumeID used to identify the volume in cinder. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            return self._set("volume_id", value)

    class BuilderContext(BuilderContextBase["CinderPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CinderPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CinderPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CinderPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            CinderPersistentVolumeSource, CinderPersistentVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType Filesystem type to mount. Must be a filesystem type supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://examples.k8s.io/mysql-cinder-pd/README.md'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: Defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
    secret_ref: Annotated[SecretReference | None, Field(alias="secretRef")] = None
    "secretRef is Optional: points to a secret object containing parameters used to connect to OpenStack."
    volume_id: Annotated[str, Field(alias="volumeID")]
    "volumeID used to identify the volume in cinder. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"


class CinderVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CinderVolumeSource"]:
            return CinderVolumeSource

        def build(self) -> "CinderVolumeSource":
            return CinderVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://examples.k8s.io/mysql-cinder-pd/README.md'
            return self._set("fs_type", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "CinderVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "CinderVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is optional: points to a secret object containing parameters used to connect to OpenStack."
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def volume_id(self, value: str, /) -> Self:
            "volumeID used to identify the volume in cinder. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            return self._set("volume_id", value)

    class BuilderContext(BuilderContextBase["CinderVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CinderVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CinderVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CinderVolumeSource", Builder]:
        return GenericListBuilder[CinderVolumeSource, CinderVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://examples.k8s.io/mysql-cinder-pd/README.md'
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef is optional: points to a secret object containing parameters used to connect to OpenStack."
    volume_id: Annotated[str, Field(alias="volumeID")]
    "volumeID used to identify the volume in cinder. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"


class ConfigMapProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMapProjection"]:
            return ConfigMapProjection

        def build(self) -> "ConfigMapProjection":
            return ConfigMapProjection(**self._attrs)

        @overload
        def items(
            self, value_or_callback: list[KeyToPath] | None, /
        ) -> "ConfigMapProjection.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[KeyToPath, KeyToPath.Builder]],
                GenericListBuilder[KeyToPath, KeyToPath.Builder] | list[KeyToPath],
            ],
            /,
        ) -> "ConfigMapProjection.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KeyToPath.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KeyToPath.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KeyToPath.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "optional specify whether the ConfigMap or its keys must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["ConfigMapProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMapProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMapProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMapProjection", Builder]:
        return GenericListBuilder[ConfigMapProjection, ConfigMapProjection.Builder]()

    items: list[KeyToPath] | None = None
    "items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "optional specify whether the ConfigMap or its keys must be defined"


class ConfigMapVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ConfigMapVolumeSource"]:
            return ConfigMapVolumeSource

        def build(self) -> "ConfigMapVolumeSource":
            return ConfigMapVolumeSource(**self._attrs)

        def default_mode(self, value: int | None, /) -> Self:
            "defaultMode is optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("default_mode", value)

        @overload
        def items(
            self, value_or_callback: list[KeyToPath] | None, /
        ) -> "ConfigMapVolumeSource.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[KeyToPath, KeyToPath.Builder]],
                GenericListBuilder[KeyToPath, KeyToPath.Builder] | list[KeyToPath],
            ],
            /,
        ) -> "ConfigMapVolumeSource.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[KeyToPath.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[KeyToPath.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(KeyToPath.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def optional(self, value: bool | None, /) -> Self:
            "optional specify whether the ConfigMap or its keys must be defined"
            return self._set("optional", value)

    class BuilderContext(BuilderContextBase["ConfigMapVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ConfigMapVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ConfigMapVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ConfigMapVolumeSource", Builder]:
        return GenericListBuilder[ConfigMapVolumeSource, ConfigMapVolumeSource.Builder]()

    default_mode: Annotated[int | None, Field(alias="defaultMode")] = None
    "defaultMode is optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    items: list[KeyToPath] | None = None
    "items if unspecified, each key-value pair in the Data field of the referenced ConfigMap will be projected into the volume as a file whose name is the key and content is the value. If specified, the listed keys will be projected into the specified paths, and unlisted keys will not be present. If a key is specified which is not present in the ConfigMap, the volume setup will error unless it is marked optional. Paths must be relative and may not contain the \u0027..\u0027 path or start with \u0027..\u0027."
    name: str | None = None
    "Name of the referent. This field is effectively required, but due to backwards compatibility is allowed to be empty. Instances of this type with an empty value here are almost certainly wrong. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    optional: bool | None = None
    "optional specify whether the ConfigMap or its keys must be defined"


class ContainerRestartRule(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerRestartRule"]:
            return ContainerRestartRule

        def build(self) -> "ContainerRestartRule":
            return ContainerRestartRule(**self._attrs)

        def action(self, value: str, /) -> Self:
            'Specifies the action taken on a container exit if the requirements are satisfied. The only possible value is "Restart" to restart the container.'
            return self._set("action", value)

        @overload
        def exit_codes(
            self, value_or_callback: ContainerRestartRuleOnExitCodes | None, /
        ) -> "ContainerRestartRule.Builder": ...

        @overload
        def exit_codes(
            self,
            value_or_callback: Callable[
                [ContainerRestartRuleOnExitCodes.Builder],
                ContainerRestartRuleOnExitCodes.Builder | ContainerRestartRuleOnExitCodes,
            ],
            /,
        ) -> "ContainerRestartRule.Builder": ...

        @overload
        def exit_codes(
            self, value_or_callback: Never = ...
        ) -> "ContainerRestartRuleOnExitCodes.BuilderContext": ...

        def exit_codes(self, value_or_callback=UNSET, /):
            "Represents the exit codes to check on container exits."
            if self._in_context and value_or_callback is UNSET:
                context = ContainerRestartRuleOnExitCodes.BuilderContext()
                context._parent_builder = self
                context._field_name = "exit_codes"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerRestartRuleOnExitCodes.builder())
                if isinstance(output, ContainerRestartRuleOnExitCodes.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("exit_codes", value)

    class BuilderContext(BuilderContextBase["ContainerRestartRule.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerRestartRule.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerRestartRule."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerRestartRule", Builder]:
        return GenericListBuilder[ContainerRestartRule, ContainerRestartRule.Builder]()

    action: str
    'Specifies the action taken on a container exit if the requirements are satisfied. The only possible value is "Restart" to restart the container.'
    exit_codes: Annotated[ContainerRestartRuleOnExitCodes | None, Field(alias="exitCodes")] = None
    "Represents the exit codes to check on container exits."


class ContainerState(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerState"]:
            return ContainerState

        def build(self) -> "ContainerState":
            return ContainerState(**self._attrs)

        @overload
        def running(
            self, value_or_callback: ContainerStateRunning | None, /
        ) -> "ContainerState.Builder": ...

        @overload
        def running(
            self,
            value_or_callback: Callable[
                [ContainerStateRunning.Builder],
                ContainerStateRunning.Builder | ContainerStateRunning,
            ],
            /,
        ) -> "ContainerState.Builder": ...

        @overload
        def running(
            self, value_or_callback: Never = ...
        ) -> "ContainerStateRunning.BuilderContext": ...

        def running(self, value_or_callback=UNSET, /):
            "Details about a running container"
            if self._in_context and value_or_callback is UNSET:
                context = ContainerStateRunning.BuilderContext()
                context._parent_builder = self
                context._field_name = "running"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStateRunning.builder())
                if isinstance(output, ContainerStateRunning.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("running", value)

        @overload
        def terminated(
            self, value_or_callback: ContainerStateTerminated | None, /
        ) -> "ContainerState.Builder": ...

        @overload
        def terminated(
            self,
            value_or_callback: Callable[
                [ContainerStateTerminated.Builder],
                ContainerStateTerminated.Builder | ContainerStateTerminated,
            ],
            /,
        ) -> "ContainerState.Builder": ...

        @overload
        def terminated(
            self, value_or_callback: Never = ...
        ) -> "ContainerStateTerminated.BuilderContext": ...

        def terminated(self, value_or_callback=UNSET, /):
            "Details about a terminated container"
            if self._in_context and value_or_callback is UNSET:
                context = ContainerStateTerminated.BuilderContext()
                context._parent_builder = self
                context._field_name = "terminated"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStateTerminated.builder())
                if isinstance(output, ContainerStateTerminated.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("terminated", value)

        @overload
        def waiting(
            self, value_or_callback: ContainerStateWaiting | None, /
        ) -> "ContainerState.Builder": ...

        @overload
        def waiting(
            self,
            value_or_callback: Callable[
                [ContainerStateWaiting.Builder],
                ContainerStateWaiting.Builder | ContainerStateWaiting,
            ],
            /,
        ) -> "ContainerState.Builder": ...

        @overload
        def waiting(
            self, value_or_callback: Never = ...
        ) -> "ContainerStateWaiting.BuilderContext": ...

        def waiting(self, value_or_callback=UNSET, /):
            "Details about a waiting container"
            if self._in_context and value_or_callback is UNSET:
                context = ContainerStateWaiting.BuilderContext()
                context._parent_builder = self
                context._field_name = "waiting"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStateWaiting.builder())
                if isinstance(output, ContainerStateWaiting.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("waiting", value)

    class BuilderContext(BuilderContextBase["ContainerState.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerState.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerState."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerState", Builder]:
        return GenericListBuilder[ContainerState, ContainerState.Builder]()

    running: ContainerStateRunning | None = None
    "Details about a running container"
    terminated: ContainerStateTerminated | None = None
    "Details about a terminated container"
    waiting: ContainerStateWaiting | None = None
    "Details about a waiting container"


class ContainerUser(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerUser"]:
            return ContainerUser

        def build(self) -> "ContainerUser":
            return ContainerUser(**self._attrs)

        @overload
        def linux(
            self, value_or_callback: LinuxContainerUser | None, /
        ) -> "ContainerUser.Builder": ...

        @overload
        def linux(
            self,
            value_or_callback: Callable[
                [LinuxContainerUser.Builder],
                LinuxContainerUser.Builder | LinuxContainerUser,
            ],
            /,
        ) -> "ContainerUser.Builder": ...

        @overload
        def linux(self, value_or_callback: Never = ...) -> "LinuxContainerUser.BuilderContext": ...

        def linux(self, value_or_callback=UNSET, /):
            "Linux holds user identity information initially attached to the first process of the containers in Linux. Note that the actual running identity can be changed if the process has enough privilege to do so."
            if self._in_context and value_or_callback is UNSET:
                context = LinuxContainerUser.BuilderContext()
                context._parent_builder = self
                context._field_name = "linux"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LinuxContainerUser.builder())
                if isinstance(output, LinuxContainerUser.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("linux", value)

    class BuilderContext(BuilderContextBase["ContainerUser.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerUser.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerUser."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerUser", Builder]:
        return GenericListBuilder[ContainerUser, ContainerUser.Builder]()

    linux: LinuxContainerUser | None = None
    "Linux holds user identity information initially attached to the first process of the containers in Linux. Note that the actual running identity can be changed if the process has enough privilege to do so."


class DownwardAPIVolumeFile(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DownwardAPIVolumeFile"]:
            return DownwardAPIVolumeFile

        def build(self) -> "DownwardAPIVolumeFile":
            return DownwardAPIVolumeFile(**self._attrs)

        @overload
        def field_ref(
            self, value_or_callback: ObjectFieldSelector | None, /
        ) -> "DownwardAPIVolumeFile.Builder": ...

        @overload
        def field_ref(
            self,
            value_or_callback: Callable[
                [ObjectFieldSelector.Builder],
                ObjectFieldSelector.Builder | ObjectFieldSelector,
            ],
            /,
        ) -> "DownwardAPIVolumeFile.Builder": ...

        @overload
        def field_ref(
            self, value_or_callback: Never = ...
        ) -> "ObjectFieldSelector.BuilderContext": ...

        def field_ref(self, value_or_callback=UNSET, /):
            "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectFieldSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "field_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectFieldSelector.builder())
                if isinstance(output, ObjectFieldSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("field_ref", value)

        def mode(self, value: int | None, /) -> Self:
            "Optional: mode bits used to set permissions on this file, must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("mode", value)

        def path(self, value: str, /) -> Self:
            "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the \u0027..\u0027 path. Must be utf-8 encoded. The first item of the relative path must not start with \u0027..\u0027"
            return self._set("path", value)

        @overload
        def resource_field_ref(
            self, value_or_callback: ResourceFieldSelector | None, /
        ) -> "DownwardAPIVolumeFile.Builder": ...

        @overload
        def resource_field_ref(
            self,
            value_or_callback: Callable[
                [ResourceFieldSelector.Builder],
                ResourceFieldSelector.Builder | ResourceFieldSelector,
            ],
            /,
        ) -> "DownwardAPIVolumeFile.Builder": ...

        @overload
        def resource_field_ref(
            self, value_or_callback: Never = ...
        ) -> "ResourceFieldSelector.BuilderContext": ...

        def resource_field_ref(self, value_or_callback=UNSET, /):
            "Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceFieldSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "resource_field_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceFieldSelector.builder())
                if isinstance(output, ResourceFieldSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_field_ref", value)

    class BuilderContext(BuilderContextBase["DownwardAPIVolumeFile.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DownwardAPIVolumeFile.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DownwardAPIVolumeFile."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DownwardAPIVolumeFile", Builder]:
        return GenericListBuilder[DownwardAPIVolumeFile, DownwardAPIVolumeFile.Builder]()

    field_ref: Annotated[ObjectFieldSelector | None, Field(alias="fieldRef")] = None
    "Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported."
    mode: int | None = None
    "Optional: mode bits used to set permissions on this file, must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    path: str
    "Required: Path is  the relative path name of the file to be created. Must not be absolute or contain the \u0027..\u0027 path. Must be utf-8 encoded. The first item of the relative path must not start with \u0027..\u0027"
    resource_field_ref: Annotated[ResourceFieldSelector | None, Field(alias="resourceFieldRef")] = (
        None
    )
    "Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported."


class DownwardAPIVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DownwardAPIVolumeSource"]:
            return DownwardAPIVolumeSource

        def build(self) -> "DownwardAPIVolumeSource":
            return DownwardAPIVolumeSource(**self._attrs)

        def default_mode(self, value: int | None, /) -> Self:
            "Optional: mode bits to use on created files by default. Must be a Optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("default_mode", value)

        @overload
        def items(
            self, value_or_callback: list[DownwardAPIVolumeFile] | None, /
        ) -> "DownwardAPIVolumeSource.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DownwardAPIVolumeFile, DownwardAPIVolumeFile.Builder]],
                GenericListBuilder[DownwardAPIVolumeFile, DownwardAPIVolumeFile.Builder]
                | list[DownwardAPIVolumeFile],
            ],
            /,
        ) -> "DownwardAPIVolumeSource.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DownwardAPIVolumeFile.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "Items is a list of downward API volume file"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DownwardAPIVolumeFile.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DownwardAPIVolumeFile.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

    class BuilderContext(BuilderContextBase["DownwardAPIVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DownwardAPIVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DownwardAPIVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DownwardAPIVolumeSource", Builder]:
        return GenericListBuilder[DownwardAPIVolumeSource, DownwardAPIVolumeSource.Builder]()

    default_mode: Annotated[int | None, Field(alias="defaultMode")] = None
    "Optional: mode bits to use on created files by default. Must be a Optional: mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    items: list[DownwardAPIVolumeFile] | None = None
    "Items is a list of downward API volume file"


class EndpointAddress(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointAddress"]:
            return EndpointAddress

        def build(self) -> "EndpointAddress":
            return EndpointAddress(**self._attrs)

        def hostname(self, value: str | None, /) -> Self:
            "The Hostname of this endpoint"
            return self._set("hostname", value)

        def ip(self, value: str, /) -> Self:
            "The IP of this endpoint. May not be loopback (127.0.0.0/8 or ::1), link-local (169.254.0.0/16 or fe80::/10), or link-local multicast (224.0.0.0/24 or ff02::/16)."
            return self._set("ip", value)

        def node_name(self, value: str | None, /) -> Self:
            "Optional: Node hosting this endpoint. This can be used to determine endpoints local to a node."
            return self._set("node_name", value)

        @overload
        def target_ref(
            self, value_or_callback: ObjectReference | None, /
        ) -> "EndpointAddress.Builder": ...

        @overload
        def target_ref(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "EndpointAddress.Builder": ...

        @overload
        def target_ref(
            self, value_or_callback: Never = ...
        ) -> "ObjectReference.BuilderContext": ...

        def target_ref(self, value_or_callback=UNSET, /):
            "Reference to object providing the endpoint."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "target_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target_ref", value)

    class BuilderContext(BuilderContextBase["EndpointAddress.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointAddress.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointAddress."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointAddress", Builder]:
        return GenericListBuilder[EndpointAddress, EndpointAddress.Builder]()

    hostname: str | None = None
    "The Hostname of this endpoint"
    ip: str
    "The IP of this endpoint. May not be loopback (127.0.0.0/8 or ::1), link-local (169.254.0.0/16 or fe80::/10), or link-local multicast (224.0.0.0/24 or ff02::/16)."
    node_name: Annotated[str | None, Field(alias="nodeName")] = None
    "Optional: Node hosting this endpoint. This can be used to determine endpoints local to a node."
    target_ref: Annotated[ObjectReference | None, Field(alias="targetRef")] = None
    "Reference to object providing the endpoint."


class EndpointSubset(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EndpointSubset"]:
            return EndpointSubset

        def build(self) -> "EndpointSubset":
            return EndpointSubset(**self._attrs)

        @overload
        def addresses(
            self, value_or_callback: list[EndpointAddress] | None, /
        ) -> "EndpointSubset.Builder": ...

        @overload
        def addresses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EndpointAddress, EndpointAddress.Builder]],
                GenericListBuilder[EndpointAddress, EndpointAddress.Builder]
                | list[EndpointAddress],
            ],
            /,
        ) -> "EndpointSubset.Builder": ...

        @overload
        def addresses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EndpointAddress.Builder]: ...

        def addresses(self, value_or_callback=UNSET, /):
            "IP addresses which offer the related ports that are marked as ready. These endpoints should be considered safe for load balancers and clients to utilize."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EndpointAddress.Builder]()
                context._parent_builder = self
                context._field_name = "addresses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointAddress.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("addresses", value)

        @overload
        def not_ready_addresses(
            self, value_or_callback: list[EndpointAddress] | None, /
        ) -> "EndpointSubset.Builder": ...

        @overload
        def not_ready_addresses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EndpointAddress, EndpointAddress.Builder]],
                GenericListBuilder[EndpointAddress, EndpointAddress.Builder]
                | list[EndpointAddress],
            ],
            /,
        ) -> "EndpointSubset.Builder": ...

        @overload
        def not_ready_addresses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EndpointAddress.Builder]: ...

        def not_ready_addresses(self, value_or_callback=UNSET, /):
            "IP addresses which offer the related ports but are not currently marked as ready because they have not yet finished starting, have recently failed a readiness check, or have recently failed a liveness check."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EndpointAddress.Builder]()
                context._parent_builder = self
                context._field_name = "not_ready_addresses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointAddress.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("not_ready_addresses", value)

        @overload
        def ports(
            self, value_or_callback: list[EndpointPort] | None, /
        ) -> "EndpointSubset.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EndpointPort, EndpointPort.Builder]],
                GenericListBuilder[EndpointPort, EndpointPort.Builder] | list[EndpointPort],
            ],
            /,
        ) -> "EndpointSubset.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EndpointPort.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            "Port numbers available on the related IP addresses."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EndpointPort.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointPort.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

    class BuilderContext(BuilderContextBase["EndpointSubset.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EndpointSubset.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EndpointSubset."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EndpointSubset", Builder]:
        return GenericListBuilder[EndpointSubset, EndpointSubset.Builder]()

    addresses: list[EndpointAddress] | None = None
    "IP addresses which offer the related ports that are marked as ready. These endpoints should be considered safe for load balancers and clients to utilize."
    not_ready_addresses: Annotated[
        list[EndpointAddress] | None, Field(alias="notReadyAddresses")
    ] = None
    "IP addresses which offer the related ports but are not currently marked as ready because they have not yet finished starting, have recently failed a readiness check, or have recently failed a liveness check."
    ports: list[EndpointPort] | None = None
    "Port numbers available on the related IP addresses."


class Endpoints(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "endpoints",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Endpoints"]:
            return Endpoints

        def build(self) -> "Endpoints":
            return Endpoints(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Endpoints"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Endpoints.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Endpoints.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def subsets(
            self, value_or_callback: list[EndpointSubset] | None, /
        ) -> "Endpoints.Builder": ...

        @overload
        def subsets(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EndpointSubset, EndpointSubset.Builder]],
                GenericListBuilder[EndpointSubset, EndpointSubset.Builder] | list[EndpointSubset],
            ],
            /,
        ) -> "Endpoints.Builder": ...

        @overload
        def subsets(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EndpointSubset.Builder]: ...

        def subsets(self, value_or_callback=UNSET, /):
            "The set of all endpoints is the union of all subsets. Addresses are placed into subsets according to the IPs they share. A single address with multiple ports, some of which are ready and some of which are not (because they come from different containers) will result in the address being displayed in different subsets for the different ports. No address will appear in both Addresses and NotReadyAddresses in the same subset. Sets of addresses and ports that comprise a service."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EndpointSubset.Builder]()
                context._parent_builder = self
                context._field_name = "subsets"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EndpointSubset.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("subsets", value)

    class BuilderContext(BuilderContextBase["Endpoints.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Endpoints.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Endpoints."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Endpoints", Builder]:
        return GenericListBuilder[Endpoints, Endpoints.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Endpoints"] = "Endpoints"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    subsets: list[EndpointSubset] | None = None
    "The set of all endpoints is the union of all subsets. Addresses are placed into subsets according to the IPs they share. A single address with multiple ports, some of which are ready and some of which are not (because they come from different containers) will result in the address being displayed in different subsets for the different ports. No address will appear in both Addresses and NotReadyAddresses in the same subset. Sets of addresses and ports that comprise a service."


EndpointsList = ResourceList[Endpoints]


class EnvFromSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EnvFromSource"]:
            return EnvFromSource

        def build(self) -> "EnvFromSource":
            return EnvFromSource(**self._attrs)

        @overload
        def config_map_ref(
            self, value_or_callback: ConfigMapEnvSource | None, /
        ) -> "EnvFromSource.Builder": ...

        @overload
        def config_map_ref(
            self,
            value_or_callback: Callable[
                [ConfigMapEnvSource.Builder],
                ConfigMapEnvSource.Builder | ConfigMapEnvSource,
            ],
            /,
        ) -> "EnvFromSource.Builder": ...

        @overload
        def config_map_ref(
            self, value_or_callback: Never = ...
        ) -> "ConfigMapEnvSource.BuilderContext": ...

        def config_map_ref(self, value_or_callback=UNSET, /):
            "The ConfigMap to select from"
            if self._in_context and value_or_callback is UNSET:
                context = ConfigMapEnvSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_map_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ConfigMapEnvSource.builder())
                if isinstance(output, ConfigMapEnvSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_map_ref", value)

        def prefix(self, value: str | None, /) -> Self:
            "Optional text to prepend to the name of each environment variable. May consist of any printable ASCII characters except \u0027=\u0027."
            return self._set("prefix", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretEnvSource | None, /
        ) -> "EnvFromSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretEnvSource.Builder], SecretEnvSource.Builder | SecretEnvSource
            ],
            /,
        ) -> "EnvFromSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretEnvSource.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "The Secret to select from"
            if self._in_context and value_or_callback is UNSET:
                context = SecretEnvSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretEnvSource.builder())
                if isinstance(output, SecretEnvSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

    class BuilderContext(BuilderContextBase["EnvFromSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EnvFromSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EnvFromSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EnvFromSource", Builder]:
        return GenericListBuilder[EnvFromSource, EnvFromSource.Builder]()

    config_map_ref: Annotated[ConfigMapEnvSource | None, Field(alias="configMapRef")] = None
    "The ConfigMap to select from"
    prefix: str | None = None
    "Optional text to prepend to the name of each environment variable. May consist of any printable ASCII characters except \u0027=\u0027."
    secret_ref: Annotated[SecretEnvSource | None, Field(alias="secretRef")] = None
    "The Secret to select from"


class EnvVarSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EnvVarSource"]:
            return EnvVarSource

        def build(self) -> "EnvVarSource":
            return EnvVarSource(**self._attrs)

        @overload
        def config_map_key_ref(
            self, value_or_callback: ConfigMapKeySelector | None, /
        ) -> "EnvVarSource.Builder": ...

        @overload
        def config_map_key_ref(
            self,
            value_or_callback: Callable[
                [ConfigMapKeySelector.Builder],
                ConfigMapKeySelector.Builder | ConfigMapKeySelector,
            ],
            /,
        ) -> "EnvVarSource.Builder": ...

        @overload
        def config_map_key_ref(
            self, value_or_callback: Never = ...
        ) -> "ConfigMapKeySelector.BuilderContext": ...

        def config_map_key_ref(self, value_or_callback=UNSET, /):
            "Selects a key of a ConfigMap."
            if self._in_context and value_or_callback is UNSET:
                context = ConfigMapKeySelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_map_key_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ConfigMapKeySelector.builder())
                if isinstance(output, ConfigMapKeySelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_map_key_ref", value)

        @overload
        def field_ref(
            self, value_or_callback: ObjectFieldSelector | None, /
        ) -> "EnvVarSource.Builder": ...

        @overload
        def field_ref(
            self,
            value_or_callback: Callable[
                [ObjectFieldSelector.Builder],
                ObjectFieldSelector.Builder | ObjectFieldSelector,
            ],
            /,
        ) -> "EnvVarSource.Builder": ...

        @overload
        def field_ref(
            self, value_or_callback: Never = ...
        ) -> "ObjectFieldSelector.BuilderContext": ...

        def field_ref(self, value_or_callback=UNSET, /):
            "Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels[\u0027\u003cKEY\u003e\u0027]`, `metadata.annotations[\u0027\u003cKEY\u003e\u0027]`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectFieldSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "field_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectFieldSelector.builder())
                if isinstance(output, ObjectFieldSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("field_ref", value)

        @overload
        def file_key_ref(
            self, value_or_callback: FileKeySelector | None, /
        ) -> "EnvVarSource.Builder": ...

        @overload
        def file_key_ref(
            self,
            value_or_callback: Callable[
                [FileKeySelector.Builder], FileKeySelector.Builder | FileKeySelector
            ],
            /,
        ) -> "EnvVarSource.Builder": ...

        @overload
        def file_key_ref(
            self, value_or_callback: Never = ...
        ) -> "FileKeySelector.BuilderContext": ...

        def file_key_ref(self, value_or_callback=UNSET, /):
            "FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled."
            if self._in_context and value_or_callback is UNSET:
                context = FileKeySelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "file_key_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FileKeySelector.builder())
                if isinstance(output, FileKeySelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("file_key_ref", value)

        @overload
        def resource_field_ref(
            self, value_or_callback: ResourceFieldSelector | None, /
        ) -> "EnvVarSource.Builder": ...

        @overload
        def resource_field_ref(
            self,
            value_or_callback: Callable[
                [ResourceFieldSelector.Builder],
                ResourceFieldSelector.Builder | ResourceFieldSelector,
            ],
            /,
        ) -> "EnvVarSource.Builder": ...

        @overload
        def resource_field_ref(
            self, value_or_callback: Never = ...
        ) -> "ResourceFieldSelector.BuilderContext": ...

        def resource_field_ref(self, value_or_callback=UNSET, /):
            "Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceFieldSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "resource_field_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceFieldSelector.builder())
                if isinstance(output, ResourceFieldSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_field_ref", value)

        @overload
        def secret_key_ref(
            self, value_or_callback: SecretKeySelector | None, /
        ) -> "EnvVarSource.Builder": ...

        @overload
        def secret_key_ref(
            self,
            value_or_callback: Callable[
                [SecretKeySelector.Builder],
                SecretKeySelector.Builder | SecretKeySelector,
            ],
            /,
        ) -> "EnvVarSource.Builder": ...

        @overload
        def secret_key_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretKeySelector.BuilderContext": ...

        def secret_key_ref(self, value_or_callback=UNSET, /):
            "Selects a key of a secret in the pod\u0027s namespace"
            if self._in_context and value_or_callback is UNSET:
                context = SecretKeySelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_key_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretKeySelector.builder())
                if isinstance(output, SecretKeySelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_key_ref", value)

    class BuilderContext(BuilderContextBase["EnvVarSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EnvVarSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EnvVarSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EnvVarSource", Builder]:
        return GenericListBuilder[EnvVarSource, EnvVarSource.Builder]()

    config_map_key_ref: Annotated[ConfigMapKeySelector | None, Field(alias="configMapKeyRef")] = (
        None
    )
    "Selects a key of a ConfigMap."
    field_ref: Annotated[ObjectFieldSelector | None, Field(alias="fieldRef")] = None
    "Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels[\u0027\u003cKEY\u003e\u0027]`, `metadata.annotations[\u0027\u003cKEY\u003e\u0027]`, spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs."
    file_key_ref: Annotated[FileKeySelector | None, Field(alias="fileKeyRef")] = None
    "FileKeyRef selects a key of the env file. Requires the EnvFiles feature gate to be enabled."
    resource_field_ref: Annotated[ResourceFieldSelector | None, Field(alias="resourceFieldRef")] = (
        None
    )
    "Selects a resource of the container: only resources limits and requests (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported."
    secret_key_ref: Annotated[SecretKeySelector | None, Field(alias="secretKeyRef")] = None
    "Selects a key of a secret in the pod\u0027s namespace"


class Event(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "events",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Event"]:
            return Event

        def build(self) -> "Event":
            return Event(**self._attrs)

        def action(self, value: str | None, /) -> Self:
            "What action was taken/failed regarding to the Regarding object."
            return self._set("action", value)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def count(self, value: int | None, /) -> Self:
            "The number of times this event has occurred."
            return self._set("count", value)

        @overload
        def event_time(self, value_or_callback: MicroTime | None, /) -> "Event.Builder": ...

        @overload
        def event_time(
            self,
            value_or_callback: Callable[[MicroTime.Builder], MicroTime.Builder | MicroTime],
            /,
        ) -> "Event.Builder": ...

        @overload
        def event_time(self, value_or_callback: Never = ...) -> "MicroTime.BuilderContext": ...

        def event_time(self, value_or_callback=UNSET, /):
            "Time when this Event was first observed."
            if self._in_context and value_or_callback is UNSET:
                context = MicroTime.BuilderContext()
                context._parent_builder = self
                context._field_name = "event_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(MicroTime.builder())
                if isinstance(output, MicroTime.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("event_time", value)

        @overload
        def first_timestamp(self, value_or_callback: Time | None, /) -> "Event.Builder": ...

        @overload
        def first_timestamp(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "Event.Builder": ...

        @overload
        def first_timestamp(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def first_timestamp(self, value_or_callback=UNSET, /):
            "The time at which the event was first recorded. (Time of server receipt is in TypeMeta.)"
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "first_timestamp"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("first_timestamp", value)

        @overload
        def involved_object(self, value_or_callback: ObjectReference, /) -> "Event.Builder": ...

        @overload
        def involved_object(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "Event.Builder": ...

        @overload
        def involved_object(
            self, value_or_callback: Never = ...
        ) -> "ObjectReference.BuilderContext": ...

        def involved_object(self, value_or_callback=UNSET, /):
            "The object that this event is about."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "involved_object"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("involved_object", value)

        def kind(self, value: Literal["Event"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def last_timestamp(self, value_or_callback: Time | None, /) -> "Event.Builder": ...

        @overload
        def last_timestamp(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "Event.Builder": ...

        @overload
        def last_timestamp(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def last_timestamp(self, value_or_callback=UNSET, /):
            "The time at which the most recent occurrence of this event was recorded."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_timestamp"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_timestamp", value)

        def message(self, value: str | None, /) -> Self:
            "A human-readable description of the status of this operation."
            return self._set("message", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Event.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Event.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        def reason(self, value: str | None, /) -> Self:
            "This should be a short, machine understandable string that gives the reason for the transition into the object\u0027s current status."
            return self._set("reason", value)

        @overload
        def related(self, value_or_callback: ObjectReference | None, /) -> "Event.Builder": ...

        @overload
        def related(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "Event.Builder": ...

        @overload
        def related(self, value_or_callback: Never = ...) -> "ObjectReference.BuilderContext": ...

        def related(self, value_or_callback=UNSET, /):
            "Optional secondary object for more complex actions."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "related"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("related", value)

        def reporting_component(self, value: str | None, /) -> Self:
            "Name of the controller that emitted this Event, e.g. `kubernetes.io/kubelet`."
            return self._set("reporting_component", value)

        def reporting_instance(self, value: str | None, /) -> Self:
            "ID of the controller instance, e.g. `kubelet-xyzf`."
            return self._set("reporting_instance", value)

        @overload
        def series(self, value_or_callback: EventSeries | None, /) -> "Event.Builder": ...

        @overload
        def series(
            self,
            value_or_callback: Callable[[EventSeries.Builder], EventSeries.Builder | EventSeries],
            /,
        ) -> "Event.Builder": ...

        @overload
        def series(self, value_or_callback: Never = ...) -> "EventSeries.BuilderContext": ...

        def series(self, value_or_callback=UNSET, /):
            "Data about the Event series this event represents or nil if it\u0027s a singleton Event."
            if self._in_context and value_or_callback is UNSET:
                context = EventSeries.BuilderContext()
                context._parent_builder = self
                context._field_name = "series"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EventSeries.builder())
                if isinstance(output, EventSeries.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("series", value)

        @overload
        def source(self, value_or_callback: EventSource | None, /) -> "Event.Builder": ...

        @overload
        def source(
            self,
            value_or_callback: Callable[[EventSource.Builder], EventSource.Builder | EventSource],
            /,
        ) -> "Event.Builder": ...

        @overload
        def source(self, value_or_callback: Never = ...) -> "EventSource.BuilderContext": ...

        def source(self, value_or_callback=UNSET, /):
            "The component reporting this event. Should be a short machine understandable string."
            if self._in_context and value_or_callback is UNSET:
                context = EventSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "source"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EventSource.builder())
                if isinstance(output, EventSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("source", value)

        def type(self, value: str | None, /) -> Self:
            "Type of this event (Normal, Warning), new types could be added in the future"
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["Event.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Event.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Event."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Event", Builder]:
        return GenericListBuilder[Event, Event.Builder]()

    action: str | None = None
    "What action was taken/failed regarding to the Regarding object."
    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    count: int | None = None
    "The number of times this event has occurred."
    event_time: Annotated[MicroTime | None, Field(alias="eventTime")] = None
    "Time when this Event was first observed."
    first_timestamp: Annotated[Time | None, Field(alias="firstTimestamp")] = None
    "The time at which the event was first recorded. (Time of server receipt is in TypeMeta.)"
    involved_object: Annotated[ObjectReference, Field(alias="involvedObject")]
    "The object that this event is about."
    kind: Literal["Event"] = "Event"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    last_timestamp: Annotated[Time | None, Field(alias="lastTimestamp")] = None
    "The time at which the most recent occurrence of this event was recorded."
    message: str | None = None
    "A human-readable description of the status of this operation."
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    reason: str | None = None
    "This should be a short, machine understandable string that gives the reason for the transition into the object\u0027s current status."
    related: ObjectReference | None = None
    "Optional secondary object for more complex actions."
    reporting_component: Annotated[str | None, Field(alias="reportingComponent")] = None
    "Name of the controller that emitted this Event, e.g. `kubernetes.io/kubelet`."
    reporting_instance: Annotated[str | None, Field(alias="reportingInstance")] = None
    "ID of the controller instance, e.g. `kubelet-xyzf`."
    series: EventSeries | None = None
    "Data about the Event series this event represents or nil if it\u0027s a singleton Event."
    source: EventSource | None = None
    "The component reporting this event. Should be a short machine understandable string."
    type: str | None = None
    "Type of this event (Normal, Warning), new types could be added in the future"


EventList = ResourceList[Event]


class FlexPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["FlexPersistentVolumeSource"]:
            return FlexPersistentVolumeSource

        def build(self) -> "FlexPersistentVolumeSource":
            return FlexPersistentVolumeSource(**self._attrs)

        def driver(self, value: str, /) -> Self:
            "driver is the name of the driver to use for this volume."
            return self._set("driver", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.'
            return self._set("fs_type", value)

        def options(self, value: dict[str, str] | None, /) -> Self:
            "options is Optional: this field holds extra command options if any."
            return self._set("options", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "FlexPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "FlexPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is Optional: SecretRef is reference to the secret object containing sensitive information to pass to the plugin scripts. This may be empty if no secret object is specified. If the secret object contains more than one secret, all secrets are passed to the plugin scripts."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

    class BuilderContext(BuilderContextBase["FlexPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = FlexPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for FlexPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["FlexPersistentVolumeSource", Builder]:
        return GenericListBuilder[FlexPersistentVolumeSource, FlexPersistentVolumeSource.Builder]()

    driver: str
    "driver is the name of the driver to use for this volume."
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the Filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.'
    options: dict[str, str] | None = None
    "options is Optional: this field holds extra command options if any."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[SecretReference | None, Field(alias="secretRef")] = None
    "secretRef is Optional: SecretRef is reference to the secret object containing sensitive information to pass to the plugin scripts. This may be empty if no secret object is specified. If the secret object contains more than one secret, all secrets are passed to the plugin scripts."


class FlexVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["FlexVolumeSource"]:
            return FlexVolumeSource

        def build(self) -> "FlexVolumeSource":
            return FlexVolumeSource(**self._attrs)

        def driver(self, value: str, /) -> Self:
            "driver is the name of the driver to use for this volume."
            return self._set("driver", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.'
            return self._set("fs_type", value)

        def options(self, value: dict[str, str] | None, /) -> Self:
            "options is Optional: this field holds extra command options if any."
            return self._set("options", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly is Optional: defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "FlexVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "FlexVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is Optional: secretRef is reference to the secret object containing sensitive information to pass to the plugin scripts. This may be empty if no secret object is specified. If the secret object contains more than one secret, all secrets are passed to the plugin scripts."
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

    class BuilderContext(BuilderContextBase["FlexVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = FlexVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for FlexVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["FlexVolumeSource", Builder]:
        return GenericListBuilder[FlexVolumeSource, FlexVolumeSource.Builder]()

    driver: str
    "driver is the name of the driver to use for this volume."
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.'
    options: dict[str, str] | None = None
    "options is Optional: this field holds extra command options if any."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly is Optional: defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef is Optional: secretRef is reference to the secret object containing sensitive information to pass to the plugin scripts. This may be empty if no secret object is specified. If the secret object contains more than one secret, all secrets are passed to the plugin scripts."


class HTTPGetAction(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["HTTPGetAction"]:
            return HTTPGetAction

        def build(self) -> "HTTPGetAction":
            return HTTPGetAction(**self._attrs)

        def host(self, value: str | None, /) -> Self:
            'Host name to connect to, defaults to the pod IP. You probably want to set "Host" in httpHeaders instead.'
            return self._set("host", value)

        @overload
        def http_headers(
            self, value_or_callback: list[HTTPHeader] | None, /
        ) -> "HTTPGetAction.Builder": ...

        @overload
        def http_headers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[HTTPHeader, HTTPHeader.Builder]],
                GenericListBuilder[HTTPHeader, HTTPHeader.Builder] | list[HTTPHeader],
            ],
            /,
        ) -> "HTTPGetAction.Builder": ...

        @overload
        def http_headers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[HTTPHeader.Builder]: ...

        def http_headers(self, value_or_callback=UNSET, /):
            "Custom headers to set in the request. HTTP allows repeated headers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[HTTPHeader.Builder]()
                context._parent_builder = self
                context._field_name = "http_headers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HTTPHeader.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("http_headers", value)

        def path(self, value: str | None, /) -> Self:
            "Path to access on the HTTP server."
            return self._set("path", value)

        @overload
        def port(self, value_or_callback: IntOrString, /) -> "HTTPGetAction.Builder": ...

        @overload
        def port(
            self,
            value_or_callback: Callable[[IntOrString.Builder], IntOrString.Builder | IntOrString],
            /,
        ) -> "HTTPGetAction.Builder": ...

        @overload
        def port(self, value_or_callback: Never = ...) -> "IntOrString.BuilderContext": ...

        def port(self, value_or_callback=UNSET, /):
            "Name or number of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME."
            if self._in_context and value_or_callback is UNSET:
                context = IntOrString.BuilderContext()
                context._parent_builder = self
                context._field_name = "port"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IntOrString.builder())
                if isinstance(output, IntOrString.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("port", value)

        def scheme(self, value: str | None, /) -> Self:
            "Scheme to use for connecting to the host. Defaults to HTTP."
            return self._set("scheme", value)

    class BuilderContext(BuilderContextBase["HTTPGetAction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = HTTPGetAction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for HTTPGetAction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["HTTPGetAction", Builder]:
        return GenericListBuilder[HTTPGetAction, HTTPGetAction.Builder]()

    host: str | None = None
    'Host name to connect to, defaults to the pod IP. You probably want to set "Host" in httpHeaders instead.'
    http_headers: Annotated[list[HTTPHeader] | None, Field(alias="httpHeaders")] = None
    "Custom headers to set in the request. HTTP allows repeated headers."
    path: str | None = None
    "Path to access on the HTTP server."
    port: IntOrString
    "Name or number of the port to access on the container. Number must be in the range 1 to 65535. Name must be an IANA_SVC_NAME."
    scheme: str | None = None
    "Scheme to use for connecting to the host. Defaults to HTTP."


class ISCSIPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ISCSIPersistentVolumeSource"]:
            return ISCSIPersistentVolumeSource

        def build(self) -> "ISCSIPersistentVolumeSource":
            return ISCSIPersistentVolumeSource(**self._attrs)

        def chap_auth_discovery(self, value: bool | None, /) -> Self:
            "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication"
            return self._set("chap_auth_discovery", value)

        def chap_auth_session(self, value: bool | None, /) -> Self:
            "chapAuthSession defines whether support iSCSI Session CHAP authentication"
            return self._set("chap_auth_session", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi'
            return self._set("fs_type", value)

        def initiator_name(self, value: str | None, /) -> Self:
            "initiatorName is the custom iSCSI Initiator Name. If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface \u003ctarget portal\u003e:\u003cvolume name\u003e will be created for the connection."
            return self._set("initiator_name", value)

        def iqn(self, value: str, /) -> Self:
            "iqn is Target iSCSI Qualified Name."
            return self._set("iqn", value)

        def iscsi_interface(self, value: str | None, /) -> Self:
            "iscsiInterface is the interface Name that uses an iSCSI transport. Defaults to \u0027default\u0027 (tcp)."
            return self._set("iscsi_interface", value)

        def lun(self, value: int, /) -> Self:
            "lun is iSCSI Target Lun number."
            return self._set("lun", value)

        def portals(self, value: list[str] | None, /) -> Self:
            "portals is the iSCSI Target Portal List. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
            return self._set("portals", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "ISCSIPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "ISCSIPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is the CHAP Secret for iSCSI target and initiator authentication"
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def target_portal(self, value: str, /) -> Self:
            "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
            return self._set("target_portal", value)

    class BuilderContext(BuilderContextBase["ISCSIPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ISCSIPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ISCSIPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ISCSIPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            ISCSIPersistentVolumeSource, ISCSIPersistentVolumeSource.Builder
        ]()

    chap_auth_discovery: Annotated[bool | None, Field(alias="chapAuthDiscovery")] = None
    "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication"
    chap_auth_session: Annotated[bool | None, Field(alias="chapAuthSession")] = None
    "chapAuthSession defines whether support iSCSI Session CHAP authentication"
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi'
    initiator_name: Annotated[str | None, Field(alias="initiatorName")] = None
    "initiatorName is the custom iSCSI Initiator Name. If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface \u003ctarget portal\u003e:\u003cvolume name\u003e will be created for the connection."
    iqn: str
    "iqn is Target iSCSI Qualified Name."
    iscsi_interface: Annotated[str | None, Field(alias="iscsiInterface")] = None
    "iscsiInterface is the interface Name that uses an iSCSI transport. Defaults to \u0027default\u0027 (tcp)."
    lun: int
    "lun is iSCSI Target Lun number."
    portals: list[str] | None = None
    "portals is the iSCSI Target Portal List. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false."
    secret_ref: Annotated[SecretReference | None, Field(alias="secretRef")] = None
    "secretRef is the CHAP Secret for iSCSI target and initiator authentication"
    target_portal: Annotated[str, Field(alias="targetPortal")]
    "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."


class ISCSIVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ISCSIVolumeSource"]:
            return ISCSIVolumeSource

        def build(self) -> "ISCSIVolumeSource":
            return ISCSIVolumeSource(**self._attrs)

        def chap_auth_discovery(self, value: bool | None, /) -> Self:
            "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication"
            return self._set("chap_auth_discovery", value)

        def chap_auth_session(self, value: bool | None, /) -> Self:
            "chapAuthSession defines whether support iSCSI Session CHAP authentication"
            return self._set("chap_auth_session", value)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi'
            return self._set("fs_type", value)

        def initiator_name(self, value: str | None, /) -> Self:
            "initiatorName is the custom iSCSI Initiator Name. If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface \u003ctarget portal\u003e:\u003cvolume name\u003e will be created for the connection."
            return self._set("initiator_name", value)

        def iqn(self, value: str, /) -> Self:
            "iqn is the target iSCSI Qualified Name."
            return self._set("iqn", value)

        def iscsi_interface(self, value: str | None, /) -> Self:
            "iscsiInterface is the interface Name that uses an iSCSI transport. Defaults to \u0027default\u0027 (tcp)."
            return self._set("iscsi_interface", value)

        def lun(self, value: int, /) -> Self:
            "lun represents iSCSI Target Lun number."
            return self._set("lun", value)

        def portals(self, value: list[str] | None, /) -> Self:
            "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
            return self._set("portals", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: LocalObjectReference | None, /
        ) -> "ISCSIVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [LocalObjectReference.Builder],
                LocalObjectReference.Builder | LocalObjectReference,
            ],
            /,
        ) -> "ISCSIVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "LocalObjectReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is the CHAP Secret for iSCSI target and initiator authentication"
            if self._in_context and value_or_callback is UNSET:
                context = LocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.builder())
                if isinstance(output, LocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def target_portal(self, value: str, /) -> Self:
            "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
            return self._set("target_portal", value)

    class BuilderContext(BuilderContextBase["ISCSIVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ISCSIVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ISCSIVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ISCSIVolumeSource", Builder]:
        return GenericListBuilder[ISCSIVolumeSource, ISCSIVolumeSource.Builder]()

    chap_auth_discovery: Annotated[bool | None, Field(alias="chapAuthDiscovery")] = None
    "chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication"
    chap_auth_session: Annotated[bool | None, Field(alias="chapAuthSession")] = None
    "chapAuthSession defines whether support iSCSI Session CHAP authentication"
    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi'
    initiator_name: Annotated[str | None, Field(alias="initiatorName")] = None
    "initiatorName is the custom iSCSI Initiator Name. If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface \u003ctarget portal\u003e:\u003cvolume name\u003e will be created for the connection."
    iqn: str
    "iqn is the target iSCSI Qualified Name."
    iscsi_interface: Annotated[str | None, Field(alias="iscsiInterface")] = None
    "iscsiInterface is the interface Name that uses an iSCSI transport. Defaults to \u0027default\u0027 (tcp)."
    lun: int
    "lun represents iSCSI Target Lun number."
    portals: list[str] | None = None
    "portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false."
    secret_ref: Annotated[LocalObjectReference | None, Field(alias="secretRef")] = None
    "secretRef is the CHAP Secret for iSCSI target and initiator authentication"
    target_portal: Annotated[str, Field(alias="targetPortal")]
    "targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port is other than default (typically TCP ports 860 and 3260)."


class LifecycleHandler(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LifecycleHandler"]:
            return LifecycleHandler

        def build(self) -> "LifecycleHandler":
            return LifecycleHandler(**self._attrs)

        @overload
        def exec(self, value_or_callback: ExecAction | None, /) -> "LifecycleHandler.Builder": ...

        @overload
        def exec(
            self,
            value_or_callback: Callable[[ExecAction.Builder], ExecAction.Builder | ExecAction],
            /,
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def exec(self, value_or_callback: Never = ...) -> "ExecAction.BuilderContext": ...

        def exec(self, value_or_callback=UNSET, /):
            "Exec specifies a command to execute in the container."
            if self._in_context and value_or_callback is UNSET:
                context = ExecAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "exec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ExecAction.builder())
                if isinstance(output, ExecAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("exec", value)

        @overload
        def http_get(
            self, value_or_callback: HTTPGetAction | None, /
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def http_get(
            self,
            value_or_callback: Callable[
                [HTTPGetAction.Builder], HTTPGetAction.Builder | HTTPGetAction
            ],
            /,
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def http_get(self, value_or_callback: Never = ...) -> "HTTPGetAction.BuilderContext": ...

        def http_get(self, value_or_callback=UNSET, /):
            "HTTPGet specifies an HTTP GET request to perform."
            if self._in_context and value_or_callback is UNSET:
                context = HTTPGetAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "http_get"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HTTPGetAction.builder())
                if isinstance(output, HTTPGetAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("http_get", value)

        @overload
        def sleep(self, value_or_callback: SleepAction | None, /) -> "LifecycleHandler.Builder": ...

        @overload
        def sleep(
            self,
            value_or_callback: Callable[[SleepAction.Builder], SleepAction.Builder | SleepAction],
            /,
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def sleep(self, value_or_callback: Never = ...) -> "SleepAction.BuilderContext": ...

        def sleep(self, value_or_callback=UNSET, /):
            "Sleep represents a duration that the container should sleep."
            if self._in_context and value_or_callback is UNSET:
                context = SleepAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "sleep"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SleepAction.builder())
                if isinstance(output, SleepAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("sleep", value)

        @overload
        def tcp_socket(
            self, value_or_callback: TCPSocketAction | None, /
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def tcp_socket(
            self,
            value_or_callback: Callable[
                [TCPSocketAction.Builder], TCPSocketAction.Builder | TCPSocketAction
            ],
            /,
        ) -> "LifecycleHandler.Builder": ...

        @overload
        def tcp_socket(
            self, value_or_callback: Never = ...
        ) -> "TCPSocketAction.BuilderContext": ...

        def tcp_socket(self, value_or_callback=UNSET, /):
            "Deprecated. TCPSocket is NOT supported as a LifecycleHandler and kept for backward compatibility. There is no validation of this field and lifecycle hooks will fail at runtime when it is specified."
            if self._in_context and value_or_callback is UNSET:
                context = TCPSocketAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "tcp_socket"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TCPSocketAction.builder())
                if isinstance(output, TCPSocketAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("tcp_socket", value)

    class BuilderContext(BuilderContextBase["LifecycleHandler.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LifecycleHandler.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LifecycleHandler."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LifecycleHandler", Builder]:
        return GenericListBuilder[LifecycleHandler, LifecycleHandler.Builder]()

    exec: ExecAction | None = None
    "Exec specifies a command to execute in the container."
    http_get: Annotated[HTTPGetAction | None, Field(alias="httpGet")] = None
    "HTTPGet specifies an HTTP GET request to perform."
    sleep: SleepAction | None = None
    "Sleep represents a duration that the container should sleep."
    tcp_socket: Annotated[TCPSocketAction | None, Field(alias="tcpSocket")] = None
    "Deprecated. TCPSocket is NOT supported as a LifecycleHandler and kept for backward compatibility. There is no validation of this field and lifecycle hooks will fail at runtime when it is specified."


class LimitRange(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "limitranges",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LimitRange"]:
            return LimitRange

        def build(self) -> "LimitRange":
            return LimitRange(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["LimitRange"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "LimitRange.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "LimitRange.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: LimitRangeSpec | None, /) -> "LimitRange.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [LimitRangeSpec.Builder], LimitRangeSpec.Builder | LimitRangeSpec
            ],
            /,
        ) -> "LimitRange.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "LimitRangeSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the limits enforced. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = LimitRangeSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LimitRangeSpec.builder())
                if isinstance(output, LimitRangeSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["LimitRange.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LimitRange.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LimitRange."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LimitRange", Builder]:
        return GenericListBuilder[LimitRange, LimitRange.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["LimitRange"] = "LimitRange"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: LimitRangeSpec | None = None
    "Spec defines the limits enforced. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


LimitRangeList = ResourceList[LimitRange]


class LoadBalancerIngress(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LoadBalancerIngress"]:
            return LoadBalancerIngress

        def build(self) -> "LoadBalancerIngress":
            return LoadBalancerIngress(**self._attrs)

        def hostname(self, value: str | None, /) -> Self:
            "Hostname is set for load-balancer ingress points that are DNS based (typically AWS load-balancers)"
            return self._set("hostname", value)

        def ip(self, value: str | None, /) -> Self:
            "IP is set for load-balancer ingress points that are IP based (typically GCE or OpenStack load-balancers)"
            return self._set("ip", value)

        def ip_mode(self, value: str | None, /) -> Self:
            'IPMode specifies how the load-balancer IP behaves, and may only be specified when the ip field is specified. Setting this to "VIP" indicates that traffic is delivered to the node with the destination set to the load-balancer\u0027s IP and port. Setting this to "Proxy" indicates that traffic is delivered to the node or pod with the destination set to the node\u0027s IP and node port or the pod\u0027s IP and port. Service implementations may use this information to adjust traffic routing.'
            return self._set("ip_mode", value)

        @overload
        def ports(
            self, value_or_callback: list[PortStatus] | None, /
        ) -> "LoadBalancerIngress.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PortStatus, PortStatus.Builder]],
                GenericListBuilder[PortStatus, PortStatus.Builder] | list[PortStatus],
            ],
            /,
        ) -> "LoadBalancerIngress.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PortStatus.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            "Ports is a list of records of service ports If used, every port defined in the service should have an entry in it"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PortStatus.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PortStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

    class BuilderContext(BuilderContextBase["LoadBalancerIngress.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LoadBalancerIngress.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LoadBalancerIngress."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LoadBalancerIngress", Builder]:
        return GenericListBuilder[LoadBalancerIngress, LoadBalancerIngress.Builder]()

    hostname: str | None = None
    "Hostname is set for load-balancer ingress points that are DNS based (typically AWS load-balancers)"
    ip: str | None = None
    "IP is set for load-balancer ingress points that are IP based (typically GCE or OpenStack load-balancers)"
    ip_mode: Annotated[str | None, Field(alias="ipMode")] = None
    'IPMode specifies how the load-balancer IP behaves, and may only be specified when the ip field is specified. Setting this to "VIP" indicates that traffic is delivered to the node with the destination set to the load-balancer\u0027s IP and port. Setting this to "Proxy" indicates that traffic is delivered to the node or pod with the destination set to the node\u0027s IP and node port or the pod\u0027s IP and port. Service implementations may use this information to adjust traffic routing.'
    ports: list[PortStatus] | None = None
    "Ports is a list of records of service ports If used, every port defined in the service should have an entry in it"


class LoadBalancerStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["LoadBalancerStatus"]:
            return LoadBalancerStatus

        def build(self) -> "LoadBalancerStatus":
            return LoadBalancerStatus(**self._attrs)

        @overload
        def ingress(
            self, value_or_callback: list[LoadBalancerIngress] | None, /
        ) -> "LoadBalancerStatus.Builder": ...

        @overload
        def ingress(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[LoadBalancerIngress, LoadBalancerIngress.Builder]],
                GenericListBuilder[LoadBalancerIngress, LoadBalancerIngress.Builder]
                | list[LoadBalancerIngress],
            ],
            /,
        ) -> "LoadBalancerStatus.Builder": ...

        @overload
        def ingress(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[LoadBalancerIngress.Builder]: ...

        def ingress(self, value_or_callback=UNSET, /):
            "Ingress is a list containing ingress points for the load-balancer. Traffic intended for the service should be sent to these ingress points."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[LoadBalancerIngress.Builder]()
                context._parent_builder = self
                context._field_name = "ingress"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LoadBalancerIngress.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ingress", value)

    class BuilderContext(BuilderContextBase["LoadBalancerStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = LoadBalancerStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for LoadBalancerStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["LoadBalancerStatus", Builder]:
        return GenericListBuilder[LoadBalancerStatus, LoadBalancerStatus.Builder]()

    ingress: list[LoadBalancerIngress] | None = None
    "Ingress is a list containing ingress points for the load-balancer. Traffic intended for the service should be sent to these ingress points."


class Namespace(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "namespaces",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Namespace"]:
            return Namespace

        def build(self) -> "Namespace":
            return Namespace(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Namespace"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Namespace.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Namespace.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: NamespaceSpec | None, /) -> "Namespace.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [NamespaceSpec.Builder], NamespaceSpec.Builder | NamespaceSpec
            ],
            /,
        ) -> "Namespace.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "NamespaceSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the behavior of the Namespace. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = NamespaceSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NamespaceSpec.builder())
                if isinstance(output, NamespaceSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: NamespaceStatus | None, /) -> "Namespace.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [NamespaceStatus.Builder], NamespaceStatus.Builder | NamespaceStatus
            ],
            /,
        ) -> "Namespace.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "NamespaceStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Status describes the current status of a Namespace. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = NamespaceStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NamespaceStatus.builder())
                if isinstance(output, NamespaceStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Namespace.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Namespace.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Namespace."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Namespace", Builder]:
        return GenericListBuilder[Namespace, Namespace.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Namespace"] = "Namespace"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: NamespaceSpec | None = None
    "Spec defines the behavior of the Namespace. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: NamespaceStatus | None = None
    "Status describes the current status of a Namespace. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


NamespaceList = ResourceList[Namespace]


class NodeRuntimeHandler(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeRuntimeHandler"]:
            return NodeRuntimeHandler

        def build(self) -> "NodeRuntimeHandler":
            return NodeRuntimeHandler(**self._attrs)

        @overload
        def features(
            self, value_or_callback: NodeRuntimeHandlerFeatures | None, /
        ) -> "NodeRuntimeHandler.Builder": ...

        @overload
        def features(
            self,
            value_or_callback: Callable[
                [NodeRuntimeHandlerFeatures.Builder],
                NodeRuntimeHandlerFeatures.Builder | NodeRuntimeHandlerFeatures,
            ],
            /,
        ) -> "NodeRuntimeHandler.Builder": ...

        @overload
        def features(
            self, value_or_callback: Never = ...
        ) -> "NodeRuntimeHandlerFeatures.BuilderContext": ...

        def features(self, value_or_callback=UNSET, /):
            "Supported features."
            if self._in_context and value_or_callback is UNSET:
                context = NodeRuntimeHandlerFeatures.BuilderContext()
                context._parent_builder = self
                context._field_name = "features"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeRuntimeHandlerFeatures.builder())
                if isinstance(output, NodeRuntimeHandlerFeatures.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("features", value)

        def name(self, value: str | None, /) -> Self:
            "Runtime handler name. Empty for the default runtime handler."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["NodeRuntimeHandler.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeRuntimeHandler.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeRuntimeHandler."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeRuntimeHandler", Builder]:
        return GenericListBuilder[NodeRuntimeHandler, NodeRuntimeHandler.Builder]()

    features: NodeRuntimeHandlerFeatures | None = None
    "Supported features."
    name: str | None = None
    "Runtime handler name. Empty for the default runtime handler."


class NodeSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSelector"]:
            return NodeSelector

        def build(self) -> "NodeSelector":
            return NodeSelector(**self._attrs)

        @overload
        def node_selector_terms(
            self, value_or_callback: list[NodeSelectorTerm], /
        ) -> "NodeSelector.Builder": ...

        @overload
        def node_selector_terms(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeSelectorTerm, NodeSelectorTerm.Builder]],
                GenericListBuilder[NodeSelectorTerm, NodeSelectorTerm.Builder]
                | list[NodeSelectorTerm],
            ],
            /,
        ) -> "NodeSelector.Builder": ...

        @overload
        def node_selector_terms(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeSelectorTerm.Builder]: ...

        def node_selector_terms(self, value_or_callback=UNSET, /):
            "Required. A list of node selector terms. The terms are ORed."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeSelectorTerm.Builder]()
                context._parent_builder = self
                context._field_name = "node_selector_terms"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelectorTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_selector_terms", value)

    class BuilderContext(BuilderContextBase["NodeSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSelector", Builder]:
        return GenericListBuilder[NodeSelector, NodeSelector.Builder]()

    node_selector_terms: Annotated[list[NodeSelectorTerm], Field(alias="nodeSelectorTerms")]
    "Required. A list of node selector terms. The terms are ORed."


class NodeSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeSpec"]:
            return NodeSpec

        def build(self) -> "NodeSpec":
            return NodeSpec(**self._attrs)

        @overload
        def config_source(
            self, value_or_callback: NodeConfigSource | None, /
        ) -> "NodeSpec.Builder": ...

        @overload
        def config_source(
            self,
            value_or_callback: Callable[
                [NodeConfigSource.Builder], NodeConfigSource.Builder | NodeConfigSource
            ],
            /,
        ) -> "NodeSpec.Builder": ...

        @overload
        def config_source(
            self, value_or_callback: Never = ...
        ) -> "NodeConfigSource.BuilderContext": ...

        def config_source(self, value_or_callback=UNSET, /):
            "Deprecated: Previously used to specify the source of the node\u0027s configuration for the DynamicKubeletConfig feature. This feature is removed."
            if self._in_context and value_or_callback is UNSET:
                context = NodeConfigSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_source"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeConfigSource.builder())
                if isinstance(output, NodeConfigSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_source", value)

        def external_id(self, value: str | None, /) -> Self:
            "Deprecated. Not all kubelets will set this field. Remove field after 1.13. see: https://issues.k8s.io/61966"
            return self._set("external_id", value)

        def pod_cidr(self, value: str | None, /) -> Self:
            "PodCIDR represents the pod IP range assigned to the node."
            return self._set("pod_cidr", value)

        def pod_cid_rs(self, value: list[str] | None, /) -> Self:
            "podCIDRs represents the IP ranges assigned to the node for usage by Pods on that node. If this field is specified, the 0th entry must match the podCIDR field. It may contain at most 1 value for each of IPv4 and IPv6."
            return self._set("pod_cid_rs", value)

        def provider_id(self, value: str | None, /) -> Self:
            "ID of the node assigned by the cloud provider in the format: \u003cProviderName\u003e://\u003cProviderSpecificNodeID\u003e"
            return self._set("provider_id", value)

        @overload
        def taints(self, value_or_callback: list[Taint] | None, /) -> "NodeSpec.Builder": ...

        @overload
        def taints(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Taint, Taint.Builder]],
                GenericListBuilder[Taint, Taint.Builder] | list[Taint],
            ],
            /,
        ) -> "NodeSpec.Builder": ...

        @overload
        def taints(self, value_or_callback: Never = ...) -> ListBuilderContext[Taint.Builder]: ...

        def taints(self, value_or_callback=UNSET, /):
            "If specified, the node\u0027s taints."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Taint.Builder]()
                context._parent_builder = self
                context._field_name = "taints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Taint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("taints", value)

        def unschedulable(self, value: bool | None, /) -> Self:
            "Unschedulable controls node schedulability of new pods. By default, node is schedulable. More info: https://kubernetes.io/docs/concepts/nodes/node/#manual-node-administration"
            return self._set("unschedulable", value)

    class BuilderContext(BuilderContextBase["NodeSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeSpec", Builder]:
        return GenericListBuilder[NodeSpec, NodeSpec.Builder]()

    config_source: Annotated[NodeConfigSource | None, Field(alias="configSource")] = None
    "Deprecated: Previously used to specify the source of the node\u0027s configuration for the DynamicKubeletConfig feature. This feature is removed."
    external_id: Annotated[str | None, Field(alias="externalID")] = None
    "Deprecated. Not all kubelets will set this field. Remove field after 1.13. see: https://issues.k8s.io/61966"
    pod_cidr: Annotated[str | None, Field(alias="podCIDR")] = None
    "PodCIDR represents the pod IP range assigned to the node."
    pod_cid_rs: Annotated[list[str] | None, Field(alias="podCIDRs")] = None
    "podCIDRs represents the IP ranges assigned to the node for usage by Pods on that node. If this field is specified, the 0th entry must match the podCIDR field. It may contain at most 1 value for each of IPv4 and IPv6."
    provider_id: Annotated[str | None, Field(alias="providerID")] = None
    "ID of the node assigned by the cloud provider in the format: \u003cProviderName\u003e://\u003cProviderSpecificNodeID\u003e"
    taints: list[Taint] | None = None
    "If specified, the node\u0027s taints."
    unschedulable: bool | None = None
    "Unschedulable controls node schedulability of new pods. By default, node is schedulable. More info: https://kubernetes.io/docs/concepts/nodes/node/#manual-node-administration"


class NodeStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeStatus"]:
            return NodeStatus

        def build(self) -> "NodeStatus":
            return NodeStatus(**self._attrs)

        @overload
        def addresses(
            self, value_or_callback: list[NodeAddress] | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def addresses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeAddress, NodeAddress.Builder]],
                GenericListBuilder[NodeAddress, NodeAddress.Builder] | list[NodeAddress],
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def addresses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeAddress.Builder]: ...

        def addresses(self, value_or_callback=UNSET, /):
            "List of addresses reachable to the node. Queried from cloud provider, if available. More info: https://kubernetes.io/docs/reference/node/node-status/#addresses Note: This field is declared as mergeable, but the merge key is not sufficiently unique, which can cause data corruption when it is merged. Callers should instead use a full-replacement patch. See https://pr.k8s.io/79391 for an example. Consumers should assume that addresses can change during the lifetime of a Node. However, there are some exceptions where this may not be possible, such as Pods that inherit a Node\u0027s address in its own status or consumers of the downward API (status.hostIP)."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeAddress.Builder]()
                context._parent_builder = self
                context._field_name = "addresses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeAddress.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("addresses", value)

        def allocatable(self, value: dict[str, Quantity] | None, /) -> Self:
            "Allocatable represents the resources of a node that are available for scheduling. Defaults to Capacity."
            return self._set("allocatable", value)

        def capacity(self, value: dict[str, Quantity] | None, /) -> Self:
            "Capacity represents the total resources of a node. More info: https://kubernetes.io/docs/reference/node/node-status/#capacity"
            return self._set("capacity", value)

        @overload
        def conditions(
            self, value_or_callback: list[NodeCondition] | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeCondition, NodeCondition.Builder]],
                GenericListBuilder[NodeCondition, NodeCondition.Builder] | list[NodeCondition],
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions is an array of current observed node conditions. More info: https://kubernetes.io/docs/reference/node/node-status/#condition"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def config(self, value_or_callback: NodeConfigStatus | None, /) -> "NodeStatus.Builder": ...

        @overload
        def config(
            self,
            value_or_callback: Callable[
                [NodeConfigStatus.Builder], NodeConfigStatus.Builder | NodeConfigStatus
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def config(self, value_or_callback: Never = ...) -> "NodeConfigStatus.BuilderContext": ...

        def config(self, value_or_callback=UNSET, /):
            "Status of the config assigned to the node via the dynamic Kubelet config feature."
            if self._in_context and value_or_callback is UNSET:
                context = NodeConfigStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeConfigStatus.builder())
                if isinstance(output, NodeConfigStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config", value)

        @overload
        def daemon_endpoints(
            self, value_or_callback: NodeDaemonEndpoints | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def daemon_endpoints(
            self,
            value_or_callback: Callable[
                [NodeDaemonEndpoints.Builder],
                NodeDaemonEndpoints.Builder | NodeDaemonEndpoints,
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def daemon_endpoints(
            self, value_or_callback: Never = ...
        ) -> "NodeDaemonEndpoints.BuilderContext": ...

        def daemon_endpoints(self, value_or_callback=UNSET, /):
            "Endpoints of daemons running on the Node."
            if self._in_context and value_or_callback is UNSET:
                context = NodeDaemonEndpoints.BuilderContext()
                context._parent_builder = self
                context._field_name = "daemon_endpoints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeDaemonEndpoints.builder())
                if isinstance(output, NodeDaemonEndpoints.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("daemon_endpoints", value)

        @overload
        def features(self, value_or_callback: NodeFeatures | None, /) -> "NodeStatus.Builder": ...

        @overload
        def features(
            self,
            value_or_callback: Callable[
                [NodeFeatures.Builder], NodeFeatures.Builder | NodeFeatures
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def features(self, value_or_callback: Never = ...) -> "NodeFeatures.BuilderContext": ...

        def features(self, value_or_callback=UNSET, /):
            "Features describes the set of features implemented by the CRI implementation."
            if self._in_context and value_or_callback is UNSET:
                context = NodeFeatures.BuilderContext()
                context._parent_builder = self
                context._field_name = "features"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeFeatures.builder())
                if isinstance(output, NodeFeatures.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("features", value)

        @overload
        def images(
            self, value_or_callback: list[ContainerImage] | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def images(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerImage, ContainerImage.Builder]],
                GenericListBuilder[ContainerImage, ContainerImage.Builder] | list[ContainerImage],
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def images(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerImage.Builder]: ...

        def images(self, value_or_callback=UNSET, /):
            "List of container images on this node"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerImage.Builder]()
                context._parent_builder = self
                context._field_name = "images"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerImage.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("images", value)

        @overload
        def node_info(
            self, value_or_callback: NodeSystemInfo | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def node_info(
            self,
            value_or_callback: Callable[
                [NodeSystemInfo.Builder], NodeSystemInfo.Builder | NodeSystemInfo
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def node_info(self, value_or_callback: Never = ...) -> "NodeSystemInfo.BuilderContext": ...

        def node_info(self, value_or_callback=UNSET, /):
            "Set of ids/uuids to uniquely identify the node. More info: https://kubernetes.io/docs/reference/node/node-status/#info"
            if self._in_context and value_or_callback is UNSET:
                context = NodeSystemInfo.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_info"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSystemInfo.builder())
                if isinstance(output, NodeSystemInfo.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_info", value)

        def phase(self, value: str | None, /) -> Self:
            "NodePhase is the recently observed lifecycle phase of the node. More info: https://kubernetes.io/docs/concepts/nodes/node/#phase The field is never populated, and now is deprecated."
            return self._set("phase", value)

        @overload
        def runtime_handlers(
            self, value_or_callback: list[NodeRuntimeHandler] | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def runtime_handlers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[NodeRuntimeHandler, NodeRuntimeHandler.Builder]],
                GenericListBuilder[NodeRuntimeHandler, NodeRuntimeHandler.Builder]
                | list[NodeRuntimeHandler],
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def runtime_handlers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[NodeRuntimeHandler.Builder]: ...

        def runtime_handlers(self, value_or_callback=UNSET, /):
            "The available runtime handlers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[NodeRuntimeHandler.Builder]()
                context._parent_builder = self
                context._field_name = "runtime_handlers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeRuntimeHandler.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("runtime_handlers", value)

        @overload
        def volumes_attached(
            self, value_or_callback: list[AttachedVolume] | None, /
        ) -> "NodeStatus.Builder": ...

        @overload
        def volumes_attached(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[AttachedVolume, AttachedVolume.Builder]],
                GenericListBuilder[AttachedVolume, AttachedVolume.Builder] | list[AttachedVolume],
            ],
            /,
        ) -> "NodeStatus.Builder": ...

        @overload
        def volumes_attached(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[AttachedVolume.Builder]: ...

        def volumes_attached(self, value_or_callback=UNSET, /):
            "List of volumes that are attached to the node."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[AttachedVolume.Builder]()
                context._parent_builder = self
                context._field_name = "volumes_attached"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AttachedVolume.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volumes_attached", value)

        def volumes_in_use(self, value: list[str] | None, /) -> Self:
            "List of attachable volumes in use (mounted) by the node."
            return self._set("volumes_in_use", value)

    class BuilderContext(BuilderContextBase["NodeStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeStatus", Builder]:
        return GenericListBuilder[NodeStatus, NodeStatus.Builder]()

    addresses: list[NodeAddress] | None = None
    "List of addresses reachable to the node. Queried from cloud provider, if available. More info: https://kubernetes.io/docs/reference/node/node-status/#addresses Note: This field is declared as mergeable, but the merge key is not sufficiently unique, which can cause data corruption when it is merged. Callers should instead use a full-replacement patch. See https://pr.k8s.io/79391 for an example. Consumers should assume that addresses can change during the lifetime of a Node. However, there are some exceptions where this may not be possible, such as Pods that inherit a Node\u0027s address in its own status or consumers of the downward API (status.hostIP)."
    allocatable: dict[str, Quantity] | None = None
    "Allocatable represents the resources of a node that are available for scheduling. Defaults to Capacity."
    capacity: dict[str, Quantity] | None = None
    "Capacity represents the total resources of a node. More info: https://kubernetes.io/docs/reference/node/node-status/#capacity"
    conditions: list[NodeCondition] | None = None
    "Conditions is an array of current observed node conditions. More info: https://kubernetes.io/docs/reference/node/node-status/#condition"
    config: NodeConfigStatus | None = None
    "Status of the config assigned to the node via the dynamic Kubelet config feature."
    daemon_endpoints: Annotated[NodeDaemonEndpoints | None, Field(alias="daemonEndpoints")] = None
    "Endpoints of daemons running on the Node."
    features: NodeFeatures | None = None
    "Features describes the set of features implemented by the CRI implementation."
    images: list[ContainerImage] | None = None
    "List of container images on this node"
    node_info: Annotated[NodeSystemInfo | None, Field(alias="nodeInfo")] = None
    "Set of ids/uuids to uniquely identify the node. More info: https://kubernetes.io/docs/reference/node/node-status/#info"
    phase: str | None = None
    "NodePhase is the recently observed lifecycle phase of the node. More info: https://kubernetes.io/docs/concepts/nodes/node/#phase The field is never populated, and now is deprecated."
    runtime_handlers: Annotated[list[NodeRuntimeHandler] | None, Field(alias="runtimeHandlers")] = (
        None
    )
    "The available runtime handlers."
    volumes_attached: Annotated[list[AttachedVolume] | None, Field(alias="volumesAttached")] = None
    "List of volumes that are attached to the node."
    volumes_in_use: Annotated[list[str] | None, Field(alias="volumesInUse")] = None
    "List of attachable volumes in use (mounted) by the node."


class PersistentVolumeClaimSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaimSpec"]:
            return PersistentVolumeClaimSpec

        def build(self) -> "PersistentVolumeClaimSpec":
            return PersistentVolumeClaimSpec(**self._attrs)

        def access_modes(self, value: list[str] | None, /) -> Self:
            "accessModes contains the desired access modes the volume should have. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1"
            return self._set("access_modes", value)

        @overload
        def data_source(
            self, value_or_callback: TypedLocalObjectReference | None, /
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def data_source(
            self,
            value_or_callback: Callable[
                [TypedLocalObjectReference.Builder],
                TypedLocalObjectReference.Builder | TypedLocalObjectReference,
            ],
            /,
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def data_source(
            self, value_or_callback: Never = ...
        ) -> "TypedLocalObjectReference.BuilderContext": ...

        def data_source(self, value_or_callback=UNSET, /):
            "dataSource field can be used to specify either: * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot) * An existing PVC (PersistentVolumeClaim) If the provisioner or an external controller can support the specified data source, it will create a new volume based on the contents of the specified data source. When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef, and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified. If the namespace is specified, then dataSourceRef will not be copied to dataSource."
            if self._in_context and value_or_callback is UNSET:
                context = TypedLocalObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "data_source"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TypedLocalObjectReference.builder())
                if isinstance(output, TypedLocalObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("data_source", value)

        @overload
        def data_source_ref(
            self, value_or_callback: TypedObjectReference | None, /
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def data_source_ref(
            self,
            value_or_callback: Callable[
                [TypedObjectReference.Builder],
                TypedObjectReference.Builder | TypedObjectReference,
            ],
            /,
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def data_source_ref(
            self, value_or_callback: Never = ...
        ) -> "TypedObjectReference.BuilderContext": ...

        def data_source_ref(self, value_or_callback=UNSET, /):
            "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty volume is desired. This may be any object from a non-empty API group (non core object) or a PersistentVolumeClaim object. When this field is specified, volume binding will only succeed if the type of the specified object matches some installed volume populator or dynamic provisioner. This field will replace the functionality of the dataSource field and as such if both fields are non-empty, they must have the same value. For backwards compatibility, when namespace isn\u0027t specified in dataSourceRef, both fields (dataSource and dataSourceRef) will be set to the same value automatically if one of them is empty and the other is non-empty. When namespace is specified in dataSourceRef, dataSource isn\u0027t set to the same value and must be empty. There are three important differences between dataSource and dataSourceRef: * While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled. (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled."
            if self._in_context and value_or_callback is UNSET:
                context = TypedObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "data_source_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TypedObjectReference.builder())
                if isinstance(output, TypedObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("data_source_ref", value)

        @overload
        def resources(
            self, value_or_callback: VolumeResourceRequirements | None, /
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [VolumeResourceRequirements.Builder],
                VolumeResourceRequirements.Builder | VolumeResourceRequirements,
            ],
            /,
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> "VolumeResourceRequirements.BuilderContext": ...

        def resources(self, value_or_callback=UNSET, /):
            "resources represents the minimum resources the volume should have. If RecoverVolumeExpansionFailure feature is enabled users are allowed to specify resource requirements that are lower than previous value but must still be higher than capacity recorded in the status field of the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources"
            if self._in_context and value_or_callback is UNSET:
                context = VolumeResourceRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeResourceRequirements.builder())
                if isinstance(output, VolumeResourceRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

        @overload
        def selector(
            self, value_or_callback: LabelSelector | None, /
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def selector(
            self,
            value_or_callback: Callable[
                [LabelSelector.Builder], LabelSelector.Builder | LabelSelector
            ],
            /,
        ) -> "PersistentVolumeClaimSpec.Builder": ...

        @overload
        def selector(self, value_or_callback: Never = ...) -> "LabelSelector.BuilderContext": ...

        def selector(self, value_or_callback=UNSET, /):
            "selector is a label query over volumes to consider for binding."
            if self._in_context and value_or_callback is UNSET:
                context = LabelSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LabelSelector.builder())
                if isinstance(output, LabelSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("selector", value)

        def storage_class_name(self, value: str | None, /) -> Self:
            "storageClassName is the name of the StorageClass required by the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1"
            return self._set("storage_class_name", value)

        def volume_attributes_class_name(self, value: str | None, /) -> Self:
            "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim. If specified, the CSI driver will create or update the volume with the attributes defined in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName, it can be changed after the claim is created. An empty string or nil value indicates that no VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state, this field can be reset to its previous value (including nil) to cancel the modification. If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource exists. More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/"
            return self._set("volume_attributes_class_name", value)

        def volume_mode(self, value: str | None, /) -> Self:
            "volumeMode defines what type of volume is required by the claim. Value of Filesystem is implied when not included in claim spec."
            return self._set("volume_mode", value)

        def volume_name(self, value: str | None, /) -> Self:
            "volumeName is the binding reference to the PersistentVolume backing this claim."
            return self._set("volume_name", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaimSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaimSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaimSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolumeClaimSpec", Builder]:
        return GenericListBuilder[PersistentVolumeClaimSpec, PersistentVolumeClaimSpec.Builder]()

    access_modes: Annotated[list[str] | None, Field(alias="accessModes")] = None
    "accessModes contains the desired access modes the volume should have. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1"
    data_source: Annotated[TypedLocalObjectReference | None, Field(alias="dataSource")] = None
    "dataSource field can be used to specify either: * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot) * An existing PVC (PersistentVolumeClaim) If the provisioner or an external controller can support the specified data source, it will create a new volume based on the contents of the specified data source. When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef, and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified. If the namespace is specified, then dataSourceRef will not be copied to dataSource."
    data_source_ref: Annotated[TypedObjectReference | None, Field(alias="dataSourceRef")] = None
    "dataSourceRef specifies the object from which to populate the volume with data, if a non-empty volume is desired. This may be any object from a non-empty API group (non core object) or a PersistentVolumeClaim object. When this field is specified, volume binding will only succeed if the type of the specified object matches some installed volume populator or dynamic provisioner. This field will replace the functionality of the dataSource field and as such if both fields are non-empty, they must have the same value. For backwards compatibility, when namespace isn\u0027t specified in dataSourceRef, both fields (dataSource and dataSourceRef) will be set to the same value automatically if one of them is empty and the other is non-empty. When namespace is specified in dataSourceRef, dataSource isn\u0027t set to the same value and must be empty. There are three important differences between dataSource and dataSourceRef: * While dataSource only allows two specific types of objects, dataSourceRef\n  allows any non-core object, as well as PersistentVolumeClaim objects.\n* While dataSource ignores disallowed values (dropping them), dataSourceRef\n  preserves all values, and generates an error if a disallowed value is\n  specified.\n* While dataSource only allows local objects, dataSourceRef allows objects\n  in any namespaces.\n(Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled. (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled."
    resources: VolumeResourceRequirements | None = None
    "resources represents the minimum resources the volume should have. If RecoverVolumeExpansionFailure feature is enabled users are allowed to specify resource requirements that are lower than previous value but must still be higher than capacity recorded in the status field of the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources"
    selector: LabelSelector | None = None
    "selector is a label query over volumes to consider for binding."
    storage_class_name: Annotated[str | None, Field(alias="storageClassName")] = None
    "storageClassName is the name of the StorageClass required by the claim. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1"
    volume_attributes_class_name: Annotated[
        str | None, Field(alias="volumeAttributesClassName")
    ] = None
    "volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim. If specified, the CSI driver will create or update the volume with the attributes defined in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName, it can be changed after the claim is created. An empty string or nil value indicates that no VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state, this field can be reset to its previous value (including nil) to cancel the modification. If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource exists. More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/"
    volume_mode: Annotated[str | None, Field(alias="volumeMode")] = None
    "volumeMode defines what type of volume is required by the claim. Value of Filesystem is implied when not included in claim spec."
    volume_name: Annotated[str | None, Field(alias="volumeName")] = None
    "volumeName is the binding reference to the PersistentVolume backing this claim."


class PersistentVolumeClaimTemplate(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaimTemplate"]:
            return PersistentVolumeClaimTemplate

        def build(self) -> "PersistentVolumeClaimTemplate":
            return PersistentVolumeClaimTemplate(**self._attrs)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "PersistentVolumeClaimTemplate.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PersistentVolumeClaimTemplate.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "May contain labels and annotations that will be copied into the PVC when creating it. No other fields are allowed and will be rejected during validation."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: PersistentVolumeClaimSpec, /
        ) -> "PersistentVolumeClaimTemplate.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [PersistentVolumeClaimSpec.Builder],
                PersistentVolumeClaimSpec.Builder | PersistentVolumeClaimSpec,
            ],
            /,
        ) -> "PersistentVolumeClaimTemplate.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeClaimSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "The specification for the PersistentVolumeClaim. The entire content is copied unchanged into the PVC that gets created from this template. The same fields as in a PersistentVolumeClaim are also valid here."
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeClaimSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimSpec.builder())
                if isinstance(output, PersistentVolumeClaimSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaimTemplate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaimTemplate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaimTemplate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["PersistentVolumeClaimTemplate", Builder]:
        return GenericListBuilder[
            PersistentVolumeClaimTemplate, PersistentVolumeClaimTemplate.Builder
        ]()

    metadata: ObjectMeta | None = None
    "May contain labels and annotations that will be copied into the PVC when creating it. No other fields are allowed and will be rejected during validation."
    spec: PersistentVolumeClaimSpec
    "The specification for the PersistentVolumeClaim. The entire content is copied unchanged into the PVC that gets created from this template. The same fields as in a PersistentVolumeClaim are also valid here."


class PodAffinity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodAffinity"]:
            return PodAffinity

        def build(self) -> "PodAffinity":
            return PodAffinity(**self._attrs)

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: list[WeightedPodAffinityTerm] | None, /
        ) -> "PodAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[WeightedPodAffinityTerm, WeightedPodAffinityTerm.Builder]],
                GenericListBuilder[WeightedPodAffinityTerm, WeightedPodAffinityTerm.Builder]
                | list[WeightedPodAffinityTerm],
            ],
            /,
        ) -> "PodAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[WeightedPodAffinityTerm.Builder]: ...

        def preferred_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            'The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[WeightedPodAffinityTerm.Builder]()
                context._parent_builder = self
                context._field_name = "preferred_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WeightedPodAffinityTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("preferred_during_scheduling_ignored_during_execution", value)

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: list[PodAffinityTerm] | None, /
        ) -> "PodAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodAffinityTerm, PodAffinityTerm.Builder]],
                GenericListBuilder[PodAffinityTerm, PodAffinityTerm.Builder]
                | list[PodAffinityTerm],
            ],
            /,
        ) -> "PodAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodAffinityTerm.Builder]: ...

        def required_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            "If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodAffinityTerm.Builder]()
                context._parent_builder = self
                context._field_name = "required_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodAffinityTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("required_during_scheduling_ignored_during_execution", value)

    class BuilderContext(BuilderContextBase["PodAffinity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodAffinity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodAffinity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodAffinity", Builder]:
        return GenericListBuilder[PodAffinity, PodAffinity.Builder]()

    preferred_during_scheduling_ignored_during_execution: Annotated[
        list[WeightedPodAffinityTerm] | None,
        Field(alias="preferredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    'The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.'
    required_during_scheduling_ignored_during_execution: Annotated[
        list[PodAffinityTerm] | None,
        Field(alias="requiredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    "If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied."


class PodAntiAffinity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodAntiAffinity"]:
            return PodAntiAffinity

        def build(self) -> "PodAntiAffinity":
            return PodAntiAffinity(**self._attrs)

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: list[WeightedPodAffinityTerm] | None, /
        ) -> "PodAntiAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[WeightedPodAffinityTerm, WeightedPodAffinityTerm.Builder]],
                GenericListBuilder[WeightedPodAffinityTerm, WeightedPodAffinityTerm.Builder]
                | list[WeightedPodAffinityTerm],
            ],
            /,
        ) -> "PodAntiAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[WeightedPodAffinityTerm.Builder]: ...

        def preferred_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            'The scheduler will prefer to schedule pods to nodes that satisfy the anti-affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling anti-affinity expressions, etc.), compute a sum by iterating through the elements of this field and subtracting "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[WeightedPodAffinityTerm.Builder]()
                context._parent_builder = self
                context._field_name = "preferred_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WeightedPodAffinityTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("preferred_during_scheduling_ignored_during_execution", value)

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: list[PodAffinityTerm] | None, /
        ) -> "PodAntiAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodAffinityTerm, PodAffinityTerm.Builder]],
                GenericListBuilder[PodAffinityTerm, PodAffinityTerm.Builder]
                | list[PodAffinityTerm],
            ],
            /,
        ) -> "PodAntiAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodAffinityTerm.Builder]: ...

        def required_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            "If the anti-affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the anti-affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodAffinityTerm.Builder]()
                context._parent_builder = self
                context._field_name = "required_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodAffinityTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("required_during_scheduling_ignored_during_execution", value)

    class BuilderContext(BuilderContextBase["PodAntiAffinity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodAntiAffinity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodAntiAffinity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodAntiAffinity", Builder]:
        return GenericListBuilder[PodAntiAffinity, PodAntiAffinity.Builder]()

    preferred_during_scheduling_ignored_during_execution: Annotated[
        list[WeightedPodAffinityTerm] | None,
        Field(alias="preferredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    'The scheduler will prefer to schedule pods to nodes that satisfy the anti-affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling anti-affinity expressions, etc.), compute a sum by iterating through the elements of this field and subtracting "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.'
    required_during_scheduling_ignored_during_execution: Annotated[
        list[PodAffinityTerm] | None,
        Field(alias="requiredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    "If the anti-affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the anti-affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied."


class PodDNSConfig(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodDNSConfig"]:
            return PodDNSConfig

        def build(self) -> "PodDNSConfig":
            return PodDNSConfig(**self._attrs)

        def nameservers(self, value: list[str] | None, /) -> Self:
            "A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed."
            return self._set("nameservers", value)

        @overload
        def options(
            self, value_or_callback: list[PodDNSConfigOption] | None, /
        ) -> "PodDNSConfig.Builder": ...

        @overload
        def options(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodDNSConfigOption, PodDNSConfigOption.Builder]],
                GenericListBuilder[PodDNSConfigOption, PodDNSConfigOption.Builder]
                | list[PodDNSConfigOption],
            ],
            /,
        ) -> "PodDNSConfig.Builder": ...

        @overload
        def options(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodDNSConfigOption.Builder]: ...

        def options(self, value_or_callback=UNSET, /):
            "A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodDNSConfigOption.Builder]()
                context._parent_builder = self
                context._field_name = "options"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodDNSConfigOption.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("options", value)

        def searches(self, value: list[str] | None, /) -> Self:
            "A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed."
            return self._set("searches", value)

    class BuilderContext(BuilderContextBase["PodDNSConfig.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodDNSConfig.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodDNSConfig."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodDNSConfig", Builder]:
        return GenericListBuilder[PodDNSConfig, PodDNSConfig.Builder]()

    nameservers: list[str] | None = None
    "A list of DNS name server IP addresses. This will be appended to the base nameservers generated from DNSPolicy. Duplicated nameservers will be removed."
    options: list[PodDNSConfigOption] | None = None
    "A list of DNS resolver options. This will be merged with the base options generated from DNSPolicy. Duplicated entries will be removed. Resolution options given in Options will override those that appear in the base DNSPolicy."
    searches: list[str] | None = None
    "A list of DNS search domains for host-name lookup. This will be appended to the base search paths generated from DNSPolicy. Duplicated search paths will be removed."


class PodSecurityContext(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodSecurityContext"]:
            return PodSecurityContext

        def build(self) -> "PodSecurityContext":
            return PodSecurityContext(**self._attrs)

        @overload
        def app_armor_profile(
            self, value_or_callback: AppArmorProfile | None, /
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def app_armor_profile(
            self,
            value_or_callback: Callable[
                [AppArmorProfile.Builder], AppArmorProfile.Builder | AppArmorProfile
            ],
            /,
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def app_armor_profile(
            self, value_or_callback: Never = ...
        ) -> "AppArmorProfile.BuilderContext": ...

        def app_armor_profile(self, value_or_callback=UNSET, /):
            "appArmorProfile is the AppArmor options to use by the containers in this pod. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = AppArmorProfile.BuilderContext()
                context._parent_builder = self
                context._field_name = "app_armor_profile"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AppArmorProfile.builder())
                if isinstance(output, AppArmorProfile.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("app_armor_profile", value)

        def fs_group(self, value: int | None, /) -> Self:
            "A special supplemental group that applies to all containers in a pod. Some volume types allow the Kubelet to change the ownership of that volume to be owned by the pod:\n\n1. The owning GID will be the FSGroup 2. The setgid bit is set (new files created in the volume will be owned by FSGroup) 3. The permission bits are OR\u0027d with rw-rw----\n\nIf unset, the Kubelet will not modify the ownership and permissions of any volume. Note that this field cannot be set when spec.os.name is windows."
            return self._set("fs_group", value)

        def fs_group_change_policy(self, value: str | None, /) -> Self:
            'fsGroupChangePolicy defines behavior of changing ownership and permission of the volume before being exposed inside Pod. This field will only apply to volume types which support fsGroup based ownership(and permissions). It will have no effect on ephemeral volume types such as: secret, configmaps and emptydir. Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used. Note that this field cannot be set when spec.os.name is windows.'
            return self._set("fs_group_change_policy", value)

        def run_as_group(self, value: int | None, /) -> Self:
            "The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
            return self._set("run_as_group", value)

        def run_as_non_root(self, value: bool | None, /) -> Self:
            "Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."
            return self._set("run_as_non_root", value)

        def run_as_user(self, value: int | None, /) -> Self:
            "The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
            return self._set("run_as_user", value)

        def se_linux_change_policy(self, value: str | None, /) -> Self:
            'seLinuxChangePolicy defines how the container\u0027s SELinux label is applied to all volumes used by the Pod. It has no effect on nodes that do not support SELinux or to volumes does not support SELinux. Valid values are "MountOption" and "Recursive".\n\n"Recursive" means relabeling of all files on all Pod volumes by the container runtime. This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node.\n\n"MountOption" mounts all eligible Pod volumes with `-o context` mount option. This requires all Pods that share the same volume to use the same SELinux label. It is not possible to share the same volume among privileged and unprivileged Pods. Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their CSIDriver instance. Other volumes are always re-labelled recursively. "MountOption" value is allowed only when SELinuxMount feature gate is enabled.\n\nIf not specified and SELinuxMount feature gate is enabled, "MountOption" is used. If not specified and SELinuxMount feature gate is disabled, "MountOption" is used for ReadWriteOncePod volumes and "Recursive" for all other volumes.\n\nThis field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers.\n\nAll Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state. Note that this field cannot be set when spec.os.name is windows.'
            return self._set("se_linux_change_policy", value)

        @overload
        def se_linux_options(
            self, value_or_callback: SELinuxOptions | None, /
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def se_linux_options(
            self,
            value_or_callback: Callable[
                [SELinuxOptions.Builder], SELinuxOptions.Builder | SELinuxOptions
            ],
            /,
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def se_linux_options(
            self, value_or_callback: Never = ...
        ) -> "SELinuxOptions.BuilderContext": ...

        def se_linux_options(self, value_or_callback=UNSET, /):
            "The SELinux context to be applied to all containers. If unspecified, the container runtime will allocate a random SELinux context for each container.  May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = SELinuxOptions.BuilderContext()
                context._parent_builder = self
                context._field_name = "se_linux_options"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SELinuxOptions.builder())
                if isinstance(output, SELinuxOptions.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("se_linux_options", value)

        @overload
        def seccomp_profile(
            self, value_or_callback: SeccompProfile | None, /
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def seccomp_profile(
            self,
            value_or_callback: Callable[
                [SeccompProfile.Builder], SeccompProfile.Builder | SeccompProfile
            ],
            /,
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def seccomp_profile(
            self, value_or_callback: Never = ...
        ) -> "SeccompProfile.BuilderContext": ...

        def seccomp_profile(self, value_or_callback=UNSET, /):
            "The seccomp options to use by the containers in this pod. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = SeccompProfile.BuilderContext()
                context._parent_builder = self
                context._field_name = "seccomp_profile"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SeccompProfile.builder())
                if isinstance(output, SeccompProfile.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("seccomp_profile", value)

        def supplemental_groups(self, value: list[int] | None, /) -> Self:
            "A list of groups applied to the first process run in each container, in addition to the container\u0027s primary GID and fsGroup (if specified).  If the SupplementalGroupsPolicy feature is enabled, the supplementalGroupsPolicy field determines whether these are in addition to or instead of any group memberships defined in the container image. If unspecified, no additional groups are added, though group memberships defined in the container image may still be used, depending on the supplementalGroupsPolicy field. Note that this field cannot be set when spec.os.name is windows."
            return self._set("supplemental_groups", value)

        def supplemental_groups_policy(self, value: str | None, /) -> Self:
            'Defines how supplemental groups of the first container processes are calculated. Valid values are "Merge" and "Strict". If not specified, "Merge" is used. (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled and the container runtime must implement support for this feature. Note that this field cannot be set when spec.os.name is windows.'
            return self._set("supplemental_groups_policy", value)

        @overload
        def sysctls(
            self, value_or_callback: list[Sysctl] | None, /
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def sysctls(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Sysctl, Sysctl.Builder]],
                GenericListBuilder[Sysctl, Sysctl.Builder] | list[Sysctl],
            ],
            /,
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def sysctls(self, value_or_callback: Never = ...) -> ListBuilderContext[Sysctl.Builder]: ...

        def sysctls(self, value_or_callback=UNSET, /):
            "Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported sysctls (by the container runtime) might fail to launch. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Sysctl.Builder]()
                context._parent_builder = self
                context._field_name = "sysctls"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Sysctl.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("sysctls", value)

        @overload
        def windows_options(
            self, value_or_callback: WindowsSecurityContextOptions | None, /
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def windows_options(
            self,
            value_or_callback: Callable[
                [WindowsSecurityContextOptions.Builder],
                WindowsSecurityContextOptions.Builder | WindowsSecurityContextOptions,
            ],
            /,
        ) -> "PodSecurityContext.Builder": ...

        @overload
        def windows_options(
            self, value_or_callback: Never = ...
        ) -> "WindowsSecurityContextOptions.BuilderContext": ...

        def windows_options(self, value_or_callback=UNSET, /):
            "The Windows specific settings applied to all containers. If unspecified, the options within a container\u0027s SecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux."
            if self._in_context and value_or_callback is UNSET:
                context = WindowsSecurityContextOptions.BuilderContext()
                context._parent_builder = self
                context._field_name = "windows_options"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WindowsSecurityContextOptions.builder())
                if isinstance(output, WindowsSecurityContextOptions.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("windows_options", value)

    class BuilderContext(BuilderContextBase["PodSecurityContext.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodSecurityContext.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodSecurityContext."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodSecurityContext", Builder]:
        return GenericListBuilder[PodSecurityContext, PodSecurityContext.Builder]()

    app_armor_profile: Annotated[AppArmorProfile | None, Field(alias="appArmorProfile")] = None
    "appArmorProfile is the AppArmor options to use by the containers in this pod. Note that this field cannot be set when spec.os.name is windows."
    fs_group: Annotated[int | None, Field(alias="fsGroup")] = None
    "A special supplemental group that applies to all containers in a pod. Some volume types allow the Kubelet to change the ownership of that volume to be owned by the pod:\n\n1. The owning GID will be the FSGroup 2. The setgid bit is set (new files created in the volume will be owned by FSGroup) 3. The permission bits are OR\u0027d with rw-rw----\n\nIf unset, the Kubelet will not modify the ownership and permissions of any volume. Note that this field cannot be set when spec.os.name is windows."
    fs_group_change_policy: Annotated[str | None, Field(alias="fsGroupChangePolicy")] = None
    'fsGroupChangePolicy defines behavior of changing ownership and permission of the volume before being exposed inside Pod. This field will only apply to volume types which support fsGroup based ownership(and permissions). It will have no effect on ephemeral volume types such as: secret, configmaps and emptydir. Valid values are "OnRootMismatch" and "Always". If not specified, "Always" is used. Note that this field cannot be set when spec.os.name is windows.'
    run_as_group: Annotated[int | None, Field(alias="runAsGroup")] = None
    "The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
    run_as_non_root: Annotated[bool | None, Field(alias="runAsNonRoot")] = None
    "Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."
    run_as_user: Annotated[int | None, Field(alias="runAsUser")] = None
    "The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
    se_linux_change_policy: Annotated[str | None, Field(alias="seLinuxChangePolicy")] = None
    'seLinuxChangePolicy defines how the container\u0027s SELinux label is applied to all volumes used by the Pod. It has no effect on nodes that do not support SELinux or to volumes does not support SELinux. Valid values are "MountOption" and "Recursive".\n\n"Recursive" means relabeling of all files on all Pod volumes by the container runtime. This may be slow for large volumes, but allows mixing privileged and unprivileged Pods sharing the same volume on the same node.\n\n"MountOption" mounts all eligible Pod volumes with `-o context` mount option. This requires all Pods that share the same volume to use the same SELinux label. It is not possible to share the same volume among privileged and unprivileged Pods. Eligible volumes are in-tree FibreChannel and iSCSI volumes, and all CSI volumes whose CSI driver announces SELinux support by setting spec.seLinuxMount: true in their CSIDriver instance. Other volumes are always re-labelled recursively. "MountOption" value is allowed only when SELinuxMount feature gate is enabled.\n\nIf not specified and SELinuxMount feature gate is enabled, "MountOption" is used. If not specified and SELinuxMount feature gate is disabled, "MountOption" is used for ReadWriteOncePod volumes and "Recursive" for all other volumes.\n\nThis field affects only Pods that have SELinux label set, either in PodSecurityContext or in SecurityContext of all containers.\n\nAll Pods that use the same volume should use the same seLinuxChangePolicy, otherwise some pods can get stuck in ContainerCreating state. Note that this field cannot be set when spec.os.name is windows.'
    se_linux_options: Annotated[SELinuxOptions | None, Field(alias="seLinuxOptions")] = None
    "The SELinux context to be applied to all containers. If unspecified, the container runtime will allocate a random SELinux context for each container.  May also be set in SecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence for that container. Note that this field cannot be set when spec.os.name is windows."
    seccomp_profile: Annotated[SeccompProfile | None, Field(alias="seccompProfile")] = None
    "The seccomp options to use by the containers in this pod. Note that this field cannot be set when spec.os.name is windows."
    supplemental_groups: Annotated[list[int] | None, Field(alias="supplementalGroups")] = None
    "A list of groups applied to the first process run in each container, in addition to the container\u0027s primary GID and fsGroup (if specified).  If the SupplementalGroupsPolicy feature is enabled, the supplementalGroupsPolicy field determines whether these are in addition to or instead of any group memberships defined in the container image. If unspecified, no additional groups are added, though group memberships defined in the container image may still be used, depending on the supplementalGroupsPolicy field. Note that this field cannot be set when spec.os.name is windows."
    supplemental_groups_policy: Annotated[str | None, Field(alias="supplementalGroupsPolicy")] = (
        None
    )
    'Defines how supplemental groups of the first container processes are calculated. Valid values are "Merge" and "Strict". If not specified, "Merge" is used. (Alpha) Using the field requires the SupplementalGroupsPolicy feature gate to be enabled and the container runtime must implement support for this feature. Note that this field cannot be set when spec.os.name is windows.'
    sysctls: list[Sysctl] | None = None
    "Sysctls hold a list of namespaced sysctls used for the pod. Pods with unsupported sysctls (by the container runtime) might fail to launch. Note that this field cannot be set when spec.os.name is windows."
    windows_options: Annotated[
        WindowsSecurityContextOptions | None, Field(alias="windowsOptions")
    ] = None
    "The Windows specific settings applied to all containers. If unspecified, the options within a container\u0027s SecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux."


class Probe(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Probe"]:
            return Probe

        def build(self) -> "Probe":
            return Probe(**self._attrs)

        @overload
        def exec(self, value_or_callback: ExecAction | None, /) -> "Probe.Builder": ...

        @overload
        def exec(
            self,
            value_or_callback: Callable[[ExecAction.Builder], ExecAction.Builder | ExecAction],
            /,
        ) -> "Probe.Builder": ...

        @overload
        def exec(self, value_or_callback: Never = ...) -> "ExecAction.BuilderContext": ...

        def exec(self, value_or_callback=UNSET, /):
            "Exec specifies a command to execute in the container."
            if self._in_context and value_or_callback is UNSET:
                context = ExecAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "exec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ExecAction.builder())
                if isinstance(output, ExecAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("exec", value)

        def failure_threshold(self, value: int | None, /) -> Self:
            "Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1."
            return self._set("failure_threshold", value)

        @overload
        def grpc(self, value_or_callback: GRPCAction | None, /) -> "Probe.Builder": ...

        @overload
        def grpc(
            self,
            value_or_callback: Callable[[GRPCAction.Builder], GRPCAction.Builder | GRPCAction],
            /,
        ) -> "Probe.Builder": ...

        @overload
        def grpc(self, value_or_callback: Never = ...) -> "GRPCAction.BuilderContext": ...

        def grpc(self, value_or_callback=UNSET, /):
            "GRPC specifies a GRPC HealthCheckRequest."
            if self._in_context and value_or_callback is UNSET:
                context = GRPCAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "grpc"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GRPCAction.builder())
                if isinstance(output, GRPCAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("grpc", value)

        @overload
        def http_get(self, value_or_callback: HTTPGetAction | None, /) -> "Probe.Builder": ...

        @overload
        def http_get(
            self,
            value_or_callback: Callable[
                [HTTPGetAction.Builder], HTTPGetAction.Builder | HTTPGetAction
            ],
            /,
        ) -> "Probe.Builder": ...

        @overload
        def http_get(self, value_or_callback: Never = ...) -> "HTTPGetAction.BuilderContext": ...

        def http_get(self, value_or_callback=UNSET, /):
            "HTTPGet specifies an HTTP GET request to perform."
            if self._in_context and value_or_callback is UNSET:
                context = HTTPGetAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "http_get"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HTTPGetAction.builder())
                if isinstance(output, HTTPGetAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("http_get", value)

        def initial_delay_seconds(self, value: int | None, /) -> Self:
            "Number of seconds after the container has started before liveness probes are initiated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
            return self._set("initial_delay_seconds", value)

        def period_seconds(self, value: int | None, /) -> Self:
            "How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1."
            return self._set("period_seconds", value)

        def success_threshold(self, value: int | None, /) -> Self:
            "Minimum consecutive successes for the probe to be considered successful after having failed. Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1."
            return self._set("success_threshold", value)

        @overload
        def tcp_socket(self, value_or_callback: TCPSocketAction | None, /) -> "Probe.Builder": ...

        @overload
        def tcp_socket(
            self,
            value_or_callback: Callable[
                [TCPSocketAction.Builder], TCPSocketAction.Builder | TCPSocketAction
            ],
            /,
        ) -> "Probe.Builder": ...

        @overload
        def tcp_socket(
            self, value_or_callback: Never = ...
        ) -> "TCPSocketAction.BuilderContext": ...

        def tcp_socket(self, value_or_callback=UNSET, /):
            "TCPSocket specifies a connection to a TCP port."
            if self._in_context and value_or_callback is UNSET:
                context = TCPSocketAction.BuilderContext()
                context._parent_builder = self
                context._field_name = "tcp_socket"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TCPSocketAction.builder())
                if isinstance(output, TCPSocketAction.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("tcp_socket", value)

        def termination_grace_period_seconds(self, value: int | None, /) -> Self:
            "Optional duration in seconds the pod needs to terminate gracefully upon probe failure. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. If this value is nil, the pod\u0027s terminationGracePeriodSeconds will be used. Otherwise, this value overrides the value provided by the pod spec. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate. Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset."
            return self._set("termination_grace_period_seconds", value)

        def timeout_seconds(self, value: int | None, /) -> Self:
            "Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
            return self._set("timeout_seconds", value)

    class BuilderContext(BuilderContextBase["Probe.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Probe.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Probe."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Probe", Builder]:
        return GenericListBuilder[Probe, Probe.Builder]()

    exec: ExecAction | None = None
    "Exec specifies a command to execute in the container."
    failure_threshold: Annotated[int | None, Field(alias="failureThreshold")] = None
    "Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1."
    grpc: GRPCAction | None = None
    "GRPC specifies a GRPC HealthCheckRequest."
    http_get: Annotated[HTTPGetAction | None, Field(alias="httpGet")] = None
    "HTTPGet specifies an HTTP GET request to perform."
    initial_delay_seconds: Annotated[int | None, Field(alias="initialDelaySeconds")] = None
    "Number of seconds after the container has started before liveness probes are initiated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
    period_seconds: Annotated[int | None, Field(alias="periodSeconds")] = None
    "How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1."
    success_threshold: Annotated[int | None, Field(alias="successThreshold")] = None
    "Minimum consecutive successes for the probe to be considered successful after having failed. Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1."
    tcp_socket: Annotated[TCPSocketAction | None, Field(alias="tcpSocket")] = None
    "TCPSocket specifies a connection to a TCP port."
    termination_grace_period_seconds: Annotated[
        int | None, Field(alias="terminationGracePeriodSeconds")
    ] = None
    "Optional duration in seconds the pod needs to terminate gracefully upon probe failure. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. If this value is nil, the pod\u0027s terminationGracePeriodSeconds will be used. Otherwise, this value overrides the value provided by the pod spec. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate. Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset."
    timeout_seconds: Annotated[int | None, Field(alias="timeoutSeconds")] = None
    "Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"


class RBDPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["RBDPersistentVolumeSource"]:
            return RBDPersistentVolumeSource

        def build(self) -> "RBDPersistentVolumeSource":
            return RBDPersistentVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd'
            return self._set("fs_type", value)

        def image(self, value: str, /) -> Self:
            "image is the rados image name. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("image", value)

        def keyring(self, value: str | None, /) -> Self:
            "keyring is the path to key ring for RBDUser. Default is /etc/ceph/keyring. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("keyring", value)

        def monitors(self, value: list[str], /) -> Self:
            "monitors is a collection of Ceph monitors. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("monitors", value)

        def pool(self, value: str | None, /) -> Self:
            "pool is the rados pool name. Default is rbd. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("pool", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference | None, /
        ) -> "RBDPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "RBDPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef is name of the authentication secret for RBDUser. If provided overrides keyring. Default is nil. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def user(self, value: str | None, /) -> Self:
            "user is the rados user name. Default is admin. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
            return self._set("user", value)

    class BuilderContext(BuilderContextBase["RBDPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = RBDPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for RBDPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["RBDPersistentVolumeSource", Builder]:
        return GenericListBuilder[RBDPersistentVolumeSource, RBDPersistentVolumeSource.Builder]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type of the volume that you want to mount. Tip: Ensure that the filesystem type is supported by the host operating system. Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified. More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd'
    image: str
    "image is the rados image name. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    keyring: str | None = None
    "keyring is the path to key ring for RBDUser. Default is /etc/ceph/keyring. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    monitors: list[str]
    "monitors is a collection of Ceph monitors. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    pool: str | None = None
    "pool is the rados pool name. Default is rbd. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly here will force the ReadOnly setting in VolumeMounts. Defaults to false. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    secret_ref: Annotated[SecretReference | None, Field(alias="secretRef")] = None
    "secretRef is name of the authentication secret for RBDUser. If provided overrides keyring. Default is nil. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"
    user: str | None = None
    "user is the rados user name. Default is admin. More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it"


class ScaleIOPersistentVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ScaleIOPersistentVolumeSource"]:
            return ScaleIOPersistentVolumeSource

        def build(self) -> "ScaleIOPersistentVolumeSource":
            return ScaleIOPersistentVolumeSource(**self._attrs)

        def fs_type(self, value: str | None, /) -> Self:
            'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Default is "xfs"'
            return self._set("fs_type", value)

        def gateway(self, value: str, /) -> Self:
            "gateway is the host address of the ScaleIO API Gateway."
            return self._set("gateway", value)

        def protection_domain(self, value: str | None, /) -> Self:
            "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage."
            return self._set("protection_domain", value)

        def read_only(self, value: bool | None, /) -> Self:
            "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
            return self._set("read_only", value)

        @overload
        def secret_ref(
            self, value_or_callback: SecretReference, /
        ) -> "ScaleIOPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self,
            value_or_callback: Callable[
                [SecretReference.Builder], SecretReference.Builder | SecretReference
            ],
            /,
        ) -> "ScaleIOPersistentVolumeSource.Builder": ...

        @overload
        def secret_ref(
            self, value_or_callback: Never = ...
        ) -> "SecretReference.BuilderContext": ...

        def secret_ref(self, value_or_callback=UNSET, /):
            "secretRef references to the secret for ScaleIO user and other sensitive information. If this is not provided, Login operation will fail."
            if self._in_context and value_or_callback is UNSET:
                context = SecretReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretReference.builder())
                if isinstance(output, SecretReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret_ref", value)

        def ssl_enabled(self, value: bool | None, /) -> Self:
            "sslEnabled is the flag to enable/disable SSL communication with Gateway, default false"
            return self._set("ssl_enabled", value)

        def storage_mode(self, value: str | None, /) -> Self:
            "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned. Default is ThinProvisioned."
            return self._set("storage_mode", value)

        def storage_pool(self, value: str | None, /) -> Self:
            "storagePool is the ScaleIO Storage Pool associated with the protection domain."
            return self._set("storage_pool", value)

        def system(self, value: str, /) -> Self:
            "system is the name of the storage system as configured in ScaleIO."
            return self._set("system", value)

        def volume_name(self, value: str | None, /) -> Self:
            "volumeName is the name of a volume already created in the ScaleIO system that is associated with this volume source."
            return self._set("volume_name", value)

    class BuilderContext(BuilderContextBase["ScaleIOPersistentVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ScaleIOPersistentVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ScaleIOPersistentVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ScaleIOPersistentVolumeSource", Builder]:
        return GenericListBuilder[
            ScaleIOPersistentVolumeSource, ScaleIOPersistentVolumeSource.Builder
        ]()

    fs_type: Annotated[str | None, Field(alias="fsType")] = None
    'fsType is the filesystem type to mount. Must be a filesystem type supported by the host operating system. Ex. "ext4", "xfs", "ntfs". Default is "xfs"'
    gateway: str
    "gateway is the host address of the ScaleIO API Gateway."
    protection_domain: Annotated[str | None, Field(alias="protectionDomain")] = None
    "protectionDomain is the name of the ScaleIO Protection Domain for the configured storage."
    read_only: Annotated[bool | None, Field(alias="readOnly")] = None
    "readOnly defaults to false (read/write). ReadOnly here will force the ReadOnly setting in VolumeMounts."
    secret_ref: Annotated[SecretReference, Field(alias="secretRef")]
    "secretRef references to the secret for ScaleIO user and other sensitive information. If this is not provided, Login operation will fail."
    ssl_enabled: Annotated[bool | None, Field(alias="sslEnabled")] = None
    "sslEnabled is the flag to enable/disable SSL communication with Gateway, default false"
    storage_mode: Annotated[str | None, Field(alias="storageMode")] = None
    "storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned. Default is ThinProvisioned."
    storage_pool: Annotated[str | None, Field(alias="storagePool")] = None
    "storagePool is the ScaleIO Storage Pool associated with the protection domain."
    system: str
    "system is the name of the storage system as configured in ScaleIO."
    volume_name: Annotated[str | None, Field(alias="volumeName")] = None
    "volumeName is the name of a volume already created in the ScaleIO system that is associated with this volume source."


class ScopeSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ScopeSelector"]:
            return ScopeSelector

        def build(self) -> "ScopeSelector":
            return ScopeSelector(**self._attrs)

        @overload
        def match_expressions(
            self, value_or_callback: list[ScopedResourceSelectorRequirement] | None, /
        ) -> "ScopeSelector.Builder": ...

        @overload
        def match_expressions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        ScopedResourceSelectorRequirement,
                        ScopedResourceSelectorRequirement.Builder,
                    ]
                ],
                GenericListBuilder[
                    ScopedResourceSelectorRequirement,
                    ScopedResourceSelectorRequirement.Builder,
                ]
                | list[ScopedResourceSelectorRequirement],
            ],
            /,
        ) -> "ScopeSelector.Builder": ...

        @overload
        def match_expressions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ScopedResourceSelectorRequirement.Builder]: ...

        def match_expressions(self, value_or_callback=UNSET, /):
            "A list of scope selector requirements by scope of the resources."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ScopedResourceSelectorRequirement.Builder]()
                context._parent_builder = self
                context._field_name = "match_expressions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ScopedResourceSelectorRequirement.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("match_expressions", value)

    class BuilderContext(BuilderContextBase["ScopeSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ScopeSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ScopeSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ScopeSelector", Builder]:
        return GenericListBuilder[ScopeSelector, ScopeSelector.Builder]()

    match_expressions: Annotated[
        list[ScopedResourceSelectorRequirement] | None, Field(alias="matchExpressions")
    ] = None
    "A list of scope selector requirements by scope of the resources."


class SecurityContext(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["SecurityContext"]:
            return SecurityContext

        def build(self) -> "SecurityContext":
            return SecurityContext(**self._attrs)

        def allow_privilege_escalation(self, value: bool | None, /) -> Self:
            "AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. This bool directly controls if the no_new_privs flag will be set on the container process. AllowPrivilegeEscalation is true always when the container is: 1) run as Privileged 2) has CAP_SYS_ADMIN Note that this field cannot be set when spec.os.name is windows."
            return self._set("allow_privilege_escalation", value)

        @overload
        def app_armor_profile(
            self, value_or_callback: AppArmorProfile | None, /
        ) -> "SecurityContext.Builder": ...

        @overload
        def app_armor_profile(
            self,
            value_or_callback: Callable[
                [AppArmorProfile.Builder], AppArmorProfile.Builder | AppArmorProfile
            ],
            /,
        ) -> "SecurityContext.Builder": ...

        @overload
        def app_armor_profile(
            self, value_or_callback: Never = ...
        ) -> "AppArmorProfile.BuilderContext": ...

        def app_armor_profile(self, value_or_callback=UNSET, /):
            "appArmorProfile is the AppArmor options to use by this container. If set, this profile overrides the pod\u0027s appArmorProfile. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = AppArmorProfile.BuilderContext()
                context._parent_builder = self
                context._field_name = "app_armor_profile"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AppArmorProfile.builder())
                if isinstance(output, AppArmorProfile.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("app_armor_profile", value)

        @overload
        def capabilities(
            self, value_or_callback: Capabilities | None, /
        ) -> "SecurityContext.Builder": ...

        @overload
        def capabilities(
            self,
            value_or_callback: Callable[
                [Capabilities.Builder], Capabilities.Builder | Capabilities
            ],
            /,
        ) -> "SecurityContext.Builder": ...

        @overload
        def capabilities(self, value_or_callback: Never = ...) -> "Capabilities.BuilderContext": ...

        def capabilities(self, value_or_callback=UNSET, /):
            "The capabilities to add/drop when running containers. Defaults to the default set of capabilities granted by the container runtime. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = Capabilities.BuilderContext()
                context._parent_builder = self
                context._field_name = "capabilities"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Capabilities.builder())
                if isinstance(output, Capabilities.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("capabilities", value)

        def privileged(self, value: bool | None, /) -> Self:
            "Run container in privileged mode. Processes in privileged containers are essentially equivalent to root on the host. Defaults to false. Note that this field cannot be set when spec.os.name is windows."
            return self._set("privileged", value)

        def proc_mount(self, value: str | None, /) -> Self:
            "procMount denotes the type of proc mount to use for the containers. The default value is Default which uses the container runtime defaults for readonly paths and masked paths. This requires the ProcMountType feature flag to be enabled. Note that this field cannot be set when spec.os.name is windows."
            return self._set("proc_mount", value)

        def read_only_root_filesystem(self, value: bool | None, /) -> Self:
            "Whether this container has a read-only root filesystem. Default is false. Note that this field cannot be set when spec.os.name is windows."
            return self._set("read_only_root_filesystem", value)

        def run_as_group(self, value: int | None, /) -> Self:
            "The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
            return self._set("run_as_group", value)

        def run_as_non_root(self, value: bool | None, /) -> Self:
            "Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."
            return self._set("run_as_non_root", value)

        def run_as_user(self, value: int | None, /) -> Self:
            "The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
            return self._set("run_as_user", value)

        @overload
        def se_linux_options(
            self, value_or_callback: SELinuxOptions | None, /
        ) -> "SecurityContext.Builder": ...

        @overload
        def se_linux_options(
            self,
            value_or_callback: Callable[
                [SELinuxOptions.Builder], SELinuxOptions.Builder | SELinuxOptions
            ],
            /,
        ) -> "SecurityContext.Builder": ...

        @overload
        def se_linux_options(
            self, value_or_callback: Never = ...
        ) -> "SELinuxOptions.BuilderContext": ...

        def se_linux_options(self, value_or_callback=UNSET, /):
            "The SELinux context to be applied to the container. If unspecified, the container runtime will allocate a random SELinux context for each container.  May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = SELinuxOptions.BuilderContext()
                context._parent_builder = self
                context._field_name = "se_linux_options"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SELinuxOptions.builder())
                if isinstance(output, SELinuxOptions.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("se_linux_options", value)

        @overload
        def seccomp_profile(
            self, value_or_callback: SeccompProfile | None, /
        ) -> "SecurityContext.Builder": ...

        @overload
        def seccomp_profile(
            self,
            value_or_callback: Callable[
                [SeccompProfile.Builder], SeccompProfile.Builder | SeccompProfile
            ],
            /,
        ) -> "SecurityContext.Builder": ...

        @overload
        def seccomp_profile(
            self, value_or_callback: Never = ...
        ) -> "SeccompProfile.BuilderContext": ...

        def seccomp_profile(self, value_or_callback=UNSET, /):
            "The seccomp options to use by this container. If seccomp options are provided at both the pod \u0026 container level, the container options override the pod options. Note that this field cannot be set when spec.os.name is windows."
            if self._in_context and value_or_callback is UNSET:
                context = SeccompProfile.BuilderContext()
                context._parent_builder = self
                context._field_name = "seccomp_profile"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SeccompProfile.builder())
                if isinstance(output, SeccompProfile.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("seccomp_profile", value)

        @overload
        def windows_options(
            self, value_or_callback: WindowsSecurityContextOptions | None, /
        ) -> "SecurityContext.Builder": ...

        @overload
        def windows_options(
            self,
            value_or_callback: Callable[
                [WindowsSecurityContextOptions.Builder],
                WindowsSecurityContextOptions.Builder | WindowsSecurityContextOptions,
            ],
            /,
        ) -> "SecurityContext.Builder": ...

        @overload
        def windows_options(
            self, value_or_callback: Never = ...
        ) -> "WindowsSecurityContextOptions.BuilderContext": ...

        def windows_options(self, value_or_callback=UNSET, /):
            "The Windows specific settings applied to all containers. If unspecified, the options from the PodSecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux."
            if self._in_context and value_or_callback is UNSET:
                context = WindowsSecurityContextOptions.BuilderContext()
                context._parent_builder = self
                context._field_name = "windows_options"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(WindowsSecurityContextOptions.builder())
                if isinstance(output, WindowsSecurityContextOptions.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("windows_options", value)

    class BuilderContext(BuilderContextBase["SecurityContext.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = SecurityContext.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for SecurityContext."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["SecurityContext", Builder]:
        return GenericListBuilder[SecurityContext, SecurityContext.Builder]()

    allow_privilege_escalation: Annotated[bool | None, Field(alias="allowPrivilegeEscalation")] = (
        None
    )
    "AllowPrivilegeEscalation controls whether a process can gain more privileges than its parent process. This bool directly controls if the no_new_privs flag will be set on the container process. AllowPrivilegeEscalation is true always when the container is: 1) run as Privileged 2) has CAP_SYS_ADMIN Note that this field cannot be set when spec.os.name is windows."
    app_armor_profile: Annotated[AppArmorProfile | None, Field(alias="appArmorProfile")] = None
    "appArmorProfile is the AppArmor options to use by this container. If set, this profile overrides the pod\u0027s appArmorProfile. Note that this field cannot be set when spec.os.name is windows."
    capabilities: Capabilities | None = None
    "The capabilities to add/drop when running containers. Defaults to the default set of capabilities granted by the container runtime. Note that this field cannot be set when spec.os.name is windows."
    privileged: bool | None = None
    "Run container in privileged mode. Processes in privileged containers are essentially equivalent to root on the host. Defaults to false. Note that this field cannot be set when spec.os.name is windows."
    proc_mount: Annotated[str | None, Field(alias="procMount")] = None
    "procMount denotes the type of proc mount to use for the containers. The default value is Default which uses the container runtime defaults for readonly paths and masked paths. This requires the ProcMountType feature flag to be enabled. Note that this field cannot be set when spec.os.name is windows."
    read_only_root_filesystem: Annotated[bool | None, Field(alias="readOnlyRootFilesystem")] = None
    "Whether this container has a read-only root filesystem. Default is false. Note that this field cannot be set when spec.os.name is windows."
    run_as_group: Annotated[int | None, Field(alias="runAsGroup")] = None
    "The GID to run the entrypoint of the container process. Uses runtime default if unset. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
    run_as_non_root: Annotated[bool | None, Field(alias="runAsNonRoot")] = None
    "Indicates that the container must run as a non-root user. If true, the Kubelet will validate the image at runtime to ensure that it does not run as UID 0 (root) and fail to start the container if it does. If unset or false, no such validation will be performed. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence."
    run_as_user: Annotated[int | None, Field(alias="runAsUser")] = None
    "The UID to run the entrypoint of the container process. Defaults to user specified in image metadata if unspecified. May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
    se_linux_options: Annotated[SELinuxOptions | None, Field(alias="seLinuxOptions")] = None
    "The SELinux context to be applied to the container. If unspecified, the container runtime will allocate a random SELinux context for each container.  May also be set in PodSecurityContext.  If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is windows."
    seccomp_profile: Annotated[SeccompProfile | None, Field(alias="seccompProfile")] = None
    "The seccomp options to use by this container. If seccomp options are provided at both the pod \u0026 container level, the container options override the pod options. Note that this field cannot be set when spec.os.name is windows."
    windows_options: Annotated[
        WindowsSecurityContextOptions | None, Field(alias="windowsOptions")
    ] = None
    "The Windows specific settings applied to all containers. If unspecified, the options from the PodSecurityContext will be used. If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence. Note that this field cannot be set when spec.os.name is linux."


class ServiceSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServiceSpec"]:
            return ServiceSpec

        def build(self) -> "ServiceSpec":
            return ServiceSpec(**self._attrs)

        def allocate_load_balancer_node_ports(self, value: bool | None, /) -> Self:
            'allocateLoadBalancerNodePorts defines if NodePorts will be automatically allocated for services with type LoadBalancer.  Default is "true". It may be set to "false" if the cluster load-balancer does not rely on NodePorts.  If the caller requests specific NodePorts (by specifying a value), those requests will be respected, regardless of this field. This field may only be set for services with type LoadBalancer and will be cleared if the type is changed to any other type.'
            return self._set("allocate_load_balancer_node_ports", value)

        def cluster_ip(self, value: str | None, /) -> Self:
            'clusterIP is the IP address of the service and is usually assigned randomly. If an address is specified manually, is in-range (as per system configuration), and is not in use, it will be allocated to the service; otherwise creation of the service will fail. This field may not be changed through updates unless the type field is also being changed to ExternalName (which requires this field to be blank) or the type field is being changed from ExternalName (in which case this field may optionally be specified, as describe above).  Valid values are "None", empty string (""), or a valid IP address. Setting this to "None" makes a "headless service" (no virtual IP), which is useful when direct endpoint connections are preferred and proxying is not required.  Only applies to types ClusterIP, NodePort, and LoadBalancer. If this field is specified when creating a Service of type ExternalName, creation will fail. This field will be wiped when updating a Service to type ExternalName. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
            return self._set("cluster_ip", value)

        def cluster_i_ps(self, value: list[str] | None, /) -> Self:
            'ClusterIPs is a list of IP addresses assigned to this service, and are usually assigned randomly.  If an address is specified manually, is in-range (as per system configuration), and is not in use, it will be allocated to the service; otherwise creation of the service will fail. This field may not be changed through updates unless the type field is also being changed to ExternalName (which requires this field to be empty) or the type field is being changed from ExternalName (in which case this field may optionally be specified, as describe above).  Valid values are "None", empty string (""), or a valid IP address.  Setting this to "None" makes a "headless service" (no virtual IP), which is useful when direct endpoint connections are preferred and proxying is not required.  Only applies to types ClusterIP, NodePort, and LoadBalancer. If this field is specified when creating a Service of type ExternalName, creation will fail. This field will be wiped when updating a Service to type ExternalName.  If this field is not specified, it will be initialized from the clusterIP field.  If this field is specified, clients must ensure that clusterIPs[0] and clusterIP have the same value.\n\nThis field may hold a maximum of two entries (dual-stack IPs, in either order). These IPs must correspond to the values of the ipFamilies field. Both clusterIPs and ipFamilies are governed by the ipFamilyPolicy field. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
            return self._set("cluster_i_ps", value)

        def external_i_ps(self, value: list[str] | None, /) -> Self:
            "externalIPs is a list of IP addresses for which nodes in the cluster will also accept traffic for this service.  These IPs are not managed by Kubernetes.  The user is responsible for ensuring that traffic arrives at a node with this IP.  A common example is external load-balancers that are not part of the Kubernetes system."
            return self._set("external_i_ps", value)

        def external_name(self, value: str | None, /) -> Self:
            'externalName is the external reference that discovery mechanisms will return as an alias for this service (e.g. a DNS CNAME record). No proxying will be involved.  Must be a lowercase RFC-1123 hostname (https://tools.ietf.org/html/rfc1123) and requires `type` to be "ExternalName".'
            return self._set("external_name", value)

        def external_traffic_policy(self, value: str | None, /) -> Self:
            'externalTrafficPolicy describes how nodes distribute service traffic they receive on one of the Service\u0027s "externally-facing" addresses (NodePorts, ExternalIPs, and LoadBalancer IPs). If set to "Local", the proxy will configure the service in a way that assumes that external load balancers will take care of balancing the service traffic between nodes, and so each node will deliver traffic only to the node-local endpoints of the service, without masquerading the client source IP. (Traffic mistakenly sent to a node with no endpoints will be dropped.) The default value, "Cluster", uses the standard behavior of routing to all endpoints evenly (possibly modified by topology and other features). Note that traffic sent to an External IP or LoadBalancer IP from within the cluster will always get "Cluster" semantics, but clients sending to a NodePort from within the cluster may need to take traffic policy into account when picking a node.'
            return self._set("external_traffic_policy", value)

        def health_check_node_port(self, value: int | None, /) -> Self:
            "healthCheckNodePort specifies the healthcheck nodePort for the service. This only applies when type is set to LoadBalancer and externalTrafficPolicy is set to Local. If a value is specified, is in-range, and is not in use, it will be used.  If not specified, a value will be automatically allocated.  External systems (e.g. load-balancers) can use this port to determine if a given node holds endpoints for this service or not.  If this field is specified when creating a Service which does not need it, creation will fail. This field will be wiped when updating a Service to no longer need it (e.g. changing type). This field cannot be updated once set."
            return self._set("health_check_node_port", value)

        def internal_traffic_policy(self, value: str | None, /) -> Self:
            'InternalTrafficPolicy describes how nodes distribute service traffic they receive on the ClusterIP. If set to "Local", the proxy will assume that pods only want to talk to endpoints of the service on the same node as the pod, dropping the traffic if there are no local endpoints. The default value, "Cluster", uses the standard behavior of routing to all endpoints evenly (possibly modified by topology and other features).'
            return self._set("internal_traffic_policy", value)

        def ip_families(self, value: list[str] | None, /) -> Self:
            'IPFamilies is a list of IP families (e.g. IPv4, IPv6) assigned to this service. This field is usually assigned automatically based on cluster configuration and the ipFamilyPolicy field. If this field is specified manually, the requested family is available in the cluster, and ipFamilyPolicy allows it, it will be used; otherwise creation of the service will fail. This field is conditionally mutable: it allows for adding or removing a secondary IP family, but it does not allow changing the primary IP family of the Service. Valid values are "IPv4" and "IPv6".  This field only applies to Services of types ClusterIP, NodePort, and LoadBalancer, and does apply to "headless" services. This field will be wiped when updating a Service to type ExternalName.\n\nThis field may hold a maximum of two entries (dual-stack families, in either order).  These families must correspond to the values of the clusterIPs field, if specified. Both clusterIPs and ipFamilies are governed by the ipFamilyPolicy field.'
            return self._set("ip_families", value)

        def ip_family_policy(self, value: str | None, /) -> Self:
            'IPFamilyPolicy represents the dual-stack-ness requested or required by this Service. If there is no value provided, then this field will be set to SingleStack. Services can be "SingleStack" (a single IP family), "PreferDualStack" (two IP families on dual-stack configured clusters or a single IP family on single-stack clusters), or "RequireDualStack" (two IP families on dual-stack configured clusters, otherwise fail). The ipFamilies and clusterIPs fields depend on the value of this field. This field will be wiped when updating a service to type ExternalName.'
            return self._set("ip_family_policy", value)

        def load_balancer_class(self, value: str | None, /) -> Self:
            'loadBalancerClass is the class of the load balancer implementation this Service belongs to. If specified, the value of this field must be a label-style identifier, with an optional prefix, e.g. "internal-vip" or "example.com/internal-vip". Unprefixed names are reserved for end-users. This field can only be set when the Service type is \u0027LoadBalancer\u0027. If not set, the default load balancer implementation is used, today this is typically done through the cloud provider integration, but should apply for any default implementation. If set, it is assumed that a load balancer implementation is watching for Services with a matching class. Any default load balancer implementation (e.g. cloud providers) should ignore Services that set this field. This field can only be set when creating or updating a Service to type \u0027LoadBalancer\u0027. Once set, it can not be changed. This field will be wiped when a service is updated to a non \u0027LoadBalancer\u0027 type.'
            return self._set("load_balancer_class", value)

        def load_balancer_ip(self, value: str | None, /) -> Self:
            "Only applies to Service Type: LoadBalancer. This feature depends on whether the underlying cloud-provider supports specifying the loadBalancerIP when a load balancer is created. This field will be ignored if the cloud-provider does not support the feature. Deprecated: This field was under-specified and its meaning varies across implementations. Using it is non-portable and it may not support dual-stack. Users are encouraged to use implementation-specific annotations when available."
            return self._set("load_balancer_ip", value)

        def load_balancer_source_ranges(self, value: list[str] | None, /) -> Self:
            'If specified and supported by the platform, this will restrict traffic through the cloud-provider load-balancer will be restricted to the specified client IPs. This field will be ignored if the cloud-provider does not support the feature." More info: https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/'
            return self._set("load_balancer_source_ranges", value)

        @overload
        def ports(
            self, value_or_callback: list[ServicePort] | None, /
        ) -> "ServiceSpec.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ServicePort, ServicePort.Builder]],
                GenericListBuilder[ServicePort, ServicePort.Builder] | list[ServicePort],
            ],
            /,
        ) -> "ServiceSpec.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ServicePort.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            "The list of ports that are exposed by this service. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ServicePort.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServicePort.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

        def publish_not_ready_addresses(self, value: bool | None, /) -> Self:
            'publishNotReadyAddresses indicates that any agent which deals with endpoints for this Service should disregard any indications of ready/not-ready. The primary use case for setting this field is for a StatefulSet\u0027s Headless Service to propagate SRV DNS records for its Pods for the purpose of peer discovery. The Kubernetes controllers that generate Endpoints and EndpointSlice resources for Services interpret this to mean that all endpoints are considered "ready" even if the Pods themselves are not. Agents which consume only Kubernetes generated endpoints through the Endpoints or EndpointSlice resources can safely assume this behavior.'
            return self._set("publish_not_ready_addresses", value)

        def selector(self, value: dict[str, str] | None, /) -> Self:
            "Route service traffic to pods with label keys and values matching this selector. If empty or not present, the service is assumed to have an external process managing its endpoints, which Kubernetes will not modify. Only applies to types ClusterIP, NodePort, and LoadBalancer. Ignored if type is ExternalName. More info: https://kubernetes.io/docs/concepts/services-networking/service/"
            return self._set("selector", value)

        def session_affinity(self, value: str | None, /) -> Self:
            'Supports "ClientIP" and "None". Used to maintain session affinity. Enable client IP based session affinity. Must be ClientIP or None. Defaults to None. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
            return self._set("session_affinity", value)

        @overload
        def session_affinity_config(
            self, value_or_callback: SessionAffinityConfig | None, /
        ) -> "ServiceSpec.Builder": ...

        @overload
        def session_affinity_config(
            self,
            value_or_callback: Callable[
                [SessionAffinityConfig.Builder],
                SessionAffinityConfig.Builder | SessionAffinityConfig,
            ],
            /,
        ) -> "ServiceSpec.Builder": ...

        @overload
        def session_affinity_config(
            self, value_or_callback: Never = ...
        ) -> "SessionAffinityConfig.BuilderContext": ...

        def session_affinity_config(self, value_or_callback=UNSET, /):
            "sessionAffinityConfig contains the configurations of session affinity."
            if self._in_context and value_or_callback is UNSET:
                context = SessionAffinityConfig.BuilderContext()
                context._parent_builder = self
                context._field_name = "session_affinity_config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SessionAffinityConfig.builder())
                if isinstance(output, SessionAffinityConfig.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("session_affinity_config", value)

        def traffic_distribution(self, value: str | None, /) -> Self:
            'TrafficDistribution offers a way to express preferences for how traffic is distributed to Service endpoints. Implementations can use this field as a hint, but are not required to guarantee strict adherence. If the field is not set, the implementation will apply its default routing strategy. If set to "PreferClose", implementations should prioritize endpoints that are in the same zone.'
            return self._set("traffic_distribution", value)

        def type(self, value: str | None, /) -> Self:
            'type determines how the Service is exposed. Defaults to ClusterIP. Valid options are ExternalName, ClusterIP, NodePort, and LoadBalancer. "ClusterIP" allocates a cluster-internal IP address for load-balancing to endpoints. Endpoints are determined by the selector or if that is not specified, by manual construction of an Endpoints object or EndpointSlice objects. If clusterIP is "None", no virtual IP is allocated and the endpoints are published as a set of endpoints rather than a virtual IP. "NodePort" builds on ClusterIP and allocates a port on every node which routes to the same endpoints as the clusterIP. "LoadBalancer" builds on NodePort and creates an external load-balancer (if supported in the current cloud) which routes to the same endpoints as the clusterIP. "ExternalName" aliases this service to the specified externalName. Several other fields do not apply to ExternalName services. More info: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types'
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["ServiceSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServiceSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServiceSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServiceSpec", Builder]:
        return GenericListBuilder[ServiceSpec, ServiceSpec.Builder]()

    allocate_load_balancer_node_ports: Annotated[
        bool | None, Field(alias="allocateLoadBalancerNodePorts")
    ] = None
    'allocateLoadBalancerNodePorts defines if NodePorts will be automatically allocated for services with type LoadBalancer.  Default is "true". It may be set to "false" if the cluster load-balancer does not rely on NodePorts.  If the caller requests specific NodePorts (by specifying a value), those requests will be respected, regardless of this field. This field may only be set for services with type LoadBalancer and will be cleared if the type is changed to any other type.'
    cluster_ip: Annotated[str | None, Field(alias="clusterIP")] = None
    'clusterIP is the IP address of the service and is usually assigned randomly. If an address is specified manually, is in-range (as per system configuration), and is not in use, it will be allocated to the service; otherwise creation of the service will fail. This field may not be changed through updates unless the type field is also being changed to ExternalName (which requires this field to be blank) or the type field is being changed from ExternalName (in which case this field may optionally be specified, as describe above).  Valid values are "None", empty string (""), or a valid IP address. Setting this to "None" makes a "headless service" (no virtual IP), which is useful when direct endpoint connections are preferred and proxying is not required.  Only applies to types ClusterIP, NodePort, and LoadBalancer. If this field is specified when creating a Service of type ExternalName, creation will fail. This field will be wiped when updating a Service to type ExternalName. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
    cluster_i_ps: Annotated[list[str] | None, Field(alias="clusterIPs")] = None
    'ClusterIPs is a list of IP addresses assigned to this service, and are usually assigned randomly.  If an address is specified manually, is in-range (as per system configuration), and is not in use, it will be allocated to the service; otherwise creation of the service will fail. This field may not be changed through updates unless the type field is also being changed to ExternalName (which requires this field to be empty) or the type field is being changed from ExternalName (in which case this field may optionally be specified, as describe above).  Valid values are "None", empty string (""), or a valid IP address.  Setting this to "None" makes a "headless service" (no virtual IP), which is useful when direct endpoint connections are preferred and proxying is not required.  Only applies to types ClusterIP, NodePort, and LoadBalancer. If this field is specified when creating a Service of type ExternalName, creation will fail. This field will be wiped when updating a Service to type ExternalName.  If this field is not specified, it will be initialized from the clusterIP field.  If this field is specified, clients must ensure that clusterIPs[0] and clusterIP have the same value.\n\nThis field may hold a maximum of two entries (dual-stack IPs, in either order). These IPs must correspond to the values of the ipFamilies field. Both clusterIPs and ipFamilies are governed by the ipFamilyPolicy field. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
    external_i_ps: Annotated[list[str] | None, Field(alias="externalIPs")] = None
    "externalIPs is a list of IP addresses for which nodes in the cluster will also accept traffic for this service.  These IPs are not managed by Kubernetes.  The user is responsible for ensuring that traffic arrives at a node with this IP.  A common example is external load-balancers that are not part of the Kubernetes system."
    external_name: Annotated[str | None, Field(alias="externalName")] = None
    'externalName is the external reference that discovery mechanisms will return as an alias for this service (e.g. a DNS CNAME record). No proxying will be involved.  Must be a lowercase RFC-1123 hostname (https://tools.ietf.org/html/rfc1123) and requires `type` to be "ExternalName".'
    external_traffic_policy: Annotated[str | None, Field(alias="externalTrafficPolicy")] = None
    'externalTrafficPolicy describes how nodes distribute service traffic they receive on one of the Service\u0027s "externally-facing" addresses (NodePorts, ExternalIPs, and LoadBalancer IPs). If set to "Local", the proxy will configure the service in a way that assumes that external load balancers will take care of balancing the service traffic between nodes, and so each node will deliver traffic only to the node-local endpoints of the service, without masquerading the client source IP. (Traffic mistakenly sent to a node with no endpoints will be dropped.) The default value, "Cluster", uses the standard behavior of routing to all endpoints evenly (possibly modified by topology and other features). Note that traffic sent to an External IP or LoadBalancer IP from within the cluster will always get "Cluster" semantics, but clients sending to a NodePort from within the cluster may need to take traffic policy into account when picking a node.'
    health_check_node_port: Annotated[int | None, Field(alias="healthCheckNodePort")] = None
    "healthCheckNodePort specifies the healthcheck nodePort for the service. This only applies when type is set to LoadBalancer and externalTrafficPolicy is set to Local. If a value is specified, is in-range, and is not in use, it will be used.  If not specified, a value will be automatically allocated.  External systems (e.g. load-balancers) can use this port to determine if a given node holds endpoints for this service or not.  If this field is specified when creating a Service which does not need it, creation will fail. This field will be wiped when updating a Service to no longer need it (e.g. changing type). This field cannot be updated once set."
    internal_traffic_policy: Annotated[str | None, Field(alias="internalTrafficPolicy")] = None
    'InternalTrafficPolicy describes how nodes distribute service traffic they receive on the ClusterIP. If set to "Local", the proxy will assume that pods only want to talk to endpoints of the service on the same node as the pod, dropping the traffic if there are no local endpoints. The default value, "Cluster", uses the standard behavior of routing to all endpoints evenly (possibly modified by topology and other features).'
    ip_families: Annotated[list[str] | None, Field(alias="ipFamilies")] = None
    'IPFamilies is a list of IP families (e.g. IPv4, IPv6) assigned to this service. This field is usually assigned automatically based on cluster configuration and the ipFamilyPolicy field. If this field is specified manually, the requested family is available in the cluster, and ipFamilyPolicy allows it, it will be used; otherwise creation of the service will fail. This field is conditionally mutable: it allows for adding or removing a secondary IP family, but it does not allow changing the primary IP family of the Service. Valid values are "IPv4" and "IPv6".  This field only applies to Services of types ClusterIP, NodePort, and LoadBalancer, and does apply to "headless" services. This field will be wiped when updating a Service to type ExternalName.\n\nThis field may hold a maximum of two entries (dual-stack families, in either order).  These families must correspond to the values of the clusterIPs field, if specified. Both clusterIPs and ipFamilies are governed by the ipFamilyPolicy field.'
    ip_family_policy: Annotated[str | None, Field(alias="ipFamilyPolicy")] = None
    'IPFamilyPolicy represents the dual-stack-ness requested or required by this Service. If there is no value provided, then this field will be set to SingleStack. Services can be "SingleStack" (a single IP family), "PreferDualStack" (two IP families on dual-stack configured clusters or a single IP family on single-stack clusters), or "RequireDualStack" (two IP families on dual-stack configured clusters, otherwise fail). The ipFamilies and clusterIPs fields depend on the value of this field. This field will be wiped when updating a service to type ExternalName.'
    load_balancer_class: Annotated[str | None, Field(alias="loadBalancerClass")] = None
    'loadBalancerClass is the class of the load balancer implementation this Service belongs to. If specified, the value of this field must be a label-style identifier, with an optional prefix, e.g. "internal-vip" or "example.com/internal-vip". Unprefixed names are reserved for end-users. This field can only be set when the Service type is \u0027LoadBalancer\u0027. If not set, the default load balancer implementation is used, today this is typically done through the cloud provider integration, but should apply for any default implementation. If set, it is assumed that a load balancer implementation is watching for Services with a matching class. Any default load balancer implementation (e.g. cloud providers) should ignore Services that set this field. This field can only be set when creating or updating a Service to type \u0027LoadBalancer\u0027. Once set, it can not be changed. This field will be wiped when a service is updated to a non \u0027LoadBalancer\u0027 type.'
    load_balancer_ip: Annotated[str | None, Field(alias="loadBalancerIP")] = None
    "Only applies to Service Type: LoadBalancer. This feature depends on whether the underlying cloud-provider supports specifying the loadBalancerIP when a load balancer is created. This field will be ignored if the cloud-provider does not support the feature. Deprecated: This field was under-specified and its meaning varies across implementations. Using it is non-portable and it may not support dual-stack. Users are encouraged to use implementation-specific annotations when available."
    load_balancer_source_ranges: Annotated[
        list[str] | None, Field(alias="loadBalancerSourceRanges")
    ] = None
    'If specified and supported by the platform, this will restrict traffic through the cloud-provider load-balancer will be restricted to the specified client IPs. This field will be ignored if the cloud-provider does not support the feature." More info: https://kubernetes.io/docs/tasks/access-application-cluster/create-external-load-balancer/'
    ports: list[ServicePort] | None = None
    "The list of ports that are exposed by this service. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies"
    publish_not_ready_addresses: Annotated[bool | None, Field(alias="publishNotReadyAddresses")] = (
        None
    )
    'publishNotReadyAddresses indicates that any agent which deals with endpoints for this Service should disregard any indications of ready/not-ready. The primary use case for setting this field is for a StatefulSet\u0027s Headless Service to propagate SRV DNS records for its Pods for the purpose of peer discovery. The Kubernetes controllers that generate Endpoints and EndpointSlice resources for Services interpret this to mean that all endpoints are considered "ready" even if the Pods themselves are not. Agents which consume only Kubernetes generated endpoints through the Endpoints or EndpointSlice resources can safely assume this behavior.'
    selector: dict[str, str] | None = None
    "Route service traffic to pods with label keys and values matching this selector. If empty or not present, the service is assumed to have an external process managing its endpoints, which Kubernetes will not modify. Only applies to types ClusterIP, NodePort, and LoadBalancer. Ignored if type is ExternalName. More info: https://kubernetes.io/docs/concepts/services-networking/service/"
    session_affinity: Annotated[str | None, Field(alias="sessionAffinity")] = None
    'Supports "ClientIP" and "None". Used to maintain session affinity. Enable client IP based session affinity. Must be ClientIP or None. Defaults to None. More info: https://kubernetes.io/docs/concepts/services-networking/service/#virtual-ips-and-service-proxies'
    session_affinity_config: Annotated[
        SessionAffinityConfig | None, Field(alias="sessionAffinityConfig")
    ] = None
    "sessionAffinityConfig contains the configurations of session affinity."
    traffic_distribution: Annotated[str | None, Field(alias="trafficDistribution")] = None
    'TrafficDistribution offers a way to express preferences for how traffic is distributed to Service endpoints. Implementations can use this field as a hint, but are not required to guarantee strict adherence. If the field is not set, the implementation will apply its default routing strategy. If set to "PreferClose", implementations should prioritize endpoints that are in the same zone.'
    type: str | None = None
    'type determines how the Service is exposed. Defaults to ClusterIP. Valid options are ExternalName, ClusterIP, NodePort, and LoadBalancer. "ClusterIP" allocates a cluster-internal IP address for load-balancing to endpoints. Endpoints are determined by the selector or if that is not specified, by manual construction of an Endpoints object or EndpointSlice objects. If clusterIP is "None", no virtual IP is allocated and the endpoints are published as a set of endpoints rather than a virtual IP. "NodePort" builds on ClusterIP and allocates a port on every node which routes to the same endpoints as the clusterIP. "LoadBalancer" builds on NodePort and creates an external load-balancer (if supported in the current cloud) which routes to the same endpoints as the clusterIP. "ExternalName" aliases this service to the specified externalName. Several other fields do not apply to ExternalName services. More info: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types'


class ServiceStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServiceStatus"]:
            return ServiceStatus

        def build(self) -> "ServiceStatus":
            return ServiceStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "ServiceStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "ServiceStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Current service state"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def load_balancer(
            self, value_or_callback: LoadBalancerStatus | None, /
        ) -> "ServiceStatus.Builder": ...

        @overload
        def load_balancer(
            self,
            value_or_callback: Callable[
                [LoadBalancerStatus.Builder],
                LoadBalancerStatus.Builder | LoadBalancerStatus,
            ],
            /,
        ) -> "ServiceStatus.Builder": ...

        @overload
        def load_balancer(
            self, value_or_callback: Never = ...
        ) -> "LoadBalancerStatus.BuilderContext": ...

        def load_balancer(self, value_or_callback=UNSET, /):
            "LoadBalancer contains the current status of the load-balancer, if one is present."
            if self._in_context and value_or_callback is UNSET:
                context = LoadBalancerStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "load_balancer"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LoadBalancerStatus.builder())
                if isinstance(output, LoadBalancerStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("load_balancer", value)

    class BuilderContext(BuilderContextBase["ServiceStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServiceStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServiceStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServiceStatus", Builder]:
        return GenericListBuilder[ServiceStatus, ServiceStatus.Builder]()

    conditions: list[Condition] | None = None
    "Current service state"
    load_balancer: Annotated[LoadBalancerStatus | None, Field(alias="loadBalancer")] = None
    "LoadBalancer contains the current status of the load-balancer, if one is present."


class VolumeNodeAffinity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeNodeAffinity"]:
            return VolumeNodeAffinity

        def build(self) -> "VolumeNodeAffinity":
            return VolumeNodeAffinity(**self._attrs)

        @overload
        def required(
            self, value_or_callback: NodeSelector | None, /
        ) -> "VolumeNodeAffinity.Builder": ...

        @overload
        def required(
            self,
            value_or_callback: Callable[
                [NodeSelector.Builder], NodeSelector.Builder | NodeSelector
            ],
            /,
        ) -> "VolumeNodeAffinity.Builder": ...

        @overload
        def required(self, value_or_callback: Never = ...) -> "NodeSelector.BuilderContext": ...

        def required(self, value_or_callback=UNSET, /):
            "required specifies hard node constraints that must be met."
            if self._in_context and value_or_callback is UNSET:
                context = NodeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "required"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelector.builder())
                if isinstance(output, NodeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("required", value)

    class BuilderContext(BuilderContextBase["VolumeNodeAffinity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeNodeAffinity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeNodeAffinity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeNodeAffinity", Builder]:
        return GenericListBuilder[VolumeNodeAffinity, VolumeNodeAffinity.Builder]()

    required: NodeSelector | None = None
    "required specifies hard node constraints that must be met."


class ContainerStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ContainerStatus"]:
            return ContainerStatus

        def build(self) -> "ContainerStatus":
            return ContainerStatus(**self._attrs)

        def allocated_resources(self, value: dict[str, Quantity] | None, /) -> Self:
            "AllocatedResources represents the compute resources allocated for this container by the node. Kubelet sets this value to Container.Resources.Requests upon successful pod admission and after successfully admitting desired pod resize."
            return self._set("allocated_resources", value)

        @overload
        def allocated_resources_status(
            self, value_or_callback: list[ResourceStatus] | None, /
        ) -> "ContainerStatus.Builder": ...

        @overload
        def allocated_resources_status(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ResourceStatus, ResourceStatus.Builder]],
                GenericListBuilder[ResourceStatus, ResourceStatus.Builder] | list[ResourceStatus],
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def allocated_resources_status(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ResourceStatus.Builder]: ...

        def allocated_resources_status(self, value_or_callback=UNSET, /):
            "AllocatedResourcesStatus represents the status of various resources allocated for this Pod."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ResourceStatus.Builder]()
                context._parent_builder = self
                context._field_name = "allocated_resources_status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("allocated_resources_status", value)

        def container_id(self, value: str | None, /) -> Self:
            'ContainerID is the ID of the container in the format \u0027\u003ctype\u003e://\u003ccontainer_id\u003e\u0027. Where type is a container runtime identifier, returned from Version call of CRI API (for example "containerd").'
            return self._set("container_id", value)

        def image(self, value: str, /) -> Self:
            "Image is the name of container image that the container is running. The container image may not match the image used in the PodSpec, as it may have been resolved by the runtime. More info: https://kubernetes.io/docs/concepts/containers/images."
            return self._set("image", value)

        def image_id(self, value: str, /) -> Self:
            "ImageID is the image ID of the container\u0027s image. The image ID may not match the image ID of the image used in the PodSpec, as it may have been resolved by the runtime."
            return self._set("image_id", value)

        @overload
        def last_state(
            self, value_or_callback: ContainerState | None, /
        ) -> "ContainerStatus.Builder": ...

        @overload
        def last_state(
            self,
            value_or_callback: Callable[
                [ContainerState.Builder], ContainerState.Builder | ContainerState
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def last_state(self, value_or_callback: Never = ...) -> "ContainerState.BuilderContext": ...

        def last_state(self, value_or_callback=UNSET, /):
            "LastTerminationState holds the last termination state of the container to help debug container crashes and restarts. This field is not populated if the container is still running and RestartCount is 0."
            if self._in_context and value_or_callback is UNSET:
                context = ContainerState.BuilderContext()
                context._parent_builder = self
                context._field_name = "last_state"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerState.builder())
                if isinstance(output, ContainerState.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("last_state", value)

        def name(self, value: str, /) -> Self:
            "Name is a DNS_LABEL representing the unique name of the container. Each container in a pod must have a unique name across all container types. Cannot be updated."
            return self._set("name", value)

        def ready(self, value: bool, /) -> Self:
            "Ready specifies whether the container is currently passing its readiness check. The value will change as readiness probes keep executing. If no readiness probes are specified, this field defaults to true once the container is fully started (see Started field).\n\nThe value is typically used to determine whether a container is ready to accept traffic."
            return self._set("ready", value)

        @overload
        def resources(
            self, value_or_callback: ResourceRequirements | None, /
        ) -> "ContainerStatus.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [ResourceRequirements.Builder],
                ResourceRequirements.Builder | ResourceRequirements,
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> "ResourceRequirements.BuilderContext": ...

        def resources(self, value_or_callback=UNSET, /):
            "Resources represents the compute resource requests and limits that have been successfully enacted on the running container after it has been started or has been successfully resized."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceRequirements.builder())
                if isinstance(output, ResourceRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

        def restart_count(self, value: int, /) -> Self:
            "RestartCount holds the number of times the container has been restarted. Kubelet makes an effort to always increment the value, but there are cases when the state may be lost due to node restarts and then the value may be reset to 0. The value is never negative."
            return self._set("restart_count", value)

        def started(self, value: bool | None, /) -> Self:
            "Started indicates whether the container has finished its postStart lifecycle hook and passed its startup probe. Initialized as false, becomes true after startupProbe is considered successful. Resets to false when the container is restarted, or if kubelet loses state temporarily. In both cases, startup probes will run again. Is always true when no startupProbe is defined and container is running and has passed the postStart lifecycle hook. The null value must be treated the same as false."
            return self._set("started", value)

        @overload
        def state(
            self, value_or_callback: ContainerState | None, /
        ) -> "ContainerStatus.Builder": ...

        @overload
        def state(
            self,
            value_or_callback: Callable[
                [ContainerState.Builder], ContainerState.Builder | ContainerState
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def state(self, value_or_callback: Never = ...) -> "ContainerState.BuilderContext": ...

        def state(self, value_or_callback=UNSET, /):
            "State holds details about the container\u0027s current condition."
            if self._in_context and value_or_callback is UNSET:
                context = ContainerState.BuilderContext()
                context._parent_builder = self
                context._field_name = "state"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerState.builder())
                if isinstance(output, ContainerState.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("state", value)

        def stop_signal(self, value: str | None, /) -> Self:
            "StopSignal reports the effective stop signal for this container"
            return self._set("stop_signal", value)

        @overload
        def user(self, value_or_callback: ContainerUser | None, /) -> "ContainerStatus.Builder": ...

        @overload
        def user(
            self,
            value_or_callback: Callable[
                [ContainerUser.Builder], ContainerUser.Builder | ContainerUser
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def user(self, value_or_callback: Never = ...) -> "ContainerUser.BuilderContext": ...

        def user(self, value_or_callback=UNSET, /):
            "User represents user identity information initially attached to the first process of the container"
            if self._in_context and value_or_callback is UNSET:
                context = ContainerUser.BuilderContext()
                context._parent_builder = self
                context._field_name = "user"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerUser.builder())
                if isinstance(output, ContainerUser.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("user", value)

        @overload
        def volume_mounts(
            self, value_or_callback: list[VolumeMountStatus] | None, /
        ) -> "ContainerStatus.Builder": ...

        @overload
        def volume_mounts(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeMountStatus, VolumeMountStatus.Builder]],
                GenericListBuilder[VolumeMountStatus, VolumeMountStatus.Builder]
                | list[VolumeMountStatus],
            ],
            /,
        ) -> "ContainerStatus.Builder": ...

        @overload
        def volume_mounts(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeMountStatus.Builder]: ...

        def volume_mounts(self, value_or_callback=UNSET, /):
            "Status of volume mounts."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeMountStatus.Builder]()
                context._parent_builder = self
                context._field_name = "volume_mounts"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeMountStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_mounts", value)

    class BuilderContext(BuilderContextBase["ContainerStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ContainerStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ContainerStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ContainerStatus", Builder]:
        return GenericListBuilder[ContainerStatus, ContainerStatus.Builder]()

    allocated_resources: Annotated[
        dict[str, Quantity] | None, Field(alias="allocatedResources")
    ] = None
    "AllocatedResources represents the compute resources allocated for this container by the node. Kubelet sets this value to Container.Resources.Requests upon successful pod admission and after successfully admitting desired pod resize."
    allocated_resources_status: Annotated[
        list[ResourceStatus] | None, Field(alias="allocatedResourcesStatus")
    ] = None
    "AllocatedResourcesStatus represents the status of various resources allocated for this Pod."
    container_id: Annotated[str | None, Field(alias="containerID")] = None
    'ContainerID is the ID of the container in the format \u0027\u003ctype\u003e://\u003ccontainer_id\u003e\u0027. Where type is a container runtime identifier, returned from Version call of CRI API (for example "containerd").'
    image: str
    "Image is the name of container image that the container is running. The container image may not match the image used in the PodSpec, as it may have been resolved by the runtime. More info: https://kubernetes.io/docs/concepts/containers/images."
    image_id: Annotated[str, Field(alias="imageID")]
    "ImageID is the image ID of the container\u0027s image. The image ID may not match the image ID of the image used in the PodSpec, as it may have been resolved by the runtime."
    last_state: Annotated[ContainerState | None, Field(alias="lastState")] = None
    "LastTerminationState holds the last termination state of the container to help debug container crashes and restarts. This field is not populated if the container is still running and RestartCount is 0."
    name: str
    "Name is a DNS_LABEL representing the unique name of the container. Each container in a pod must have a unique name across all container types. Cannot be updated."
    ready: bool
    "Ready specifies whether the container is currently passing its readiness check. The value will change as readiness probes keep executing. If no readiness probes are specified, this field defaults to true once the container is fully started (see Started field).\n\nThe value is typically used to determine whether a container is ready to accept traffic."
    resources: ResourceRequirements | None = None
    "Resources represents the compute resource requests and limits that have been successfully enacted on the running container after it has been started or has been successfully resized."
    restart_count: Annotated[int, Field(alias="restartCount")]
    "RestartCount holds the number of times the container has been restarted. Kubelet makes an effort to always increment the value, but there are cases when the state may be lost due to node restarts and then the value may be reset to 0. The value is never negative."
    started: bool | None = None
    "Started indicates whether the container has finished its postStart lifecycle hook and passed its startup probe. Initialized as false, becomes true after startupProbe is considered successful. Resets to false when the container is restarted, or if kubelet loses state temporarily. In both cases, startup probes will run again. Is always true when no startupProbe is defined and container is running and has passed the postStart lifecycle hook. The null value must be treated the same as false."
    state: ContainerState | None = None
    "State holds details about the container\u0027s current condition."
    stop_signal: Annotated[str | None, Field(alias="stopSignal")] = None
    "StopSignal reports the effective stop signal for this container"
    user: ContainerUser | None = None
    "User represents user identity information initially attached to the first process of the container"
    volume_mounts: Annotated[list[VolumeMountStatus] | None, Field(alias="volumeMounts")] = None
    "Status of volume mounts."


class DownwardAPIProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DownwardAPIProjection"]:
            return DownwardAPIProjection

        def build(self) -> "DownwardAPIProjection":
            return DownwardAPIProjection(**self._attrs)

        @overload
        def items(
            self, value_or_callback: list[DownwardAPIVolumeFile] | None, /
        ) -> "DownwardAPIProjection.Builder": ...

        @overload
        def items(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DownwardAPIVolumeFile, DownwardAPIVolumeFile.Builder]],
                GenericListBuilder[DownwardAPIVolumeFile, DownwardAPIVolumeFile.Builder]
                | list[DownwardAPIVolumeFile],
            ],
            /,
        ) -> "DownwardAPIProjection.Builder": ...

        @overload
        def items(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DownwardAPIVolumeFile.Builder]: ...

        def items(self, value_or_callback=UNSET, /):
            "Items is a list of DownwardAPIVolume file"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DownwardAPIVolumeFile.Builder]()
                context._parent_builder = self
                context._field_name = "items"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DownwardAPIVolumeFile.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("items", value)

    class BuilderContext(BuilderContextBase["DownwardAPIProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DownwardAPIProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DownwardAPIProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DownwardAPIProjection", Builder]:
        return GenericListBuilder[DownwardAPIProjection, DownwardAPIProjection.Builder]()

    items: list[DownwardAPIVolumeFile] | None = None
    "Items is a list of DownwardAPIVolume file"


class EnvVar(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EnvVar"]:
            return EnvVar

        def build(self) -> "EnvVar":
            return EnvVar(**self._attrs)

        def name(self, value: str, /) -> Self:
            "Name of the environment variable. May consist of any printable ASCII characters except \u0027=\u0027."
            return self._set("name", value)

        def value(self, value: str | None, /) -> Self:
            'Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".'
            return self._set("value", value)

        @overload
        def value_from(self, value_or_callback: EnvVarSource | None, /) -> "EnvVar.Builder": ...

        @overload
        def value_from(
            self,
            value_or_callback: Callable[
                [EnvVarSource.Builder], EnvVarSource.Builder | EnvVarSource
            ],
            /,
        ) -> "EnvVar.Builder": ...

        @overload
        def value_from(self, value_or_callback: Never = ...) -> "EnvVarSource.BuilderContext": ...

        def value_from(self, value_or_callback=UNSET, /):
            "Source for the environment variable\u0027s value. Cannot be used if value is not empty."
            if self._in_context and value_or_callback is UNSET:
                context = EnvVarSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "value_from"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EnvVarSource.builder())
                if isinstance(output, EnvVarSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("value_from", value)

    class BuilderContext(BuilderContextBase["EnvVar.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EnvVar.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EnvVar."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EnvVar", Builder]:
        return GenericListBuilder[EnvVar, EnvVar.Builder]()

    name: str
    "Name of the environment variable. May consist of any printable ASCII characters except \u0027=\u0027."
    value: str | None = None
    'Variable references $(VAR_NAME) are expanded using the previously defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".'
    value_from: Annotated[EnvVarSource | None, Field(alias="valueFrom")] = None
    "Source for the environment variable\u0027s value. Cannot be used if value is not empty."


class EphemeralVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EphemeralVolumeSource"]:
            return EphemeralVolumeSource

        def build(self) -> "EphemeralVolumeSource":
            return EphemeralVolumeSource(**self._attrs)

        @overload
        def volume_claim_template(
            self, value_or_callback: PersistentVolumeClaimTemplate | None, /
        ) -> "EphemeralVolumeSource.Builder": ...

        @overload
        def volume_claim_template(
            self,
            value_or_callback: Callable[
                [PersistentVolumeClaimTemplate.Builder],
                PersistentVolumeClaimTemplate.Builder | PersistentVolumeClaimTemplate,
            ],
            /,
        ) -> "EphemeralVolumeSource.Builder": ...

        @overload
        def volume_claim_template(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeClaimTemplate.BuilderContext": ...

        def volume_claim_template(self, value_or_callback=UNSET, /):
            "Will be used to create a stand-alone PVC to provision the volume. The pod in which this EphemeralVolumeSource is embedded will be the owner of the PVC, i.e. the PVC will be deleted together with the pod.  The name of the PVC will be `\u003cpod name\u003e-\u003cvolume name\u003e` where `\u003cvolume name\u003e` is the name from the `PodSpec.Volumes` array entry. Pod validation will reject the pod if the concatenated name is not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod will *not* be used for the pod to avoid using an unrelated volume by mistake. Starting the pod is then blocked until the unrelated PVC is removed. If such a pre-created PVC is meant to be used by the pod, the PVC has to updated with an owner reference to the pod once the pod exists. Normally this should not be necessary, but it may be useful when manually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes to the PVC after it has been created.\n\nRequired, must not be nil."
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeClaimTemplate.BuilderContext()
                context._parent_builder = self
                context._field_name = "volume_claim_template"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimTemplate.builder())
                if isinstance(output, PersistentVolumeClaimTemplate.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_claim_template", value)

    class BuilderContext(BuilderContextBase["EphemeralVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EphemeralVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EphemeralVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EphemeralVolumeSource", Builder]:
        return GenericListBuilder[EphemeralVolumeSource, EphemeralVolumeSource.Builder]()

    volume_claim_template: Annotated[
        PersistentVolumeClaimTemplate | None, Field(alias="volumeClaimTemplate")
    ] = None
    "Will be used to create a stand-alone PVC to provision the volume. The pod in which this EphemeralVolumeSource is embedded will be the owner of the PVC, i.e. the PVC will be deleted together with the pod.  The name of the PVC will be `\u003cpod name\u003e-\u003cvolume name\u003e` where `\u003cvolume name\u003e` is the name from the `PodSpec.Volumes` array entry. Pod validation will reject the pod if the concatenated name is not valid for a PVC (for example, too long).\n\nAn existing PVC with that name that is not owned by the pod will *not* be used for the pod to avoid using an unrelated volume by mistake. Starting the pod is then blocked until the unrelated PVC is removed. If such a pre-created PVC is meant to be used by the pod, the PVC has to updated with an owner reference to the pod once the pod exists. Normally this should not be necessary, but it may be useful when manually reconstructing a broken cluster.\n\nThis field is read-only and no changes will be made by Kubernetes to the PVC after it has been created.\n\nRequired, must not be nil."


class Lifecycle(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Lifecycle"]:
            return Lifecycle

        def build(self) -> "Lifecycle":
            return Lifecycle(**self._attrs)

        @overload
        def post_start(
            self, value_or_callback: LifecycleHandler | None, /
        ) -> "Lifecycle.Builder": ...

        @overload
        def post_start(
            self,
            value_or_callback: Callable[
                [LifecycleHandler.Builder], LifecycleHandler.Builder | LifecycleHandler
            ],
            /,
        ) -> "Lifecycle.Builder": ...

        @overload
        def post_start(
            self, value_or_callback: Never = ...
        ) -> "LifecycleHandler.BuilderContext": ...

        def post_start(self, value_or_callback=UNSET, /):
            "PostStart is called immediately after a container is created. If the handler fails, the container is terminated and restarted according to its restart policy. Other management of the container blocks until the hook completes. More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks"
            if self._in_context and value_or_callback is UNSET:
                context = LifecycleHandler.BuilderContext()
                context._parent_builder = self
                context._field_name = "post_start"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LifecycleHandler.builder())
                if isinstance(output, LifecycleHandler.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("post_start", value)

        @overload
        def pre_stop(
            self, value_or_callback: LifecycleHandler | None, /
        ) -> "Lifecycle.Builder": ...

        @overload
        def pre_stop(
            self,
            value_or_callback: Callable[
                [LifecycleHandler.Builder], LifecycleHandler.Builder | LifecycleHandler
            ],
            /,
        ) -> "Lifecycle.Builder": ...

        @overload
        def pre_stop(self, value_or_callback: Never = ...) -> "LifecycleHandler.BuilderContext": ...

        def pre_stop(self, value_or_callback=UNSET, /):
            "PreStop is called immediately before a container is terminated due to an API request or management event such as liveness/startup probe failure, preemption, resource contention, etc. The handler is not called if the container crashes or exits. The Pod\u0027s termination grace period countdown begins before the PreStop hook is executed. Regardless of the outcome of the handler, the container will eventually terminate within the Pod\u0027s termination grace period (unless delayed by finalizers). Other management of the container blocks until the hook completes or until the termination grace period is reached. More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks"
            if self._in_context and value_or_callback is UNSET:
                context = LifecycleHandler.BuilderContext()
                context._parent_builder = self
                context._field_name = "pre_stop"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LifecycleHandler.builder())
                if isinstance(output, LifecycleHandler.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pre_stop", value)

        def stop_signal(self, value: str | None, /) -> Self:
            "StopSignal defines which signal will be sent to a container when it is being stopped. If not specified, the default is defined by the container runtime in use. StopSignal can only be set for Pods with a non-empty .spec.os.name"
            return self._set("stop_signal", value)

    class BuilderContext(BuilderContextBase["Lifecycle.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Lifecycle.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Lifecycle."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Lifecycle", Builder]:
        return GenericListBuilder[Lifecycle, Lifecycle.Builder]()

    post_start: Annotated[LifecycleHandler | None, Field(alias="postStart")] = None
    "PostStart is called immediately after a container is created. If the handler fails, the container is terminated and restarted according to its restart policy. Other management of the container blocks until the hook completes. More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks"
    pre_stop: Annotated[LifecycleHandler | None, Field(alias="preStop")] = None
    "PreStop is called immediately before a container is terminated due to an API request or management event such as liveness/startup probe failure, preemption, resource contention, etc. The handler is not called if the container crashes or exits. The Pod\u0027s termination grace period countdown begins before the PreStop hook is executed. Regardless of the outcome of the handler, the container will eventually terminate within the Pod\u0027s termination grace period (unless delayed by finalizers). Other management of the container blocks until the hook completes or until the termination grace period is reached. More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks"
    stop_signal: Annotated[str | None, Field(alias="stopSignal")] = None
    "StopSignal defines which signal will be sent to a container when it is being stopped. If not specified, the default is defined by the container runtime in use. StopSignal can only be set for Pods with a non-empty .spec.os.name"


class Node(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "nodes",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Node"]:
            return Node

        def build(self) -> "Node":
            return Node(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Node"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Node.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Node.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: NodeSpec | None, /) -> "Node.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[NodeSpec.Builder], NodeSpec.Builder | NodeSpec],
            /,
        ) -> "Node.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "NodeSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the behavior of a node. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = NodeSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSpec.builder())
                if isinstance(output, NodeSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: NodeStatus | None, /) -> "Node.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[[NodeStatus.Builder], NodeStatus.Builder | NodeStatus],
            /,
        ) -> "Node.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "NodeStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Most recently observed status of the node. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = NodeStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeStatus.builder())
                if isinstance(output, NodeStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Node.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Node.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Node."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Node", Builder]:
        return GenericListBuilder[Node, Node.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Node"] = "Node"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: NodeSpec | None = None
    "Spec defines the behavior of a node. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: NodeStatus | None = None
    "Most recently observed status of the node. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


class NodeAffinity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NodeAffinity"]:
            return NodeAffinity

        def build(self) -> "NodeAffinity":
            return NodeAffinity(**self._attrs)

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: list[PreferredSchedulingTerm] | None, /
        ) -> "NodeAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PreferredSchedulingTerm, PreferredSchedulingTerm.Builder]],
                GenericListBuilder[PreferredSchedulingTerm, PreferredSchedulingTerm.Builder]
                | list[PreferredSchedulingTerm],
            ],
            /,
        ) -> "NodeAffinity.Builder": ...

        @overload
        def preferred_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PreferredSchedulingTerm.Builder]: ...

        def preferred_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            'The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node matches the corresponding matchExpressions; the node(s) with the highest sum are the most preferred.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PreferredSchedulingTerm.Builder]()
                context._parent_builder = self
                context._field_name = "preferred_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PreferredSchedulingTerm.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("preferred_during_scheduling_ignored_during_execution", value)

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: NodeSelector | None, /
        ) -> "NodeAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self,
            value_or_callback: Callable[
                [NodeSelector.Builder], NodeSelector.Builder | NodeSelector
            ],
            /,
        ) -> "NodeAffinity.Builder": ...

        @overload
        def required_during_scheduling_ignored_during_execution(
            self, value_or_callback: Never = ...
        ) -> "NodeSelector.BuilderContext": ...

        def required_during_scheduling_ignored_during_execution(self, value_or_callback=UNSET, /):
            "If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to an update), the system may or may not try to eventually evict the pod from its node."
            if self._in_context and value_or_callback is UNSET:
                context = NodeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "required_during_scheduling_ignored_during_execution"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeSelector.builder())
                if isinstance(output, NodeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("required_during_scheduling_ignored_during_execution", value)

    class BuilderContext(BuilderContextBase["NodeAffinity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NodeAffinity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NodeAffinity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NodeAffinity", Builder]:
        return GenericListBuilder[NodeAffinity, NodeAffinity.Builder]()

    preferred_during_scheduling_ignored_during_execution: Annotated[
        list[PreferredSchedulingTerm] | None,
        Field(alias="preferredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    'The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node matches the corresponding matchExpressions; the node(s) with the highest sum are the most preferred.'
    required_during_scheduling_ignored_during_execution: Annotated[
        NodeSelector | None,
        Field(alias="requiredDuringSchedulingIgnoredDuringExecution"),
    ] = None
    "If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to an update), the system may or may not try to eventually evict the pod from its node."


NodeList = ResourceList[Node]


class PersistentVolumeClaim(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "persistentvolumeclaims",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeClaim"]:
            return PersistentVolumeClaim

        def build(self) -> "PersistentVolumeClaim":
            return PersistentVolumeClaim(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["PersistentVolumeClaim"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: PersistentVolumeClaimSpec | None, /
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [PersistentVolumeClaimSpec.Builder],
                PersistentVolumeClaimSpec.Builder | PersistentVolumeClaimSpec,
            ],
            /,
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeClaimSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the desired characteristics of a volume requested by a pod author. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeClaimSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimSpec.builder())
                if isinstance(output, PersistentVolumeClaimSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: PersistentVolumeClaimStatus | None, /
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [PersistentVolumeClaimStatus.Builder],
                PersistentVolumeClaimStatus.Builder | PersistentVolumeClaimStatus,
            ],
            /,
        ) -> "PersistentVolumeClaim.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeClaimStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the current information/status of a persistent volume claim. Read-only. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeClaimStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimStatus.builder())
                if isinstance(output, PersistentVolumeClaimStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolumeClaim", Builder]:
        return GenericListBuilder[PersistentVolumeClaim, PersistentVolumeClaim.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["PersistentVolumeClaim"] = "PersistentVolumeClaim"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: PersistentVolumeClaimSpec | None = None
    "spec defines the desired characteristics of a volume requested by a pod author. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
    status: PersistentVolumeClaimStatus | None = None
    "status represents the current information/status of a persistent volume claim. Read-only. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"


PersistentVolumeClaimList = ResourceList[PersistentVolumeClaim]


class PersistentVolumeSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolumeSpec"]:
            return PersistentVolumeSpec

        def build(self) -> "PersistentVolumeSpec":
            return PersistentVolumeSpec(**self._attrs)

        def access_modes(self, value: list[str] | None, /) -> Self:
            "accessModes contains all ways the volume can be mounted. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes"
            return self._set("access_modes", value)

        @overload
        def aws_elastic_block_store(
            self, value_or_callback: AWSElasticBlockStoreVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def aws_elastic_block_store(
            self,
            value_or_callback: Callable[
                [AWSElasticBlockStoreVolumeSource.Builder],
                AWSElasticBlockStoreVolumeSource.Builder | AWSElasticBlockStoreVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def aws_elastic_block_store(
            self, value_or_callback: Never = ...
        ) -> "AWSElasticBlockStoreVolumeSource.BuilderContext": ...

        def aws_elastic_block_store(self, value_or_callback=UNSET, /):
            "awsElasticBlockStore represents an AWS Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
            if self._in_context and value_or_callback is UNSET:
                context = AWSElasticBlockStoreVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "aws_elastic_block_store"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AWSElasticBlockStoreVolumeSource.builder())
                if isinstance(output, AWSElasticBlockStoreVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("aws_elastic_block_store", value)

        @overload
        def azure_disk(
            self, value_or_callback: AzureDiskVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def azure_disk(
            self,
            value_or_callback: Callable[
                [AzureDiskVolumeSource.Builder],
                AzureDiskVolumeSource.Builder | AzureDiskVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def azure_disk(
            self, value_or_callback: Never = ...
        ) -> "AzureDiskVolumeSource.BuilderContext": ...

        def azure_disk(self, value_or_callback=UNSET, /):
            "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod. Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type are redirected to the disk.csi.azure.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = AzureDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "azure_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AzureDiskVolumeSource.builder())
                if isinstance(output, AzureDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("azure_disk", value)

        @overload
        def azure_file(
            self, value_or_callback: AzureFilePersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def azure_file(
            self,
            value_or_callback: Callable[
                [AzureFilePersistentVolumeSource.Builder],
                AzureFilePersistentVolumeSource.Builder | AzureFilePersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def azure_file(
            self, value_or_callback: Never = ...
        ) -> "AzureFilePersistentVolumeSource.BuilderContext": ...

        def azure_file(self, value_or_callback=UNSET, /):
            "azureFile represents an Azure File Service mount on the host and bind mount to the pod. Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type are redirected to the file.csi.azure.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = AzureFilePersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "azure_file"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AzureFilePersistentVolumeSource.builder())
                if isinstance(output, AzureFilePersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("azure_file", value)

        def capacity(self, value: dict[str, Quantity] | None, /) -> Self:
            "capacity is the description of the persistent volume\u0027s resources and capacity. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#capacity"
            return self._set("capacity", value)

        @overload
        def cephfs(
            self, value_or_callback: CephFSPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def cephfs(
            self,
            value_or_callback: Callable[
                [CephFSPersistentVolumeSource.Builder],
                CephFSPersistentVolumeSource.Builder | CephFSPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def cephfs(
            self, value_or_callback: Never = ...
        ) -> "CephFSPersistentVolumeSource.BuilderContext": ...

        def cephfs(self, value_or_callback=UNSET, /):
            "cephFS represents a Ceph FS mount on the host that shares a pod\u0027s lifetime. Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = CephFSPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "cephfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CephFSPersistentVolumeSource.builder())
                if isinstance(output, CephFSPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cephfs", value)

        @overload
        def cinder(
            self, value_or_callback: CinderPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def cinder(
            self,
            value_or_callback: Callable[
                [CinderPersistentVolumeSource.Builder],
                CinderPersistentVolumeSource.Builder | CinderPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def cinder(
            self, value_or_callback: Never = ...
        ) -> "CinderPersistentVolumeSource.BuilderContext": ...

        def cinder(self, value_or_callback=UNSET, /):
            "cinder represents a cinder volume attached and mounted on kubelets host machine. Deprecated: Cinder is deprecated. All operations for the in-tree cinder type are redirected to the cinder.csi.openstack.org CSI driver. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            if self._in_context and value_or_callback is UNSET:
                context = CinderPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "cinder"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CinderPersistentVolumeSource.builder())
                if isinstance(output, CinderPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cinder", value)

        @overload
        def claim_ref(
            self, value_or_callback: ObjectReference | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def claim_ref(
            self,
            value_or_callback: Callable[
                [ObjectReference.Builder], ObjectReference.Builder | ObjectReference
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def claim_ref(self, value_or_callback: Never = ...) -> "ObjectReference.BuilderContext": ...

        def claim_ref(self, value_or_callback=UNSET, /):
            "claimRef is part of a bi-directional binding between PersistentVolume and PersistentVolumeClaim. Expected to be non-nil when bound. claim.VolumeName is the authoritative bind between PV and PVC. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#binding"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "claim_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectReference.builder())
                if isinstance(output, ObjectReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("claim_ref", value)

        @overload
        def csi(
            self, value_or_callback: CSIPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def csi(
            self,
            value_or_callback: Callable[
                [CSIPersistentVolumeSource.Builder],
                CSIPersistentVolumeSource.Builder | CSIPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def csi(
            self, value_or_callback: Never = ...
        ) -> "CSIPersistentVolumeSource.BuilderContext": ...

        def csi(self, value_or_callback=UNSET, /):
            "csi represents storage that is handled by an external CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = CSIPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "csi"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSIPersistentVolumeSource.builder())
                if isinstance(output, CSIPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("csi", value)

        @overload
        def fc(
            self, value_or_callback: FCVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def fc(
            self,
            value_or_callback: Callable[
                [FCVolumeSource.Builder], FCVolumeSource.Builder | FCVolumeSource
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def fc(self, value_or_callback: Never = ...) -> "FCVolumeSource.BuilderContext": ...

        def fc(self, value_or_callback=UNSET, /):
            "fc represents a Fibre Channel resource that is attached to a kubelet\u0027s host machine and then exposed to the pod."
            if self._in_context and value_or_callback is UNSET:
                context = FCVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "fc"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FCVolumeSource.builder())
                if isinstance(output, FCVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("fc", value)

        @overload
        def flex_volume(
            self, value_or_callback: FlexPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def flex_volume(
            self,
            value_or_callback: Callable[
                [FlexPersistentVolumeSource.Builder],
                FlexPersistentVolumeSource.Builder | FlexPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def flex_volume(
            self, value_or_callback: Never = ...
        ) -> "FlexPersistentVolumeSource.BuilderContext": ...

        def flex_volume(self, value_or_callback=UNSET, /):
            "flexVolume represents a generic volume resource that is provisioned/attached using an exec based plugin. Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead."
            if self._in_context and value_or_callback is UNSET:
                context = FlexPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "flex_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FlexPersistentVolumeSource.builder())
                if isinstance(output, FlexPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("flex_volume", value)

        @overload
        def flocker(
            self, value_or_callback: FlockerVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def flocker(
            self,
            value_or_callback: Callable[
                [FlockerVolumeSource.Builder],
                FlockerVolumeSource.Builder | FlockerVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def flocker(
            self, value_or_callback: Never = ...
        ) -> "FlockerVolumeSource.BuilderContext": ...

        def flocker(self, value_or_callback=UNSET, /):
            "flocker represents a Flocker volume attached to a kubelet\u0027s host machine and exposed to the pod for its usage. This depends on the Flocker control service being running. Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = FlockerVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "flocker"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FlockerVolumeSource.builder())
                if isinstance(output, FlockerVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("flocker", value)

        @overload
        def gce_persistent_disk(
            self, value_or_callback: GCEPersistentDiskVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def gce_persistent_disk(
            self,
            value_or_callback: Callable[
                [GCEPersistentDiskVolumeSource.Builder],
                GCEPersistentDiskVolumeSource.Builder | GCEPersistentDiskVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def gce_persistent_disk(
            self, value_or_callback: Never = ...
        ) -> "GCEPersistentDiskVolumeSource.BuilderContext": ...

        def gce_persistent_disk(self, value_or_callback=UNSET, /):
            "gcePersistentDisk represents a GCE Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Provisioned by an admin. Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
            if self._in_context and value_or_callback is UNSET:
                context = GCEPersistentDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "gce_persistent_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GCEPersistentDiskVolumeSource.builder())
                if isinstance(output, GCEPersistentDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("gce_persistent_disk", value)

        @overload
        def glusterfs(
            self, value_or_callback: GlusterfsPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def glusterfs(
            self,
            value_or_callback: Callable[
                [GlusterfsPersistentVolumeSource.Builder],
                GlusterfsPersistentVolumeSource.Builder | GlusterfsPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def glusterfs(
            self, value_or_callback: Never = ...
        ) -> "GlusterfsPersistentVolumeSource.BuilderContext": ...

        def glusterfs(self, value_or_callback=UNSET, /):
            "glusterfs represents a Glusterfs volume that is attached to a host and exposed to the pod. Provisioned by an admin. Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported. More info: https://examples.k8s.io/volumes/glusterfs/README.md"
            if self._in_context and value_or_callback is UNSET:
                context = GlusterfsPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "glusterfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GlusterfsPersistentVolumeSource.builder())
                if isinstance(output, GlusterfsPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("glusterfs", value)

        @overload
        def host_path(
            self, value_or_callback: HostPathVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def host_path(
            self,
            value_or_callback: Callable[
                [HostPathVolumeSource.Builder],
                HostPathVolumeSource.Builder | HostPathVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def host_path(
            self, value_or_callback: Never = ...
        ) -> "HostPathVolumeSource.BuilderContext": ...

        def host_path(self, value_or_callback=UNSET, /):
            "hostPath represents a directory on the host. Provisioned by a developer or tester. This is useful for single-node development and testing only! On-host storage is not supported in any way and WILL NOT WORK in a multi-node cluster. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
            if self._in_context and value_or_callback is UNSET:
                context = HostPathVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "host_path"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HostPathVolumeSource.builder())
                if isinstance(output, HostPathVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("host_path", value)

        @overload
        def iscsi(
            self, value_or_callback: ISCSIPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def iscsi(
            self,
            value_or_callback: Callable[
                [ISCSIPersistentVolumeSource.Builder],
                ISCSIPersistentVolumeSource.Builder | ISCSIPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def iscsi(
            self, value_or_callback: Never = ...
        ) -> "ISCSIPersistentVolumeSource.BuilderContext": ...

        def iscsi(self, value_or_callback=UNSET, /):
            "iscsi represents an ISCSI Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Provisioned by an admin."
            if self._in_context and value_or_callback is UNSET:
                context = ISCSIPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "iscsi"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ISCSIPersistentVolumeSource.builder())
                if isinstance(output, ISCSIPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("iscsi", value)

        @overload
        def local(
            self, value_or_callback: LocalVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def local(
            self,
            value_or_callback: Callable[
                [LocalVolumeSource.Builder],
                LocalVolumeSource.Builder | LocalVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def local(self, value_or_callback: Never = ...) -> "LocalVolumeSource.BuilderContext": ...

        def local(self, value_or_callback=UNSET, /):
            "local represents directly-attached storage with node affinity"
            if self._in_context and value_or_callback is UNSET:
                context = LocalVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "local"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalVolumeSource.builder())
                if isinstance(output, LocalVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("local", value)

        def mount_options(self, value: list[str] | None, /) -> Self:
            'mountOptions is the list of mount options, e.g. ["ro", "soft"]. Not validated - mount will simply fail if one is invalid. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes/#mount-options'
            return self._set("mount_options", value)

        @overload
        def nfs(
            self, value_or_callback: NFSVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def nfs(
            self,
            value_or_callback: Callable[
                [NFSVolumeSource.Builder], NFSVolumeSource.Builder | NFSVolumeSource
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def nfs(self, value_or_callback: Never = ...) -> "NFSVolumeSource.BuilderContext": ...

        def nfs(self, value_or_callback=UNSET, /):
            "nfs represents an NFS mount on the host. Provisioned by an admin. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
            if self._in_context and value_or_callback is UNSET:
                context = NFSVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "nfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NFSVolumeSource.builder())
                if isinstance(output, NFSVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("nfs", value)

        @overload
        def node_affinity(
            self, value_or_callback: VolumeNodeAffinity | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def node_affinity(
            self,
            value_or_callback: Callable[
                [VolumeNodeAffinity.Builder],
                VolumeNodeAffinity.Builder | VolumeNodeAffinity,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def node_affinity(
            self, value_or_callback: Never = ...
        ) -> "VolumeNodeAffinity.BuilderContext": ...

        def node_affinity(self, value_or_callback=UNSET, /):
            "nodeAffinity defines constraints that limit what nodes this volume can be accessed from. This field influences the scheduling of pods that use this volume."
            if self._in_context and value_or_callback is UNSET:
                context = VolumeNodeAffinity.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_affinity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeNodeAffinity.builder())
                if isinstance(output, VolumeNodeAffinity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_affinity", value)

        def persistent_volume_reclaim_policy(self, value: str | None, /) -> Self:
            "persistentVolumeReclaimPolicy defines what happens to a persistent volume when released from its claim. Valid options are Retain (default for manually created PersistentVolumes), Delete (default for dynamically provisioned PersistentVolumes), and Recycle (deprecated). Recycle must be supported by the volume plugin underlying this PersistentVolume. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#reclaiming"
            return self._set("persistent_volume_reclaim_policy", value)

        @overload
        def photon_persistent_disk(
            self, value_or_callback: PhotonPersistentDiskVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def photon_persistent_disk(
            self,
            value_or_callback: Callable[
                [PhotonPersistentDiskVolumeSource.Builder],
                PhotonPersistentDiskVolumeSource.Builder | PhotonPersistentDiskVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def photon_persistent_disk(
            self, value_or_callback: Never = ...
        ) -> "PhotonPersistentDiskVolumeSource.BuilderContext": ...

        def photon_persistent_disk(self, value_or_callback=UNSET, /):
            "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine. Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = PhotonPersistentDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "photon_persistent_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PhotonPersistentDiskVolumeSource.builder())
                if isinstance(output, PhotonPersistentDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("photon_persistent_disk", value)

        @overload
        def portworx_volume(
            self, value_or_callback: PortworxVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def portworx_volume(
            self,
            value_or_callback: Callable[
                [PortworxVolumeSource.Builder],
                PortworxVolumeSource.Builder | PortworxVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def portworx_volume(
            self, value_or_callback: Never = ...
        ) -> "PortworxVolumeSource.BuilderContext": ...

        def portworx_volume(self, value_or_callback=UNSET, /):
            "portworxVolume represents a portworx volume attached and mounted on kubelets host machine. Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate is on."
            if self._in_context and value_or_callback is UNSET:
                context = PortworxVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "portworx_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PortworxVolumeSource.builder())
                if isinstance(output, PortworxVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("portworx_volume", value)

        @overload
        def quobyte(
            self, value_or_callback: QuobyteVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def quobyte(
            self,
            value_or_callback: Callable[
                [QuobyteVolumeSource.Builder],
                QuobyteVolumeSource.Builder | QuobyteVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def quobyte(
            self, value_or_callback: Never = ...
        ) -> "QuobyteVolumeSource.BuilderContext": ...

        def quobyte(self, value_or_callback=UNSET, /):
            "quobyte represents a Quobyte mount on the host that shares a pod\u0027s lifetime. Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = QuobyteVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "quobyte"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(QuobyteVolumeSource.builder())
                if isinstance(output, QuobyteVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("quobyte", value)

        @overload
        def rbd(
            self, value_or_callback: RBDPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def rbd(
            self,
            value_or_callback: Callable[
                [RBDPersistentVolumeSource.Builder],
                RBDPersistentVolumeSource.Builder | RBDPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def rbd(
            self, value_or_callback: Never = ...
        ) -> "RBDPersistentVolumeSource.BuilderContext": ...

        def rbd(self, value_or_callback=UNSET, /):
            "rbd represents a Rados Block Device mount on the host that shares a pod\u0027s lifetime. Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported. More info: https://examples.k8s.io/volumes/rbd/README.md"
            if self._in_context and value_or_callback is UNSET:
                context = RBDPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "rbd"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(RBDPersistentVolumeSource.builder())
                if isinstance(output, RBDPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("rbd", value)

        @overload
        def scale_io(
            self, value_or_callback: ScaleIOPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def scale_io(
            self,
            value_or_callback: Callable[
                [ScaleIOPersistentVolumeSource.Builder],
                ScaleIOPersistentVolumeSource.Builder | ScaleIOPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def scale_io(
            self, value_or_callback: Never = ...
        ) -> "ScaleIOPersistentVolumeSource.BuilderContext": ...

        def scale_io(self, value_or_callback=UNSET, /):
            "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes. Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = ScaleIOPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "scale_io"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ScaleIOPersistentVolumeSource.builder())
                if isinstance(output, ScaleIOPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scale_io", value)

        def storage_class_name(self, value: str | None, /) -> Self:
            "storageClassName is the name of StorageClass to which this persistent volume belongs. Empty value means that this volume does not belong to any StorageClass."
            return self._set("storage_class_name", value)

        @overload
        def storageos(
            self, value_or_callback: StorageOSPersistentVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def storageos(
            self,
            value_or_callback: Callable[
                [StorageOSPersistentVolumeSource.Builder],
                StorageOSPersistentVolumeSource.Builder | StorageOSPersistentVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def storageos(
            self, value_or_callback: Never = ...
        ) -> "StorageOSPersistentVolumeSource.BuilderContext": ...

        def storageos(self, value_or_callback=UNSET, /):
            "storageOS represents a StorageOS volume that is attached to the kubelet\u0027s host machine and mounted into the pod. Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported. More info: https://examples.k8s.io/volumes/storageos/README.md"
            if self._in_context and value_or_callback is UNSET:
                context = StorageOSPersistentVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "storageos"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageOSPersistentVolumeSource.builder())
                if isinstance(output, StorageOSPersistentVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("storageos", value)

        def volume_attributes_class_name(self, value: str | None, /) -> Self:
            "Name of VolumeAttributesClass to which this persistent volume belongs. Empty value is not allowed. When this field is not set, it indicates that this volume does not belong to any VolumeAttributesClass. This field is mutable and can be changed by the CSI driver after a volume has been updated successfully to a new class. For an unbound PersistentVolume, the volumeAttributesClassName will be matched with unbound PersistentVolumeClaims during the binding process."
            return self._set("volume_attributes_class_name", value)

        def volume_mode(self, value: str | None, /) -> Self:
            "volumeMode defines if a volume is intended to be used with a formatted filesystem or to remain in raw block state. Value of Filesystem is implied when not included in spec."
            return self._set("volume_mode", value)

        @overload
        def vsphere_volume(
            self, value_or_callback: VsphereVirtualDiskVolumeSource | None, /
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def vsphere_volume(
            self,
            value_or_callback: Callable[
                [VsphereVirtualDiskVolumeSource.Builder],
                VsphereVirtualDiskVolumeSource.Builder | VsphereVirtualDiskVolumeSource,
            ],
            /,
        ) -> "PersistentVolumeSpec.Builder": ...

        @overload
        def vsphere_volume(
            self, value_or_callback: Never = ...
        ) -> "VsphereVirtualDiskVolumeSource.BuilderContext": ...

        def vsphere_volume(self, value_or_callback=UNSET, /):
            "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine. Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type are redirected to the csi.vsphere.vmware.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = VsphereVirtualDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "vsphere_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VsphereVirtualDiskVolumeSource.builder())
                if isinstance(output, VsphereVirtualDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("vsphere_volume", value)

    class BuilderContext(BuilderContextBase["PersistentVolumeSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolumeSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolumeSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolumeSpec", Builder]:
        return GenericListBuilder[PersistentVolumeSpec, PersistentVolumeSpec.Builder]()

    access_modes: Annotated[list[str] | None, Field(alias="accessModes")] = None
    "accessModes contains all ways the volume can be mounted. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes"
    aws_elastic_block_store: Annotated[
        AWSElasticBlockStoreVolumeSource | None, Field(alias="awsElasticBlockStore")
    ] = None
    "awsElasticBlockStore represents an AWS Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
    azure_disk: Annotated[AzureDiskVolumeSource | None, Field(alias="azureDisk")] = None
    "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod. Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type are redirected to the disk.csi.azure.com CSI driver."
    azure_file: Annotated[AzureFilePersistentVolumeSource | None, Field(alias="azureFile")] = None
    "azureFile represents an Azure File Service mount on the host and bind mount to the pod. Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type are redirected to the file.csi.azure.com CSI driver."
    capacity: dict[str, Quantity] | None = None
    "capacity is the description of the persistent volume\u0027s resources and capacity. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#capacity"
    cephfs: CephFSPersistentVolumeSource | None = None
    "cephFS represents a Ceph FS mount on the host that shares a pod\u0027s lifetime. Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported."
    cinder: CinderPersistentVolumeSource | None = None
    "cinder represents a cinder volume attached and mounted on kubelets host machine. Deprecated: Cinder is deprecated. All operations for the in-tree cinder type are redirected to the cinder.csi.openstack.org CSI driver. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
    claim_ref: Annotated[ObjectReference | None, Field(alias="claimRef")] = None
    "claimRef is part of a bi-directional binding between PersistentVolume and PersistentVolumeClaim. Expected to be non-nil when bound. claim.VolumeName is the authoritative bind between PV and PVC. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#binding"
    csi: CSIPersistentVolumeSource | None = None
    "csi represents storage that is handled by an external CSI driver."
    fc: FCVolumeSource | None = None
    "fc represents a Fibre Channel resource that is attached to a kubelet\u0027s host machine and then exposed to the pod."
    flex_volume: Annotated[FlexPersistentVolumeSource | None, Field(alias="flexVolume")] = None
    "flexVolume represents a generic volume resource that is provisioned/attached using an exec based plugin. Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead."
    flocker: FlockerVolumeSource | None = None
    "flocker represents a Flocker volume attached to a kubelet\u0027s host machine and exposed to the pod for its usage. This depends on the Flocker control service being running. Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported."
    gce_persistent_disk: Annotated[
        GCEPersistentDiskVolumeSource | None, Field(alias="gcePersistentDisk")
    ] = None
    "gcePersistentDisk represents a GCE Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Provisioned by an admin. Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
    glusterfs: GlusterfsPersistentVolumeSource | None = None
    "glusterfs represents a Glusterfs volume that is attached to a host and exposed to the pod. Provisioned by an admin. Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported. More info: https://examples.k8s.io/volumes/glusterfs/README.md"
    host_path: Annotated[HostPathVolumeSource | None, Field(alias="hostPath")] = None
    "hostPath represents a directory on the host. Provisioned by a developer or tester. This is useful for single-node development and testing only! On-host storage is not supported in any way and WILL NOT WORK in a multi-node cluster. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
    iscsi: ISCSIPersistentVolumeSource | None = None
    "iscsi represents an ISCSI Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Provisioned by an admin."
    local: LocalVolumeSource | None = None
    "local represents directly-attached storage with node affinity"
    mount_options: Annotated[list[str] | None, Field(alias="mountOptions")] = None
    'mountOptions is the list of mount options, e.g. ["ro", "soft"]. Not validated - mount will simply fail if one is invalid. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes/#mount-options'
    nfs: NFSVolumeSource | None = None
    "nfs represents an NFS mount on the host. Provisioned by an admin. More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
    node_affinity: Annotated[VolumeNodeAffinity | None, Field(alias="nodeAffinity")] = None
    "nodeAffinity defines constraints that limit what nodes this volume can be accessed from. This field influences the scheduling of pods that use this volume."
    persistent_volume_reclaim_policy: Annotated[
        str | None, Field(alias="persistentVolumeReclaimPolicy")
    ] = None
    "persistentVolumeReclaimPolicy defines what happens to a persistent volume when released from its claim. Valid options are Retain (default for manually created PersistentVolumes), Delete (default for dynamically provisioned PersistentVolumes), and Recycle (deprecated). Recycle must be supported by the volume plugin underlying this PersistentVolume. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#reclaiming"
    photon_persistent_disk: Annotated[
        PhotonPersistentDiskVolumeSource | None, Field(alias="photonPersistentDisk")
    ] = None
    "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine. Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported."
    portworx_volume: Annotated[PortworxVolumeSource | None, Field(alias="portworxVolume")] = None
    "portworxVolume represents a portworx volume attached and mounted on kubelets host machine. Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate is on."
    quobyte: QuobyteVolumeSource | None = None
    "quobyte represents a Quobyte mount on the host that shares a pod\u0027s lifetime. Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported."
    rbd: RBDPersistentVolumeSource | None = None
    "rbd represents a Rados Block Device mount on the host that shares a pod\u0027s lifetime. Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported. More info: https://examples.k8s.io/volumes/rbd/README.md"
    scale_io: Annotated[ScaleIOPersistentVolumeSource | None, Field(alias="scaleIO")] = None
    "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes. Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported."
    storage_class_name: Annotated[str | None, Field(alias="storageClassName")] = None
    "storageClassName is the name of StorageClass to which this persistent volume belongs. Empty value means that this volume does not belong to any StorageClass."
    storageos: StorageOSPersistentVolumeSource | None = None
    "storageOS represents a StorageOS volume that is attached to the kubelet\u0027s host machine and mounted into the pod. Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported. More info: https://examples.k8s.io/volumes/storageos/README.md"
    volume_attributes_class_name: Annotated[
        str | None, Field(alias="volumeAttributesClassName")
    ] = None
    "Name of VolumeAttributesClass to which this persistent volume belongs. Empty value is not allowed. When this field is not set, it indicates that this volume does not belong to any VolumeAttributesClass. This field is mutable and can be changed by the CSI driver after a volume has been updated successfully to a new class. For an unbound PersistentVolume, the volumeAttributesClassName will be matched with unbound PersistentVolumeClaims during the binding process."
    volume_mode: Annotated[str | None, Field(alias="volumeMode")] = None
    "volumeMode defines if a volume is intended to be used with a formatted filesystem or to remain in raw block state. Value of Filesystem is implied when not included in spec."
    vsphere_volume: Annotated[
        VsphereVirtualDiskVolumeSource | None, Field(alias="vsphereVolume")
    ] = None
    "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine. Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type are redirected to the csi.vsphere.vmware.com CSI driver."


class PodStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodStatus"]:
            return PodStatus

        def build(self) -> "PodStatus":
            return PodStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[PodCondition] | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodCondition, PodCondition.Builder]],
                GenericListBuilder[PodCondition, PodCondition.Builder] | list[PodCondition],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodCondition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Current service state of pod. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodCondition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodCondition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def container_statuses(
            self, value_or_callback: list[ContainerStatus] | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def container_statuses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerStatus, ContainerStatus.Builder]],
                GenericListBuilder[ContainerStatus, ContainerStatus.Builder]
                | list[ContainerStatus],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def container_statuses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerStatus.Builder]: ...

        def container_statuses(self, value_or_callback=UNSET, /):
            "Statuses of containers in this pod. Each container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-and-container-status"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerStatus.Builder]()
                context._parent_builder = self
                context._field_name = "container_statuses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("container_statuses", value)

        @overload
        def ephemeral_container_statuses(
            self, value_or_callback: list[ContainerStatus] | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def ephemeral_container_statuses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerStatus, ContainerStatus.Builder]],
                GenericListBuilder[ContainerStatus, ContainerStatus.Builder]
                | list[ContainerStatus],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def ephemeral_container_statuses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerStatus.Builder]: ...

        def ephemeral_container_statuses(self, value_or_callback=UNSET, /):
            "Statuses for any ephemeral containers that have run in this pod. Each ephemeral container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-and-container-status"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerStatus.Builder]()
                context._parent_builder = self
                context._field_name = "ephemeral_container_statuses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ephemeral_container_statuses", value)

        @overload
        def extended_resource_claim_status(
            self, value_or_callback: PodExtendedResourceClaimStatus | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def extended_resource_claim_status(
            self,
            value_or_callback: Callable[
                [PodExtendedResourceClaimStatus.Builder],
                PodExtendedResourceClaimStatus.Builder | PodExtendedResourceClaimStatus,
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def extended_resource_claim_status(
            self, value_or_callback: Never = ...
        ) -> "PodExtendedResourceClaimStatus.BuilderContext": ...

        def extended_resource_claim_status(self, value_or_callback=UNSET, /):
            "Status of extended resource claim backed by DRA."
            if self._in_context and value_or_callback is UNSET:
                context = PodExtendedResourceClaimStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "extended_resource_claim_status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodExtendedResourceClaimStatus.builder())
                if isinstance(output, PodExtendedResourceClaimStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("extended_resource_claim_status", value)

        def host_ip(self, value: str | None, /) -> Self:
            "hostIP holds the IP address of the host to which the pod is assigned. Empty if the pod has not started yet. A pod can be assigned to a node that has a problem in kubelet which in turns mean that HostIP will not be updated even if there is a node is assigned to pod"
            return self._set("host_ip", value)

        @overload
        def host_i_ps(self, value_or_callback: list[HostIP] | None, /) -> "PodStatus.Builder": ...

        @overload
        def host_i_ps(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[HostIP, HostIP.Builder]],
                GenericListBuilder[HostIP, HostIP.Builder] | list[HostIP],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def host_i_ps(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[HostIP.Builder]: ...

        def host_i_ps(self, value_or_callback=UNSET, /):
            "hostIPs holds the IP addresses allocated to the host. If this field is specified, the first entry must match the hostIP field. This list is empty if the pod has not started yet. A pod can be assigned to a node that has a problem in kubelet which in turns means that HostIPs will not be updated even if there is a node is assigned to this pod."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[HostIP.Builder]()
                context._parent_builder = self
                context._field_name = "host_i_ps"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HostIP.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("host_i_ps", value)

        @overload
        def init_container_statuses(
            self, value_or_callback: list[ContainerStatus] | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def init_container_statuses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerStatus, ContainerStatus.Builder]],
                GenericListBuilder[ContainerStatus, ContainerStatus.Builder]
                | list[ContainerStatus],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def init_container_statuses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerStatus.Builder]: ...

        def init_container_statuses(self, value_or_callback=UNSET, /):
            "Statuses of init containers in this pod. The most recent successful non-restartable init container will have ready = true, the most recently started container will have startTime set. Each init container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-and-container-status"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerStatus.Builder]()
                context._parent_builder = self
                context._field_name = "init_container_statuses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("init_container_statuses", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about why the pod is in this condition."
            return self._set("message", value)

        def nominated_node_name(self, value: str | None, /) -> Self:
            "nominatedNodeName is set only when this pod preempts other pods on the node, but it cannot be scheduled right away as preemption victims receive their graceful termination periods. This field does not guarantee that the pod will be scheduled on this node. Scheduler may decide to place the pod elsewhere if other nodes become available sooner. Scheduler may also decide to give the resources on this node to a higher priority pod that is created after preemption. As a result, this field may be different than PodSpec.nodeName when the pod is scheduled."
            return self._set("nominated_node_name", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "If set, this represents the .metadata.generation that the pod status was set based upon. This is an alpha field. Enable PodObservedGenerationTracking to be able to use this field."
            return self._set("observed_generation", value)

        def phase(self, value: str | None, /) -> Self:
            "The phase of a Pod is a simple, high-level summary of where the Pod is in its lifecycle. The conditions array, the reason and message fields, and the individual container status arrays contain more detail about the pod\u0027s status. There are five possible phase values:\n\nPending: The pod has been accepted by the Kubernetes system, but one or more of the container images has not been created. This includes time before being scheduled as well as time spent downloading images over the network, which could take a while. Running: The pod has been bound to a node, and all of the containers have been created. At least one container is still running, or is in the process of starting or restarting. Succeeded: All containers in the pod have terminated in success, and will not be restarted. Failed: All containers in the pod have terminated, and at least one container has terminated in failure. The container either exited with non-zero status or was terminated by the system. Unknown: For some reason the state of the pod could not be obtained, typically due to an error in communicating with the host of the pod.\n\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-phase"
            return self._set("phase", value)

        def pod_ip(self, value: str | None, /) -> Self:
            "podIP address allocated to the pod. Routable at least within the cluster. Empty if not yet allocated."
            return self._set("pod_ip", value)

        @overload
        def pod_i_ps(self, value_or_callback: list[PodIP] | None, /) -> "PodStatus.Builder": ...

        @overload
        def pod_i_ps(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodIP, PodIP.Builder]],
                GenericListBuilder[PodIP, PodIP.Builder] | list[PodIP],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def pod_i_ps(self, value_or_callback: Never = ...) -> ListBuilderContext[PodIP.Builder]: ...

        def pod_i_ps(self, value_or_callback=UNSET, /):
            "podIPs holds the IP addresses allocated to the pod. If this field is specified, the 0th entry must match the podIP field. Pods may be allocated at most 1 value for each of IPv4 and IPv6. This list is empty if no IPs have been allocated yet."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodIP.Builder]()
                context._parent_builder = self
                context._field_name = "pod_i_ps"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodIP.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_i_ps", value)

        def qos_class(self, value: str | None, /) -> Self:
            "The Quality of Service (QOS) classification assigned to the pod based on resource requirements See PodQOSClass type for available QOS classes More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/#quality-of-service-classes"
            return self._set("qos_class", value)

        def reason(self, value: str | None, /) -> Self:
            "A brief CamelCase message indicating details about why the pod is in this state. e.g. \u0027Evicted\u0027"
            return self._set("reason", value)

        def resize(self, value: str | None, /) -> Self:
            'Status of resources resize desired for pod\u0027s containers. It is empty if no resources resize is pending. Any changes to container resources will automatically set this to "Proposed" Deprecated: Resize status is moved to two pod conditions PodResizePending and PodResizeInProgress. PodResizePending will track states where the spec has been resized, but the Kubelet has not yet allocated the resources. PodResizeInProgress will track in-progress resizes, and should be present whenever allocated resources != acknowledged resources.'
            return self._set("resize", value)

        @overload
        def resource_claim_statuses(
            self, value_or_callback: list[PodResourceClaimStatus] | None, /
        ) -> "PodStatus.Builder": ...

        @overload
        def resource_claim_statuses(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodResourceClaimStatus, PodResourceClaimStatus.Builder]],
                GenericListBuilder[PodResourceClaimStatus, PodResourceClaimStatus.Builder]
                | list[PodResourceClaimStatus],
            ],
            /,
        ) -> "PodStatus.Builder": ...

        @overload
        def resource_claim_statuses(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodResourceClaimStatus.Builder]: ...

        def resource_claim_statuses(self, value_or_callback=UNSET, /):
            "Status of resource claims."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodResourceClaimStatus.Builder]()
                context._parent_builder = self
                context._field_name = "resource_claim_statuses"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodResourceClaimStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_claim_statuses", value)

        @overload
        def start_time(self, value_or_callback: Time | None, /) -> "PodStatus.Builder": ...

        @overload
        def start_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "PodStatus.Builder": ...

        @overload
        def start_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def start_time(self, value_or_callback=UNSET, /):
            "RFC 3339 date and time at which the object was acknowledged by the Kubelet. This is before the Kubelet pulled the container image(s) for the pod."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "start_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("start_time", value)

    class BuilderContext(BuilderContextBase["PodStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodStatus", Builder]:
        return GenericListBuilder[PodStatus, PodStatus.Builder]()

    conditions: list[PodCondition] | None = None
    "Current service state of pod. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-conditions"
    container_statuses: Annotated[
        list[ContainerStatus] | None, Field(alias="containerStatuses")
    ] = None
    "Statuses of containers in this pod. Each container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-and-container-status"
    ephemeral_container_statuses: Annotated[
        list[ContainerStatus] | None, Field(alias="ephemeralContainerStatuses")
    ] = None
    "Statuses for any ephemeral containers that have run in this pod. Each ephemeral container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-and-container-status"
    extended_resource_claim_status: Annotated[
        PodExtendedResourceClaimStatus | None,
        Field(alias="extendedResourceClaimStatus"),
    ] = None
    "Status of extended resource claim backed by DRA."
    host_ip: Annotated[str | None, Field(alias="hostIP")] = None
    "hostIP holds the IP address of the host to which the pod is assigned. Empty if the pod has not started yet. A pod can be assigned to a node that has a problem in kubelet which in turns mean that HostIP will not be updated even if there is a node is assigned to pod"
    host_i_ps: Annotated[list[HostIP] | None, Field(alias="hostIPs")] = None
    "hostIPs holds the IP addresses allocated to the host. If this field is specified, the first entry must match the hostIP field. This list is empty if the pod has not started yet. A pod can be assigned to a node that has a problem in kubelet which in turns means that HostIPs will not be updated even if there is a node is assigned to this pod."
    init_container_statuses: Annotated[
        list[ContainerStatus] | None, Field(alias="initContainerStatuses")
    ] = None
    "Statuses of init containers in this pod. The most recent successful non-restartable init container will have ready = true, the most recently started container will have startTime set. Each init container in the pod should have at most one status in this list, and all statuses should be for containers in the pod. However this is not enforced. If a status for a non-existent container is present in the list, or the list has duplicate names, the behavior of various Kubernetes components is not defined and those statuses might be ignored. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#pod-and-container-status"
    message: str | None = None
    "A human readable message indicating details about why the pod is in this condition."
    nominated_node_name: Annotated[str | None, Field(alias="nominatedNodeName")] = None
    "nominatedNodeName is set only when this pod preempts other pods on the node, but it cannot be scheduled right away as preemption victims receive their graceful termination periods. This field does not guarantee that the pod will be scheduled on this node. Scheduler may decide to place the pod elsewhere if other nodes become available sooner. Scheduler may also decide to give the resources on this node to a higher priority pod that is created after preemption. As a result, this field may be different than PodSpec.nodeName when the pod is scheduled."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "If set, this represents the .metadata.generation that the pod status was set based upon. This is an alpha field. Enable PodObservedGenerationTracking to be able to use this field."
    phase: str | None = None
    "The phase of a Pod is a simple, high-level summary of where the Pod is in its lifecycle. The conditions array, the reason and message fields, and the individual container status arrays contain more detail about the pod\u0027s status. There are five possible phase values:\n\nPending: The pod has been accepted by the Kubernetes system, but one or more of the container images has not been created. This includes time before being scheduled as well as time spent downloading images over the network, which could take a while. Running: The pod has been bound to a node, and all of the containers have been created. At least one container is still running, or is in the process of starting or restarting. Succeeded: All containers in the pod have terminated in success, and will not be restarted. Failed: All containers in the pod have terminated, and at least one container has terminated in failure. The container either exited with non-zero status or was terminated by the system. Unknown: For some reason the state of the pod could not be obtained, typically due to an error in communicating with the host of the pod.\n\nMore info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#pod-phase"
    pod_ip: Annotated[str | None, Field(alias="podIP")] = None
    "podIP address allocated to the pod. Routable at least within the cluster. Empty if not yet allocated."
    pod_i_ps: Annotated[list[PodIP] | None, Field(alias="podIPs")] = None
    "podIPs holds the IP addresses allocated to the pod. If this field is specified, the 0th entry must match the podIP field. Pods may be allocated at most 1 value for each of IPv4 and IPv6. This list is empty if no IPs have been allocated yet."
    qos_class: Annotated[str | None, Field(alias="qosClass")] = None
    "The Quality of Service (QOS) classification assigned to the pod based on resource requirements See PodQOSClass type for available QOS classes More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/#quality-of-service-classes"
    reason: str | None = None
    "A brief CamelCase message indicating details about why the pod is in this state. e.g. \u0027Evicted\u0027"
    resize: str | None = None
    'Status of resources resize desired for pod\u0027s containers. It is empty if no resources resize is pending. Any changes to container resources will automatically set this to "Proposed" Deprecated: Resize status is moved to two pod conditions PodResizePending and PodResizeInProgress. PodResizePending will track states where the spec has been resized, but the Kubelet has not yet allocated the resources. PodResizeInProgress will track in-progress resizes, and should be present whenever allocated resources != acknowledged resources.'
    resource_claim_statuses: Annotated[
        list[PodResourceClaimStatus] | None, Field(alias="resourceClaimStatuses")
    ] = None
    "Status of resource claims."
    start_time: Annotated[Time | None, Field(alias="startTime")] = None
    "RFC 3339 date and time at which the object was acknowledged by the Kubelet. This is before the Kubelet pulled the container image(s) for the pod."


class ResourceQuotaSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceQuotaSpec"]:
            return ResourceQuotaSpec

        def build(self) -> "ResourceQuotaSpec":
            return ResourceQuotaSpec(**self._attrs)

        def hard(self, value: dict[str, Quantity] | None, /) -> Self:
            "hard is the set of desired hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/"
            return self._set("hard", value)

        @overload
        def scope_selector(
            self, value_or_callback: ScopeSelector | None, /
        ) -> "ResourceQuotaSpec.Builder": ...

        @overload
        def scope_selector(
            self,
            value_or_callback: Callable[
                [ScopeSelector.Builder], ScopeSelector.Builder | ScopeSelector
            ],
            /,
        ) -> "ResourceQuotaSpec.Builder": ...

        @overload
        def scope_selector(
            self, value_or_callback: Never = ...
        ) -> "ScopeSelector.BuilderContext": ...

        def scope_selector(self, value_or_callback=UNSET, /):
            "scopeSelector is also a collection of filters like scopes that must match each object tracked by a quota but expressed using ScopeSelectorOperator in combination with possible values. For a resource to match, both scopes AND scopeSelector (if specified in spec), must be matched."
            if self._in_context and value_or_callback is UNSET:
                context = ScopeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "scope_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ScopeSelector.builder())
                if isinstance(output, ScopeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scope_selector", value)

        def scopes(self, value: list[str] | None, /) -> Self:
            "A collection of filters that must match each object tracked by a quota. If not specified, the quota matches all objects."
            return self._set("scopes", value)

    class BuilderContext(BuilderContextBase["ResourceQuotaSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceQuotaSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceQuotaSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceQuotaSpec", Builder]:
        return GenericListBuilder[ResourceQuotaSpec, ResourceQuotaSpec.Builder]()

    hard: dict[str, Quantity] | None = None
    "hard is the set of desired hard limits for each named resource. More info: https://kubernetes.io/docs/concepts/policy/resource-quotas/"
    scope_selector: Annotated[ScopeSelector | None, Field(alias="scopeSelector")] = None
    "scopeSelector is also a collection of filters like scopes that must match each object tracked by a quota but expressed using ScopeSelectorOperator in combination with possible values. For a resource to match, both scopes AND scopeSelector (if specified in spec), must be matched."
    scopes: list[str] | None = None
    "A collection of filters that must match each object tracked by a quota. If not specified, the quota matches all objects."


class Service(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "services",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Service"]:
            return Service

        def build(self) -> "Service":
            return Service(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Service"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Service.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Service.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: ServiceSpec | None, /) -> "Service.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[ServiceSpec.Builder], ServiceSpec.Builder | ServiceSpec],
            /,
        ) -> "Service.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "ServiceSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the behavior of a service. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ServiceSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServiceSpec.builder())
                if isinstance(output, ServiceSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: ServiceStatus | None, /) -> "Service.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [ServiceStatus.Builder], ServiceStatus.Builder | ServiceStatus
            ],
            /,
        ) -> "Service.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "ServiceStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Most recently observed status of the service. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ServiceStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServiceStatus.builder())
                if isinstance(output, ServiceStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Service.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Service.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Service."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Service", Builder]:
        return GenericListBuilder[Service, Service.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Service"] = "Service"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: ServiceSpec | None = None
    "Spec defines the behavior of a service. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: ServiceStatus | None = None
    "Most recently observed status of the service. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


ServiceList = ResourceList[Service]


class VolumeProjection(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["VolumeProjection"]:
            return VolumeProjection

        def build(self) -> "VolumeProjection":
            return VolumeProjection(**self._attrs)

        @overload
        def cluster_trust_bundle(
            self, value_or_callback: ClusterTrustBundleProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def cluster_trust_bundle(
            self,
            value_or_callback: Callable[
                [ClusterTrustBundleProjection.Builder],
                ClusterTrustBundleProjection.Builder | ClusterTrustBundleProjection,
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def cluster_trust_bundle(
            self, value_or_callback: Never = ...
        ) -> "ClusterTrustBundleProjection.BuilderContext": ...

        def cluster_trust_bundle(self, value_or_callback=UNSET, /):
            "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time."
            if self._in_context and value_or_callback is UNSET:
                context = ClusterTrustBundleProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "cluster_trust_bundle"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ClusterTrustBundleProjection.builder())
                if isinstance(output, ClusterTrustBundleProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cluster_trust_bundle", value)

        @overload
        def config_map(
            self, value_or_callback: ConfigMapProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def config_map(
            self,
            value_or_callback: Callable[
                [ConfigMapProjection.Builder],
                ConfigMapProjection.Builder | ConfigMapProjection,
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def config_map(
            self, value_or_callback: Never = ...
        ) -> "ConfigMapProjection.BuilderContext": ...

        def config_map(self, value_or_callback=UNSET, /):
            "configMap information about the configMap data to project"
            if self._in_context and value_or_callback is UNSET:
                context = ConfigMapProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_map"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ConfigMapProjection.builder())
                if isinstance(output, ConfigMapProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_map", value)

        @overload
        def downward_api(
            self, value_or_callback: DownwardAPIProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def downward_api(
            self,
            value_or_callback: Callable[
                [DownwardAPIProjection.Builder],
                DownwardAPIProjection.Builder | DownwardAPIProjection,
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def downward_api(
            self, value_or_callback: Never = ...
        ) -> "DownwardAPIProjection.BuilderContext": ...

        def downward_api(self, value_or_callback=UNSET, /):
            "downwardAPI information about the downwardAPI data to project"
            if self._in_context and value_or_callback is UNSET:
                context = DownwardAPIProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "downward_api"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DownwardAPIProjection.builder())
                if isinstance(output, DownwardAPIProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("downward_api", value)

        @overload
        def pod_certificate(
            self, value_or_callback: PodCertificateProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def pod_certificate(
            self,
            value_or_callback: Callable[
                [PodCertificateProjection.Builder],
                PodCertificateProjection.Builder | PodCertificateProjection,
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def pod_certificate(
            self, value_or_callback: Never = ...
        ) -> "PodCertificateProjection.BuilderContext": ...

        def pod_certificate(self, value_or_callback=UNSET, /):
            "Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.\n\nThe named signer controls chooses the format of the certificate it issues; consult the signer implementation\u0027s documentation to learn how to use the certificates it issues."
            if self._in_context and value_or_callback is UNSET:
                context = PodCertificateProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod_certificate"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodCertificateProjection.builder())
                if isinstance(output, PodCertificateProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_certificate", value)

        @overload
        def secret(
            self, value_or_callback: SecretProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def secret(
            self,
            value_or_callback: Callable[
                [SecretProjection.Builder], SecretProjection.Builder | SecretProjection
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def secret(self, value_or_callback: Never = ...) -> "SecretProjection.BuilderContext": ...

        def secret(self, value_or_callback=UNSET, /):
            "secret information about the secret data to project"
            if self._in_context and value_or_callback is UNSET:
                context = SecretProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretProjection.builder())
                if isinstance(output, SecretProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret", value)

        @overload
        def service_account_token(
            self, value_or_callback: ServiceAccountTokenProjection | None, /
        ) -> "VolumeProjection.Builder": ...

        @overload
        def service_account_token(
            self,
            value_or_callback: Callable[
                [ServiceAccountTokenProjection.Builder],
                ServiceAccountTokenProjection.Builder | ServiceAccountTokenProjection,
            ],
            /,
        ) -> "VolumeProjection.Builder": ...

        @overload
        def service_account_token(
            self, value_or_callback: Never = ...
        ) -> "ServiceAccountTokenProjection.BuilderContext": ...

        def service_account_token(self, value_or_callback=UNSET, /):
            "serviceAccountToken is information about the serviceAccountToken data to project"
            if self._in_context and value_or_callback is UNSET:
                context = ServiceAccountTokenProjection.BuilderContext()
                context._parent_builder = self
                context._field_name = "service_account_token"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServiceAccountTokenProjection.builder())
                if isinstance(output, ServiceAccountTokenProjection.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("service_account_token", value)

    class BuilderContext(BuilderContextBase["VolumeProjection.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = VolumeProjection.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for VolumeProjection."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["VolumeProjection", Builder]:
        return GenericListBuilder[VolumeProjection, VolumeProjection.Builder]()

    cluster_trust_bundle: Annotated[
        ClusterTrustBundleProjection | None, Field(alias="clusterTrustBundle")
    ] = None
    "ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field of ClusterTrustBundle objects in an auto-updating file.\n\nAlpha, gated by the ClusterTrustBundleProjection feature gate.\n\nClusterTrustBundle objects can either be selected by name, or by the combination of signer name and a label selector.\n\nKubelet performs aggressive normalization of the PEM contents written into the pod filesystem.  Esoteric PEM features such as inter-block comments and block headers are stripped.  Certificates are deduplicated. The ordering of certificates within the file is arbitrary, and Kubelet may change the order over time."
    config_map: Annotated[ConfigMapProjection | None, Field(alias="configMap")] = None
    "configMap information about the configMap data to project"
    downward_api: Annotated[DownwardAPIProjection | None, Field(alias="downwardAPI")] = None
    "downwardAPI information about the downwardAPI data to project"
    pod_certificate: Annotated[PodCertificateProjection | None, Field(alias="podCertificate")] = (
        None
    )
    "Projects an auto-rotating credential bundle (private key and certificate chain) that the pod can use either as a TLS client or server.\n\nKubelet generates a private key and uses it to send a PodCertificateRequest to the named signer.  Once the signer approves the request and issues a certificate chain, Kubelet writes the key and certificate chain to the pod filesystem.  The pod does not start until certificates have been issued for each podCertificate projected volume source in its spec.\n\nKubelet will begin trying to rotate the certificate at the time indicated by the signer using the PodCertificateRequest.Status.BeginRefreshAt timestamp.\n\nKubelet can write a single file, indicated by the credentialBundlePath field, or separate files, indicated by the keyPath and certificateChainPath fields.\n\nThe credential bundle is a single file in PEM format.  The first PEM entry is the private key (in PKCS#8 format), and the remaining PEM entries are the certificate chain issued by the signer (typically, signers will return their certificate chain in leaf-to-root order).\n\nPrefer using the credential bundle format, since your application code can read it atomically.  If you use keyPath and certificateChainPath, your application must make two separate file reads. If these coincide with a certificate rotation, it is possible that the private key and leaf certificate you read may not correspond to each other.  Your application will need to check for this condition, and re-read until they are consistent.\n\nThe named signer controls chooses the format of the certificate it issues; consult the signer implementation\u0027s documentation to learn how to use the certificates it issues."
    secret: SecretProjection | None = None
    "secret information about the secret data to project"
    service_account_token: Annotated[
        ServiceAccountTokenProjection | None, Field(alias="serviceAccountToken")
    ] = None
    "serviceAccountToken is information about the serviceAccountToken data to project"


class Affinity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Affinity"]:
            return Affinity

        def build(self) -> "Affinity":
            return Affinity(**self._attrs)

        @overload
        def node_affinity(
            self, value_or_callback: NodeAffinity | None, /
        ) -> "Affinity.Builder": ...

        @overload
        def node_affinity(
            self,
            value_or_callback: Callable[
                [NodeAffinity.Builder], NodeAffinity.Builder | NodeAffinity
            ],
            /,
        ) -> "Affinity.Builder": ...

        @overload
        def node_affinity(
            self, value_or_callback: Never = ...
        ) -> "NodeAffinity.BuilderContext": ...

        def node_affinity(self, value_or_callback=UNSET, /):
            "Describes node affinity scheduling rules for the pod."
            if self._in_context and value_or_callback is UNSET:
                context = NodeAffinity.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_affinity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NodeAffinity.builder())
                if isinstance(output, NodeAffinity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_affinity", value)

        @overload
        def pod_affinity(self, value_or_callback: PodAffinity | None, /) -> "Affinity.Builder": ...

        @overload
        def pod_affinity(
            self,
            value_or_callback: Callable[[PodAffinity.Builder], PodAffinity.Builder | PodAffinity],
            /,
        ) -> "Affinity.Builder": ...

        @overload
        def pod_affinity(self, value_or_callback: Never = ...) -> "PodAffinity.BuilderContext": ...

        def pod_affinity(self, value_or_callback=UNSET, /):
            "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s))."
            if self._in_context and value_or_callback is UNSET:
                context = PodAffinity.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod_affinity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodAffinity.builder())
                if isinstance(output, PodAffinity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_affinity", value)

        @overload
        def pod_anti_affinity(
            self, value_or_callback: PodAntiAffinity | None, /
        ) -> "Affinity.Builder": ...

        @overload
        def pod_anti_affinity(
            self,
            value_or_callback: Callable[
                [PodAntiAffinity.Builder], PodAntiAffinity.Builder | PodAntiAffinity
            ],
            /,
        ) -> "Affinity.Builder": ...

        @overload
        def pod_anti_affinity(
            self, value_or_callback: Never = ...
        ) -> "PodAntiAffinity.BuilderContext": ...

        def pod_anti_affinity(self, value_or_callback=UNSET, /):
            "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s))."
            if self._in_context and value_or_callback is UNSET:
                context = PodAntiAffinity.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod_anti_affinity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodAntiAffinity.builder())
                if isinstance(output, PodAntiAffinity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod_anti_affinity", value)

    class BuilderContext(BuilderContextBase["Affinity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Affinity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Affinity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Affinity", Builder]:
        return GenericListBuilder[Affinity, Affinity.Builder]()

    node_affinity: Annotated[NodeAffinity | None, Field(alias="nodeAffinity")] = None
    "Describes node affinity scheduling rules for the pod."
    pod_affinity: Annotated[PodAffinity | None, Field(alias="podAffinity")] = None
    "Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s))."
    pod_anti_affinity: Annotated[PodAntiAffinity | None, Field(alias="podAntiAffinity")] = None
    "Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s))."


class Container(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Container"]:
            return Container

        def build(self) -> "Container":
            return Container(**self._attrs)

        def args(self, value: list[str] | None, /) -> Self:
            'Arguments to the entrypoint. The container image\u0027s CMD is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
            return self._set("args", value)

        def command(self, value: list[str] | None, /) -> Self:
            'Entrypoint array. Not executed within a shell. The container image\u0027s ENTRYPOINT is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
            return self._set("command", value)

        @overload
        def env(self, value_or_callback: list[EnvVar] | None, /) -> "Container.Builder": ...

        @overload
        def env(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EnvVar, EnvVar.Builder]],
                GenericListBuilder[EnvVar, EnvVar.Builder] | list[EnvVar],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def env(self, value_or_callback: Never = ...) -> ListBuilderContext[EnvVar.Builder]: ...

        def env(self, value_or_callback=UNSET, /):
            "List of environment variables to set in the container. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EnvVar.Builder]()
                context._parent_builder = self
                context._field_name = "env"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EnvVar.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("env", value)

        @overload
        def env_from(
            self, value_or_callback: list[EnvFromSource] | None, /
        ) -> "Container.Builder": ...

        @overload
        def env_from(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EnvFromSource, EnvFromSource.Builder]],
                GenericListBuilder[EnvFromSource, EnvFromSource.Builder] | list[EnvFromSource],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def env_from(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EnvFromSource.Builder]: ...

        def env_from(self, value_or_callback=UNSET, /):
            "List of sources to populate environment variables in the container. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EnvFromSource.Builder]()
                context._parent_builder = self
                context._field_name = "env_from"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EnvFromSource.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("env_from", value)

        def image(self, value: str | None, /) -> Self:
            "Container image name. More info: https://kubernetes.io/docs/concepts/containers/images This field is optional to allow higher level config management to default or override container images in workload controllers like Deployments and StatefulSets."
            return self._set("image", value)

        def image_pull_policy(self, value: str | None, /) -> Self:
            "Image pull policy. One of Always, Never, IfNotPresent. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise. Cannot be updated. More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
            return self._set("image_pull_policy", value)

        @overload
        def lifecycle(self, value_or_callback: Lifecycle | None, /) -> "Container.Builder": ...

        @overload
        def lifecycle(
            self,
            value_or_callback: Callable[[Lifecycle.Builder], Lifecycle.Builder | Lifecycle],
            /,
        ) -> "Container.Builder": ...

        @overload
        def lifecycle(self, value_or_callback: Never = ...) -> "Lifecycle.BuilderContext": ...

        def lifecycle(self, value_or_callback=UNSET, /):
            "Actions that the management system should take in response to container lifecycle events. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = Lifecycle.BuilderContext()
                context._parent_builder = self
                context._field_name = "lifecycle"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Lifecycle.builder())
                if isinstance(output, Lifecycle.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("lifecycle", value)

        @overload
        def liveness_probe(self, value_or_callback: Probe | None, /) -> "Container.Builder": ...

        @overload
        def liveness_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "Container.Builder": ...

        @overload
        def liveness_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def liveness_probe(self, value_or_callback=UNSET, /):
            "Periodic probe of container liveness. Container will be restarted if the probe fails. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "liveness_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("liveness_probe", value)

        def name(self, value: str, /) -> Self:
            "Name of the container specified as a DNS_LABEL. Each container in a pod must have a unique name (DNS_LABEL). Cannot be updated."
            return self._set("name", value)

        @overload
        def ports(
            self, value_or_callback: list[ContainerPort] | None, /
        ) -> "Container.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerPort, ContainerPort.Builder]],
                GenericListBuilder[ContainerPort, ContainerPort.Builder] | list[ContainerPort],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerPort.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            'List of ports to expose from the container. Not specifying a port here DOES NOT prevent that port from being exposed. Any port which is listening on the default "0.0.0.0" address inside a container will be accessible from the network. Modifying this array with strategic merge patch may corrupt the data. For more information See https://github.com/kubernetes/kubernetes/issues/108255. Cannot be updated.'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerPort.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerPort.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

        @overload
        def readiness_probe(self, value_or_callback: Probe | None, /) -> "Container.Builder": ...

        @overload
        def readiness_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "Container.Builder": ...

        @overload
        def readiness_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def readiness_probe(self, value_or_callback=UNSET, /):
            "Periodic probe of container service readiness. Container will be removed from service endpoints if the probe fails. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "readiness_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("readiness_probe", value)

        @overload
        def resize_policy(
            self, value_or_callback: list[ContainerResizePolicy] | None, /
        ) -> "Container.Builder": ...

        @overload
        def resize_policy(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerResizePolicy, ContainerResizePolicy.Builder]],
                GenericListBuilder[ContainerResizePolicy, ContainerResizePolicy.Builder]
                | list[ContainerResizePolicy],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def resize_policy(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerResizePolicy.Builder]: ...

        def resize_policy(self, value_or_callback=UNSET, /):
            "Resources resize policy for the container."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerResizePolicy.Builder]()
                context._parent_builder = self
                context._field_name = "resize_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerResizePolicy.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resize_policy", value)

        @overload
        def resources(
            self, value_or_callback: ResourceRequirements | None, /
        ) -> "Container.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [ResourceRequirements.Builder],
                ResourceRequirements.Builder | ResourceRequirements,
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> "ResourceRequirements.BuilderContext": ...

        def resources(self, value_or_callback=UNSET, /):
            "Compute Resources required by this container. Cannot be updated. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
            if self._in_context and value_or_callback is UNSET:
                context = ResourceRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceRequirements.builder())
                if isinstance(output, ResourceRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

        def restart_policy(self, value: str | None, /) -> Self:
            'RestartPolicy defines the restart behavior of individual containers in a pod. This overrides the pod-level restart policy. When this field is not specified, the restart behavior is defined by the Pod\u0027s restart policy and the container type. Additionally, setting the RestartPolicy as "Always" for the init container will have the following effect: this init container will be continually restarted on exit until all regular containers have terminated. Once all regular containers have completed, all init containers with restartPolicy "Always" will be shut down. This lifecycle differs from normal init containers and is often referred to as a "sidecar" container. Although this init container still starts in the init container sequence, it does not wait for the container to complete before proceeding to the next init container. Instead, the next init container starts immediately after this init container is started, or after any startupProbe has successfully completed.'
            return self._set("restart_policy", value)

        @overload
        def restart_policy_rules(
            self, value_or_callback: list[ContainerRestartRule] | None, /
        ) -> "Container.Builder": ...

        @overload
        def restart_policy_rules(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerRestartRule, ContainerRestartRule.Builder]],
                GenericListBuilder[ContainerRestartRule, ContainerRestartRule.Builder]
                | list[ContainerRestartRule],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def restart_policy_rules(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerRestartRule.Builder]: ...

        def restart_policy_rules(self, value_or_callback=UNSET, /):
            "Represents a list of rules to be checked to determine if the container should be restarted on exit. The rules are evaluated in order. Once a rule matches a container exit condition, the remaining rules are ignored. If no rule matches the container exit condition, the Container-level restart policy determines the whether the container is restarted or not. Constraints on the rules: - At most 20 rules are allowed. - Rules can have the same action. - Identical rules are not forbidden in validations. When rules are specified, container MUST set RestartPolicy explicitly even it if matches the Pod\u0027s RestartPolicy."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerRestartRule.Builder]()
                context._parent_builder = self
                context._field_name = "restart_policy_rules"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerRestartRule.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("restart_policy_rules", value)

        @overload
        def security_context(
            self, value_or_callback: SecurityContext | None, /
        ) -> "Container.Builder": ...

        @overload
        def security_context(
            self,
            value_or_callback: Callable[
                [SecurityContext.Builder], SecurityContext.Builder | SecurityContext
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def security_context(
            self, value_or_callback: Never = ...
        ) -> "SecurityContext.BuilderContext": ...

        def security_context(self, value_or_callback=UNSET, /):
            "SecurityContext defines the security options the container should be run with. If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext. More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
            if self._in_context and value_or_callback is UNSET:
                context = SecurityContext.BuilderContext()
                context._parent_builder = self
                context._field_name = "security_context"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecurityContext.builder())
                if isinstance(output, SecurityContext.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("security_context", value)

        @overload
        def startup_probe(self, value_or_callback: Probe | None, /) -> "Container.Builder": ...

        @overload
        def startup_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "Container.Builder": ...

        @overload
        def startup_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def startup_probe(self, value_or_callback=UNSET, /):
            "StartupProbe indicates that the Pod has successfully initialized. If specified, no other probes are executed until this completes successfully. If this probe fails, the Pod will be restarted, just as if the livenessProbe failed. This can be used to provide different probe parameters at the beginning of a Pod\u0027s lifecycle, when it might take a long time to load data or warm a cache, than during steady-state operation. This cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "startup_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("startup_probe", value)

        def stdin(self, value: bool | None, /) -> Self:
            "Whether this container should allocate a buffer for stdin in the container runtime. If this is not set, reads from stdin in the container will always result in EOF. Default is false."
            return self._set("stdin", value)

        def stdin_once(self, value: bool | None, /) -> Self:
            "Whether the container runtime should close the stdin channel after it has been opened by a single attach. When stdin is true the stdin stream will remain open across multiple attach sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the first client attaches to stdin, and then remains open and accepts data until the client disconnects, at which time stdin is closed and remains closed until the container is restarted. If this flag is false, a container processes that reads from stdin will never receive an EOF. Default is false"
            return self._set("stdin_once", value)

        def termination_message_path(self, value: str | None, /) -> Self:
            "Optional: Path at which the file to which the container\u0027s termination message will be written is mounted into the container\u0027s filesystem. Message written is intended to be brief final status, such as an assertion failure message. Will be truncated by the node if greater than 4096 bytes. The total message length across all containers will be limited to 12kb. Defaults to /dev/termination-log. Cannot be updated."
            return self._set("termination_message_path", value)

        def termination_message_policy(self, value: str | None, /) -> Self:
            "Indicate how the termination message should be populated. File will use the contents of terminationMessagePath to populate the container status message on both success and failure. FallbackToLogsOnError will use the last chunk of container log output if the termination message file is empty and the container exited with an error. The log output is limited to 2048 bytes or 80 lines, whichever is smaller. Defaults to File. Cannot be updated."
            return self._set("termination_message_policy", value)

        def tty(self, value: bool | None, /) -> Self:
            "Whether this container should allocate a TTY for itself, also requires \u0027stdin\u0027 to be true. Default is false."
            return self._set("tty", value)

        @overload
        def volume_devices(
            self, value_or_callback: list[VolumeDevice] | None, /
        ) -> "Container.Builder": ...

        @overload
        def volume_devices(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeDevice, VolumeDevice.Builder]],
                GenericListBuilder[VolumeDevice, VolumeDevice.Builder] | list[VolumeDevice],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def volume_devices(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeDevice.Builder]: ...

        def volume_devices(self, value_or_callback=UNSET, /):
            "volumeDevices is the list of block devices to be used by the container."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeDevice.Builder]()
                context._parent_builder = self
                context._field_name = "volume_devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeDevice.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_devices", value)

        @overload
        def volume_mounts(
            self, value_or_callback: list[VolumeMount] | None, /
        ) -> "Container.Builder": ...

        @overload
        def volume_mounts(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeMount, VolumeMount.Builder]],
                GenericListBuilder[VolumeMount, VolumeMount.Builder] | list[VolumeMount],
            ],
            /,
        ) -> "Container.Builder": ...

        @overload
        def volume_mounts(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeMount.Builder]: ...

        def volume_mounts(self, value_or_callback=UNSET, /):
            "Pod volumes to mount into the container\u0027s filesystem. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeMount.Builder]()
                context._parent_builder = self
                context._field_name = "volume_mounts"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeMount.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_mounts", value)

        def working_dir(self, value: str | None, /) -> Self:
            "Container\u0027s working directory. If not specified, the container runtime\u0027s default will be used, which might be configured in the container image. Cannot be updated."
            return self._set("working_dir", value)

    class BuilderContext(BuilderContextBase["Container.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Container.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Container."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Container", Builder]:
        return GenericListBuilder[Container, Container.Builder]()

    args: list[str] | None = None
    'Arguments to the entrypoint. The container image\u0027s CMD is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
    command: list[str] | None = None
    'Entrypoint array. Not executed within a shell. The container image\u0027s ENTRYPOINT is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
    env: list[EnvVar] | None = None
    "List of environment variables to set in the container. Cannot be updated."
    env_from: Annotated[list[EnvFromSource] | None, Field(alias="envFrom")] = None
    "List of sources to populate environment variables in the container. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Cannot be updated."
    image: str | None = None
    "Container image name. More info: https://kubernetes.io/docs/concepts/containers/images This field is optional to allow higher level config management to default or override container images in workload controllers like Deployments and StatefulSets."
    image_pull_policy: Annotated[str | None, Field(alias="imagePullPolicy")] = None
    "Image pull policy. One of Always, Never, IfNotPresent. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise. Cannot be updated. More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
    lifecycle: Lifecycle | None = None
    "Actions that the management system should take in response to container lifecycle events. Cannot be updated."
    liveness_probe: Annotated[Probe | None, Field(alias="livenessProbe")] = None
    "Periodic probe of container liveness. Container will be restarted if the probe fails. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
    name: str
    "Name of the container specified as a DNS_LABEL. Each container in a pod must have a unique name (DNS_LABEL). Cannot be updated."
    ports: list[ContainerPort] | None = None
    'List of ports to expose from the container. Not specifying a port here DOES NOT prevent that port from being exposed. Any port which is listening on the default "0.0.0.0" address inside a container will be accessible from the network. Modifying this array with strategic merge patch may corrupt the data. For more information See https://github.com/kubernetes/kubernetes/issues/108255. Cannot be updated.'
    readiness_probe: Annotated[Probe | None, Field(alias="readinessProbe")] = None
    "Periodic probe of container service readiness. Container will be removed from service endpoints if the probe fails. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
    resize_policy: Annotated[list[ContainerResizePolicy] | None, Field(alias="resizePolicy")] = None
    "Resources resize policy for the container."
    resources: ResourceRequirements | None = None
    "Compute Resources required by this container. Cannot be updated. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/"
    restart_policy: Annotated[str | None, Field(alias="restartPolicy")] = None
    'RestartPolicy defines the restart behavior of individual containers in a pod. This overrides the pod-level restart policy. When this field is not specified, the restart behavior is defined by the Pod\u0027s restart policy and the container type. Additionally, setting the RestartPolicy as "Always" for the init container will have the following effect: this init container will be continually restarted on exit until all regular containers have terminated. Once all regular containers have completed, all init containers with restartPolicy "Always" will be shut down. This lifecycle differs from normal init containers and is often referred to as a "sidecar" container. Although this init container still starts in the init container sequence, it does not wait for the container to complete before proceeding to the next init container. Instead, the next init container starts immediately after this init container is started, or after any startupProbe has successfully completed.'
    restart_policy_rules: Annotated[
        list[ContainerRestartRule] | None, Field(alias="restartPolicyRules")
    ] = None
    "Represents a list of rules to be checked to determine if the container should be restarted on exit. The rules are evaluated in order. Once a rule matches a container exit condition, the remaining rules are ignored. If no rule matches the container exit condition, the Container-level restart policy determines the whether the container is restarted or not. Constraints on the rules: - At most 20 rules are allowed. - Rules can have the same action. - Identical rules are not forbidden in validations. When rules are specified, container MUST set RestartPolicy explicitly even it if matches the Pod\u0027s RestartPolicy."
    security_context: Annotated[SecurityContext | None, Field(alias="securityContext")] = None
    "SecurityContext defines the security options the container should be run with. If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext. More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/"
    startup_probe: Annotated[Probe | None, Field(alias="startupProbe")] = None
    "StartupProbe indicates that the Pod has successfully initialized. If specified, no other probes are executed until this completes successfully. If this probe fails, the Pod will be restarted, just as if the livenessProbe failed. This can be used to provide different probe parameters at the beginning of a Pod\u0027s lifecycle, when it might take a long time to load data or warm a cache, than during steady-state operation. This cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes"
    stdin: bool | None = None
    "Whether this container should allocate a buffer for stdin in the container runtime. If this is not set, reads from stdin in the container will always result in EOF. Default is false."
    stdin_once: Annotated[bool | None, Field(alias="stdinOnce")] = None
    "Whether the container runtime should close the stdin channel after it has been opened by a single attach. When stdin is true the stdin stream will remain open across multiple attach sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the first client attaches to stdin, and then remains open and accepts data until the client disconnects, at which time stdin is closed and remains closed until the container is restarted. If this flag is false, a container processes that reads from stdin will never receive an EOF. Default is false"
    termination_message_path: Annotated[str | None, Field(alias="terminationMessagePath")] = None
    "Optional: Path at which the file to which the container\u0027s termination message will be written is mounted into the container\u0027s filesystem. Message written is intended to be brief final status, such as an assertion failure message. Will be truncated by the node if greater than 4096 bytes. The total message length across all containers will be limited to 12kb. Defaults to /dev/termination-log. Cannot be updated."
    termination_message_policy: Annotated[str | None, Field(alias="terminationMessagePolicy")] = (
        None
    )
    "Indicate how the termination message should be populated. File will use the contents of terminationMessagePath to populate the container status message on both success and failure. FallbackToLogsOnError will use the last chunk of container log output if the termination message file is empty and the container exited with an error. The log output is limited to 2048 bytes or 80 lines, whichever is smaller. Defaults to File. Cannot be updated."
    tty: bool | None = None
    "Whether this container should allocate a TTY for itself, also requires \u0027stdin\u0027 to be true. Default is false."
    volume_devices: Annotated[list[VolumeDevice] | None, Field(alias="volumeDevices")] = None
    "volumeDevices is the list of block devices to be used by the container."
    volume_mounts: Annotated[list[VolumeMount] | None, Field(alias="volumeMounts")] = None
    "Pod volumes to mount into the container\u0027s filesystem. Cannot be updated."
    working_dir: Annotated[str | None, Field(alias="workingDir")] = None
    "Container\u0027s working directory. If not specified, the container runtime\u0027s default will be used, which might be configured in the container image. Cannot be updated."


class EphemeralContainer(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EphemeralContainer"]:
            return EphemeralContainer

        def build(self) -> "EphemeralContainer":
            return EphemeralContainer(**self._attrs)

        def args(self, value: list[str] | None, /) -> Self:
            'Arguments to the entrypoint. The image\u0027s CMD is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
            return self._set("args", value)

        def command(self, value: list[str] | None, /) -> Self:
            'Entrypoint array. Not executed within a shell. The image\u0027s ENTRYPOINT is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
            return self._set("command", value)

        @overload
        def env(
            self, value_or_callback: list[EnvVar] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def env(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EnvVar, EnvVar.Builder]],
                GenericListBuilder[EnvVar, EnvVar.Builder] | list[EnvVar],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def env(self, value_or_callback: Never = ...) -> ListBuilderContext[EnvVar.Builder]: ...

        def env(self, value_or_callback=UNSET, /):
            "List of environment variables to set in the container. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EnvVar.Builder]()
                context._parent_builder = self
                context._field_name = "env"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EnvVar.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("env", value)

        @overload
        def env_from(
            self, value_or_callback: list[EnvFromSource] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def env_from(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EnvFromSource, EnvFromSource.Builder]],
                GenericListBuilder[EnvFromSource, EnvFromSource.Builder] | list[EnvFromSource],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def env_from(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EnvFromSource.Builder]: ...

        def env_from(self, value_or_callback=UNSET, /):
            "List of sources to populate environment variables in the container. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EnvFromSource.Builder]()
                context._parent_builder = self
                context._field_name = "env_from"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EnvFromSource.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("env_from", value)

        def image(self, value: str | None, /) -> Self:
            "Container image name. More info: https://kubernetes.io/docs/concepts/containers/images"
            return self._set("image", value)

        def image_pull_policy(self, value: str | None, /) -> Self:
            "Image pull policy. One of Always, Never, IfNotPresent. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise. Cannot be updated. More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
            return self._set("image_pull_policy", value)

        @overload
        def lifecycle(
            self, value_or_callback: Lifecycle | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def lifecycle(
            self,
            value_or_callback: Callable[[Lifecycle.Builder], Lifecycle.Builder | Lifecycle],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def lifecycle(self, value_or_callback: Never = ...) -> "Lifecycle.BuilderContext": ...

        def lifecycle(self, value_or_callback=UNSET, /):
            "Lifecycle is not allowed for ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = Lifecycle.BuilderContext()
                context._parent_builder = self
                context._field_name = "lifecycle"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Lifecycle.builder())
                if isinstance(output, Lifecycle.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("lifecycle", value)

        @overload
        def liveness_probe(
            self, value_or_callback: Probe | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def liveness_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def liveness_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def liveness_probe(self, value_or_callback=UNSET, /):
            "Probes are not allowed for ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "liveness_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("liveness_probe", value)

        def name(self, value: str, /) -> Self:
            "Name of the ephemeral container specified as a DNS_LABEL. This name must be unique among all containers, init containers and ephemeral containers."
            return self._set("name", value)

        @overload
        def ports(
            self, value_or_callback: list[ContainerPort] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def ports(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerPort, ContainerPort.Builder]],
                GenericListBuilder[ContainerPort, ContainerPort.Builder] | list[ContainerPort],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def ports(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerPort.Builder]: ...

        def ports(self, value_or_callback=UNSET, /):
            "Ports are not allowed for ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerPort.Builder]()
                context._parent_builder = self
                context._field_name = "ports"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerPort.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ports", value)

        @overload
        def readiness_probe(
            self, value_or_callback: Probe | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def readiness_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def readiness_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def readiness_probe(self, value_or_callback=UNSET, /):
            "Probes are not allowed for ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "readiness_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("readiness_probe", value)

        @overload
        def resize_policy(
            self, value_or_callback: list[ContainerResizePolicy] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def resize_policy(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerResizePolicy, ContainerResizePolicy.Builder]],
                GenericListBuilder[ContainerResizePolicy, ContainerResizePolicy.Builder]
                | list[ContainerResizePolicy],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def resize_policy(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerResizePolicy.Builder]: ...

        def resize_policy(self, value_or_callback=UNSET, /):
            "Resources resize policy for the container."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerResizePolicy.Builder]()
                context._parent_builder = self
                context._field_name = "resize_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerResizePolicy.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resize_policy", value)

        @overload
        def resources(
            self, value_or_callback: ResourceRequirements | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [ResourceRequirements.Builder],
                ResourceRequirements.Builder | ResourceRequirements,
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> "ResourceRequirements.BuilderContext": ...

        def resources(self, value_or_callback=UNSET, /):
            "Resources are not allowed for ephemeral containers. Ephemeral containers use spare resources already allocated to the pod."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceRequirements.builder())
                if isinstance(output, ResourceRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

        def restart_policy(self, value: str | None, /) -> Self:
            "Restart policy for the container to manage the restart behavior of each container within a pod. You cannot set this field on ephemeral containers."
            return self._set("restart_policy", value)

        @overload
        def restart_policy_rules(
            self, value_or_callback: list[ContainerRestartRule] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def restart_policy_rules(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ContainerRestartRule, ContainerRestartRule.Builder]],
                GenericListBuilder[ContainerRestartRule, ContainerRestartRule.Builder]
                | list[ContainerRestartRule],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def restart_policy_rules(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ContainerRestartRule.Builder]: ...

        def restart_policy_rules(self, value_or_callback=UNSET, /):
            "Represents a list of rules to be checked to determine if the container should be restarted on exit. You cannot set this field on ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ContainerRestartRule.Builder]()
                context._parent_builder = self
                context._field_name = "restart_policy_rules"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ContainerRestartRule.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("restart_policy_rules", value)

        @overload
        def security_context(
            self, value_or_callback: SecurityContext | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def security_context(
            self,
            value_or_callback: Callable[
                [SecurityContext.Builder], SecurityContext.Builder | SecurityContext
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def security_context(
            self, value_or_callback: Never = ...
        ) -> "SecurityContext.BuilderContext": ...

        def security_context(self, value_or_callback=UNSET, /):
            "Optional: SecurityContext defines the security options the ephemeral container should be run with. If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext."
            if self._in_context and value_or_callback is UNSET:
                context = SecurityContext.BuilderContext()
                context._parent_builder = self
                context._field_name = "security_context"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecurityContext.builder())
                if isinstance(output, SecurityContext.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("security_context", value)

        @overload
        def startup_probe(
            self, value_or_callback: Probe | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def startup_probe(
            self, value_or_callback: Callable[[Probe.Builder], Probe.Builder | Probe], /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def startup_probe(self, value_or_callback: Never = ...) -> "Probe.BuilderContext": ...

        def startup_probe(self, value_or_callback=UNSET, /):
            "Probes are not allowed for ephemeral containers."
            if self._in_context and value_or_callback is UNSET:
                context = Probe.BuilderContext()
                context._parent_builder = self
                context._field_name = "startup_probe"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Probe.builder())
                if isinstance(output, Probe.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("startup_probe", value)

        def stdin(self, value: bool | None, /) -> Self:
            "Whether this container should allocate a buffer for stdin in the container runtime. If this is not set, reads from stdin in the container will always result in EOF. Default is false."
            return self._set("stdin", value)

        def stdin_once(self, value: bool | None, /) -> Self:
            "Whether the container runtime should close the stdin channel after it has been opened by a single attach. When stdin is true the stdin stream will remain open across multiple attach sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the first client attaches to stdin, and then remains open and accepts data until the client disconnects, at which time stdin is closed and remains closed until the container is restarted. If this flag is false, a container processes that reads from stdin will never receive an EOF. Default is false"
            return self._set("stdin_once", value)

        def target_container_name(self, value: str | None, /) -> Self:
            "If set, the name of the container from PodSpec that this ephemeral container targets. The ephemeral container will be run in the namespaces (IPC, PID, etc) of this container. If not set then the ephemeral container uses the namespaces configured in the Pod spec.\n\nThe container runtime must implement support for this feature. If the runtime does not support namespace targeting then the result of setting this field is undefined."
            return self._set("target_container_name", value)

        def termination_message_path(self, value: str | None, /) -> Self:
            "Optional: Path at which the file to which the container\u0027s termination message will be written is mounted into the container\u0027s filesystem. Message written is intended to be brief final status, such as an assertion failure message. Will be truncated by the node if greater than 4096 bytes. The total message length across all containers will be limited to 12kb. Defaults to /dev/termination-log. Cannot be updated."
            return self._set("termination_message_path", value)

        def termination_message_policy(self, value: str | None, /) -> Self:
            "Indicate how the termination message should be populated. File will use the contents of terminationMessagePath to populate the container status message on both success and failure. FallbackToLogsOnError will use the last chunk of container log output if the termination message file is empty and the container exited with an error. The log output is limited to 2048 bytes or 80 lines, whichever is smaller. Defaults to File. Cannot be updated."
            return self._set("termination_message_policy", value)

        def tty(self, value: bool | None, /) -> Self:
            "Whether this container should allocate a TTY for itself, also requires \u0027stdin\u0027 to be true. Default is false."
            return self._set("tty", value)

        @overload
        def volume_devices(
            self, value_or_callback: list[VolumeDevice] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def volume_devices(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeDevice, VolumeDevice.Builder]],
                GenericListBuilder[VolumeDevice, VolumeDevice.Builder] | list[VolumeDevice],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def volume_devices(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeDevice.Builder]: ...

        def volume_devices(self, value_or_callback=UNSET, /):
            "volumeDevices is the list of block devices to be used by the container."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeDevice.Builder]()
                context._parent_builder = self
                context._field_name = "volume_devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeDevice.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_devices", value)

        @overload
        def volume_mounts(
            self, value_or_callback: list[VolumeMount] | None, /
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def volume_mounts(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeMount, VolumeMount.Builder]],
                GenericListBuilder[VolumeMount, VolumeMount.Builder] | list[VolumeMount],
            ],
            /,
        ) -> "EphemeralContainer.Builder": ...

        @overload
        def volume_mounts(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeMount.Builder]: ...

        def volume_mounts(self, value_or_callback=UNSET, /):
            "Pod volumes to mount into the container\u0027s filesystem. Subpath mounts are not allowed for ephemeral containers. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeMount.Builder]()
                context._parent_builder = self
                context._field_name = "volume_mounts"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeMount.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volume_mounts", value)

        def working_dir(self, value: str | None, /) -> Self:
            "Container\u0027s working directory. If not specified, the container runtime\u0027s default will be used, which might be configured in the container image. Cannot be updated."
            return self._set("working_dir", value)

    class BuilderContext(BuilderContextBase["EphemeralContainer.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EphemeralContainer.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EphemeralContainer."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EphemeralContainer", Builder]:
        return GenericListBuilder[EphemeralContainer, EphemeralContainer.Builder]()

    args: list[str] | None = None
    'Arguments to the entrypoint. The image\u0027s CMD is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
    command: list[str] | None = None
    'Entrypoint array. Not executed within a shell. The image\u0027s ENTRYPOINT is used if this is not provided. Variable references $(VAR_NAME) are expanded using the container\u0027s environment. If a variable cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless of whether the variable exists or not. Cannot be updated. More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell'
    env: list[EnvVar] | None = None
    "List of environment variables to set in the container. Cannot be updated."
    env_from: Annotated[list[EnvFromSource] | None, Field(alias="envFrom")] = None
    "List of sources to populate environment variables in the container. The keys defined within a source may consist of any printable ASCII characters except \u0027=\u0027. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Cannot be updated."
    image: str | None = None
    "Container image name. More info: https://kubernetes.io/docs/concepts/containers/images"
    image_pull_policy: Annotated[str | None, Field(alias="imagePullPolicy")] = None
    "Image pull policy. One of Always, Never, IfNotPresent. Defaults to Always if :latest tag is specified, or IfNotPresent otherwise. Cannot be updated. More info: https://kubernetes.io/docs/concepts/containers/images#updating-images"
    lifecycle: Lifecycle | None = None
    "Lifecycle is not allowed for ephemeral containers."
    liveness_probe: Annotated[Probe | None, Field(alias="livenessProbe")] = None
    "Probes are not allowed for ephemeral containers."
    name: str
    "Name of the ephemeral container specified as a DNS_LABEL. This name must be unique among all containers, init containers and ephemeral containers."
    ports: list[ContainerPort] | None = None
    "Ports are not allowed for ephemeral containers."
    readiness_probe: Annotated[Probe | None, Field(alias="readinessProbe")] = None
    "Probes are not allowed for ephemeral containers."
    resize_policy: Annotated[list[ContainerResizePolicy] | None, Field(alias="resizePolicy")] = None
    "Resources resize policy for the container."
    resources: ResourceRequirements | None = None
    "Resources are not allowed for ephemeral containers. Ephemeral containers use spare resources already allocated to the pod."
    restart_policy: Annotated[str | None, Field(alias="restartPolicy")] = None
    "Restart policy for the container to manage the restart behavior of each container within a pod. You cannot set this field on ephemeral containers."
    restart_policy_rules: Annotated[
        list[ContainerRestartRule] | None, Field(alias="restartPolicyRules")
    ] = None
    "Represents a list of rules to be checked to determine if the container should be restarted on exit. You cannot set this field on ephemeral containers."
    security_context: Annotated[SecurityContext | None, Field(alias="securityContext")] = None
    "Optional: SecurityContext defines the security options the ephemeral container should be run with. If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext."
    startup_probe: Annotated[Probe | None, Field(alias="startupProbe")] = None
    "Probes are not allowed for ephemeral containers."
    stdin: bool | None = None
    "Whether this container should allocate a buffer for stdin in the container runtime. If this is not set, reads from stdin in the container will always result in EOF. Default is false."
    stdin_once: Annotated[bool | None, Field(alias="stdinOnce")] = None
    "Whether the container runtime should close the stdin channel after it has been opened by a single attach. When stdin is true the stdin stream will remain open across multiple attach sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the first client attaches to stdin, and then remains open and accepts data until the client disconnects, at which time stdin is closed and remains closed until the container is restarted. If this flag is false, a container processes that reads from stdin will never receive an EOF. Default is false"
    target_container_name: Annotated[str | None, Field(alias="targetContainerName")] = None
    "If set, the name of the container from PodSpec that this ephemeral container targets. The ephemeral container will be run in the namespaces (IPC, PID, etc) of this container. If not set then the ephemeral container uses the namespaces configured in the Pod spec.\n\nThe container runtime must implement support for this feature. If the runtime does not support namespace targeting then the result of setting this field is undefined."
    termination_message_path: Annotated[str | None, Field(alias="terminationMessagePath")] = None
    "Optional: Path at which the file to which the container\u0027s termination message will be written is mounted into the container\u0027s filesystem. Message written is intended to be brief final status, such as an assertion failure message. Will be truncated by the node if greater than 4096 bytes. The total message length across all containers will be limited to 12kb. Defaults to /dev/termination-log. Cannot be updated."
    termination_message_policy: Annotated[str | None, Field(alias="terminationMessagePolicy")] = (
        None
    )
    "Indicate how the termination message should be populated. File will use the contents of terminationMessagePath to populate the container status message on both success and failure. FallbackToLogsOnError will use the last chunk of container log output if the termination message file is empty and the container exited with an error. The log output is limited to 2048 bytes or 80 lines, whichever is smaller. Defaults to File. Cannot be updated."
    tty: bool | None = None
    "Whether this container should allocate a TTY for itself, also requires \u0027stdin\u0027 to be true. Default is false."
    volume_devices: Annotated[list[VolumeDevice] | None, Field(alias="volumeDevices")] = None
    "volumeDevices is the list of block devices to be used by the container."
    volume_mounts: Annotated[list[VolumeMount] | None, Field(alias="volumeMounts")] = None
    "Pod volumes to mount into the container\u0027s filesystem. Subpath mounts are not allowed for ephemeral containers. Cannot be updated."
    working_dir: Annotated[str | None, Field(alias="workingDir")] = None
    "Container\u0027s working directory. If not specified, the container runtime\u0027s default will be used, which might be configured in the container image. Cannot be updated."


class PersistentVolume(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "persistentvolumes",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PersistentVolume"]:
            return PersistentVolume

        def build(self) -> "PersistentVolume":
            return PersistentVolume(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["PersistentVolume"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "PersistentVolume.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PersistentVolume.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: PersistentVolumeSpec | None, /
        ) -> "PersistentVolume.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [PersistentVolumeSpec.Builder],
                PersistentVolumeSpec.Builder | PersistentVolumeSpec,
            ],
            /,
        ) -> "PersistentVolume.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "PersistentVolumeSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines a specification of a persistent volume owned by the cluster. Provisioned by an administrator. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistent-volumes"
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeSpec.builder())
                if isinstance(output, PersistentVolumeSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: PersistentVolumeStatus | None, /
        ) -> "PersistentVolume.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [PersistentVolumeStatus.Builder],
                PersistentVolumeStatus.Builder | PersistentVolumeStatus,
            ],
            /,
        ) -> "PersistentVolume.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the current information/status for the persistent volume. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistent-volumes"
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeStatus.builder())
                if isinstance(output, PersistentVolumeStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["PersistentVolume.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PersistentVolume.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PersistentVolume."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PersistentVolume", Builder]:
        return GenericListBuilder[PersistentVolume, PersistentVolume.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["PersistentVolume"] = "PersistentVolume"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: PersistentVolumeSpec | None = None
    "spec defines a specification of a persistent volume owned by the cluster. Provisioned by an administrator. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistent-volumes"
    status: PersistentVolumeStatus | None = None
    "status represents the current information/status for the persistent volume. Populated by the system. Read-only. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistent-volumes"


PersistentVolumeList = ResourceList[PersistentVolume]


class ProjectedVolumeSource(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ProjectedVolumeSource"]:
            return ProjectedVolumeSource

        def build(self) -> "ProjectedVolumeSource":
            return ProjectedVolumeSource(**self._attrs)

        def default_mode(self, value: int | None, /) -> Self:
            "defaultMode are the mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
            return self._set("default_mode", value)

        @overload
        def sources(
            self, value_or_callback: list[VolumeProjection] | None, /
        ) -> "ProjectedVolumeSource.Builder": ...

        @overload
        def sources(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[VolumeProjection, VolumeProjection.Builder]],
                GenericListBuilder[VolumeProjection, VolumeProjection.Builder]
                | list[VolumeProjection],
            ],
            /,
        ) -> "ProjectedVolumeSource.Builder": ...

        @overload
        def sources(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[VolumeProjection.Builder]: ...

        def sources(self, value_or_callback=UNSET, /):
            "sources is the list of volume projections. Each entry in this list handles one source."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[VolumeProjection.Builder]()
                context._parent_builder = self
                context._field_name = "sources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VolumeProjection.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("sources", value)

    class BuilderContext(BuilderContextBase["ProjectedVolumeSource.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ProjectedVolumeSource.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ProjectedVolumeSource."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ProjectedVolumeSource", Builder]:
        return GenericListBuilder[ProjectedVolumeSource, ProjectedVolumeSource.Builder]()

    default_mode: Annotated[int | None, Field(alias="defaultMode")] = None
    "defaultMode are the mode bits used to set permissions on created files by default. Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511. YAML accepts both octal and decimal values, JSON requires decimal values for mode bits. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set."
    sources: list[VolumeProjection] | None = None
    "sources is the list of volume projections. Each entry in this list handles one source."


class ResourceQuota(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "resourcequotas",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceQuota"]:
            return ResourceQuota

        def build(self) -> "ResourceQuota":
            return ResourceQuota(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ResourceQuota"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "ResourceQuota.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ResourceQuota.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ResourceQuotaSpec | None, /
        ) -> "ResourceQuota.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ResourceQuotaSpec.Builder],
                ResourceQuotaSpec.Builder | ResourceQuotaSpec,
            ],
            /,
        ) -> "ResourceQuota.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "ResourceQuotaSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the desired quota. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ResourceQuotaSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceQuotaSpec.builder())
                if isinstance(output, ResourceQuotaSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: ResourceQuotaStatus | None, /
        ) -> "ResourceQuota.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [ResourceQuotaStatus.Builder],
                ResourceQuotaStatus.Builder | ResourceQuotaStatus,
            ],
            /,
        ) -> "ResourceQuota.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "ResourceQuotaStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Status defines the actual enforced quota and its current usage. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ResourceQuotaStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceQuotaStatus.builder())
                if isinstance(output, ResourceQuotaStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["ResourceQuota.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceQuota.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceQuota."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceQuota", Builder]:
        return GenericListBuilder[ResourceQuota, ResourceQuota.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ResourceQuota"] = "ResourceQuota"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: ResourceQuotaSpec | None = None
    "Spec defines the desired quota. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: ResourceQuotaStatus | None = None
    "Status defines the actual enforced quota and its current usage. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


ResourceQuotaList = ResourceList[ResourceQuota]


class Volume(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Volume"]:
            return Volume

        def build(self) -> "Volume":
            return Volume(**self._attrs)

        @overload
        def aws_elastic_block_store(
            self, value_or_callback: AWSElasticBlockStoreVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def aws_elastic_block_store(
            self,
            value_or_callback: Callable[
                [AWSElasticBlockStoreVolumeSource.Builder],
                AWSElasticBlockStoreVolumeSource.Builder | AWSElasticBlockStoreVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def aws_elastic_block_store(
            self, value_or_callback: Never = ...
        ) -> "AWSElasticBlockStoreVolumeSource.BuilderContext": ...

        def aws_elastic_block_store(self, value_or_callback=UNSET, /):
            "awsElasticBlockStore represents an AWS Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
            if self._in_context and value_or_callback is UNSET:
                context = AWSElasticBlockStoreVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "aws_elastic_block_store"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AWSElasticBlockStoreVolumeSource.builder())
                if isinstance(output, AWSElasticBlockStoreVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("aws_elastic_block_store", value)

        @overload
        def azure_disk(
            self, value_or_callback: AzureDiskVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def azure_disk(
            self,
            value_or_callback: Callable[
                [AzureDiskVolumeSource.Builder],
                AzureDiskVolumeSource.Builder | AzureDiskVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def azure_disk(
            self, value_or_callback: Never = ...
        ) -> "AzureDiskVolumeSource.BuilderContext": ...

        def azure_disk(self, value_or_callback=UNSET, /):
            "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod. Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type are redirected to the disk.csi.azure.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = AzureDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "azure_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AzureDiskVolumeSource.builder())
                if isinstance(output, AzureDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("azure_disk", value)

        @overload
        def azure_file(
            self, value_or_callback: AzureFileVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def azure_file(
            self,
            value_or_callback: Callable[
                [AzureFileVolumeSource.Builder],
                AzureFileVolumeSource.Builder | AzureFileVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def azure_file(
            self, value_or_callback: Never = ...
        ) -> "AzureFileVolumeSource.BuilderContext": ...

        def azure_file(self, value_or_callback=UNSET, /):
            "azureFile represents an Azure File Service mount on the host and bind mount to the pod. Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type are redirected to the file.csi.azure.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = AzureFileVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "azure_file"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AzureFileVolumeSource.builder())
                if isinstance(output, AzureFileVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("azure_file", value)

        @overload
        def cephfs(self, value_or_callback: CephFSVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def cephfs(
            self,
            value_or_callback: Callable[
                [CephFSVolumeSource.Builder],
                CephFSVolumeSource.Builder | CephFSVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def cephfs(self, value_or_callback: Never = ...) -> "CephFSVolumeSource.BuilderContext": ...

        def cephfs(self, value_or_callback=UNSET, /):
            "cephFS represents a Ceph FS mount on the host that shares a pod\u0027s lifetime. Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = CephFSVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "cephfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CephFSVolumeSource.builder())
                if isinstance(output, CephFSVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cephfs", value)

        @overload
        def cinder(self, value_or_callback: CinderVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def cinder(
            self,
            value_or_callback: Callable[
                [CinderVolumeSource.Builder],
                CinderVolumeSource.Builder | CinderVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def cinder(self, value_or_callback: Never = ...) -> "CinderVolumeSource.BuilderContext": ...

        def cinder(self, value_or_callback=UNSET, /):
            "cinder represents a cinder volume attached and mounted on kubelets host machine. Deprecated: Cinder is deprecated. All operations for the in-tree cinder type are redirected to the cinder.csi.openstack.org CSI driver. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
            if self._in_context and value_or_callback is UNSET:
                context = CinderVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "cinder"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CinderVolumeSource.builder())
                if isinstance(output, CinderVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cinder", value)

        @overload
        def config_map(
            self, value_or_callback: ConfigMapVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def config_map(
            self,
            value_or_callback: Callable[
                [ConfigMapVolumeSource.Builder],
                ConfigMapVolumeSource.Builder | ConfigMapVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def config_map(
            self, value_or_callback: Never = ...
        ) -> "ConfigMapVolumeSource.BuilderContext": ...

        def config_map(self, value_or_callback=UNSET, /):
            "configMap represents a configMap that should populate this volume"
            if self._in_context and value_or_callback is UNSET:
                context = ConfigMapVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "config_map"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ConfigMapVolumeSource.builder())
                if isinstance(output, ConfigMapVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("config_map", value)

        @overload
        def csi(self, value_or_callback: CSIVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def csi(
            self,
            value_or_callback: Callable[
                [CSIVolumeSource.Builder], CSIVolumeSource.Builder | CSIVolumeSource
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def csi(self, value_or_callback: Never = ...) -> "CSIVolumeSource.BuilderContext": ...

        def csi(self, value_or_callback=UNSET, /):
            "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers."
            if self._in_context and value_or_callback is UNSET:
                context = CSIVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "csi"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CSIVolumeSource.builder())
                if isinstance(output, CSIVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("csi", value)

        @overload
        def downward_api(
            self, value_or_callback: DownwardAPIVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def downward_api(
            self,
            value_or_callback: Callable[
                [DownwardAPIVolumeSource.Builder],
                DownwardAPIVolumeSource.Builder | DownwardAPIVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def downward_api(
            self, value_or_callback: Never = ...
        ) -> "DownwardAPIVolumeSource.BuilderContext": ...

        def downward_api(self, value_or_callback=UNSET, /):
            "downwardAPI represents downward API about the pod that should populate this volume"
            if self._in_context and value_or_callback is UNSET:
                context = DownwardAPIVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "downward_api"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DownwardAPIVolumeSource.builder())
                if isinstance(output, DownwardAPIVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("downward_api", value)

        @overload
        def empty_dir(
            self, value_or_callback: EmptyDirVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def empty_dir(
            self,
            value_or_callback: Callable[
                [EmptyDirVolumeSource.Builder],
                EmptyDirVolumeSource.Builder | EmptyDirVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def empty_dir(
            self, value_or_callback: Never = ...
        ) -> "EmptyDirVolumeSource.BuilderContext": ...

        def empty_dir(self, value_or_callback=UNSET, /):
            "emptyDir represents a temporary directory that shares a pod\u0027s lifetime. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir"
            if self._in_context and value_or_callback is UNSET:
                context = EmptyDirVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "empty_dir"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EmptyDirVolumeSource.builder())
                if isinstance(output, EmptyDirVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("empty_dir", value)

        @overload
        def ephemeral(
            self, value_or_callback: EphemeralVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def ephemeral(
            self,
            value_or_callback: Callable[
                [EphemeralVolumeSource.Builder],
                EphemeralVolumeSource.Builder | EphemeralVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def ephemeral(
            self, value_or_callback: Never = ...
        ) -> "EphemeralVolumeSource.BuilderContext": ...

        def ephemeral(self, value_or_callback=UNSET, /):
            "ephemeral represents a volume that is handled by a cluster storage driver. The volume\u0027s lifecycle is tied to the pod that defines it - it will be created before the pod starts, and deleted when the pod is removed.\n\nUse this if: a) the volume is only needed while the pod runs, b) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and d) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific APIs for volumes that persist for longer than the lifecycle of an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to be used that way - see the documentation of the driver for more information.\n\nA pod can use both types of ephemeral volumes and persistent volumes at the same time."
            if self._in_context and value_or_callback is UNSET:
                context = EphemeralVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "ephemeral"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EphemeralVolumeSource.builder())
                if isinstance(output, EphemeralVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("ephemeral", value)

        @overload
        def fc(self, value_or_callback: FCVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def fc(
            self,
            value_or_callback: Callable[
                [FCVolumeSource.Builder], FCVolumeSource.Builder | FCVolumeSource
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def fc(self, value_or_callback: Never = ...) -> "FCVolumeSource.BuilderContext": ...

        def fc(self, value_or_callback=UNSET, /):
            "fc represents a Fibre Channel resource that is attached to a kubelet\u0027s host machine and then exposed to the pod."
            if self._in_context and value_or_callback is UNSET:
                context = FCVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "fc"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FCVolumeSource.builder())
                if isinstance(output, FCVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("fc", value)

        @overload
        def flex_volume(
            self, value_or_callback: FlexVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def flex_volume(
            self,
            value_or_callback: Callable[
                [FlexVolumeSource.Builder], FlexVolumeSource.Builder | FlexVolumeSource
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def flex_volume(
            self, value_or_callback: Never = ...
        ) -> "FlexVolumeSource.BuilderContext": ...

        def flex_volume(self, value_or_callback=UNSET, /):
            "flexVolume represents a generic volume resource that is provisioned/attached using an exec based plugin. Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead."
            if self._in_context and value_or_callback is UNSET:
                context = FlexVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "flex_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FlexVolumeSource.builder())
                if isinstance(output, FlexVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("flex_volume", value)

        @overload
        def flocker(self, value_or_callback: FlockerVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def flocker(
            self,
            value_or_callback: Callable[
                [FlockerVolumeSource.Builder],
                FlockerVolumeSource.Builder | FlockerVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def flocker(
            self, value_or_callback: Never = ...
        ) -> "FlockerVolumeSource.BuilderContext": ...

        def flocker(self, value_or_callback=UNSET, /):
            "flocker represents a Flocker volume attached to a kubelet\u0027s host machine. This depends on the Flocker control service being running. Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = FlockerVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "flocker"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(FlockerVolumeSource.builder())
                if isinstance(output, FlockerVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("flocker", value)

        @overload
        def gce_persistent_disk(
            self, value_or_callback: GCEPersistentDiskVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def gce_persistent_disk(
            self,
            value_or_callback: Callable[
                [GCEPersistentDiskVolumeSource.Builder],
                GCEPersistentDiskVolumeSource.Builder | GCEPersistentDiskVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def gce_persistent_disk(
            self, value_or_callback: Never = ...
        ) -> "GCEPersistentDiskVolumeSource.BuilderContext": ...

        def gce_persistent_disk(self, value_or_callback=UNSET, /):
            "gcePersistentDisk represents a GCE Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
            if self._in_context and value_or_callback is UNSET:
                context = GCEPersistentDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "gce_persistent_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GCEPersistentDiskVolumeSource.builder())
                if isinstance(output, GCEPersistentDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("gce_persistent_disk", value)

        @overload
        def git_repo(
            self, value_or_callback: GitRepoVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def git_repo(
            self,
            value_or_callback: Callable[
                [GitRepoVolumeSource.Builder],
                GitRepoVolumeSource.Builder | GitRepoVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def git_repo(
            self, value_or_callback: Never = ...
        ) -> "GitRepoVolumeSource.BuilderContext": ...

        def git_repo(self, value_or_callback=UNSET, /):
            "gitRepo represents a git repository at a particular revision. Deprecated: GitRepo is deprecated. To provision a container with a git repo, mount an EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir into the Pod\u0027s container."
            if self._in_context and value_or_callback is UNSET:
                context = GitRepoVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "git_repo"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GitRepoVolumeSource.builder())
                if isinstance(output, GitRepoVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("git_repo", value)

        @overload
        def glusterfs(
            self, value_or_callback: GlusterfsVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def glusterfs(
            self,
            value_or_callback: Callable[
                [GlusterfsVolumeSource.Builder],
                GlusterfsVolumeSource.Builder | GlusterfsVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def glusterfs(
            self, value_or_callback: Never = ...
        ) -> "GlusterfsVolumeSource.BuilderContext": ...

        def glusterfs(self, value_or_callback=UNSET, /):
            "glusterfs represents a Glusterfs mount on the host that shares a pod\u0027s lifetime. Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = GlusterfsVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "glusterfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(GlusterfsVolumeSource.builder())
                if isinstance(output, GlusterfsVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("glusterfs", value)

        @overload
        def host_path(
            self, value_or_callback: HostPathVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def host_path(
            self,
            value_or_callback: Callable[
                [HostPathVolumeSource.Builder],
                HostPathVolumeSource.Builder | HostPathVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def host_path(
            self, value_or_callback: Never = ...
        ) -> "HostPathVolumeSource.BuilderContext": ...

        def host_path(self, value_or_callback=UNSET, /):
            "hostPath represents a pre-existing file or directory on the host machine that is directly exposed to the container. This is generally used for system agents or other privileged things that are allowed to see the host machine. Most containers will NOT need this. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
            if self._in_context and value_or_callback is UNSET:
                context = HostPathVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "host_path"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HostPathVolumeSource.builder())
                if isinstance(output, HostPathVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("host_path", value)

        @overload
        def image(self, value_or_callback: ImageVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def image(
            self,
            value_or_callback: Callable[
                [ImageVolumeSource.Builder],
                ImageVolumeSource.Builder | ImageVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def image(self, value_or_callback: Never = ...) -> "ImageVolumeSource.BuilderContext": ...

        def image(self, value_or_callback=UNSET, /):
            "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet\u0027s host machine. The volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails. - Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn\u0027t present. - IfNotPresent: the kubelet pulls if the reference isn\u0027t already present on disk. Container creation will fail if the reference isn\u0027t present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation. A failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message. The types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field. The OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images. The volume will be mounted read-only (ro) and non-executable files (noexec). Sub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33. The field spec.securityContext.fsGroupChangePolicy has no effect on this volume type."
            if self._in_context and value_or_callback is UNSET:
                context = ImageVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "image"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ImageVolumeSource.builder())
                if isinstance(output, ImageVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("image", value)

        @overload
        def iscsi(self, value_or_callback: ISCSIVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def iscsi(
            self,
            value_or_callback: Callable[
                [ISCSIVolumeSource.Builder],
                ISCSIVolumeSource.Builder | ISCSIVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def iscsi(self, value_or_callback: Never = ...) -> "ISCSIVolumeSource.BuilderContext": ...

        def iscsi(self, value_or_callback=UNSET, /):
            "iscsi represents an ISCSI Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi"
            if self._in_context and value_or_callback is UNSET:
                context = ISCSIVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "iscsi"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ISCSIVolumeSource.builder())
                if isinstance(output, ISCSIVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("iscsi", value)

        def name(self, value: str, /) -> Self:
            "name of the volume. Must be a DNS_LABEL and unique within the pod. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        @overload
        def nfs(self, value_or_callback: NFSVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def nfs(
            self,
            value_or_callback: Callable[
                [NFSVolumeSource.Builder], NFSVolumeSource.Builder | NFSVolumeSource
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def nfs(self, value_or_callback: Never = ...) -> "NFSVolumeSource.BuilderContext": ...

        def nfs(self, value_or_callback=UNSET, /):
            "nfs represents an NFS mount on the host that shares a pod\u0027s lifetime More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
            if self._in_context and value_or_callback is UNSET:
                context = NFSVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "nfs"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NFSVolumeSource.builder())
                if isinstance(output, NFSVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("nfs", value)

        @overload
        def persistent_volume_claim(
            self, value_or_callback: PersistentVolumeClaimVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def persistent_volume_claim(
            self,
            value_or_callback: Callable[
                [PersistentVolumeClaimVolumeSource.Builder],
                PersistentVolumeClaimVolumeSource.Builder | PersistentVolumeClaimVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def persistent_volume_claim(
            self, value_or_callback: Never = ...
        ) -> "PersistentVolumeClaimVolumeSource.BuilderContext": ...

        def persistent_volume_claim(self, value_or_callback=UNSET, /):
            "persistentVolumeClaimVolumeSource represents a reference to a PersistentVolumeClaim in the same namespace. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
            if self._in_context and value_or_callback is UNSET:
                context = PersistentVolumeClaimVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "persistent_volume_claim"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PersistentVolumeClaimVolumeSource.builder())
                if isinstance(output, PersistentVolumeClaimVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("persistent_volume_claim", value)

        @overload
        def photon_persistent_disk(
            self, value_or_callback: PhotonPersistentDiskVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def photon_persistent_disk(
            self,
            value_or_callback: Callable[
                [PhotonPersistentDiskVolumeSource.Builder],
                PhotonPersistentDiskVolumeSource.Builder | PhotonPersistentDiskVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def photon_persistent_disk(
            self, value_or_callback: Never = ...
        ) -> "PhotonPersistentDiskVolumeSource.BuilderContext": ...

        def photon_persistent_disk(self, value_or_callback=UNSET, /):
            "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine. Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = PhotonPersistentDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "photon_persistent_disk"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PhotonPersistentDiskVolumeSource.builder())
                if isinstance(output, PhotonPersistentDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("photon_persistent_disk", value)

        @overload
        def portworx_volume(
            self, value_or_callback: PortworxVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def portworx_volume(
            self,
            value_or_callback: Callable[
                [PortworxVolumeSource.Builder],
                PortworxVolumeSource.Builder | PortworxVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def portworx_volume(
            self, value_or_callback: Never = ...
        ) -> "PortworxVolumeSource.BuilderContext": ...

        def portworx_volume(self, value_or_callback=UNSET, /):
            "portworxVolume represents a portworx volume attached and mounted on kubelets host machine. Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate is on."
            if self._in_context and value_or_callback is UNSET:
                context = PortworxVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "portworx_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PortworxVolumeSource.builder())
                if isinstance(output, PortworxVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("portworx_volume", value)

        @overload
        def projected(
            self, value_or_callback: ProjectedVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def projected(
            self,
            value_or_callback: Callable[
                [ProjectedVolumeSource.Builder],
                ProjectedVolumeSource.Builder | ProjectedVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def projected(
            self, value_or_callback: Never = ...
        ) -> "ProjectedVolumeSource.BuilderContext": ...

        def projected(self, value_or_callback=UNSET, /):
            "projected items for all in one resources secrets, configmaps, and downward API"
            if self._in_context and value_or_callback is UNSET:
                context = ProjectedVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "projected"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ProjectedVolumeSource.builder())
                if isinstance(output, ProjectedVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("projected", value)

        @overload
        def quobyte(self, value_or_callback: QuobyteVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def quobyte(
            self,
            value_or_callback: Callable[
                [QuobyteVolumeSource.Builder],
                QuobyteVolumeSource.Builder | QuobyteVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def quobyte(
            self, value_or_callback: Never = ...
        ) -> "QuobyteVolumeSource.BuilderContext": ...

        def quobyte(self, value_or_callback=UNSET, /):
            "quobyte represents a Quobyte mount on the host that shares a pod\u0027s lifetime. Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = QuobyteVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "quobyte"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(QuobyteVolumeSource.builder())
                if isinstance(output, QuobyteVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("quobyte", value)

        @overload
        def rbd(self, value_or_callback: RBDVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def rbd(
            self,
            value_or_callback: Callable[
                [RBDVolumeSource.Builder], RBDVolumeSource.Builder | RBDVolumeSource
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def rbd(self, value_or_callback: Never = ...) -> "RBDVolumeSource.BuilderContext": ...

        def rbd(self, value_or_callback=UNSET, /):
            "rbd represents a Rados Block Device mount on the host that shares a pod\u0027s lifetime. Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = RBDVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "rbd"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(RBDVolumeSource.builder())
                if isinstance(output, RBDVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("rbd", value)

        @overload
        def scale_io(
            self, value_or_callback: ScaleIOVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def scale_io(
            self,
            value_or_callback: Callable[
                [ScaleIOVolumeSource.Builder],
                ScaleIOVolumeSource.Builder | ScaleIOVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def scale_io(
            self, value_or_callback: Never = ...
        ) -> "ScaleIOVolumeSource.BuilderContext": ...

        def scale_io(self, value_or_callback=UNSET, /):
            "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes. Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = ScaleIOVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "scale_io"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ScaleIOVolumeSource.builder())
                if isinstance(output, ScaleIOVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scale_io", value)

        @overload
        def secret(self, value_or_callback: SecretVolumeSource | None, /) -> "Volume.Builder": ...

        @overload
        def secret(
            self,
            value_or_callback: Callable[
                [SecretVolumeSource.Builder],
                SecretVolumeSource.Builder | SecretVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def secret(self, value_or_callback: Never = ...) -> "SecretVolumeSource.BuilderContext": ...

        def secret(self, value_or_callback=UNSET, /):
            "secret represents a secret that should populate this volume. More info: https://kubernetes.io/docs/concepts/storage/volumes#secret"
            if self._in_context and value_or_callback is UNSET:
                context = SecretVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "secret"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(SecretVolumeSource.builder())
                if isinstance(output, SecretVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("secret", value)

        @overload
        def storageos(
            self, value_or_callback: StorageOSVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def storageos(
            self,
            value_or_callback: Callable[
                [StorageOSVolumeSource.Builder],
                StorageOSVolumeSource.Builder | StorageOSVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def storageos(
            self, value_or_callback: Never = ...
        ) -> "StorageOSVolumeSource.BuilderContext": ...

        def storageos(self, value_or_callback=UNSET, /):
            "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes. Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported."
            if self._in_context and value_or_callback is UNSET:
                context = StorageOSVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "storageos"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(StorageOSVolumeSource.builder())
                if isinstance(output, StorageOSVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("storageos", value)

        @overload
        def vsphere_volume(
            self, value_or_callback: VsphereVirtualDiskVolumeSource | None, /
        ) -> "Volume.Builder": ...

        @overload
        def vsphere_volume(
            self,
            value_or_callback: Callable[
                [VsphereVirtualDiskVolumeSource.Builder],
                VsphereVirtualDiskVolumeSource.Builder | VsphereVirtualDiskVolumeSource,
            ],
            /,
        ) -> "Volume.Builder": ...

        @overload
        def vsphere_volume(
            self, value_or_callback: Never = ...
        ) -> "VsphereVirtualDiskVolumeSource.BuilderContext": ...

        def vsphere_volume(self, value_or_callback=UNSET, /):
            "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine. Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type are redirected to the csi.vsphere.vmware.com CSI driver."
            if self._in_context and value_or_callback is UNSET:
                context = VsphereVirtualDiskVolumeSource.BuilderContext()
                context._parent_builder = self
                context._field_name = "vsphere_volume"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(VsphereVirtualDiskVolumeSource.builder())
                if isinstance(output, VsphereVirtualDiskVolumeSource.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("vsphere_volume", value)

    class BuilderContext(BuilderContextBase["Volume.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Volume.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Volume."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Volume", Builder]:
        return GenericListBuilder[Volume, Volume.Builder]()

    aws_elastic_block_store: Annotated[
        AWSElasticBlockStoreVolumeSource | None, Field(alias="awsElasticBlockStore")
    ] = None
    "awsElasticBlockStore represents an AWS Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore"
    azure_disk: Annotated[AzureDiskVolumeSource | None, Field(alias="azureDisk")] = None
    "azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod. Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type are redirected to the disk.csi.azure.com CSI driver."
    azure_file: Annotated[AzureFileVolumeSource | None, Field(alias="azureFile")] = None
    "azureFile represents an Azure File Service mount on the host and bind mount to the pod. Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type are redirected to the file.csi.azure.com CSI driver."
    cephfs: CephFSVolumeSource | None = None
    "cephFS represents a Ceph FS mount on the host that shares a pod\u0027s lifetime. Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported."
    cinder: CinderVolumeSource | None = None
    "cinder represents a cinder volume attached and mounted on kubelets host machine. Deprecated: Cinder is deprecated. All operations for the in-tree cinder type are redirected to the cinder.csi.openstack.org CSI driver. More info: https://examples.k8s.io/mysql-cinder-pd/README.md"
    config_map: Annotated[ConfigMapVolumeSource | None, Field(alias="configMap")] = None
    "configMap represents a configMap that should populate this volume"
    csi: CSIVolumeSource | None = None
    "csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers."
    downward_api: Annotated[DownwardAPIVolumeSource | None, Field(alias="downwardAPI")] = None
    "downwardAPI represents downward API about the pod that should populate this volume"
    empty_dir: Annotated[EmptyDirVolumeSource | None, Field(alias="emptyDir")] = None
    "emptyDir represents a temporary directory that shares a pod\u0027s lifetime. More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir"
    ephemeral: EphemeralVolumeSource | None = None
    "ephemeral represents a volume that is handled by a cluster storage driver. The volume\u0027s lifecycle is tied to the pod that defines it - it will be created before the pod starts, and deleted when the pod is removed.\n\nUse this if: a) the volume is only needed while the pod runs, b) features of normal volumes like restoring from snapshot or capacity\n   tracking are needed,\nc) the storage driver is specified through a storage class, and d) the storage driver supports dynamic volume provisioning through\n   a PersistentVolumeClaim (see EphemeralVolumeSource for more\n   information on the connection between this volume type\n   and PersistentVolumeClaim).\n\nUse PersistentVolumeClaim or one of the vendor-specific APIs for volumes that persist for longer than the lifecycle of an individual pod.\n\nUse CSI for light-weight local ephemeral volumes if the CSI driver is meant to be used that way - see the documentation of the driver for more information.\n\nA pod can use both types of ephemeral volumes and persistent volumes at the same time."
    fc: FCVolumeSource | None = None
    "fc represents a Fibre Channel resource that is attached to a kubelet\u0027s host machine and then exposed to the pod."
    flex_volume: Annotated[FlexVolumeSource | None, Field(alias="flexVolume")] = None
    "flexVolume represents a generic volume resource that is provisioned/attached using an exec based plugin. Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead."
    flocker: FlockerVolumeSource | None = None
    "flocker represents a Flocker volume attached to a kubelet\u0027s host machine. This depends on the Flocker control service being running. Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported."
    gce_persistent_disk: Annotated[
        GCEPersistentDiskVolumeSource | None, Field(alias="gcePersistentDisk")
    ] = None
    "gcePersistentDisk represents a GCE Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver. More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk"
    git_repo: Annotated[GitRepoVolumeSource | None, Field(alias="gitRepo")] = None
    "gitRepo represents a git repository at a particular revision. Deprecated: GitRepo is deprecated. To provision a container with a git repo, mount an EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir into the Pod\u0027s container."
    glusterfs: GlusterfsVolumeSource | None = None
    "glusterfs represents a Glusterfs mount on the host that shares a pod\u0027s lifetime. Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported."
    host_path: Annotated[HostPathVolumeSource | None, Field(alias="hostPath")] = None
    "hostPath represents a pre-existing file or directory on the host machine that is directly exposed to the container. This is generally used for system agents or other privileged things that are allowed to see the host machine. Most containers will NOT need this. More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath"
    image: ImageVolumeSource | None = None
    "image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet\u0027s host machine. The volume is resolved at pod startup depending on which PullPolicy value is provided:\n\n- Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails. - Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn\u0027t present. - IfNotPresent: the kubelet pulls if the reference isn\u0027t already present on disk. Container creation will fail if the reference isn\u0027t present and the pull fails.\n\nThe volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation. A failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message. The types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field. The OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images. The volume will be mounted read-only (ro) and non-executable files (noexec). Sub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33. The field spec.securityContext.fsGroupChangePolicy has no effect on this volume type."
    iscsi: ISCSIVolumeSource | None = None
    "iscsi represents an ISCSI Disk resource that is attached to a kubelet\u0027s host machine and then exposed to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi"
    name: str
    "name of the volume. Must be a DNS_LABEL and unique within the pod. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    nfs: NFSVolumeSource | None = None
    "nfs represents an NFS mount on the host that shares a pod\u0027s lifetime More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs"
    persistent_volume_claim: Annotated[
        PersistentVolumeClaimVolumeSource | None, Field(alias="persistentVolumeClaim")
    ] = None
    "persistentVolumeClaimVolumeSource represents a reference to a PersistentVolumeClaim in the same namespace. More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims"
    photon_persistent_disk: Annotated[
        PhotonPersistentDiskVolumeSource | None, Field(alias="photonPersistentDisk")
    ] = None
    "photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine. Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported."
    portworx_volume: Annotated[PortworxVolumeSource | None, Field(alias="portworxVolume")] = None
    "portworxVolume represents a portworx volume attached and mounted on kubelets host machine. Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate is on."
    projected: ProjectedVolumeSource | None = None
    "projected items for all in one resources secrets, configmaps, and downward API"
    quobyte: QuobyteVolumeSource | None = None
    "quobyte represents a Quobyte mount on the host that shares a pod\u0027s lifetime. Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported."
    rbd: RBDVolumeSource | None = None
    "rbd represents a Rados Block Device mount on the host that shares a pod\u0027s lifetime. Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported."
    scale_io: Annotated[ScaleIOVolumeSource | None, Field(alias="scaleIO")] = None
    "scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes. Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported."
    secret: SecretVolumeSource | None = None
    "secret represents a secret that should populate this volume. More info: https://kubernetes.io/docs/concepts/storage/volumes#secret"
    storageos: StorageOSVolumeSource | None = None
    "storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes. Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported."
    vsphere_volume: Annotated[
        VsphereVirtualDiskVolumeSource | None, Field(alias="vsphereVolume")
    ] = None
    "vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine. Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type are redirected to the csi.vsphere.vmware.com CSI driver."


class PodSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodSpec"]:
            return PodSpec

        def build(self) -> "PodSpec":
            return PodSpec(**self._attrs)

        def active_deadline_seconds(self, value: int | None, /) -> Self:
            "Optional duration in seconds the pod may be active on the node relative to StartTime before the system will actively try to mark it failed and kill associated containers. Value must be a positive integer."
            return self._set("active_deadline_seconds", value)

        @overload
        def affinity(self, value_or_callback: Affinity | None, /) -> "PodSpec.Builder": ...

        @overload
        def affinity(
            self,
            value_or_callback: Callable[[Affinity.Builder], Affinity.Builder | Affinity],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def affinity(self, value_or_callback: Never = ...) -> "Affinity.BuilderContext": ...

        def affinity(self, value_or_callback=UNSET, /):
            "If specified, the pod\u0027s scheduling constraints"
            if self._in_context and value_or_callback is UNSET:
                context = Affinity.BuilderContext()
                context._parent_builder = self
                context._field_name = "affinity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Affinity.builder())
                if isinstance(output, Affinity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("affinity", value)

        def automount_service_account_token(self, value: bool | None, /) -> Self:
            "AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
            return self._set("automount_service_account_token", value)

        @overload
        def containers(self, value_or_callback: list[Container], /) -> "PodSpec.Builder": ...

        @overload
        def containers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Container, Container.Builder]],
                GenericListBuilder[Container, Container.Builder] | list[Container],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def containers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Container.Builder]: ...

        def containers(self, value_or_callback=UNSET, /):
            "List of containers belonging to the pod. Containers cannot currently be added or removed. There must be at least one container in a Pod. Cannot be updated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Container.Builder]()
                context._parent_builder = self
                context._field_name = "containers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Container.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("containers", value)

        @overload
        def dns_config(self, value_or_callback: PodDNSConfig | None, /) -> "PodSpec.Builder": ...

        @overload
        def dns_config(
            self,
            value_or_callback: Callable[
                [PodDNSConfig.Builder], PodDNSConfig.Builder | PodDNSConfig
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def dns_config(self, value_or_callback: Never = ...) -> "PodDNSConfig.BuilderContext": ...

        def dns_config(self, value_or_callback=UNSET, /):
            "Specifies the DNS parameters of a pod. Parameters specified here will be merged to the generated DNS configuration based on DNSPolicy."
            if self._in_context and value_or_callback is UNSET:
                context = PodDNSConfig.BuilderContext()
                context._parent_builder = self
                context._field_name = "dns_config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodDNSConfig.builder())
                if isinstance(output, PodDNSConfig.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("dns_config", value)

        def dns_policy(self, value: str | None, /) -> Self:
            'Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are \u0027ClusterFirstWithHostNet\u0027, \u0027ClusterFirst\u0027, \u0027Default\u0027 or \u0027None\u0027. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy. To have DNS options set along with hostNetwork, you have to specify DNS policy explicitly to \u0027ClusterFirstWithHostNet\u0027.'
            return self._set("dns_policy", value)

        def enable_service_links(self, value: bool | None, /) -> Self:
            "EnableServiceLinks indicates whether information about services should be injected into pod\u0027s environment variables, matching the syntax of Docker links. Optional: Defaults to true."
            return self._set("enable_service_links", value)

        @overload
        def ephemeral_containers(
            self, value_or_callback: list[EphemeralContainer] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def ephemeral_containers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[EphemeralContainer, EphemeralContainer.Builder]],
                GenericListBuilder[EphemeralContainer, EphemeralContainer.Builder]
                | list[EphemeralContainer],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def ephemeral_containers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[EphemeralContainer.Builder]: ...

        def ephemeral_containers(self, value_or_callback=UNSET, /):
            "List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing pod to perform user-initiated actions such as debugging. This list cannot be specified when creating a pod, and it cannot be modified by updating the pod spec. In order to add an ephemeral container to an existing pod, use the pod\u0027s ephemeralcontainers subresource."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[EphemeralContainer.Builder]()
                context._parent_builder = self
                context._field_name = "ephemeral_containers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EphemeralContainer.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ephemeral_containers", value)

        @overload
        def host_aliases(
            self, value_or_callback: list[HostAlias] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def host_aliases(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[HostAlias, HostAlias.Builder]],
                GenericListBuilder[HostAlias, HostAlias.Builder] | list[HostAlias],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def host_aliases(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[HostAlias.Builder]: ...

        def host_aliases(self, value_or_callback=UNSET, /):
            "HostAliases is an optional list of hosts and IPs that will be injected into the pod\u0027s hosts file if specified."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[HostAlias.Builder]()
                context._parent_builder = self
                context._field_name = "host_aliases"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(HostAlias.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("host_aliases", value)

        def host_ipc(self, value: bool | None, /) -> Self:
            "Use the host\u0027s ipc namespace. Optional: Default to false."
            return self._set("host_ipc", value)

        def host_network(self, value: bool | None, /) -> Self:
            "Host networking requested for this pod. Use the host\u0027s network namespace. When using HostNetwork you should specify ports so the scheduler is aware. When `hostNetwork` is true, specified `hostPort` fields in port definitions must match `containerPort`, and unspecified `hostPort` fields in port definitions are defaulted to match `containerPort`. Default to false."
            return self._set("host_network", value)

        def host_pid(self, value: bool | None, /) -> Self:
            "Use the host\u0027s pid namespace. Optional: Default to false."
            return self._set("host_pid", value)

        def host_users(self, value: bool | None, /) -> Self:
            "Use the host\u0027s user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new userns is created for the pod. Setting false is useful for mitigating container breakout vulnerabilities even allowing users to run their containers as root without actually having root privileges on the host. This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
            return self._set("host_users", value)

        def hostname(self, value: str | None, /) -> Self:
            "Specifies the hostname of the Pod If not specified, the pod\u0027s hostname will be set to a system-defined value."
            return self._set("hostname", value)

        def hostname_override(self, value: str | None, /) -> Self:
            "HostnameOverride specifies an explicit override for the pod\u0027s hostname as perceived by the pod. This field only specifies the pod\u0027s hostname and does not affect its DNS records. When this field is set to a non-empty string: - It takes precedence over the values set in `hostname` and `subdomain`. - The Pod\u0027s hostname will be set to this value. - `setHostnameAsFQDN` must be nil or set to false. - `hostNetwork` must be set to false.\n\nThis field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters. Requires the HostnameOverride feature gate to be enabled."
            return self._set("hostname_override", value)

        @overload
        def image_pull_secrets(
            self, value_or_callback: list[LocalObjectReference] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def image_pull_secrets(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[LocalObjectReference, LocalObjectReference.Builder]],
                GenericListBuilder[LocalObjectReference, LocalObjectReference.Builder]
                | list[LocalObjectReference],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def image_pull_secrets(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[LocalObjectReference.Builder]: ...

        def image_pull_secrets(self, value_or_callback=UNSET, /):
            "ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec. If specified, these secrets will be passed to individual puller implementations for them to use. More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[LocalObjectReference.Builder]()
                context._parent_builder = self
                context._field_name = "image_pull_secrets"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(LocalObjectReference.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("image_pull_secrets", value)

        @overload
        def init_containers(
            self, value_or_callback: list[Container] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def init_containers(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Container, Container.Builder]],
                GenericListBuilder[Container, Container.Builder] | list[Container],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def init_containers(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Container.Builder]: ...

        def init_containers(self, value_or_callback=UNSET, /):
            "List of initialization containers belonging to the pod. Init containers are executed in order prior to containers being started. If any init container fails, the pod is considered to have failed and is handled according to its restartPolicy. The name for an init container or normal container must be unique among all containers. Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes. The resourceRequirements of an init container are taken into account during scheduling by finding the highest request/limit for each resource type, and then using the max of that value or the sum of the normal containers. Limits are applied to init containers in a similar fashion. Init containers cannot currently be added or removed. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Container.Builder]()
                context._parent_builder = self
                context._field_name = "init_containers"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Container.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("init_containers", value)

        def node_name(self, value: str | None, /) -> Self:
            "NodeName indicates in which node this pod is scheduled. If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName. Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod. This field should not be used to express a desire for the pod to be scheduled on a specific node. https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
            return self._set("node_name", value)

        def node_selector(self, value: dict[str, str] | None, /) -> Self:
            "NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node\u0027s labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
            return self._set("node_selector", value)

        @overload
        def os(self, value_or_callback: PodOS | None, /) -> "PodSpec.Builder": ...

        @overload
        def os(
            self, value_or_callback: Callable[[PodOS.Builder], PodOS.Builder | PodOS], /
        ) -> "PodSpec.Builder": ...

        @overload
        def os(self, value_or_callback: Never = ...) -> "PodOS.BuilderContext": ...

        def os(self, value_or_callback=UNSET, /):
            "Specifies the OS of the containers in the pod. Some pod and container fields are restricted if this is set.\n\nIf the OS field is set to linux, the following fields must be unset: -securityContext.windowsOptions\n\nIf the OS field is set to windows, following fields must be unset: - spec.hostPID - spec.hostIPC - spec.hostUsers - spec.resources - spec.securityContext.appArmorProfile - spec.securityContext.seLinuxOptions - spec.securityContext.seccompProfile - spec.securityContext.fsGroup - spec.securityContext.fsGroupChangePolicy - spec.securityContext.sysctls - spec.shareProcessNamespace - spec.securityContext.runAsUser - spec.securityContext.runAsGroup - spec.securityContext.supplementalGroups - spec.securityContext.supplementalGroupsPolicy - spec.containers[*].securityContext.appArmorProfile - spec.containers[*].securityContext.seLinuxOptions - spec.containers[*].securityContext.seccompProfile - spec.containers[*].securityContext.capabilities - spec.containers[*].securityContext.readOnlyRootFilesystem - spec.containers[*].securityContext.privileged - spec.containers[*].securityContext.allowPrivilegeEscalation - spec.containers[*].securityContext.procMount - spec.containers[*].securityContext.runAsUser - spec.containers[*].securityContext.runAsGroup"
            if self._in_context and value_or_callback is UNSET:
                context = PodOS.BuilderContext()
                context._parent_builder = self
                context._field_name = "os"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodOS.builder())
                if isinstance(output, PodOS.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("os", value)

        def overhead(self, value: dict[str, Quantity] | None, /) -> Self:
            "Overhead represents the resource overhead associated with running a pod for a given RuntimeClass. This field will be autopopulated at admission time by the RuntimeClass admission controller. If the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests. The RuntimeClass admission controller will reject Pod create requests which have the overhead already set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero. More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
            return self._set("overhead", value)

        def preemption_policy(self, value: str | None, /) -> Self:
            "PreemptionPolicy is the Policy for preempting pods with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset."
            return self._set("preemption_policy", value)

        def priority(self, value: int | None, /) -> Self:
            "The priority value. Various system components use this field to find the priority of the pod. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority."
            return self._set("priority", value)

        def priority_class_name(self, value: str | None, /) -> Self:
            'If specified, indicates the pod\u0027s priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default.'
            return self._set("priority_class_name", value)

        @overload
        def readiness_gates(
            self, value_or_callback: list[PodReadinessGate] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def readiness_gates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodReadinessGate, PodReadinessGate.Builder]],
                GenericListBuilder[PodReadinessGate, PodReadinessGate.Builder]
                | list[PodReadinessGate],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def readiness_gates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodReadinessGate.Builder]: ...

        def readiness_gates(self, value_or_callback=UNSET, /):
            'If specified, all readiness gates will be evaluated for pod readiness. A pod is ready when all its containers are ready AND all conditions specified in the readiness gates have status equal to "True" More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates'
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodReadinessGate.Builder]()
                context._parent_builder = self
                context._field_name = "readiness_gates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodReadinessGate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("readiness_gates", value)

        @overload
        def resource_claims(
            self, value_or_callback: list[PodResourceClaim] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def resource_claims(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodResourceClaim, PodResourceClaim.Builder]],
                GenericListBuilder[PodResourceClaim, PodResourceClaim.Builder]
                | list[PodResourceClaim],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def resource_claims(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodResourceClaim.Builder]: ...

        def resource_claims(self, value_or_callback=UNSET, /):
            "ResourceClaims defines which ResourceClaims must be allocated and reserved before the Pod is allowed to start. The resources will be made available to those containers which consume them by name.\n\nThis is an alpha field and requires enabling the DynamicResourceAllocation feature gate.\n\nThis field is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodResourceClaim.Builder]()
                context._parent_builder = self
                context._field_name = "resource_claims"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodResourceClaim.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("resource_claims", value)

        @overload
        def resources(
            self, value_or_callback: ResourceRequirements | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def resources(
            self,
            value_or_callback: Callable[
                [ResourceRequirements.Builder],
                ResourceRequirements.Builder | ResourceRequirements,
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def resources(
            self, value_or_callback: Never = ...
        ) -> "ResourceRequirements.BuilderContext": ...

        def resources(self, value_or_callback=UNSET, /):
            'Resources is the total amount of CPU and Memory resources required by all containers in the pod. It supports specifying Requests and Limits for "cpu", "memory" and "hugepages-" resource names only. ResourceClaims are not supported.\n\nThis field enables fine-grained control over resource allocation for the entire pod, allowing resource sharing among containers in a pod.\n\nThis is an alpha field and requires enabling the PodLevelResources feature gate.'
            if self._in_context and value_or_callback is UNSET:
                context = ResourceRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "resources"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceRequirements.builder())
                if isinstance(output, ResourceRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("resources", value)

        def restart_policy(self, value: str | None, /) -> Self:
            "Restart policy for all containers within the pod. One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted. Default to Always. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
            return self._set("restart_policy", value)

        def runtime_class_name(self, value: str | None, /) -> Self:
            'RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class'
            return self._set("runtime_class_name", value)

        def scheduler_name(self, value: str | None, /) -> Self:
            "If specified, the pod will be dispatched by specified scheduler. If not specified, the pod will be dispatched by default scheduler."
            return self._set("scheduler_name", value)

        @overload
        def scheduling_gates(
            self, value_or_callback: list[PodSchedulingGate] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def scheduling_gates(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[PodSchedulingGate, PodSchedulingGate.Builder]],
                GenericListBuilder[PodSchedulingGate, PodSchedulingGate.Builder]
                | list[PodSchedulingGate],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def scheduling_gates(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[PodSchedulingGate.Builder]: ...

        def scheduling_gates(self, value_or_callback=UNSET, /):
            "SchedulingGates is an opaque list of values that if specified will block scheduling the pod. If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the scheduler will not attempt to schedule the pod.\n\nSchedulingGates can only be set at pod creation time, and be removed only afterwards."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[PodSchedulingGate.Builder]()
                context._parent_builder = self
                context._field_name = "scheduling_gates"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodSchedulingGate.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling_gates", value)

        @overload
        def security_context(
            self, value_or_callback: PodSecurityContext | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def security_context(
            self,
            value_or_callback: Callable[
                [PodSecurityContext.Builder],
                PodSecurityContext.Builder | PodSecurityContext,
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def security_context(
            self, value_or_callback: Never = ...
        ) -> "PodSecurityContext.BuilderContext": ...

        def security_context(self, value_or_callback=UNSET, /):
            "SecurityContext holds pod-level security attributes and common container settings. Optional: Defaults to empty.  See type description for default values of each field."
            if self._in_context and value_or_callback is UNSET:
                context = PodSecurityContext.BuilderContext()
                context._parent_builder = self
                context._field_name = "security_context"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodSecurityContext.builder())
                if isinstance(output, PodSecurityContext.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("security_context", value)

        def service_account(self, value: str | None, /) -> Self:
            "DeprecatedServiceAccount is a deprecated alias for ServiceAccountName. Deprecated: Use serviceAccountName instead."
            return self._set("service_account", value)

        def service_account_name(self, value: str | None, /) -> Self:
            "ServiceAccountName is the name of the ServiceAccount to use to run this pod. More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
            return self._set("service_account_name", value)

        def set_hostname_as_fqdn(self, value: bool | None, /) -> Self:
            "If true the pod\u0027s hostname will be configured as the pod\u0027s FQDN, rather than the leaf name (the default). In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname). In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\\\SYSTEM\\\\CurrentControlSet\\\\Services\\\\Tcpip\\\\Parameters to FQDN. If a pod does not have FQDN, this has no effect. Default to false."
            return self._set("set_hostname_as_fqdn", value)

        def share_process_namespace(self, value: bool | None, /) -> Self:
            "Share a single process namespace between all of the containers in a pod. When this is set containers will be able to view and signal processes from other containers in the same pod, and the first process in each container will not be assigned PID 1. HostPID and ShareProcessNamespace cannot both be set. Optional: Default to false."
            return self._set("share_process_namespace", value)

        def subdomain(self, value: str | None, /) -> Self:
            'If specified, the fully qualified Pod hostname will be "\u003chostname\u003e.\u003csubdomain\u003e.\u003cpod namespace\u003e.svc.\u003ccluster domain\u003e". If not specified, the pod will not have a domainname at all.'
            return self._set("subdomain", value)

        def termination_grace_period_seconds(self, value: int | None, /) -> Self:
            "Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds."
            return self._set("termination_grace_period_seconds", value)

        @overload
        def tolerations(
            self, value_or_callback: list[Toleration] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def tolerations(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Toleration, Toleration.Builder]],
                GenericListBuilder[Toleration, Toleration.Builder] | list[Toleration],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def tolerations(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Toleration.Builder]: ...

        def tolerations(self, value_or_callback=UNSET, /):
            "If specified, the pod\u0027s tolerations."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Toleration.Builder]()
                context._parent_builder = self
                context._field_name = "tolerations"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Toleration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tolerations", value)

        @overload
        def topology_spread_constraints(
            self, value_or_callback: list[TopologySpreadConstraint] | None, /
        ) -> "PodSpec.Builder": ...

        @overload
        def topology_spread_constraints(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TopologySpreadConstraint, TopologySpreadConstraint.Builder]],
                GenericListBuilder[TopologySpreadConstraint, TopologySpreadConstraint.Builder]
                | list[TopologySpreadConstraint],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def topology_spread_constraints(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TopologySpreadConstraint.Builder]: ...

        def topology_spread_constraints(self, value_or_callback=UNSET, /):
            "TopologySpreadConstraints describes how a group of pods ought to spread across topology domains. Scheduler will schedule pods in a way which abides by the constraints. All topologySpreadConstraints are ANDed."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TopologySpreadConstraint.Builder]()
                context._parent_builder = self
                context._field_name = "topology_spread_constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TopologySpreadConstraint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("topology_spread_constraints", value)

        @overload
        def volumes(self, value_or_callback: list[Volume] | None, /) -> "PodSpec.Builder": ...

        @overload
        def volumes(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Volume, Volume.Builder]],
                GenericListBuilder[Volume, Volume.Builder] | list[Volume],
            ],
            /,
        ) -> "PodSpec.Builder": ...

        @overload
        def volumes(self, value_or_callback: Never = ...) -> ListBuilderContext[Volume.Builder]: ...

        def volumes(self, value_or_callback=UNSET, /):
            "List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes"
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Volume.Builder]()
                context._parent_builder = self
                context._field_name = "volumes"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Volume.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("volumes", value)

    class BuilderContext(BuilderContextBase["PodSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodSpec", Builder]:
        return GenericListBuilder[PodSpec, PodSpec.Builder]()

    active_deadline_seconds: Annotated[int | None, Field(alias="activeDeadlineSeconds")] = None
    "Optional duration in seconds the pod may be active on the node relative to StartTime before the system will actively try to mark it failed and kill associated containers. Value must be a positive integer."
    affinity: Affinity | None = None
    "If specified, the pod\u0027s scheduling constraints"
    automount_service_account_token: Annotated[
        bool | None, Field(alias="automountServiceAccountToken")
    ] = None
    "AutomountServiceAccountToken indicates whether a service account token should be automatically mounted."
    containers: list[Container]
    "List of containers belonging to the pod. Containers cannot currently be added or removed. There must be at least one container in a Pod. Cannot be updated."
    dns_config: Annotated[PodDNSConfig | None, Field(alias="dnsConfig")] = None
    "Specifies the DNS parameters of a pod. Parameters specified here will be merged to the generated DNS configuration based on DNSPolicy."
    dns_policy: Annotated[str | None, Field(alias="dnsPolicy")] = None
    'Set DNS policy for the pod. Defaults to "ClusterFirst". Valid values are \u0027ClusterFirstWithHostNet\u0027, \u0027ClusterFirst\u0027, \u0027Default\u0027 or \u0027None\u0027. DNS parameters given in DNSConfig will be merged with the policy selected with DNSPolicy. To have DNS options set along with hostNetwork, you have to specify DNS policy explicitly to \u0027ClusterFirstWithHostNet\u0027.'
    enable_service_links: Annotated[bool | None, Field(alias="enableServiceLinks")] = None
    "EnableServiceLinks indicates whether information about services should be injected into pod\u0027s environment variables, matching the syntax of Docker links. Optional: Defaults to true."
    ephemeral_containers: Annotated[
        list[EphemeralContainer] | None, Field(alias="ephemeralContainers")
    ] = None
    "List of ephemeral containers run in this pod. Ephemeral containers may be run in an existing pod to perform user-initiated actions such as debugging. This list cannot be specified when creating a pod, and it cannot be modified by updating the pod spec. In order to add an ephemeral container to an existing pod, use the pod\u0027s ephemeralcontainers subresource."
    host_aliases: Annotated[list[HostAlias] | None, Field(alias="hostAliases")] = None
    "HostAliases is an optional list of hosts and IPs that will be injected into the pod\u0027s hosts file if specified."
    host_ipc: Annotated[bool | None, Field(alias="hostIPC")] = None
    "Use the host\u0027s ipc namespace. Optional: Default to false."
    host_network: Annotated[bool | None, Field(alias="hostNetwork")] = None
    "Host networking requested for this pod. Use the host\u0027s network namespace. When using HostNetwork you should specify ports so the scheduler is aware. When `hostNetwork` is true, specified `hostPort` fields in port definitions must match `containerPort`, and unspecified `hostPort` fields in port definitions are defaulted to match `containerPort`. Default to false."
    host_pid: Annotated[bool | None, Field(alias="hostPID")] = None
    "Use the host\u0027s pid namespace. Optional: Default to false."
    host_users: Annotated[bool | None, Field(alias="hostUsers")] = None
    "Use the host\u0027s user namespace. Optional: Default to true. If set to true or not present, the pod will be run in the host user namespace, useful for when the pod needs a feature only available to the host user namespace, such as loading a kernel module with CAP_SYS_MODULE. When set to false, a new userns is created for the pod. Setting false is useful for mitigating container breakout vulnerabilities even allowing users to run their containers as root without actually having root privileges on the host. This field is alpha-level and is only honored by servers that enable the UserNamespacesSupport feature."
    hostname: str | None = None
    "Specifies the hostname of the Pod If not specified, the pod\u0027s hostname will be set to a system-defined value."
    hostname_override: Annotated[str | None, Field(alias="hostnameOverride")] = None
    "HostnameOverride specifies an explicit override for the pod\u0027s hostname as perceived by the pod. This field only specifies the pod\u0027s hostname and does not affect its DNS records. When this field is set to a non-empty string: - It takes precedence over the values set in `hostname` and `subdomain`. - The Pod\u0027s hostname will be set to this value. - `setHostnameAsFQDN` must be nil or set to false. - `hostNetwork` must be set to false.\n\nThis field must be a valid DNS subdomain as defined in RFC 1123 and contain at most 64 characters. Requires the HostnameOverride feature gate to be enabled."
    image_pull_secrets: Annotated[
        list[LocalObjectReference] | None, Field(alias="imagePullSecrets")
    ] = None
    "ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec. If specified, these secrets will be passed to individual puller implementations for them to use. More info: https://kubernetes.io/docs/concepts/containers/images#specifying-imagepullsecrets-on-a-pod"
    init_containers: Annotated[list[Container] | None, Field(alias="initContainers")] = None
    "List of initialization containers belonging to the pod. Init containers are executed in order prior to containers being started. If any init container fails, the pod is considered to have failed and is handled according to its restartPolicy. The name for an init container or normal container must be unique among all containers. Init containers may not have Lifecycle actions, Readiness probes, Liveness probes, or Startup probes. The resourceRequirements of an init container are taken into account during scheduling by finding the highest request/limit for each resource type, and then using the max of that value or the sum of the normal containers. Limits are applied to init containers in a similar fashion. Init containers cannot currently be added or removed. Cannot be updated. More info: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/"
    node_name: Annotated[str | None, Field(alias="nodeName")] = None
    "NodeName indicates in which node this pod is scheduled. If empty, this pod is a candidate for scheduling by the scheduler defined in schedulerName. Once this field is set, the kubelet for this node becomes responsible for the lifecycle of this pod. This field should not be used to express a desire for the pod to be scheduled on a specific node. https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodename"
    node_selector: Annotated[dict[str, str] | None, Field(alias="nodeSelector")] = None
    "NodeSelector is a selector which must be true for the pod to fit on a node. Selector which must match a node\u0027s labels for the pod to be scheduled on that node. More info: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/"
    os: PodOS | None = None
    "Specifies the OS of the containers in the pod. Some pod and container fields are restricted if this is set.\n\nIf the OS field is set to linux, the following fields must be unset: -securityContext.windowsOptions\n\nIf the OS field is set to windows, following fields must be unset: - spec.hostPID - spec.hostIPC - spec.hostUsers - spec.resources - spec.securityContext.appArmorProfile - spec.securityContext.seLinuxOptions - spec.securityContext.seccompProfile - spec.securityContext.fsGroup - spec.securityContext.fsGroupChangePolicy - spec.securityContext.sysctls - spec.shareProcessNamespace - spec.securityContext.runAsUser - spec.securityContext.runAsGroup - spec.securityContext.supplementalGroups - spec.securityContext.supplementalGroupsPolicy - spec.containers[*].securityContext.appArmorProfile - spec.containers[*].securityContext.seLinuxOptions - spec.containers[*].securityContext.seccompProfile - spec.containers[*].securityContext.capabilities - spec.containers[*].securityContext.readOnlyRootFilesystem - spec.containers[*].securityContext.privileged - spec.containers[*].securityContext.allowPrivilegeEscalation - spec.containers[*].securityContext.procMount - spec.containers[*].securityContext.runAsUser - spec.containers[*].securityContext.runAsGroup"
    overhead: dict[str, Quantity] | None = None
    "Overhead represents the resource overhead associated with running a pod for a given RuntimeClass. This field will be autopopulated at admission time by the RuntimeClass admission controller. If the RuntimeClass admission controller is enabled, overhead must not be set in Pod create requests. The RuntimeClass admission controller will reject Pod create requests which have the overhead already set. If RuntimeClass is configured and selected in the PodSpec, Overhead will be set to the value defined in the corresponding RuntimeClass, otherwise it will remain unset and treated as zero. More info: https://git.k8s.io/enhancements/keps/sig-node/688-pod-overhead/README.md"
    preemption_policy: Annotated[str | None, Field(alias="preemptionPolicy")] = None
    "PreemptionPolicy is the Policy for preempting pods with lower priority. One of Never, PreemptLowerPriority. Defaults to PreemptLowerPriority if unset."
    priority: int | None = None
    "The priority value. Various system components use this field to find the priority of the pod. When Priority Admission Controller is enabled, it prevents users from setting this field. The admission controller populates this field from PriorityClassName. The higher the value, the higher the priority."
    priority_class_name: Annotated[str | None, Field(alias="priorityClassName")] = None
    'If specified, indicates the pod\u0027s priority. "system-node-critical" and "system-cluster-critical" are two special keywords which indicate the highest priorities with the former being the highest priority. Any other name must be defined by creating a PriorityClass object with that name. If not specified, the pod priority will be default or zero if there is no default.'
    readiness_gates: Annotated[list[PodReadinessGate] | None, Field(alias="readinessGates")] = None
    'If specified, all readiness gates will be evaluated for pod readiness. A pod is ready when all its containers are ready AND all conditions specified in the readiness gates have status equal to "True" More info: https://git.k8s.io/enhancements/keps/sig-network/580-pod-readiness-gates'
    resource_claims: Annotated[list[PodResourceClaim] | None, Field(alias="resourceClaims")] = None
    "ResourceClaims defines which ResourceClaims must be allocated and reserved before the Pod is allowed to start. The resources will be made available to those containers which consume them by name.\n\nThis is an alpha field and requires enabling the DynamicResourceAllocation feature gate.\n\nThis field is immutable."
    resources: ResourceRequirements | None = None
    'Resources is the total amount of CPU and Memory resources required by all containers in the pod. It supports specifying Requests and Limits for "cpu", "memory" and "hugepages-" resource names only. ResourceClaims are not supported.\n\nThis field enables fine-grained control over resource allocation for the entire pod, allowing resource sharing among containers in a pod.\n\nThis is an alpha field and requires enabling the PodLevelResources feature gate.'
    restart_policy: Annotated[str | None, Field(alias="restartPolicy")] = None
    "Restart policy for all containers within the pod. One of Always, OnFailure, Never. In some contexts, only a subset of those values may be permitted. Default to Always. More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy"
    runtime_class_name: Annotated[str | None, Field(alias="runtimeClassName")] = None
    'RuntimeClassName refers to a RuntimeClass object in the node.k8s.io group, which should be used to run this pod.  If no RuntimeClass resource matches the named class, the pod will not be run. If unset or empty, the "legacy" RuntimeClass will be used, which is an implicit class with an empty definition that uses the default runtime handler. More info: https://git.k8s.io/enhancements/keps/sig-node/585-runtime-class'
    scheduler_name: Annotated[str | None, Field(alias="schedulerName")] = None
    "If specified, the pod will be dispatched by specified scheduler. If not specified, the pod will be dispatched by default scheduler."
    scheduling_gates: Annotated[list[PodSchedulingGate] | None, Field(alias="schedulingGates")] = (
        None
    )
    "SchedulingGates is an opaque list of values that if specified will block scheduling the pod. If schedulingGates is not empty, the pod will stay in the SchedulingGated state and the scheduler will not attempt to schedule the pod.\n\nSchedulingGates can only be set at pod creation time, and be removed only afterwards."
    security_context: Annotated[PodSecurityContext | None, Field(alias="securityContext")] = None
    "SecurityContext holds pod-level security attributes and common container settings. Optional: Defaults to empty.  See type description for default values of each field."
    service_account: Annotated[str | None, Field(alias="serviceAccount")] = None
    "DeprecatedServiceAccount is a deprecated alias for ServiceAccountName. Deprecated: Use serviceAccountName instead."
    service_account_name: Annotated[str | None, Field(alias="serviceAccountName")] = None
    "ServiceAccountName is the name of the ServiceAccount to use to run this pod. More info: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/"
    set_hostname_as_fqdn: Annotated[bool | None, Field(alias="setHostnameAsFQDN")] = None
    "If true the pod\u0027s hostname will be configured as the pod\u0027s FQDN, rather than the leaf name (the default). In Linux containers, this means setting the FQDN in the hostname field of the kernel (the nodename field of struct utsname). In Windows containers, this means setting the registry value of hostname for the registry key HKEY_LOCAL_MACHINE\\\\SYSTEM\\\\CurrentControlSet\\\\Services\\\\Tcpip\\\\Parameters to FQDN. If a pod does not have FQDN, this has no effect. Default to false."
    share_process_namespace: Annotated[bool | None, Field(alias="shareProcessNamespace")] = None
    "Share a single process namespace between all of the containers in a pod. When this is set containers will be able to view and signal processes from other containers in the same pod, and the first process in each container will not be assigned PID 1. HostPID and ShareProcessNamespace cannot both be set. Optional: Default to false."
    subdomain: str | None = None
    'If specified, the fully qualified Pod hostname will be "\u003chostname\u003e.\u003csubdomain\u003e.\u003cpod namespace\u003e.svc.\u003ccluster domain\u003e". If not specified, the pod will not have a domainname at all.'
    termination_grace_period_seconds: Annotated[
        int | None, Field(alias="terminationGracePeriodSeconds")
    ] = None
    "Optional duration in seconds the pod needs to terminate gracefully. May be decreased in delete request. Value must be non-negative integer. The value zero indicates stop immediately via the kill signal (no opportunity to shut down). If this value is nil, the default grace period will be used instead. The grace period is the duration in seconds after the processes running in the pod are sent a termination signal and the time when the processes are forcibly halted with a kill signal. Set this value longer than the expected cleanup time for your process. Defaults to 30 seconds."
    tolerations: list[Toleration] | None = None
    "If specified, the pod\u0027s tolerations."
    topology_spread_constraints: Annotated[
        list[TopologySpreadConstraint] | None, Field(alias="topologySpreadConstraints")
    ] = None
    "TopologySpreadConstraints describes how a group of pods ought to spread across topology domains. Scheduler will schedule pods in a way which abides by the constraints. All topologySpreadConstraints are ANDed."
    volumes: list[Volume] | None = None
    "List of volumes that can be mounted by containers belonging to the pod. More info: https://kubernetes.io/docs/concepts/storage/volumes"


class PodTemplateSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodTemplateSpec"]:
            return PodTemplateSpec

        def build(self) -> "PodTemplateSpec":
            return PodTemplateSpec(**self._attrs)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "PodTemplateSpec.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PodTemplateSpec.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: PodSpec | None, /) -> "PodTemplateSpec.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[PodSpec.Builder], PodSpec.Builder | PodSpec],
            /,
        ) -> "PodTemplateSpec.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "PodSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Specification of the desired behavior of the pod. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = PodSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodSpec.builder())
                if isinstance(output, PodSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["PodTemplateSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodTemplateSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodTemplateSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodTemplateSpec", Builder]:
        return GenericListBuilder[PodTemplateSpec, PodTemplateSpec.Builder]()

    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: PodSpec | None = None
    "Specification of the desired behavior of the pod. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


class ReplicationControllerSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ReplicationControllerSpec"]:
            return ReplicationControllerSpec

        def build(self) -> "ReplicationControllerSpec":
            return ReplicationControllerSpec(**self._attrs)

        def min_ready_seconds(self, value: int | None, /) -> Self:
            "Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready)"
            return self._set("min_ready_seconds", value)

        def replicas(self, value: int | None, /) -> Self:
            "Replicas is the number of desired replicas. This is a pointer to distinguish between explicit zero and unspecified. Defaults to 1. More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#what-is-a-replicationcontroller"
            return self._set("replicas", value)

        def selector(self, value: dict[str, str] | None, /) -> Self:
            "Selector is a label query over pods that should match the Replicas count. If Selector is empty, it is defaulted to the labels present on the Pod template. Label keys and values that must match in order to be controlled by this replication controller, if empty defaulted to labels on Pod template. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors"
            return self._set("selector", value)

        @overload
        def template(
            self, value_or_callback: PodTemplateSpec | None, /
        ) -> "ReplicationControllerSpec.Builder": ...

        @overload
        def template(
            self,
            value_or_callback: Callable[
                [PodTemplateSpec.Builder], PodTemplateSpec.Builder | PodTemplateSpec
            ],
            /,
        ) -> "ReplicationControllerSpec.Builder": ...

        @overload
        def template(self, value_or_callback: Never = ...) -> "PodTemplateSpec.BuilderContext": ...

        def template(self, value_or_callback=UNSET, /):
            'Template is the object that describes the pod that will be created if insufficient replicas are detected. This takes precedence over a TemplateRef. The only allowed template.spec.restartPolicy value is "Always". More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#pod-template'
            if self._in_context and value_or_callback is UNSET:
                context = PodTemplateSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "template"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodTemplateSpec.builder())
                if isinstance(output, PodTemplateSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("template", value)

    class BuilderContext(BuilderContextBase["ReplicationControllerSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ReplicationControllerSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ReplicationControllerSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ReplicationControllerSpec", Builder]:
        return GenericListBuilder[ReplicationControllerSpec, ReplicationControllerSpec.Builder]()

    min_ready_seconds: Annotated[int | None, Field(alias="minReadySeconds")] = None
    "Minimum number of seconds for which a newly created pod should be ready without any of its container crashing, for it to be considered available. Defaults to 0 (pod will be considered available as soon as it is ready)"
    replicas: int | None = None
    "Replicas is the number of desired replicas. This is a pointer to distinguish between explicit zero and unspecified. Defaults to 1. More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#what-is-a-replicationcontroller"
    selector: dict[str, str] | None = None
    "Selector is a label query over pods that should match the Replicas count. If Selector is empty, it is defaulted to the labels present on the Pod template. Label keys and values that must match in order to be controlled by this replication controller, if empty defaulted to labels on Pod template. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors"
    template: PodTemplateSpec | None = None
    'Template is the object that describes the pod that will be created if insufficient replicas are detected. This takes precedence over a TemplateRef. The only allowed template.spec.restartPolicy value is "Always". More info: https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller#pod-template'


class Pod(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "pods",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Pod"]:
            return Pod

        def build(self) -> "Pod":
            return Pod(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Pod"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Pod.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Pod.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: PodSpec | None, /) -> "Pod.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[PodSpec.Builder], PodSpec.Builder | PodSpec],
            /,
        ) -> "Pod.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "PodSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Specification of the desired behavior of the pod. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = PodSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodSpec.builder())
                if isinstance(output, PodSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: PodStatus | None, /) -> "Pod.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[[PodStatus.Builder], PodStatus.Builder | PodStatus],
            /,
        ) -> "Pod.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "PodStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Most recently observed status of the pod. This data may not be up to date. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = PodStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodStatus.builder())
                if isinstance(output, PodStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Pod.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Pod.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Pod."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Pod", Builder]:
        return GenericListBuilder[Pod, Pod.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Pod"] = "Pod"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: PodSpec | None = None
    "Specification of the desired behavior of the pod. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: PodStatus | None = None
    "Most recently observed status of the pod. This data may not be up to date. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


PodList = ResourceList[Pod]


class PodTemplate(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "podtemplates",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["PodTemplate"]:
            return PodTemplate

        def build(self) -> "PodTemplate":
            return PodTemplate(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["PodTemplate"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "PodTemplate.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "PodTemplate.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def template(
            self, value_or_callback: PodTemplateSpec | None, /
        ) -> "PodTemplate.Builder": ...

        @overload
        def template(
            self,
            value_or_callback: Callable[
                [PodTemplateSpec.Builder], PodTemplateSpec.Builder | PodTemplateSpec
            ],
            /,
        ) -> "PodTemplate.Builder": ...

        @overload
        def template(self, value_or_callback: Never = ...) -> "PodTemplateSpec.BuilderContext": ...

        def template(self, value_or_callback=UNSET, /):
            "Template defines the pods that will be created from this pod template. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = PodTemplateSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "template"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(PodTemplateSpec.builder())
                if isinstance(output, PodTemplateSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("template", value)

    class BuilderContext(BuilderContextBase["PodTemplate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = PodTemplate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for PodTemplate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["PodTemplate", Builder]:
        return GenericListBuilder[PodTemplate, PodTemplate.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["PodTemplate"] = "PodTemplate"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    template: PodTemplateSpec | None = None
    "Template defines the pods that will be created from this pod template. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


PodTemplateList = ResourceList[PodTemplate]


class ReplicationController(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "replicationcontrollers",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ReplicationController"]:
            return ReplicationController

        def build(self) -> "ReplicationController":
            return ReplicationController(**self._attrs)

        def api_version(self, value: Literal["v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ReplicationController"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ReplicationController.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ReplicationController.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "If the Labels of a ReplicationController are empty, they are defaulted to be the same as the Pod(s) that the replication controller manages. Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ReplicationControllerSpec | None, /
        ) -> "ReplicationController.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ReplicationControllerSpec.Builder],
                ReplicationControllerSpec.Builder | ReplicationControllerSpec,
            ],
            /,
        ) -> "ReplicationController.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "ReplicationControllerSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines the specification of the desired behavior of the replication controller. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ReplicationControllerSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ReplicationControllerSpec.builder())
                if isinstance(output, ReplicationControllerSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: ReplicationControllerStatus | None, /
        ) -> "ReplicationController.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [ReplicationControllerStatus.Builder],
                ReplicationControllerStatus.Builder | ReplicationControllerStatus,
            ],
            /,
        ) -> "ReplicationController.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "ReplicationControllerStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Status is the most recently observed status of the replication controller. This data may be out of date by some window of time. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = ReplicationControllerStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ReplicationControllerStatus.builder())
                if isinstance(output, ReplicationControllerStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["ReplicationController.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ReplicationController.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ReplicationController."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ReplicationController", Builder]:
        return GenericListBuilder[ReplicationController, ReplicationController.Builder]()

    api_version: Annotated[Literal["v1"], Field(alias="apiVersion")] = "v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ReplicationController"] = "ReplicationController"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "If the Labels of a ReplicationController are empty, they are defaulted to be the same as the Pod(s) that the replication controller manages. Standard object\u0027s metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    spec: ReplicationControllerSpec | None = None
    "Spec defines the specification of the desired behavior of the replication controller. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: ReplicationControllerStatus | None = None
    "Status is the most recently observed status of the replication controller. This data may be out of date by some window of time. Populated by the system. Read-only. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


ReplicationControllerList = ResourceList[ReplicationController]
