# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    ObjectMeta,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class CELDeviceSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CELDeviceSelector"]:
            return CELDeviceSelector

        def build(self) -> "CELDeviceSelector":
            return CELDeviceSelector(**self._attrs)

        def expression(self, value: str, /) -> Self:
            'Expression is a CEL expression which evaluates a single device. It must evaluate to true when the device under consideration satisfies the desired criteria, and false when it does not. Any other result is an error and causes allocation of devices to abort.\n\nThe expression\u0027s input is an object named "device", which carries the following properties:\n - driver (string): the name of the driver which defines this device.\n - attributes (map[string]object): the device\u0027s attributes, grouped by prefix\n   (e.g. device.attributes["dra.example.com"] evaluates to an object with all\n   of the attributes which were prefixed by "dra.example.com".\n - capacity (map[string]object): the device\u0027s capacities, grouped by prefix.\n\nExample: Consider a device with driver="dra.example.com", which exposes two attributes named "model" and "ext.example.com/family" and which exposes one capacity named "modules". This input to this expression would have the following fields:\n\n    device.driver\n    device.attributes["dra.example.com"].model\n    device.attributes["ext.example.com"].family\n    device.capacity["dra.example.com"].modules\n\nThe device.driver field can be used to check for a specific driver, either as a high-level precondition (i.e. you only want to consider devices from this driver) or as part of a multi-clause expression that is meant to consider devices from different drivers.\n\nThe value type of each attribute is defined by the device definition, and users who write these expressions must consult the documentation for their specific drivers. The value type of each capacity is Quantity.\n\nIf an unknown prefix is used as a lookup in either device.attributes or device.capacity, an empty map will be returned. Any reference to an unknown field will cause an evaluation error and allocation to abort.\n\nA robust expression should check for the existence of attributes before referencing them.\n\nFor ease of use, the cel.bind() function is enabled, and can be used to simplify expressions that access multiple attributes with the same domain. For example:\n\n    cel.bind(dra, device.attributes["dra.example.com"], dra.someBool \u0026\u0026 dra.anotherBool)\n\nThe length of the expression must be smaller or equal to 10 Ki. The cost of evaluating it is also limited based on the estimated number of logical steps.'
            return self._set("expression", value)

    class BuilderContext(BuilderContextBase["CELDeviceSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CELDeviceSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CELDeviceSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CELDeviceSelector", Builder]:
        return GenericListBuilder[CELDeviceSelector, CELDeviceSelector.Builder]()

    expression: str
    'Expression is a CEL expression which evaluates a single device. It must evaluate to true when the device under consideration satisfies the desired criteria, and false when it does not. Any other result is an error and causes allocation of devices to abort.\n\nThe expression\u0027s input is an object named "device", which carries the following properties:\n - driver (string): the name of the driver which defines this device.\n - attributes (map[string]object): the device\u0027s attributes, grouped by prefix\n   (e.g. device.attributes["dra.example.com"] evaluates to an object with all\n   of the attributes which were prefixed by "dra.example.com".\n - capacity (map[string]object): the device\u0027s capacities, grouped by prefix.\n\nExample: Consider a device with driver="dra.example.com", which exposes two attributes named "model" and "ext.example.com/family" and which exposes one capacity named "modules". This input to this expression would have the following fields:\n\n    device.driver\n    device.attributes["dra.example.com"].model\n    device.attributes["ext.example.com"].family\n    device.capacity["dra.example.com"].modules\n\nThe device.driver field can be used to check for a specific driver, either as a high-level precondition (i.e. you only want to consider devices from this driver) or as part of a multi-clause expression that is meant to consider devices from different drivers.\n\nThe value type of each attribute is defined by the device definition, and users who write these expressions must consult the documentation for their specific drivers. The value type of each capacity is Quantity.\n\nIf an unknown prefix is used as a lookup in either device.attributes or device.capacity, an empty map will be returned. Any reference to an unknown field will cause an evaluation error and allocation to abort.\n\nA robust expression should check for the existence of attributes before referencing them.\n\nFor ease of use, the cel.bind() function is enabled, and can be used to simplify expressions that access multiple attributes with the same domain. For example:\n\n    cel.bind(dra, device.attributes["dra.example.com"], dra.someBool \u0026\u0026 dra.anotherBool)\n\nThe length of the expression must be smaller or equal to 10 Ki. The cost of evaluating it is also limited based on the estimated number of logical steps.'


class DeviceSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceSelector"]:
            return DeviceSelector

        def build(self) -> "DeviceSelector":
            return DeviceSelector(**self._attrs)

        @overload
        def cel(
            self, value_or_callback: CELDeviceSelector | None, /
        ) -> "DeviceSelector.Builder": ...

        @overload
        def cel(
            self,
            value_or_callback: Callable[
                [CELDeviceSelector.Builder],
                CELDeviceSelector.Builder | CELDeviceSelector,
            ],
            /,
        ) -> "DeviceSelector.Builder": ...

        @overload
        def cel(self, value_or_callback: Never = ...) -> "CELDeviceSelector.BuilderContext": ...

        def cel(self, value_or_callback=UNSET, /):
            "CEL contains a CEL expression for selecting a device."
            if self._in_context and value_or_callback is UNSET:
                context = CELDeviceSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "cel"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CELDeviceSelector.builder())
                if isinstance(output, CELDeviceSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cel", value)

    class BuilderContext(BuilderContextBase["DeviceSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceSelector", Builder]:
        return GenericListBuilder[DeviceSelector, DeviceSelector.Builder]()

    cel: CELDeviceSelector | None = None
    "CEL contains a CEL expression for selecting a device."


class DeviceTaint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceTaint"]:
            return DeviceTaint

        def build(self) -> "DeviceTaint":
            return DeviceTaint(**self._attrs)

        def effect(self, value: str, /) -> Self:
            "The effect of the taint on claims that do not tolerate the taint and through such claims on the pods using them. Valid effects are NoSchedule and NoExecute. PreferNoSchedule as used for nodes is not valid here."
            return self._set("effect", value)

        def key(self, value: str, /) -> Self:
            "The taint key to be applied to a device. Must be a label name."
            return self._set("key", value)

        @overload
        def time_added(self, value_or_callback: Time | None, /) -> "DeviceTaint.Builder": ...

        @overload
        def time_added(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "DeviceTaint.Builder": ...

        @overload
        def time_added(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def time_added(self, value_or_callback=UNSET, /):
            "TimeAdded represents the time at which the taint was added. Added automatically during create or update if not set."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "time_added"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("time_added", value)

        def value(self, value: str | None, /) -> Self:
            "The taint value corresponding to the taint key. Must be a label value."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["DeviceTaint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceTaint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceTaint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceTaint", Builder]:
        return GenericListBuilder[DeviceTaint, DeviceTaint.Builder]()

    effect: str
    "The effect of the taint on claims that do not tolerate the taint and through such claims on the pods using them. Valid effects are NoSchedule and NoExecute. PreferNoSchedule as used for nodes is not valid here."
    key: str
    "The taint key to be applied to a device. Must be a label name."
    time_added: Annotated[Time | None, Field(alias="timeAdded")] = None
    "TimeAdded represents the time at which the taint was added. Added automatically during create or update if not set."
    value: str | None = None
    "The taint value corresponding to the taint key. Must be a label value."


class DeviceTaintSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceTaintSelector"]:
            return DeviceTaintSelector

        def build(self) -> "DeviceTaintSelector":
            return DeviceTaintSelector(**self._attrs)

        def device(self, value: str | None, /) -> Self:
            "If device is set, only devices with that name are selected. This field corresponds to slice.spec.devices[].name.\n\nSetting also driver and pool may be required to avoid ambiguity, but is not required."
            return self._set("device", value)

        def device_class_name(self, value: str | None, /) -> Self:
            "If DeviceClassName is set, the selectors defined there must be satisfied by a device to be selected. This field corresponds to class.metadata.name."
            return self._set("device_class_name", value)

        def driver(self, value: str | None, /) -> Self:
            "If driver is set, only devices from that driver are selected. This fields corresponds to slice.spec.driver."
            return self._set("driver", value)

        def pool(self, value: str | None, /) -> Self:
            "If pool is set, only devices in that pool are selected.\n\nAlso setting the driver name may be useful to avoid ambiguity when different drivers use the same pool name, but this is not required because selecting pools from different drivers may also be useful, for example when drivers with node-local devices use the node name as their pool name."
            return self._set("pool", value)

        @overload
        def selectors(
            self, value_or_callback: list[DeviceSelector] | None, /
        ) -> "DeviceTaintSelector.Builder": ...

        @overload
        def selectors(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceSelector, DeviceSelector.Builder]],
                GenericListBuilder[DeviceSelector, DeviceSelector.Builder] | list[DeviceSelector],
            ],
            /,
        ) -> "DeviceTaintSelector.Builder": ...

        @overload
        def selectors(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceSelector.Builder]: ...

        def selectors(self, value_or_callback=UNSET, /):
            "Selectors contains the same selection criteria as a ResourceClaim. Currently, CEL expressions are supported. All of these selectors must be satisfied."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceSelector.Builder]()
                context._parent_builder = self
                context._field_name = "selectors"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceSelector.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("selectors", value)

    class BuilderContext(BuilderContextBase["DeviceTaintSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceTaintSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceTaintSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceTaintSelector", Builder]:
        return GenericListBuilder[DeviceTaintSelector, DeviceTaintSelector.Builder]()

    device: str | None = None
    "If device is set, only devices with that name are selected. This field corresponds to slice.spec.devices[].name.\n\nSetting also driver and pool may be required to avoid ambiguity, but is not required."
    device_class_name: Annotated[str | None, Field(alias="deviceClassName")] = None
    "If DeviceClassName is set, the selectors defined there must be satisfied by a device to be selected. This field corresponds to class.metadata.name."
    driver: str | None = None
    "If driver is set, only devices from that driver are selected. This fields corresponds to slice.spec.driver."
    pool: str | None = None
    "If pool is set, only devices in that pool are selected.\n\nAlso setting the driver name may be useful to avoid ambiguity when different drivers use the same pool name, but this is not required because selecting pools from different drivers may also be useful, for example when drivers with node-local devices use the node name as their pool name."
    selectors: list[DeviceSelector] | None = None
    "Selectors contains the same selection criteria as a ResourceClaim. Currently, CEL expressions are supported. All of these selectors must be satisfied."


class DeviceTaintRuleSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceTaintRuleSpec"]:
            return DeviceTaintRuleSpec

        def build(self) -> "DeviceTaintRuleSpec":
            return DeviceTaintRuleSpec(**self._attrs)

        @overload
        def device_selector(
            self, value_or_callback: DeviceTaintSelector | None, /
        ) -> "DeviceTaintRuleSpec.Builder": ...

        @overload
        def device_selector(
            self,
            value_or_callback: Callable[
                [DeviceTaintSelector.Builder],
                DeviceTaintSelector.Builder | DeviceTaintSelector,
            ],
            /,
        ) -> "DeviceTaintRuleSpec.Builder": ...

        @overload
        def device_selector(
            self, value_or_callback: Never = ...
        ) -> "DeviceTaintSelector.BuilderContext": ...

        def device_selector(self, value_or_callback=UNSET, /):
            "DeviceSelector defines which device(s) the taint is applied to. All selector criteria must be satified for a device to match. The empty selector matches all devices. Without a selector, no devices are matches."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceTaintSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "device_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceTaintSelector.builder())
                if isinstance(output, DeviceTaintSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("device_selector", value)

        @overload
        def taint(self, value_or_callback: DeviceTaint, /) -> "DeviceTaintRuleSpec.Builder": ...

        @overload
        def taint(
            self,
            value_or_callback: Callable[[DeviceTaint.Builder], DeviceTaint.Builder | DeviceTaint],
            /,
        ) -> "DeviceTaintRuleSpec.Builder": ...

        @overload
        def taint(self, value_or_callback: Never = ...) -> "DeviceTaint.BuilderContext": ...

        def taint(self, value_or_callback=UNSET, /):
            "The taint that gets applied to matching devices."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceTaint.BuilderContext()
                context._parent_builder = self
                context._field_name = "taint"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceTaint.builder())
                if isinstance(output, DeviceTaint.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("taint", value)

    class BuilderContext(BuilderContextBase["DeviceTaintRuleSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceTaintRuleSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceTaintRuleSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceTaintRuleSpec", Builder]:
        return GenericListBuilder[DeviceTaintRuleSpec, DeviceTaintRuleSpec.Builder]()

    device_selector: Annotated[DeviceTaintSelector | None, Field(alias="deviceSelector")] = None
    "DeviceSelector defines which device(s) the taint is applied to. All selector criteria must be satified for a device to match. The empty selector matches all devices. Without a selector, no devices are matches."
    taint: DeviceTaint
    "The taint that gets applied to matching devices."


class DeviceTaintRule(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "devicetaintrules",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceTaintRule"]:
            return DeviceTaintRule

        def build(self) -> "DeviceTaintRule":
            return DeviceTaintRule(**self._attrs)

        def api_version(self, value: Literal["resource.k8s.io/v1alpha3"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["DeviceTaintRule"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "DeviceTaintRule.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "DeviceTaintRule.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: DeviceTaintRuleSpec, /) -> "DeviceTaintRule.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [DeviceTaintRuleSpec.Builder],
                DeviceTaintRuleSpec.Builder | DeviceTaintRuleSpec,
            ],
            /,
        ) -> "DeviceTaintRule.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "DeviceTaintRuleSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec specifies the selector and one taint.\n\nChanging the spec automatically increments the metadata.generation number."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceTaintRuleSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceTaintRuleSpec.builder())
                if isinstance(output, DeviceTaintRuleSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["DeviceTaintRule.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceTaintRule.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceTaintRule."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceTaintRule", Builder]:
        return GenericListBuilder[DeviceTaintRule, DeviceTaintRule.Builder]()

    api_version: Annotated[Literal["resource.k8s.io/v1alpha3"], Field(alias="apiVersion")] = (
        "resource.k8s.io/v1alpha3"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["DeviceTaintRule"] = "DeviceTaintRule"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object metadata"
    spec: DeviceTaintRuleSpec
    "Spec specifies the selector and one taint.\n\nChanging the spec automatically increments the metadata.generation number."


DeviceTaintRuleList = ResourceList[DeviceTaintRule]
