# Generated by cloudcoil-model-codegen v0.7.0
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    Condition,
    ObjectMeta,
    Quantity,
    RawExtension,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field

from ..core import v1


class CELDeviceSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CELDeviceSelector"]:
            return CELDeviceSelector

        def build(self) -> "CELDeviceSelector":
            return CELDeviceSelector(**self._attrs)

        def expression(self, value: str, /) -> Self:
            'Expression is a CEL expression which evaluates a single device. It must evaluate to true when the device under consideration satisfies the desired criteria, and false when it does not. Any other result is an error and causes allocation of devices to abort.\n\nThe expression\u0027s input is an object named "device", which carries the following properties:\n - driver (string): the name of the driver which defines this device.\n - attributes (map[string]object): the device\u0027s attributes, grouped by prefix\n   (e.g. device.attributes["dra.example.com"] evaluates to an object with all\n   of the attributes which were prefixed by "dra.example.com".\n - capacity (map[string]object): the device\u0027s capacities, grouped by prefix.\n - allowMultipleAllocations (bool): the allowMultipleAllocations property of the device\n   (v1.34+ with the DRAConsumableCapacity feature enabled).\n\nExample: Consider a device with driver="dra.example.com", which exposes two attributes named "model" and "ext.example.com/family" and which exposes one capacity named "modules". This input to this expression would have the following fields:\n\n    device.driver\n    device.attributes["dra.example.com"].model\n    device.attributes["ext.example.com"].family\n    device.capacity["dra.example.com"].modules\n\nThe device.driver field can be used to check for a specific driver, either as a high-level precondition (i.e. you only want to consider devices from this driver) or as part of a multi-clause expression that is meant to consider devices from different drivers.\n\nThe value type of each attribute is defined by the device definition, and users who write these expressions must consult the documentation for their specific drivers. The value type of each capacity is Quantity.\n\nIf an unknown prefix is used as a lookup in either device.attributes or device.capacity, an empty map will be returned. Any reference to an unknown field will cause an evaluation error and allocation to abort.\n\nA robust expression should check for the existence of attributes before referencing them.\n\nFor ease of use, the cel.bind() function is enabled, and can be used to simplify expressions that access multiple attributes with the same domain. For example:\n\n    cel.bind(dra, device.attributes["dra.example.com"], dra.someBool \u0026\u0026 dra.anotherBool)\n\nThe length of the expression must be smaller or equal to 10 Ki. The cost of evaluating it is also limited based on the estimated number of logical steps.'
            return self._set("expression", value)

    class BuilderContext(BuilderContextBase["CELDeviceSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CELDeviceSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CELDeviceSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CELDeviceSelector", Builder]:
        return GenericListBuilder[CELDeviceSelector, CELDeviceSelector.Builder]()

    expression: str
    'Expression is a CEL expression which evaluates a single device. It must evaluate to true when the device under consideration satisfies the desired criteria, and false when it does not. Any other result is an error and causes allocation of devices to abort.\n\nThe expression\u0027s input is an object named "device", which carries the following properties:\n - driver (string): the name of the driver which defines this device.\n - attributes (map[string]object): the device\u0027s attributes, grouped by prefix\n   (e.g. device.attributes["dra.example.com"] evaluates to an object with all\n   of the attributes which were prefixed by "dra.example.com".\n - capacity (map[string]object): the device\u0027s capacities, grouped by prefix.\n - allowMultipleAllocations (bool): the allowMultipleAllocations property of the device\n   (v1.34+ with the DRAConsumableCapacity feature enabled).\n\nExample: Consider a device with driver="dra.example.com", which exposes two attributes named "model" and "ext.example.com/family" and which exposes one capacity named "modules". This input to this expression would have the following fields:\n\n    device.driver\n    device.attributes["dra.example.com"].model\n    device.attributes["ext.example.com"].family\n    device.capacity["dra.example.com"].modules\n\nThe device.driver field can be used to check for a specific driver, either as a high-level precondition (i.e. you only want to consider devices from this driver) or as part of a multi-clause expression that is meant to consider devices from different drivers.\n\nThe value type of each attribute is defined by the device definition, and users who write these expressions must consult the documentation for their specific drivers. The value type of each capacity is Quantity.\n\nIf an unknown prefix is used as a lookup in either device.attributes or device.capacity, an empty map will be returned. Any reference to an unknown field will cause an evaluation error and allocation to abort.\n\nA robust expression should check for the existence of attributes before referencing them.\n\nFor ease of use, the cel.bind() function is enabled, and can be used to simplify expressions that access multiple attributes with the same domain. For example:\n\n    cel.bind(dra, device.attributes["dra.example.com"], dra.someBool \u0026\u0026 dra.anotherBool)\n\nThe length of the expression must be smaller or equal to 10 Ki. The cost of evaluating it is also limited based on the estimated number of logical steps.'


class CapacityRequestPolicyRange(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CapacityRequestPolicyRange"]:
            return CapacityRequestPolicyRange

        def build(self) -> "CapacityRequestPolicyRange":
            return CapacityRequestPolicyRange(**self._attrs)

        @overload
        def max(
            self, value_or_callback: Quantity | None, /
        ) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def max(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def max(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def max(self, value_or_callback=UNSET, /):
            "Max defines the upper limit for capacity that can be requested.\n\nMax must be less than or equal to the capacity value. Min and requestPolicy.default must be less than or equal to the maximum."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "max"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("max", value)

        @overload
        def min(self, value_or_callback: Quantity, /) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def min(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def min(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def min(self, value_or_callback=UNSET, /):
            "Min specifies the minimum capacity allowed for a consumption request.\n\nMin must be greater than or equal to zero, and less than or equal to the capacity value. requestPolicy.default must be more than or equal to the minimum."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "min"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("min", value)

        @overload
        def step(
            self, value_or_callback: Quantity | None, /
        ) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def step(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CapacityRequestPolicyRange.Builder": ...

        @overload
        def step(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def step(self, value_or_callback=UNSET, /):
            "Step defines the step size between valid capacity amounts within the range.\n\nMax (if set) and requestPolicy.default must be a multiple of Step. Min + Step must be less than or equal to the capacity value."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "step"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("step", value)

    class BuilderContext(BuilderContextBase["CapacityRequestPolicyRange.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CapacityRequestPolicyRange.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CapacityRequestPolicyRange."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CapacityRequestPolicyRange", Builder]:
        return GenericListBuilder[CapacityRequestPolicyRange, CapacityRequestPolicyRange.Builder]()

    max: Quantity | None = None
    "Max defines the upper limit for capacity that can be requested.\n\nMax must be less than or equal to the capacity value. Min and requestPolicy.default must be less than or equal to the maximum."
    min: Quantity
    "Min specifies the minimum capacity allowed for a consumption request.\n\nMin must be greater than or equal to zero, and less than or equal to the capacity value. requestPolicy.default must be more than or equal to the minimum."
    step: Quantity | None = None
    "Step defines the step size between valid capacity amounts within the range.\n\nMax (if set) and requestPolicy.default must be a multiple of Step. Min + Step must be less than or equal to the capacity value."


class CapacityRequirements(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CapacityRequirements"]:
            return CapacityRequirements

        def build(self) -> "CapacityRequirements":
            return CapacityRequirements(**self._attrs)

        def requests(self, value: dict[str, Quantity] | None, /) -> Self:
            "Requests represent individual device resource requests for distinct resources, all of which must be provided by the device.\n\nThis value is used as an additional filtering condition against the available capacity on the device. This is semantically equivalent to a CEL selector with `device.capacity[\u003cdomain\u003e].\u003cname\u003e.compareTo(quantity(\u003crequest quantity\u003e)) \u003e= 0`. For example, device.capacity[\u0027test-driver.cdi.k8s.io\u0027].counters.compareTo(quantity(\u00272\u0027)) \u003e= 0.\n\nWhen a requestPolicy is defined, the requested amount is adjusted upward to the nearest valid value based on the policy. If the requested amount cannot be adjusted to a valid value\u2014because it exceeds what the requestPolicy allows\u2014 the device is considered ineligible for allocation.\n\nFor any capacity that is not explicitly requested: - If no requestPolicy is set, the default consumed capacity is equal to the full device capacity\n  (i.e., the whole device is claimed).\n- If a requestPolicy is set, the default consumed capacity is determined according to that policy.\n\nIf the device allows multiple allocation, the aggregated amount across all requests must not exceed the capacity value. The consumed capacity, which may be adjusted based on the requestPolicy if defined, is recorded in the resource claim\u2019s status.devices[*].consumedCapacity field."
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["CapacityRequirements.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CapacityRequirements.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CapacityRequirements."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CapacityRequirements", Builder]:
        return GenericListBuilder[CapacityRequirements, CapacityRequirements.Builder]()

    requests: dict[str, Quantity] | None = None
    "Requests represent individual device resource requests for distinct resources, all of which must be provided by the device.\n\nThis value is used as an additional filtering condition against the available capacity on the device. This is semantically equivalent to a CEL selector with `device.capacity[\u003cdomain\u003e].\u003cname\u003e.compareTo(quantity(\u003crequest quantity\u003e)) \u003e= 0`. For example, device.capacity[\u0027test-driver.cdi.k8s.io\u0027].counters.compareTo(quantity(\u00272\u0027)) \u003e= 0.\n\nWhen a requestPolicy is defined, the requested amount is adjusted upward to the nearest valid value based on the policy. If the requested amount cannot be adjusted to a valid value\u2014because it exceeds what the requestPolicy allows\u2014 the device is considered ineligible for allocation.\n\nFor any capacity that is not explicitly requested: - If no requestPolicy is set, the default consumed capacity is equal to the full device capacity\n  (i.e., the whole device is claimed).\n- If a requestPolicy is set, the default consumed capacity is determined according to that policy.\n\nIf the device allows multiple allocation, the aggregated amount across all requests must not exceed the capacity value. The consumed capacity, which may be adjusted based on the requestPolicy if defined, is recorded in the resource claim\u2019s status.devices[*].consumedCapacity field."


class Counter(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Counter"]:
            return Counter

        def build(self) -> "Counter":
            return Counter(**self._attrs)

        @overload
        def value(self, value_or_callback: Quantity, /) -> "Counter.Builder": ...

        @overload
        def value(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "Counter.Builder": ...

        @overload
        def value(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def value(self, value_or_callback=UNSET, /):
            "Value defines how much of a certain device counter is available."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "value"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["Counter.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Counter.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Counter."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Counter", Builder]:
        return GenericListBuilder[Counter, Counter.Builder]()

    value: Quantity
    "Value defines how much of a certain device counter is available."


class CounterSet(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CounterSet"]:
            return CounterSet

        def build(self) -> "CounterSet":
            return CounterSet(**self._attrs)

        def counters(self, value: dict[str, Counter], /) -> Self:
            "Counters defines the set of counters for this CounterSet The name of each counter must be unique in that set and must be a DNS label.\n\nThe maximum number of counters is 32."
            return self._set("counters", value)

        def name(self, value: str, /) -> Self:
            "Name defines the name of the counter set. It must be a DNS label."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["CounterSet.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CounterSet.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CounterSet."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CounterSet", Builder]:
        return GenericListBuilder[CounterSet, CounterSet.Builder]()

    counters: dict[str, Counter]
    "Counters defines the set of counters for this CounterSet The name of each counter must be unique in that set and must be a DNS label.\n\nThe maximum number of counters is 32."
    name: str
    "Name defines the name of the counter set. It must be a DNS label."


class DeviceAttribute(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceAttribute"]:
            return DeviceAttribute

        def build(self) -> "DeviceAttribute":
            return DeviceAttribute(**self._attrs)

        def bool_(self, value: bool | None, /) -> Self:
            "BoolValue is a true/false value."
            return self._set("bool_", value)

        def int_(self, value: int | None, /) -> Self:
            "IntValue is a number."
            return self._set("int_", value)

        def string(self, value: str | None, /) -> Self:
            "StringValue is a string. Must not be longer than 64 characters."
            return self._set("string", value)

        def version(self, value: str | None, /) -> Self:
            "VersionValue is a semantic version according to semver.org spec 2.0.0. Must not be longer than 64 characters."
            return self._set("version", value)

    class BuilderContext(BuilderContextBase["DeviceAttribute.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceAttribute.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceAttribute."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceAttribute", Builder]:
        return GenericListBuilder[DeviceAttribute, DeviceAttribute.Builder]()

    bool_: Annotated[bool | None, Field(alias="bool")] = None
    "BoolValue is a true/false value."
    int_: Annotated[int | None, Field(alias="int")] = None
    "IntValue is a number."
    string: str | None = None
    "StringValue is a string. Must not be longer than 64 characters."
    version: str | None = None
    "VersionValue is a semantic version according to semver.org spec 2.0.0. Must not be longer than 64 characters."


class DeviceConstraint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceConstraint"]:
            return DeviceConstraint

        def build(self) -> "DeviceConstraint":
            return DeviceConstraint(**self._attrs)

        def distinct_attribute(self, value: str | None, /) -> Self:
            "DistinctAttribute requires that all devices in question have this attribute and that its type and value are unique across those devices.\n\nThis acts as the inverse of MatchAttribute.\n\nThis constraint is used to avoid allocating multiple requests to the same device by ensuring attribute-level differentiation.\n\nThis is useful for scenarios where resource requests must be fulfilled by separate physical devices. For example, a container requests two network interfaces that must be allocated from two different physical NICs."
            return self._set("distinct_attribute", value)

        def match_attribute(self, value: str | None, /) -> Self:
            'MatchAttribute requires that all devices in question have this attribute and that its type and value are the same across those devices.\n\nFor example, if you specified "dra.example.com/numa" (a hypothetical example!), then only devices in the same NUMA node will be chosen. A device which does not have that attribute will not be chosen. All devices should use a value of the same type for this attribute because that is part of its specification, but if one device doesn\u0027t, then it also will not be chosen.\n\nMust include the domain qualifier.'
            return self._set("match_attribute", value)

        def requests(self, value: list[str] | None, /) -> Self:
            "Requests is a list of the one or more requests in this claim which must co-satisfy this constraint. If a request is fulfilled by multiple devices, then all of the devices must satisfy the constraint. If this is not specified, this constraint applies to all requests in this claim.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the constraint applies to all subrequests."
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["DeviceConstraint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceConstraint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceConstraint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceConstraint", Builder]:
        return GenericListBuilder[DeviceConstraint, DeviceConstraint.Builder]()

    distinct_attribute: Annotated[str | None, Field(alias="distinctAttribute")] = None
    "DistinctAttribute requires that all devices in question have this attribute and that its type and value are unique across those devices.\n\nThis acts as the inverse of MatchAttribute.\n\nThis constraint is used to avoid allocating multiple requests to the same device by ensuring attribute-level differentiation.\n\nThis is useful for scenarios where resource requests must be fulfilled by separate physical devices. For example, a container requests two network interfaces that must be allocated from two different physical NICs."
    match_attribute: Annotated[str | None, Field(alias="matchAttribute")] = None
    'MatchAttribute requires that all devices in question have this attribute and that its type and value are the same across those devices.\n\nFor example, if you specified "dra.example.com/numa" (a hypothetical example!), then only devices in the same NUMA node will be chosen. A device which does not have that attribute will not be chosen. All devices should use a value of the same type for this attribute because that is part of its specification, but if one device doesn\u0027t, then it also will not be chosen.\n\nMust include the domain qualifier.'
    requests: list[str] | None = None
    "Requests is a list of the one or more requests in this claim which must co-satisfy this constraint. If a request is fulfilled by multiple devices, then all of the devices must satisfy the constraint. If this is not specified, this constraint applies to all requests in this claim.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the constraint applies to all subrequests."


class DeviceCounterConsumption(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceCounterConsumption"]:
            return DeviceCounterConsumption

        def build(self) -> "DeviceCounterConsumption":
            return DeviceCounterConsumption(**self._attrs)

        def counter_set(self, value: str, /) -> Self:
            "CounterSet is the name of the set from which the counters defined will be consumed."
            return self._set("counter_set", value)

        def counters(self, value: dict[str, Counter], /) -> Self:
            "Counters defines the counters that will be consumed by the device.\n\nThe maximum number of counters is 32."
            return self._set("counters", value)

    class BuilderContext(BuilderContextBase["DeviceCounterConsumption.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceCounterConsumption.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceCounterConsumption."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceCounterConsumption", Builder]:
        return GenericListBuilder[DeviceCounterConsumption, DeviceCounterConsumption.Builder]()

    counter_set: Annotated[str, Field(alias="counterSet")]
    "CounterSet is the name of the set from which the counters defined will be consumed."
    counters: dict[str, Counter]
    "Counters defines the counters that will be consumed by the device.\n\nThe maximum number of counters is 32."


class DeviceSelector(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceSelector"]:
            return DeviceSelector

        def build(self) -> "DeviceSelector":
            return DeviceSelector(**self._attrs)

        @overload
        def cel(
            self, value_or_callback: CELDeviceSelector | None, /
        ) -> "DeviceSelector.Builder": ...

        @overload
        def cel(
            self,
            value_or_callback: Callable[
                [CELDeviceSelector.Builder],
                CELDeviceSelector.Builder | CELDeviceSelector,
            ],
            /,
        ) -> "DeviceSelector.Builder": ...

        @overload
        def cel(self, value_or_callback: Never = ...) -> "CELDeviceSelector.BuilderContext": ...

        def cel(self, value_or_callback=UNSET, /):
            "CEL contains a CEL expression for selecting a device."
            if self._in_context and value_or_callback is UNSET:
                context = CELDeviceSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "cel"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CELDeviceSelector.builder())
                if isinstance(output, CELDeviceSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("cel", value)

    class BuilderContext(BuilderContextBase["DeviceSelector.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceSelector.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceSelector."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceSelector", Builder]:
        return GenericListBuilder[DeviceSelector, DeviceSelector.Builder]()

    cel: CELDeviceSelector | None = None
    "CEL contains a CEL expression for selecting a device."


class DeviceTaint(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceTaint"]:
            return DeviceTaint

        def build(self) -> "DeviceTaint":
            return DeviceTaint(**self._attrs)

        def effect(self, value: str, /) -> Self:
            "The effect of the taint on claims that do not tolerate the taint and through such claims on the pods using them.\n\nValid effects are None, NoSchedule and NoExecute. PreferNoSchedule as used for nodes is not valid here. More effects may get added in the future. Consumers must treat unknown effects like None."
            return self._set("effect", value)

        def key(self, value: str, /) -> Self:
            "The taint key to be applied to a device. Must be a label name."
            return self._set("key", value)

        @overload
        def time_added(self, value_or_callback: Time | None, /) -> "DeviceTaint.Builder": ...

        @overload
        def time_added(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "DeviceTaint.Builder": ...

        @overload
        def time_added(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def time_added(self, value_or_callback=UNSET, /):
            "TimeAdded represents the time at which the taint was added. Added automatically during create or update if not set."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "time_added"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("time_added", value)

        def value(self, value: str | None, /) -> Self:
            "The taint value corresponding to the taint key. Must be a label value."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["DeviceTaint.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceTaint.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceTaint."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceTaint", Builder]:
        return GenericListBuilder[DeviceTaint, DeviceTaint.Builder]()

    effect: str
    "The effect of the taint on claims that do not tolerate the taint and through such claims on the pods using them.\n\nValid effects are None, NoSchedule and NoExecute. PreferNoSchedule as used for nodes is not valid here. More effects may get added in the future. Consumers must treat unknown effects like None."
    key: str
    "The taint key to be applied to a device. Must be a label name."
    time_added: Annotated[Time | None, Field(alias="timeAdded")] = None
    "TimeAdded represents the time at which the taint was added. Added automatically during create or update if not set."
    value: str | None = None
    "The taint value corresponding to the taint key. Must be a label value."


class DeviceToleration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceToleration"]:
            return DeviceToleration

        def build(self) -> "DeviceToleration":
            return DeviceToleration(**self._attrs)

        def effect(self, value: str | None, /) -> Self:
            "Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule and NoExecute."
            return self._set("effect", value)

        def key(self, value: str | None, /) -> Self:
            "Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys. Must be a label name."
            return self._set("key", value)

        def operator(self, value: str | None, /) -> Self:
            "Operator represents a key\u0027s relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a ResourceClaim can tolerate all taints of a particular category."
            return self._set("operator", value)

        def toleration_seconds(self, value: int | None, /) -> Self:
            "TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system. If larger than zero, the time when the pod needs to be evicted is calculated as \u003ctime when taint was adedd\u003e + \u003ctoleration seconds\u003e."
            return self._set("toleration_seconds", value)

        def value(self, value: str | None, /) -> Self:
            "Value is the taint value the toleration matches to. If the operator is Exists, the value must be empty, otherwise just a regular string. Must be a label value."
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["DeviceToleration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceToleration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceToleration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceToleration", Builder]:
        return GenericListBuilder[DeviceToleration, DeviceToleration.Builder]()

    effect: str | None = None
    "Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule and NoExecute."
    key: str | None = None
    "Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys. Must be a label name."
    operator: str | None = None
    "Operator represents a key\u0027s relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a ResourceClaim can tolerate all taints of a particular category."
    toleration_seconds: Annotated[int | None, Field(alias="tolerationSeconds")] = None
    "TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system. If larger than zero, the time when the pod needs to be evicted is calculated as \u003ctime when taint was adedd\u003e + \u003ctoleration seconds\u003e."
    value: str | None = None
    "Value is the taint value the toleration matches to. If the operator is Exists, the value must be empty, otherwise just a regular string. Must be a label value."


class NetworkDeviceData(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["NetworkDeviceData"]:
            return NetworkDeviceData

        def build(self) -> "NetworkDeviceData":
            return NetworkDeviceData(**self._attrs)

        def hardware_address(self, value: str | None, /) -> Self:
            "HardwareAddress represents the hardware address (e.g. MAC Address) of the device\u0027s network interface.\n\nMust not be longer than 128 characters."
            return self._set("hardware_address", value)

        def interface_name(self, value: str | None, /) -> Self:
            "InterfaceName specifies the name of the network interface associated with the allocated device. This might be the name of a physical or virtual network interface being configured in the pod.\n\nMust not be longer than 256 characters."
            return self._set("interface_name", value)

        def ips(self, value: list[str] | None, /) -> Self:
            'IPs lists the network addresses assigned to the device\u0027s network interface. This can include both IPv4 and IPv6 addresses. The IPs are in the CIDR notation, which includes both the address and the associated subnet mask. e.g.: "192.0.2.5/24" for IPv4 and "2001:db8::5/64" for IPv6.\n\nMust not contain more than 16 entries.'
            return self._set("ips", value)

    class BuilderContext(BuilderContextBase["NetworkDeviceData.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = NetworkDeviceData.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for NetworkDeviceData."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["NetworkDeviceData", Builder]:
        return GenericListBuilder[NetworkDeviceData, NetworkDeviceData.Builder]()

    hardware_address: Annotated[str | None, Field(alias="hardwareAddress")] = None
    "HardwareAddress represents the hardware address (e.g. MAC Address) of the device\u0027s network interface.\n\nMust not be longer than 128 characters."
    interface_name: Annotated[str | None, Field(alias="interfaceName")] = None
    "InterfaceName specifies the name of the network interface associated with the allocated device. This might be the name of a physical or virtual network interface being configured in the pod.\n\nMust not be longer than 256 characters."
    ips: list[str] | None = None
    'IPs lists the network addresses assigned to the device\u0027s network interface. This can include both IPv4 and IPv6 addresses. The IPs are in the CIDR notation, which includes both the address and the associated subnet mask. e.g.: "192.0.2.5/24" for IPv4 and "2001:db8::5/64" for IPv6.\n\nMust not contain more than 16 entries.'


class OpaqueDeviceConfiguration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["OpaqueDeviceConfiguration"]:
            return OpaqueDeviceConfiguration

        def build(self) -> "OpaqueDeviceConfiguration":
            return OpaqueDeviceConfiguration(**self._attrs)

        def driver(self, value: str, /) -> Self:
            "Driver is used to determine which kubelet plugin needs to be passed these configuration parameters.\n\nAn admission policy provided by the driver developer could use this to decide whether it needs to validate them.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
            return self._set("driver", value)

        @overload
        def parameters(
            self, value_or_callback: RawExtension, /
        ) -> "OpaqueDeviceConfiguration.Builder": ...

        @overload
        def parameters(
            self,
            value_or_callback: Callable[
                [RawExtension.Builder], RawExtension.Builder | RawExtension
            ],
            /,
        ) -> "OpaqueDeviceConfiguration.Builder": ...

        @overload
        def parameters(self, value_or_callback: Never = ...) -> "RawExtension.BuilderContext": ...

        def parameters(self, value_or_callback=UNSET, /):
            'Parameters can contain arbitrary data. It is the responsibility of the driver developer to handle validation and versioning. Typically this includes self-identification and a version ("kind" + "apiVersion" for Kubernetes types), with conversion between different versions.\n\nThe length of the raw data must be smaller or equal to 10 Ki.'
            if self._in_context and value_or_callback is UNSET:
                context = RawExtension.BuilderContext()
                context._parent_builder = self
                context._field_name = "parameters"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(RawExtension.builder())
                if isinstance(output, RawExtension.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("parameters", value)

    class BuilderContext(BuilderContextBase["OpaqueDeviceConfiguration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = OpaqueDeviceConfiguration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for OpaqueDeviceConfiguration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["OpaqueDeviceConfiguration", Builder]:
        return GenericListBuilder[OpaqueDeviceConfiguration, OpaqueDeviceConfiguration.Builder]()

    driver: str
    "Driver is used to determine which kubelet plugin needs to be passed these configuration parameters.\n\nAn admission policy provided by the driver developer could use this to decide whether it needs to validate them.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
    parameters: RawExtension
    'Parameters can contain arbitrary data. It is the responsibility of the driver developer to handle validation and versioning. Typically this includes self-identification and a version ("kind" + "apiVersion" for Kubernetes types), with conversion between different versions.\n\nThe length of the raw data must be smaller or equal to 10 Ki.'


class ResourceClaimConsumerReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaimConsumerReference"]:
            return ResourceClaimConsumerReference

        def build(self) -> "ResourceClaimConsumerReference":
            return ResourceClaimConsumerReference(**self._attrs)

        def api_group(self, value: str | None, /) -> Self:
            "APIGroup is the group for the resource being referenced. It is empty for the core API. This matches the group in the APIVersion that is used when creating the resources."
            return self._set("api_group", value)

        def name(self, value: str, /) -> Self:
            "Name is the name of resource being referenced."
            return self._set("name", value)

        def resource(self, value: str, /) -> Self:
            'Resource is the type of resource being referenced, for example "pods".'
            return self._set("resource", value)

        def uid(self, value: str, /) -> Self:
            "UID identifies exactly one incarnation of the resource."
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["ResourceClaimConsumerReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaimConsumerReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaimConsumerReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ResourceClaimConsumerReference", Builder]:
        return GenericListBuilder[
            ResourceClaimConsumerReference, ResourceClaimConsumerReference.Builder
        ]()

    api_group: Annotated[str | None, Field(alias="apiGroup")] = None
    "APIGroup is the group for the resource being referenced. It is empty for the core API. This matches the group in the APIVersion that is used when creating the resources."
    name: str
    "Name is the name of resource being referenced."
    resource: str
    'Resource is the type of resource being referenced, for example "pods".'
    uid: str
    "UID identifies exactly one incarnation of the resource."


class ResourcePool(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourcePool"]:
            return ResourcePool

        def build(self) -> "ResourcePool":
            return ResourcePool(**self._attrs)

        def generation(self, value: int, /) -> Self:
            "Generation tracks the change in a pool over time. Whenever a driver changes something about one or more of the resources in a pool, it must change the generation in all ResourceSlices which are part of that pool. Consumers of ResourceSlices should only consider resources from the pool with the highest generation number. The generation may be reset by drivers, which should be fine for consumers, assuming that all ResourceSlices in a pool are updated to match or deleted.\n\nCombined with ResourceSliceCount, this mechanism enables consumers to detect pools which are comprised of multiple ResourceSlices and are in an incomplete state."
            return self._set("generation", value)

        def name(self, value: str, /) -> Self:
            "Name is used to identify the pool. For node-local devices, this is often the node name, but this is not required.\n\nIt must not be longer than 253 characters and must consist of one or more DNS sub-domains separated by slashes. This field is immutable."
            return self._set("name", value)

        def resource_slice_count(self, value: int, /) -> Self:
            "ResourceSliceCount is the total number of ResourceSlices in the pool at this generation number. Must be greater than zero.\n\nConsumers can use this to check whether they have seen all ResourceSlices belonging to the same pool."
            return self._set("resource_slice_count", value)

    class BuilderContext(BuilderContextBase["ResourcePool.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourcePool.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourcePool."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourcePool", Builder]:
        return GenericListBuilder[ResourcePool, ResourcePool.Builder]()

    generation: int
    "Generation tracks the change in a pool over time. Whenever a driver changes something about one or more of the resources in a pool, it must change the generation in all ResourceSlices which are part of that pool. Consumers of ResourceSlices should only consider resources from the pool with the highest generation number. The generation may be reset by drivers, which should be fine for consumers, assuming that all ResourceSlices in a pool are updated to match or deleted.\n\nCombined with ResourceSliceCount, this mechanism enables consumers to detect pools which are comprised of multiple ResourceSlices and are in an incomplete state."
    name: str
    "Name is used to identify the pool. For node-local devices, this is often the node name, but this is not required.\n\nIt must not be longer than 253 characters and must consist of one or more DNS sub-domains separated by slashes. This field is immutable."
    resource_slice_count: Annotated[int, Field(alias="resourceSliceCount")]
    "ResourceSliceCount is the total number of ResourceSlices in the pool at this generation number. Must be greater than zero.\n\nConsumers can use this to check whether they have seen all ResourceSlices belonging to the same pool."


class AllocatedDeviceStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AllocatedDeviceStatus"]:
            return AllocatedDeviceStatus

        def build(self) -> "AllocatedDeviceStatus":
            return AllocatedDeviceStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions contains the latest observation of the device\u0027s state. If the device has been configured according to the class and claim config references, the `Ready` condition should be True.\n\nMust not contain more than 8 entries."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def data(
            self, value_or_callback: RawExtension | None, /
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def data(
            self,
            value_or_callback: Callable[
                [RawExtension.Builder], RawExtension.Builder | RawExtension
            ],
            /,
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def data(self, value_or_callback: Never = ...) -> "RawExtension.BuilderContext": ...

        def data(self, value_or_callback=UNSET, /):
            "Data contains arbitrary driver-specific data.\n\nThe length of the raw data must be smaller or equal to 10 Ki."
            if self._in_context and value_or_callback is UNSET:
                context = RawExtension.BuilderContext()
                context._parent_builder = self
                context._field_name = "data"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(RawExtension.builder())
                if isinstance(output, RawExtension.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("data", value)

        def device(self, value: str, /) -> Self:
            "Device references one device instance via its name in the driver\u0027s resource pool. It must be a DNS label."
            return self._set("device", value)

        def driver(self, value: str, /) -> Self:
            "Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
            return self._set("driver", value)

        @overload
        def network_data(
            self, value_or_callback: NetworkDeviceData | None, /
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def network_data(
            self,
            value_or_callback: Callable[
                [NetworkDeviceData.Builder],
                NetworkDeviceData.Builder | NetworkDeviceData,
            ],
            /,
        ) -> "AllocatedDeviceStatus.Builder": ...

        @overload
        def network_data(
            self, value_or_callback: Never = ...
        ) -> "NetworkDeviceData.BuilderContext": ...

        def network_data(self, value_or_callback=UNSET, /):
            "NetworkData contains network-related information specific to the device."
            if self._in_context and value_or_callback is UNSET:
                context = NetworkDeviceData.BuilderContext()
                context._parent_builder = self
                context._field_name = "network_data"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(NetworkDeviceData.builder())
                if isinstance(output, NetworkDeviceData.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("network_data", value)

        def pool(self, value: str, /) -> Self:
            "This name together with the driver name and the device name field identify which device was allocated (`\u003cdriver name\u003e/\u003cpool name\u003e/\u003cdevice name\u003e`).\n\nMust not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes."
            return self._set("pool", value)

        def share_id(self, value: str | None, /) -> Self:
            "ShareID uniquely identifies an individual allocation share of the device."
            return self._set("share_id", value)

    class BuilderContext(BuilderContextBase["AllocatedDeviceStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AllocatedDeviceStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AllocatedDeviceStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AllocatedDeviceStatus", Builder]:
        return GenericListBuilder[AllocatedDeviceStatus, AllocatedDeviceStatus.Builder]()

    conditions: list[Condition] | None = None
    "Conditions contains the latest observation of the device\u0027s state. If the device has been configured according to the class and claim config references, the `Ready` condition should be True.\n\nMust not contain more than 8 entries."
    data: RawExtension | None = None
    "Data contains arbitrary driver-specific data.\n\nThe length of the raw data must be smaller or equal to 10 Ki."
    device: str
    "Device references one device instance via its name in the driver\u0027s resource pool. It must be a DNS label."
    driver: str
    "Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
    network_data: Annotated[NetworkDeviceData | None, Field(alias="networkData")] = None
    "NetworkData contains network-related information specific to the device."
    pool: str
    "This name together with the driver name and the device name field identify which device was allocated (`\u003cdriver name\u003e/\u003cpool name\u003e/\u003cdevice name\u003e`).\n\nMust not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes."
    share_id: Annotated[str | None, Field(alias="shareID")] = None
    "ShareID uniquely identifies an individual allocation share of the device."


class CapacityRequestPolicy(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CapacityRequestPolicy"]:
            return CapacityRequestPolicy

        def build(self) -> "CapacityRequestPolicy":
            return CapacityRequestPolicy(**self._attrs)

        @overload
        def default(
            self, value_or_callback: Quantity | None, /
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def default(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def default(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def default(self, value_or_callback=UNSET, /):
            "Default specifies how much of this capacity is consumed by a request that does not contain an entry for it in DeviceRequest\u0027s Capacity."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "default"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("default", value)

        @overload
        def valid_range(
            self, value_or_callback: CapacityRequestPolicyRange | None, /
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def valid_range(
            self,
            value_or_callback: Callable[
                [CapacityRequestPolicyRange.Builder],
                CapacityRequestPolicyRange.Builder | CapacityRequestPolicyRange,
            ],
            /,
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def valid_range(
            self, value_or_callback: Never = ...
        ) -> "CapacityRequestPolicyRange.BuilderContext": ...

        def valid_range(self, value_or_callback=UNSET, /):
            "ValidRange defines an acceptable quantity value range in consuming requests.\n\nIf this field is set, Default must be defined and it must fall within the defined ValidRange.\n\nIf the requested amount does not fall within the defined range, the request violates the policy, and this device cannot be allocated.\n\nIf the request doesn\u0027t contain this capacity entry, Default value is used."
            if self._in_context and value_or_callback is UNSET:
                context = CapacityRequestPolicyRange.BuilderContext()
                context._parent_builder = self
                context._field_name = "valid_range"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CapacityRequestPolicyRange.builder())
                if isinstance(output, CapacityRequestPolicyRange.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("valid_range", value)

        @overload
        def valid_values(
            self, value_or_callback: list[Quantity] | None, /
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def valid_values(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Quantity, Quantity.Builder]],
                GenericListBuilder[Quantity, Quantity.Builder] | list[Quantity],
            ],
            /,
        ) -> "CapacityRequestPolicy.Builder": ...

        @overload
        def valid_values(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Quantity.Builder]: ...

        def valid_values(self, value_or_callback=UNSET, /):
            "ValidValues defines a set of acceptable quantity values in consuming requests.\n\nMust not contain more than 10 entries. Must be sorted in ascending order.\n\nIf this field is set, Default must be defined and it must be included in ValidValues list.\n\nIf the requested amount does not match any valid value but smaller than some valid values, the scheduler calculates the smallest valid value that is greater than or equal to the request. That is: min(ceil(requestedValue) \u2208 validValues), where requestedValue \u2264 max(validValues).\n\nIf the requested amount exceeds all valid values, the request violates the policy, and this device cannot be allocated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Quantity.Builder]()
                context._parent_builder = self
                context._field_name = "valid_values"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("valid_values", value)

    class BuilderContext(BuilderContextBase["CapacityRequestPolicy.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CapacityRequestPolicy.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CapacityRequestPolicy."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CapacityRequestPolicy", Builder]:
        return GenericListBuilder[CapacityRequestPolicy, CapacityRequestPolicy.Builder]()

    default: Quantity | None = None
    "Default specifies how much of this capacity is consumed by a request that does not contain an entry for it in DeviceRequest\u0027s Capacity."
    valid_range: Annotated[CapacityRequestPolicyRange | None, Field(alias="validRange")] = None
    "ValidRange defines an acceptable quantity value range in consuming requests.\n\nIf this field is set, Default must be defined and it must fall within the defined ValidRange.\n\nIf the requested amount does not fall within the defined range, the request violates the policy, and this device cannot be allocated.\n\nIf the request doesn\u0027t contain this capacity entry, Default value is used."
    valid_values: Annotated[list[Quantity] | None, Field(alias="validValues")] = None
    "ValidValues defines a set of acceptable quantity values in consuming requests.\n\nMust not contain more than 10 entries. Must be sorted in ascending order.\n\nIf this field is set, Default must be defined and it must be included in ValidValues list.\n\nIf the requested amount does not match any valid value but smaller than some valid values, the scheduler calculates the smallest valid value that is greater than or equal to the request. That is: min(ceil(requestedValue) \u2208 validValues), where requestedValue \u2264 max(validValues).\n\nIf the requested amount exceeds all valid values, the request violates the policy, and this device cannot be allocated."


class DeviceAllocationConfiguration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceAllocationConfiguration"]:
            return DeviceAllocationConfiguration

        def build(self) -> "DeviceAllocationConfiguration":
            return DeviceAllocationConfiguration(**self._attrs)

        @overload
        def opaque(
            self, value_or_callback: OpaqueDeviceConfiguration | None, /
        ) -> "DeviceAllocationConfiguration.Builder": ...

        @overload
        def opaque(
            self,
            value_or_callback: Callable[
                [OpaqueDeviceConfiguration.Builder],
                OpaqueDeviceConfiguration.Builder | OpaqueDeviceConfiguration,
            ],
            /,
        ) -> "DeviceAllocationConfiguration.Builder": ...

        @overload
        def opaque(
            self, value_or_callback: Never = ...
        ) -> "OpaqueDeviceConfiguration.BuilderContext": ...

        def opaque(self, value_or_callback=UNSET, /):
            "Opaque provides driver-specific configuration parameters."
            if self._in_context and value_or_callback is UNSET:
                context = OpaqueDeviceConfiguration.BuilderContext()
                context._parent_builder = self
                context._field_name = "opaque"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(OpaqueDeviceConfiguration.builder())
                if isinstance(output, OpaqueDeviceConfiguration.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("opaque", value)

        def requests(self, value: list[str] | None, /) -> Self:
            "Requests lists the names of requests where the configuration applies. If empty, its applies to all requests.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the configuration applies to all subrequests."
            return self._set("requests", value)

        def source(self, value: str, /) -> Self:
            "Source records whether the configuration comes from a class and thus is not something that a normal user would have been able to set or from a claim."
            return self._set("source", value)

    class BuilderContext(BuilderContextBase["DeviceAllocationConfiguration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceAllocationConfiguration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceAllocationConfiguration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["DeviceAllocationConfiguration", Builder]:
        return GenericListBuilder[
            DeviceAllocationConfiguration, DeviceAllocationConfiguration.Builder
        ]()

    opaque: OpaqueDeviceConfiguration | None = None
    "Opaque provides driver-specific configuration parameters."
    requests: list[str] | None = None
    "Requests lists the names of requests where the configuration applies. If empty, its applies to all requests.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the configuration applies to all subrequests."
    source: str
    "Source records whether the configuration comes from a class and thus is not something that a normal user would have been able to set or from a claim."


class DeviceCapacity(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceCapacity"]:
            return DeviceCapacity

        def build(self) -> "DeviceCapacity":
            return DeviceCapacity(**self._attrs)

        @overload
        def request_policy(
            self, value_or_callback: CapacityRequestPolicy | None, /
        ) -> "DeviceCapacity.Builder": ...

        @overload
        def request_policy(
            self,
            value_or_callback: Callable[
                [CapacityRequestPolicy.Builder],
                CapacityRequestPolicy.Builder | CapacityRequestPolicy,
            ],
            /,
        ) -> "DeviceCapacity.Builder": ...

        @overload
        def request_policy(
            self, value_or_callback: Never = ...
        ) -> "CapacityRequestPolicy.BuilderContext": ...

        def request_policy(self, value_or_callback=UNSET, /):
            "RequestPolicy defines how this DeviceCapacity must be consumed when the device is allowed to be shared by multiple allocations.\n\nThe Device must have allowMultipleAllocations set to true in order to set a requestPolicy.\n\nIf unset, capacity requests are unconstrained: requests can consume any amount of capacity, as long as the total consumed across all allocations does not exceed the device\u0027s defined capacity. If request is also unset, default is the full capacity value."
            if self._in_context and value_or_callback is UNSET:
                context = CapacityRequestPolicy.BuilderContext()
                context._parent_builder = self
                context._field_name = "request_policy"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CapacityRequestPolicy.builder())
                if isinstance(output, CapacityRequestPolicy.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("request_policy", value)

        @overload
        def value(self, value_or_callback: Quantity, /) -> "DeviceCapacity.Builder": ...

        @overload
        def value(
            self,
            value_or_callback: Callable[[Quantity.Builder], Quantity.Builder | Quantity],
            /,
        ) -> "DeviceCapacity.Builder": ...

        @overload
        def value(self, value_or_callback: Never = ...) -> "Quantity.BuilderContext": ...

        def value(self, value_or_callback=UNSET, /):
            "Value defines how much of a certain capacity that device has.\n\nThis field reflects the fixed total capacity and does not change. The consumed amount is tracked separately by scheduler and does not affect this value."
            if self._in_context and value_or_callback is UNSET:
                context = Quantity.BuilderContext()
                context._parent_builder = self
                context._field_name = "value"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Quantity.builder())
                if isinstance(output, Quantity.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("value", value)

    class BuilderContext(BuilderContextBase["DeviceCapacity.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceCapacity.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceCapacity."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceCapacity", Builder]:
        return GenericListBuilder[DeviceCapacity, DeviceCapacity.Builder]()

    request_policy: Annotated[CapacityRequestPolicy | None, Field(alias="requestPolicy")] = None
    "RequestPolicy defines how this DeviceCapacity must be consumed when the device is allowed to be shared by multiple allocations.\n\nThe Device must have allowMultipleAllocations set to true in order to set a requestPolicy.\n\nIf unset, capacity requests are unconstrained: requests can consume any amount of capacity, as long as the total consumed across all allocations does not exceed the device\u0027s defined capacity. If request is also unset, default is the full capacity value."
    value: Quantity
    "Value defines how much of a certain capacity that device has.\n\nThis field reflects the fixed total capacity and does not change. The consumed amount is tracked separately by scheduler and does not affect this value."


class DeviceClaimConfiguration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceClaimConfiguration"]:
            return DeviceClaimConfiguration

        def build(self) -> "DeviceClaimConfiguration":
            return DeviceClaimConfiguration(**self._attrs)

        @overload
        def opaque(
            self, value_or_callback: OpaqueDeviceConfiguration | None, /
        ) -> "DeviceClaimConfiguration.Builder": ...

        @overload
        def opaque(
            self,
            value_or_callback: Callable[
                [OpaqueDeviceConfiguration.Builder],
                OpaqueDeviceConfiguration.Builder | OpaqueDeviceConfiguration,
            ],
            /,
        ) -> "DeviceClaimConfiguration.Builder": ...

        @overload
        def opaque(
            self, value_or_callback: Never = ...
        ) -> "OpaqueDeviceConfiguration.BuilderContext": ...

        def opaque(self, value_or_callback=UNSET, /):
            "Opaque provides driver-specific configuration parameters."
            if self._in_context and value_or_callback is UNSET:
                context = OpaqueDeviceConfiguration.BuilderContext()
                context._parent_builder = self
                context._field_name = "opaque"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(OpaqueDeviceConfiguration.builder())
                if isinstance(output, OpaqueDeviceConfiguration.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("opaque", value)

        def requests(self, value: list[str] | None, /) -> Self:
            "Requests lists the names of requests where the configuration applies. If empty, it applies to all requests.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the configuration applies to all subrequests."
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["DeviceClaimConfiguration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceClaimConfiguration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceClaimConfiguration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceClaimConfiguration", Builder]:
        return GenericListBuilder[DeviceClaimConfiguration, DeviceClaimConfiguration.Builder]()

    opaque: OpaqueDeviceConfiguration | None = None
    "Opaque provides driver-specific configuration parameters."
    requests: list[str] | None = None
    "Requests lists the names of requests where the configuration applies. If empty, it applies to all requests.\n\nReferences to subrequests must include the name of the main request and may include the subrequest using the format \u003cmain request\u003e[/\u003csubrequest\u003e]. If just the main request is given, the configuration applies to all subrequests."


class DeviceClassConfiguration(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceClassConfiguration"]:
            return DeviceClassConfiguration

        def build(self) -> "DeviceClassConfiguration":
            return DeviceClassConfiguration(**self._attrs)

        @overload
        def opaque(
            self, value_or_callback: OpaqueDeviceConfiguration | None, /
        ) -> "DeviceClassConfiguration.Builder": ...

        @overload
        def opaque(
            self,
            value_or_callback: Callable[
                [OpaqueDeviceConfiguration.Builder],
                OpaqueDeviceConfiguration.Builder | OpaqueDeviceConfiguration,
            ],
            /,
        ) -> "DeviceClassConfiguration.Builder": ...

        @overload
        def opaque(
            self, value_or_callback: Never = ...
        ) -> "OpaqueDeviceConfiguration.BuilderContext": ...

        def opaque(self, value_or_callback=UNSET, /):
            "Opaque provides driver-specific configuration parameters."
            if self._in_context and value_or_callback is UNSET:
                context = OpaqueDeviceConfiguration.BuilderContext()
                context._parent_builder = self
                context._field_name = "opaque"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(OpaqueDeviceConfiguration.builder())
                if isinstance(output, OpaqueDeviceConfiguration.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("opaque", value)

    class BuilderContext(BuilderContextBase["DeviceClassConfiguration.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceClassConfiguration.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceClassConfiguration."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceClassConfiguration", Builder]:
        return GenericListBuilder[DeviceClassConfiguration, DeviceClassConfiguration.Builder]()

    opaque: OpaqueDeviceConfiguration | None = None
    "Opaque provides driver-specific configuration parameters."


class DeviceClassSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceClassSpec"]:
            return DeviceClassSpec

        def build(self) -> "DeviceClassSpec":
            return DeviceClassSpec(**self._attrs)

        @overload
        def config(
            self, value_or_callback: list[DeviceClassConfiguration] | None, /
        ) -> "DeviceClassSpec.Builder": ...

        @overload
        def config(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceClassConfiguration, DeviceClassConfiguration.Builder]],
                GenericListBuilder[DeviceClassConfiguration, DeviceClassConfiguration.Builder]
                | list[DeviceClassConfiguration],
            ],
            /,
        ) -> "DeviceClassSpec.Builder": ...

        @overload
        def config(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceClassConfiguration.Builder]: ...

        def config(self, value_or_callback=UNSET, /):
            "Config defines configuration parameters that apply to each device that is claimed via this class. Some classses may potentially be satisfied by multiple drivers, so each instance of a vendor configuration applies to exactly one driver.\n\nThey are passed to the driver, but are not considered while allocating the claim."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceClassConfiguration.Builder]()
                context._parent_builder = self
                context._field_name = "config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceClassConfiguration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("config", value)

        def extended_resource_name(self, value: str | None, /) -> Self:
            "ExtendedResourceName is the extended resource name for the devices of this class. The devices of this class can be used to satisfy a pod\u0027s extended resource requests. It has the same format as the name of a pod\u0027s extended resource. It should be unique among all the device classes in a cluster. If two device classes have the same name, then the class created later is picked to satisfy a pod\u0027s extended resource requests. If two classes are created at the same time, then the name of the class lexicographically sorted first is picked.\n\nThis is an alpha field."
            return self._set("extended_resource_name", value)

        @overload
        def selectors(
            self, value_or_callback: list[DeviceSelector] | None, /
        ) -> "DeviceClassSpec.Builder": ...

        @overload
        def selectors(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceSelector, DeviceSelector.Builder]],
                GenericListBuilder[DeviceSelector, DeviceSelector.Builder] | list[DeviceSelector],
            ],
            /,
        ) -> "DeviceClassSpec.Builder": ...

        @overload
        def selectors(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceSelector.Builder]: ...

        def selectors(self, value_or_callback=UNSET, /):
            "Each selector must be satisfied by a device which is claimed via this class."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceSelector.Builder]()
                context._parent_builder = self
                context._field_name = "selectors"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceSelector.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("selectors", value)

    class BuilderContext(BuilderContextBase["DeviceClassSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceClassSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceClassSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceClassSpec", Builder]:
        return GenericListBuilder[DeviceClassSpec, DeviceClassSpec.Builder]()

    config: list[DeviceClassConfiguration] | None = None
    "Config defines configuration parameters that apply to each device that is claimed via this class. Some classses may potentially be satisfied by multiple drivers, so each instance of a vendor configuration applies to exactly one driver.\n\nThey are passed to the driver, but are not considered while allocating the claim."
    extended_resource_name: Annotated[str | None, Field(alias="extendedResourceName")] = None
    "ExtendedResourceName is the extended resource name for the devices of this class. The devices of this class can be used to satisfy a pod\u0027s extended resource requests. It has the same format as the name of a pod\u0027s extended resource. It should be unique among all the device classes in a cluster. If two device classes have the same name, then the class created later is picked to satisfy a pod\u0027s extended resource requests. If two classes are created at the same time, then the name of the class lexicographically sorted first is picked.\n\nThis is an alpha field."
    selectors: list[DeviceSelector] | None = None
    "Each selector must be satisfied by a device which is claimed via this class."


class DeviceRequestAllocationResult(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceRequestAllocationResult"]:
            return DeviceRequestAllocationResult

        def build(self) -> "DeviceRequestAllocationResult":
            return DeviceRequestAllocationResult(**self._attrs)

        def admin_access(self, value: bool | None, /) -> Self:
            "AdminAccess indicates that this device was allocated for administrative access. See the corresponding request field for a definition of mode.\n\nThis is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled."
            return self._set("admin_access", value)

        def binding_conditions(self, value: list[str] | None, /) -> Self:
            "BindingConditions contains a copy of the BindingConditions from the corresponding ResourceSlice at the time of allocation.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
            return self._set("binding_conditions", value)

        def binding_failure_conditions(self, value: list[str] | None, /) -> Self:
            "BindingFailureConditions contains a copy of the BindingFailureConditions from the corresponding ResourceSlice at the time of allocation.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
            return self._set("binding_failure_conditions", value)

        def consumed_capacity(self, value: dict[str, Quantity] | None, /) -> Self:
            "ConsumedCapacity tracks the amount of capacity consumed per device as part of the claim request. The consumed amount may differ from the requested amount: it is rounded up to the nearest valid value based on the device\u2019s requestPolicy if applicable (i.e., may not be less than the requested amount).\n\nThe total consumed capacity for each device must not exceed the DeviceCapacity\u0027s Value.\n\nThis field is populated only for devices that allow multiple allocations. All capacity entries are included, even if the consumed amount is zero."
            return self._set("consumed_capacity", value)

        def device(self, value: str, /) -> Self:
            "Device references one device instance via its name in the driver\u0027s resource pool. It must be a DNS label."
            return self._set("device", value)

        def driver(self, value: str, /) -> Self:
            "Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
            return self._set("driver", value)

        def pool(self, value: str, /) -> Self:
            "This name together with the driver name and the device name field identify which device was allocated (`\u003cdriver name\u003e/\u003cpool name\u003e/\u003cdevice name\u003e`).\n\nMust not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes."
            return self._set("pool", value)

        def request(self, value: str, /) -> Self:
            "Request is the name of the request in the claim which caused this device to be allocated. If it references a subrequest in the firstAvailable list on a DeviceRequest, this field must include both the name of the main request and the subrequest using the format \u003cmain request\u003e/\u003csubrequest\u003e.\n\nMultiple devices may have been allocated per request."
            return self._set("request", value)

        def share_id(self, value: str | None, /) -> Self:
            "ShareID uniquely identifies an individual allocation share of the device, used when the device supports multiple simultaneous allocations. It serves as an additional map key to differentiate concurrent shares of the same device."
            return self._set("share_id", value)

        @overload
        def tolerations(
            self, value_or_callback: list[DeviceToleration] | None, /
        ) -> "DeviceRequestAllocationResult.Builder": ...

        @overload
        def tolerations(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceToleration, DeviceToleration.Builder]],
                GenericListBuilder[DeviceToleration, DeviceToleration.Builder]
                | list[DeviceToleration],
            ],
            /,
        ) -> "DeviceRequestAllocationResult.Builder": ...

        @overload
        def tolerations(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceToleration.Builder]: ...

        def tolerations(self, value_or_callback=UNSET, /):
            "A copy of all tolerations specified in the request at the time when the device got allocated.\n\nThe maximum number of tolerations is 16.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceToleration.Builder]()
                context._parent_builder = self
                context._field_name = "tolerations"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceToleration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tolerations", value)

    class BuilderContext(BuilderContextBase["DeviceRequestAllocationResult.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceRequestAllocationResult.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceRequestAllocationResult."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["DeviceRequestAllocationResult", Builder]:
        return GenericListBuilder[
            DeviceRequestAllocationResult, DeviceRequestAllocationResult.Builder
        ]()

    admin_access: Annotated[bool | None, Field(alias="adminAccess")] = None
    "AdminAccess indicates that this device was allocated for administrative access. See the corresponding request field for a definition of mode.\n\nThis is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled."
    binding_conditions: Annotated[list[str] | None, Field(alias="bindingConditions")] = None
    "BindingConditions contains a copy of the BindingConditions from the corresponding ResourceSlice at the time of allocation.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
    binding_failure_conditions: Annotated[
        list[str] | None, Field(alias="bindingFailureConditions")
    ] = None
    "BindingFailureConditions contains a copy of the BindingFailureConditions from the corresponding ResourceSlice at the time of allocation.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
    consumed_capacity: Annotated[dict[str, Quantity] | None, Field(alias="consumedCapacity")] = None
    "ConsumedCapacity tracks the amount of capacity consumed per device as part of the claim request. The consumed amount may differ from the requested amount: it is rounded up to the nearest valid value based on the device\u2019s requestPolicy if applicable (i.e., may not be less than the requested amount).\n\nThe total consumed capacity for each device must not exceed the DeviceCapacity\u0027s Value.\n\nThis field is populated only for devices that allow multiple allocations. All capacity entries are included, even if the consumed amount is zero."
    device: str
    "Device references one device instance via its name in the driver\u0027s resource pool. It must be a DNS label."
    driver: str
    "Driver specifies the name of the DRA driver whose kubelet plugin should be invoked to process the allocation once the claim is needed on a node.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters."
    pool: str
    "This name together with the driver name and the device name field identify which device was allocated (`\u003cdriver name\u003e/\u003cpool name\u003e/\u003cdevice name\u003e`).\n\nMust not be longer than 253 characters and may contain one or more DNS sub-domains separated by slashes."
    request: str
    "Request is the name of the request in the claim which caused this device to be allocated. If it references a subrequest in the firstAvailable list on a DeviceRequest, this field must include both the name of the main request and the subrequest using the format \u003cmain request\u003e/\u003csubrequest\u003e.\n\nMultiple devices may have been allocated per request."
    share_id: Annotated[str | None, Field(alias="shareID")] = None
    "ShareID uniquely identifies an individual allocation share of the device, used when the device supports multiple simultaneous allocations. It serves as an additional map key to differentiate concurrent shares of the same device."
    tolerations: list[DeviceToleration] | None = None
    "A copy of all tolerations specified in the request at the time when the device got allocated.\n\nThe maximum number of tolerations is 16.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."


class DeviceSubRequest(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceSubRequest"]:
            return DeviceSubRequest

        def build(self) -> "DeviceSubRequest":
            return DeviceSubRequest(**self._attrs)

        def allocation_mode(self, value: str | None, /) -> Self:
            "AllocationMode and its related fields define how devices are allocated to satisfy this subrequest. Supported values are:\n\n- ExactCount: This request is for a specific number of devices.\n  This is the default. The exact number is provided in the\n  count field.\n\n- All: This subrequest is for all of the matching devices in a pool.\n  Allocation will fail if some devices are already allocated,\n  unless adminAccess is requested.\n\nIf AllocationMode is not specified, the default mode is ExactCount. If the mode is ExactCount and count is not specified, the default count is one. Any other subrequests must specify this field.\n\nMore modes may get added in the future. Clients must refuse to handle requests with unknown modes."
            return self._set("allocation_mode", value)

        @overload
        def capacity(
            self, value_or_callback: CapacityRequirements | None, /
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def capacity(
            self,
            value_or_callback: Callable[
                [CapacityRequirements.Builder],
                CapacityRequirements.Builder | CapacityRequirements,
            ],
            /,
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def capacity(
            self, value_or_callback: Never = ...
        ) -> "CapacityRequirements.BuilderContext": ...

        def capacity(self, value_or_callback=UNSET, /):
            "Capacity define resource requirements against each capacity.\n\nIf this field is unset and the device supports multiple allocations, the default value will be applied to each capacity according to requestPolicy. For the capacity that has no requestPolicy, default is the full capacity value.\n\nApplies to each device allocation. If Count \u003e 1, the request fails if there aren\u0027t enough devices that meet the requirements. If AllocationMode is set to All, the request fails if there are devices that otherwise match the request, and have this capacity, with a value \u003e= the requested amount, but which cannot be allocated to this request."
            if self._in_context and value_or_callback is UNSET:
                context = CapacityRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "capacity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CapacityRequirements.builder())
                if isinstance(output, CapacityRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("capacity", value)

        def count(self, value: int | None, /) -> Self:
            'Count is used only when the count mode is "ExactCount". Must be greater than zero. If AllocationMode is ExactCount and this field is not specified, the default is one.'
            return self._set("count", value)

        def device_class_name(self, value: str, /) -> Self:
            "DeviceClassName references a specific DeviceClass, which can define additional configuration and selectors to be inherited by this subrequest.\n\nA class is required. Which classes are available depends on the cluster.\n\nAdministrators may use this to restrict which devices may get requested by only installing classes with selectors for permitted devices. If users are free to request anything without restrictions, then administrators can create an empty DeviceClass for users to reference."
            return self._set("device_class_name", value)

        def name(self, value: str, /) -> Self:
            "Name can be used to reference this subrequest in the list of constraints or the list of configurations for the claim. References must use the format \u003cmain request\u003e/\u003csubrequest\u003e.\n\nMust be a DNS label."
            return self._set("name", value)

        @overload
        def selectors(
            self, value_or_callback: list[DeviceSelector] | None, /
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def selectors(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceSelector, DeviceSelector.Builder]],
                GenericListBuilder[DeviceSelector, DeviceSelector.Builder] | list[DeviceSelector],
            ],
            /,
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def selectors(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceSelector.Builder]: ...

        def selectors(self, value_or_callback=UNSET, /):
            "Selectors define criteria which must be satisfied by a specific device in order for that device to be considered for this subrequest. All selectors must be satisfied for a device to be considered."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceSelector.Builder]()
                context._parent_builder = self
                context._field_name = "selectors"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceSelector.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("selectors", value)

        @overload
        def tolerations(
            self, value_or_callback: list[DeviceToleration] | None, /
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def tolerations(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceToleration, DeviceToleration.Builder]],
                GenericListBuilder[DeviceToleration, DeviceToleration.Builder]
                | list[DeviceToleration],
            ],
            /,
        ) -> "DeviceSubRequest.Builder": ...

        @overload
        def tolerations(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceToleration.Builder]: ...

        def tolerations(self, value_or_callback=UNSET, /):
            "If specified, the request\u0027s tolerations.\n\nTolerations for NoSchedule are required to allocate a device which has a taint with that effect. The same applies to NoExecute.\n\nIn addition, should any of the allocated devices get tainted with NoExecute after allocation and that effect is not tolerated, then all pods consuming the ResourceClaim get deleted to evict them. The scheduler will not let new pods reserve the claim while it has these tainted devices. Once all pods are evicted, the claim will get deallocated.\n\nThe maximum number of tolerations is 16.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceToleration.Builder]()
                context._parent_builder = self
                context._field_name = "tolerations"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceToleration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tolerations", value)

    class BuilderContext(BuilderContextBase["DeviceSubRequest.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceSubRequest.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceSubRequest."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceSubRequest", Builder]:
        return GenericListBuilder[DeviceSubRequest, DeviceSubRequest.Builder]()

    allocation_mode: Annotated[str | None, Field(alias="allocationMode")] = None
    "AllocationMode and its related fields define how devices are allocated to satisfy this subrequest. Supported values are:\n\n- ExactCount: This request is for a specific number of devices.\n  This is the default. The exact number is provided in the\n  count field.\n\n- All: This subrequest is for all of the matching devices in a pool.\n  Allocation will fail if some devices are already allocated,\n  unless adminAccess is requested.\n\nIf AllocationMode is not specified, the default mode is ExactCount. If the mode is ExactCount and count is not specified, the default count is one. Any other subrequests must specify this field.\n\nMore modes may get added in the future. Clients must refuse to handle requests with unknown modes."
    capacity: CapacityRequirements | None = None
    "Capacity define resource requirements against each capacity.\n\nIf this field is unset and the device supports multiple allocations, the default value will be applied to each capacity according to requestPolicy. For the capacity that has no requestPolicy, default is the full capacity value.\n\nApplies to each device allocation. If Count \u003e 1, the request fails if there aren\u0027t enough devices that meet the requirements. If AllocationMode is set to All, the request fails if there are devices that otherwise match the request, and have this capacity, with a value \u003e= the requested amount, but which cannot be allocated to this request."
    count: int | None = None
    'Count is used only when the count mode is "ExactCount". Must be greater than zero. If AllocationMode is ExactCount and this field is not specified, the default is one.'
    device_class_name: Annotated[str, Field(alias="deviceClassName")]
    "DeviceClassName references a specific DeviceClass, which can define additional configuration and selectors to be inherited by this subrequest.\n\nA class is required. Which classes are available depends on the cluster.\n\nAdministrators may use this to restrict which devices may get requested by only installing classes with selectors for permitted devices. If users are free to request anything without restrictions, then administrators can create an empty DeviceClass for users to reference."
    name: str
    "Name can be used to reference this subrequest in the list of constraints or the list of configurations for the claim. References must use the format \u003cmain request\u003e/\u003csubrequest\u003e.\n\nMust be a DNS label."
    selectors: list[DeviceSelector] | None = None
    "Selectors define criteria which must be satisfied by a specific device in order for that device to be considered for this subrequest. All selectors must be satisfied for a device to be considered."
    tolerations: list[DeviceToleration] | None = None
    "If specified, the request\u0027s tolerations.\n\nTolerations for NoSchedule are required to allocate a device which has a taint with that effect. The same applies to NoExecute.\n\nIn addition, should any of the allocated devices get tainted with NoExecute after allocation and that effect is not tolerated, then all pods consuming the ResourceClaim get deleted to evict them. The scheduler will not let new pods reserve the claim while it has these tainted devices. Once all pods are evicted, the claim will get deallocated.\n\nThe maximum number of tolerations is 16.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."


class BasicDevice(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["BasicDevice"]:
            return BasicDevice

        def build(self) -> "BasicDevice":
            return BasicDevice(**self._attrs)

        def all_nodes(self, value: bool | None, /) -> Self:
            "AllNodes indicates that all nodes have access to the device.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
            return self._set("all_nodes", value)

        def allow_multiple_allocations(self, value: bool | None, /) -> Self:
            "AllowMultipleAllocations marks whether the device is allowed to be allocated to multiple DeviceRequests.\n\nIf AllowMultipleAllocations is set to true, the device can be allocated more than once, and all of its capacity is consumable, regardless of whether the requestPolicy is defined or not."
            return self._set("allow_multiple_allocations", value)

        def attributes(self, value: dict[str, DeviceAttribute] | None, /) -> Self:
            "Attributes defines the set of attributes for this device. The name of each attribute must be unique in that set.\n\nThe maximum number of attributes and capacities combined is 32."
            return self._set("attributes", value)

        def binding_conditions(self, value: list[str] | None, /) -> Self:
            "BindingConditions defines the conditions for proceeding with binding. All of these conditions must be set in the per-device status conditions with a value of True to proceed with binding the pod to the node while scheduling the pod.\n\nThe maximum number of binding conditions is 4.\n\nThe conditions must be a valid condition type string.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
            return self._set("binding_conditions", value)

        def binding_failure_conditions(self, value: list[str] | None, /) -> Self:
            "BindingFailureConditions defines the conditions for binding failure. They may be set in the per-device status conditions. If any is true, a binding failure occurred.\n\nThe maximum number of binding failure conditions is 4.\n\nThe conditions must be a valid condition type string.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
            return self._set("binding_failure_conditions", value)

        def binds_to_node(self, value: bool | None, /) -> Self:
            "BindsToNode indicates if the usage of an allocation involving this device has to be limited to exactly the node that was chosen when allocating the claim. If set to true, the scheduler will set the ResourceClaim.Status.Allocation.NodeSelector to match the node where the allocation was made.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
            return self._set("binds_to_node", value)

        def capacity(self, value: dict[str, DeviceCapacity] | None, /) -> Self:
            "Capacity defines the set of capacities for this device. The name of each capacity must be unique in that set.\n\nThe maximum number of attributes and capacities combined is 32."
            return self._set("capacity", value)

        @overload
        def consumes_counters(
            self, value_or_callback: list[DeviceCounterConsumption] | None, /
        ) -> "BasicDevice.Builder": ...

        @overload
        def consumes_counters(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceCounterConsumption, DeviceCounterConsumption.Builder]],
                GenericListBuilder[DeviceCounterConsumption, DeviceCounterConsumption.Builder]
                | list[DeviceCounterConsumption],
            ],
            /,
        ) -> "BasicDevice.Builder": ...

        @overload
        def consumes_counters(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceCounterConsumption.Builder]: ...

        def consumes_counters(self, value_or_callback=UNSET, /):
            "ConsumesCounters defines a list of references to sharedCounters and the set of counters that the device will consume from those counter sets.\n\nThere can only be a single entry per counterSet.\n\nThe maximum number of device counter consumptions per device is 2."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceCounterConsumption.Builder]()
                context._parent_builder = self
                context._field_name = "consumes_counters"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceCounterConsumption.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("consumes_counters", value)

        def node_name(self, value: str | None, /) -> Self:
            "NodeName identifies the node where the device is available.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
            return self._set("node_name", value)

        @overload
        def node_selector(
            self, value_or_callback: v1.NodeSelector | None, /
        ) -> "BasicDevice.Builder": ...

        @overload
        def node_selector(
            self,
            value_or_callback: Callable[
                [v1.NodeSelector.Builder], v1.NodeSelector.Builder | v1.NodeSelector
            ],
            /,
        ) -> "BasicDevice.Builder": ...

        @overload
        def node_selector(
            self, value_or_callback: Never = ...
        ) -> "v1.NodeSelector.BuilderContext": ...

        def node_selector(self, value_or_callback=UNSET, /):
            "NodeSelector defines the nodes where the device is available.\n\nMust use exactly one term.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
            if self._in_context and value_or_callback is UNSET:
                context = v1.NodeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.NodeSelector.builder())
                if isinstance(output, v1.NodeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_selector", value)

        @overload
        def taints(
            self, value_or_callback: list[DeviceTaint] | None, /
        ) -> "BasicDevice.Builder": ...

        @overload
        def taints(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceTaint, DeviceTaint.Builder]],
                GenericListBuilder[DeviceTaint, DeviceTaint.Builder] | list[DeviceTaint],
            ],
            /,
        ) -> "BasicDevice.Builder": ...

        @overload
        def taints(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceTaint.Builder]: ...

        def taints(self, value_or_callback=UNSET, /):
            "If specified, these are the driver-defined taints.\n\nThe maximum number of taints is 16. If taints are set for any device in a ResourceSlice, then the maximum number of allowed devices per ResourceSlice is 64 instead of 128.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceTaint.Builder]()
                context._parent_builder = self
                context._field_name = "taints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceTaint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("taints", value)

    class BuilderContext(BuilderContextBase["BasicDevice.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = BasicDevice.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for BasicDevice."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["BasicDevice", Builder]:
        return GenericListBuilder[BasicDevice, BasicDevice.Builder]()

    all_nodes: Annotated[bool | None, Field(alias="allNodes")] = None
    "AllNodes indicates that all nodes have access to the device.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
    allow_multiple_allocations: Annotated[bool | None, Field(alias="allowMultipleAllocations")] = (
        None
    )
    "AllowMultipleAllocations marks whether the device is allowed to be allocated to multiple DeviceRequests.\n\nIf AllowMultipleAllocations is set to true, the device can be allocated more than once, and all of its capacity is consumable, regardless of whether the requestPolicy is defined or not."
    attributes: dict[str, DeviceAttribute] | None = None
    "Attributes defines the set of attributes for this device. The name of each attribute must be unique in that set.\n\nThe maximum number of attributes and capacities combined is 32."
    binding_conditions: Annotated[list[str] | None, Field(alias="bindingConditions")] = None
    "BindingConditions defines the conditions for proceeding with binding. All of these conditions must be set in the per-device status conditions with a value of True to proceed with binding the pod to the node while scheduling the pod.\n\nThe maximum number of binding conditions is 4.\n\nThe conditions must be a valid condition type string.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
    binding_failure_conditions: Annotated[
        list[str] | None, Field(alias="bindingFailureConditions")
    ] = None
    "BindingFailureConditions defines the conditions for binding failure. They may be set in the per-device status conditions. If any is true, a binding failure occurred.\n\nThe maximum number of binding failure conditions is 4.\n\nThe conditions must be a valid condition type string.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
    binds_to_node: Annotated[bool | None, Field(alias="bindsToNode")] = None
    "BindsToNode indicates if the usage of an allocation involving this device has to be limited to exactly the node that was chosen when allocating the claim. If set to true, the scheduler will set the ResourceClaim.Status.Allocation.NodeSelector to match the node where the allocation was made.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gates."
    capacity: dict[str, DeviceCapacity] | None = None
    "Capacity defines the set of capacities for this device. The name of each capacity must be unique in that set.\n\nThe maximum number of attributes and capacities combined is 32."
    consumes_counters: Annotated[
        list[DeviceCounterConsumption] | None, Field(alias="consumesCounters")
    ] = None
    "ConsumesCounters defines a list of references to sharedCounters and the set of counters that the device will consume from those counter sets.\n\nThere can only be a single entry per counterSet.\n\nThe maximum number of device counter consumptions per device is 2."
    node_name: Annotated[str | None, Field(alias="nodeName")] = None
    "NodeName identifies the node where the device is available.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
    node_selector: Annotated[v1.NodeSelector | None, Field(alias="nodeSelector")] = None
    "NodeSelector defines the nodes where the device is available.\n\nMust use exactly one term.\n\nMust only be set if Spec.PerDeviceNodeSelection is set to true. At most one of NodeName, NodeSelector and AllNodes can be set."
    taints: list[DeviceTaint] | None = None
    "If specified, these are the driver-defined taints.\n\nThe maximum number of taints is 16. If taints are set for any device in a ResourceSlice, then the maximum number of allowed devices per ResourceSlice is 64 instead of 128.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."


class Device(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Device"]:
            return Device

        def build(self) -> "Device":
            return Device(**self._attrs)

        @overload
        def basic(self, value_or_callback: BasicDevice | None, /) -> "Device.Builder": ...

        @overload
        def basic(
            self,
            value_or_callback: Callable[[BasicDevice.Builder], BasicDevice.Builder | BasicDevice],
            /,
        ) -> "Device.Builder": ...

        @overload
        def basic(self, value_or_callback: Never = ...) -> "BasicDevice.BuilderContext": ...

        def basic(self, value_or_callback=UNSET, /):
            "Basic defines one device instance."
            if self._in_context and value_or_callback is UNSET:
                context = BasicDevice.BuilderContext()
                context._parent_builder = self
                context._field_name = "basic"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(BasicDevice.builder())
                if isinstance(output, BasicDevice.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("basic", value)

        def name(self, value: str, /) -> Self:
            "Name is unique identifier among all devices managed by the driver in the pool. It must be a DNS label."
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["Device.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Device.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Device."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Device", Builder]:
        return GenericListBuilder[Device, Device.Builder]()

    basic: BasicDevice | None = None
    "Basic defines one device instance."
    name: str
    "Name is unique identifier among all devices managed by the driver in the pool. It must be a DNS label."


class DeviceAllocationResult(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceAllocationResult"]:
            return DeviceAllocationResult

        def build(self) -> "DeviceAllocationResult":
            return DeviceAllocationResult(**self._attrs)

        @overload
        def config(
            self, value_or_callback: list[DeviceAllocationConfiguration] | None, /
        ) -> "DeviceAllocationResult.Builder": ...

        @overload
        def config(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        DeviceAllocationConfiguration,
                        DeviceAllocationConfiguration.Builder,
                    ]
                ],
                GenericListBuilder[
                    DeviceAllocationConfiguration, DeviceAllocationConfiguration.Builder
                ]
                | list[DeviceAllocationConfiguration],
            ],
            /,
        ) -> "DeviceAllocationResult.Builder": ...

        @overload
        def config(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceAllocationConfiguration.Builder]: ...

        def config(self, value_or_callback=UNSET, /):
            "This field is a combination of all the claim and class configuration parameters. Drivers can distinguish between those based on a flag.\n\nThis includes configuration parameters for drivers which have no allocated devices in the result because it is up to the drivers which configuration parameters they support. They can silently ignore unknown configuration parameters."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceAllocationConfiguration.Builder]()
                context._parent_builder = self
                context._field_name = "config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceAllocationConfiguration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("config", value)

        @overload
        def results(
            self, value_or_callback: list[DeviceRequestAllocationResult] | None, /
        ) -> "DeviceAllocationResult.Builder": ...

        @overload
        def results(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        DeviceRequestAllocationResult,
                        DeviceRequestAllocationResult.Builder,
                    ]
                ],
                GenericListBuilder[
                    DeviceRequestAllocationResult, DeviceRequestAllocationResult.Builder
                ]
                | list[DeviceRequestAllocationResult],
            ],
            /,
        ) -> "DeviceAllocationResult.Builder": ...

        @overload
        def results(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceRequestAllocationResult.Builder]: ...

        def results(self, value_or_callback=UNSET, /):
            "Results lists all allocated devices."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceRequestAllocationResult.Builder]()
                context._parent_builder = self
                context._field_name = "results"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceRequestAllocationResult.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("results", value)

    class BuilderContext(BuilderContextBase["DeviceAllocationResult.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceAllocationResult.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceAllocationResult."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceAllocationResult", Builder]:
        return GenericListBuilder[DeviceAllocationResult, DeviceAllocationResult.Builder]()

    config: list[DeviceAllocationConfiguration] | None = None
    "This field is a combination of all the claim and class configuration parameters. Drivers can distinguish between those based on a flag.\n\nThis includes configuration parameters for drivers which have no allocated devices in the result because it is up to the drivers which configuration parameters they support. They can silently ignore unknown configuration parameters."
    results: list[DeviceRequestAllocationResult] | None = None
    "Results lists all allocated devices."


class DeviceClass(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "deviceclasses",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceClass"]:
            return DeviceClass

        def build(self) -> "DeviceClass":
            return DeviceClass(**self._attrs)

        def api_version(self, value: Literal["resource.k8s.io/v1beta1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["DeviceClass"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "DeviceClass.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "DeviceClass.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: DeviceClassSpec, /) -> "DeviceClass.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [DeviceClassSpec.Builder], DeviceClassSpec.Builder | DeviceClassSpec
            ],
            /,
        ) -> "DeviceClass.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "DeviceClassSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec defines what can be allocated and how to configure it.\n\nThis is mutable. Consumers have to be prepared for classes changing at any time, either because they get updated or replaced. Claim allocations are done once based on whatever was set in classes at the time of allocation.\n\nChanging the spec automatically increments the metadata.generation number."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceClassSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceClassSpec.builder())
                if isinstance(output, DeviceClassSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["DeviceClass.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceClass.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceClass."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceClass", Builder]:
        return GenericListBuilder[DeviceClass, DeviceClass.Builder]()

    api_version: Annotated[Literal["resource.k8s.io/v1beta1"], Field(alias="apiVersion")] = (
        "resource.k8s.io/v1beta1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["DeviceClass"] = "DeviceClass"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object metadata"
    spec: DeviceClassSpec
    "Spec defines what can be allocated and how to configure it.\n\nThis is mutable. Consumers have to be prepared for classes changing at any time, either because they get updated or replaced. Claim allocations are done once based on whatever was set in classes at the time of allocation.\n\nChanging the spec automatically increments the metadata.generation number."


DeviceClassList = ResourceList[DeviceClass]


class DeviceRequest(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceRequest"]:
            return DeviceRequest

        def build(self) -> "DeviceRequest":
            return DeviceRequest(**self._attrs)

        def admin_access(self, value: bool | None, /) -> Self:
            "AdminAccess indicates that this is a claim for administrative access to the device(s). Claims with AdminAccess are expected to be used for monitoring or other management services for a device.  They ignore all ordinary claims to the device with respect to access modes and any resource allocations.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nThis is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled."
            return self._set("admin_access", value)

        def allocation_mode(self, value: str | None, /) -> Self:
            "AllocationMode and its related fields define how devices are allocated to satisfy this request. Supported values are:\n\n- ExactCount: This request is for a specific number of devices.\n  This is the default. The exact number is provided in the\n  count field.\n\n- All: This request is for all of the matching devices in a pool.\n  At least one device must exist on the node for the allocation to succeed.\n  Allocation will fail if some devices are already allocated,\n  unless adminAccess is requested.\n\nIf AllocationMode is not specified, the default mode is ExactCount. If the mode is ExactCount and count is not specified, the default count is one. Any other requests must specify this field.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nMore modes may get added in the future. Clients must refuse to handle requests with unknown modes."
            return self._set("allocation_mode", value)

        @overload
        def capacity(
            self, value_or_callback: CapacityRequirements | None, /
        ) -> "DeviceRequest.Builder": ...

        @overload
        def capacity(
            self,
            value_or_callback: Callable[
                [CapacityRequirements.Builder],
                CapacityRequirements.Builder | CapacityRequirements,
            ],
            /,
        ) -> "DeviceRequest.Builder": ...

        @overload
        def capacity(
            self, value_or_callback: Never = ...
        ) -> "CapacityRequirements.BuilderContext": ...

        def capacity(self, value_or_callback=UNSET, /):
            "Capacity define resource requirements against each capacity.\n\nIf this field is unset and the device supports multiple allocations, the default value will be applied to each capacity according to requestPolicy. For the capacity that has no requestPolicy, default is the full capacity value.\n\nApplies to each device allocation. If Count \u003e 1, the request fails if there aren\u0027t enough devices that meet the requirements. If AllocationMode is set to All, the request fails if there are devices that otherwise match the request, and have this capacity, with a value \u003e= the requested amount, but which cannot be allocated to this request."
            if self._in_context and value_or_callback is UNSET:
                context = CapacityRequirements.BuilderContext()
                context._parent_builder = self
                context._field_name = "capacity"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CapacityRequirements.builder())
                if isinstance(output, CapacityRequirements.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("capacity", value)

        def count(self, value: int | None, /) -> Self:
            'Count is used only when the count mode is "ExactCount". Must be greater than zero. If AllocationMode is ExactCount and this field is not specified, the default is one.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.'
            return self._set("count", value)

        def device_class_name(self, value: str | None, /) -> Self:
            "DeviceClassName references a specific DeviceClass, which can define additional configuration and selectors to be inherited by this request.\n\nA class is required if no subrequests are specified in the firstAvailable list and no class can be set if subrequests are specified in the firstAvailable list. Which classes are available depends on the cluster.\n\nAdministrators may use this to restrict which devices may get requested by only installing classes with selectors for permitted devices. If users are free to request anything without restrictions, then administrators can create an empty DeviceClass for users to reference."
            return self._set("device_class_name", value)

        @overload
        def first_available(
            self, value_or_callback: list[DeviceSubRequest] | None, /
        ) -> "DeviceRequest.Builder": ...

        @overload
        def first_available(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceSubRequest, DeviceSubRequest.Builder]],
                GenericListBuilder[DeviceSubRequest, DeviceSubRequest.Builder]
                | list[DeviceSubRequest],
            ],
            /,
        ) -> "DeviceRequest.Builder": ...

        @overload
        def first_available(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceSubRequest.Builder]: ...

        def first_available(self, value_or_callback=UNSET, /):
            "FirstAvailable contains subrequests, of which exactly one will be satisfied by the scheduler to satisfy this request. It tries to satisfy them in the order in which they are listed here. So if there are two entries in the list, the scheduler will only check the second one if it determines that the first one cannot be used.\n\nThis field may only be set in the entries of DeviceClaim.Requests.\n\nDRA does not yet implement scoring, so the scheduler will select the first set of devices that satisfies all the requests in the claim. And if the requirements can be satisfied on more than one node, other scheduling features will determine which node is chosen. This means that the set of devices allocated to a claim might not be the optimal set available to the cluster. Scoring will be implemented later."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceSubRequest.Builder]()
                context._parent_builder = self
                context._field_name = "first_available"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceSubRequest.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("first_available", value)

        def name(self, value: str, /) -> Self:
            "Name can be used to reference this request in a pod.spec.containers[].resources.claims entry and in a constraint of the claim.\n\nMust be a DNS label and unique among all DeviceRequests in a ResourceClaim."
            return self._set("name", value)

        @overload
        def selectors(
            self, value_or_callback: list[DeviceSelector] | None, /
        ) -> "DeviceRequest.Builder": ...

        @overload
        def selectors(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceSelector, DeviceSelector.Builder]],
                GenericListBuilder[DeviceSelector, DeviceSelector.Builder] | list[DeviceSelector],
            ],
            /,
        ) -> "DeviceRequest.Builder": ...

        @overload
        def selectors(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceSelector.Builder]: ...

        def selectors(self, value_or_callback=UNSET, /):
            "Selectors define criteria which must be satisfied by a specific device in order for that device to be considered for this request. All selectors must be satisfied for a device to be considered.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceSelector.Builder]()
                context._parent_builder = self
                context._field_name = "selectors"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceSelector.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("selectors", value)

        @overload
        def tolerations(
            self, value_or_callback: list[DeviceToleration] | None, /
        ) -> "DeviceRequest.Builder": ...

        @overload
        def tolerations(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceToleration, DeviceToleration.Builder]],
                GenericListBuilder[DeviceToleration, DeviceToleration.Builder]
                | list[DeviceToleration],
            ],
            /,
        ) -> "DeviceRequest.Builder": ...

        @overload
        def tolerations(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceToleration.Builder]: ...

        def tolerations(self, value_or_callback=UNSET, /):
            "If specified, the request\u0027s tolerations.\n\nTolerations for NoSchedule are required to allocate a device which has a taint with that effect. The same applies to NoExecute.\n\nIn addition, should any of the allocated devices get tainted with NoExecute after allocation and that effect is not tolerated, then all pods consuming the ResourceClaim get deleted to evict them. The scheduler will not let new pods reserve the claim while it has these tainted devices. Once all pods are evicted, the claim will get deallocated.\n\nThe maximum number of tolerations is 16.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceToleration.Builder]()
                context._parent_builder = self
                context._field_name = "tolerations"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceToleration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tolerations", value)

    class BuilderContext(BuilderContextBase["DeviceRequest.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceRequest.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceRequest."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceRequest", Builder]:
        return GenericListBuilder[DeviceRequest, DeviceRequest.Builder]()

    admin_access: Annotated[bool | None, Field(alias="adminAccess")] = None
    "AdminAccess indicates that this is a claim for administrative access to the device(s). Claims with AdminAccess are expected to be used for monitoring or other management services for a device.  They ignore all ordinary claims to the device with respect to access modes and any resource allocations.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nThis is an alpha field and requires enabling the DRAAdminAccess feature gate. Admin access is disabled if this field is unset or set to false, otherwise it is enabled."
    allocation_mode: Annotated[str | None, Field(alias="allocationMode")] = None
    "AllocationMode and its related fields define how devices are allocated to satisfy this request. Supported values are:\n\n- ExactCount: This request is for a specific number of devices.\n  This is the default. The exact number is provided in the\n  count field.\n\n- All: This request is for all of the matching devices in a pool.\n  At least one device must exist on the node for the allocation to succeed.\n  Allocation will fail if some devices are already allocated,\n  unless adminAccess is requested.\n\nIf AllocationMode is not specified, the default mode is ExactCount. If the mode is ExactCount and count is not specified, the default count is one. Any other requests must specify this field.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nMore modes may get added in the future. Clients must refuse to handle requests with unknown modes."
    capacity: CapacityRequirements | None = None
    "Capacity define resource requirements against each capacity.\n\nIf this field is unset and the device supports multiple allocations, the default value will be applied to each capacity according to requestPolicy. For the capacity that has no requestPolicy, default is the full capacity value.\n\nApplies to each device allocation. If Count \u003e 1, the request fails if there aren\u0027t enough devices that meet the requirements. If AllocationMode is set to All, the request fails if there are devices that otherwise match the request, and have this capacity, with a value \u003e= the requested amount, but which cannot be allocated to this request."
    count: int | None = None
    'Count is used only when the count mode is "ExactCount". Must be greater than zero. If AllocationMode is ExactCount and this field is not specified, the default is one.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.'
    device_class_name: Annotated[str | None, Field(alias="deviceClassName")] = None
    "DeviceClassName references a specific DeviceClass, which can define additional configuration and selectors to be inherited by this request.\n\nA class is required if no subrequests are specified in the firstAvailable list and no class can be set if subrequests are specified in the firstAvailable list. Which classes are available depends on the cluster.\n\nAdministrators may use this to restrict which devices may get requested by only installing classes with selectors for permitted devices. If users are free to request anything without restrictions, then administrators can create an empty DeviceClass for users to reference."
    first_available: Annotated[list[DeviceSubRequest] | None, Field(alias="firstAvailable")] = None
    "FirstAvailable contains subrequests, of which exactly one will be satisfied by the scheduler to satisfy this request. It tries to satisfy them in the order in which they are listed here. So if there are two entries in the list, the scheduler will only check the second one if it determines that the first one cannot be used.\n\nThis field may only be set in the entries of DeviceClaim.Requests.\n\nDRA does not yet implement scoring, so the scheduler will select the first set of devices that satisfies all the requests in the claim. And if the requirements can be satisfied on more than one node, other scheduling features will determine which node is chosen. This means that the set of devices allocated to a claim might not be the optimal set available to the cluster. Scoring will be implemented later."
    name: str
    "Name can be used to reference this request in a pod.spec.containers[].resources.claims entry and in a constraint of the claim.\n\nMust be a DNS label and unique among all DeviceRequests in a ResourceClaim."
    selectors: list[DeviceSelector] | None = None
    "Selectors define criteria which must be satisfied by a specific device in order for that device to be considered for this request. All selectors must be satisfied for a device to be considered.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list."
    tolerations: list[DeviceToleration] | None = None
    "If specified, the request\u0027s tolerations.\n\nTolerations for NoSchedule are required to allocate a device which has a taint with that effect. The same applies to NoExecute.\n\nIn addition, should any of the allocated devices get tainted with NoExecute after allocation and that effect is not tolerated, then all pods consuming the ResourceClaim get deleted to evict them. The scheduler will not let new pods reserve the claim while it has these tainted devices. Once all pods are evicted, the claim will get deallocated.\n\nThe maximum number of tolerations is 16.\n\nThis field can only be set when deviceClassName is set and no subrequests are specified in the firstAvailable list.\n\nThis is an alpha field and requires enabling the DRADeviceTaints feature gate."


class ResourceSliceSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceSliceSpec"]:
            return ResourceSliceSpec

        def build(self) -> "ResourceSliceSpec":
            return ResourceSliceSpec(**self._attrs)

        def all_nodes(self, value: bool | None, /) -> Self:
            "AllNodes indicates that all nodes have access to the resources in the pool.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
            return self._set("all_nodes", value)

        @overload
        def devices(
            self, value_or_callback: list[Device] | None, /
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def devices(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Device, Device.Builder]],
                GenericListBuilder[Device, Device.Builder] | list[Device],
            ],
            /,
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def devices(self, value_or_callback: Never = ...) -> ListBuilderContext[Device.Builder]: ...

        def devices(self, value_or_callback=UNSET, /):
            "Devices lists some or all of the devices in this pool.\n\nMust not have more than 128 entries. If any device uses taints or consumes counters the limit is 64.\n\nOnly one of Devices and SharedCounters can be set in a ResourceSlice."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Device.Builder]()
                context._parent_builder = self
                context._field_name = "devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Device.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("devices", value)

        def driver(self, value: str, /) -> Self:
            "Driver identifies the DRA driver providing the capacity information. A field selector can be used to list only ResourceSlice objects with a certain driver name.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters. This field is immutable."
            return self._set("driver", value)

        def node_name(self, value: str | None, /) -> Self:
            "NodeName identifies the node which provides the resources in this pool. A field selector can be used to list only ResourceSlice objects belonging to a certain node.\n\nThis field can be used to limit access from nodes to ResourceSlices with the same node name. It also indicates to autoscalers that adding new nodes of the same type as some old node might also make new resources available.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set. This field is immutable."
            return self._set("node_name", value)

        @overload
        def node_selector(
            self, value_or_callback: v1.NodeSelector | None, /
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def node_selector(
            self,
            value_or_callback: Callable[
                [v1.NodeSelector.Builder], v1.NodeSelector.Builder | v1.NodeSelector
            ],
            /,
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def node_selector(
            self, value_or_callback: Never = ...
        ) -> "v1.NodeSelector.BuilderContext": ...

        def node_selector(self, value_or_callback=UNSET, /):
            "NodeSelector defines which nodes have access to the resources in the pool, when that pool is not limited to a single node.\n\nMust use exactly one term.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
            if self._in_context and value_or_callback is UNSET:
                context = v1.NodeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.NodeSelector.builder())
                if isinstance(output, v1.NodeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_selector", value)

        def per_device_node_selection(self, value: bool | None, /) -> Self:
            "PerDeviceNodeSelection defines whether the access from nodes to resources in the pool is set on the ResourceSlice level or on each device. If it is set to true, every device defined the ResourceSlice must specify this individually.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
            return self._set("per_device_node_selection", value)

        @overload
        def pool(self, value_or_callback: ResourcePool, /) -> "ResourceSliceSpec.Builder": ...

        @overload
        def pool(
            self,
            value_or_callback: Callable[
                [ResourcePool.Builder], ResourcePool.Builder | ResourcePool
            ],
            /,
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def pool(self, value_or_callback: Never = ...) -> "ResourcePool.BuilderContext": ...

        def pool(self, value_or_callback=UNSET, /):
            "Pool describes the pool that this ResourceSlice belongs to."
            if self._in_context and value_or_callback is UNSET:
                context = ResourcePool.BuilderContext()
                context._parent_builder = self
                context._field_name = "pool"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourcePool.builder())
                if isinstance(output, ResourcePool.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pool", value)

        @overload
        def shared_counters(
            self, value_or_callback: list[CounterSet] | None, /
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def shared_counters(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[CounterSet, CounterSet.Builder]],
                GenericListBuilder[CounterSet, CounterSet.Builder] | list[CounterSet],
            ],
            /,
        ) -> "ResourceSliceSpec.Builder": ...

        @overload
        def shared_counters(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CounterSet.Builder]: ...

        def shared_counters(self, value_or_callback=UNSET, /):
            "SharedCounters defines a list of counter sets, each of which has a name and a list of counters available.\n\nThe names of the counter sets must be unique in the ResourcePool.\n\nOnly one of Devices and SharedCounters can be set in a ResourceSlice.\n\nThe maximum number of counter sets is 8."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CounterSet.Builder]()
                context._parent_builder = self
                context._field_name = "shared_counters"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CounterSet.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("shared_counters", value)

    class BuilderContext(BuilderContextBase["ResourceSliceSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceSliceSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceSliceSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceSliceSpec", Builder]:
        return GenericListBuilder[ResourceSliceSpec, ResourceSliceSpec.Builder]()

    all_nodes: Annotated[bool | None, Field(alias="allNodes")] = None
    "AllNodes indicates that all nodes have access to the resources in the pool.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
    devices: list[Device] | None = None
    "Devices lists some or all of the devices in this pool.\n\nMust not have more than 128 entries. If any device uses taints or consumes counters the limit is 64.\n\nOnly one of Devices and SharedCounters can be set in a ResourceSlice."
    driver: str
    "Driver identifies the DRA driver providing the capacity information. A field selector can be used to list only ResourceSlice objects with a certain driver name.\n\nMust be a DNS subdomain and should end with a DNS domain owned by the vendor of the driver. It should use only lower case characters. This field is immutable."
    node_name: Annotated[str | None, Field(alias="nodeName")] = None
    "NodeName identifies the node which provides the resources in this pool. A field selector can be used to list only ResourceSlice objects belonging to a certain node.\n\nThis field can be used to limit access from nodes to ResourceSlices with the same node name. It also indicates to autoscalers that adding new nodes of the same type as some old node might also make new resources available.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set. This field is immutable."
    node_selector: Annotated[v1.NodeSelector | None, Field(alias="nodeSelector")] = None
    "NodeSelector defines which nodes have access to the resources in the pool, when that pool is not limited to a single node.\n\nMust use exactly one term.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
    per_device_node_selection: Annotated[bool | None, Field(alias="perDeviceNodeSelection")] = None
    "PerDeviceNodeSelection defines whether the access from nodes to resources in the pool is set on the ResourceSlice level or on each device. If it is set to true, every device defined the ResourceSlice must specify this individually.\n\nExactly one of NodeName, NodeSelector, AllNodes, and PerDeviceNodeSelection must be set."
    pool: ResourcePool
    "Pool describes the pool that this ResourceSlice belongs to."
    shared_counters: Annotated[list[CounterSet] | None, Field(alias="sharedCounters")] = None
    "SharedCounters defines a list of counter sets, each of which has a name and a list of counters available.\n\nThe names of the counter sets must be unique in the ResourcePool.\n\nOnly one of Devices and SharedCounters can be set in a ResourceSlice.\n\nThe maximum number of counter sets is 8."


class AllocationResult(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["AllocationResult"]:
            return AllocationResult

        def build(self) -> "AllocationResult":
            return AllocationResult(**self._attrs)

        @overload
        def allocation_timestamp(
            self, value_or_callback: Time | None, /
        ) -> "AllocationResult.Builder": ...

        @overload
        def allocation_timestamp(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "AllocationResult.Builder": ...

        @overload
        def allocation_timestamp(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def allocation_timestamp(self, value_or_callback=UNSET, /):
            "AllocationTimestamp stores the time when the resources were allocated. This field is not guaranteed to be set, in which case that time is unknown.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gate."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "allocation_timestamp"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("allocation_timestamp", value)

        @overload
        def devices(
            self, value_or_callback: DeviceAllocationResult | None, /
        ) -> "AllocationResult.Builder": ...

        @overload
        def devices(
            self,
            value_or_callback: Callable[
                [DeviceAllocationResult.Builder],
                DeviceAllocationResult.Builder | DeviceAllocationResult,
            ],
            /,
        ) -> "AllocationResult.Builder": ...

        @overload
        def devices(
            self, value_or_callback: Never = ...
        ) -> "DeviceAllocationResult.BuilderContext": ...

        def devices(self, value_or_callback=UNSET, /):
            "Devices is the result of allocating devices."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceAllocationResult.BuilderContext()
                context._parent_builder = self
                context._field_name = "devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceAllocationResult.builder())
                if isinstance(output, DeviceAllocationResult.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("devices", value)

        @overload
        def node_selector(
            self, value_or_callback: v1.NodeSelector | None, /
        ) -> "AllocationResult.Builder": ...

        @overload
        def node_selector(
            self,
            value_or_callback: Callable[
                [v1.NodeSelector.Builder], v1.NodeSelector.Builder | v1.NodeSelector
            ],
            /,
        ) -> "AllocationResult.Builder": ...

        @overload
        def node_selector(
            self, value_or_callback: Never = ...
        ) -> "v1.NodeSelector.BuilderContext": ...

        def node_selector(self, value_or_callback=UNSET, /):
            "NodeSelector defines where the allocated resources are available. If unset, they are available everywhere."
            if self._in_context and value_or_callback is UNSET:
                context = v1.NodeSelector.BuilderContext()
                context._parent_builder = self
                context._field_name = "node_selector"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.NodeSelector.builder())
                if isinstance(output, v1.NodeSelector.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("node_selector", value)

    class BuilderContext(BuilderContextBase["AllocationResult.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = AllocationResult.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for AllocationResult."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["AllocationResult", Builder]:
        return GenericListBuilder[AllocationResult, AllocationResult.Builder]()

    allocation_timestamp: Annotated[Time | None, Field(alias="allocationTimestamp")] = None
    "AllocationTimestamp stores the time when the resources were allocated. This field is not guaranteed to be set, in which case that time is unknown.\n\nThis is an alpha field and requires enabling the DRADeviceBindingConditions and DRAResourceClaimDeviceStatus feature gate."
    devices: DeviceAllocationResult | None = None
    "Devices is the result of allocating devices."
    node_selector: Annotated[v1.NodeSelector | None, Field(alias="nodeSelector")] = None
    "NodeSelector defines where the allocated resources are available. If unset, they are available everywhere."


class DeviceClaim(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["DeviceClaim"]:
            return DeviceClaim

        def build(self) -> "DeviceClaim":
            return DeviceClaim(**self._attrs)

        @overload
        def config(
            self, value_or_callback: list[DeviceClaimConfiguration] | None, /
        ) -> "DeviceClaim.Builder": ...

        @overload
        def config(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceClaimConfiguration, DeviceClaimConfiguration.Builder]],
                GenericListBuilder[DeviceClaimConfiguration, DeviceClaimConfiguration.Builder]
                | list[DeviceClaimConfiguration],
            ],
            /,
        ) -> "DeviceClaim.Builder": ...

        @overload
        def config(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceClaimConfiguration.Builder]: ...

        def config(self, value_or_callback=UNSET, /):
            "This field holds configuration for multiple potential drivers which could satisfy requests in this claim. It is ignored while allocating the claim."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceClaimConfiguration.Builder]()
                context._parent_builder = self
                context._field_name = "config"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceClaimConfiguration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("config", value)

        @overload
        def constraints(
            self, value_or_callback: list[DeviceConstraint] | None, /
        ) -> "DeviceClaim.Builder": ...

        @overload
        def constraints(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceConstraint, DeviceConstraint.Builder]],
                GenericListBuilder[DeviceConstraint, DeviceConstraint.Builder]
                | list[DeviceConstraint],
            ],
            /,
        ) -> "DeviceClaim.Builder": ...

        @overload
        def constraints(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceConstraint.Builder]: ...

        def constraints(self, value_or_callback=UNSET, /):
            "These constraints must be satisfied by the set of devices that get allocated for the claim."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceConstraint.Builder]()
                context._parent_builder = self
                context._field_name = "constraints"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceConstraint.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("constraints", value)

        @overload
        def requests(
            self, value_or_callback: list[DeviceRequest] | None, /
        ) -> "DeviceClaim.Builder": ...

        @overload
        def requests(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[DeviceRequest, DeviceRequest.Builder]],
                GenericListBuilder[DeviceRequest, DeviceRequest.Builder] | list[DeviceRequest],
            ],
            /,
        ) -> "DeviceClaim.Builder": ...

        @overload
        def requests(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[DeviceRequest.Builder]: ...

        def requests(self, value_or_callback=UNSET, /):
            "Requests represent individual requests for distinct devices which must all be satisfied. If empty, nothing needs to be allocated."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[DeviceRequest.Builder]()
                context._parent_builder = self
                context._field_name = "requests"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceRequest.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("requests", value)

    class BuilderContext(BuilderContextBase["DeviceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = DeviceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for DeviceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["DeviceClaim", Builder]:
        return GenericListBuilder[DeviceClaim, DeviceClaim.Builder]()

    config: list[DeviceClaimConfiguration] | None = None
    "This field holds configuration for multiple potential drivers which could satisfy requests in this claim. It is ignored while allocating the claim."
    constraints: list[DeviceConstraint] | None = None
    "These constraints must be satisfied by the set of devices that get allocated for the claim."
    requests: list[DeviceRequest] | None = None
    "Requests represent individual requests for distinct devices which must all be satisfied. If empty, nothing needs to be allocated."


class ResourceClaimSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaimSpec"]:
            return ResourceClaimSpec

        def build(self) -> "ResourceClaimSpec":
            return ResourceClaimSpec(**self._attrs)

        @overload
        def devices(
            self, value_or_callback: DeviceClaim | None, /
        ) -> "ResourceClaimSpec.Builder": ...

        @overload
        def devices(
            self,
            value_or_callback: Callable[[DeviceClaim.Builder], DeviceClaim.Builder | DeviceClaim],
            /,
        ) -> "ResourceClaimSpec.Builder": ...

        @overload
        def devices(self, value_or_callback: Never = ...) -> "DeviceClaim.BuilderContext": ...

        def devices(self, value_or_callback=UNSET, /):
            "Devices defines how to request devices."
            if self._in_context and value_or_callback is UNSET:
                context = DeviceClaim.BuilderContext()
                context._parent_builder = self
                context._field_name = "devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(DeviceClaim.builder())
                if isinstance(output, DeviceClaim.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("devices", value)

    class BuilderContext(BuilderContextBase["ResourceClaimSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaimSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaimSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaimSpec", Builder]:
        return GenericListBuilder[ResourceClaimSpec, ResourceClaimSpec.Builder]()

    devices: DeviceClaim | None = None
    "Devices defines how to request devices."


class ResourceClaimStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaimStatus"]:
            return ResourceClaimStatus

        def build(self) -> "ResourceClaimStatus":
            return ResourceClaimStatus(**self._attrs)

        @overload
        def allocation(
            self, value_or_callback: AllocationResult | None, /
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def allocation(
            self,
            value_or_callback: Callable[
                [AllocationResult.Builder], AllocationResult.Builder | AllocationResult
            ],
            /,
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def allocation(
            self, value_or_callback: Never = ...
        ) -> "AllocationResult.BuilderContext": ...

        def allocation(self, value_or_callback=UNSET, /):
            "Allocation is set once the claim has been allocated successfully."
            if self._in_context and value_or_callback is UNSET:
                context = AllocationResult.BuilderContext()
                context._parent_builder = self
                context._field_name = "allocation"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AllocationResult.builder())
                if isinstance(output, AllocationResult.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("allocation", value)

        @overload
        def devices(
            self, value_or_callback: list[AllocatedDeviceStatus] | None, /
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def devices(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[AllocatedDeviceStatus, AllocatedDeviceStatus.Builder]],
                GenericListBuilder[AllocatedDeviceStatus, AllocatedDeviceStatus.Builder]
                | list[AllocatedDeviceStatus],
            ],
            /,
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def devices(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[AllocatedDeviceStatus.Builder]: ...

        def devices(self, value_or_callback=UNSET, /):
            "Devices contains the status of each device allocated for this claim, as reported by the driver. This can include driver-specific information. Entries are owned by their respective drivers."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[AllocatedDeviceStatus.Builder]()
                context._parent_builder = self
                context._field_name = "devices"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(AllocatedDeviceStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("devices", value)

        @overload
        def reserved_for(
            self, value_or_callback: list[ResourceClaimConsumerReference] | None, /
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def reserved_for(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        ResourceClaimConsumerReference,
                        ResourceClaimConsumerReference.Builder,
                    ]
                ],
                GenericListBuilder[
                    ResourceClaimConsumerReference,
                    ResourceClaimConsumerReference.Builder,
                ]
                | list[ResourceClaimConsumerReference],
            ],
            /,
        ) -> "ResourceClaimStatus.Builder": ...

        @overload
        def reserved_for(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ResourceClaimConsumerReference.Builder]: ...

        def reserved_for(self, value_or_callback=UNSET, /):
            "ReservedFor indicates which entities are currently allowed to use the claim. A Pod which references a ResourceClaim which is not reserved for that Pod will not be started. A claim that is in use or might be in use because it has been reserved must not get deallocated.\n\nIn a cluster with multiple scheduler instances, two pods might get scheduled concurrently by different schedulers. When they reference the same ResourceClaim which already has reached its maximum number of consumers, only one pod can be scheduled.\n\nBoth schedulers try to add their pod to the claim.status.reservedFor field, but only the update that reaches the API server first gets stored. The other one fails with an error and the scheduler which issued it knows that it must put the pod back into the queue, waiting for the ResourceClaim to become usable again.\n\nThere can be at most 256 such reservations. This may get increased in the future, but not reduced."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ResourceClaimConsumerReference.Builder]()
                context._parent_builder = self
                context._field_name = "reserved_for"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaimConsumerReference.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("reserved_for", value)

    class BuilderContext(BuilderContextBase["ResourceClaimStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaimStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaimStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaimStatus", Builder]:
        return GenericListBuilder[ResourceClaimStatus, ResourceClaimStatus.Builder]()

    allocation: AllocationResult | None = None
    "Allocation is set once the claim has been allocated successfully."
    devices: list[AllocatedDeviceStatus] | None = None
    "Devices contains the status of each device allocated for this claim, as reported by the driver. This can include driver-specific information. Entries are owned by their respective drivers."
    reserved_for: Annotated[
        list[ResourceClaimConsumerReference] | None, Field(alias="reservedFor")
    ] = None
    "ReservedFor indicates which entities are currently allowed to use the claim. A Pod which references a ResourceClaim which is not reserved for that Pod will not be started. A claim that is in use or might be in use because it has been reserved must not get deallocated.\n\nIn a cluster with multiple scheduler instances, two pods might get scheduled concurrently by different schedulers. When they reference the same ResourceClaim which already has reached its maximum number of consumers, only one pod can be scheduled.\n\nBoth schedulers try to add their pod to the claim.status.reservedFor field, but only the update that reaches the API server first gets stored. The other one fails with an error and the scheduler which issued it knows that it must put the pod back into the queue, waiting for the ResourceClaim to become usable again.\n\nThere can be at most 256 such reservations. This may get increased in the future, but not reduced."


class ResourceClaimTemplateSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaimTemplateSpec"]:
            return ResourceClaimTemplateSpec

        def build(self) -> "ResourceClaimTemplateSpec":
            return ResourceClaimTemplateSpec(**self._attrs)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ResourceClaimTemplateSpec.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ResourceClaimTemplateSpec.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "ObjectMeta may contain labels and annotations that will be copied into the ResourceClaim when creating it. No other fields are allowed and will be rejected during validation."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ResourceClaimSpec, /
        ) -> "ResourceClaimTemplateSpec.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ResourceClaimSpec.Builder],
                ResourceClaimSpec.Builder | ResourceClaimSpec,
            ],
            /,
        ) -> "ResourceClaimTemplateSpec.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "ResourceClaimSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec for the ResourceClaim. The entire content is copied unchanged into the ResourceClaim that gets created from this template. The same fields as in a ResourceClaim are also valid here."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceClaimSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaimSpec.builder())
                if isinstance(output, ResourceClaimSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["ResourceClaimTemplateSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaimTemplateSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaimTemplateSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaimTemplateSpec", Builder]:
        return GenericListBuilder[ResourceClaimTemplateSpec, ResourceClaimTemplateSpec.Builder]()

    metadata: ObjectMeta | None = None
    "ObjectMeta may contain labels and annotations that will be copied into the ResourceClaim when creating it. No other fields are allowed and will be rejected during validation."
    spec: ResourceClaimSpec
    "Spec for the ResourceClaim. The entire content is copied unchanged into the ResourceClaim that gets created from this template. The same fields as in a ResourceClaim are also valid here."


class ResourceSlice(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "resourceslices",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceSlice"]:
            return ResourceSlice

        def build(self) -> "ResourceSlice":
            return ResourceSlice(**self._attrs)

        def api_version(self, value: Literal["resource.k8s.io/v1beta1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ResourceSlice"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "ResourceSlice.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ResourceSlice.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: ResourceSliceSpec, /) -> "ResourceSlice.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ResourceSliceSpec.Builder],
                ResourceSliceSpec.Builder | ResourceSliceSpec,
            ],
            /,
        ) -> "ResourceSlice.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "ResourceSliceSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Contains the information published by the driver.\n\nChanging the spec automatically increments the metadata.generation number."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceSliceSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceSliceSpec.builder())
                if isinstance(output, ResourceSliceSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["ResourceSlice.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceSlice.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceSlice."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceSlice", Builder]:
        return GenericListBuilder[ResourceSlice, ResourceSlice.Builder]()

    api_version: Annotated[Literal["resource.k8s.io/v1beta1"], Field(alias="apiVersion")] = (
        "resource.k8s.io/v1beta1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ResourceSlice"] = "ResourceSlice"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object metadata"
    spec: ResourceSliceSpec
    "Contains the information published by the driver.\n\nChanging the spec automatically increments the metadata.generation number."


ResourceSliceList = ResourceList[ResourceSlice]


class ResourceClaim(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "resourceclaims",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaim"]:
            return ResourceClaim

        def build(self) -> "ResourceClaim":
            return ResourceClaim(**self._attrs)

        def api_version(self, value: Literal["resource.k8s.io/v1beta1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ResourceClaim"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "ResourceClaim.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ResourceClaim.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: ResourceClaimSpec, /) -> "ResourceClaim.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ResourceClaimSpec.Builder],
                ResourceClaimSpec.Builder | ResourceClaimSpec,
            ],
            /,
        ) -> "ResourceClaim.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "ResourceClaimSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Spec describes what is being requested and how to configure it. The spec is immutable."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceClaimSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaimSpec.builder())
                if isinstance(output, ResourceClaimSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: ResourceClaimStatus | None, /
        ) -> "ResourceClaim.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [ResourceClaimStatus.Builder],
                ResourceClaimStatus.Builder | ResourceClaimStatus,
            ],
            /,
        ) -> "ResourceClaim.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "ResourceClaimStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "Status describes whether the claim is ready to use and what has been allocated."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceClaimStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaimStatus.builder())
                if isinstance(output, ResourceClaimStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["ResourceClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaim", Builder]:
        return GenericListBuilder[ResourceClaim, ResourceClaim.Builder]()

    api_version: Annotated[Literal["resource.k8s.io/v1beta1"], Field(alias="apiVersion")] = (
        "resource.k8s.io/v1beta1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ResourceClaim"] = "ResourceClaim"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object metadata"
    spec: ResourceClaimSpec
    "Spec describes what is being requested and how to configure it. The spec is immutable."
    status: ResourceClaimStatus | None = None
    "Status describes whether the claim is ready to use and what has been allocated."


ResourceClaimList = ResourceList[ResourceClaim]


class ResourceClaimTemplate(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "resourceclaimtemplates",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResourceClaimTemplate"]:
            return ResourceClaimTemplate

        def build(self) -> "ResourceClaimTemplate":
            return ResourceClaimTemplate(**self._attrs)

        def api_version(self, value: Literal["resource.k8s.io/v1beta1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ResourceClaimTemplate"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ResourceClaimTemplate.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ResourceClaimTemplate.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "Standard object metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ResourceClaimTemplateSpec, /
        ) -> "ResourceClaimTemplate.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ResourceClaimTemplateSpec.Builder],
                ResourceClaimTemplateSpec.Builder | ResourceClaimTemplateSpec,
            ],
            /,
        ) -> "ResourceClaimTemplate.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "ResourceClaimTemplateSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "Describes the ResourceClaim that is to be generated.\n\nThis field is immutable. A ResourceClaim will get created by the control plane for a Pod when needed and then not get updated anymore."
            if self._in_context and value_or_callback is UNSET:
                context = ResourceClaimTemplateSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResourceClaimTemplateSpec.builder())
                if isinstance(output, ResourceClaimTemplateSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["ResourceClaimTemplate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResourceClaimTemplate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResourceClaimTemplate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResourceClaimTemplate", Builder]:
        return GenericListBuilder[ResourceClaimTemplate, ResourceClaimTemplate.Builder]()

    api_version: Annotated[Literal["resource.k8s.io/v1beta1"], Field(alias="apiVersion")] = (
        "resource.k8s.io/v1beta1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ResourceClaimTemplate"] = "ResourceClaimTemplate"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "Standard object metadata"
    spec: ResourceClaimTemplateSpec
    "Describes the ResourceClaim that is to be generated.\n\nThis field is immutable. A ResourceClaim will get created by the control plane for a Pod when needed and then not get updated anymore."


ResourceClaimTemplateList = ResourceList[ResourceClaimTemplate]
