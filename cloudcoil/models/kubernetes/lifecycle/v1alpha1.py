# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    Condition,
    ObjectMeta,
    Time,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field


class EvictionPodReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionPodReference"]:
            return EvictionPodReference

        def build(self) -> "EvictionPodReference":
            return EvictionPodReference(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name of the target. This field is required."
            return self._set("name", value)

        def uid(self, value: str, /) -> Self:
            "uid of the target. It can be found in .metadata.uid of the target and is a lowercase UUID in 8-4-4-4-12 format. This field is required."
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["EvictionPodReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionPodReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionPodReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionPodReference", Builder]:
        return GenericListBuilder[EvictionPodReference, EvictionPodReference.Builder]()

    name: str
    "name of the target. This field is required."
    uid: str
    "uid of the target. It can be found in .metadata.uid of the target and is a lowercase UUID in 8-4-4-4-12 format. This field is required."


class EvictionRequestPodReference(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionRequestPodReference"]:
            return EvictionRequestPodReference

        def build(self) -> "EvictionRequestPodReference":
            return EvictionRequestPodReference(**self._attrs)

        def name(self, value: str, /) -> Self:
            "name of the target. This field is required."
            return self._set("name", value)

        def uid(self, value: str, /) -> Self:
            "uid of the target. It can be found in .metadata.uid of the target and is a lowercase UUID in 8-4-4-4-12 format. This field is required."
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["EvictionRequestPodReference.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionRequestPodReference.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionRequestPodReference."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionRequestPodReference", Builder]:
        return GenericListBuilder[
            EvictionRequestPodReference, EvictionRequestPodReference.Builder
        ]()

    name: str
    "name of the target. This field is required."
    uid: str
    "uid of the target. It can be found in .metadata.uid of the target and is a lowercase UUID in 8-4-4-4-12 format. This field is required."


class EvictionRequestStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionRequestStatus"]:
            return EvictionRequestStatus

        def build(self) -> "EvictionRequestStatus":
            return EvictionRequestStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "EvictionRequestStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "EvictionRequestStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "conditions contain information about the eviction request.\n\nEvictionRequest specific conditions are: TargetEvicted or Failed (managed by evictionrequest-controller). - Failed means that the eviction request is no longer being processed\n  by any eviction responder. This can happen if the request is canceled or if no responder\n  managed to evict the target (e.g. terminate or delete a pod).\n- TargetEvicted means that the target has been evicted (e.g. a pod has been terminated or deleted).\n\nThese conditions can be reset if the eviction was unsuccessful and a new Eviction intent has been submitted.\n\nThe maximum length of the conditions list is 100."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "observedGeneration is EvictionRequest\u0027s .metadata.generation observed by the evictionrequest-controller. The observed generation value cannot be negative and can only be incremented. The minimum value is 1. This field is managed by evictionrequest-controller."
            return self._set("observed_generation", value)

    class BuilderContext(BuilderContextBase["EvictionRequestStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionRequestStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionRequestStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionRequestStatus", Builder]:
        return GenericListBuilder[EvictionRequestStatus, EvictionRequestStatus.Builder]()

    conditions: list[Condition] | None = None
    "conditions contain information about the eviction request.\n\nEvictionRequest specific conditions are: TargetEvicted or Failed (managed by evictionrequest-controller). - Failed means that the eviction request is no longer being processed\n  by any eviction responder. This can happen if the request is canceled or if no responder\n  managed to evict the target (e.g. terminate or delete a pod).\n- TargetEvicted means that the target has been evicted (e.g. a pod has been terminated or deleted).\n\nThese conditions can be reset if the eviction was unsuccessful and a new Eviction intent has been submitted.\n\nThe maximum length of the conditions list is 100."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "observedGeneration is EvictionRequest\u0027s .metadata.generation observed by the evictionrequest-controller. The observed generation value cannot be negative and can only be incremented. The minimum value is 1. This field is managed by evictionrequest-controller."


class EvictionRequestTarget(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionRequestTarget"]:
            return EvictionRequestTarget

        def build(self) -> "EvictionRequestTarget":
            return EvictionRequestTarget(**self._attrs)

        @overload
        def pod(
            self, value_or_callback: EvictionRequestPodReference | None, /
        ) -> "EvictionRequestTarget.Builder": ...

        @overload
        def pod(
            self,
            value_or_callback: Callable[
                [EvictionRequestPodReference.Builder],
                EvictionRequestPodReference.Builder | EvictionRequestPodReference,
            ],
            /,
        ) -> "EvictionRequestTarget.Builder": ...

        @overload
        def pod(
            self, value_or_callback: Never = ...
        ) -> "EvictionRequestPodReference.BuilderContext": ...

        def pod(self, value_or_callback=UNSET, /):
            "pod references a pod that is subject to eviction/termination. Pods that are part of a PodGroup (.spec.schedulingGroup is set) are not supported."
            if self._in_context and value_or_callback is UNSET:
                context = EvictionRequestPodReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionRequestPodReference.builder())
                if isinstance(output, EvictionRequestPodReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod", value)

    class BuilderContext(BuilderContextBase["EvictionRequestTarget.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionRequestTarget.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionRequestTarget."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionRequestTarget", Builder]:
        return GenericListBuilder[EvictionRequestTarget, EvictionRequestTarget.Builder]()

    pod: EvictionRequestPodReference | None = None
    "pod references a pod that is subject to eviction/termination. Pods that are part of a PodGroup (.spec.schedulingGroup is set) are not supported."


class EvictionTarget(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionTarget"]:
            return EvictionTarget

        def build(self) -> "EvictionTarget":
            return EvictionTarget(**self._attrs)

        @overload
        def pod(
            self, value_or_callback: EvictionPodReference | None, /
        ) -> "EvictionTarget.Builder": ...

        @overload
        def pod(
            self,
            value_or_callback: Callable[
                [EvictionPodReference.Builder],
                EvictionPodReference.Builder | EvictionPodReference,
            ],
            /,
        ) -> "EvictionTarget.Builder": ...

        @overload
        def pod(self, value_or_callback: Never = ...) -> "EvictionPodReference.BuilderContext": ...

        def pod(self, value_or_callback=UNSET, /):
            "pod references a pod that is subject to eviction/termination. Pods that are part of a PodGroup (.spec.schedulingGroup is set) are not supported."
            if self._in_context and value_or_callback is UNSET:
                context = EvictionPodReference.BuilderContext()
                context._parent_builder = self
                context._field_name = "pod"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionPodReference.builder())
                if isinstance(output, EvictionPodReference.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("pod", value)

    class BuilderContext(BuilderContextBase["EvictionTarget.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionTarget.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionTarget."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionTarget", Builder]:
        return GenericListBuilder[EvictionTarget, EvictionTarget.Builder]()

    pod: EvictionPodReference | None = None
    "pod references a pod that is subject to eviction/termination. Pods that are part of a PodGroup (.spec.schedulingGroup is set) are not supported."


class Requester(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Requester"]:
            return Requester

        def build(self) -> "Requester":
            return Requester(**self._attrs)

        def intent(self, value: str, /) -> Self:
            "intent specifies the action that should be taken for the specified target.\n\n- Eviction means that the requester is interested in the eviction of the target. - Withdrawn means that the requester is no longer interested in the eviction of the target.\n  If all requesters\u0027 intents are withdrawn, the eviction will be canceled.\n  Cancellation consequences:\n  - Inactive responders will never run.\n  - Active responders are expected to cancel the eviction.\n  - Completed or Interrupted responders should not take any action."
            return self._set("intent", value)

        def name(self, value: str, /) -> Self:
            'name allows you to identify the entity, that requested the eviction of the target.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field must be unique for each requester. This field is required.'
            return self._set("name", value)

    class BuilderContext(BuilderContextBase["Requester.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Requester.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Requester."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Requester", Builder]:
        return GenericListBuilder[Requester, Requester.Builder]()

    intent: str
    "intent specifies the action that should be taken for the specified target.\n\n- Eviction means that the requester is interested in the eviction of the target. - Withdrawn means that the requester is no longer interested in the eviction of the target.\n  If all requesters\u0027 intents are withdrawn, the eviction will be canceled.\n  Cancellation consequences:\n  - Inactive responders will never run.\n  - Active responders are expected to cancel the eviction.\n  - Completed or Interrupted responders should not take any action."
    name: str
    'name allows you to identify the entity, that requested the eviction of the target.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field must be unique for each requester. This field is required.'


class ResponderStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ResponderStatus"]:
            return ResponderStatus

        def build(self) -> "ResponderStatus":
            return ResponderStatus(**self._attrs)

        @overload
        def completion_time(
            self, value_or_callback: Time | None, /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def completion_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def completion_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def completion_time(self, value_or_callback=UNSET, /):
            "completionTime tracks the time at which the Responder stopped processing the eviction request. Completion means that the responders has either fully or partially completed the eviction process, which may have resulted in target eviction (e.g. pod termination). It should reflect the present time when set. This field becomes immutable once set."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "completion_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("completion_time", value)

        @overload
        def expected_completion_time(
            self, value_or_callback: Time | None, /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def expected_completion_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def expected_completion_time(
            self, value_or_callback: Never = ...
        ) -> "Time.BuilderContext": ...

        def expected_completion_time(self, value_or_callback=UNSET, /):
            "expectedCompletionTime is the time at which the eviction process step is expected to end for the responder. The time cannot be set to the past. May be omitted if no estimate can be made."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "expected_completion_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("expected_completion_time", value)

        @overload
        def heartbeat_time(
            self, value_or_callback: Time | None, /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def heartbeat_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def heartbeat_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def heartbeat_time(self, value_or_callback=UNSET, /):
            "heartbeatTime is the last time at which the eviction process was reported to be in progress by the responder. It should reflect the present time when set. Responders should avoid heartbeats more frequent than 20 seconds to avoid overloading the control-plane."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "heartbeat_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("heartbeat_time", value)

        def message(self, value: str | None, /) -> Self:
            "message provides human-readable details about the state of the responder and the eviction process. Maximum length is 4000 characters."
            return self._set("message", value)

        def name(self, value: str, /) -> Self:
            'name allows you to identify the responder reacting to the Eviction.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field is initialized by Kubernetes and must be unique for each responder. This field is required.'
            return self._set("name", value)

        @overload
        def start_time(self, value_or_callback: Time | None, /) -> "ResponderStatus.Builder": ...

        @overload
        def start_time(
            self, value_or_callback: Callable[[Time.Builder], Time.Builder | Time], /
        ) -> "ResponderStatus.Builder": ...

        @overload
        def start_time(self, value_or_callback: Never = ...) -> "Time.BuilderContext": ...

        def start_time(self, value_or_callback=UNSET, /):
            "startTime tracks the time at which this responder was designated as active and should start processing the eviction request. It should reflect the present time when set. This field is initialized by Kubernetes when this responder becomes active. This field becomes immutable once set."
            if self._in_context and value_or_callback is UNSET:
                context = Time.BuilderContext()
                context._parent_builder = self
                context._field_name = "start_time"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Time.builder())
                if isinstance(output, Time.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("start_time", value)

    class BuilderContext(BuilderContextBase["ResponderStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ResponderStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ResponderStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ResponderStatus", Builder]:
        return GenericListBuilder[ResponderStatus, ResponderStatus.Builder]()

    completion_time: Annotated[Time | None, Field(alias="completionTime")] = None
    "completionTime tracks the time at which the Responder stopped processing the eviction request. Completion means that the responders has either fully or partially completed the eviction process, which may have resulted in target eviction (e.g. pod termination). It should reflect the present time when set. This field becomes immutable once set."
    expected_completion_time: Annotated[Time | None, Field(alias="expectedCompletionTime")] = None
    "expectedCompletionTime is the time at which the eviction process step is expected to end for the responder. The time cannot be set to the past. May be omitted if no estimate can be made."
    heartbeat_time: Annotated[Time | None, Field(alias="heartbeatTime")] = None
    "heartbeatTime is the last time at which the eviction process was reported to be in progress by the responder. It should reflect the present time when set. Responders should avoid heartbeats more frequent than 20 seconds to avoid overloading the control-plane."
    message: str | None = None
    "message provides human-readable details about the state of the responder and the eviction process. Maximum length is 4000 characters."
    name: str
    'name allows you to identify the responder reacting to the Eviction.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field is initialized by Kubernetes and must be unique for each responder. This field is required.'
    start_time: Annotated[Time | None, Field(alias="startTime")] = None
    "startTime tracks the time at which this responder was designated as active and should start processing the eviction request. It should reflect the present time when set. This field is initialized by Kubernetes when this responder becomes active. This field becomes immutable once set."


class TargetResponder(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["TargetResponder"]:
            return TargetResponder

        def build(self) -> "TargetResponder":
            return TargetResponder(**self._attrs)

        def name(self, value: str, /) -> Self:
            'name allows you to identify the responder reacting to the Eviction.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field must be unique for each responder. This field is required.'
            return self._set("name", value)

        def priority(self, value: int, /) -> Self:
            "priority for this responder. Higher priorities are selected first by the evictionrequest-controller. If there are responders with the same priority, the responder whose domain name comes first in the alphabetical higher domain order, will be picked. This means that the top domain labels are compared alphabetically first, followed by the lower domain labels. The key is compared last.\n\nThe responder that is the managing controller of the pod should set the value of this field to 10000 to allow both for preemption or fallback registration by other responders.\n\nThe minimum value is 0 and the maximum value is 100000. The interval 0-999 is reserved for responders with *.k8s.io suffix. This field is required and immutable."
            return self._set("priority", value)

        def state(self, value: str, /) -> Self:
            "state specifies a state that is assigned by the evictionrequest-controller. Responders should observe this state in order to navigate their lifecycle. - Inactive means that the responder should not yet process this eviction request. - Active means that the responder is either running or expected to start soon.\n  Also, startTime has been set in the ResponderStatus by the evictionrequest-controller.\n\n  An active responder should currently interact with the eviction process by updating\n  .status.responders, where .name is the active responder name. ResponderStatus fields\n  should be periodically updated to indicate the progress or completion of the eviction process.\n  If .status.responders[].heartbeatTime field is not updated within the heartbeat deadline defined\n  by the Eviction API (currently 20 minutes), the eviction is passed over to the next responder\n\t with a lower priority. Only one responder can be active at a time.\n- Interrupted means that the responder has failed to start or failed to update\n  heartbeatTime in ResponderStatus in a timely manner.\n- Canceled means that the responder has been canceled. In other words, there\tis no\n  EvictionRequest with the same target and Eviction intent in .spec.intent.\n- Completed means that the responder has successfully completed and set completionTime\n  in ResponderStatus.\n\nPlease refer to the ResponderStatus in .status.responders for more details on each responder."
            return self._set("state", value)

    class BuilderContext(BuilderContextBase["TargetResponder.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = TargetResponder.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for TargetResponder."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["TargetResponder", Builder]:
        return GenericListBuilder[TargetResponder, TargetResponder.Builder]()

    name: str
    'name allows you to identify the responder reacting to the Eviction.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). This field must be unique for each responder. This field is required.'
    priority: int
    "priority for this responder. Higher priorities are selected first by the evictionrequest-controller. If there are responders with the same priority, the responder whose domain name comes first in the alphabetical higher domain order, will be picked. This means that the top domain labels are compared alphabetically first, followed by the lower domain labels. The key is compared last.\n\nThe responder that is the managing controller of the pod should set the value of this field to 10000 to allow both for preemption or fallback registration by other responders.\n\nThe minimum value is 0 and the maximum value is 100000. The interval 0-999 is reserved for responders with *.k8s.io suffix. This field is required and immutable."
    state: str
    "state specifies a state that is assigned by the evictionrequest-controller. Responders should observe this state in order to navigate their lifecycle. - Inactive means that the responder should not yet process this eviction request. - Active means that the responder is either running or expected to start soon.\n  Also, startTime has been set in the ResponderStatus by the evictionrequest-controller.\n\n  An active responder should currently interact with the eviction process by updating\n  .status.responders, where .name is the active responder name. ResponderStatus fields\n  should be periodically updated to indicate the progress or completion of the eviction process.\n  If .status.responders[].heartbeatTime field is not updated within the heartbeat deadline defined\n  by the Eviction API (currently 20 minutes), the eviction is passed over to the next responder\n\t with a lower priority. Only one responder can be active at a time.\n- Interrupted means that the responder has failed to start or failed to update\n  heartbeatTime in ResponderStatus in a timely manner.\n- Canceled means that the responder has been canceled. In other words, there\tis no\n  EvictionRequest with the same target and Eviction intent in .spec.intent.\n- Completed means that the responder has successfully completed and set completionTime\n  in ResponderStatus.\n\nPlease refer to the ResponderStatus in .status.responders for more details on each responder."


class EvictionRequestSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionRequestSpec"]:
            return EvictionRequestSpec

        def build(self) -> "EvictionRequestSpec":
            return EvictionRequestSpec(**self._attrs)

        def intent(self, value: str, /) -> Self:
            "intent specifies the action that should be taken for the specified target.\n\n- Eviction means that the requester is interested in the eviction of the target. - Withdrawn means that the requester is no longer interested in the eviction of the target.\n  If all requesters\u0027 intents are withdrawn for a common target, the eviction will be canceled.\n  Cancellation consequences:\n  - Inactive responders will never run.\n  - Active responders are expected to cancel the eviction.\n  - Completed or Interrupted responders should not take any action."
            return self._set("intent", value)

        def requester(self, value: str, /) -> Self:
            'requester allows you to identify the entity, that requested the eviction of the target.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). Domain names *.k8s.io and *.kubernetes.io are reserved. This field is required and immutable.'
            return self._set("requester", value)

        @overload
        def target(
            self, value_or_callback: EvictionRequestTarget, /
        ) -> "EvictionRequestSpec.Builder": ...

        @overload
        def target(
            self,
            value_or_callback: Callable[
                [EvictionRequestTarget.Builder],
                EvictionRequestTarget.Builder | EvictionRequestTarget,
            ],
            /,
        ) -> "EvictionRequestSpec.Builder": ...

        @overload
        def target(
            self, value_or_callback: Never = ...
        ) -> "EvictionRequestTarget.BuilderContext": ...

        def target(self, value_or_callback=UNSET, /):
            "target contains a reference to an object (e.g. a pod) that should be evicted. This field is required and immutable."
            if self._in_context and value_or_callback is UNSET:
                context = EvictionRequestTarget.BuilderContext()
                context._parent_builder = self
                context._field_name = "target"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionRequestTarget.builder())
                if isinstance(output, EvictionRequestTarget.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target", value)

    class BuilderContext(BuilderContextBase["EvictionRequestSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionRequestSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionRequestSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionRequestSpec", Builder]:
        return GenericListBuilder[EvictionRequestSpec, EvictionRequestSpec.Builder]()

    intent: str
    "intent specifies the action that should be taken for the specified target.\n\n- Eviction means that the requester is interested in the eviction of the target. - Withdrawn means that the requester is no longer interested in the eviction of the target.\n  If all requesters\u0027 intents are withdrawn for a common target, the eviction will be canceled.\n  Cancellation consequences:\n  - Inactive responders will never run.\n  - Active responders are expected to cancel the eviction.\n  - Completed or Interrupted responders should not take any action."
    requester: str
    'requester allows you to identify the entity, that requested the eviction of the target.\n\nIt must be a valid domain-prefixed key (such as "acme.io/foo"). Domain names *.k8s.io and *.kubernetes.io are reserved. This field is required and immutable.'
    target: EvictionRequestTarget
    "target contains a reference to an object (e.g. a pod) that should be evicted. This field is required and immutable."


class EvictionSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionSpec"]:
            return EvictionSpec

        def build(self) -> "EvictionSpec":
            return EvictionSpec(**self._attrs)

        @overload
        def target(self, value_or_callback: EvictionTarget, /) -> "EvictionSpec.Builder": ...

        @overload
        def target(
            self,
            value_or_callback: Callable[
                [EvictionTarget.Builder], EvictionTarget.Builder | EvictionTarget
            ],
            /,
        ) -> "EvictionSpec.Builder": ...

        @overload
        def target(self, value_or_callback: Never = ...) -> "EvictionTarget.BuilderContext": ...

        def target(self, value_or_callback=UNSET, /):
            "target contains a reference to an object (e.g. a pod) that should be evicted. This field is required and immutable."
            if self._in_context and value_or_callback is UNSET:
                context = EvictionTarget.BuilderContext()
                context._parent_builder = self
                context._field_name = "target"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionTarget.builder())
                if isinstance(output, EvictionTarget.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("target", value)

    class BuilderContext(BuilderContextBase["EvictionSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionSpec", Builder]:
        return GenericListBuilder[EvictionSpec, EvictionSpec.Builder]()

    target: EvictionTarget
    "target contains a reference to an object (e.g. a pod) that should be evicted. This field is required and immutable."


class EvictionStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionStatus"]:
            return EvictionStatus

        def build(self) -> "EvictionStatus":
            return EvictionStatus(**self._attrs)

        @overload
        def conditions(
            self, value_or_callback: list[Condition] | None, /
        ) -> "EvictionStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Condition, Condition.Builder]],
                GenericListBuilder[Condition, Condition.Builder] | list[Condition],
            ],
            /,
        ) -> "EvictionStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Condition.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "conditions contain information about the eviction request.\n\nEviction specific conditions are: TargetEvicted or Failed (managed by evictionrequest-controller). - Failed means that the eviction request is no longer being processed\n  by any eviction responder. This can happen if the request is canceled or if no responder\n  managed to evict the target (e.g. terminate or delete a pod).\n- TargetEvicted means that the target has been evicted (e.g. a pod has been terminated or deleted).\n\n\tThe maximum length of the conditions list is 100."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Condition.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Condition.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "observedGeneration is Eviction\u0027s .metadata.generation observed by the evictionrequest-controller. The observed generation value cannot be negative and can only be incremented. The minimum value is 1. This field is managed by evictionrequest-controller."
            return self._set("observed_generation", value)

        @overload
        def requesters(
            self, value_or_callback: list[Requester] | None, /
        ) -> "EvictionStatus.Builder": ...

        @overload
        def requesters(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[Requester, Requester.Builder]],
                GenericListBuilder[Requester, Requester.Builder] | list[Requester],
            ],
            /,
        ) -> "EvictionStatus.Builder": ...

        @overload
        def requesters(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[Requester.Builder]: ...

        def requesters(self, value_or_callback=UNSET, /):
            "requesters allow you to identify the entities, that requested the eviction of the target. If all the requesters withdraw their eviction intent, the eviction will be canceled.\n\nThe maximum length of the requesters list is 100. If this limit is exceeded, requesters with Withdrawn intent should be dropped first."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[Requester.Builder]()
                context._parent_builder = self
                context._field_name = "requesters"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Requester.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("requesters", value)

        @overload
        def responders(
            self, value_or_callback: list[ResponderStatus] | None, /
        ) -> "EvictionStatus.Builder": ...

        @overload
        def responders(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[ResponderStatus, ResponderStatus.Builder]],
                GenericListBuilder[ResponderStatus, ResponderStatus.Builder]
                | list[ResponderStatus],
            ],
            /,
        ) -> "EvictionStatus.Builder": ...

        @overload
        def responders(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ResponderStatus.Builder]: ...

        def responders(self, value_or_callback=UNSET, /):
            "responders represents the eviction process status of each declared responder.\n\nThe responder list should be the same length and have the same .name fields as .status.targetResponders. Only responders with .name that have Active state in .targetResponders[].state should be updated and can be mutated. First initialization of the list is allowed.\n\nEach ResponderStatus is initialized by evictionrequest-controller and then managed by the designated responder."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ResponderStatus.Builder]()
                context._parent_builder = self
                context._field_name = "responders"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ResponderStatus.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("responders", value)

        @overload
        def target_responders(
            self, value_or_callback: list[TargetResponder] | None, /
        ) -> "EvictionStatus.Builder": ...

        @overload
        def target_responders(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[TargetResponder, TargetResponder.Builder]],
                GenericListBuilder[TargetResponder, TargetResponder.Builder]
                | list[TargetResponder],
            ],
            /,
        ) -> "EvictionStatus.Builder": ...

        @overload
        def target_responders(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[TargetResponder.Builder]: ...

        def target_responders(self, value_or_callback=UNSET, /):
            "targetResponders reference responders that should eventually respond to this eviction to help with the graceful eviction of a target. These responders are selected sequentially, according to their specified priority by setting the Active state to the TargetResponder .state field. The maximum number of active responders allowed is 1. Eventually each responder can end up in an Interrupted, Canceled or, Completed state. Responders should observe these states in order to navigate their lifecycle.\n\nIf the target is a pod, the field is populated from Pod\u0027s .spec.evictionResponders. Default responders may be added to the list according to the target.\n\nDefault responders: - imperative-eviction.k8s.io/evictor responder with a priority of 100 is added to the list if the\n  target is a pod. It will call the imperative Eviction API (pods/\u003cname\u003e/eviction subresource).\n  This call may not succeed due to PodDisruptionBudgets, which may block the pod termination.\n  It will update the responder message and try again with a backoff.\n\nThe maximum length of the responders list is 11. The length and keys of the list cannot change once set. This field is managed by evictionrequest-controller."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[TargetResponder.Builder]()
                context._parent_builder = self
                context._field_name = "target_responders"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(TargetResponder.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("target_responders", value)

    class BuilderContext(BuilderContextBase["EvictionStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionStatus", Builder]:
        return GenericListBuilder[EvictionStatus, EvictionStatus.Builder]()

    conditions: list[Condition] | None = None
    "conditions contain information about the eviction request.\n\nEviction specific conditions are: TargetEvicted or Failed (managed by evictionrequest-controller). - Failed means that the eviction request is no longer being processed\n  by any eviction responder. This can happen if the request is canceled or if no responder\n  managed to evict the target (e.g. terminate or delete a pod).\n- TargetEvicted means that the target has been evicted (e.g. a pod has been terminated or deleted).\n\n\tThe maximum length of the conditions list is 100."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "observedGeneration is Eviction\u0027s .metadata.generation observed by the evictionrequest-controller. The observed generation value cannot be negative and can only be incremented. The minimum value is 1. This field is managed by evictionrequest-controller."
    requesters: list[Requester] | None = None
    "requesters allow you to identify the entities, that requested the eviction of the target. If all the requesters withdraw their eviction intent, the eviction will be canceled.\n\nThe maximum length of the requesters list is 100. If this limit is exceeded, requesters with Withdrawn intent should be dropped first."
    responders: list[ResponderStatus] | None = None
    "responders represents the eviction process status of each declared responder.\n\nThe responder list should be the same length and have the same .name fields as .status.targetResponders. Only responders with .name that have Active state in .targetResponders[].state should be updated and can be mutated. First initialization of the list is allowed.\n\nEach ResponderStatus is initialized by evictionrequest-controller and then managed by the designated responder."
    target_responders: Annotated[list[TargetResponder] | None, Field(alias="targetResponders")] = (
        None
    )
    "targetResponders reference responders that should eventually respond to this eviction to help with the graceful eviction of a target. These responders are selected sequentially, according to their specified priority by setting the Active state to the TargetResponder .state field. The maximum number of active responders allowed is 1. Eventually each responder can end up in an Interrupted, Canceled or, Completed state. Responders should observe these states in order to navigate their lifecycle.\n\nIf the target is a pod, the field is populated from Pod\u0027s .spec.evictionResponders. Default responders may be added to the list according to the target.\n\nDefault responders: - imperative-eviction.k8s.io/evictor responder with a priority of 100 is added to the list if the\n  target is a pod. It will call the imperative Eviction API (pods/\u003cname\u003e/eviction subresource).\n  This call may not succeed due to PodDisruptionBudgets, which may block the pod termination.\n  It will update the responder message and try again with a backoff.\n\nThe maximum length of the responders list is 11. The length and keys of the list cannot change once set. This field is managed by evictionrequest-controller."


class Eviction(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "evictions",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Eviction"]:
            return Eviction

        def build(self) -> "Eviction":
            return Eviction(**self._attrs)

        def api_version(self, value: Literal["lifecycle.k8s.io/v1alpha1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Eviction"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Eviction.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Eviction.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            'metadata is the standard object metadata; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata. .metadata.name set by the evictionrequest-controller is purely informative and subject to change. .spec.target field should be used to identify the target precisesly.\n\nThe requester and responder names will be used as label keys and added to the labels of the eviction in one of the following formats: 1. acme.io/foo: "requester" 2. acme.io/foo: "responder" 3. acme.io/foo: "requester-responder"\n\nPlease see EvictionParticipantRole for available role label values.'
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: EvictionSpec, /) -> "Eviction.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [EvictionSpec.Builder], EvictionSpec.Builder | EvictionSpec
            ],
            /,
        ) -> "Eviction.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "EvictionSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the eviction specification. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = EvictionSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionSpec.builder())
                if isinstance(output, EvictionSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: EvictionStatus | None, /) -> "Eviction.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [EvictionStatus.Builder], EvictionStatus.Builder | EvictionStatus
            ],
            /,
        ) -> "Eviction.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "EvictionStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the most recently observed status of the eviction. Populated by responders and evictionrequest-controller. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = EvictionStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionStatus.builder())
                if isinstance(output, EvictionStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Eviction.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Eviction.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Eviction."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Eviction", Builder]:
        return GenericListBuilder[Eviction, Eviction.Builder]()

    api_version: Annotated[Literal["lifecycle.k8s.io/v1alpha1"], Field(alias="apiVersion")] = (
        "lifecycle.k8s.io/v1alpha1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Eviction"] = "Eviction"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    'metadata is the standard object metadata; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata. .metadata.name set by the evictionrequest-controller is purely informative and subject to change. .spec.target field should be used to identify the target precisesly.\n\nThe requester and responder names will be used as label keys and added to the labels of the eviction in one of the following formats: 1. acme.io/foo: "requester" 2. acme.io/foo: "responder" 3. acme.io/foo: "requester-responder"\n\nPlease see EvictionParticipantRole for available role label values.'
    spec: EvictionSpec
    "spec defines the eviction specification. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: EvictionStatus | None = None
    "status represents the most recently observed status of the eviction. Populated by responders and evictionrequest-controller. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


EvictionList = ResourceList[Eviction]


class EvictionRequest(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "evictionrequests",
        "scope": "Namespaced",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["EvictionRequest"]:
            return EvictionRequest

        def build(self) -> "EvictionRequest":
            return EvictionRequest(**self._attrs)

        def api_version(self, value: Literal["lifecycle.k8s.io/v1alpha1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["EvictionRequest"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "EvictionRequest.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "EvictionRequest.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "metadata is the standard object metadata; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata."
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: EvictionRequestSpec, /) -> "EvictionRequest.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [EvictionRequestSpec.Builder],
                EvictionRequestSpec.Builder | EvictionRequestSpec,
            ],
            /,
        ) -> "EvictionRequest.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "EvictionRequestSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            "spec defines the eviction request specification. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = EvictionRequestSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionRequestSpec.builder())
                if isinstance(output, EvictionRequestSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: EvictionRequestStatus | None, /
        ) -> "EvictionRequest.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [EvictionRequestStatus.Builder],
                EvictionRequestStatus.Builder | EvictionRequestStatus,
            ],
            /,
        ) -> "EvictionRequest.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "EvictionRequestStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            "status represents the most recently observed status of the eviction request. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
            if self._in_context and value_or_callback is UNSET:
                context = EvictionRequestStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(EvictionRequestStatus.builder())
                if isinstance(output, EvictionRequestStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["EvictionRequest.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = EvictionRequest.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for EvictionRequest."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["EvictionRequest", Builder]:
        return GenericListBuilder[EvictionRequest, EvictionRequest.Builder]()

    api_version: Annotated[Literal["lifecycle.k8s.io/v1alpha1"], Field(alias="apiVersion")] = (
        "lifecycle.k8s.io/v1alpha1"
    )
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["EvictionRequest"] = "EvictionRequest"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "metadata is the standard object metadata; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata."
    spec: EvictionRequestSpec
    "spec defines the eviction request specification. https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"
    status: EvictionRequestStatus | None = None
    "status represents the most recently observed status of the eviction request. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status"


EvictionRequestList = ResourceList[EvictionRequest]
