# Generated by cloudcoil-model-codegen v0.7.0
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import (
    ObjectMeta,
    Quantity,
)
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource, ResourceList
from pydantic import Field

from ..core import v1


class Overhead(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Overhead"]:
            return Overhead

        def build(self) -> "Overhead":
            return Overhead(**self._attrs)

        def pod_fixed(self, value: dict[str, Quantity] | None, /) -> Self:
            "podFixed represents the fixed resource overhead associated with running a pod."
            return self._set("pod_fixed", value)

    class BuilderContext(BuilderContextBase["Overhead.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Overhead.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Overhead."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Overhead", Builder]:
        return GenericListBuilder[Overhead, Overhead.Builder]()

    pod_fixed: Annotated[dict[str, Quantity] | None, Field(alias="podFixed")] = None
    "podFixed represents the fixed resource overhead associated with running a pod."


class Scheduling(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Scheduling"]:
            return Scheduling

        def build(self) -> "Scheduling":
            return Scheduling(**self._attrs)

        def node_selector(self, value: dict[str, str] | None, /) -> Self:
            "nodeSelector lists labels that must be present on nodes that support this RuntimeClass. Pods using this RuntimeClass can only be scheduled to a node matched by this selector. The RuntimeClass nodeSelector is merged with a pod\u0027s existing nodeSelector. Any conflicts will cause the pod to be rejected in admission."
            return self._set("node_selector", value)

        @overload
        def tolerations(
            self, value_or_callback: list[v1.Toleration] | None, /
        ) -> "Scheduling.Builder": ...

        @overload
        def tolerations(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[v1.Toleration, v1.Toleration.Builder]],
                GenericListBuilder[v1.Toleration, v1.Toleration.Builder] | list[v1.Toleration],
            ],
            /,
        ) -> "Scheduling.Builder": ...

        @overload
        def tolerations(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[v1.Toleration.Builder]: ...

        def tolerations(self, value_or_callback=UNSET, /):
            "tolerations are appended (excluding duplicates) to pods running with this RuntimeClass during admission, effectively unioning the set of nodes tolerated by the pod and the RuntimeClass."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[v1.Toleration.Builder]()
                context._parent_builder = self
                context._field_name = "tolerations"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(v1.Toleration.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tolerations", value)

    class BuilderContext(BuilderContextBase["Scheduling.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Scheduling.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Scheduling."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Scheduling", Builder]:
        return GenericListBuilder[Scheduling, Scheduling.Builder]()

    node_selector: Annotated[dict[str, str] | None, Field(alias="nodeSelector")] = None
    "nodeSelector lists labels that must be present on nodes that support this RuntimeClass. Pods using this RuntimeClass can only be scheduled to a node matched by this selector. The RuntimeClass nodeSelector is merged with a pod\u0027s existing nodeSelector. Any conflicts will cause the pod to be rejected in admission."
    tolerations: list[v1.Toleration] | None = None
    "tolerations are appended (excluding duplicates) to pods running with this RuntimeClass during admission, effectively unioning the set of nodes tolerated by the pod and the RuntimeClass."


class RuntimeClass(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "runtimeclasses",
        "scope": "Cluster",
        "status": False,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["RuntimeClass"]:
            return RuntimeClass

        def build(self) -> "RuntimeClass":
            return RuntimeClass(**self._attrs)

        def api_version(self, value: Literal["node.k8s.io/v1"], /) -> Self:
            "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def handler(self, value: str, /) -> Self:
            'handler specifies the underlying runtime and configuration that the CRI implementation will use to handle pods of this class. The possible values are specific to the node \u0026 CRI configuration.  It is assumed that all handlers are available on every node, and handlers of the same name are equivalent on every node. For example, a handler called "runc" might specify that the runc OCI runtime (using native Linux containers) will be used to run the containers in a pod. The Handler must be lowercase, conform to the DNS Label (RFC 1123) requirements, and is immutable.'
            return self._set("handler", value)

        def kind(self, value: Literal["RuntimeClass"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "RuntimeClass.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "RuntimeClass.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            "More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def overhead(self, value_or_callback: Overhead | None, /) -> "RuntimeClass.Builder": ...

        @overload
        def overhead(
            self,
            value_or_callback: Callable[[Overhead.Builder], Overhead.Builder | Overhead],
            /,
        ) -> "RuntimeClass.Builder": ...

        @overload
        def overhead(self, value_or_callback: Never = ...) -> "Overhead.BuilderContext": ...

        def overhead(self, value_or_callback=UNSET, /):
            "overhead represents the resource overhead associated with running a pod for a given RuntimeClass. For more details, see\n https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/"
            if self._in_context and value_or_callback is UNSET:
                context = Overhead.BuilderContext()
                context._parent_builder = self
                context._field_name = "overhead"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Overhead.builder())
                if isinstance(output, Overhead.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("overhead", value)

        @overload
        def scheduling(self, value_or_callback: Scheduling | None, /) -> "RuntimeClass.Builder": ...

        @overload
        def scheduling(
            self,
            value_or_callback: Callable[[Scheduling.Builder], Scheduling.Builder | Scheduling],
            /,
        ) -> "RuntimeClass.Builder": ...

        @overload
        def scheduling(self, value_or_callback: Never = ...) -> "Scheduling.BuilderContext": ...

        def scheduling(self, value_or_callback=UNSET, /):
            "scheduling holds the scheduling constraints to ensure that pods running with this RuntimeClass are scheduled to nodes that support it. If scheduling is nil, this RuntimeClass is assumed to be supported by all nodes."
            if self._in_context and value_or_callback is UNSET:
                context = Scheduling.BuilderContext()
                context._parent_builder = self
                context._field_name = "scheduling"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(Scheduling.builder())
                if isinstance(output, Scheduling.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("scheduling", value)

    class BuilderContext(BuilderContextBase["RuntimeClass.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = RuntimeClass.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for RuntimeClass."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["RuntimeClass", Builder]:
        return GenericListBuilder[RuntimeClass, RuntimeClass.Builder]()

    api_version: Annotated[Literal["node.k8s.io/v1"], Field(alias="apiVersion")] = "node.k8s.io/v1"
    "APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    handler: str
    'handler specifies the underlying runtime and configuration that the CRI implementation will use to handle pods of this class. The possible values are specific to the node \u0026 CRI configuration.  It is assumed that all handlers are available on every node, and handlers of the same name are equivalent on every node. For example, a handler called "runc" might specify that the runc OCI runtime (using native Linux containers) will be used to run the containers in a pod. The Handler must be lowercase, conform to the DNS Label (RFC 1123) requirements, and is immutable.'
    kind: Literal["RuntimeClass"] = "RuntimeClass"
    "Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    "More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata"
    overhead: Overhead | None = None
    "overhead represents the resource overhead associated with running a pod for a given RuntimeClass. For more details, see\n https://kubernetes.io/docs/concepts/scheduling-eviction/pod-overhead/"
    scheduling: Scheduling | None = None
    "scheduling holds the scheduling constraints to ensure that pods running with this RuntimeClass are scheduled to nodes that support it. If scheduling is nil, this RuntimeClass is assumed to be supported by all nodes."


RuntimeClassList = ResourceList[RuntimeClass]
